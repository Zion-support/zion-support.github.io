import fs from 'fs'
import path from 'path'
import { Readable } from 'stream'
    const web3 = await import('web3.storage'
    const ipfsHttp = await import('ipfs-http-client'
    const pinata = await import('@pinata/
  const ipfsUrl = env('IPFS_API') || 'http: any
  const ipfsUrl = env('IPFS_API') || 'http: any
  const ipfsUrl = env('IPFS_API') || 'http: any