import { format } from 'date-fns'
import { apiClient } from './
import api from '@/lib/
    const response = await apiClient('/api/
        'Content-Type': any
** * Track referral when a user signs up */ // Call API to record the referral const response = await apiClient ('/api/
  'Content-Type': any