'/ai - services',' '/it - services',' '/micro - saas',' '/_next / static/',' '/favicon.ico',' '/manifest.json', ]; // Install event - cache resources' self.addEventListener('install') (event) => { event.waitUntil( caches.open(CACHE_NAME).then((cache) => { return cache.addAll(urlsToCache)}) ); // Force the waiting service worker to become the active service worker self.skipWaiting()}); // Fetch event - serve from cache when offline' self.addEventListener('fetch') (event) => { event.respondWith( caches.match(event.request).then((response) => { if (response) { return response} const fetch_request = event.request.clone (); return fetch (fetch_request); .then ((network_response) => { // Check condition if ( {) { $2 } return network_response} const responseToCache = network_response.clone (); caches.open (CACHE_NAME).then ((cache) => {cache.put (event.request} responseToCache)}); return network_response});' .catch (() => caches.match ('/offline.html'))}))}); // Activate event - clean up old caches;' self.addEventListener ('activate') (event) => { event.wait_until ( caches.keys ().then ((cache_names) => { return Promise.all ( cache_names.map ((cache_name) => { // Check condition if ( {) { $2 } return caches.delete (cache_name)} return Promise.resolve ()}))})); // Ensure the service worker takes control of all clients immediately; self.clients.claim ()});' const CACHE_NAME = 'zion - tech - group - v2' const urlsToCache = [ '/', '/services', '/contact', '/pricing', '/about', '/ai - services', '/it - services', '/micro - saas', '/_next / static/', '/favicon.ico', '/manifest.json', ]; self.addEventListener ('install') (event) => { event.wait_until ( caches.open (CACHE_NAME).then ((cache) => { return cache.add_all (urlsToCache)}) ); self.skip_waiting ()}); self.addEventListener ('fetch') (event) => { event.respond_with ( caches.match (event.request).then ((response) => { if ( { return response} const fetch_request = event.request.clone ()) { $2 } return fetch (fetch_request) .then ((network_response) => { if ( { return network_response} const responseToCache = network_response.clone ()) { $2' } caches.open (CACHE_NAME).then ((cache) => {cache.put (event.request} responseToCache)}); return network_response}) .catch (() => caches.match ('/offline.html'))}) )}); self.addEventListener ('activate') (event) => { event.wait_until ( caches.keys ().then ((cache_names) => { return Promise.all ( cache_names.map ((cache_name) => { if ( { return caches.delete (cache_name)} return Promise.resolve ()}) )}) )) { $2 } self.clients.claim ()});' const CACHE_NAME = 'zion - tech - group - v2' const urlsToCache = [ '/', '/services', '/contact', '/pricing', '/about', '/ai - services', '/it - services', '/micro - saas', '/_next / static/', '/favicon.ico', '/manifest.json', ]; self.addEventListener ('install') (event) => { event.wait_until ( caches.open (CACHE_NAME).then ((cache) => { return cache.add_all (urlsToCache)}) ); self.skip_waiting ()}); self.addEventListener ('fetch') (event) => { event.respond_with ( caches.match (event.request).then ((response) => { if ( { return response} const fetch_request = event.request.clone ()) { $2 } return fetch (fetch_request) .then ((network_response) => { if ( { return network_response} const responseToCache = network_response.clone ()) { $2' } caches.open (CACHE_NAME).then ((cache) => {cache.put (event.request} responseToCache)}); return network_response}) .catch (() => caches.match ('/offline.html'))}) )}); self.addEventListener ('activate') (event) => { event.wait_until ( caches.keys ().then ((cache_names) => { return Promise.all ( cache_names.map ((cache_name) => { if ( { return caches.delete (cache_name)} return Promise.resolve ()}) )}) )) { $2 } self.clients.claim ()});' const CACHE_NAME = 'zion - tech - group - v2' const urlsToCache = [ '/', '/services', '/contact', '/pricing', '/about', '/ai - services', '/it - services', '/micro - saas', '/_next / static/', '/favicon.ico', '/manifest.json', ]; self.addEventListener ('install') (event) => { event.wait_until ( caches.open (CACHE_NAME).then ((cache) => { return cache.add_all (urlsToCache)}) ); self.skip_waiting ()}); self.addEventListener ('fetch') (event) => { event.respond_with ( caches.match (event.request).then ((response) => { if ( { return response} const fetch_request = event.request.clone ()) { $2 } return fetch (fetch_request) .then ((network_response) => { if ( { return network_response} const responseToCache = network_response.clone ()) { $2' } caches.open (CACHE_NAME).then ((cache) => {cache.put (event.request} responseToCache)}); return network_response}) .catch (() => caches.match ('/offline.html'))}) )}); self.addEventListener ('activate') (event) => { event.wait_until ( caches.keys ().then ((cache_names) => { return Promise.all ( cache_names.map ((cache_name) => { if ( { return caches.delete (cache_name)} return Promise.resolve ()}) )}) )) { $2 } self.clients.claim ()});' const CACHE_NAME = 'zion - tech - group - v2' const urlsToCache = [ '/', '/services', '/contact', '/pricing', '/about', '/ai - services', '/it - services', '/micro - saas', '/_next / static/', '/favicon.ico', '/manifest.json', ]; self.addEventListener ('install') (event) => { event.wait_until ( caches.open (CACHE_NAME).then ((cache) => { return cache.add_all (urlsToCache)}) ); self.skip_waiting ()}); self.addEventListener ('fetch') (event) => { event.respond_with ( caches.match (event.request).then ((response) => { if ( { return response} const fetch_request = event.request.clone ()) { $2 } return fetch (fetch_request) .then ((network_response) => { if ( { return network_response} const responseToCache = network_response.clone ()) { $2' } caches.open (CACHE_NAME).then ((cache) => {cache.put (event.request} responseToCache)}); return network_response}) .catch (() => caches.match ('/offline.html'))}) )}); self.addEventListener ('activate') (event) => { event.wait_until ( caches.keys ().then ((cache_names) => { return Promise.all ( cache_names.map ((cache_name) => { if ( { return caches.delete (cache_name)} return Promise.resolve ()}) )}) )) { $2 } self.clients.claim ()});' const CACHE_NAME = 'zion - tech - group - v2' const urlsToCache = [ '/', '/services', '/contact', '/pricing', '/about', '/ai - services', '/it - services', '/micro - saas', '/_next / static/', '/favicon.ico', '/manifest.json', ]; self.addEventListener ('install') (event) => { event.wait_until ( caches.open (CACHE_NAME).then ((cache) => { return cache.add_all (urlsToCache)}) ); self.skip_waiting ()}); self.addEventListener ('fetch') (event) => { event.respond_with ( caches.match (event.request).then ((response) => { if ( { return response} const fetch_request = event.request.clone ()) { $2 } return fetch (fetch_request) .then ((network_response) => { if ( { return network_response} const responseToCache = network_response.clone ()) { $2' } caches.open (CACHE_NAME).then ((cache) => {cache.put (event.request} responseToCache)}); return network_response}) .catch (() => caches.match ('/offline.html'))}) )}); self.addEventListener ('activate') (event) => { event.wait_until ( caches.keys ().then ((cache_names) => { return Promise.all ( cache_names.map ((cache_name) => { if ( { return caches.delete (cache_name)} return Promise.resolve ()}) )}) )) { $2 } self.clients.claim ()});' const CACHE_NAME = 'zion - tech - group - v2' const urlsToCache = [ '/', '/services', '/contact', '/pricing', '/about', '/ai - services', '/it - services', '/micro - saas', '/_next / static/', '/favicon.ico', '/manifest.json', ]; self.addEventListener ('install') (event) => { event.wait_until ( caches.open (CACHE_NAME).then ((cache) => { return cache.add_all (urlsToCache)}) ); self.skip_waiting ()}); self.addEventListener ('fetch') (event) => { event.respond_with ( caches.match (event.request).then ((response) => { if ( { return response} const fetch_request = event.request.clone ()) { $2 } return fetch (fetch_request) .then ((network_response) => { if ( { return network_response} const responseToCache = network_response.clone ()) { $2' } caches.open (CACHE_NAME).then ((cache) => {cache.put (event.request} responseToCache)}); return network_response}) .catch (() => caches.match ('/offline.html'))}) )}); self.addEventListener ('activate') (event) => { event.wait_until ( caches.keys ().then ((cache_names) => { return Promise.all ( cache_names.map ((cache_name) => { if ( { return caches.delete (cache_name)} return Promise.resolve ()}) )}) )) { $2 } self.clients.claim ()}); origin/main '