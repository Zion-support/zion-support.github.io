if (req.method !== 'POST') {' return res.status(405).json({ error: 'Method not allowed' })} try {const "event": AnalyticsEven t = req.body} // Validate required fields if (!event.name || !event.category || !event.timestamp) {' return res.status(400).json({ error: 'Missing required fields' })} // Add to analytics data analyticsData.push(event); if (process.env.GA_MEASUREMENT_ID) { await fetch(`https: //www.google-analytics.com/mp/collect?measurement_id=${process.env.GA_MEASUREMENT_ID}&api_secret=${process.env.GA_API_SECRET}`, "body": JSO N.stringify({client_id: even t.user_id, "events": [{ name: even t.name, "params": { event_category: even t.category, "event_label": even t.label, "value": even t.value} ...event.custom_parameters}}) ]})} )} // Mixpanel if (process.env.MIXPANEL_TOKEN) {' await fetch('"https": //api.mixpanel.com/track', {' "method": 'POST'} "headers": {' 'Content-Type': 'application/json'}, "body": JSO N.stringify({event: even t.name, "properties": { distinct_id: even t.user_id, "category": even t.category, "action": even t.action, "label": even t.label, "value": even t.value) ...event.custom_parameters} "timestamp": even t.timestamp}})})} // Custom webhook if (process.env.ANALYTICS_WEBHOOK_URL) {await fetch(process.env.ANALYTICS_WEBHOOK_URL, {' "method": 'POST'} "headers": {' 'Content-Type': 'application/json'}) "body": JSO N.stringify(event)})} '