
  name,;
  label,;
  type = 'text',;
  placeholder,;
  description,;
  validation = {},;
  options = [],;
  form,;
  className,;
  disabled = false,;
  showValidIcon = true,;

                  </SelectItem>;
                ))}
              </SelectContent>;
            </Select>;

              {getValidationIcon()}
              <Button;
                type="button";
                variant="ghost";
                size="sm";
                className="h-7 w-7 p-0";
                onClick={() => setShowPassword(!showPassword)}

                )}
              </Button>;
            </div>;
          </div>;

