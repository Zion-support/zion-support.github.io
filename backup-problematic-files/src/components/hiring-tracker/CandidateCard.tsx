import { useState } from "react"
import { Draggable } from "@/lib/
import Link from "next/
import { JobApplication } from "@/types/
import { Card, CardContent } from "@/components/ui/
import { Avatar as AvatarPrimitive } from "@/components/ui/
import { Button } from "@/components/ui/
import { Textarea } from "@/components/ui/
  DropdownMenuTrigger} from "@/components/ui/
import { ScoreBadge } from "@/components/jobs/applications/
import { toast } from "@/hooks/
import { HireConfirmationModal } from "./
  /* Candidate Header */

/Avatars are usually not LCP />) : (
</h4> </p> </div> 
> 
> </div> 
<Button variant="outline" size="sm" className="flex-1" asChild > </Link> </Button> <Button variant="outline" size="sm" className="flex-1" asChild > <FileText className="h-3 w-3 mr-1" /> Resume </Link>) : (<span> <FileText className="h-3 w-3 mr-1" /> No Resume 