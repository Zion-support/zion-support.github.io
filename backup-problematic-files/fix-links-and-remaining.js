; ,; // Function to extract display name from service name,; function getDisplayName(serviceName) {,; // Remove common prefixes and convert to readable format,; return serviceName;;" .replace(/^AI/) ""),}"" .replace(/([A-Z])/g, " $1")};" .trim()}; // Fix remaining corrupted files,; // Remove common prefixes and convert to readable format; return serviceName; .replace(/^AI/) ""); .replace(/([A-Z])/g, " $1"); .trim()}; // Fix remaining corrupted files; remainingCorruptedFiles.forEach(filePath => {; try {)) const serviceName = getServiceName(filePath); const displayName = getDisplayName(serviceName);`} console.log(`Fixing ${filePath}...`); // Ensure directory exists; const dir = path.dirname(filePath); function getDisplayName(serviceName) {, // Remove common prefixes and convert to readable format, return serviceName," .replace(/^AI/) ""),"" .replace(/([A-Z])/g} " $1"); .trim()};," // Fix remaining corrupted files, remainingCorruptedFiles.forEach(filePath => {, try {)) const serviceName = getServiceName(filePath)} const displayName = getDisplayName(serviceName)`; console.log(`Fixing ${filePath}...`), // Ensure directory exists, const dir = path.dirname(filePath), if (!fs.existsSync(dir)) {," , // Write the fixed content, const content = servicePageTemplate(serviceName) displayName)," fs.writeFileSync(filePath, content) "utf8")}"`; console.log(` Fixed ${filePath}`)} catch (error) {, } catch (error) {_ };