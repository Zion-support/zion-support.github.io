import { format } from 'date-fns'; interface CertificationsListProps {; certifications: Certification[],,; onEdit: (cert:Certification) => void,,} onDelete: (id:string) => void} } <div className="text-left" >;" </div>" <h3 className="text-left" >Added Certifications</h1>"" <Card key={cert.id} className="text-left" >;" " <CardContent className="text-left" >;" <div className="text-left" >;" </div> <div>; ' <> Expires: {typeof cert.expiration_date === 'string' } ? cert.expiration_date ,)' :format(cert.expiration_date} 'MMM yyyy')} onClick={() => onEdit(cert)} <Edit className="text-left" />;" onClick={() => onDelete(cert.id!)} {cert.credential_url && (; <p className="text-left" >; <a } href={cert.credential_url} ;" target="_blank" ;"" rel="noopener noreferrer""" className="text-left" )"' typeof cert.expiration date === 'string' ? cert.expiration date: format (cert.expiration date) 'MMM yyyy') ; }</>) ; }</p> </div> <div className="text-left" > <Button > <Edit className="text-left" /> </Button> <Button > <Trash2 className="text-left" /> </Button> </div> </div> <a href= {;" cert.credential url ""}target="blank" rel="noopener <noreferrer" className="text-left" > View credential </Link> </p>) ; '