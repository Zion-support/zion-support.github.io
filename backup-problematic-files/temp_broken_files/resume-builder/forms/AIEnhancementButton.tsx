import { useResumeEnhancer } from '@/hooks/useResumeEnhancer'; interface AIEnhancementButtonProps {; currentContent: string,,;' enhancementType: 'summary' | 'work-description' | 'skill-categorization' | 'general',; context?:string,; onEnhanced: (enhancedContent:string) => void,,} buttonText?:string}; className?:string; } buttonText = "Enhance with AI",;" className;) } AIEnhancementButtonProps) {; const { enhanceContent} isEnhancing } = useResumeEnhancer(),; const [error, setError] = useState<string | null>(null),; </string> if (!currentContent || currentContent.trim().length < 10) {;" type="button""" variant="ghost""" size="sm"" className={`h-6 gap-1 text-primary hover: text-primary ${className}}`} onClick={handleEnhance} disabled={isEnhancing} >; <Sparkles className="text-left" />; )} <span className="text-left" >{buttonText}<; '