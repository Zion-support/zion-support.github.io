import React, { useState, useMemo } from 'react'
import Head from 'next/
import Link from 'next/
import { ultimate2026Services } from '../data/
import { revolutionary2026Innovations } from '../data/
    website: any
    { id:'low', name: any
    { id:'medium', name: any
    { id:'high', name: any
        (selectedPriceRange = = 'low' && service.price != 'Custom pricing' && parseInt(service.price.replace(/[^0-9]/
        (selectedPriceRange = = 'medium' && service.price != 'Custom pricing' && parseInt(service.price.replace(/[^0-9]/g, '') >= 1000 && parseInt(service.price.replace(/[^0-9]/
        (selectedPriceRange = = 'high' && service.price != 'Custom pricing' && parseInt(service.price.replace(/[^0-9]/
          const priceA = a.price = = 'Custom pricing' ? 999999 :parseInt(a.price.replace(/[^0-9]/
          const priceB = b.price = = 'Custom pricing' ? 999999 :parseInt(b.price.replace(/[^0-9]/
and emerging technology solutions to gain competitive advantages and drive unprecedented growth. </p> <div className="flex flex-col sm:flex-row gap-4 justify-center" > <Link href="/contact" className="inline-flex items-center px-8 py-4 bg-white text-purple-600 font-semibold rounded-full hover:bg-gray-100 transition-all duration-300 transform hover:scale-105" > <Phone className="w-5 h-5 mr-2" /> Get Started Today </Link> <Link href="/pricing" className="inline-flex items-center px-8 py-4 border-2 border-white text-white font-semibold rounded-full hover:bg-white hover:text-purple-600 transition-all duration-300" > <DollarSign className="w-5 h-5 mr-2" /> View Pricing </Link> </div> <div className="mt-12 grid grid-cols-1 md:grid-cols-3 gap-8" > <div className="text-center" > <div className="text-3xl font-bold text-white mb-2" >500%+</div> <div className="text-white/80" >Average ROI</div> </div> <div className="text-center" > <div className="text-3xl font-bold text-white mb-2" >24/7</div> <div className="text-white/80" >Support Available</div> </div> <div className="text-center" > <div className="text-3xl font-bold text-white mb-2" >99.9%</div> <div className="text-white/80" >Uptime Guarantee</div> </div> </div> </motion.div> </div> 