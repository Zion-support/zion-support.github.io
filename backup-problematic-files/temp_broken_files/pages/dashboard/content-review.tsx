import useSWR from 'swr'
import React, { useMemo, useState } from 'react'
import EnhancedLayout from '../../components/layout/
import ModerationModal from '../../components/admin/
  if (role != 'admin') return { redirect:{ destination: any
  const { data, mutate } = useSWR(`/api/admin/moderation/
      method:'POST', headers:{ 'Content-Type': any