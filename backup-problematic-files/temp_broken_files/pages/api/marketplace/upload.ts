import type { NextApiRequest, NextApiResponse } from "next"
  if (req.method != "POST") return res.status(405).json({ ok:false, error: any
  if (!name) return res.status(400).json({ ok:false, error: any