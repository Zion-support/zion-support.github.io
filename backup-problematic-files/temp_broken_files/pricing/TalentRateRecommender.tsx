import { Sparkles } from 'lucide-react'; interface TalentRateRecommenderProps {; skills: string[],,; yearsExperience: number,,; location?:string,} onSuggestionApplied: (value:number) => void,}; rateType:"hourly" | "fixed"" } export const TalentRateRecommender: React.FC<TalentRateRecommenderProps> = ({) ) const [suggestion, setSuggestion] = useState<PricingSuggestion | null>(null),; if (skills.length === 0 || yearsExperience <= 0) {; return,; const result = await getTalentRateSuggestion(params),} setSuggestion(result)}; } finally {; setIsLoading(false)}; },; // Track this suggestion application; if (user && user.id) {; trackPricingSuggestion({} }),; return (;" <div className="text-left" >;" </div> <div>; <Button;" type="button""" variant="outline"" onClick={generateSuggestion} disabled={skills.length === 0 || yearsExperience <= 0}" className="text-left" ;" >; " <Sparkles className="text-left" /> Optimize Rate with AI)" </div> '