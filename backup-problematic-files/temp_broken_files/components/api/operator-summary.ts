import type { NextApiRequest, NextApiResponse } from 'next'
    const r = await fetch(`${req.headers['x-forwarded-proto'] || 'http'
    const jobs24 = metrics?.marketplace?.find(m: any) => m.key = = 'jobs_24h'
    const voters = metrics?.dao?.find(m: any) => m.key = = 'voter_participation'
    const wallets = metrics?.token?.find(m: any) => m.key = = 'active_wallets'
    const tx = metrics?.token?.find(m: any) => m.key = = 'tx_volume_daily'
    const instances = metrics?.multiverse?.find(m: any) => m.key = = 'active_instances'
    res.status(200).json({ summary: [], error: any