import React, { useState } from 'react'
import Head from 'next/
import Card from '../components/ui/
import Button from '../components/ui/
                      className={`p-4 rounded-lg border ${result.status = = 'valid' ? 'border-green-500/30 bg-green-500/
                        result.status = = 'suspicious' ? 'border-yellow-500/30 bg-yellow-500/
                        result.status = = 'disposable' ? 'border-orange-500/30 bg-orange-500/
                        'border-red-500/30 bg-red-500/