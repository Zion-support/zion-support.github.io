import React, { useEffect, useMemo, useState } from "react"
import Badges from "./
    const res = await fetch("/api/wallet/
      headers: { "Content-Type": any
    const res = await fetch("/api/wallet/
      method: "POST", headers: { "Content-Type": any
            <div className="text-gray-600&quot;>Rate: 1 {symbol} = ${summary?.config.usdPerToken?.toFixed(2) ?? &quot;0.00"}