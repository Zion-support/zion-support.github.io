import React from 'react'
import Head from 'next/
import Card from '../components/ui/
import Button from '../components/ui/
      pricing: any
        return parseInt(b.users.replace(/[^0-9]/g, '') - parseInt(a.users.replace(/[^0-9]/
href={`/tool-details/${tool.name.toLowerCase().replace(/\s+/