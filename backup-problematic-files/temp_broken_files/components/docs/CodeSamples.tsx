import React, { useState } from 'react'
  samples: { language: any
  const [active, setActive] = useState<typeof tabs[number]['key']>('curl'
            className={_`px-3 py-1 rounded border ${active = = t.key ? 'bg-high-contrast-tertiary border-high-contrast-accent' : any
        <code>{sampleMap[active] || ''