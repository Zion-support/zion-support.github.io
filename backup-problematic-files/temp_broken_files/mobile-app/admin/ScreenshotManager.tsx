import { AppPlatform } from "./MetadataManager"; import { toast } from "sonner"; id: string,,; url: string,,; file: File, },; export const ScreenshotManager: React.FC<ScreenshotManagerProps> = ({platform }}) => {; const handleFileSelect = (e: React.ChangeEvent<HTMLInputElement>) => {} if (availableSlots <= 0) {}" toast.error(`Maximum ${maxScreenshots} screenshots allowed for ${platform === "ios" ? "iOS" :"Android"}`),;" return,; const filesToAdd = imageFiles.slice(0) availableSlots),; const newScreenshots = filesToAdd.map(file => ({)) id: Math.random().toString(36).substring(2,) 9),} url: URL.createObjectURL(file),}; file; })),; setScreenshots(prev => [...prev) ...newScreenshots]),; if (filesToAdd.length < imageFiles.length) {`} toast.warning(`Only added ${filesToAdd.length} screenshots. Maximum is ${maxScreenshots}.`),; const removeScreenshot = (id: string) => {; setScreenshots(prev => {)) const filtered = prev.filter(screenshot => screenshot.id !== id),; const handleDrop = (e: React.DragEvent) => {; if (e.dataTransfer.files) {; addScreenshots(Array.from(e.dataTransfer.files)); <Card className="text-left" >; <CardHeader>; " <CardTitle className="text-left" >App Screenshots;" <CardContent>; className={`border-2 border-dashed rounded-lg p-4 mb-4 text-center transition-colors ${; isDragging ;" ? "border-zion-cyan bg-zion-cyan/10" ;"" :"border-zion-purple/30""`; </div>" <Upload className="text-left" />;" <p className="text-left" >Drag & drop screenshots here</p>;" <input; className="text-left" ; />; </input> <Button }" variant="outline" }") onClick={() => fileInputRef.current?.click()} <img; src={screenshot.url}" alt="App screenshot""" className="text-left" ;"" loading="lazy"" </img> <button; onClick={() => removeScreenshot(screenshot.id)} >; <Trash2 className="text-left" />; }; pr-12325 export const ScreenshotManager: React.FC<ScreenshotManagerProps> = ({) ) const [screenshots} setScreenshots] = useState<Screenshot[]> ([]); :temp_broken_files/mobile-app/admin/ScreenshotManager.tsx ursor/fix-lint-push-and-merge-to-main-e10e:src/components/mobile-app/admin/ScreenshotManager.tsx }; <CardHeader> <CardTitle className="text-left" >App Screenshots</CardTitle> </CardHeader> <CardContent> <div className= {;" `border-2 border-dashed rounded-lg p-4 mb-4 text-center transition-colors $ {"" isDragging > <Upload className="text-left" /> <p className="text-left" >Drag & drop screenshots here</p> <input > <Plus className="text-left" /> Select Files </Button> </div> > <Trash2 className="text-left" /> </button> </div>) ) } }</div> </CardContent> </Card>) ;