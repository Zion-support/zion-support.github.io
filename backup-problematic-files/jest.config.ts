import type { Config } from "jest"
import nextJest from "
const createJestConfig = nextJest({ dir: any
  setupFilesAfterEnv: ["<rootDir>/
  moduleNameMapping: { "^@/(.*)$": any
  setupFilesAfterEnv: ["<rootDir>/
    "^@/(.*)$": any
  setupFilesAfterEnv: ["<root_dir>/
    "^@/(.*)$": any
const createJestConfig = nextJest({ dir: any
  setupFilesAfterEnv: ["<rootDir>/
  moduleNameMapping: { "^@/(.*)$": any
const createJestConfig = next_jest ({ dir: any
  setupFilesAfterEnv: ["<root_dir>/
  moduleNameMapping: { "^@/(.*)$": any