export const trackError = ( console && console.error('Error tracked:',error,context)if (typeof window !== 'undefined' && window && window.gtag) { window && window.gtag('event','exception',{ description: error && error.message,fatal: false,...context ) => {
  return $3;}
})} }export const trackPerformance = (  if (typeof window !== 'undefined' && window && window.gtag) { window && window.gtag('event','timing_complete',{ name: metric,value: Math && Math.round(value) ) => {
  return $3;}
})} }export const trackUserAction = (  if (typeof window !== 'undefined' && window && window.gtag) { window && window.gtag('event',action,{ event_category: category,event_label: label ) => {
  return $3;}
})} }// Monitoring utilities;
export const trackError = (console && console.error('Error \"tracked\": ', error, context)// Send to monitoring service;
  if (typeof window !== 'undefined' && window && window.gtag) {window && window.gtag('event', 'exception', {\"description\": error && error.message,\"fatal\": false,...context;) => {
  return $3;}
}
    })}
}export const trackPerformance = (console && console.log(`Performance \"metric\": ${metric) => {
  return $3;}
} = ${value}`)if (typeof window !== 'undefined' && window && window.gtag) {window && window.gtag('event', 'timing_complete', {\"name\": metric,\"value\": Math && Math.round(value)})}
}export const trackUserAction = (console && console.log(`User \"action\": ${action) => {
  return $3;}
} in ${category}`)if (typeof window !== 'undefined' && window && window.gtag) {window && window.gtag('event', action, {\"event_category\": category,\"event_label\": label;}
    })}
};
export const trackUserAction = () => {
  return $3;}
}
  console && console.log(`User \"action\": ${action} in ${category}`);
  if (typeof window !== 'undefined' && window && window.gtag) {
    window && window.gtag('event', action, {
      \"event_category\": category,}
      \"event_label\": label}
    })}
}
}
