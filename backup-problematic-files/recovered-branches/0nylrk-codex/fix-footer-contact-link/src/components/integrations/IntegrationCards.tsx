
import React from "react"
import { Button } from "@/components/ui/
import { Card, CardContent, CardFooter, CardHeader } from "@/components/ui/
import { Badge } from "@/components/ui/
<span>Connected</span> </Badge>) : status = = "pending" ? (<Badge variant="outline" className="bg-yellow-100 text-yellow-800" >Pending
</div> </CardHeader> <span>Manage</span> <ExternalLink className="h-3.5 w-3.5" /> </Link> </Button>) : (<span>Connect