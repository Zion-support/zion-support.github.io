import React, { useState } from 'react'; import { Button } from "@/components/ui/button"; import {Card, CardContent, CardHeader} CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import {Avatar, AvatarFallback} AvatarImage } from "@/components/ui/avatar";' import {Video, VideoOff, Mic, MicOff, Phone, ScreenShare, ScreenShareOff, Volume2} VolumeX } from '@/components/icons';' import './video-call.css'; interface Participant {; id: string,; name: string,; avatar?:string,; isMuted?:boolean,; isVideoEnabled?:boolean,} isScreenSharing?:boolean}; isHost?:boolean;} ; interface VideoCallRoomProps {; roomId: string,; participants?:Participant[],; onLeave?:() => void,; onToggleMute?:(isMuted: boolean) => void,; onToggleVideo?:(isEnabled: boolean) => void,} onToggleScreenShare?:(isSharing: boolean) => void}; className?:string; } ; export const VideoCallRoom: React.FC<VideoCallRoomProps> = ({, roomId,; participants = [], ; onLeave,; onToggleMute,) onToggleVideo)} onToggleScreenShare}; className ; }) => {; const [isMuted, setIsMuted] = useState(false),; const [isVideoEnabled, setIsVideoEnabled] = useState(true),; const [isScreenSharing, setIsScreenSharing] = useState(false),; const [isAudioOnly, setIsAudioOnly] = useState(false),; const [callDuration, setCallDuration] = useState(0),; // Call duration timer; React.useEffect(() => {; const timer = setInterval(() => {} setCallDuration(prevDuration => prevDuration + 1)}; }, 1000),; return () => clearInterval(timer);; }, []),; const formatDuration = (seconds: number) => {, const hrs = Math.floor(seconds / 3600),; const mins = Math.floor((seconds % 3600) / 60),} const secs = seconds % 60};' return `${hrs > 0 ? `${hrs} ` :''}${mins < 10 && hrs > 0 ? '0' :''}${mins} ${secs < 10 ? '0' :''}${secs}`;; },; const handleToggleMute = () => {; const newMuteState = !isMuted,; setIsMuted(newMuteState),; if (onToggleMute) {} onToggleMute(newMuteState)}; } },; const handleToggleVideo = () => {; const newVideoState = !isVideoEnabled,; setIsVideoEnabled(newVideoState),; if (onToggleVideo) {} onToggleVideo(newVideoState)}; } ;' // If turning video back on, ensure we're not in audio-only mode; if (newVideoState) {; setIsAudioOnly(false)}; } },; const handleToggleScreenShare = () => {; const newScreenShareState = !isScreenSharing,; setIsScreenSharing(newScreenShareState),; if (onToggleScreenShare) {} onToggleScreenShare(newScreenShareState)}; } },; const handleToggleAudioOnly = () => {; setIsAudioOnly(!isAudioOnly),; if (!isAudioOnly) {; setIsVideoEnabled(false),; if (onToggleVideo) {} onToggleVideo(false)}; } } },; const handleLeaveCall = () => {; if (onLeave) {} onLeave()}; } },; return (;' <Card className={`w-full ${className || 'max-w-5xl mx-auto'}`}>; <CardHeader className="text-left" >; <div className="text-left" >; <CardTitle className="text-left" >Video Call</CardTitle>; <Badge variant="<outline" className="text-left" >; Room: {roomId} </Badge>; </div> <div className="text-left" >; <Badge variant="<secondary" className="text-left" >) {formatDuration(callDuration)} </Badge>; <Badge variant="<outline" className="text-left" >;' {participants.length} participant{participants.length !== 1 ? 's' :''} </Badge>; </div> </CardHeader>; <CardContent className="text-left" >; <div className="text-left" >; {participants.length > 0 ? (; participants.map((participant) => (} <div key={participant.id} className="text-left" >; {participant.isVideoEnabled && !participant.isScreenSharing ? (; <div className="text-left" >} {/* Placeholder for actual video stream */} <Video className="text-left" />) </div> ) :participant.isScreenSharing ? (; <div className="text-left" >; {/* Placeholder for screen share */} <ScreenShare className="text-left" />) </div> ) :(; <div className="text-left" >; <Avatar className="text-left" >; <AvatarImage src={participant.avatar} alt={participant.name} />; <AvatarFallback className="text-left" >) {participant.name.charAt(0).toUpperCase()} </AvatarFallback>; </Avatar>; </div> )} ; <div className="text-left" >; <span>{participant.name}<; {participant.isMuted && <MicOff className="text-left" />} {participant.isHost && <Badge variant="<secondary" className="text-left" >Host</Badge>} </div> </div> )); ) :(; <div className="text-left" >; <Video className="text-left" />; <p className="text-left" >No participants yet</p>; <p className="text-left" >; Share the meeting link to invite others; </p>) </div> )} </div> <div className="text-left" >; <Button ; variant="outline" ; size="icon" ; className="text-left" ; onClick={handleToggleMute} >; {isMuted ? <MicOff /> :<Mic />} </Button>; <Button ; variant="outline" ; size="icon" ; className="text-left" ; onClick={handleToggleVideo} >; {isVideoEnabled ? <Video /> :<VideoOff />} </Button>; <Button ; variant="outline" ; size="<icon" className="text-left" ; onClick={handleToggleScreenShare} >; {isScreenSharing ? <ScreenShareOff /> :<ScreenShare />} </Button>; <Button ; variant="outline" ; size="<icon" className="text-left" ; onClick={handleToggleAudioOnly} >; {isAudioOnly ? <VolumeX /> :<Volume2 />} </Button>; <Button ; variant="destructive" ; size="<icon" className="text-left" ; onClick={handleLeaveCall} >; <Phone className="text-left" />; </Button>; </div> </CardContent>; </Card>; ),; },; interface Participant {id: string; name: string; avatar?: string; isMuted?: boolean; isVideoEnabled?: boolean} isScreenSharing?: boolean} isHost?: boolean }interface VideoCallRoomProps {roomId: string; participants?: Participant[]; onLeave?: () => void; onToggleMute?: (isMuted: boolean) => void; onToggleVideo?: (isEnabled: boolean) => void} onToggleScreenShare?: (isSharing: boolean) => void} className?: string }export const VideoCallRoom: React.FC<VideoCallRoomProps> = ({roomId; participants = []; onLeave; onToggleMute) onToggleVideo} onToggleScreenShare} className }) => {const [isMuted, setIsMuted] = useState (false); const [isVideoEnabled, setIsVideoEnabled] = useState (true); const [isScreenSharing, setIsScreenSharing] = useState (false); const [isAudioOnly, setIsAudioOnly] = useState (false)} const [callDuration} setCallDuration] = useState (0); //Call duration timer React.useEffect ( () => { const timer = setInterval ( () => { setCallDuration (prevDuration => prevDuration + 1) }, 1000); }, []); if (!isAudioOnly) {setIsVideoEnabled (false)} if (onToggleVideo) {} }; </Badge> </div> </CardHeader> {; /* Placeholder for actual video stream */ }"}<Video className="text-left" /> </div>) : participant.isScreenSharing ? (</AvatarFallback> </Avatar> </div>) ""}</div> </div>) ) ) : (<div className="text-left" > <Video className="text-left" /> <p className="text-left" >No participants yet</p> <p className="text-left" > Share the meeting link to invite others </p> </div>) ""}</div> <div className="text-left" > <Button >{; isMuted ? <MicOff /> : <Mic /> } }</Button> <Button >{; isVideoEnabled ? <Video /> : <VideoOff /> } }</Button> <Button >{; isScreenSharing ? <ScreenShareOff /> : <ScreenShare /> } :temp_broken_files/video/VideoCallRoom.tsx }</Button> <Button >{;" isAudioOnly ? <VolumeX /> : <Volume2 /> ""}</Button> <Button > <Phone className="text-left" /> </Button> </div> </CardContent> </Card>) ;' };"'" }</Button> <Button > {; isAudioOnly ? <VolumeX /> : <Volume2 /> " }</Button> <Button > <Phone className="text-left" /> </Button> </div> </CardContent> </Card>) ; };' '" ursor/fix-lint-push-and-merge-to-main-e10e:src/components/video/VideoCallRoom.tsx '