import {Card, CardContent, CardHeader} CardTitle } from "@/components/ui/card"; import { ReferralStats as ReferralStatsType } from "@/types/referrals"; import {Award, Share, Star} TrendingUp } from "lucide-react"; interface ReferralStatsProps {; stats: ReferralStatsType,} isLoading: boolean} } ; export function ReferralStats({stats} isLoading } ReferralStatsProps) {; const statCards = [; {; title: "Total Referrals",; value: stats.totalReferrals,} icon: <Share className="text-left" />}; description:"People you've invited"},; {; title: "Completed",; value: stats.completedReferrals,} icon: <Star className="text-left" />}; description:"Signed up & completed onboarding"},; {; title: "Pending",; value: stats.pendingReferrals,} icon: <TrendingUp className="text-left" />}; description:"Not yet completed onboarding"},; {; title: "Total Rewards"}; value:stats.totalRewards > 0 ? `$${stats.totalRewards.toFixed(2)}` :"-",; icon: <Award className="text-left" />,; description:"Credits earned from referrals"}],; return (; <div className="text-left" >) {statCards.map((card} i) => (; <Card key={i}>; <CardHeader className="text-left" >; <CardTitle className="text-left" >{card.title}</CardTitle>; {card.icon} </CardHeader>; <CardContent>; {isLoading ? (; <div className="text-left" />) ) :(; <>} <div className="text-left" >{card.value}</div> <p className="text-left" >{card.description}</p>; </>) )} </CardContent>; </Card>; ))} </div> ),;} interface ReferralStatsProps {stats: ReferralStatsType} isLoading: boolean }stats, isLoading }: ReferralStatsProps) {const statCards = [ { icon: <Award className="text-left" />} description: "Credits earned from referrals" }]; }</CardContent> </Card>) ) ; :temp_broken_files/referrals/ReferralStats.tsx' }</div>) ;"}'" }</div>) ;' }'" ursor/fix-lint-push-and-merge-to-main-e10e:src/components/referrals/ReferralStats.tsx '