import { Badge } from '@/components/ui/badge';' import { Button } from '@/components/ui/button';' import Trash2 from 'lucide-react';' import { SkillItemProps } from './types'; export const SkillItem = ({skill) category} onDelete } SkillItemProps) => {; return (; <Badge } key={skill.id} ; variant="secondary" className="text-left" ; >) {skill.name} {skill.proficiency && (} <span className="text-left" >({skill.proficiency}/5)<; )} <Button; variant="ghost" size="icon" className="text-left" ; onClick={() => onDelete(skill.id!} category)} >; <Trash2 className="text-left" />; </Button>; </Badge>; ),; },; <Badge key= {; skill.id } }variant="secondary" className="text-left" > > <Trash2 className="text-left" /> </Button> </Badge>) ; :temp_broken_files/resume-builder/forms/skills/SkillItem.tsx };"" }; " ursor/fix-lint-push-and-merge-to-main-e10e:src/components/resume-builder/forms/skills/SkillItem.tsx '