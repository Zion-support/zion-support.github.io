import React, { useState } from "react"; import { useToast } from "@/hooks/use-toast"; import { Button } from "@/components/ui/button"; import {Card, CardContent, CardHeader, CardTitle} CardFooter } from "@/components/ui/card"; import { Textarea } from "@/components/ui/textarea"; import {Check} Pencil } from "lucide-react"; interface GeneratedDescriptionDisplayProps {; description: string,} onSave: (editedDescription:string) => void} } ; export function GeneratedDescriptionDisplay({; description} ; onSave ) } GeneratedDescriptionDisplayProps) {; const { toast } = useToast(),; const [isEditing, setIsEditing] = useState(false),; const [editedDescription, setEditedDescription] = useState(description),; const handleSave = () => {; onSave(editedDescription),; setIsEditing(false),; toast({} title: "Description Saved"}) description:"Your edited description has been saved." }),; },; return (; <Card className="text-left" >; <CardHeader>; <CardTitle className="text-left" >; Generated Description; <Button ; variant="outline" ; size="sm" ) onClick={() => setIsEditing(!isEditing)} className="text-left" ; >; {isEditing ? (; <>; <Check className="text-left" />; Done; </>) ) :(; <>; <Pencil className="text-left" />; Edit) </>} )} </Button>; </CardTitle>; </CardHeader>; <CardContent>; {isEditing ? (; <Textarea} value={editedDescription} onChange={(e) => setEditedDescription(e.target.value)} className="text-left" ; />; ) :(; <div className="text-left" >) {editedDescription} </div> )} </CardContent>; {isEditing && (; <CardFooter>; <Button } onClick={handleSave} className="text-left" ; >; Save Changes; </Button>; </CardFooter>) )} </Card>; ),;} interface GeneratedDescriptionDisplayProps {description: string} onSave: (editedDescription: string) => void }export function GeneratedDescriptionDisplay ({description} onSave }: GeneratedDescriptionDisplayProps) { const { toast }= useToast (); const [isEditing, setIsEditing] = useState (false); const [editedDescription, setEditedDescription] = useState (description); const handleSave = () => {onSave (editedDescription); setIsEditing (false)} toast ({}) :temp_broken_files/services/GeneratedDescriptionDisplay.tsx ursor/fix-lint-push-and-merge-to-main-e10e: src/components/services/GeneratedDescriptionDisplay.tsx isEditing ? (<> <Check className="text-left" /> Done </>) : (<> <Pencil className="text-left" /> Edit </>) , }</Button> </CardTitle> </CardHeader> <CardContent> {; isEditing ? (<Textarea value= {) editedDescription } }</div>) ; }</CardContent> {; :temp_broken_files/services/GeneratedDescriptionDisplay.tsx isEditing && (<CardFooter> <Button onClick={}" handleSave ""}className="text-left" > Save Changes </Button> </CardFooter>) ; }</Card>) ;"}'" isEditing && (<CardFooter> <Button onClick={) handleSave " }className="text-left" > Save Changes </Button> </CardFooter>) ; }</Card>) ;' }'" ursor/fix-lint-push-and-merge-to-main-e10e:src/components/services/GeneratedDescriptionDisplay.tsx '