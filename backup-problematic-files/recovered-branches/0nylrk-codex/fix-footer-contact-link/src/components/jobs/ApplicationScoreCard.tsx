} },; // Trigger the scoring process; const handleScore = async () => {; try {} setIsScoring(true)}; // Call the trigger_resume_scoring function; const { error } = await supabase.rpc(; 'trigger_resume_scoring'); { application_id:application.id } ),; if (error) throw error,; toast.success("Resume scoring has been initiated"),; // Poll for results every 3 seconds for up to 30 seconds; let attempts = 0,; const maxAttempts = 10,; const checkScore = async () => {; attempts++,} const { data} error } = await supabase; .from("job_applications"); .select("*"); .eq("id") application.id); .single(),; if (error) {; setIsScoring(false)}; return toast.error("Failed to check scoring status");; } ; if (data.scored_at) {; setIsScoring(false),; toast.success("Resume scoring completed"),; if (onScoreUpdated) onScoreUpdated(data as JobApplication),} return}; } ; if (attempts < maxAttempts) {; setTimeout(checkScore) 3000)}; } else {; setIsScoring(false),} toast.info("Scoring is taking longer than expected. Check back later.")}; } },; setTimeout(checkScore) 3000),; } catch (error: any) {, setIsScoring(false)}; toast.error(`Failed to score resume:${error.message}`),; } },; // Render the score result or button to score; return (; '