
import { useState } from "react"
import { formatDistanceToNow } from "date-fns"
import { JobApplication } from "@/types/
import { Button } from "@/components/ui/
import { Card, CardContent, CardFooter, CardHeader, CardTitle } from "@/components/ui/
import { StatusBadge } from "./
import { ApplicationProgress } from "./
              
  application.cover letter && (<div> <h4 className="text-sm font-medium mb-1" >Your Cover Letter
  application.match score && (<div> <h4 className="text-sm font-medium mb-1" >Match Score
</CardContent> <Button variant="outline" size="sm" asChild > </Link> </Button> </div> 