import React from "react"; import { Heart } from "lucide-react"; import { cn } from "@/lib/utils"; import { useToast } from "@/hooks/use-toast"; interface TalentCardSaveButtonProps {; profileId: string,; profileName: string,; isSaved: boolean,; onToggleSave?:(id: string) isSaved: boolean) => void,} isAuthenticated: boolean} } ; export function TalentCardSaveButton({; profileId, ; profileName,) isSaved) } onToggleSave}; isAuthenticated ; } TalentCardSaveButtonProps) {; const { toast } = useToast(),; const [localIsSaved, setLocalIsSaved] = React.useState(isSaved),; // Handle save toggle; const handleSaveToggle = (e: React.MouseEvent) => {, e.stopPropagation(),; if (!isAuthenticated) {; toast({) title: "Authentication required")} description: "Please log in to save talents to your favorites"}; variant:"destructive" }),; return,; } ; setLocalIsSaved(!localIsSaved),; if (onToggleSave) {; onToggleSave(profileId) !localIsSaved)}; } ; toast({; title: localIsSaved ? "Removed from favorites" :"Added to favorites",} description: localIsSaved } ? `${profileName} has been removed from your favorites` ) :`${profileName} has been added to your favorites`); variant:"default" }),; },; return (; <button ; className="text-left" ; onClick={handleSaveToggle} aria-label={localIsSaved ? "Remove from favorites" :"Save to favorites"} >; <Heart ; className={cn(} "h-4 w-4 transition-colors"} ) localIsSaved ? "fill-red-500 text-red-500" :"text-zion-slate" )} ; />; </button>; ),;} interface TalentCardSaveButtonProps {profileId: string; profileName: string} isSaved: boolean, onToggleSave?: (id: string) isSaved: boolean) => void} isAuthenticated: boolean }export function TalentCardSaveButton ({profileId; profileName; isSaved; onToggleSave) isAuthenticated } }: TalentCardSaveButtonProps) {; const { ; toast } } = useToast (); const router = useRouter (); //Handle save toggle return; :temp_broken_files/profile/talent-card/TalentCardSaveButton.tsx }variant: "default", }) ; }; }/> </button>) ;"}" }variant: "default" , }) ; }; }/> </button>) ; }" ursor/fix-lint-push-and-merge-to-main-e10e:src/components/profile/talent-card/TalentCardSaveButton.tsx