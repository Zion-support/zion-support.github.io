import React, { useState } from "react"
import { useForm } from "react-hook-form"
import { zodResolver } from "@hookform/resolvers/
import { Button } from "@/components/ui/
import { Input } from "@/components/ui/
import { Textarea } from "@/components/ui/
import { Switch } from "@/components/ui/
import { Badge } from "@/components/ui/
import { Separator } from "@/components/ui/
  FormMessage} from "@/components/ui/
import { Card, CardContent, CardDescription, CardFooter, CardHeader, CardTitle } from "@/components/ui/
import { toast } from "@/components/ui/
import { supabase } from "@/integrations/supabase/
import { AspectRatio } from "@/components/ui/
import { useAuth } from "@/hooks/
                              
  <p>Your profile has been enhanced with AI. You're now more discoverable to recruiters and companies!
> <div className="pt-2"> <FormLabel className="text-zion-slate-light" >Availability Message