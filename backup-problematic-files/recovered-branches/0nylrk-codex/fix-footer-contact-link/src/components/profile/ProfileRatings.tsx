
                reviews={reviews}
                isLoading={isLoading}
                onReportReview={reportReview}
              />;
            </TabsContent>;

                isLoading={isLoading}
                onReportReview={reportReview}
              />;
            </TabsContent>;

              />;
            </TabsContent>;
          </Tabs>;
        </div>;
      </div>;

}