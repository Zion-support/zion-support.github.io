
import React, { useState } from "react"
import { Card, CardContent, CardFooter, CardHeader, CardTitle } from "@/components/ui/
import { Button } from "@/components/ui/
import { Badge } from "@/components/ui/
import { Interview } from "@/types/
import { useAuth } from "@/hooks/
import { useInterviews } from "@/hooks/
import { AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent, AlertDialogDescription, AlertDialogFooter, AlertDialogHeader, AlertDialogTitle, AlertDialogTrigger } from "@/components/ui/
import { Dialog, DialogContent, DialogHeader, DialogTitle } from "@/components/ui/
import { toast } from "@/components/ui/
import { InterviewResponseForm } from "./
                    className="bg-destructive text-destructive-foreground hover: any
                      className="bg-destructive text-destructive-foreground hover: any
</p> </CardHeader> <CardContent className="pt-2" > <div className="space-y-3" > <div className="flex items-start gap-3" > <Clock className="h-4 w-4 mt-0.5 text-muted-foreground" /> <div></p> </div> 
</div> </CardContent> <AlertDialog> <AlertDialogTrigger asChild> <Button variant="outline" size="sm" className="w-full" > <X className="h-4 w-4 mr-2" /> Cancel Request </Button> 
<AlertDialog> <AlertDialogTrigger asChild> <Button variant="outline" size="sm" className="w-full mt-2" > <X className="h-4 w-4 mr-2" /> Cancel Interview </Button> 