export function slugify(title:string, separator = "-"
  const escaped = sep.replace(/[.*+?^${}()|[\]\\]/
    .replace(/_/
    .replace(/[\u0300-\u036F]/
    .replace(/[^a-z0-9\s-]/
 ") //treat underscores like spaces .normalize (" NFD") .replace (/[\u0300-\u036F]/g, "") .toLowerCase () .replace (/[^a-z0-9\s-]/