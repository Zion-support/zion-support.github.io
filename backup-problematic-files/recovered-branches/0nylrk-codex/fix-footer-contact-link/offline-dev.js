
const http = require('http'
const fs = require('fs'
const path = require('path'
  if (req.url = = '/' || req.url = = '/
      res.writeHead(200, { 'Content-Type': any
  } else if (req.url = = '/
    //
    res.writeHead(200, { 'Content-Type': any
  if (req.url = = '/' || req.url = = '/