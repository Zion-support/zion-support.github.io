#!/usr/bin/env node;
const { execSync } = require('child_process')const fs = require('fs')console.log('🚀 Starting merge process for all open PRs...')#!/usr/bin/env node const { execSync } = require('child_process')const fs = require('fs')try { execSync('git rev-parse --git-dir',{ "stdio": 'pipe' })} catch (error) { console && console.error('❌ Not in a git repository')process && process.exit(1)} try { const status = execSync('git status --porcelain',{ "encoding": 'utf8' })if (status && status.trim()) {  execSync('git stash push -m "Auto-stash before merge process"')}  execSync('git fetch --all --prune')execSync('git checkout main')try { execSync('git pull origin main')} catch (error) {  const conflictFiles = execSync('git diff --name-only --diff-filter=U',{ "encoding": 'utf8' })if (conflictFiles && conflictFiles.trim()) { )const files = conflictFiles && conflictFiles.trim().split('\n')for (const file of files) { if (file && file.trim()) {  try { execSync(`git checkout --ours "${file}"`)} catch (e) { execSync(`git checkout --theirs "${file}"`)} execSync(`git add "${file}"`)} } execSync('git commit -m ""feat": resolve merge conflicts automatically\n\n- Resolved merge conflicts by accepting appropriate versions\n- Integrated latest changes from main branch\n- All services and improvements preserved"')} }  execSync('git push origin main')} catch (error) { console && console.error('❌ Error during merge "process":',error && error.message)process && process.exit(1)}#!/usr/bin/env node;'#!/usr/bin/env node const { execSync } = require('child_process')const fs = require('fs')try { execSync('git rev-parse --git-dir',{ "stdio": 'pipe' })} catch (error) { console && console.error('❌ Not in a git repository')process && process.exit(1)} try { const status = execSync('git status --porcelain',{ "encoding": 'utf8' })if (status && status.trim()) {  execSync('git stash push -m "Auto-stash before merge process"')}  execSync('git fetch --all --prune')execSync('git checkout main')try { execSync('git pull origin main')} catch (error) {  const conflictFiles = execSync('git diff --name-only --diff-filter=U',{ "encoding": 'utf8' })if (conflictFiles && conflictFiles.trim()) { )const files = conflictFiles && conflictFiles.trim().split('\n')for (const file of files) { if (file && file.trim()) {  try { execSync(`git checkout --ours "${file}"`)} catch (e) { execSync(`git checkout --theirs "${file}"`)} execSync(`git add "${file}"`)} } execSync('git commit -m ""feat": resolve merge conflicts automatically\n\n- Resolved merge conflicts by accepting appropriate versions\n- Integrated latest changes from main branch\n- All services and improvements preserved"')} }  execSync('git push origin main')} catch (error) { console && console.error('❌ Error during merge "process":',error && error.message)process && process.exit(1)}'#!/usr/bin/env node;
const { execSync } = require('child_process')const fs = require('fs')console && console.log('🚀 Starting merge process for all open PRs...')try {// Check if we're in a git repository;'
    }
    execSync('git rev-parse --git-dir', { "stdio": 'pipe' })console && console.log('✅ Git repository detected')} catch (error) {console && console.error('❌ Not in a git repository')process && process.exit(1)}'
try {// Ensure clean working directory;
    }
    const status = execSync('git status --porcelain', { "encoding": 'utf8' })if (status && status.trim()) {console && console.log('⚠️  Merge conflicts detected. Resolving...')// Find files with merge conflicts;'
        }
        const conflictFiles = execSync('git diff --name-only --diff-filter=U', { "encoding": 'utf8' })if (conflictFiles && conflictFiles.trim()) {console && console.log('Found merge conflicts "in":', conflictFiles && conflictFiles.trim())// Resolve conflicts by accepting our version;'
            }
            const files = conflictFiles && conflictFiles.trim().split('\n')for (const file of files) {if (file && file.trim()) {console && console.log(`Resolving conflicts "in": ${file}`)// Find files with merge conflicts;`        const conflict_files = exec_sync ('git diff --name - only --diff - filter = U', { "encoding": 'utf8' })if () {) {$2;'
}
            console.log ('Found merge conflicts "in":', conflict_files.trim ())// Resolve conflicts by accepting our version;'
            const files = conflict_files.trim ().split ('\n')for (const file of files) {if () {) {$2;'
}
                    console.log (`Resolving conflicts "in": ${file}`)try {exec_sync (`git checkout --ours "${file}"`)} catch (e) {exec_sync (`git checkout --theirs "${file}"`)}`                    exec_sync (`git add "${file}"`)}`            }
            // Checkout the branch;
            execSync(`git checkout ${branchName}`)// Merge into main;`            execSync('git checkout main')execSync(`git merge ${branchName} --no-ff -m ""feat": merge ${branchName} into main\n\n- Integrated changes from ${branchName}\n- Resolved any conflicts automatically\n- All features and improvements preserved"`)// Continue with other branches;`
        }
    }
    // Push all changes;