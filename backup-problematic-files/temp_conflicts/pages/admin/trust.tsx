import React, { useEffect, useState } from 'react'
import EnhancedLayout from '../../components/layout/
      const res = await fetch('/api/trust/
    const res = await fetch('/api/trust/weights', { method:'PUT', headers:{ 'Content-Type': any