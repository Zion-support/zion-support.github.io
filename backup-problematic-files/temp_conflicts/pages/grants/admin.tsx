import { useEffect, useMemo, useState } from 'react'
import EnhancedLayout from '../../components/layout/
import type { GrantApplication, Milestone } from '../../types/
  const headers = useMemo() => (token ? { Authorization:`Bearer ${token}`, 'Content-Type':'application/json' } { 'Content-Type': any
    fetch('/api/
    await fetch(`/api/grants/${id}/
    await fetch(`/api/grants/${selected.id}/
    await fetch(`/api/grants/${selected.id}/milestones/${milestoneId}/