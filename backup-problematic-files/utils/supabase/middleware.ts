import { NextResponse } from 'next/
import type { NextRequest } from 'next/
  if (pathname = = '/dashboard' || pathname = = '/dashboard/
    const target = role = = 'client' ? '/dashboard/client' : any
  matcher: ['/dashboard/dashboard/