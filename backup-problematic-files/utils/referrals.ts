export type ReferralEvent = 'visit' | 'signup' | 'profile_completed' | 'job_created' | 'hire'
    const refCode = code || (typeof window != 'undefined' ? (localStorage.getItem('ref_code') || '') : any
    await fetch('/api/referrals/
      headers: { 'Content-Type': any