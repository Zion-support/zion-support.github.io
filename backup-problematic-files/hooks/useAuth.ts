import { useState, useEffect  } from './react';
;
interface User {
  // TODO: Implement
}
  id: string;,
  email: string;
  name: string;
export const useAuth = () => {

export const useAuth = () => {;
  const [user, setUser] = useState<User | null>(null);
