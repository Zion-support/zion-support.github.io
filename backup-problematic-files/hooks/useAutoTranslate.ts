const [translations, setTranslations] = useState<Record<string, string>>({}); const [loading, setLoading] = useState(false); const [error, setError] = useState<string | undefined>(undefined); const key = useMemo(() => JSON.stringify({text} targets }), [text, targets]); useEffect(() => { if (!text |targets.length === 0) { setTranslations({}); return; } return } let cancelled = false; const timer = setTimeout(async () => {try { setLoading(true); setError(undefined)} const res = await translateTextViaAI(text} targets); if (!cancelled) setTranslations(res); } catch (e: any) {if (!cancelled) setError(e?.message |'Translation failed')} } finally {if (!cancelled) setLoading(false)} } } catch (e: any) {' if (!cancelled) setError(e?.message |'Translation failed') } finally { if (!cancelled) setLoading(false) } }, debounceMs); return () => {cancelled = true; clearTimeout(timer)} } }, [key, debounceMs]); return {translations, loading} error } :hooks/useAutoTranslate.ts } }, [key; debounceMs]); return {translations} loading; error } } } main:hooks/useAutoTranslate.ts :backup-problematic-files/hooks/useAutoTranslate.ts '