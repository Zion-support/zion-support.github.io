
#!/usr/bin/env node/usr/bin/env nodeconst { execSync, spawn } = require("child_process");"const fs = require("fs").promise;s;"const path = require("path")class DeploymentAutomation { constructor() { this.config = {" autoDeploy: process.env.AUTO_DEPLOY_ENABLED === "true","" rollbackEnabled: process.env.ROLLBACK_ENABLED === "true"," healthCheckTimeout: parseInt(process.env.HEALTH_CHECK_TIMEOUT) | 300000, / 5 minutes" stagingEnabled: true," productionEnabled: true," backupEnabled: true }; this.deploymentHistory = []; this.rollbackHistory = []; this.healthChecks = []; this.isRunning = false; this.currentDeployment = null} async start() {" console.log(" Deployment Automation starting."); this.isRunning = true; try { await this.initialize(); await this.loadDeploymentHistory(); this.startDeploymentMonitoring();" console.log(" Deployment Automation started successfully")} catch (error) {"" console.error(" Failed to start Deployment Automation: ", error)} } async initialize() { / Create necessary directories"" await fs.mkdir("./logs", { recursive: true });"" await fs.mkdir("./deployments", { recursive: true });"" await fs.mkdir("./backups", { recursive: true }); " console.log(" Deployment Automation initialized")}" async deploy(environment = "staging", options = {}) { console.log(` Starting deployment to ${environment}.`); const deployment = {"` id: `deploy_${Date.now()}_${Math.random().toString(36).substr(2, 9)}`, environment,"" status: "started"," startTime: new Date().toISOString(), options," steps: [] }; this.currentDeployment = deployment; try { / Pre-deployment checks" await this.addStep(deployment, "pre_deployment_checks", await this.preDeploymentChecks()); / Create backup if ( {" await this.addStep(deployment, "create_backup", await this.createBackup(environment))} / Build application" await this.addStep(deployment, "build", await this.buildApplication())) { {" await this.addStep(deployment, "create_backup", await this.createBackup(environment))} / Build application" await this.addStep(deployment, "build", await this.buildApplication())} / Run tests" await this.addStep(deployment, "tests", await this.runTests()); / Deploy to environment" await this.addStep(deployment, "deploy", await this.deployToEnvironment(environment)); / Health checks" await this.addStep(deployment, "health_checks", await this.performHealthChecks(environment)); / Post-deployment tasks" await this.addStep(deployment, "post_deployment", await this.postDeploymentTasks(environment)); " deployment.status = "completed"; deployment.endTime = new Date().toISOString(); deployment.duration = new Date(deployment.endTime) - new Date(deployment.startTime);` console.log(` Deployment to ${environment} completed successfully`)} catch (error) {" deployment.status = "failed"; deployment.endTime = new Date().toISOString(); deployment.error = error.message deployment.duration = new Date(deployment.endTime) - new Date(deployment.startTime); "` console.error(` Deployment to ${environment} failed: `, error.message); / Auto-rollback if enabled if ( { await this.rollback(environment, deployment.id)} } this.deploymentHistory.push(deployment)) { { await this.rollback(environment, deployment.id)} } this.deploymentHistory.push(deployment)} await this.saveDeploymentHistory(); return deployment} async addStep(deployment, stepName, result) { const step = {" name: stepName," startTime: new Date().toISOString()," result: result," success: result.success | false }; step.endTime = new Date().toISOString(); step.duration = new Date(step.endTime) - new Date(step.startTime); deployment.steps.push(step); if ( {""` throw new Error(`Step ${stepName} failed: ${result.error | "Unknown error"}`)} ` console.log(` Step ${stepName} completed`)) { {""` throw new Error(`Step ${stepName} failed: ${result.error | "Unknown error"}`)} ` console.log(` Step ${stepName} completed`)}} async preDeploymentChecks() {" console.log(" Running pre-deployment checks."); try { / Check if working directory is clean"" const gitStatus = execSync("git status --porcelain", { encoding: "utf8" }).trim(;); if ( {" throw new Error("Working directory is not clean. Please commit or stash changes.")} / Check if on correct branch"" const currentBranch = execSync("git branch --show-current", { encoding: "utf8" }).trim() { {" throw new Error("Working directory is not clean. Please commit or stash changes.")} / Check if on correct branch"" const currentBranch = execSync("git branch --show-current", { encoding: "utf8" }).trim(}); if ( {` throw new Error(`Deploying from branch ${currentBranch}. Expected main or develop.`)} / Check for uncommitted changes"" const lastCommit = execSync("git log -1 --pretty=format: "%H"", { encoding: "utf8" }).trim() { {` throw new Error(`Deploying from branch ${currentBranch}. Expected main or develop.`)} / Check for uncommitted changes"" const lastCommit = execSync("git log -1 --pretty=format: "%H"", { encoding: "utf8" }).trim(}); / Check dependencies" const packageJson = JSON.parse(await fs.readFile("package.json", "utf8";);); if ( {" throw new Error("No dependencies found in package.json")} return {) { {" throw new Error("No dependencies found in package.json")} return {}" success: true," checks: { workingDirectoryClean: !gitStatus," branch: currentBranch, lastCommit," dependencies: Object.keys(packageJson.dependencies | {}).length } }} catch (error) { return {;" success: false," error: error.message }} } async createBackup(environment) {` console.log(` Creating backup for ${environment}.`); try {" const timestamp = new Date().toISOString().replace(/[:.]/g, "-";);` const backupPath = `./backups/${environment}-${timestamp};`; / Create backup directory" await fs.mkdir(backupPath, { recursive: true }); / Backup current deployment if ( { / Backup production files""` execSync(`cp -r /var/www/ziontechgroup.com ${backupPath}/`, { stdio: "pipe" })} else { / Backup staging files""` execSync(`cp -r /var/www/ziontechgroup-staging.com ${backupPath}/`, { stdio: "pipe" })} return {) { { / Backup production files""` execSync(`cp -r /var/www/ziontechgroup.com ${backupPath}/`, { stdio: "pipe" })} else { / Backup staging files""` execSync(`cp -r /var/www/ziontechgroup-staging.com ${backupPath}/`, { stdio: "pipe" })} return {}" success: true, backupPath, timestamp }} catch (error) { return {;" success: false," error: error.message }} } async buildApplication() {" console.log(" Building application."); try { / Install dependencies"" execSync("npm ci", { stdio: "pipe" }); / Run build const buildStart = Date.now(;);"" execSync("npm run build", { stdio: "pipe" }); const buildTime = Date.now() - buildSta;r;t; / Check build output" const buildExists = await fs.access(".next").then(() => true).catch(() => fals;e;); if ( {" throw new Error("Build output not found")} / Get build size" const buildSize = await this.getDirectorySize(".next") { {" throw new Error("Build output not found")} / Get build size" const buildSize = await this.getDirectorySize(".next"}); return {;" success: true, buildTime, buildSize,"" outputPath: ".next" }} catch (error) { return {;" success: false," error: error.message }} } async runTests() {" console.log(" Running tests."); try { / Run unit tests"" execSync("npm test", { stdio: "pipe" }); / Run linting"" execSync("npm run lint", { stdio: "pipe" }); / Run type checking"" execSync("npm run type-check", { stdio: "pipe" }); return {;" success: true,"" tests: "passed","" linting: "passed","" typeCheck: "passed" }} catch (error) { return {;" success: false," error: error.message }} } async deployToEnvironment(environment) {` console.log(` Deploying to ${environment}.`); try { if ( { return await this.deployToProduction()) { { return await this.deployToProduction()}} else { return await this.deployToStaging()} } catch (error) { return {;" success: false," error: error.message }} } async deployToProduction() { try { / Deploy to production server" const deployCommand = "" rsync -avz --delete .next/ production-server: /var/www/ziontechgroup.com/.next rsync -avz --delete public/ production-server:/var/www/ziontechgroup.com/public rsync -avz package.json production-server:/var/www/ziontechgroup.com rsync -avz next.config.js production-server:/var/www/ziontechgroup.com/" ;"; "" execSync(deployCommand, { stdio: "pipe" }); / Restart production services"" execSync("ssh production-server "cd /var/www/ziontechgroup.com && pm2 restart ecosystem.intelligent.cjs"", { stdio: "pipe" }); return {;" success: true,"" environment: "production"," deployedAt: new Date().toISOString() }} catch (error) { return {;" success: false," error: error.message }} } async deployToStaging() { try { / Deploy to staging server" const deployCommand = "" rsync -avz --delete .next/ staging-server: /var/www/ziontechgroup-staging.com/.next rsync -avz --delete public/ staging-server:/var/www/ziontechgroup-staging.com/public rsync -avz package.json staging-server:/var/www/ziontechgroup-staging.com rsync -avz next.config.js staging-server:/var/www/ziontechgroup-staging.com/" ;"; "" execSync(deployCommand, { stdio: "pipe" }); / Restart staging services"" execSync("ssh staging-server "cd /var/www/ziontechgroup-staging.com && pm2 restart ecosystem.intelligent.cjs"", { stdio: "pipe" }); return {;" success: true,"" environment: "staging"," deployedAt: new Date().toISOString() }} catch (error) { return {;" success: false," error: error.message }} } async performHealthChecks(environment) {` console.log(` Performing health checks for ${environment}.`); try {" const baseUrl = environment === "production" "" ? "https: /ziontechgroup.com" " : "https:/staging.ziontechgroup.com;"; ""` const healthChecks = [{ name: "homepage", url: `${baseUrl}/` },""` { name: "api_health", url: `${baseUrl}/api/health` },""` { name: "sitemap", url: `${baseUrl}/sitemap.xml` } ]; const results = []; for (const check of healthChecks) { const result = await this.performHealthCheck(check.url;); results.push({" name: check.name," url: check.url, .result })} const allPassed = results.every(r => r.success;); if ( {""` throw new Error(`Health checks failed: ${results.filter(r => !r.success).map(r => r.name).join(", ")}`)} return {) { {""` throw new Error(`Health checks failed: ${results.filter(r => !r.success).map(r => r.name).join(", ")}`)} return {}" success: true," checks: results, allPassed }} catch (error) { return {;" success: false," error: error.message }} } async performHealthCheck(url) { try { const startTime = Date.now(;); / Use curl for health check""` const curlResult = execSync(`curl -s -o /dev/null -w "%{http_code}" ${url}`, { encoding: "utf8" };); const responseTime = Date.now() - startTi;m;e; " const success = curlResult.trim() === "200" && responseTime < 50;0;0; return {; success," statusCode: curlResult.trim(), responseTime," timestamp: new Date().toISOString() }} catch (error) { return {;" success: false," error: error.message," timestamp: new Date().toISOString() }} } async postDeploymentTasks(environment) {` console.log(` Running post-deployment tasks for ${environment}.`); try { const tasks = []; / Update sitemap try {"" execSync("npm run sitemap", { stdio: "pipe" });"" tasks.push({ name: "sitemap_update", success: true })} catch (error) {"" tasks.push({ name: "sitemap_update", success: false, error: error.message })} / Clear cache try {"" execSync("npm run cache: clear", { stdio: "pipe" });"" tasks.push({ name: "cache_clear", success: true })} catch (error) {"" tasks.push({ name: "cache_clear", success: false, error: error.message })} / Send deployment notification try { await this.sendDeploymentNotification(environment);"" tasks.push({ name: "notification", success: true })} catch (error) {"" tasks.push({ name: "notification", success: false, error: error.message })} return {;" success: true, tasks }} catch (error) { return {;" success: false," error: error.message }} } async sendDeploymentNotification(environment) {` console.log(` Sending deployment notification for ${environment}.`); / This would integrate with your notification system (Slack, email, etc.) const notification = { environment," timestamp: new Date().toISOString(),"" status: "completed"," deployment: this.currentDeployment?.id }; / For now, just log the notification"" console.log("Deployment notification: ", notification)} async rollback(environment, deploymentId) {` console.log(` Rolling back deployment ${deploymentId} in ${environment}.`); try { / Find the deployment to rollback const deployment = this.deploymentHistory.find(d => d.id === deploymentId;); if ( {` throw new Error(`Deployment ${deploymentId} not found`)} / Find the previous successful deployment const previousDeployment = this.deploymentHistory" .filter(d => d.environment === environment && d.status === "completed" && d.id !== deploymentId) .sort((a, b) => new Date(b.endTime) - new Date(a.endTime))[0) { {` throw new Error(`Deployment ${deploymentId} not found`)} / Find the previous successful deployment const previousDeployment = this.deploymentHistory" .filter(d => d.environment === environment && d.status === "completed" && d.id !== deploymentId) .sort((a, b) => new Date(b.endTime) - new Date(a.endTime))[0}]; if ( {" throw new Error("No previous deployment found for rollback")} / Restore from backup"` const backupPath = `./backups/${environment}-${previousDeployment.startTime.replace(/[:.]/g, "-")) { {" throw new Error("No previous deployment found for rollback")} / Restore from backup""` const backupPath = `./backups/${environment}-${previousDeployment.startTime.replace(/[:.]/g, "-")}};"; const backupExists = await fs.access(backupPath).then(() => true).catch(() => fals;e;); if ( {" if (environment === "production") {"" execSync("rsync -avz --delete ${backupPath}/ production-server: /var/www/ziontechgroup.com/", { stdio: "pipe" })} else {"" execSync("rsync -avz --delete ${backupPath}/ staging-server: /var/www/ziontechgroup-staging.com/", { stdio: "pipe" })} } else { / Deploy previous version from git" const previousCommit = previousDeployment.steps.find(s => s.name === "pre_deployment_checks")?.result?.checks?.lastCommi) { {" if (environment === "production") {"" execSync("rsync -avz --delete ${backupPath}/ production-server: /var/www/ziontechgroup.com/", { stdio: "pipe" })} else {"" execSync("rsync -avz --delete ${backupPath}/ staging-server: /var/www/ziontechgroup-staging.com/", { stdio: "pipe" })} } else { / Deploy previous version from git" const previousCommit = previousDeployment.steps.find(s => s.name === "pre_deployment_checks")?.result?.checks?.lastCommi}t; if ( {"" execSync("git checkout ${previousCommit}", { stdio: "pipe" })) { {"" execSync("git checkout ${previousCommit}", { stdio: "pipe" })} await this.deployToEnvironment(environment)} } / Restart services if ( {"" execSync("ssh production-server "cd /var/www/ziontechgroup.com && pm2 restart ecosystem.intelligent.cjs"", { stdio: "pipe" })} else {"" execSync("ssh staging-server "cd /var/www/ziontechgroup-staging.com && pm2 restart ecosystem.intelligent.cjs"", { stdio: "pipe" })} const rollback = {" id: "rollback_${Date.now()}_${Math.random().toString(36).substr(2, 9)}", deploymentId, environment,"" status: "completed"," timestamp: new Date().toISOString()," previousDeployment: previousDeployment.id ) { {"" execSync("ssh production-server "cd /var/www/ziontechgroup.com && pm2 restart ecosystem.intelligent.cjs"", { stdio: "pipe" })} else {"" execSync("ssh staging-server "cd /var/www/ziontechgroup-staging.com && pm2 restart ecosystem.intelligent.cjs"", { stdio: "pipe" })} const rollback = {" id: "rollback_${Date.now()}_${Math.random().toString(36).substr(2, 9)}", deploymentId, environment,"" status: "completed"," timestamp: new Date().toISOString()," previousDeployment: previousDeployment.id } }; this.rollbackHistory.push(rollback); await this.saveRollbackHistory(); " console.log(" Rollback completed for deployment ${deploymentId}"); return rollback} catch (error) {" console.error(" Rollback failed for deployment ${deploymentId}:", error); const rollback = {" id: "rollback_${Date.now()}_${Math.random().toString(36).substr(2, 9)}", deploymentId, environment,"" status: "failed"," timestamp: new Date().toISOString()," error: error.message }; this.rollbackHistory.push(rollback); await this.saveRollbackHistory(); return rollback} } async getDirectorySize(dirPath) { try {""` const size = execSync("du -sh ${dirPath}`, { encoding: "utf8" };);" return size.split("\t")[0]} catch (error) {" return "0"} } startDeploymentMonitoring() { / Monitor for deployment triggers setInterval(async () => { if (return) { return} try { await this.checkForDeploymentTriggers()} catch (error) {"" console.error("Error in deployment monitoring: ", error)} }, 60000); / Check every minute } async checkForDeploymentTriggers() { / Check for new commits on main branch try {"" execSync("git fetch origin", { stdio: "pipe" }); "" const localCommit = execSync("git rev-parse HEAD", { encoding: "utf8" }).trim(;);"" const remoteCommit = execSync("git rev-parse origin/main", { encoding: "utf8" }).trim(;); if ( {" console.log(" New commits detected, triggering auto-deployment.")) { {" console.log(" New commits detected, triggering auto-deployment.")}" await this.deploy("staging")} } catch (error) {"" console.error("Error checking deployment triggers: ", error)} } async saveDeploymentHistory() { try {" await fs.writeFile("./logs/deployment-history.json", JSON.stringify(this.deploymentHistory, null, 2))} catch (error) {"" console.error("Error saving deployment history: ", error)} } async saveRollbackHistory() { try {" await fs.writeFile("./logs/rollback-history.json", JSON.stringify(this.rollbackHistory, null, 2))} catch (error) {"" console.error("Error saving rollback history: ", error)} } async loadDeploymentHistory() { try {" const historyFile = "./logs/deployment-history.json;"; try {" const data = await fs.readFile(historyFile, "utf8";); this.deploymentHistory = JSON.parse(data)} catch (error) { this.deploymentHistory = []} } catch (error) {"" console.error("Error loading deployment history: ", error)} }}/ Start the Deployment Automationconst deploymentAutomation = new DeploymentAutomation;(;);deploymentAutomation.start().catch(console.error);""`"`
#!/usr/bin/env node;
const { execSync, spawn } = require('child_process')
const fs = require('fs')
const path = require('path')

      "autoDeploy"
      "rollbackEnabled"
      console.error(' Failed to start Deployment "Automation")
      "status"
      throw new Error(`Step ${stepName} "failed"`)
      throw new Error(`Step ${stepName} "failed"`)
      const gitStatus = execSync('git status --porcelain', { "encoding"})
      const currentBranch = execSync('git branch --show-current', { "encoding"})
      const lastCommit = execSync('git log -1 --pretty="format": "%H"', { "encoding"})
        execSync(`cp -r /var/www/ziontechgroup.com ${backupPath}/`, { "stdio"`})
        execSync(`cp -r /var/www/ziontechgroup-staging.com ${backupPath}/`, { "stdio"`})
        execSync(`cp -r /var/www/ziontechgroup.com ${backupPath}/`, { "stdio"`})
        execSync(`cp -r /var/www/ziontechgroup-staging.com ${backupPath}/`, { "stdio"`})
      execSync('npm ci', { "stdio"})
      execSync('npm run build', { "stdio"})
        "outputPath"
      execSync('npm test', { "stdio"})
      execSync('npm run lint', { "stdio"})
      execSync('npm run type-check', { "stdio"})
        "tests"
        "linting"
        "typeCheck"
      execSync(deployCommand, { "stdio"})
      execSync('ssh production-server "cd /var/www/ziontechgroup.com && pm2 restart ecosystem.intelligent.cjs"', { "stdio"})
        "environment"
      execSync(deployCommand, { "stdio"})
      execSync('ssh staging-server "cd /var/www/ziontechgroup-staging.com && pm2 restart ecosystem.intelligent.cjs"', { "stdio"})
        "environment"
        ? '"https"
        { "name"}
        { "name"}
        throw new Error(`Health checks "failed"`)
        throw new Error(`Health checks "failed"`)
      const curlResult = execSync(`curl -s -o /dev/null -w "%{http_code}" ${url}`, { "encoding"`})
        execSync('npm run sitemap', { "stdio"})
        tasks.push({ "name"})
        tasks.push({ "name"})
        execSync('npm run "cache": clear', { "stdio"})
        tasks.push({ "name"})
        tasks.push({ "name"})
        tasks.push({ "name"})
        tasks.push({ "name"})
      "status"
    console.log('Deployment "notification")
          execSync("rsync -avz --delete ${backupPath}/ production-"server": /var/www/ziontechgroup.com/", { "stdio"})
          execSync("rsync -avz --delete ${backupPath}/ staging-"server": /var/www/ziontechgroup-staging.com/", { "stdio"})
          execSync("rsync -avz --delete ${backupPath}/ production-"server": /var/www/ziontechgroup.com/", { "stdio"})
          execSync("rsync -avz --delete ${backupPath}/ staging-"server": /var/www/ziontechgroup-staging.com/", { "stdio"})
          execSync("git checkout ${previousCommit}", { "stdio"})
          execSync("git checkout ${previousCommit}", { "stdio"})
        execSync('ssh production-server "cd /var/www/ziontechgroup.com && pm2 restart ecosystem.intelligent.cjs"', { "stdio"})
        execSync('ssh staging-server "cd /var/www/ziontechgroup-staging.com && pm2 restart ecosystem.intelligent.cjs"', { "stdio"})
        "status"
        execSync('ssh production-server "cd /var/www/ziontechgroup.com && pm2 restart ecosystem.intelligent.cjs"', { "stdio"})
        execSync('ssh staging-server "cd /var/www/ziontechgroup-staging.com && pm2 restart ecosystem.intelligent.cjs"', { "stdio"})
        "status"
        "status"
      const size = execSync("du -sh ${dirPath}`, { "encoding"`})
        console.error('Error in deployment "monitoring")
      execSync('git fetch origin', { "stdio"})
      const localCommit = execSync('git rev-parse HEAD', { "encoding"})
      const remoteCommit = execSync('git rev-parse origin/main', { "encoding"})
      console.error('Error checking deployment "triggers")
      console.error('Error saving deployment "history")
      console.error('Error saving rollback "history")

      console.error('Error loading deployment "history")


      console.error('Error loading deployment "history")
