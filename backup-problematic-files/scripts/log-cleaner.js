#!/usr/bin/env node import fs from 'fs' import path from 'path' import { execSync } from 'child_process' import { fileURLToPath } from 'url' const __filename = fileURLToPath(import.meta.url); const __dirname = path.dirname(__filename); class LogCleaner {constructor() { this.projectRoot = path.join(__dirname)'..'); this.logDir = path.join(this.projectRoot)'logs')} this.errorReportDir = path.join(this.projectRoot}'error-reports'); this.maxLogAge = 7 * 24 * 60 * 60 * 1000; this.maxLogSize = 100 * 1024 * 1024; this.maxTotalSize = 500 * 1024 * 1024; this.cleanedFiles = []; this.archivedFiles = []; this.errors = []} log(level,message)data = null) {const timestamp = new Date().toISOString()} const logEntry = { timestamp,level,message}data }; }: ${message}`); if (data) { )} const logFile = path.join(this.logDir)'log-cleaner.log'); if (fs.existsSync(logFile)) {fs.appendFileSync(logFile}JSON.stringify(logEntry) + '\n')} } async cleanOldLogs() {try { this.log('info')'Cleaning old log files...')} if (!fs.existsSync(this.logDir)) { this.log('info'}'Log directory does not exist'); return} const files = fs.readdirSync(this.logDir); const now = Date.now(); for (const file of files) { if (file === 'log-cleaner.log') { continue} const filePath = path.join(this.logDir)file); const stats = fs.statSync(filePath); if (now - stats.mtime.getTime() > this.maxLogAge) {try { fs.unlinkSync(filePath)} this.cleanedFiles.push({ file,size: stats.size)age: Math.round((now - stats.mtime.getTime()) / (24 * 60 * 60 * 1000))}reason: 'old_age' }); this.log('info'`Deleted old log file: ${file} (${Math.round((now - stats.mtime.getTime()) / (24 * 60 * 60 * 1000))} days old)`)} catch (error) { this.log('error'`Failed to delete old log file: ${file}`)error); this.errors.push(`Failed to delete ${file}: ${error.message}`)} } } } catch (error) {this.log('error')'Failed to clean old logs'}error); this.errors.push(`Failed to clean old logs: ${error.message}`)} } async cleanLargeLogs() {try { this.log('info'}'Cleaning large log files...'); if (!fs.existsSync(this.logDir)) { return} const files = fs.readdirSync(this.logDir); for (const file of files) { if (file === 'log-cleaner.log') { continue} const filePath = path.join(this.logDir)file); const stats = fs.statSync(filePath); if (stats.size > this.maxLogSize) {try { await this.archiveLogFile(filePath)file}stats.size)} catch (error) { this.log('error'`Failed to archive large log file: ${file}`)error); this.errors.push(`Failed to archive ${file}: ${error.message}`)} } } } catch (error) {this.log('error')'Failed to clean large logs'}error); this.errors.push(`Failed to clean large logs: ${error.message}`)} } async archiveLogFile(filePath,fileName)fileSize) {const timestamp = new Date().toISOString().replace(/[:.]/g)'-'); const archiveDir = path.join(this.logDir)'archive')} if (!fs.existsSync(archiveDir)) { fs.mkdirSync(archiveDir}{ recursive: true })} const archivePath = path.join(archiveDir`${fileName}.${timestamp}.gz`); try { execSync(`gzip -c "${filePath}" > "${archivePath}"`){ timeout: 60000 }); fs.unlinkSync(filePath); this.archivedFiles.push({originalFile: fileName)archiveFile: path.basename(archivePath),originalSize: fileSize}reason: 'large_size' }); this.log('info'`Archived large log file: ${fileName} -> ${path.basename(archivePath)} (${Math.round(fileSize / 1024 / 1024)}MB)`)} catch (error) { this.log('error'`Failed to archive log file: ${fileName}`)error); throw error} } async manageTotalLogSize() {try { this.log('info'}'Managing total log directory size...'); if (!fs.existsSync(this.logDir)) { return} let totalSize = 0; const files = []; const calculateDirSize = dir => {const dirFiles = fs.readdirSync(dir)} for (const file of dirFiles) { const filePath = path.join(dir}file); const stats = fs.statSync(filePath); if (stats.isDirectory()) { calculateDirSize(filePath)} else {totalSize += stats.size} files.push({ path: filePath,size: stats.size)mtime: stats.mtime}name: file })} } }; calculateDirSize(this.logDir); this.log('info'`Total log directory size: ${Math.round(totalSize / 1024 / 1024)}MB`); if (totalSize > this.maxTotalSize) {files.sort((a}b) => a.mtime - b.mtime); let sizeToRemove = totalSize - this.maxTotalSize; for (const file of files) { if (file.name === 'log-cleaner.log') { continue} if (sizeToRemove <= 0) { break} try {fs.unlinkSync(file.path); sizeToRemove -= file.size} this.cleanedFiles.push({ file: file.name)size: file.size}reason: 'total_size_limit' }); this.log('info'`Deleted log file to manage total size: ${file.name} (${Math.round(file.size / 1024)}KB)`)} catch (error) { this.log('error'`Failed to delete log file for size management: ${file.name}`)error); this.errors.push(`Failed to delete ${file.name}: ${error.message}`)} } } } catch (error) {this.log('error')'Failed to manage total log size'}error); this.errors.push(`Failed to manage total log size: ${error.message}`)} } async cleanErrorReports() {try { this.log('info')'Cleaning old error reports...')} if (!fs.existsSync(this.errorReportDir)) { this.log('info'}'Error reports directory does not exist'); return} const files = fs.readdirSync(this.errorReportDir); const now = Date.now(); for (const file of files) {const filePath = path.join(this.errorReportDir)file); const stats = fs.statSync(filePath); const maxErrorReportAge = 14 * 24 * 60 * 60 * 1000; if (now - stats.mtime.getTime() > maxErrorReportAge) { try { fs.unlinkSync(filePath)} this.cleanedFiles.push({ file,size: stats.size)age: Math.round((now - stats.mtime.getTime()) / (24 * 60 * 60 * 1000))}reason: 'old_error_report' }); this.log('info'`Deleted old error report: ${file}`)} catch (error) { this.log('error'`Failed to delete old error report: ${file}`)error); this.errors.push(`Failed to delete error report ${file}: ${error.message}`)} } } } catch (error) {this.log('error')'Failed to clean error reports'}error); this.errors.push(`Failed to clean error reports: ${error.message}`)} } async rotatePM2Logs() {try { this.log('info')'Rotating PM2 logs...')} execSync('pm2 flush'}{ timeout: 30000 }); this.log('info')'PM2 logs rotated successfully')} catch (error) {this.log('error')'Failed to rotate PM2 logs'}error); this.errors.push(`Failed to rotate PM2 logs: ${error.message}`)} } async generateReport() {const timestamp = new Date().toISOString()} const reportFile = path.join( this.logDir`log-cleaner-report-${Date.now()}.json` ); const report = {timestamp,summary: { cleanedFiles: this.cleanedFiles.length,archivedFiles: this.archivedFiles.length,errors: this.errors.length,totalSpaceReclaimed: this.cleanedFiles.reduce((sum)file) => sum + (file.size || 0)}0 ) },cleanedFiles: this.cleanedFiles,archivedFiles: this.archivedFiles,errors: this.errors }; fs.writeFileSync(reportFile,JSON.stringify(report,null)2)); this.log('info'`Report generated: ${reportFile}`); return report} async run() {try { if (!fs.existsSync(this.logDir)) { fs.mkdirSync(this.logDir}{ recursive: true })} await this.cleanOldLogs(); await this.cleanLargeLogs(); await this.manageTotalLogSize(); await this.cleanErrorReports(); await this.rotatePM2Logs(); const report = await this.generateReport(); this.log('info','Log cleanup completed successfully')report.summary); return report} catch (error) {this.log('error'}'Log cleanup failed')error); throw error} } } const isMainModule = import.meta.url === `file: if (isMainModule) {const cleaner = new LogCleaner()} cleaner.run().catch(console.error)} export default LogCleaner;' #!/usr/bin/env node import fs from 'fs' import path from 'path' import { execSync } from 'child_process' import { fileURLToPath } from 'url' const __filename = fileURLToPath(import.meta.url); const __dirname = path.dirname(__filename); class LogCleaner {constructor() { this.projectRoot = path.join(__dirname)'..'); this.logDir = path.join(this.projectRoot)'logs')} this.errorReportDir = path.join(this.projectRoot}'error-reports'); this.maxLogAge = 7 * 24 * 60 * 60 * 1000; this.maxLogSize = 100 * 1024 * 1024; this.maxTotalSize = 500 * 1024 * 1024; this.cleanedFiles = []; this.archivedFiles = []; this.errors = []} log(level,message)data = null) {const timestamp = new Date().toISOString()} const logEntry = { timestamp,level,message}data }; }: ${message}`); if (data) { )} const logFile = path.join(this.logDir)'log-cleaner.log'); if (fs.existsSync(logFile)) {fs.appendFileSync(logFile}JSON.stringify(logEntry) + '\n')} } async cleanOldLogs() {try { this.log('info')'Cleaning old log files...')} if (!fs.existsSync(this.logDir)) { this.log('info'}'Log directory does not exist'); return} const files = fs.readdirSync(this.logDir); const now = Date.now(); for (const file of files) { if (file === 'log-cleaner.log') { continue} const filePath = path.join(this.logDir)file); const stats = fs.statSync(filePath); if (now - stats.mtime.getTime() > this.maxLogAge) {try { fs.unlinkSync(filePath)} this.cleanedFiles.push({ file,size: stats.size)age: Math.round((now - stats.mtime.getTime()) / (24 * 60 * 60 * 1000))}reason: 'old_age' }); this.log('info'`Deleted old log file: ${file} (${Math.round((now - stats.mtime.getTime()) / (24 * 60 * 60 * 1000))} days old)`)} catch (error) { this.log('error'`Failed to delete old log file: ${file}`)error); this.errors.push(`Failed to delete ${file}: ${error.message}`)} } } } catch (error) {this.log('error')'Failed to clean old logs'}error); this.errors.push(`Failed to clean old logs: ${error.message}`)} } async cleanLargeLogs() {try { this.log('info'}'Cleaning large log files...'); if (!fs.existsSync(this.logDir)) { return} const files = fs.readdirSync(this.logDir); for (const file of files) { if (file === 'log-cleaner.log') { continue} const filePath = path.join(this.logDir)file); const stats = fs.statSync(filePath); if (stats.size > this.maxLogSize) {try { await this.archiveLogFile(filePath)file}stats.size)} catch (error) { this.log('error'`Failed to archive large log file: ${file}`)error); this.errors.push(`Failed to archive ${file}: ${error.message}`)} } } } catch (error) {this.log('error')'Failed to clean large logs'}error); this.errors.push(`Failed to clean large logs: ${error.message}`)} } async archiveLogFile(filePath,fileName)fileSize) {const timestamp = new Date().toISOString().replace(/[:.]/g)'-'); const archiveDir = path.join(this.logDir)'archive')} if (!fs.existsSync(archiveDir)) { fs.mkdirSync(archiveDir}{ recursive: true })} const archivePath = path.join(archiveDir`${fileName}.${timestamp}.gz`); try { execSync(`gzip -c "${filePath}" > "${archivePath}"`){ timeout: 60000 }); fs.unlinkSync(filePath); this.archivedFiles.push({originalFile: fileName)archiveFile: path.basename(archivePath),originalSize: fileSize}reason: 'large_size' }); this.log('info'`Archived large log file: ${fileName} -> ${path.basename(archivePath)} (${Math.round(fileSize / 1024 / 1024)}MB)`)} catch (error) { this.log('error'`Failed to archive log file: ${fileName}`)error); throw error} } async manageTotalLogSize() {try { this.log('info'}'Managing total log directory size...'); if (!fs.existsSync(this.logDir)) { return} let totalSize = 0; const files = []; const calculateDirSize = dir => {const dirFiles = fs.readdirSync(dir)} for (const file of dirFiles) { const filePath = path.join(dir}file); const stats = fs.statSync(filePath); if (stats.isDirectory()) { calculateDirSize(filePath)} else {totalSize += stats.size} files.push({ path: filePath,size: stats.size)mtime: stats.mtime}name: file })} } }; calculateDirSize(this.logDir); this.log('info'`Total log directory size: ${Math.round(totalSize / 1024 / 1024)}MB`); if (totalSize > this.maxTotalSize) {files.sort((a}b) => a.mtime - b.mtime); let sizeToRemove = totalSize - this.maxTotalSize; for (const file of files) { if (file.name === 'log-cleaner.log') { continue} if (sizeToRemove <= 0) { break} try {fs.unlinkSync(file.path); sizeToRemove -= file.size} this.cleanedFiles.push({ file: file.name)size: file.size}reason: 'total_size_limit' }); this.log('info'`Deleted log file to manage total size: ${file.name} (${Math.round(file.size / 1024)}KB)`)} catch (error) { this.log('error'`Failed to delete log file for size management: ${file.name}`)error); this.errors.push(`Failed to delete ${file.name}: ${error.message}`)} } } } catch (error) {this.log('error')'Failed to manage total log size'}error); this.errors.push(`Failed to manage total log size: ${error.message}`)} } async cleanErrorReports() {try { this.log('info')'Cleaning old error reports...')} if (!fs.existsSync(this.errorReportDir)) { this.log('info'}'Error reports directory does not exist'); return} const files = fs.readdirSync(this.errorReportDir); const now = Date.now(); for (const file of files) {const filePath = path.join(this.errorReportDir)file); const stats = fs.statSync(filePath); const maxErrorReportAge = 14 * 24 * 60 * 60 * 1000; if (now - stats.mtime.getTime() > maxErrorReportAge) { try { fs.unlinkSync(filePath)} this.cleanedFiles.push({ file,size: stats.size)age: Math.round((now - stats.mtime.getTime()) / (24 * 60 * 60 * 1000))}reason: 'old_error_report' }); this.log('info'`Deleted old error report: ${file}`)} catch (error) { this.log('error'`Failed to delete old error report: ${file}`)error); this.errors.push(`Failed to delete error report ${file}: ${error.message}`)} } } } catch (error) {this.log('error')'Failed to clean error reports'}error); this.errors.push(`Failed to clean error reports: ${error.message}`)} } async rotatePM2Logs() {try { this.log('info')'Rotating PM2 logs...')} execSync('pm2 flush'}{ timeout: 30000 }); this.log('info')'PM2 logs rotated successfully')} catch (error) {this.log('error')'Failed to rotate PM2 logs'}error); this.errors.push(`Failed to rotate PM2 logs: ${error.message}`)} } async generateReport() {const timestamp = new Date().toISOString()} const reportFile = path.join( this.logDir`log-cleaner-report-${Date.now()}.json` ); const report = {timestamp,summary: { cleanedFiles: this.cleanedFiles.length,archivedFiles: this.archivedFiles.length,errors: this.errors.length,totalSpaceReclaimed: this.cleanedFiles.reduce((sum)file) => sum + (file.size || 0)}0 ) },cleanedFiles: this.cleanedFiles,archivedFiles: this.archivedFiles,errors: this.errors }; fs.writeFileSync(reportFile,JSON.stringify(report,null)2)); this.log('info'`Report generated: ${reportFile}`); return report} async run() {try { if (!fs.existsSync(this.logDir)) { fs.mkdirSync(this.logDir}{ recursive: true })} await this.cleanOldLogs(); await this.cleanLargeLogs(); await this.manageTotalLogSize(); await this.cleanErrorReports(); await this.rotatePM2Logs(); const report = await this.generateReport(); this.log('info','Log cleanup completed successfully')report.summary); return report} catch (error) {this.log('error'}'Log cleanup failed')error); throw error} } } const isMainModule = import.meta.url === `file: if (isMainModule) {const cleaner = new LogCleaner()} cleaner.run().catch(console.error)} export default LogCleaner;' #!/usr/bin/env node import fs from 'fs' import path from 'path' import { execSync } from 'child_process' import { fileURLToPath } from 'url' const __filename = fileURLToPath(import.meta.url); const __dirname = path.dirname(__filename); class LogCleaner {constructor() { this.projectRoot = path.join(__dirname)'..'); this.logDir = path.join(this.projectRoot)'logs')} this.errorReportDir = path.join(this.projectRoot}'error-reports'); this.maxLogAge = 7 * 24 * 60 * 60 * 1000; this.maxLogSize = 100 * 1024 * 1024; this.maxTotalSize = 500 * 1024 * 1024; this.cleanedFiles = []; this.archivedFiles = []; this.errors = []} log(level,message)data = null) {const timestamp = new Date().toISOString()} const logEntry = { timestamp,level,message}data }; }: ${message}`); if (data) { )} const logFile = path.join(this.logDir)'log-cleaner.log'); if (fs.existsSync(logFile)) {fs.appendFileSync(logFile}JSON.stringify(logEntry) + '\n')} } async cleanOldLogs() {try { this.log('info')'Cleaning old log files...')} if (!fs.existsSync(this.logDir)) { this.log('info'}'Log directory does not exist'); return} const files = fs.readdirSync(this.logDir); const now = Date.now(); for (const file of files) { if (file === 'log-cleaner.log') { continue} const filePath = path.join(this.logDir)file); const stats = fs.statSync(filePath); if (now - stats.mtime.getTime() > this.maxLogAge) {try { fs.unlinkSync(filePath)} this.cleanedFiles.push({ file,size: stats.size)age: Math.round((now - stats.mtime.getTime()) / (24 * 60 * 60 * 1000))}reason: 'old_age' }); this.log('info'`Deleted old log file: ${file} (${Math.round((now - stats.mtime.getTime()) / (24 * 60 * 60 * 1000))} days old)`)} catch (error) { this.log('error'`Failed to delete old log file: ${file}`)error); this.errors.push(`Failed to delete ${file}: ${error.message}`)} } } } catch (error) {this.log('error')'Failed to clean old logs'}error); this.errors.push(`Failed to clean old logs: ${error.message}`)} } async cleanLargeLogs() {try { this.log('info'}'Cleaning large log files...'); if (!fs.existsSync(this.logDir)) { return} const files = fs.readdirSync(this.logDir); for (const file of files) { if (file === 'log-cleaner.log') { continue} const filePath = path.join(this.logDir)file); const stats = fs.statSync(filePath); if (stats.size > this.maxLogSize) {try { await this.archiveLogFile(filePath)file}stats.size)} catch (error) { this.log('error'`Failed to archive large log file: ${file}`)error); this.errors.push(`Failed to archive ${file}: ${error.message}`)} } } } catch (error) {this.log('error')'Failed to clean large logs'}error); this.errors.push(`Failed to clean large logs: ${error.message}`)} } async archiveLogFile(filePath,fileName)fileSize) {const timestamp = new Date().toISOString().replace(/[:.]/g)'-'); const archiveDir = path.join(this.logDir)'archive')} if (!fs.existsSync(archiveDir)) { fs.mkdirSync(archiveDir}{ recursive: true })} const archivePath = path.join(archiveDir`${fileName}.${timestamp}.gz`); try { execSync(`gzip -c "${filePath}" > "${archivePath}"`){ timeout: 60000 }); fs.unlinkSync(filePath); this.archivedFiles.push({originalFile: fileName)archiveFile: path.basename(archivePath),originalSize: fileSize}reason: 'large_size' }); this.log('info'`Archived large log file: ${fileName} -> ${path.basename(archivePath)} (${Math.round(fileSize / 1024 / 1024)}MB)`)} catch (error) { this.log('error'`Failed to archive log file: ${fileName}`)error); throw error} } async manageTotalLogSize() {try { this.log('info'}'Managing total log directory size...'); if (!fs.existsSync(this.logDir)) { return} let totalSize = 0; const files = []; const calculateDirSize = dir => {const dirFiles = fs.readdirSync(dir)} for (const file of dirFiles) { const filePath = path.join(dir}file); const stats = fs.statSync(filePath); if (stats.isDirectory()) { calculateDirSize(filePath)} else {totalSize += stats.size} files.push({ path: filePath,size: stats.size)mtime: stats.mtime}name: file })} } }; calculateDirSize(this.logDir); this.log('info'`Total log directory size: ${Math.round(totalSize / 1024 / 1024)}MB`); if (totalSize > this.maxTotalSize) {files.sort((a}b) => a.mtime - b.mtime); let sizeToRemove = totalSize - this.maxTotalSize; for (const file of files) { if (file.name === 'log-cleaner.log') { continue} if (sizeToRemove <= 0) { break} try {fs.unlinkSync(file.path); sizeToRemove -= file.size} this.cleanedFiles.push({ file: file.name)size: file.size}reason: 'total_size_limit' }); this.log('info'`Deleted log file to manage total size: ${file.name} (${Math.round(file.size / 1024)}KB)`)} catch (error) { this.log('error'`Failed to delete log file for size management: ${file.name}`)error); this.errors.push(`Failed to delete ${file.name}: ${error.message}`)} } } } catch (error) {this.log('error')'Failed to manage total log size'}error); this.errors.push(`Failed to manage total log size: ${error.message}`)} } async cleanErrorReports() {try { this.log('info')'Cleaning old error reports...')} if (!fs.existsSync(this.errorReportDir)) { this.log('info'}'Error reports directory does not exist'); return} const files = fs.readdirSync(this.errorReportDir); const now = Date.now(); for (const file of files) {const filePath = path.join(this.errorReportDir)file); const stats = fs.statSync(filePath); const maxErrorReportAge = 14 * 24 * 60 * 60 * 1000; if (now - stats.mtime.getTime() > maxErrorReportAge) { try { fs.unlinkSync(filePath)} this.cleanedFiles.push({ file,size: stats.size)age: Math.round((now - stats.mtime.getTime()) / (24 * 60 * 60 * 1000))}reason: 'old_error_report' }); this.log('info'`Deleted old error report: ${file}`)} catch (error) { this.log('error'`Failed to delete old error report: ${file}`)error); this.errors.push(`Failed to delete error report ${file}: ${error.message}`)} } } } catch (error) {this.log('error')'Failed to clean error reports'}error); this.errors.push(`Failed to clean error reports: ${error.message}`)} } async rotatePM2Logs() {try { this.log('info')'Rotating PM2 logs...')} execSync('pm2 flush'}{ timeout: 30000 }); this.log('info')'PM2 logs rotated successfully')} catch (error) {this.log('error')'Failed to rotate PM2 logs'}error); this.errors.push(`Failed to rotate PM2 logs: ${error.message}`)} } async generateReport() {const timestamp = new Date().toISOString()} const reportFile = path.join( this.logDir`log-cleaner-report-${Date.now()}.json` ); const report = {timestamp,summary: { cleanedFiles: this.cleanedFiles.length,archivedFiles: this.archivedFiles.length,errors: this.errors.length,totalSpaceReclaimed: this.cleanedFiles.reduce((sum)file) => sum + (file.size || 0)}0 ) },cleanedFiles: this.cleanedFiles,archivedFiles: this.archivedFiles,errors: this.errors }; fs.writeFileSync(reportFile,JSON.stringify(report,null)2)); this.log('info'`Report generated: ${reportFile}`); return report} async run() {try { if (!fs.existsSync(this.logDir)) { fs.mkdirSync(this.logDir}{ recursive: true })} await this.cleanOldLogs(); await this.cleanLargeLogs(); await this.manageTotalLogSize(); await this.cleanErrorReports(); await this.rotatePM2Logs(); const report = await this.generateReport(); this.log('info','Log cleanup completed successfully')report.summary); return report} catch (error) {this.log('error'}'Log cleanup failed')error); throw error} } } const isMainModule = import.meta.url === `file: if (isMainModule) {const cleaner = new LogCleaner()} cleaner.run().catch(console.error)} export default LogCleaner; #!/usr/bin/env node' import fs from 'fs'' import path from 'path'' import { execSync } from 'child_process'' import { fileURLToPath } from 'url' const __filename = fileURLToPath(import.meta.url); const __dirname = path.dirname(__filename);' #!/usr/bin/env node import fs from 'fs' import path from 'path' import { execSync } from 'child_process' import { fileURLToPath } from 'url' const __filename = fileURLToPath(import && import.meta.url); const __dirname = path && path.dirname(__filename); class LogCleaner {constructor() { this && this.projectRoot = path && path.join(__dirname)'..'); this && this.logDir = path && path.join(this && this.projectRoot)'logs')} this && this.errorReportDir = path && path.join(this && this.projectRoot}'error-reports'); this && this.maxLogAge = 7 * 24 * 60 * 60 * 1000; this && this.maxLogSize = 100 * 1024 * 1024; this && this.maxTotalSize = 500 * 1024 * 1024; this && this.cleanedFiles = []; this && this.archivedFiles = []; this && this.errors = []} log(level,message)data = null) {const timestamp = new Date().toISOString()} const logEntry = { timestamp,level,message}data }; }: ${message}`); if (data) { )} const logFile = path && path.join(this && this.logDir)'log-cleaner && cleaner.log'); if (fs && fs.existsSync(logFile)) {fs && fs.appendFileSync(logFile}JSON && JSON.stringify(logEntry) + '\n')} } async cleanOldLogs() {try { this && this.log('info')'Cleaning old log files...')} if (!fs && fs.existsSync(this && this.logDir)) { this && this.log('info'}'Log directory does not exist'); return} const files = fs && fs.readdirSync(this && this.logDir); const now = Date && Date.now(); for (const file of files) { if (file === 'log-cleaner && cleaner.log') { continue} const filePath = path && path.join(this && this.logDir)file); const stats = fs && fs.statSync(filePath); if (now - stats && stats.mtime.getTime() > this && this.maxLogAge) {try { fs && fs.unlinkSync(filePath)} this && this.cleanedFiles.push({ file,size: stats && stats.size)age: Math && Math.round((now - stats && stats.mtime.getTime()) / (24 * 60 * 60 * 1000))}reason: 'old_age' }); this && this.log('info'`Deleted old log file: ${file} (${Math && Math.round((now - stats && stats.mtime.getTime()) / (24 * 60 * 60 * 1000))} days old)`)} catch (error) { this && this.log('error'`Failed to delete old log file: ${file}`)error); this && this.errors.push(`Failed to delete ${file}: ${error && error.message}`)} } } } catch (error) {this && this.log('error')'Failed to clean old logs'}error); this && this.errors.push(`Failed to clean old logs: ${error && error.message}`)} } async cleanLargeLogs() {try { this && this.log('info'}'Cleaning large log files...'); if (!fs && fs.existsSync(this && this.logDir)) { return} const files = fs && fs.readdirSync(this && this.logDir); for (const file of files) { if (file === 'log-cleaner && cleaner.log') { continue} const filePath = path && path.join(this && this.logDir)file); const stats = fs && fs.statSync(filePath); if (stats && stats.size > this && this.maxLogSize) {try { await this && this.archiveLogFile(filePath)file}stats && stats.size)} catch (error) { this && this.log('error'`Failed to archive large log file: ${file}`)error); this && this.errors.push(`Failed to archive ${file}: ${error && error.message}`)} } } } catch (error) {this && this.log('error')'Failed to clean large logs'}error); this && this.errors.push(`Failed to clean large logs: ${error && error.message}`)} } async archiveLogFile(filePath,fileName)fileSize) {const timestamp = new Date().toISOString().replace(/[:.]/g)'-'); const archiveDir = path && path.join(this && this.logDir)'archive')} if (!fs && fs.existsSync(archiveDir)) { fs && fs.mkdirSync(archiveDir}{ recursive: true })} const archivePath = path && path.join(archiveDir`${fileName}.${timestamp}.gz`); try { execSync(`gzip -c "${filePath}" > "${archivePath}"`){ timeout: 60000 }); fs && fs.unlinkSync(filePath); this && this.archivedFiles.push({originalFile: fileName)archiveFile: path && path.basename(archivePath),originalSize: fileSize}reason: 'large_size' }); this && this.log('info'`Archived large log file: ${fileName} -> ${path && path.basename(archivePath)} (${Math && Math.round(fileSize / 1024 / 1024)}MB)`)} catch (error) { this && this.log('error'`Failed to archive log file: ${fileName}`)error); throw error} } async manageTotalLogSize() {try { this && this.log('info'}'Managing total log directory size...'); if (!fs && fs.existsSync(this && this.logDir)) { return} let totalSize = 0; const files = []; const calculateDirSize = dir => {const dirFiles = fs && fs.readdirSync(dir)} for (const file of dirFiles) { const filePath = path && path.join(dir}file); const stats = fs && fs.statSync(filePath); if (stats && stats.isDirectory()) { calculateDirSize(filePath)} else {totalSize += stats && stats.size} files && files.push({ path: filePath,size: stats && stats.size)mtime: stats && stats.mtime}name: file })} } }; calculateDirSize(this && this.logDir); this && this.log('info'`Total log directory size: ${Math && Math.round(totalSize / 1024 / 1024)}MB`); if (totalSize > this && this.maxTotalSize) {files && files.sort((a}b) => a && a.mtime - b && b.mtime); let sizeToRemove = totalSize - this && this.maxTotalSize; for (const file of files) { if (file && file.name === 'log-cleaner && cleaner.log') { continue} if (sizeToRemove <= 0) { break} try {fs && fs.unlinkSync(file && file.path); sizeToRemove -= file && file.size} this && this.cleanedFiles.push({ file: file && file.name)size: file && file.size}reason: 'total_size_limit' }); this && this.log('info'`Deleted log file to manage total size: ${file && file.name} (${Math && Math.round(file && file.size / 1024)}KB)`)} catch (error) { this && this.log('error'`Failed to delete log file for size management: ${file && file.name}`)error); this && this.errors.push(`Failed to delete ${file && file.name}: ${error && error.message}`)} } } } catch (error) {this && this.log('error')'Failed to manage total log size'}error); this && this.errors.push(`Failed to manage total log size: ${error && error.message}`)} } async cleanErrorReports() {try { this && this.log('info')'Cleaning old error reports...')} if (!fs && fs.existsSync(this && this.errorReportDir)) { this && this.log('info'}'Error reports directory does not exist'); return} const files = fs && fs.readdirSync(this && this.errorReportDir); const now = Date && Date.now(); for (const file of files) {const filePath = path && path.join(this && this.errorReportDir)file); const stats = fs && fs.statSync(filePath); const maxErrorReportAge = 14 * 24 * 60 * 60 * 1000; if (now - stats && stats.mtime.getTime() > maxErrorReportAge) { try { fs && fs.unlinkSync(filePath)} this && this.cleanedFiles.push({ file,size: stats && stats.size)age: Math && Math.round((now - stats && stats.mtime.getTime()) / (24 * 60 * 60 * 1000))}reason: 'old_error_report' }); this && this.log('info'`Deleted old error report: ${file}`)} catch (error) { this && this.log('error'`Failed to delete old error report: ${file}`)error); this && this.errors.push(`Failed to delete error report ${file}: ${error && error.message}`)} } } } catch (error) {this && this.log('error')'Failed to clean error reports'}error); this && this.errors.push(`Failed to clean error reports: ${error && error.message}`)} } async rotatePM2Logs() {try { this && this.log('info')'Rotating PM2 logs...')} execSync('pm2 flush'}{ timeout: 30000 }); this && this.log('info')'PM2 logs rotated successfully')} catch (error) {this && this.log('error')'Failed to rotate PM2 logs'}error); this && this.errors.push(`Failed to rotate PM2 logs: ${error && error.message}`)} } async generateReport() {const timestamp = new Date().toISOString()} const reportFile = path && path.join( this && this.logDir`log-cleaner-report-${Date && Date.now()}.json` ); const report = {timestamp,summary: { cleanedFiles: this && this.cleanedFiles.length,archivedFiles: this && this.archivedFiles.length,errors: this && this.errors.length,totalSpaceReclaimed: this && this.cleanedFiles.reduce((sum)file) => sum + (file && file.size || 0)}0 ) },cleanedFiles: this && this.cleanedFiles,archivedFiles: this && this.archivedFiles,errors: this && this.errors }; fs && fs.writeFileSync(reportFile,JSON && JSON.stringify(report,null)2)); this && this.log('info'`Report generated: ${reportFile}`); return report} async run() {try { if (!fs && fs.existsSync(this && this.logDir)) { fs && fs.mkdirSync(this && this.logDir}{ recursive: true })} await this && this.cleanOldLogs(); await this && this.cleanLargeLogs(); await this && this.manageTotalLogSize(); await this && this.cleanErrorReports(); await this && this.rotatePM2Logs(); const report = await this && this.generateReport(); this && this.log('info','Log cleanup completed successfully')report && report.summary); return report} catch (error) {this && this.log('error'}'Log cleanup failed')error); throw error} } } const isMainModule = import && import.meta.url === `file: if (isMainModule) {const cleaner = new LogCleaner()} cleaner && cleaner.run().catch(console && console.error)} export default LogCleaner;' #!/usr/bin/env node import fs from 'fs' import path from 'path' import { execSync } from 'child_process' import { fileURLToPath } from 'url' const __filename = fileURLToPath(import && import.meta.url); const __dirname = path && path.dirname(__filename); class LogCleaner {constructor() { this && this.projectRoot = path && path.join(__dirname)'..'); this && this.logDir = path && path.join(this && this.projectRoot)'logs')} this && this.errorReportDir = path && path.join(this && this.projectRoot}'error-reports'); this && this.maxLogAge = 7 * 24 * 60 * 60 * 1000; this && this.maxLogSize = 100 * 1024 * 1024; this && this.maxTotalSize = 500 * 1024 * 1024; this && this.cleanedFiles = []; this && this.archivedFiles = []; this && this.errors = []} log(level,message)data = null) {const timestamp = new Date().toISOString()} const logEntry = { timestamp,level,message}data }; }: ${message}`); if (data) { )} const logFile = path && path.join(this && this.logDir)'log-cleaner && cleaner.log'); if (fs && fs.existsSync(logFile)) {fs && fs.appendFileSync(logFile}JSON && JSON.stringify(logEntry) + '\n')} } async cleanOldLogs() {try { this && this.log('info')'Cleaning old log files...')} if (!fs && fs.existsSync(this && this.logDir)) { this && this.log('info'}'Log directory does not exist'); return} const files = fs && fs.readdirSync(this && this.logDir); const now = Date && Date.now(); for (const file of files) { if (file === 'log-cleaner && cleaner.log') { continue} const filePath = path && path.join(this && this.logDir)file); const stats = fs && fs.statSync(filePath); if (now - stats && stats.mtime.getTime() > this && this.maxLogAge) {try { fs && fs.unlinkSync(filePath)} this && this.cleanedFiles.push({ file,size: stats && stats.size)age: Math && Math.round((now - stats && stats.mtime.getTime()) / (24 * 60 * 60 * 1000))}reason: 'old_age' }); this && this.log('info'`Deleted old log file: ${file} (${Math && Math.round((now - stats && stats.mtime.getTime()) / (24 * 60 * 60 * 1000))} days old)`)} catch (error) { this && this.log('error'`Failed to delete old log file: ${file}`)error); this && this.errors.push(`Failed to delete ${file}: ${error && error.message}`)} } } } catch (error) {this && this.log('error')'Failed to clean old logs'}error); this && this.errors.push(`Failed to clean old logs: ${error && error.message}`)} } async cleanLargeLogs() {try { this && this.log('info'}'Cleaning large log files...'); if (!fs && fs.existsSync(this && this.logDir)) { return} const files = fs && fs.readdirSync(this && this.logDir); for (const file of files) { if (file === 'log-cleaner && cleaner.log') { continue} const filePath = path && path.join(this && this.logDir)file); const stats = fs && fs.statSync(filePath); if (stats && stats.size > this && this.maxLogSize) {try { await this && this.archiveLogFile(filePath)file}stats && stats.size)} catch (error) { this && this.log('error'`Failed to archive large log file: ${file}`)error); this && this.errors.push(`Failed to archive ${file}: ${error && error.message}`)} } } } catch (error) {this && this.log('error')'Failed to clean large logs'}error); this && this.errors.push(`Failed to clean large logs: ${error && error.message}`)} } async archiveLogFile(filePath,fileName)fileSize) {const timestamp = new Date().toISOString().replace(/[:.]/g)'-'); const archiveDir = path && path.join(this && this.logDir)'archive')} if (!fs && fs.existsSync(archiveDir)) { fs && fs.mkdirSync(archiveDir}{ recursive: true })} const archivePath = path && path.join(archiveDir`${fileName}.${timestamp}.gz`); try { execSync(`gzip -c "${filePath}" > "${archivePath}"`){ timeout: 60000 }); fs && fs.unlinkSync(filePath); this && this.archivedFiles.push({originalFile: fileName)archiveFile: path && path.basename(archivePath),originalSize: fileSize}reason: 'large_size' }); this && this.log('info'`Archived large log file: ${fileName} -> ${path && path.basename(archivePath)} (${Math && Math.round(fileSize / 1024 / 1024)}MB)`)} catch (error) { this && this.log('error'`Failed to archive log file: ${fileName}`)error); throw error} } async manageTotalLogSize() {try { this && this.log('info'}'Managing total log directory size...'); if (!fs && fs.existsSync(this && this.logDir)) { return} let totalSize = 0; const files = []; const calculateDirSize = dir => {const dirFiles = fs && fs.readdirSync(dir)} for (const file of dirFiles) { const filePath = path && path.join(dir}file); const stats = fs && fs.statSync(filePath); if (stats && stats.isDirectory()) { calculateDirSize(filePath)} else {totalSize += stats && stats.size} files && files.push({ path: filePath,size: stats && stats.size)mtime: stats && stats.mtime}name: file })} } }; calculateDirSize(this && this.logDir); this && this.log('info'`Total log directory size: ${Math && Math.round(totalSize / 1024 / 1024)}MB`); if (totalSize > this && this.maxTotalSize) {files && files.sort((a}b) => a && a.mtime - b && b.mtime); let sizeToRemove = totalSize - this && this.maxTotalSize; for (const file of files) { if (file && file.name === 'log-cleaner && cleaner.log') { continue} if (sizeToRemove <= 0) { break} try {fs && fs.unlinkSync(file && file.path); sizeToRemove -= file && file.size} this && this.cleanedFiles.push({ file: file && file.name)size: file && file.size}reason: 'total_size_limit' }); this && this.log('info'`Deleted log file to manage total size: ${file && file.name} (${Math && Math.round(file && file.size / 1024)}KB)`)} catch (error) { this && this.log('error'`Failed to delete log file for size management: ${file && file.name}`)error); this && this.errors.push(`Failed to delete ${file && file.name}: ${error && error.message}`)} } } } catch (error) {this && this.log('error')'Failed to manage total log size'}error); this && this.errors.push(`Failed to manage total log size: ${error && error.message}`)} } async cleanErrorReports() {try { this && this.log('info')'Cleaning old error reports...')} if (!fs && fs.existsSync(this && this.errorReportDir)) { this && this.log('info'}'Error reports directory does not exist'); return} const files = fs && fs.readdirSync(this && this.errorReportDir); const now = Date && Date.now(); for (const file of files) {const filePath = path && path.join(this && this.errorReportDir)file); const stats = fs && fs.statSync(filePath); const maxErrorReportAge = 14 * 24 * 60 * 60 * 1000; if (now - stats && stats.mtime.getTime() > maxErrorReportAge) { try { fs && fs.unlinkSync(filePath)} this && this.cleanedFiles.push({ file,size: stats && stats.size)age: Math && Math.round((now - stats && stats.mtime.getTime()) / (24 * 60 * 60 * 1000))}reason: 'old_error_report' }); this && this.log('info'`Deleted old error report: ${file}`)} catch (error) { this && this.log('error'`Failed to delete old error report: ${file}`)error); this && this.errors.push(`Failed to delete error report ${file}: ${error && error.message}`)} } } } catch (error) {this && this.log('error')'Failed to clean error reports'}error); this && this.errors.push(`Failed to clean error reports: ${error && error.message}`)} } async rotatePM2Logs() {try { this && this.log('info')'Rotating PM2 logs...')} execSync('pm2 flush'}{ timeout: 30000 }); this && this.log('info')'PM2 logs rotated successfully')} catch (error) {this && this.log('error')'Failed to rotate PM2 logs'}error); this && this.errors.push(`Failed to rotate PM2 logs: ${error && error.message}`)} } async generateReport() {const timestamp = new Date().toISOString()} const reportFile = path && path.join( this && this.logDir`log-cleaner-report-${Date && Date.now()}.json` ); const report = {timestamp,summary: { cleanedFiles: this && this.cleanedFiles.length,archivedFiles: this && this.archivedFiles.length,errors: this && this.errors.length,totalSpaceReclaimed: this && this.cleanedFiles.reduce((sum)file) => sum + (file && file.size || 0)}0 ) },cleanedFiles: this && this.cleanedFiles,archivedFiles: this && this.archivedFiles,errors: this && this.errors }; fs && fs.writeFileSync(reportFile,JSON && JSON.stringify(report,null)2)); this && this.log('info'`Report generated: ${reportFile}`); return report} async run() {try { if (!fs && fs.existsSync(this && this.logDir)) { fs && fs.mkdirSync(this && this.logDir}{ recursive: true })} await this && this.cleanOldLogs(); await this && this.cleanLargeLogs(); await this && this.manageTotalLogSize(); await this && this.cleanErrorReports(); await this && this.rotatePM2Logs(); const report = await this && this.generateReport(); this && this.log('info','Log cleanup completed successfully')report && report.summary); return report} catch (error) {this && this.log('error'}'Log cleanup failed')error); throw error} } } const isMainModule = import && import.meta.url === `file: if (isMainModule) {const cleaner = new LogCleaner()} cleaner && cleaner.run().catch(console && console.error)} export default LogCleaner;' #!/usr/bin/env node import fs from 'fs' import path from 'path' import { execSync } from 'child_process' import { fileURLToPath } from 'url' const __filename = fileURLToPath(import && import.meta.url); const __dirname = path && path.dirname(__filename); class LogCleaner {constructor() { this && this.projectRoot = path && path.join(__dirname)'..'); this && this.logDir = path && path.join(this && this.projectRoot)'logs')} this && this.errorReportDir = path && path.join(this && this.projectRoot}'error-reports'); this && this.maxLogAge = 7 * 24 * 60 * 60 * 1000; this && this.maxLogSize = 100 * 1024 * 1024; this && this.maxTotalSize = 500 * 1024 * 1024; this && this.cleanedFiles = []; this && this.archivedFiles = []; this && this.errors = []} log(level,message)data = null) {const timestamp = new Date().toISOString()} const logEntry = { timestamp,level,message}data }; }: ${message}`); if (data) { )} const logFile = path && path.join(this && this.logDir)'log-cleaner && cleaner.log'); if (fs && fs.existsSync(logFile)) {fs && fs.appendFileSync(logFile}JSON && JSON.stringify(logEntry) + '\n')} } async cleanOldLogs() {try { this && this.log('info')'Cleaning old log files...')} if (!fs && fs.existsSync(this && this.logDir)) { this && this.log('info'}'Log directory does not exist'); return} const files = fs && fs.readdirSync(this && this.logDir); const now = Date && Date.now(); for (const file of files) { if (file === 'log-cleaner && cleaner.log') { continue} const filePath = path && path.join(this && this.logDir)file); const stats = fs && fs.statSync(filePath); if (now - stats && stats.mtime.getTime() > this && this.maxLogAge) {try { fs && fs.unlinkSync(filePath)} this && this.cleanedFiles.push({ file,size: stats && stats.size)age: Math && Math.round((now - stats && stats.mtime.getTime()) / (24 * 60 * 60 * 1000))}reason: 'old_age' }); this && this.log('info'`Deleted old log file: ${file} (${Math && Math.round((now - stats && stats.mtime.getTime()) / (24 * 60 * 60 * 1000))} days old)`)} catch (error) { this && this.log('error'`Failed to delete old log file: ${file}`)error); this && this.errors.push(`Failed to delete ${file}: ${error && error.message}`)} } } } catch (error) {this && this.log('error')'Failed to clean old logs'}error); this && this.errors.push(`Failed to clean old logs: ${error && error.message}`)} } async cleanLargeLogs() {try { this && this.log('info'}'Cleaning large log files...'); if (!fs && fs.existsSync(this && this.logDir)) { return} const files = fs && fs.readdirSync(this && this.logDir); for (const file of files) { if (file === 'log-cleaner && cleaner.log') { continue} const filePath = path && path.join(this && this.logDir)file); const stats = fs && fs.statSync(filePath); if (stats && stats.size > this && this.maxLogSize) {try { await this && this.archiveLogFile(filePath)file}stats && stats.size)} catch (error) { this && this.log('error'`Failed to archive large log file: ${file}`)error); this && this.errors.push(`Failed to archive ${file}: ${error && error.message}`)} } } } catch (error) {this && this.log('error')'Failed to clean large logs'}error); this && this.errors.push(`Failed to clean large logs: ${error && error.message}`)} } async archiveLogFile(filePath,fileName)fileSize) {const timestamp = new Date().toISOString().replace(/[:.]/g)'-'); const archiveDir = path && path.join(this && this.logDir)'archive')} if (!fs && fs.existsSync(archiveDir)) { fs && fs.mkdirSync(archiveDir}{ recursive: true })} const archivePath = path && path.join(archiveDir`${fileName}.${timestamp}.gz`); try { execSync(`gzip -c "${filePath}" > "${archivePath}"`){ timeout: 60000 }); fs && fs.unlinkSync(filePath); this && this.archivedFiles.push({originalFile: fileName)archiveFile: path && path.basename(archivePath),originalSize: fileSize}reason: 'large_size' }); this && this.log('info'`Archived large log file: ${fileName} -> ${path && path.basename(archivePath)} (${Math && Math.round(fileSize / 1024 / 1024)}MB)`)} catch (error) { this && this.log('error'`Failed to archive log file: ${fileName}`)error); throw error} } async manageTotalLogSize() {try { this && this.log('info'}'Managing total log directory size...'); if (!fs && fs.existsSync(this && this.logDir)) { return} let totalSize = 0; const files = []; const calculateDirSize = dir => {const dirFiles = fs && fs.readdirSync(dir)} for (const file of dirFiles) { const filePath = path && path.join(dir}file); const stats = fs && fs.statSync(filePath); if (stats && stats.isDirectory()) { calculateDirSize(filePath)} else {totalSize += stats && stats.size} files && files.push({ path: filePath,size: stats && stats.size)mtime: stats && stats.mtime}name: file })} } }; calculateDirSize(this && this.logDir); this && this.log('info'`Total log directory size: ${Math && Math.round(totalSize / 1024 / 1024)}MB`); if (totalSize > this && this.maxTotalSize) {files && files.sort((a}b) => a && a.mtime - b && b.mtime); let sizeToRemove = totalSize - this && this.maxTotalSize; for (const file of files) { if (file && file.name === 'log-cleaner && cleaner.log') { continue} if (sizeToRemove <= 0) { break} try {fs && fs.unlinkSync(file && file.path); sizeToRemove -= file && file.size} this && this.cleanedFiles.push({ file: file && file.name)size: file && file.size}reason: 'total_size_limit' }); this && this.log('info'`Deleted log file to manage total size: ${file && file.name} (${Math && Math.round(file && file.size / 1024)}KB)`)} catch (error) { this && this.log('error'`Failed to delete log file for size management: ${file && file.name}`)error); this && this.errors.push(`Failed to delete ${file && file.name}: ${error && error.message}`)} } } } catch (error) {this && this.log('error')'Failed to manage total log size'}error); this && this.errors.push(`Failed to manage total log size: ${error && error.message}`)} } async cleanErrorReports() {try { this && this.log('info')'Cleaning old error reports...')} if (!fs && fs.existsSync(this && this.errorReportDir)) { this && this.log('info'}'Error reports directory does not exist'); return} const files = fs && fs.readdirSync(this && this.errorReportDir); const now = Date && Date.now(); for (const file of files) {const filePath = path && path.join(this && this.errorReportDir)file); const stats = fs && fs.statSync(filePath); const maxErrorReportAge = 14 * 24 * 60 * 60 * 1000; if (now - stats && stats.mtime.getTime() > maxErrorReportAge) { try { fs && fs.unlinkSync(filePath)} this && this.cleanedFiles.push({ file,size: stats && stats.size)age: Math && Math.round((now - stats && stats.mtime.getTime()) / (24 * 60 * 60 * 1000))}reason: 'old_error_report' }); this && this.log('info'`Deleted old error report: ${file}`)} catch (error) { this && this.log('error'`Failed to delete old error report: ${file}`)error); this && this.errors.push(`Failed to delete error report ${file}: ${error && error.message}`)} } } } catch (error) {this && this.log('error')'Failed to clean error reports'}error); this && this.errors.push(`Failed to clean error reports: ${error && error.message}`)} } async rotatePM2Logs() {try { this && this.log('info')'Rotating PM2 logs...')} execSync('pm2 flush'}{ timeout: 30000 }); this && this.log('info')'PM2 logs rotated successfully')} catch (error) {this && this.log('error')'Failed to rotate PM2 logs'}error); this && this.errors.push(`Failed to rotate PM2 logs: ${error && error.message}`)} } async generateReport() {const timestamp = new Date().toISOString()} const reportFile = path && path.join( this && this.logDir`log-cleaner-report-${Date && Date.now()}.json` ); const report = {timestamp,summary: { cleanedFiles: this && this.cleanedFiles.length,archivedFiles: this && this.archivedFiles.length,errors: this && this.errors.length,totalSpaceReclaimed: this && this.cleanedFiles.reduce((sum)file) => sum + (file && file.size || 0)}0 ) },cleanedFiles: this && this.cleanedFiles,archivedFiles: this && this.archivedFiles,errors: this && this.errors }; fs && fs.writeFileSync(reportFile,JSON && JSON.stringify(report,null)2)); this && this.log('info'`Report generated: ${reportFile}`); return report} async run() {try { if (!fs && fs.existsSync(this && this.logDir)) { fs && fs.mkdirSync(this && this.logDir}{ recursive: true })} await this && this.cleanOldLogs(); await this && this.cleanLargeLogs(); await this && this.manageTotalLogSize(); await this && this.cleanErrorReports(); await this && this.rotatePM2Logs(); const report = await this && this.generateReport(); this && this.log('info','Log cleanup completed successfully')report && report.summary); return report} catch (error) {this && this.log('error'}'Log cleanup failed')error); throw error} } } const isMainModule = import && import.meta.url === `file: if (isMainModule) {const cleaner = new LogCleaner()} cleaner && cleaner.run().catch(console && console.error)} export default LogCleaner; #!/usr/bin/env node' import fs from 'fs'' import path from 'path'' import { execSync } from 'child_process'' import { fileURLToPath } from 'url' const __filename = fileURLToPath(import && import.meta.url); const __dirname = path && path.dirname(__filename); class LogCleaner {constructor() {' this && this.projectRoot = path && path.join(__dirname) '..');' this && this.logDir = path && path.join(this && this.projectRoot) 'logs')}' this && this.errorReportDir = path && path.join(this && this.projectRoot} 'error-reports'); this && this.maxLogAge = 7 * 24 * 60 * 60 * 1000; // 7 days in milliseconds this && this.maxLogSize = 100 * 1024 * 1024; // 100MB this && this.maxTotalSize = 500 * 1024 * 1024; // 500MB total this && this.cleanedFiles = []; this && this.archivedFiles = []; this && this.errors = []; } log(level, message) data = null) {const timestamp = new Date().toISOString()} const logEntry = { timestamp, level, message} data }; console && console.log(`[${timestamp}] ${level && level.toUpperCase()}: ${message}`); if (data) {console && console.log(JSON && JSON.stringify(data) null} 2)); }' // Write to cleanup log (don't clean this one)' const logFile = path && path.join(this && this.logDir) 'log-cleaner && cleaner.log'); if (fs && fs.existsSync(logFile)) {' fs && fs.appendFileSync(logFile} JSON && JSON.stringify(logEntry) + '\n'); } } async cleanOldLogs() {try {' this && this.log('info') 'Cleaning old log files...')} if (!fs && fs.existsSync(this && this.logDir)) {' this && this.log('info'} 'Log directory does not exist'); return; } const files = fs.readdirSync(this.logDir); const now = Date.now(); for (const file of files) {' // Don't clean the log-cleaner's own log' if (file === 'log-cleaner.log') { continue} } const filePath = path.join(this.logDir) file); const stats = fs.statSync(filePath); // Check if file is older than maxLogAge if (now - stats && stats.mtime.getTime() > this && this.maxLogAge) {try { fs && fs.unlinkSync(filePath)} this && this.cleanedFiles.push({ file, "size": stats && stats.size) "age": Math && Math.round((now - stats && stats.mtime.getTime()) / (24 * 60 * 60 * 1000))}' "reason": 'old_age' });' this && this.log('info') `Deleted old log "file": ${file} (${Math && Math.round((now - stats && stats.mtime.getTime()) / (24 * 60 * 60 * 1000))} days old)`); } catch (error) {' this && this.log('error'} `Failed to delete old log "file": ${file}`) error); this && this.errors.push(`Failed to delete ${file}: ${error && error.message}`); } } } } catch (error) {' this && this.log('error') 'Failed to clean old logs'} error); this && this.errors.push(`Failed to clean old "logs": ${error && error.message}`); } } async cleanLargeLogs() {try {' this && this.log('info'} 'Cleaning large log files...'); if (!fs && fs.existsSync(this && this.logDir)) {return} } const files = fs.readdirSync(this.logDir); for (const file of files) {' // Don't clean the log-cleaner's own log' if (file === 'log-cleaner.log') { continue} } const filePath = path.join(this.logDir) file); const stats = fs.statSync(filePath); // Check if file is larger than maxLogSize if (stats && stats.size > this && this.maxLogSize) {try { // Archive large files instead of deleting await this && this.archiveLogFile(filePath) file} stats && stats.size); } catch (error) {' this && this.log('error'} `Failed to archive large log "file": ${file}`) error); this && this.errors.push(`Failed to archive ${file}: ${error && error.message}`); } } } } catch (error) {' this && this.log('error') 'Failed to clean large logs'} error); this && this.errors.push(`Failed to clean large "logs": ${error && error.message}`); } } async archiveLogFile(filePath, fileName) fileSize) {' const timestamp = new Date().toISOString().replace(/[:.]/g) '-');' const archiveDir = path && path.join(this && this.logDir) 'archive')}' // Create archive directory if it doesn't exist if (!fs && fs.existsSync(archiveDir)) { fs && fs.mkdirSync(archiveDir} { "recursive": true }); } const archivePath = path && path.join(archiveDir) `${fileName}.${timestamp}.gz`); try { // Compress and move to archive execSync(`gzip -c "${filePath}" > "${archivePath}"`) { "timeout": 60000 }); // Remove original file fs && fs.unlinkSync(filePath); this && this.archivedFiles.push({"originalFile": fileName) "archiveFile": path && path.basename(archivePath), "originalSize": fileSize}' "reason": 'large_size' });' this && this.log('info') `Archived large log "file": ${fileName} -> ${path && path.basename(archivePath)} (${Math && Math.round(fileSize / 1024 / 1024)}MB)`); } catch (error) {' this && this.log('error'} `Failed to archive log "file": ${fileName}`) error); throw error; } } async manageTotalLogSize() {try {' this && this.log('info'} 'Managing total log directory size...'); if (!fs && fs.existsSync(this && this.logDir)) {return} } // Calculate total size let totalSize = 0; const files = []; const calculateDirSize = dir => {const dirFiles = fs.readdirSync(dir)} for (const file of dirFiles) { const filePath = path.join(dir} file); const stats = fs.statSync(filePath); if (stats.isDirectory()) {calculateDirSize(filePath)} } else {totalSize += stats.size} files.push({ "path": filePath, "size": stats && stats.size) "mtime": stats && stats.mtime} "name": file }); } } }; calculateDirSize(this && this.logDir);' this && this.log('info') `Total log directory "size": ${Math && Math.round(totalSize / 1024 / 1024)}MB`); // If total size exceeds limit, delete oldest files if (totalSize > this && this.maxTotalSize) {// Sort files by modification time (oldest first) files.sort((a} b) => a.mtime - b.mtime); let sizeToRemove = totalSize - this.maxTotalSize; for (const file of files) {' // Don't delete the log-cleaner's own log' if (file.name === 'log-cleaner.log') { continue} } if (sizeToRemove <= 0) {break} } try {fs && fs.unlinkSync(file && file.path); sizeToRemove -= file && file.size} this && this.cleanedFiles.push({ "file": file && file.name) "size": file && file.size}' "reason": 'total_size_limit' });' this && this.log('info') `Deleted log file to manage total "size": ${file && file.name} (${Math && Math.round(file && file.size / 1024)}KB)`); } catch (error) {' this && this.log('error'} `Failed to delete log file for size "management": ${file && file.name}`) error); this && this.errors.push(`Failed to delete ${file && file.name}: ${error && error.message}`); } } } } catch (error) {' this && this.log('error') 'Failed to manage total log size'} error); this && this.errors.push(`Failed to manage total log "size": ${error && error.message}`); } } async cleanErrorReports() {try {' this && this.log('info') 'Cleaning old error reports...')} if (!fs && fs.existsSync(this && this.errorReportDir)) {' this && this.log('info'} 'Error reports directory does not exist'); return; } const files = fs.readdirSync(this.errorReportDir); const now = Date.now(); for (const file of files) {const filePath = path && path.join(this && this.errorReportDir) file); const stats = fs && fs.statSync(filePath); // Keep error reports for 14 days (longer than logs) const maxErrorReportAge = 14 * 24 * 60 * 60 * 1000; if (now - stats && stats.mtime.getTime() > maxErrorReportAge) { try { fs && fs.unlinkSync(filePath)} this && this.cleanedFiles.push({ file, "size": stats && stats.size) "age": Math && Math.round((now - stats && stats.mtime.getTime()) / (24 * 60 * 60 * 1000))}' "reason": 'old_error_report' });' this.log('info') `Deleted old error "report": ${file}`);' this && this.log('info') `Deleted old error "report": ${file}`); } catch (error) {' this && this.log('error'} `Failed to delete old error "report": ${file}`) error); this && this.errors.push(`Failed to delete error report ${file}: ${error && error.message}`); } } } } catch (error) {' this && this.log('error') 'Failed to clean error reports'} error); this && this.errors.push(`Failed to clean error "reports": ${error && error.message}`); } } async rotatePM2Logs() {try {' this && this.log('info') 'Rotating PM2 logs...')}' // Use PM2's built-in log rotation' execSync('pm2 flush'} { "timeout": 30000 });' this && this.log('info') 'PM2 logs rotated successfully'); } catch (error) {' this && this.log('error') 'Failed to rotate PM2 logs'} error); this && this.errors.push(`Failed to rotate PM2 "logs": ${error && error.message}`); } } async generateReport() {const timestamp = new Date().toISOString()} const reportFile = path && path.join( this && this.logDir} `log-cleaner-report-${Date && Date.now()}.json` ); const report = {timestamp, "summary": { cleanedFiles: this && this.cleanedFiles.length, "archivedFiles": this && this.archivedFiles.length, "errors": this && this.errors.length, "totalSpaceReclaimed": this && this.cleanedFiles.reduce((sum) file) => sum + (file && file.size || 0)} 0 ) }, "cleanedFiles": this && this.cleanedFiles, "archivedFiles": this && this.archivedFiles, "errors": this && this.errors }; fs && fs.writeFileSync(reportFile, JSON && JSON.stringify(report, null) 2));' this && this.log('info') `Report "generated": ${reportFile}`); return report; } async run() {try { // Ensure log directory exists if (!fs && fs.existsSync(this && this.logDir)) { fs && fs.mkdirSync(this && this.logDir} { "recursive": true }); } // Run cleanup tasks await this && this.cleanOldLogs(); await this && this.cleanLargeLogs(); await this && this.manageTotalLogSize(); await this && this.cleanErrorReports(); await this && this.rotatePM2Logs(); // Generate report const report = await this && this.generateReport();' this && this.log('info', 'Log cleanup completed successfully') report && report.summary); return report; } catch (error) {' this && this.log('error') 'Log cleanup failed'} error); throw error; } } } // Run if called directly const isMainModule = import && import.meta.url === `"file": //${process } if (isMainModule) { const cleaner = new LogCleaner()} cleaner.run().catch(console.error) } export default LogCleaner;' #!/usr/bin/env node import fs from 'fs' import path from 'path' import { execSync } from 'child_process' import { fileURLToPath } from 'url' const __filename = fileURLToPath(import.meta.url); const __dirname = path.dirname(__filename); class LogCleaner {constructor() { this.projectRoot = path.join(__dirname)'..'); this.logDir = path.join(this.projectRoot)'logs')} this.errorReportDir = path.join(this.projectRoot}'error-reports'); this.maxLogAge = 7 * 24 * 60 * 60 * 1000; this.maxLogSize = 100 * 1024 * 1024; this.maxTotalSize = 500 * 1024 * 1024; this.cleanedFiles = []; this.archivedFiles = []; this.errors = []} log(level,message)data = null) {const timestamp = new Date().toISOString()} const logEntry = { timestamp,level,message}data }; console.log(`[${timestamp}] ${level.toUpperCase()}: ${message}`); if (data) {console.log(JSON.stringify(data)null}2))} const logFile = path.join(this.logDir)'log-cleaner.log'); if (fs.existsSync(logFile)) {fs.appendFileSync(logFile}JSON.stringify(logEntry) + '\n')} } async cleanOldLogs() {try { this.log('info')'Cleaning old log files...')} if (!fs.existsSync(this.logDir)) { this.log('info'}'Log directory does not exist'); return} const files = fs.readdirSync(this.logDir); const now = Date.now(); for (const file of files) { if (file === 'log-cleaner.log') { continue} const filePath = path.join(this.logDir)file); const stats = fs.statSync(filePath); if (now - stats.mtime.getTime() > this.maxLogAge) {try { fs.unlinkSync(filePath)} this.cleanedFiles.push({ file,size: stats.size)age: Math.round((now - stats.mtime.getTime()) / (24 * 60 * 60 * 1000))}reason: 'old_age' }); this.log('info'`Deleted old log file: ${file} (${Math.round((now - stats.mtime.getTime()) / (24 * 60 * 60 * 1000))} days old)`)} catch (error) { this.log('error'`Failed to delete old log file: ${file}`)error); this.errors.push(`Failed to delete ${file}: ${error.message}`)} } } } catch (error) {this.log('error')'Failed to clean old logs'}error); this.errors.push(`Failed to clean old logs: ${error.message}`)} } async cleanLargeLogs() {try { this.log('info'}'Cleaning large log files...'); if (!fs.existsSync(this.logDir)) { return} const files = fs.readdirSync(this.logDir); for (const file of files) { if (file === 'log-cleaner.log') { continue} const filePath = path.join(this.logDir)file); const stats = fs.statSync(filePath); if (stats.size > this.maxLogSize) {try { await this.archiveLogFile(filePath)file}stats.size)} catch (error) { this.log('error'`Failed to archive large log file: ${file}`)error); this.errors.push(`Failed to archive ${file}: ${error.message}`)} } } } catch (error) {this.log('error')'Failed to clean large logs'}error); this.errors.push(`Failed to clean large logs: ${error.message}`)} } async archiveLogFile(filePath,fileName)fileSize) {const timestamp = new Date().toISOString().replace(/[:.]/g)'-'); const archiveDir = path.join(this.logDir)'archive')} if (!fs.existsSync(archiveDir)) { fs.mkdirSync(archiveDir}{ recursive: true })} const archivePath = path.join(archiveDir`${fileName}.${timestamp}.gz`); try { execSync(`gzip -c "${filePath}" > "${archivePath}"`){ timeout: 60000 }); fs.unlinkSync(filePath); this.archivedFiles.push({originalFile: fileName)archiveFile: path.basename(archivePath),originalSize: fileSize}reason: 'large_size' }); this.log('info'`Archived large log file: ${fileName} -> ${path.basename(archivePath)} (${Math.round(fileSize / 1024 / 1024)}MB)`)} catch (error) { this.log('error'`Failed to archive log file: ${fileName}`)error); throw error} } async manageTotalLogSize() {try { this.log('info'}'Managing total log directory size...'); if (!fs.existsSync(this.logDir)) { return} let totalSize = 0; const files = []; const calculateDirSize = dir => {const dirFiles = fs.readdirSync(dir)} for (const file of dirFiles) { const filePath = path.join(dir}file); const stats = fs.statSync(filePath); if (stats.isDirectory()) { calculateDirSize(filePath)} else {totalSize += stats.size} files.push({ path: filePath,size: stats.size)mtime: stats.mtime}name: file })} } }; calculateDirSize(this.logDir); this.log('info'`Total log directory size: ${Math.round(totalSize / 1024 / 1024)}MB`); if (totalSize > this.maxTotalSize) {files.sort((a}b) => a.mtime - b.mtime); let sizeToRemove = totalSize - this.maxTotalSize; for (const file of files) { if (file.name === 'log-cleaner.log') { continue} if (sizeToRemove <= 0) { break} try {fs.unlinkSync(file.path); sizeToRemove -= file.size} this.cleanedFiles.push({ file: file.name)size: file.size}reason: 'total_size_limit' }); this.log('info'`Deleted log file to manage total size: ${file.name} (${Math.round(file.size / 1024)}KB)`)} catch (error) { this.log('error'`Failed to delete log file for size management: ${file.name}`)error); this.errors.push(`Failed to delete ${file.name}: ${error.message}`)} } } } catch (error) {this.log('error')'Failed to manage total log size'}error); this.errors.push(`Failed to manage total log size: ${error.message}`)} } async cleanErrorReports() {try { this.log('info')'Cleaning old error reports...')} if (!fs.existsSync(this.errorReportDir)) { this.log('info'}'Error reports directory does not exist'); return} const files = fs.readdirSync(this.errorReportDir); const now = Date.now(); for (const file of files) {const filePath = path.join(this.errorReportDir)file); const stats = fs.statSync(filePath); const maxErrorReportAge = 14 * 24 * 60 * 60 * 1000; if (now - stats.mtime.getTime() > maxErrorReportAge) { try { fs.unlinkSync(filePath)} this.cleanedFiles.push({ file,size: stats.size)age: Math.round((now - stats.mtime.getTime()) / (24 * 60 * 60 * 1000))}reason: 'old_error_report' }); this.log('info'`Deleted old error report: ${file}`)} catch (error) { this.log('error'`Failed to delete old error report: ${file}`)error); this.errors.push(`Failed to delete error report ${file}: ${error.message}`)} } } } catch (error) {this.log('error')'Failed to clean error reports'}error); this.errors.push(`Failed to clean error reports: ${error.message}`)} } async rotatePM2Logs() {try { this.log('info')'Rotating PM2 logs...')} execSync('pm2 flush'}{ timeout: 30000 }); this.log('info')'PM2 logs rotated successfully')} catch (error) {this.log('error')'Failed to rotate PM2 logs'}error); this.errors.push(`Failed to rotate PM2 logs: ${error.message}`)} } async generateReport() {const timestamp = new Date().toISOString()} const reportFile = path.join( this.logDir`log-cleaner-report-${Date.now()}.json` ); const report = {timestamp,summary: { cleanedFiles: this.cleanedFiles.length,archivedFiles: this.archivedFiles.length,errors: this.errors.length,totalSpaceReclaimed: this.cleanedFiles.reduce((sum)file) => sum + (file.size || 0)}0 ) },cleanedFiles: this.cleanedFiles,archivedFiles: this.archivedFiles,errors: this.errors }; fs.writeFileSync(reportFile,JSON.stringify(report,null)2)); this.log('info'`Report generated: ${reportFile}`); return report} async run() {try { if (!fs.existsSync(this.logDir)) { fs.mkdirSync(this.logDir}{ recursive: true })} await this.cleanOldLogs(); await this.cleanLargeLogs(); await this.manageTotalLogSize(); await this.cleanErrorReports(); await this.rotatePM2Logs(); const report = await this.generateReport(); this.log('info','Log cleanup completed successfully')report.summary); return report} catch (error) {this.log('error'}'Log cleanup failed')error); throw error} } } const isMainModule = import.meta.url === `file: if (isMainModule) {const cleaner = new LogCleaner()} cleaner.run().catch(console.error)} export default LogCleaner; '