#!/usr/bin/env node;
const fs = require('fs')
const path = require(path')
const { execSync } = require('child_process')


      console.error('\n� Build Monitor "failed")
      console.error('\n� Build Monitor "failed")


