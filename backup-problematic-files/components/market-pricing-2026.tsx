import React, { useState } from 'react'
import Head from 'next/
import UltraAdvancedFuturisticBackground from '../components/ui/
import UltraAdvancedNavigation from '../components/layout/
import { revolutionary2026Services } from '../data/
import { emergingTech2026Services } from '../data/
import { comprehensiveIT2026Services } from '../data/
      (priceRange = = 'budget' && parseFloat(service.price.replace(/[^0-9.]/
      (priceRange = = 'mid' && parseFloat(service.price.replace(/[^0-9.]/g, '') >= 5000 && parseFloat(service.price.replace(/[^0-9.]/
      (priceRange = = 'premium' && parseFloat(service.price.replace(/[^0-9.]/
    website: any
    averagePrice: allServices.reduce(sum, service) => sum + parseFloat(service.price.replace(/[^0-9.]/
    lowestPrice: Math.min(...allServices.map(s => parseFloat(s.price.replace(/[^0-9.]/
    highestPrice: Math.max(...allServices.map(s => parseFloat(s.price.replace(/[^0-9.]/