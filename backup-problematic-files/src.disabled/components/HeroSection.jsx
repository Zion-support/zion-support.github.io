import React from 'react';
const HeroSection = ({ className }) => {return (<div className={className || ''}>;
      <h1>HeroSection</h1>;
      <p>This component is under development.</p>;
    </div>;
  )}

const HeroSection = ({ className }) => {
  return (
    <div className={className || ''}>
      <h1>HeroSection</h1>
      <p>This component is under development.</p>
    </div>
  )
}
}

