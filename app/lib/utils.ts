import React from 'react';

export default ;
function Page() {
  return (
    <div>
      ;
import { type ClassValue, clsx } from "clsx";import { twMerge } from "tailwind-merge";export function cn(...inputs="ClassValue[]) {
  return twMerge(clsx(inputs));
"}
    </div>
  );
}