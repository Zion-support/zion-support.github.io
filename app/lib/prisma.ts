// app/lib/prisma.ts
// Mock prisma object for static export
export const prisma = {
  // Add any methods that are used in the API route here if needed
  // For now, we just need the import to succeed
};