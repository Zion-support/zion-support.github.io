import Link from 'next/link';

export const metadata = { 
};

export default function BlockchainPage() {
  return (
    </div>
  );
}

    </div>
  );
}

            {tech}
          </span>
        ))}
      </div>
    </div>
  );
}

    </div>
  );
}

    </div>
  );
}