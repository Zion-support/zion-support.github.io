</li>
        </ul>
      </div>
    </section>
  );

          <li key={d} style={{ listStyle: "disc" }}>
            {d}
          </li>
        ))}
      </ul>
      </div>
  );

          "SLA guarantee",
        ]}
      />
      </div>
  );

          color: "#111827",
          fontWeight: 800,
          marginTop: 4,
          fontSize: 18,
        }}
      >
        {price}

          <li key={f} style={{ listStyle: "disc" }}>
            {f}
          </li>
        ))}
      </ul>

          textDecoration: "none",
          borderRadius: 6,
        }}
      >