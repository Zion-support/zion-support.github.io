'use client';
import { useState, useEffect } from 'react';
;

export const useLazyLoading = () => {  return null;
}
  return null;
}
  const [isLoaded, setIsLoaded] = useState(false);

  useEffect(() => {
  return null;
}
  return null;
}
    setIsLoaded(true);
  }, []);

  return isLoaded;
};
