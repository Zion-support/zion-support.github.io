// useAnalyticsContext - Placeholder file
export interface useAnalyticsContextConfig {
  enabled: boolean;
  options?: Record<string, unknown>;
}

export const defaultuseAnalyticsContextConfig: useAnalyticsContextConfig = {
  enabled: true,
  options: {}
};
