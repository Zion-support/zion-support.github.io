import React from "react";"import { Helmet } from ""export const useSEO  = (structuredData?: unknown) => {""  useEffect(() => {// Add structured data to the page""    if (structuredData) {"      ;const script  = document.createElement('script'"'      script.type = '
}}}))

;
export default ;
function Page() {
  return <div>Page content</div>;
}