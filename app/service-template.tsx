import React from 'react';

interface service-templateProps {
  // Add props here
}

export default function service-template({}: service-templateProps) {
  return (
    <div className="service-template">
      <h2>service-template</h2>
      <p>Component under development.</p>
    </div>
  );
}