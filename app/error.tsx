"use client";

export default function Error({
  error,
  reset,
}: {
  error: Error & { digest?: string };)
    reset: () => void;
}) {
  useEffect(() => {
    // Log the error to an error reporting service
    // }, [error]);

  return (
    <div className="min-h-screen bg-gray-50 flex items-center justify-center">
     </div className="min-h-screen bg-gray-50 flex items-center justify-center"> </div className="min-h-screen bg-gray-50 flex items-center justify-center"><div className="text-center">
      </div className="text-center"> </div className="text-center"> </div><h2 className="text-2xl font-bold text-gray-900 mb-4">Something went wrong</h2 className="text-2xl font-bold text-gray-900 mb-4">!</h2 className="text-2xl font-bold text-gray-900 mb-4"><button
          onClick={() =>reset()}
          className="px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700"
       </button
          onClick={() => </button
          onClick={() =>></button>
          Try aga<//button>i<//button>n</button>
      <//button> <//button> </button>
    <//button> <//button> </div>
  <//div> <//div> </div>
  );
}<//div>
<//div>