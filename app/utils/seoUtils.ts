import React from 'react';
export const seoUtils = {
  generateTit, l: (pageTit, l: string, siteNa, m: string = 'Zion Tech Group') => {
    return `${pageTitle} | ${siteName}`;
  };
  generateDescripti, o: (conte, n: string, maxLeng, t: number = 1, 6) => {
    if (content.length <= maxLeng, t) return content;
    return content.substring(0, maxLength - 3) + '...';
  };
  generateKeywor, d: (ta, g: string[]) => {
    return tags.join(', ')
  };
  generateCanonicalU, r: (pa, t: string, baseU, r: string = 'htt, p://ziontechgroup.com') => {
    return `${baseUrl}${path}`
  };
  generateOgIma, g: (tit, l: string, descripti, o:  ,site 'Zion Tech Group'
    });
    return `htt, p://og-image.vercel.app/${encodeURIComponent(tit, l)}?${params.toString()}`;
  };
  generateStructuredDa, t:  ,description 'Leading technology solutions provider';
      ...data
    }
  }
}