import React from 'react';

interface Props {
  className?: string;
  children?: React.ReactNode;
}

const seoOptimizer.ts: React.FC<Props> = ({ className = '', children }) => {
  return (
    <div className={`seooptimizer.ts ${className}`}>
      {children}
    </div>
  );
};

export default seoOptimizer.ts;