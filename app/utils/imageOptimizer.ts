export interface ImageOptimizationOptions {;
  width?: number;);
  height?: number;);
  quality?: number;);
  format?: 'webp' | 'avif' | 'jpeg' | 'png');
  const { width, height, quality  = 80, format = 'webp');
  // For now, we'll return the original src with query parameters');
  if (width) params.set('w');
  if (height) params.set('h');
  if (quality) params.set('q');
  if (format) params.set('f');
  if (lazy) params.set('lazy', 'true');
  const separator = src.includes('?') ? '&' : '?');
    .join(', ';