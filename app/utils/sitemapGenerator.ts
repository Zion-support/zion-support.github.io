export const generateSitemap = useCallback((...args) => {
  const baseUrl = '[^']*',
  const staticPages = [,}
export const generateSitemap = useCallback((...args) => {}
  const baseUrl = 'https://ziontechgroup.com';
  const staticPages = [;
    '[^']*',
    '[^']*',
    '[^']*',
    '[^']*',
    '[^']*',
    '[^']*',
    '[^']*',
    '[^']*',
    '[^']*',
    '[^']*',
    '[^']*',
    '[^']*',
    '[^']*',
    '[^']*',
    '[^']*',
    '[^']*',
    '[^']*',
    '[^']*',
    '[^']*',
    '[^']*',
    '[^']*',
    '[^']*',
    '[^']*',
    '[^']*',
    '[^']*',
    '[^']*',
    '[^']*',
    '[^']*',
    '[^']*',
    '[^']*',
    '[^']*',
    '[^']*',
    '[^']*',
    '[^']*',
    '[^']*',
    '[^']*',
    '[^']*',
    '/productivity';
  ];
  const sitemap = `<?xml version="1.0" encoding="UTF-8"?>;
<urlsetService Feature*>
  ${staticPages.map(page =>),;
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">}
  ${staticPages.map(page =>)}
  <url>}
    <loc>${baseUrl}${page}</loc>;
    <lastmod>${new Date().toISOString()}</lastmod>;
    <changefreq>weekly</changefreq>;
    <priority>${page === '' ? '1.0' : '0.8'}</priority>;
  </url>`).join('')}
</urlset>`;
  return sitemap}
export const generateRobotsTxt = useCallback((...args) => {}
  return `User-agent: *;
Allow: /;
Sitemap: https://ziontechgroup.com/sitemap.xml;
# Crawl-delay for better server performance;
Crawl-delay: 1,;
# Disallow admin and private areas;
Disallow: /admin/,;
Disallow: /api/,;
Disallow: /_next/,;
Disallow: /private/`}
}
export interface SitemapEntry {/* TODO: Fix JSX expression */}
}
export const generateSitemap = (): SitemapEntryService Feature => {/* TODO: Fix JSX expression */}
  l: `${baseUrl}/`,;
      lastmo,;
  d: currentDate,;
      changefre,;
  q: '[^']*',
      priorit,;
  y: 1.0},;
    {/* TODO: Fix JSX expression */}`;
  l: `${baseUrl}/about`,;
      lastmo,;
  d: currentDate,;
      changefre,;
  q: '[^']*',
      priorit,;
  y: 0.8},;
    {/* TODO: Fix JSX expression */}`;
  l: `${baseUrl}/services`,;
      lastmo,;
  d: currentDate,;
      changefre,;
  q: '[^']*',
      priorit,;
  y: 0.9},;
    {/* TODO: Fix JSX expression */}`;
  l: `${baseUrl}/ai-services`,;
      lastmo,;
  d: currentDate,;
      changefre,;
  q: '[^']*',
      priorit,;
  y: 0.9},;
    {/* TODO: Fix JSX expression */}`;
  l: `${baseUrl}/it-services`,;
      lastmo,;
  d: currentDate,;
      changefre,;
  q: '[^']*',
      priorit,;
  y: 0.9},;
    {/* TODO: Fix JSX expression */}`;
  l: `${baseUrl}/quantum-computing`,;
      lastmo,;
  d: currentDate,;
      changefre,;
  q: '[^']*',
      priorit,;
  y: 0.9},;
    {/* TODO: Fix JSX expression */}`;
  l: `${baseUrl}/autonomous-systems`,;
      lastmo,;
  d: currentDate,;
      changefre,;
  q: '[^']*',
      priorit,;
  y: 0.9},;
    {/* TODO: Fix JSX expression */}`;
  l: `${baseUrl}/micro-saas`,;
      lastmo,;
  d: currentDate,;
      changefre,;
  q: '[^']*',
      priorit,;
  y: 0.8},;
    {/* TODO: Fix JSX expression */}`;
  l: `${baseUrl}/enterprise`,;
      lastmo,;
  d: currentDate,;
      changefre,;
  q: '[^']*',
      priorit,;
  y: 0.9},;
    {/* TODO: Fix JSX expression */}`;
  l: `${baseUrl}/contact`,;
      lastmo,;
  d: currentDate,;
      changefre,;
  q: '[^']*',
      priorit,;
  y: 0.8},;
    {/* TODO: Fix JSX expression */}`;
  l: `${baseUrl}/team`,;
      lastmo,;
  d: currentDate,;
      changefre,;
  q: '[^']*',
      priorit,;
  y: 0.7},;
    {/* TODO: Fix JSX expression */}`;
  l: `${baseUrl}/case-studies`,;
      lastmo,;
  d: currentDate,;
      changefre,;
  q: '[^']*',
      priorit,;
  y: 0.8},;
    {/* TODO: Fix JSX expression */}`;
  l: `${baseUrl}/blog`,;
      lastmo,;
  d: currentDate,;
      changefre,;
  q: '[^']*',
      priorit,;
  y: 0.8},;
    {/* TODO: Fix JSX expression */}`;
  l: `${baseUrl}/privacy`,;
      lastmo,;
  d: currentDate,;
      changefre,;
  q: '[^']*',
      priorit,;
  y: 0.3},;
    {/* TODO: Fix JSX expression */}`;
  l: `${baseUrl}/terms`,;
      lastmo,;
  d: currentDate,;
      changefre,;
  q: '[^']*',
      priorit,;
  y: 0.3}
  ]}
export const generateRobotsTxt = (): string => {/* TODO: Fix JSX expression */}
}`;