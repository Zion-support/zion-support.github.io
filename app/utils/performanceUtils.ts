// Performanceutils utility functions

export const Performanceutils = {
  init: () => {
    },

  process: (_data: any) => {
    return data;
  },

  cleanup: () => {
    },
};

export default Performanceutils;
