import React from 'react';

interface Props {
  className?: string;
  children?: React.ReactNode;
}

const enhancedErrorHandler.ts: React.FC<Props> = ({ className = '', children }) => {
  return (
    <div className={`enhancederrorhandler.ts ${className}`}>
      {children}
    </div>
  );
};

export default enhancedErrorHandler.ts;