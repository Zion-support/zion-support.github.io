'use client;'
import React from 'react;'
export default function Component() {
  return (
    <Suspense fallback={fallback}>
      {children}
    </Suspense>
  );
    
        

      </div>
  );

        </p>
  );
}
