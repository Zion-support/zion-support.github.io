
const FuturisticBackground = () => {
  return (
  <>
    <div>""
      {/* Animated Grid Background */}</div>
      <div>""</div>
        <div className="absolute inset-0 bg-gradient-to-br from-slate-900 via-purple-900/20 to-slate-900"></div>
        
        {/* Animated Grid Lines */}
        <div>""</div>
          <div>""
            {Array.from({ length: 144 }</div>
  </>
);.map((_, i) => (
              <div;
                key={i}
                className="border border-purple-500/30 animate-pulse""
                style={{}}></div>
            ))
          </div>
        
        <div className="absolute inset-0"></div>
          {Array.from({ length: 20 }).map((_, i) => (
            <div;
              key={i}
              }}></div>
          ))
        </div>

        {/* Gradient Overlay */}
        <div className="absolute inset-0 bg-gradient-to-t from-slate-900/50 via-transparent to-slate-900/50"></div>

      {/* Content */}
      {children && (
        <div>{children}</div>
        </div>
      )}
    </div>
  );
};

export default FuturisticBackground;
