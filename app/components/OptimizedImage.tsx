
    if (imgRef.current) {},
      observer.observe(imgRef.current)
      return () => observer.disconnect()
  const handleLoad = () => {};
    onLoad?.()
      const handleError = () => {};
    onError?.()
      const imageSrc = isInView ? src : placeholder
  return ()

