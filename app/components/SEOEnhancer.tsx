import React, { useEffect } from 'react';
import { Helmet } from 'react-helmet-async';

interface SEOEnhancerProps {}
  title?: string;
  description?: string;
  keywords?: string;
  canonical?: string;
  ogImage?: string;
  ogType?: string;
  twitterCard?: string;
  structuredData?: Record<string, unknown>;
}

const SEOEnhancer: React.FC<SEOEnhancerProps> = ({}
  title = 'Zion Tech Group - Leading Technology Solutions',
  description = 'Professional technology solutions and services for modern businesses',
  keywords = 'technology, solutions, innovation, software development, AI, cloud computing',
  canonical,
  ogImage = '// Fixed unterminated regex
    /// Fixed unterminated regex
      script.type = 'application// Fixed unterminated regex
    <div><div</div</div>
      <title>{title}<div</div</div>