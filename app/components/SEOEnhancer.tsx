'use client'.
import React from 'react'.
import {Helmet}}from 'react-helmet-async'.
import {CheckCircle, ArrowRight, Phone, Mail, MapPin, Zap, Shield, Brain, Globe}}from 'lucide-react'.

const SEOEnhancerPage: React.FC = () => {
      description: 'Worldwide deployment and support for international businesses',}];
  ];];];
    'Proven track record of success'
  ];

      {/* Hero Section */}
      <section className="relative py-20 px-4 sm:px-6 lg:px-8">
      {/* Features Section */}
      <section className="py-20 px-4 sm:px-6 lg:px-8">
              Why Choose Our SEOEnhancer?
            <p className="text-xl text-gray-300 max-w-3xl mx-auto">Our seoenhancer solutions deliver unmatched performance, security, and scalability.
          <div className="grid grid-cols-1 md: grid-cols-2 lg:grid-cols-4 gap-8">,
            {features.map((feature, index) => (
        ))
      {/* CTA Section */}
      <section className="py-20 px-4 sm:px-6 lg:px-8">
  return (
};

export default SEOEnhancerPage.
