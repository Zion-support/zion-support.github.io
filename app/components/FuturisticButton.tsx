
</div>
  )
  if ($1) {};
  // If body
};
    return ()
      <>
        to={href},
      className={buttonClasses},
      onClick={onClick};
      ></>
        {content};
      </>
    )
  },
      return ()
    <buttonclassName={buttonClasses},>
      onClick={onClick},
      disabled={disabled};
    ></button>>
      {content};
    </button>
  )
},
      export default FuturisticButton
;
