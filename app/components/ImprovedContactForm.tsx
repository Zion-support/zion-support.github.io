
  email: string;
  phone: string;
  company: string;
  message: string;
  service: string;
  budget: string;
  timeline: string;
};
interface FormErrors {

    e.preventDefault();
    if (!validateForm()) {
      return;,
    };
    setIsSubmitting(true);
    try {

                </option>
              ))};
            </select>

              Send Message
            </>,
          )};
        </button>
      </form>
    </div>";
  );";";
};";";";
export default ImprovedContactForm;"