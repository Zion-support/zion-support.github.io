
      interface FormData {},
      name: string,
      email: string,
      phone: string,
      company: string,
      message: string,
      service: string
name: ","
      email: ","
      phone: ","
      company: ","
      message: ","
      service: "}),"
    } catch (error) {};"
      } finally {}"
      setIsSubmitting(false)"
        {isSubmitting ? "Sending..." : "Send Message"}"
      </button>"
    </form>"
  )"
},"
      "
import React from '