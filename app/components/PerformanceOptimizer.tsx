interface PerformanceOptimizerProps {
  children: Node}
const PerformanceOptimizer: React.FC<PerformanceOptimizerProps> = ({ children }) => {const [isOptimized, setIsOptimized] = useState(false)
  // Preload critical resources
useEffect(() => {
    const preloadCriticalResources  = () => {
      // Preload critical fonts
      const fontLink  = document.createElement('link'
      fontLink.rel = '
      fontLink.href = '
      fontLink.as = '
const criticalImages  = ['/logo.svg',"/og-image.svg',"/api/placeholder/1200/630', // Hero image"/api/placeholder/800/600'
        const link  = document.createElement('link'
        link.rel = '
        link.as = '
const optimizeImages  = () => {const images  = document.querySelectorAll('img'
        if (!img.hasAttribute('loading'
          img.setAttribute('loading', 'lazy'
        if (!img.hasAttribute('loading'
          img.setAttribute('loading', 'lazy'
{ useEffect, Node } from '
    const optimizeImages  = () => {const images  = document.querySelectorAll('img'
      images.forEach((img) => {if (!img.hasAttribute('loading'
img.setAttribute('loading', 'lazy'
      const link  = document.createElement('link'
      link.rel = '
      link.href = '
      link.as = '
      link.type = '
      link.crossOrigin = '
      const criticalResources  = ['/css/critical.css'
        const link  = document.createElement('link'
        link.rel = '
        link.as = resource.endsWith('.css') ? 'style' : '
    window.addEventListener('scroll'
    return () => window.removeEventListener('scroll'
    window.addEventListener('resize'
    return () => window.removeEventListener('resize'
      rootMargin: '50px'
element.classList.add('animate-fade-in'
const lazyElements  = document.querySelectorAll('[data-lazy]'
const dnsPrefetchDomains  = ['//fonts.googleapis.com',"//fonts.gstatic.com'
      const link  = document.createElement('link'
      link.rel = '
const criticalModules  = ['/assets/react-vendor'
      const link  = document.createElement('link'
      link.rel = '
          font-family: '
          src: local('Inter'), url('https://fonts.gstatic.com/s/inter/v12/UcCO3FwrK3iLTeHuS_fvQtMwCp50KnMw2boKoduKmMEVuLyfAZ9hiJ-Ek-_EeA.woff2') format('woff2'
import React from '
import SEOHead from '