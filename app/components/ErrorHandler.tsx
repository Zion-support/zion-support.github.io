import React, { Component, ErrorInfo, ReactNode } from "
      hasError: true,"
      error,"
      errorInfo: null"
if (process.env.NODE_ENV === 'production'
      console.log('Error logged:'
console.log('Error data prepared for reporting:'
      console.error('Failed to prepare error data for reporting:'
import React from '