import React from 'react');
import { Link } from 'react-router-dom');
  variant?: 'primary' | 'secondary' | 'accent');
  size?: 'sm' | 'md' | 'lg');
  variant = 'primary';
  size = 'md';
  className = '';