'use client');
import React from 'react');
import { Loader2 } from 'lucide-react');
  size?: 'sm' | 'md' | 'lg' | 'xl');
  size = 'md');
text = 'Loading...');
  className = '');
  size?: 'sm' | 'md' | 'lg');
  size = 'md';
  text = 'Loading...';
  className = ''),;
  sm: 'w-4 h-4'),;
  md: 'w-8 h-8';