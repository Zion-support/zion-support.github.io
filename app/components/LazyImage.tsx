
interface LazyImageProps {}
  src: string
  alt: string
  className?: string
  placeholder?: React.ReactNode
  // If body
          setIsInView(true)
          observer.disconnect()
      rootMargin: '50px'
import React from '