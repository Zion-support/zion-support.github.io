'use client';
import React from 'react';

interface SEOHeadWrapperProps {
  title: string;
  description: string;
  keywords: string[];
}

const SEOHeadWrapper: React.FC<SEOHeadWrapperProps> = ({ title, description, keywords }) => {
  return <></>;
};

export default SEOHeadWrapper;