'use client';
import React from 'react';
import Head from 'next/head''
  ogImage = '/og-image.jpg''
  className = '''
  const fullTitle = title.includes('ZionTechGroup'';
  const keywordsString = keywords.join(', ''