'use client';

import React from 'react';

interface GlobalErrorBoundaryProps {
  // Add props here
}

const GlobalErrorBoundary: React.FC<GlobalErrorBoundaryProps> = (_props) => {
  return (
    <div>
      {/* Component content */}
    </div>
  );
};

export default GlobalErrorBoundary;
