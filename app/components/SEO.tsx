import React from "react";

type SEOProps = Record<string, unknown>;

const SEO: React.FC<SEOProps> = () => {
  return (
    <div className="seo">
      {/* Component content */}
    </div>
  );
};

export default SEO;