// ProposalPDF — renders the proposal document for download/email
// @react-pdf/renderer is optional; this is a stub for build compatibility
import type { Service } from '@/data/servicesData';

export default function ProposalPDF(_props: { service?: Service }) {
  return null;
}
