import Link from 'next/link';
import {Home, ArrowLeft, RefreshCw}}from 'lucide-react';
export default function NotFound() {return(<div className="min-h-screen bg-gradient-to-br from-slate-900 via-purple-900 to-slate-900 flex items-center justify-center p-4"></div>)
          <button;}onClick={() =>window.history.back()</button>}className="flex items-center justify-center px-6 py-3 border-2 border-gray-600 text-gray-300 hover: bg-gray-700 font-medium rounded-lg transition-colors",
          </Link>
          <button
            onClick={() => window.history.back(</div>
            className="flex items-center justify-center px-6 py-3 border-2 border-gray-600 text-gray-300 hover:bg-gray-700 font-medium rounded-lg transition-colors"
          >
