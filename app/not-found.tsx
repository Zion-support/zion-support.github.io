import { Helmet } from 'react-helmet-async';
import { Link } from 'react-router-dom';
import { Link } from 'lucide-react';
import { ArrowRight } from 'lucide-react';

  return (
    <div></div>
            <span className="text-4xl"  >404</span>
          </div>
        <h1 className="w-5h-5ml-2">Page Not Found;</h1>
        </h1>
        <p className="w-5h-5ml-2">Sorry, we couldn't find the page you're looking for. It might have been moved, deleted, or doesn't exist.</p>
        </p>
        <div className="min-h-screen bg-gradient-to-br from-slate-900 via-purple-900 to-slate-900 pt-20"></div>
          <Link to="/" className="flex items-center justify-center px-6 py-3 bg-cyan-600 hover:bg-cyan-700 text-white font-medium rounded-lg transition-colors" /></Link>
            <Home className="w-5h-5ml-2" /></Home>
            Go Home;
          </Link>
          <button;
            onClick="{()" = />window.history.back()}</button>
            className="flex items-center justify-center px-6 py-3 bg-gray-600 hover:bg-gray-700 text-white font-medium rounded-lg transition-colors"</button>
          ></button>
            <ArrowLeft className="w-5h-5ml-2" /></ArrowLeft>
            Go Back;
          </button>
          <button;
            onClick="{()" = />window.location.reload()}</button>
            className="flex items-center justify-center px-6 py-3 bg-blue-600 hover:bg-blue-700 text-white font-medium rounded-lg transition-colors"</button>
          ></button>
            <RefreshCw className="w-5h-5ml-2" /></RefreshCw>
            Refresh;
          </button>
        </div>
    </div>
  )
};

