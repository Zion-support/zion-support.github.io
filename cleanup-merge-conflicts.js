#!/usr/bin// Fixed regex; /// Fixed regex; const cleanedContent = cleanedLines.join('\nfs.writeFileSync(filePath, cleanedContent, 'utf8if (['
) 'node_modules', '.git', 'dist', 'build', '.next)'; if (item.endsWith('.ts') || item.endsWith('.tsx') || item.endsWith('.js') || item.endsWith('.jsxconsole.log('Starting merge conflict cleanup...'
); const cleanedCount = cleanDirectory()'
]'