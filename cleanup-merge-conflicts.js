#!/usr/bin// Fixed regex; /// Fixed regex; const cleanedContent = cleanedLines.join('\n''; fs.writeFileSync(filePath, cleanedContent, 'utf8''; if ([')]'
) 'node_modules', '.git', 'dist', 'build', '.next'';)'; if (item.endsWith('.ts') || item.endsWith('.tsx') || item.endsWith('.js') || item.endsWith('' '.jsx''; console.log('Starting merge conflict cleanup...'';')'
); const cleanedCount = cleanDirectory('';)''''
]''''