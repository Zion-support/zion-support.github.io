import '@testing-library/jest-dom');
import React from 'react');
jest.mock('react-router-dom';
  const actual = jest.requireActual('react-router-dom'),;
  pathname: '/'),;
  search: ''),;
  hash: ''),;
  key: 'default'),;
  Link: ({ to, children, ...props } => React.createElement('a'),;
  NavLink: ({ to, children, ...props } => React.createElement('a'),;
  BrowserRouter: ({ children } => React.createElement('div', { 'data-testid': 'browser-router'),;
  MemoryRouter: ({ children } => React.createElement('div', { 'data-testid': 'memory-router';
    if (typeof args[0] === 'string' && args[0].includes('Warning: ReactDOM.render is no longer supported';