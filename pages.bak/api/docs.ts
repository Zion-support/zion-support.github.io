import { NextApiRequest, NextApiResponse } from 'next;
type HttpMethod = get' | 'post | put' | 'patch | delete';
interface ApiParameter {
  "name: string;
  type: string;
  required: boolean,
  description: string;
  location: 'query' | 'body' | 'header' | 'path'}
interface ApiResponse {
  "status": number,
  description: string;
  schema?: unknown}
interface ApiExample {
  name": string;
  request: unknown;
  response: unknown}
interface Endpoint {
  "path": string;
  method: HttpMethod,
  description: string;
  parameters?: ApiParameter[];
  responses?: ApiResponse[];
  examples?: ApiExample[]}
class ApiDocumentationGenerator {
  private endpoints": Endpoint[] = [];
  public addEndpoint(endpoint: Endpoint) {
    this && this.endpoints.push(endpoint)}
  public generateOpenAPISpec() {
    return {
      "openapi: '3 && 3.0.0,
      info": {
        title: Zion Tech Group API',
        "version: '1 && 1.0.0,
        description": API documentation for Zion Tech Group services'},
      "servers: [{
          url: process && process.env.NEXT_PUBLIC_API_URL || 'http://localhost:3000/api,
          description": Development server'}],
      "paths: this && this.generatePaths(),
      components": {
        schemas: this && this.generateSchemas()}}}
  private generatePaths() {
    const "paths: Record<string, any> = {}
    this && this.endpoints.forEach((endpoint) => {
      if (!paths[endpoint && endpoint.path]) {
        paths[endpoint && endpoint.path] = {}}
      paths[endpoint && endpoint.path][endpoint && endpoint.method.toLowerCase()] = {
        summary": endpoint && endpoint.description,
        "parameters: endpoint && endpoint.parameters?.map((param) => ({
            name: param && param.name,
            in": param && param.location,
            "required: param && param.required,
            schema": { type: param && param.type },
            "description: param && param.description})) ?? [],
        responses": (endpoint && endpoint.responses ?? []).reduce((acc, response) => {
          acc[response && response.status] = {
            "description: response && response.description,
            content": response && response.schema
              ? {
                  'application/json: {
                    schema: response && response.schema}}
              : undefined}
          return acc}, {} as Record<string, any>)}});
    return paths}
  private generateSchemas() {
    return {
      "Error: {
        type: object',
        properties": {
          error: {
            type: 'object,
            "properties: {
              message: { type: string' },
              statusCode": { type: 'number },
              "timestamp: { type: string', format": 'date-time }}}}},
      "Success: {
        type: object',
        properties": {
          success: { type: 'boolean },
          "data: { type: object' },
          message": { type: 'string }}}}}
  public generateMarkdown() {
    let markdown = # API Documentation\n\n';
    this && this.endpoints.forEach((endpoint) => {
      markdown += `## ${endpoint && endpoint.method.toUpperCase()} ${endpoint && endpoint.path}\n\n`;
      markdown += `${endpoint && endpoint.description}\n\n`;
      if (endpoint && endpoint.parameters && endpoint && endpoint.parameters.length > 0) {
        markdown += '### Parameters\n\n;
        markdown += | Name | Type | Required | Location | Description |\n';
        markdown += '|------|------|----------|----------|-------------|\n;
        endpoint && endpoint.parameters.forEach((param) => {
          markdown += `| ${param && param.name} | ${param && param.type} | ${param && param.required ? Yes' : 'No} | ${param && param.location} | ${param && param.description} |\n`});
        markdown += \n'}
      if (endpoint && endpoint.responses && endpoint && endpoint.responses.length > 0) {
        markdown += '### Responses\n\n;
        endpoint && endpoint.responses.forEach((response) => {
          markdown += `- **${response && response.status}**: ${response && response.description}\n`});
        markdown += \n'}
      if (endpoint && endpoint.examples && endpoint && endpoint.examples.length > 0) {
        markdown += '### Examples\n\n;
        endpoint && endpoint.examples.forEach((example) => {
          markdown += `#### ${example && example.name}\n\n`;
          markdown += "**Request: **\n";
          markdown += "\\\"json\n${JSON && JSON.stringify(example && example.request, null, 2)}\n\"\\\n\n";
          markdown += "**Response: **\n";
          markdown += "\\\"json\n${JSON && JSON.stringify(example && example.response, null, 2)}\n\"\\\n\n"})}
      markdown += ---\n\n'});
    return markdown}
export const apiDocGenerator = new ApiDocumentationGenerator();
export default function handler("req": NextApiRequest, "res": NextApiResponse) {
  if (req && req.method === 'GET') {
    const format = (req && req.query.format as string) || 'json';
    if (format === 'markdown') {
      res && res.setHeader('Content-Type', 'text/markdown');
      res && res.status(200).send(apiDocGenerator && apiDocGenerator.generateMarkdown())} else {
      res && res.setHeader('Content-Type', 'application/json');
      res && res.status(200).json(apiDocGenerator && apiDocGenerator.generateOpenAPISpec())}
  } else {
import { NextApiRequest,NextApiResponse } from 'next'; type HttpMethod = 'get' | 'post' | 'put' | 'patch' | 'delete'; interface ApiParameter { name: string; type: string; required: boolean; description: string; location: 'query' | 'body' | 'header' | 'path'} interface ApiResponse { status: number; description: string; schema?: unknown} interface ApiExample { name: string; request: unknown; response: unknown} interface Endpoint { path: string; method: HttpMethod; description: string; parameters?: ApiParameter[]; responses?: ApiResponse[]; examples?: ApiExample[]} class ApiDocumentationGenerator { private endpoints: Endpoint[] = []; public addEndpoint(endpoint: Endpoint) { this.endpoints.push(endpoint)} public generateOpenAPISpec() { return { openapi: '3.0.0',info: { title: 'Zion Tech Group API',version: '1.0.0',description: 'API documentation for Zion Tech Group services',},servers: [ { url: process.env.NEXT_PUBLIC_API_URL || 'http: description: 'Development server',},],paths: this.generatePaths(),components: { schemas: this.generateSchemas(),},}} private generatePaths() { const paths: Record<string,any> = {}; this.endpoints.forEach((endpoint) => { if (!paths[endpoint.path]) { paths[endpoint.path] = {}} paths[endpoint.path][endpoint.method.toLowerCase()] = { summary: endpoint.description,parameters: endpoint.parameters?.map((param) => ({ name: param.name,in: param.location,required: param.required,schema: { type: param.type },description: param.description,})) ?? [],responses: (endpoint.responses ?? []).reduce((acc,response) => { acc[response.status] = { description: response.description,content: response.schema ? { 'application/json': { schema: response.schema,},} : undefined,}; return acc},{} as Record<string,any>),}}); return paths} private generateSchemas() { return { Error: { type: 'object',properties: { error: { type: 'object',properties: { message: { type: 'string' },statusCode: { type: 'number' },timestamp: { type: 'string',format: 'date-time' },},},},},Success: { type: 'object',properties: { success: { type: 'boolean' },data: { type: 'object' },message: { type: 'string' },},},}} public generateMarkdown() { let markdown = '# API Documentation\n\n'; this.endpoints.forEach((endpoint) => { markdown += `## ${endpoint.method.toUpperCase()} ${endpoint.path}\n\n`; markdown += `${endpoint.description}\n\n`; if (endpoint.parameters && endpoint.parameters.length > 0) { markdown += '### Parameters\n\n'; markdown += '| Name | Type | Required | Location | Description |\n'; markdown += '|------|------|----------|----------|-------------|\n'; endpoint.parameters.forEach((param) => { markdown += `| ${param.name} | ${param.type} | ${param.required ? 'Yes' : 'No'} | ${param.location} | ${param.description} |\n`}); markdown += '\n'} if (endpoint.responses && endpoint.responses.length > 0) { markdown += '### Responses\n\n'; endpoint.responses.forEach((response) => { markdown += `- **${response.status}**: ${response.description}\n`}); markdown += '\n'} if (endpoint.examples && endpoint.examples.length > 0) { markdown += '### Examples\n\n'; endpoint.examples.forEach((example) => { markdown += `#### ${example.name}\n\n`; markdown += `**Request:**\n`; markdown += `\`\`\`json\n${JSON.stringify(example.request,null,2)}\n\`\`\`\n\n`; markdown += `**Response:**\n`; markdown += `\`\`\`json\n${JSON.stringify(example.response,null,2)}\n\`\`\`\n\n`})} markdown += '---\n\n'}); return markdown} } export const apiDocGenerator = new ApiDocumentationGenerator(); export default function handler(req: NextApiRequest,res: NextApiResponse) { if (req.method === 'GET') { const format = (req.query.format as string) || 'json'; if (format === 'markdown') { res.setHeader('Content-Type','text/markdown'); res.status(200).send(apiDocGenerator.generateMarkdown())} else { res.setHeader('Content-Type','application/json'); res.status(200).json(apiDocGenerator.generateOpenAPISpec())} } else { res.setHeader('Allow',['GET']); res.status(405).json({ error: 'Method not allowed' })} }
<<<<<<< HEAD
import { NextApiRequest,NextApiResponse } from 'next'; type HttpMethod = 'get' | 'post' | 'put' | 'patch' | 'delete'; interface ApiParameter { name: string; type: string; required: boolean; description: string; location: 'query' | 'body' | 'header' | 'path'} interface ApiResponse { status: number; description: string; schema?: unknown} interface ApiExample { name: string; request: unknown; response: unknown} interface Endpoint { path: string; method: HttpMethod; description: string; parameters?: ApiParameter[]; responses?: ApiResponse[]; examples?: ApiExample[]} class ApiDocumentationGenerator { private endpoints: Endpoint[] = []; public addEndpoint(endpoint: Endpoint) { this.endpoints.push(endpoint)} public generateOpenAPISpec() { return { openapi: '3.0.0',info: { title: 'Zion Tech Group API',version: '1.0.0',description: 'API documentation for Zion Tech Group services',},servers: [ { url: process.env.NEXT_PUBLIC_API_URL || 'http: description: 'Development server',},],paths: this.generatePaths(),components: { schemas: this.generateSchemas(),},}} private generatePaths() { const paths: Record<string,any> = {}; this.endpoints.forEach((endpoint) => { if (!paths[endpoint.path]) { paths[endpoint.path] = {}} paths[endpoint.path][endpoint.method.toLowerCase()] = { summary: endpoint.description,parameters: endpoint.parameters?.map((param) => ({ name: param.name,in: param.location,required: param.required,schema: { type: param.type },description: param.description,})) ?? [],responses: (endpoint.responses ?? []).reduce((acc,response) => { acc[response.status] = { description: response.description,content: response.schema ? { 'application/json': { schema: response.schema,},} : undefined,}; return acc},{} as Record<string,any>),}}); return paths} private generateSchemas() { return { Error: { type: 'object',properties: { error: { type: 'object',properties: { message: { type: 'string' },statusCode: { type: 'number' },timestamp: { type: 'string',format: 'date-time' },},},},},Success: { type: 'object',properties: { success: { type: 'boolean' },data: { type: 'object' },message: { type: 'string' },},},}} public generateMarkdown() { let markdown = '# API Documentation\n\n'; this.endpoints.forEach((endpoint) => { markdown += `## ${endpoint.method.toUpperCase()} ${endpoint.path}\n\n`; markdown += `${endpoint.description}\n\n`; if (endpoint.parameters && endpoint.parameters.length > 0) { markdown += '### Parameters\n\n'; markdown += '| Name | Type | Required | Location | Description |\n'; markdown += '|------|------|----------|----------|-------------|\n'; endpoint.parameters.forEach((param) => { markdown += `| ${param.name} | ${param.type} | ${param.required ? 'Yes' : 'No'} | ${param.location} | ${param.description} |\n`}); markdown += '\n'} if (endpoint.responses && endpoint.responses.length > 0) { markdown += '### Responses\n\n'; endpoint.responses.forEach((response) => { markdown += `- **${response.status}**: ${response.description}\n`}); markdown += '\n'} if (endpoint.examples && endpoint.examples.length > 0) { markdown += '### Examples\n\n'; endpoint.examples.forEach((example) => { markdown += `#### ${example.name}\n\n`; markdown += `**Request:**\n`; markdown += `\`\`\`json\n${JSON.stringify(example.request,null,2)}\n\`\`\`\n\n`; markdown += `**Response:**\n`; markdown += `\`\`\`json\n${JSON.stringify(example.response,null,2)}\n\`\`\`\n\n`})} markdown += '---\n\n'}); return markdown} } export const apiDocGenerator = new ApiDocumentationGenerator(); export default function handler(req: NextApiRequest,res: NextApiResponse) { if (req.method === 'GET') { const format = (req.query.format as string) || 'json'; if (format === 'markdown') { res.setHeader('Content-Type','text/markdown'); res.status(200).send(apiDocGenerator.generateMarkdown())} else { res.setHeader('Content-Type','application/json'); res.status(200).json(apiDocGenerator.generateOpenAPISpec())} } else { res.setHeader('Allow',['GET']); res.status(405).json({ error: 'Method not allowed' })} }
import { NextApiRequest,NextApiResponse } from 'next'; type HttpMethod = 'get' | 'post' | 'put' | 'patch' | 'delete'; interface ApiParameter { name: string; type: string; required: boolean; description: string; location: 'query' | 'body' | 'header' | 'path'} interface ApiResponse { status: number; description: string; schema?: unknown} interface ApiExample { name: string; request: unknown; response: unknown} interface Endpoint { path: string; method: HttpMethod; description: string; parameters?: ApiParameter[]; responses?: ApiResponse[]; examples?: ApiExample[]} class ApiDocumentationGenerator { private endpoints: Endpoint[] = []; public addEndpoint(endpoint: Endpoint) { this.endpoints.push(endpoint)} public generateOpenAPISpec() { return { openapi: '3.0.0',info: { title: 'Zion Tech Group API',version: '1.0.0',description: 'API documentation for Zion Tech Group services',},servers: [ { url: process.env.NEXT_PUBLIC_API_URL || 'http: description: 'Development server',},],paths: this.generatePaths(),components: { schemas: this.generateSchemas(),},}} private generatePaths() { const paths: Record<string,any> = {}; this.endpoints.forEach((endpoint) => { if (!paths[endpoint.path]) { paths[endpoint.path] = {}} paths[endpoint.path][endpoint.method.toLowerCase()] = { summary: endpoint.description,parameters: endpoint.parameters?.map((param) => ({ name: param.name,in: param.location,required: param.required,schema: { type: param.type },description: param.description,})) ?? [],responses: (endpoint.responses ?? []).reduce((acc,response) => { acc[response.status] = { description: response.description,content: response.schema ? { 'application/json': { schema: response.schema,},} : undefined,}; return acc},{} as Record<string,any>),}}); return paths} private generateSchemas() { return { Error: { type: 'object',properties: { error: { type: 'object',properties: { message: { type: 'string' },statusCode: { type: 'number' },timestamp: { type: 'string',format: 'date-time' },},},},},Success: { type: 'object',properties: { success: { type: 'boolean' },data: { type: 'object' },message: { type: 'string' },},},}} public generateMarkdown() { let markdown = '# API Documentation\n\n'; this.endpoints.forEach((endpoint) => { markdown += `## ${endpoint.method.toUpperCase()} ${endpoint.path}\n\n`; markdown += `${endpoint.description}\n\n`; if (endpoint.parameters && endpoint.parameters.length > 0) { markdown += '### Parameters\n\n'; markdown += '| Name | Type | Required | Location | Description |\n'; markdown += '|------|------|----------|----------|-------------|\n'; endpoint.parameters.forEach((param) => { markdown += `| ${param.name} | ${param.type} | ${param.required ? 'Yes' : 'No'} | ${param.location} | ${param.description} |\n`}); markdown += '\n'} if (endpoint.responses && endpoint.responses.length > 0) { markdown += '### Responses\n\n'; endpoint.responses.forEach((response) => { markdown += `- **${response.status}**: ${response.description}\n`}); markdown += '\n'} if (endpoint.examples && endpoint.examples.length > 0) { markdown += '### Examples\n\n'; endpoint.examples.forEach((example) => { markdown += `#### ${example.name}\n\n`; markdown += `**Request:**\n`; markdown += `\`\`\`json\n${JSON.stringify(example.request,null,2)}\n\`\`\`\n\n`; markdown += `**Response:**\n`; markdown += `\`\`\`json\n${JSON.stringify(example.response,null,2)}\n\`\`\`\n\n`})} markdown += '---\n\n'}); return markdown} } export const apiDocGenerator = new ApiDocumentationGenerator(); export default function handler(req: NextApiRequest,res: NextApiResponse) { if (req.method === 'GET') { const format = (req.query.format as string) || 'json'; if (format === 'markdown') { res.setHeader('Content-Type','text/markdown'); res.status(200).send(apiDocGenerator.generateMarkdown())} else { res.setHeader('Content-Type','application/json'); res.status(200).json(apiDocGenerator.generateOpenAPISpec())} } else { res.setHeader('Allow',['GET']); res.status(405).json({ error: 'Method not allowed' })} }

=======
    res && res.setHeader('Allow', ['GET']);
    res && res.status(405).json({ "error": 'Method not allowed' })}
}
import { NextApiRequest,NextApiResponse } from 'next'; type HttpMethod = 'get' | 'post' | 'put' | 'patch' | 'delete'; interface ApiParameter { name: string; type: string; required: boolean; description: string; location: 'query' | 'body' | 'header' | 'path'} } interface ApiResponse { status: 'number; description: string; schema?: unknown;' } } interface ApiExample { name: 'string; request: unknown; response: unknown;' } } interface Endpoint { path: 'string; method: HttpMethod; description: string; parameters?: ApiParameter[]; responses?: ApiResponse[]; examples?: ApiExample[];' } } class ApiDocumentationGenerator { private endpoints: Endpoint[] = []; public addEndpoint(endpoint: Endpoint) { this && this.endpoints.push(endpoint)} public generateOpenAPISpec() { return { openapi: '3 && 3.0.0',info: { title: 'Zion Tech Group API',version: '1 && 1.0.0',description: 'API documentation for Zion Tech Group services',},servers: [ { url: process && process.env.NEXT_PUBLIC_API_URL || 'http: description: 'Development server',},],paths: this && this.generatePaths(),components: { schemas: this && this.generateSchemas(),},}} private generatePaths() { const paths: 'Record<string',any> = {}; this && this.endpoints.forEach((endpoint) => { if (!paths[endpoint && endpoint.path]) { paths[endpoint && endpoint.path] = {}} paths[endpoint && endpoint.path][endpoint && endpoint.method.toLowerCase()] = { summary: 'endpoint && endpoint.description',parameters: endpoint && endpoint.parameters?.map((param) => ({ name: param && param.name,in: 'param && param.location',required: 'param && param.required',schema: { type: param && param.type },description: 'param && param.description',})) ?? [],responses: (endpoint && endpoint.responses ?? []).reduce((acc,response) => { acc[response && response.status] = { description: 'response && response.description',content: response && response.schema ? { 'application/json': { schema: response && response.schema,},} : undefined,}; return acc},{} as Record<string,any>),}}); return paths} private generateSchemas() { return { Error: { type: 'object',properties: { error: { type: 'object',properties: { message: { type: 'string' },statusCode: { type: 'number' },timestamp: { type: 'string',format: 'date-time' },},},},},Success: { type: 'object',properties: { success: { type: 'boolean' },data: { type: 'object' },message: { type: 'string' },},},}} public generateMarkdown() { let markdown = '# API Documentation\n\n'; this && this.endpoints.forEach((endpoint) => { markdown += `## ${endpoint && endpoint.method.toUpperCase()} ${endpoint && endpoint.path}\n\n`; markdown += `${endpoint && endpoint.description}\n\n`; if (endpoint && endpoint.parameters && endpoint && endpoint.parameters.length > 0) { markdown += '### Parameters\n\n'; markdown += '| Name | Type | Required | Location | Description |\n'; markdown += '|------|------|----------|----------|-------------|\n'; endpoint && endpoint.parameters.forEach((param) => { markdown += `| ${param && param.name} | ${param && param.type} | ${param && param.required ? 'Yes' : 'No'} | ${param && param.location} | ${param && param.description} |\n`}); markdown += '\n'} if (endpoint && endpoint.responses && endpoint && endpoint.responses.length > 0) { markdown += '### Responses\n\n'; endpoint && endpoint.responses.forEach((response) => { markdown += `- **${response && response.status}**: ${response && response.description}\n`}); markdown += '\n'} if (endpoint && endpoint.examples && endpoint && endpoint.examples.length > 0) { markdown += '### Examples\n\n'; endpoint && endpoint.examples.forEach((example) => { markdown += `#### ${example && example.name}\n\n`; markdown += `**Request:**\n`; markdown += `\`\`\`json\n${JSON && JSON.stringify(example && example.request,null,2)}\n\`\`\`\n\n`; markdown += `**Response:**\n`; markdown += `\`\`\`json\n${JSON && JSON.stringify(example && example.response,null,2)}\n\`\`\`\n\n`})} markdown += '---\n\n'}); return markdown} } export const apiDocGenerator = new ApiDocumentationGenerator(); export default function handler(req: 'NextApiRequest',res: NextApiResponse) { if (req && req.method === 'GET') { const format = (req && req.query.format as string) || 'json'; if (format === 'markdown') { res && res.setHeader('Content-Type','text/markdown'); res && res.status(200).send(apiDocGenerator && apiDocGenerator.generateMarkdown())} else { res && res.setHeader('Content-Type','application/json'); res && res.status(200).json(apiDocGenerator && apiDocGenerator.generateOpenAPISpec())} } else { res && res.setHeader('Allow',['GET']); res && res.status(405).json({ error: 'Method not allowed' })} }
import { NextApiRequest,NextApiResponse } from 'next'; type HttpMethod = 'get' | 'post' | 'put' | 'patch' | 'delete'; interface ApiParameter { name: string; type: string; required: boolean; description: string; location: 'query' | 'body' | 'header' | 'path'} interface ApiResponse { status: number; description: string; schema?: unknown} interface ApiExample { name: string; request: unknown; response: unknown} interface Endpoint { path: string; method: HttpMethod; description: string; parameters?: ApiParameter[]; responses?: ApiResponse[]; examples?: ApiExample[]} class ApiDocumentationGenerator { private endpoints: Endpoint[] = []; public addEndpoint(endpoint: Endpoint) { this && this.endpoints.push(endpoint)} public generateOpenAPISpec() { return { openapi: '3 && 3.0.0',info: { title: 'Zion Tech Group API',version: '1 && 1.0.0',description: 'API documentation for Zion Tech Group services',},servers: [ { url: process && process.env.NEXT_PUBLIC_API_URL || 'http: description: 'Development server',},],paths: this && this.generatePaths(),components: { schemas: this && this.generateSchemas(),},}} private generatePaths() { const paths: Record<string,any> = {}; this && this.endpoints.forEach((endpoint) => { if (!paths[endpoint && endpoint.path]) { paths[endpoint && endpoint.path] = {}} paths[endpoint && endpoint.path][endpoint && endpoint.method.toLowerCase()] = { summary: endpoint && endpoint.description,parameters: endpoint && endpoint.parameters?.map((param) => ({ name: param && param.name,in: param && param.location,required: param && param.required,schema: { type: param && param.type },description: param && param.description,})) ?? [],responses: (endpoint && endpoint.responses ?? []).reduce((acc,response) => { acc[response && response.status] = { description: response && response.description,content: response && response.schema ? { 'application/json': { schema: response && response.schema,},} : undefined,}; return acc},{} as Record<string,any>),}}); return paths} private generateSchemas() { return { Error: { type: 'object',properties: { error: { type: 'object',properties: { message: { type: 'string' },statusCode: { type: 'number' },timestamp: { type: 'string',format: 'date-time' },},},},},Success: { type: 'object',properties: { success: { type: 'boolean' },data: { type: 'object' },message: { type: 'string' },},},}} public generateMarkdown() { let markdown = '# API Documentation\n\n'; this && this.endpoints.forEach((endpoint) => { markdown += `## ${endpoint && endpoint.method.toUpperCase()} ${endpoint && endpoint.path}\n\n`; markdown += `${endpoint && endpoint.description}\n\n`; if (endpoint && endpoint.parameters && endpoint && endpoint.parameters.length > 0) { markdown += '### Parameters\n\n'; markdown += '| Name | Type | Required | Location | Description |\n'; markdown += '|------|------|----------|----------|-------------|\n'; endpoint && endpoint.parameters.forEach((param) => { markdown += `| ${param && param.name} | ${param && param.type} | ${param && param.required ? 'Yes' : 'No'} | ${param && param.location} | ${param && param.description} |\n`}); markdown += '\n'} if (endpoint && endpoint.responses && endpoint && endpoint.responses.length > 0) { markdown += '### Responses\n\n'; endpoint && endpoint.responses.forEach((response) => { markdown += `- **${response && response.status}**: ${response && response.description}\n`}); markdown += '\n'} if (endpoint && endpoint.examples && endpoint && endpoint.examples.length > 0) { markdown += '### Examples\n\n'; endpoint && endpoint.examples.forEach((example) => { markdown += `#### ${example && example.name}\n\n`; markdown += `**Request:**\n`; markdown += `\`\`\`json\n${JSON && JSON.stringify(example && example.request,null,2)}\n\`\`\`\n\n`; markdown += `**Response:**\n`; markdown += `\`\`\`json\n${JSON && JSON.stringify(example && example.response,null,2)}\n\`\`\`\n\n`})} markdown += '---\n\n'}); return markdown} } export const apiDocGenerator = new ApiDocumentationGenerator(); export default function handler(req: NextApiRequest,res: NextApiResponse) { if (req && req.method === 'GET') { const format = (req && req.query.format as string) || 'json'; if (format === 'markdown') { res && res.setHeader('Content-Type','text/markdown'); res && res.status(200).send(apiDocGenerator && apiDocGenerator.generateMarkdown())} else { res && res.setHeader('Content-Type','application/json'); res && res.status(200).json(apiDocGenerator && apiDocGenerator.generateOpenAPISpec())} } else { res && res.setHeader('Allow',['GET']); res && res.status(405).json({ error: 'Method not allowed' })} }
import { NextApiRequest,NextApiResponse } from 'next'; type HttpMethod = 'get' | 'post' | 'put' | 'patch' | 'delete'; interface ApiParameter { name: string; type: string; required: boolean; description: string; location: 'query' | 'body' | 'header' | 'path'} interface ApiResponse { status: number; description: string; schema?: unknown} interface ApiExample { name: string; request: unknown; response: unknown} interface Endpoint { path: string; method: HttpMethod; description: string; parameters?: ApiParameter[]; responses?: ApiResponse[]; examples?: ApiExample[]} class ApiDocumentationGenerator { private endpoints: Endpoint[] = []; public addEndpoint(endpoint: Endpoint) { this && this.endpoints.push(endpoint)} public generateOpenAPISpec() { return { openapi: '3 && 3.0.0',info: { title: 'Zion Tech Group API',version: '1 && 1.0.0',description: 'API documentation for Zion Tech Group services',},servers: [ { url: process && process.env.NEXT_PUBLIC_API_URL || 'http: description: 'Development server',},],paths: this && this.generatePaths(),components: { schemas: this && this.generateSchemas(),},}} private generatePaths() { const paths: Record<string,any> = {}; this && this.endpoints.forEach((endpoint) => { if (!paths[endpoint && endpoint.path]) { paths[endpoint && endpoint.path] = {}} paths[endpoint && endpoint.path][endpoint && endpoint.method.toLowerCase()] = { summary: endpoint && endpoint.description,parameters: endpoint && endpoint.parameters?.map((param) => ({ name: param && param.name,in: param && param.location,required: param && param.required,schema: { type: param && param.type },description: param && param.description,})) ?? [],responses: (endpoint && endpoint.responses ?? []).reduce((acc,response) => { acc[response && response.status] = { description: response && response.description,content: response && response.schema ? { 'application/json': { schema: response && response.schema,},} : undefined,}; return acc},{} as Record<string,any>),}}); return paths} private generateSchemas() { return { Error: { type: 'object',properties: { error: { type: 'object',properties: { message: { type: 'string' },statusCode: { type: 'number' },timestamp: { type: 'string',format: 'date-time' },},},},},Success: { type: 'object',properties: { success: { type: 'boolean' },data: { type: 'object' },message: { type: 'string' },},},}} public generateMarkdown() { let markdown = '# API Documentation\n\n'; this && this.endpoints.forEach((endpoint) => { markdown += `## ${endpoint && endpoint.method.toUpperCase()} ${endpoint && endpoint.path}\n\n`; markdown += `${endpoint && endpoint.description}\n\n`; if (endpoint && endpoint.parameters && endpoint && endpoint.parameters.length > 0) { markdown += '### Parameters\n\n'; markdown += '| Name | Type | Required | Location | Description |\n'; markdown += '|------|------|----------|----------|-------------|\n'; endpoint && endpoint.parameters.forEach((param) => { markdown += `| ${param && param.name} | ${param && param.type} | ${param && param.required ? 'Yes' : 'No'} | ${param && param.location} | ${param && param.description} |\n`}); markdown += '\n'} if (endpoint && endpoint.responses && endpoint && endpoint.responses.length > 0) { markdown += '### Responses\n\n'; endpoint && endpoint.responses.forEach((response) => { markdown += `- **${response && response.status}**: ${response && response.description}\n`}); markdown += '\n'} if (endpoint && endpoint.examples && endpoint && endpoint.examples.length > 0) { markdown += '### Examples\n\n'; endpoint && endpoint.examples.forEach((example) => { markdown += `#### ${example && example.name}\n\n`; markdown += `**Request:**\n`; markdown += `\`\`\`json\n${JSON && JSON.stringify(example && example.request,null,2)}\n\`\`\`\n\n`; markdown += `**Response:**\n`; markdown += `\`\`\`json\n${JSON && JSON.stringify(example && example.response,null,2)}\n\`\`\`\n\n`})} markdown += '---\n\n'}); return markdown} } export const apiDocGenerator = new ApiDocumentationGenerator(); export default function handler(req: NextApiRequest,res: NextApiResponse) { if (req && req.method === 'GET') { const format = (req && req.query.format as string) || 'json'; if (format === 'markdown') { res && res.setHeader('Content-Type','text/markdown'); res && res.status(200).send(apiDocGenerator && apiDocGenerator.generateMarkdown())} else { res && res.setHeader('Content-Type','application/json'); res && res.status(200).json(apiDocGenerator && apiDocGenerator.generateOpenAPISpec())} } else { res && res.setHeader('Allow',['GET']); res && res.status(405).json({ error: 'Method not allowed' })} }
import { NextApiRequest,NextApiResponse } from 'next'; type HttpMethod = 'get' | 'post' | 'put' | 'patch' | 'delete'; interface ApiParameter { name: string; type: string; required: boolean; description: string; location: 'query' | 'body' | 'header' | 'path'} interface ApiResponse { status: number; description: string; schema?: unknown} interface ApiExample { name: string; request: unknown; response: unknown} interface Endpoint { path: string; method: HttpMethod; description: string; parameters?: ApiParameter[]; responses?: ApiResponse[]; examples?: ApiExample[]} class ApiDocumentationGenerator { private endpoints: Endpoint[] = []; public addEndpoint(endpoint: Endpoint) { this && this.endpoints.push(endpoint)} public generateOpenAPISpec() { return { openapi: '3 && 3.0.0',info: { title: 'Zion Tech Group API',version: '1 && 1.0.0',description: 'API documentation for Zion Tech Group services',},servers: [ { url: process && process.env.NEXT_PUBLIC_API_URL || 'http: description: 'Development server',},],paths: this && this.generatePaths(),components: { schemas: this && this.generateSchemas(),},}} private generatePaths() { const paths: Record<string,any> = {}; this && this.endpoints.forEach((endpoint) => { if (!paths[endpoint && endpoint.path]) { paths[endpoint && endpoint.path] = {}} paths[endpoint && endpoint.path][endpoint && endpoint.method.toLowerCase()] = { summary: endpoint && endpoint.description,parameters: endpoint && endpoint.parameters?.map((param) => ({ name: param && param.name,in: param && param.location,required: param && param.required,schema: { type: param && param.type },description: param && param.description,})) ?? [],responses: (endpoint && endpoint.responses ?? []).reduce((acc,response) => { acc[response && response.status] = { description: response && response.description,content: response && response.schema ? { 'application/json': { schema: response && response.schema,},} : undefined,}; return acc},{} as Record<string,any>),}}); return paths} private generateSchemas() { return { Error: { type: 'object',properties: { error: { type: 'object',properties: { message: { type: 'string' },statusCode: { type: 'number' },timestamp: { type: 'string',format: 'date-time' },},},},},Success: { type: 'object',properties: { success: { type: 'boolean' },data: { type: 'object' },message: { type: 'string' },},},}} public generateMarkdown() { let markdown = '# API Documentation\n\n'; this && this.endpoints.forEach((endpoint) => { markdown += `## ${endpoint && endpoint.method.toUpperCase()} ${endpoint && endpoint.path}\n\n`; markdown += `${endpoint && endpoint.description}\n\n`; if (endpoint && endpoint.parameters && endpoint && endpoint.parameters.length > 0) { markdown += '### Parameters\n\n'; markdown += '| Name | Type | Required | Location | Description |\n'; markdown += '|------|------|----------|----------|-------------|\n'; endpoint && endpoint.parameters.forEach((param) => { markdown += `| ${param && param.name} | ${param && param.type} | ${param && param.required ? 'Yes' : 'No'} | ${param && param.location} | ${param && param.description} |\n`}); markdown += '\n'} if (endpoint && endpoint.responses && endpoint && endpoint.responses.length > 0) { markdown += '### Responses\n\n'; endpoint && endpoint.responses.forEach((response) => { markdown += `- **${response && response.status}**: ${response && response.description}\n`}); markdown += '\n'} if (endpoint && endpoint.examples && endpoint && endpoint.examples.length > 0) { markdown += '### Examples\n\n'; endpoint && endpoint.examples.forEach((example) => { markdown += `#### ${example && example.name}\n\n`; markdown += `**Request:**\n`; markdown += `\`\`\`json\n${JSON && JSON.stringify(example && example.request,null,2)}\n\`\`\`\n\n`; markdown += `**Response:**\n`; markdown += `\`\`\`json\n${JSON && JSON.stringify(example && example.response,null,2)}\n\`\`\`\n\n`})} markdown += '---\n\n'}); return markdown} } export const apiDocGenerator = new ApiDocumentationGenerator(); export default function handler(req: NextApiRequest,res: NextApiResponse) { if (req && req.method === 'GET') { const format = (req && req.query.format as string) || 'json'; if (format === 'markdown') { res && res.setHeader('Content-Type','text/markdown'); res && res.status(200).send(apiDocGenerator && apiDocGenerator.generateMarkdown())} else { res && res.setHeader('Content-Type','application/json'); res && res.status(200).json(apiDocGenerator && apiDocGenerator.generateOpenAPISpec())} } else { res && res.setHeader('Allow',['GET']); res && res.status(405).json({ error: 'Method not allowed' })} }
import { NextApiRequest,NextApiResponse } from 'next'; type HttpMethod = 'get' | 'post' | 'put' | 'patch' | 'delete'; interface ApiParameter { name: string; type: string; required: boolean; description: string; location: 'query' | 'body' | 'header' | 'path'} interface ApiResponse { status: number; description: string; schema?: unknown} interface ApiExample { name: string; request: unknown; response: unknown} interface Endpoint { path: string; method: HttpMethod; description: string; parameters?: ApiParameter[]; responses?: ApiResponse[]; examples?: ApiExample[]} class ApiDocumentationGenerator { private endpoints: Endpoint[] = []; public addEndpoint(endpoint: Endpoint) { this && this.endpoints.push(endpoint)} public generateOpenAPISpec() { return { openapi: '3 && 3.0.0',info: { title: 'Zion Tech Group API',version: '1 && 1.0.0',description: 'API documentation for Zion Tech Group services',},servers: [ { url: process && process.env.NEXT_PUBLIC_API_URL || 'http: description: 'Development server',},],paths: this && this.generatePaths(),components: { schemas: this && this.generateSchemas(),},}} private generatePaths() { const paths: Record<string,any> = {}; this && this.endpoints.forEach((endpoint) => { if (!paths[endpoint && endpoint.path]) { paths[endpoint && endpoint.path] = {}} paths[endpoint && endpoint.path][endpoint && endpoint.method.toLowerCase()] = { summary: endpoint && endpoint.description,parameters: endpoint && endpoint.parameters?.map((param) => ({ name: param && param.name,in: param && param.location,required: param && param.required,schema: { type: param && param.type },description: param && param.description,})) ?? [],responses: (endpoint && endpoint.responses ?? []).reduce((acc,response) => { acc[response && response.status] = { description: response && response.description,content: response && response.schema ? { 'application/json': { schema: response && response.schema,},} : undefined,}; return acc},{} as Record<string,any>),}}); return paths} private generateSchemas() { return { Error: { type: 'object',properties: { error: { type: 'object',properties: { message: { type: 'string' },statusCode: { type: 'number' },timestamp: { type: 'string',format: 'date-time' },},},},},Success: { type: 'object',properties: { success: { type: 'boolean' },data: { type: 'object' },message: { type: 'string' },},},}} public generateMarkdown() { let markdown = '# API Documentation\n\n'; this && this.endpoints.forEach((endpoint) => { markdown += `## ${endpoint && endpoint.method.toUpperCase()} ${endpoint && endpoint.path}\n\n`; markdown += `${endpoint && endpoint.description}\n\n`; if (endpoint && endpoint.parameters && endpoint && endpoint.parameters.length > 0) { markdown += '### Parameters\n\n'; markdown += '| Name | Type | Required | Location | Description |\n'; markdown += '|------|------|----------|----------|-------------|\n'; endpoint && endpoint.parameters.forEach((param) => { markdown += `| ${param && param.name} | ${param && param.type} | ${param && param.required ? 'Yes' : 'No'} | ${param && param.location} | ${param && param.description} |\n`}); markdown += '\n'} if (endpoint && endpoint.responses && endpoint && endpoint.responses.length > 0) { markdown += '### Responses\n\n'; endpoint && endpoint.responses.forEach((response) => { markdown += `- **${response && response.status}**: ${response && response.description}\n`}); markdown += '\n'} if (endpoint && endpoint.examples && endpoint && endpoint.examples.length > 0) { markdown += '### Examples\n\n'; endpoint && endpoint.examples.forEach((example) => { markdown += `#### ${example && example.name}\n\n`; markdown += `**Request:**\n`; markdown += `\`\`\`json\n${JSON && JSON.stringify(example && example.request,null,2)}\n\`\`\`\n\n`; markdown += `**Response:**\n`; markdown += `\`\`\`json\n${JSON && JSON.stringify(example && example.response,null,2)}\n\`\`\`\n\n`})} markdown += '---\n\n'}); return markdown} } export const apiDocGenerator = new ApiDocumentationGenerator(); export default function handler(req: NextApiRequest,res: NextApiResponse) { if (req && req.method === 'GET') { const format = (req && req.query.format as string) || 'json'; if (format === 'markdown') { res && res.setHeader('Content-Type','text/markdown'); res && res.status(200).send(apiDocGenerator && apiDocGenerator.generateMarkdown())} else { res && res.setHeader('Content-Type','application/json'); res && res.status(200).json(apiDocGenerator && apiDocGenerator.generateOpenAPISpec())} } else { res && res.setHeader('Allow',['GET']); res && res.status(405).json({ error: 'Method not allowed' })} }
import { NextApiRequest,NextApiResponse } from 'next'; type HttpMethod = 'get' | 'post' | 'put' | 'patch' | 'delete'; interface ApiParameter { name: string, type: string, required: boolean, description: string, location: 'query' | 'body' | 'header' | 'path'} } interface ApiResponse { status: 'number, description: string, schema?: unknown;' } } interface ApiExample { name: 'string, request: unknown, response: unknown;' } } interface Endpoint { path: 'string, method: HttpMethod, description: string, parameters?: ApiParameter[], responses?: ApiResponse[]; examples?: ApiExample[];' } } class ApiDocumentationGenerator { private endpoints: Endpoint[] = []; public addEndpoint(endpoint: Endpoint) { this && this.endpoints.push(endpoint)} public generateOpenAPISpec() { return { openapi: '3 && 3.0.0',info: { title: 'Zion Tech Group API',version: '1 && 1.0.0',description: 'API documentation for Zion Tech Group services',},servers: [ { url: process && process.env.NEXT_PUBLIC_API_URL || 'http: description: 'Development server',},],paths: this && this.generatePaths(),components: { schemas: this && this.generateSchemas(),},}} private generatePaths() { const paths: 'Record<string',any> = {}; this && this.endpoints.forEach((endpoint) => { if (!paths[endpoint && endpoint.path]) { paths[endpoint && endpoint.path] = {}} paths[endpoint && endpoint.path][endpoint && endpoint.method.toLowerCase()] = { summary: 'endpoint && endpoint.description',parameters: endpoint && endpoint.parameters?.map((param) => ({ name: param && param.name,in: 'param && param.location',required: 'param && param.required',schema: { type: param && param.type },description: 'param && param.description',})) ?? [],responses: (endpoint && endpoint.responses ?? []).reduce((acc,response) => { acc[response && response.status] = { description: 'response && response.description',content: response && response.schema ? { 'application/json': { schema: response && response.schema,},} : undefined,}; return acc},{} as Record<string,any>),}}); return paths} private generateSchemas() { return { Error: { type: 'object',properties: { error: { type: 'object',properties: { message: { type: 'string' },statusCode: { type: 'number' },timestamp: { type: 'string',format: 'date-time' },},},},},Success: { type: 'object',properties: { success: { type: 'boolean' },data: { type: 'object' },message: { type: 'string' },},},}} public generateMarkdown() { let markdown = '# API Documentation\n\n'; this && this.endpoints.forEach((endpoint) => { markdown += `## ${endpoint && endpoint.method.toUpperCase()} ${endpoint && endpoint.path}\n\n`; markdown += `${endpoint && endpoint.description}\n\n`; if (endpoint && endpoint.parameters && endpoint && endpoint.parameters.length > 0) { markdown += '### Parameters\n\n'; markdown += '| Name | Type | Required | Location | Description |\n'; markdown += '|------|------|----------|----------|-------------|\n'; endpoint && endpoint.parameters.forEach((param) => { markdown += `| ${param && param.name} | ${param && param.type} | ${param && param.required ? 'Yes' : 'No'} | ${param && param.location} | ${param && param.description} |\n`}); markdown += '\n'} if (endpoint && endpoint.responses && endpoint && endpoint.responses.length > 0) { markdown += '### Responses\n\n'; endpoint && endpoint.responses.forEach((response) => { markdown += `- **${response && response.status}**: ${response && response.description}\n`}); markdown += '\n'} if (endpoint && endpoint.examples && endpoint && endpoint.examples.length > 0) { markdown += '### Examples\n\n'; endpoint && endpoint.examples.forEach((example) => { markdown += `#### ${example && example.name}\n\n`; markdown += `**Request:**\n`; markdown += `\`\`\`json\n${JSON && JSON.stringify(example && example.request,null,2)}\n\`\`\`\n\n`; markdown += `**Response:**\n`; markdown += `\`\`\`json\n${JSON && JSON.stringify(example && example.response,null,2)}\n\`\`\`\n\n`})} markdown += '---\n\n'}); return markdown} } export const apiDocGenerator = new ApiDocumentationGenerator(); export default function handler(req: 'NextApiRequest',res: NextApiResponse) { if (req && req.method === 'GET') { const format = (req && req.query.format as string) || 'json'; if (format === 'markdown') { res && res.setHeader('Content-Type','text/markdown'); res && res.status(200).send(apiDocGenerator && apiDocGenerator.generateMarkdown())} else { res && res.setHeader('Content-Type','application/json'); res && res.status(200).json(apiDocGenerator && apiDocGenerator.generateOpenAPISpec())} } else { res && res.setHeader('Allow',['GET']); res && res.status(405).json({ error: 'Method not allowed' })} }
import { NextApiRequest,NextApiResponse } from 'next'; type HttpMethod = 'get' | 'post' | 'put' | 'patch' | 'delete'; interface ApiParameter { name: string, type: string, required: boolean, description: string, location: 'query' | 'body' | 'header' | 'path'} interface ApiResponse { status: number, description: string, schema?: unknown} interface ApiExample { name: string, request: unknown, response: unknown} interface Endpoint { path: string, method: HttpMethod, description: string, parameters?: ApiParameter[], responses?: ApiResponse[]; examples?: ApiExample[]} class ApiDocumentationGenerator { private endpoints: Endpoint[] = []; public addEndpoint(endpoint: Endpoint) { this && this.endpoints.push(endpoint)} public generateOpenAPISpec() { return { openapi: '3 && 3.0.0',info: { title: 'Zion Tech Group API',version: '1 && 1.0.0',description: 'API documentation for Zion Tech Group services',},servers: [ { url: process && process.env.NEXT_PUBLIC_API_URL || 'http: description: 'Development server',},],paths: this && this.generatePaths(),components: { schemas: this && this.generateSchemas(),},}} private generatePaths() { const paths: Record<string,any> = {}; this && this.endpoints.forEach((endpoint) => { if (!paths[endpoint && endpoint.path]) { paths[endpoint && endpoint.path] = {}} paths[endpoint && endpoint.path][endpoint && endpoint.method.toLowerCase()] = { summary: endpoint && endpoint.description,parameters: endpoint && endpoint.parameters?.map((param) => ({ name: param && param.name,in: param && param.location,required: param && param.required,schema: { type: param && param.type },description: param && param.description,})) ?? [],responses: (endpoint && endpoint.responses ?? []).reduce((acc,response) => { acc[response && response.status] = { description: response && response.description,content: response && response.schema ? { 'application/json': { schema: response && response.schema,},} : undefined,}; return acc},{} as Record<string,any>),}}); return paths} private generateSchemas() { return { Error: { type: 'object',properties: { error: { type: 'object',properties: { message: { type: 'string' },statusCode: { type: 'number' },timestamp: { type: 'string',format: 'date-time' },},},},},Success: { type: 'object',properties: { success: { type: 'boolean' },data: { type: 'object' },message: { type: 'string' },},},}} public generateMarkdown() { let markdown = '# API Documentation\n\n'; this && this.endpoints.forEach((endpoint) => { markdown += `## ${endpoint && endpoint.method.toUpperCase()} ${endpoint && endpoint.path}\n\n`; markdown += `${endpoint && endpoint.description}\n\n`; if (endpoint && endpoint.parameters && endpoint && endpoint.parameters.length > 0) { markdown += '### Parameters\n\n'; markdown += '| Name | Type | Required | Location | Description |\n'; markdown += '|------|------|----------|----------|-------------|\n'; endpoint && endpoint.parameters.forEach((param) => { markdown += `| ${param && param.name} | ${param && param.type} | ${param && param.required ? 'Yes' : 'No'} | ${param && param.location} | ${param && param.description} |\n`}); markdown += '\n'} if (endpoint && endpoint.responses && endpoint && endpoint.responses.length > 0) { markdown += '### Responses\n\n'; endpoint && endpoint.responses.forEach((response) => { markdown += `- **${response && response.status}**: ${response && response.description}\n`}); markdown += '\n'} if (endpoint && endpoint.examples && endpoint && endpoint.examples.length > 0) { markdown += '### Examples\n\n'; endpoint && endpoint.examples.forEach((example) => { markdown += `#### ${example && example.name}\n\n`; markdown += `**Request:**\n`; markdown += `\`\`\`json\n${JSON && JSON.stringify(example && example.request,null,2)}\n\`\`\`\n\n`; markdown += `**Response:**\n`; markdown += `\`\`\`json\n${JSON && JSON.stringify(example && example.response,null,2)}\n\`\`\`\n\n`})} markdown += '---\n\n'}); return markdown} } export const apiDocGenerator = new ApiDocumentationGenerator(); export default function handler(req: NextApiRequest,res: NextApiResponse) { if (req && req.method === 'GET') { const format = (req && req.query.format as string) || 'json'; if (format === 'markdown') { res && res.setHeader('Content-Type','text/markdown'); res && res.status(200).send(apiDocGenerator && apiDocGenerator.generateMarkdown())} else { res && res.setHeader('Content-Type','application/json'); res && res.status(200).json(apiDocGenerator && apiDocGenerator.generateOpenAPISpec())} } else { res && res.setHeader('Allow',['GET']); res && res.status(405).json({ error: 'Method not allowed' })} }
import { NextApiRequest,NextApiResponse } from 'next'; type HttpMethod = 'get' | 'post' | 'put' | 'patch' | 'delete'; interface ApiParameter { name: string, type: string, required: boolean, description: string, location: 'query' | 'body' | 'header' | 'path'} interface ApiResponse { status: number, description: string, schema?: unknown} interface ApiExample { name: string, request: unknown, response: unknown} interface Endpoint { path: string, method: HttpMethod, description: string, parameters?: ApiParameter[], responses?: ApiResponse[]; examples?: ApiExample[]} class ApiDocumentationGenerator { private endpoints: Endpoint[] = []; public addEndpoint(endpoint: Endpoint) { this && this.endpoints.push(endpoint)} public generateOpenAPISpec() { return { openapi: '3 && 3.0.0',info: { title: 'Zion Tech Group API',version: '1 && 1.0.0',description: 'API documentation for Zion Tech Group services',},servers: [ { url: process && process.env.NEXT_PUBLIC_API_URL || 'http: description: 'Development server',},],paths: this && this.generatePaths(),components: { schemas: this && this.generateSchemas(),},}} private generatePaths() { const paths: Record<string,any> = {}; this && this.endpoints.forEach((endpoint) => { if (!paths[endpoint && endpoint.path]) { paths[endpoint && endpoint.path] = {}} paths[endpoint && endpoint.path][endpoint && endpoint.method.toLowerCase()] = { summary: endpoint && endpoint.description,parameters: endpoint && endpoint.parameters?.map((param) => ({ name: param && param.name,in: param && param.location,required: param && param.required,schema: { type: param && param.type },description: param && param.description,})) ?? [],responses: (endpoint && endpoint.responses ?? []).reduce((acc,response) => { acc[response && response.status] = { description: response && response.description,content: response && response.schema ? { 'application/json': { schema: response && response.schema,},} : undefined,}; return acc},{} as Record<string,any>),}}); return paths} private generateSchemas() { return { Error: { type: 'object',properties: { error: { type: 'object',properties: { message: { type: 'string' },statusCode: { type: 'number' },timestamp: { type: 'string',format: 'date-time' },},},},},Success: { type: 'object',properties: { success: { type: 'boolean' },data: { type: 'object' },message: { type: 'string' },},},}} public generateMarkdown() { let markdown = '# API Documentation\n\n'; this && this.endpoints.forEach((endpoint) => { markdown += `## ${endpoint && endpoint.method.toUpperCase()} ${endpoint && endpoint.path}\n\n`; markdown += `${endpoint && endpoint.description}\n\n`; if (endpoint && endpoint.parameters && endpoint && endpoint.parameters.length > 0) { markdown += '### Parameters\n\n'; markdown += '| Name | Type | Required | Location | Description |\n'; markdown += '|------|------|----------|----------|-------------|\n'; endpoint && endpoint.parameters.forEach((param) => { markdown += `| ${param && param.name} | ${param && param.type} | ${param && param.required ? 'Yes' : 'No'} | ${param && param.location} | ${param && param.description} |\n`}); markdown += '\n'} if (endpoint && endpoint.responses && endpoint && endpoint.responses.length > 0) { markdown += '### Responses\n\n'; endpoint && endpoint.responses.forEach((response) => { markdown += `- **${response && response.status}**: ${response && response.description}\n`}); markdown += '\n'} if (endpoint && endpoint.examples && endpoint && endpoint.examples.length > 0) { markdown += '### Examples\n\n'; endpoint && endpoint.examples.forEach((example) => { markdown += `#### ${example && example.name}\n\n`; markdown += `**Request:**\n`; markdown += `\`\`\`json\n${JSON && JSON.stringify(example && example.request,null,2)}\n\`\`\`\n\n`; markdown += `**Response:**\n`; markdown += `\`\`\`json\n${JSON && JSON.stringify(example && example.response,null,2)}\n\`\`\`\n\n`})} markdown += '---\n\n'}); return markdown} } export const apiDocGenerator = new ApiDocumentationGenerator(); export default function handler(req: NextApiRequest,res: NextApiResponse) { if (req && req.method === 'GET') { const format = (req && req.query.format as string) || 'json'; if (format === 'markdown') { res && res.setHeader('Content-Type','text/markdown'); res && res.status(200).send(apiDocGenerator && apiDocGenerator.generateMarkdown())} else { res && res.setHeader('Content-Type','application/json'); res && res.status(200).json(apiDocGenerator && apiDocGenerator.generateOpenAPISpec())} } else { res && res.setHeader('Allow',['GET']); res && res.status(405).json({ error: 'Method not allowed' })} }
import { NextApiRequest,NextApiResponse } from 'next'; type HttpMethod = 'get' | 'post' | 'put' | 'patch' | 'delete'; interface ApiParameter { name: string, type: string, required: boolean, description: string, location: 'query' | 'body' | 'header' | 'path'} interface ApiResponse { status: number, description: string, schema?: unknown} interface ApiExample { name: string, request: unknown, response: unknown} interface Endpoint { path: string, method: HttpMethod, description: string, parameters?: ApiParameter[], responses?: ApiResponse[]; examples?: ApiExample[]} class ApiDocumentationGenerator { private endpoints: Endpoint[] = []; public addEndpoint(endpoint: Endpoint) { this && this.endpoints.push(endpoint)} public generateOpenAPISpec() { return { openapi: '3 && 3.0.0',info: { title: 'Zion Tech Group API',version: '1 && 1.0.0',description: 'API documentation for Zion Tech Group services',},servers: [ { url: process && process.env.NEXT_PUBLIC_API_URL || 'http: description: 'Development server',},],paths: this && this.generatePaths(),components: { schemas: this && this.generateSchemas(),},}} private generatePaths() { const paths: Record<string,any> = {}; this && this.endpoints.forEach((endpoint) => { if (!paths[endpoint && endpoint.path]) { paths[endpoint && endpoint.path] = {}} paths[endpoint && endpoint.path][endpoint && endpoint.method.toLowerCase()] = { summary: endpoint && endpoint.description,parameters: endpoint && endpoint.parameters?.map((param) => ({ name: param && param.name,in: param && param.location,required: param && param.required,schema: { type: param && param.type },description: param && param.description,})) ?? [],responses: (endpoint && endpoint.responses ?? []).reduce((acc,response) => { acc[response && response.status] = { description: response && response.description,content: response && response.schema ? { 'application/json': { schema: response && response.schema,},} : undefined,}; return acc},{} as Record<string,any>),}}); return paths} private generateSchemas() { return { Error: { type: 'object',properties: { error: { type: 'object',properties: { message: { type: 'string' },statusCode: { type: 'number' },timestamp: { type: 'string',format: 'date-time' },},},},},Success: { type: 'object',properties: { success: { type: 'boolean' },data: { type: 'object' },message: { type: 'string' },},},}} public generateMarkdown() { let markdown = '# API Documentation\n\n'; this && this.endpoints.forEach((endpoint) => { markdown += `## ${endpoint && endpoint.method.toUpperCase()} ${endpoint && endpoint.path}\n\n`; markdown += `${endpoint && endpoint.description}\n\n`; if (endpoint && endpoint.parameters && endpoint && endpoint.parameters.length > 0) { markdown += '### Parameters\n\n'; markdown += '| Name | Type | Required | Location | Description |\n'; markdown += '|------|------|----------|----------|-------------|\n'; endpoint && endpoint.parameters.forEach((param) => { markdown += `| ${param && param.name} | ${param && param.type} | ${param && param.required ? 'Yes' : 'No'} | ${param && param.location} | ${param && param.description} |\n`}); markdown += '\n'} if (endpoint && endpoint.responses && endpoint && endpoint.responses.length > 0) { markdown += '### Responses\n\n'; endpoint && endpoint.responses.forEach((response) => { markdown += `- **${response && response.status}**: ${response && response.description}\n`}); markdown += '\n'} if (endpoint && endpoint.examples && endpoint && endpoint.examples.length > 0) { markdown += '### Examples\n\n'; endpoint && endpoint.examples.forEach((example) => { markdown += `#### ${example && example.name}\n\n`; markdown += `**Request:**\n`; markdown += `\`\`\`json\n${JSON && JSON.stringify(example && example.request,null,2)}\n\`\`\`\n\n`; markdown += `**Response:**\n`; markdown += `\`\`\`json\n${JSON && JSON.stringify(example && example.response,null,2)}\n\`\`\`\n\n`})} markdown += '---\n\n'}); return markdown} } export const apiDocGenerator = new ApiDocumentationGenerator(); export default function handler(req: NextApiRequest,res: NextApiResponse) { if (req && req.method === 'GET') { const format = (req && req.query.format as string) || 'json'; if (format === 'markdown') { res && res.setHeader('Content-Type','text/markdown'); res && res.status(200).send(apiDocGenerator && apiDocGenerator.generateMarkdown())} else { res && res.setHeader('Content-Type','application/json'); res && res.status(200).json(apiDocGenerator && apiDocGenerator.generateOpenAPISpec())} } else { res && res.setHeader('Allow',['GET']); res && res.status(405).json({ error: 'Method not allowed' })} }
import { NextApiRequest,NextApiResponse } from 'next'; type HttpMethod = 'get' | 'post' | 'put' | 'patch' | 'delete'; interface ApiParameter { name: string, type: string, required: boolean, description: string, location: 'query' | 'body' | 'header' | 'path'} interface ApiResponse { status: number, description: string, schema?: unknown} interface ApiExample { name: string, request: unknown, response: unknown} interface Endpoint { path: string, method: HttpMethod, description: string, parameters?: ApiParameter[], responses?: ApiResponse[]; examples?: ApiExample[]} class ApiDocumentationGenerator { private endpoints: Endpoint[] = []; public addEndpoint(endpoint: Endpoint) { this && this.endpoints.push(endpoint)} public generateOpenAPISpec() { return { openapi: '3 && 3.0.0',info: { title: 'Zion Tech Group API',version: '1 && 1.0.0',description: 'API documentation for Zion Tech Group services',},servers: [ { url: process && process.env.NEXT_PUBLIC_API_URL || 'http: description: 'Development server',},],paths: this && this.generatePaths(),components: { schemas: this && this.generateSchemas(),},}} private generatePaths() { const paths: Record<string,any> = {}; this && this.endpoints.forEach((endpoint) => { if (!paths[endpoint && endpoint.path]) { paths[endpoint && endpoint.path] = {}} paths[endpoint && endpoint.path][endpoint && endpoint.method.toLowerCase()] = { summary: endpoint && endpoint.description,parameters: endpoint && endpoint.parameters?.map((param) => ({ name: param && param.name,in: param && param.location,required: param && param.required,schema: { type: param && param.type },description: param && param.description,})) ?? [],responses: (endpoint && endpoint.responses ?? []).reduce((acc,response) => { acc[response && response.status] = { description: response && response.description,content: response && response.schema ? { 'application/json': { schema: response && response.schema,},} : undefined,}; return acc},{} as Record<string,any>),}}); return paths} private generateSchemas() { return { Error: { type: 'object',properties: { error: { type: 'object',properties: { message: { type: 'string' },statusCode: { type: 'number' },timestamp: { type: 'string',format: 'date-time' },},},},},Success: { type: 'object',properties: { success: { type: 'boolean' },data: { type: 'object' },message: { type: 'string' },},},}} public generateMarkdown() { let markdown = '# API Documentation\n\n'; this && this.endpoints.forEach((endpoint) => { markdown += `## ${endpoint && endpoint.method.toUpperCase()} ${endpoint && endpoint.path}\n\n`; markdown += `${endpoint && endpoint.description}\n\n`; if (endpoint && endpoint.parameters && endpoint && endpoint.parameters.length > 0) { markdown += '### Parameters\n\n'; markdown += '| Name | Type | Required | Location | Description |\n'; markdown += '|------|------|----------|----------|-------------|\n'; endpoint && endpoint.parameters.forEach((param) => { markdown += `| ${param && param.name} | ${param && param.type} | ${param && param.required ? 'Yes' : 'No'} | ${param && param.location} | ${param && param.description} |\n`}); markdown += '\n'} if (endpoint && endpoint.responses && endpoint && endpoint.responses.length > 0) { markdown += '### Responses\n\n'; endpoint && endpoint.responses.forEach((response) => { markdown += `- **${response && response.status}**: ${response && response.description}\n`}); markdown += '\n'} if (endpoint && endpoint.examples && endpoint && endpoint.examples.length > 0) { markdown += '### Examples\n\n'; endpoint && endpoint.examples.forEach((example) => { markdown += `#### ${example && example.name}\n\n`; markdown += `**Request:**\n`; markdown += `\`\`\`json\n${JSON && JSON.stringify(example && example.request,null,2)}\n\`\`\`\n\n`; markdown += `**Response:**\n`; markdown += `\`\`\`json\n${JSON && JSON.stringify(example && example.response,null,2)}\n\`\`\`\n\n`})} markdown += '---\n\n'}); return markdown} } export const apiDocGenerator = new ApiDocumentationGenerator(); export default function handler(req: NextApiRequest,res: NextApiResponse) { if (req && req.method === 'GET') { const format = (req && req.query.format as string) || 'json'; if (format === 'markdown') { res && res.setHeader('Content-Type','text/markdown'); res && res.status(200).send(apiDocGenerator && apiDocGenerator.generateMarkdown())} else { res && res.setHeader('Content-Type','application/json'); res && res.status(200).json(apiDocGenerator && apiDocGenerator.generateOpenAPISpec())} } else { res && res.setHeader('Allow',['GET']); res && res.status(405).json({ error: 'Method not allowed' })} }
>>>>>>> origin/cursor/automate-test-improve-and-merge-code-382a:temp_exclude/pages.bak/api/docs.ts
=======
<<<<<<< HEAD
=======
import { NextApiRequest,NextApiResponse } from 'next'; type HttpMethod = 'get' | 'post' | 'put' | 'patch' | 'delete'; interface ApiParameter { name: string; type: string; required: boolean; description: string; location: 'query' | 'body' | 'header' | 'path'} interface ApiResponse { status: number; description: string; schema?: unknown} interface ApiExample { name: string; request: unknown; response: unknown} interface Endpoint { path: string; method: HttpMethod; description: string; parameters?: ApiParameter[]; responses?: ApiResponse[]; examples?: ApiExample[]} class ApiDocumentationGenerator { private endpoints: Endpoint[] = []; public addEndpoint(endpoint: Endpoint) { this.endpoints.push(endpoint)} public generateOpenAPISpec() { return { openapi: '3.0.0',info: { title: 'Zion Tech Group API',version: '1.0.0',description: 'API documentation for Zion Tech Group services',},servers: [ { url: process.env.NEXT_PUBLIC_API_URL || 'http: description: 'Development server',},],paths: this.generatePaths(),components: { schemas: this.generateSchemas(),},}} private generatePaths() { const paths: Record<string,any> = {}; this.endpoints.forEach((endpoint) => { if (!paths[endpoint.path]) { paths[endpoint.path] = {}} paths[endpoint.path][endpoint.method.toLowerCase()] = { summary: endpoint.description,parameters: endpoint.parameters?.map((param) => ({ name: param.name,in: param.location,required: param.required,schema: { type: param.type },description: param.description,})) ?? [],responses: (endpoint.responses ?? []).reduce((acc,response) => { acc[response.status] = { description: response.description,content: response.schema ? { 'application/json': { schema: response.schema,},} : undefined,}; return acc},{} as Record<string,any>),}}); return paths} private generateSchemas() { return { Error: { type: 'object',properties: { error: { type: 'object',properties: { message: { type: 'string' },statusCode: { type: 'number' },timestamp: { type: 'string',format: 'date-time' },},},},},Success: { type: 'object',properties: { success: { type: 'boolean' },data: { type: 'object' },message: { type: 'string' },},},}} public generateMarkdown() { let markdown = '# API Documentation\n\n'; this.endpoints.forEach((endpoint) => { markdown += `## ${endpoint.method.toUpperCase()} ${endpoint.path}\n\n`; markdown += `${endpoint.description}\n\n`; if (endpoint.parameters && endpoint.parameters.length > 0) { markdown += '### Parameters\n\n'; markdown += '| Name | Type | Required | Location | Description |\n'; markdown += '|------|------|----------|----------|-------------|\n'; endpoint.parameters.forEach((param) => { markdown += `| ${param.name} | ${param.type} | ${param.required ? 'Yes' : 'No'} | ${param.location} | ${param.description} |\n`}); markdown += '\n'} if (endpoint.responses && endpoint.responses.length > 0) { markdown += '### Responses\n\n'; endpoint.responses.forEach((response) => { markdown += `- **${response.status}**: ${response.description}\n`}); markdown += '\n'} if (endpoint.examples && endpoint.examples.length > 0) { markdown += '### Examples\n\n'; endpoint.examples.forEach((example) => { markdown += `#### ${example.name}\n\n`; markdown += `**Request:**\n`; markdown += `\`\`\`json\n${JSON.stringify(example.request,null,2)}\n\`\`\`\n\n`; markdown += `**Response:**\n`; markdown += `\`\`\`json\n${JSON.stringify(example.response,null,2)}\n\`\`\`\n\n`})} markdown += '---\n\n'}); return markdown} } export const apiDocGenerator = new ApiDocumentationGenerator(); export default function handler(req: NextApiRequest,res: NextApiResponse) { if (req.method === 'GET') { const format = (req.query.format as string) || 'json'; if (format === 'markdown') { res.setHeader('Content-Type','text/markdown'); res.status(200).send(apiDocGenerator.generateMarkdown())} else { res.setHeader('Content-Type','application/json'); res.status(200).json(apiDocGenerator.generateOpenAPISpec())} } else { res.setHeader('Allow',['GET']); res.status(405).json({ error: 'Method not allowed' })} }
>>>>>>> cursor/add-new-services-and-deploy-updates-0462
=======
import { NextApiRequest,NextApiResponse } from 'next'; type HttpMethod = 'get' | 'post' | 'put' | 'patch' | 'delete'; interface ApiParameter { name: string; type: string; required: boolean; description: string; location: 'query' | 'body' | 'header' | 'path'} interface ApiResponse { status: number; description: string; schema?: unknown} interface ApiExample { name: string; request: unknown; response: unknown} interface Endpoint { path: string; method: HttpMethod; description: string; parameters?: ApiParameter[]; responses?: ApiResponse[]; examples?: ApiExample[]} class ApiDocumentationGenerator { private endpoints: Endpoint[] = []; public addEndpoint(endpoint: Endpoint) { this.endpoints.push(endpoint)} public generateOpenAPISpec() { return { openapi: '3.0.0',info: { title: 'Zion Tech Group API',version: '1.0.0',description: 'API documentation for Zion Tech Group services',},servers: [ { url: process.env.NEXT_PUBLIC_API_URL || 'http: description: 'Development server',},],paths: this.generatePaths(),components: { schemas: this.generateSchemas(),},}} private generatePaths() { const paths: Record<string,any> = {}; this.endpoints.forEach((endpoint) => { if (!paths[endpoint.path]) { paths[endpoint.path] = {}} paths[endpoint.path][endpoint.method.toLowerCase()] = { summary: endpoint.description,parameters: endpoint.parameters?.map((param) => ({ name: param.name,in: param.location,required: param.required,schema: { type: param.type },description: param.description,})) ?? [],responses: (endpoint.responses ?? []).reduce((acc,response) => { acc[response.status] = { description: response.description,content: response.schema ? { 'application/json': { schema: response.schema,},} : undefined,}; return acc},{} as Record<string,any>),}}); return paths} private generateSchemas() { return { Error: { type: 'object',properties: { error: { type: 'object',properties: { message: { type: 'string' },statusCode: { type: 'number' },timestamp: { type: 'string',format: 'date-time' },},},},},Success: { type: 'object',properties: { success: { type: 'boolean' },data: { type: 'object' },message: { type: 'string' },},},}} public generateMarkdown() { let markdown = '# API Documentation\n\n'; this.endpoints.forEach((endpoint) => { markdown += `## ${endpoint.method.toUpperCase()} ${endpoint.path}\n\n`; markdown += `${endpoint.description}\n\n`; if (endpoint.parameters && endpoint.parameters.length > 0) { markdown += '### Parameters\n\n'; markdown += '| Name | Type | Required | Location | Description |\n'; markdown += '|------|------|----------|----------|-------------|\n'; endpoint.parameters.forEach((param) => { markdown += `| ${param.name} | ${param.type} | ${param.required ? 'Yes' : 'No'} | ${param.location} | ${param.description} |\n`}); markdown += '\n'} if (endpoint.responses && endpoint.responses.length > 0) { markdown += '### Responses\n\n'; endpoint.responses.forEach((response) => { markdown += `- **${response.status}**: ${response.description}\n`}); markdown += '\n'} if (endpoint.examples && endpoint.examples.length > 0) { markdown += '### Examples\n\n'; endpoint.examples.forEach((example) => { markdown += `#### ${example.name}\n\n`; markdown += `**Request:**\n`; markdown += `\`\`\`json\n${JSON.stringify(example.request,null,2)}\n\`\`\`\n\n`; markdown += `**Response:**\n`; markdown += `\`\`\`json\n${JSON.stringify(example.response,null,2)}\n\`\`\`\n\n`})} markdown += '---\n\n'}); return markdown} } export const apiDocGenerator = new ApiDocumentationGenerator(); export default function handler(req: NextApiRequest,res: NextApiResponse) { if (req.method === 'GET') { const format = (req.query.format as string) || 'json'; if (format === 'markdown') { res.setHeader('Content-Type','text/markdown'); res.status(200).send(apiDocGenerator.generateMarkdown())} else { res.setHeader('Content-Type','application/json'); res.status(200).json(apiDocGenerator.generateOpenAPISpec())} } else { res.setHeader('Allow',['GET']); res.status(405).json({ error: 'Method not allowed' })} }
import { NextApiRequest,NextApiResponse } from 'next'; type HttpMethod = 'get' | 'post' | 'put' | 'patch' | 'delete'; interface ApiParameter { name: string; type: string; required: boolean; description: string; location: 'query' | 'body' | 'header' | 'path'} interface ApiResponse { status: number; description: string; schema?: unknown} interface ApiExample { name: string; request: unknown; response: unknown} interface Endpoint { path: string; method: HttpMethod; description: string; parameters?: ApiParameter[]; responses?: ApiResponse[]; examples?: ApiExample[]} class ApiDocumentationGenerator { private endpoints: Endpoint[] = []; public addEndpoint(endpoint: Endpoint) { this.endpoints.push(endpoint)} public generateOpenAPISpec() { return { openapi: '3.0.0',info: { title: 'Zion Tech Group API',version: '1.0.0',description: 'API documentation for Zion Tech Group services',},servers: [ { url: process.env.NEXT_PUBLIC_API_URL || 'http: description: 'Development server',},],paths: this.generatePaths(),components: { schemas: this.generateSchemas(),},}} private generatePaths() { const paths: Record<string,any> = {}; this.endpoints.forEach((endpoint) => { if (!paths[endpoint.path]) { paths[endpoint.path] = {}} paths[endpoint.path][endpoint.method.toLowerCase()] = { summary: endpoint.description,parameters: endpoint.parameters?.map((param) => ({ name: param.name,in: param.location,required: param.required,schema: { type: param.type },description: param.description,})) ?? [],responses: (endpoint.responses ?? []).reduce((acc,response) => { acc[response.status] = { description: response.description,content: response.schema ? { 'application/json': { schema: response.schema,},} : undefined,}; return acc},{} as Record<string,any>),}}); return paths} private generateSchemas() { return { Error: { type: 'object',properties: { error: { type: 'object',properties: { message: { type: 'string' },statusCode: { type: 'number' },timestamp: { type: 'string',format: 'date-time' },},},},},Success: { type: 'object',properties: { success: { type: 'boolean' },data: { type: 'object' },message: { type: 'string' },},},}} public generateMarkdown() { let markdown = '# API Documentation\n\n'; this.endpoints.forEach((endpoint) => { markdown += `## ${endpoint.method.toUpperCase()} ${endpoint.path}\n\n`; markdown += `${endpoint.description}\n\n`; if (endpoint.parameters && endpoint.parameters.length > 0) { markdown += '### Parameters\n\n'; markdown += '| Name | Type | Required | Location | Description |\n'; markdown += '|------|------|----------|----------|-------------|\n'; endpoint.parameters.forEach((param) => { markdown += `| ${param.name} | ${param.type} | ${param.required ? 'Yes' : 'No'} | ${param.location} | ${param.description} |\n`}); markdown += '\n'} if (endpoint.responses && endpoint.responses.length > 0) { markdown += '### Responses\n\n'; endpoint.responses.forEach((response) => { markdown += `- **${response.status}**: ${response.description}\n`}); markdown += '\n'} if (endpoint.examples && endpoint.examples.length > 0) { markdown += '### Examples\n\n'; endpoint.examples.forEach((example) => { markdown += `#### ${example.name}\n\n`; markdown += `**Request:**\n`; markdown += `\`\`\`json\n${JSON.stringify(example.request,null,2)}\n\`\`\`\n\n`; markdown += `**Response:**\n`; markdown += `\`\`\`json\n${JSON.stringify(example.response,null,2)}\n\`\`\`\n\n`})} markdown += '---\n\n'}); return markdown} } export const apiDocGenerator = new ApiDocumentationGenerator(); export default function handler(req: NextApiRequest,res: NextApiResponse) { if (req.method === 'GET') { const format = (req.query.format as string) || 'json'; if (format === 'markdown') { res.setHeader('Content-Type','text/markdown'); res.status(200).send(apiDocGenerator.generateMarkdown())} else { res.setHeader('Content-Type','application/json'); res.status(200).json(apiDocGenerator.generateOpenAPISpec())} } else { res.setHeader('Allow',['GET']); res.status(405).json({ error: 'Method not allowed' })} }
import { NextApiRequest,NextApiResponse } from 'next'; type HttpMethod = 'get' | 'post' | 'put' | 'patch' | 'delete'; interface ApiParameter { name: string; type: string; required: boolean; description: string; location: 'query' | 'body' | 'header' | 'path'} interface ApiResponse { status: number; description: string; schema?: unknown} interface ApiExample { name: string; request: unknown; response: unknown} interface Endpoint { path: string; method: HttpMethod; description: string; parameters?: ApiParameter[]; responses?: ApiResponse[]; examples?: ApiExample[]} class ApiDocumentationGenerator { private endpoints: Endpoint[] = []; public addEndpoint(endpoint: Endpoint) { this.endpoints.push(endpoint)} public generateOpenAPISpec() { return { openapi: '3.0.0',info: { title: 'Zion Tech Group API',version: '1.0.0',description: 'API documentation for Zion Tech Group services',},servers: [ { url: process.env.NEXT_PUBLIC_API_URL || 'http: description: 'Development server',},],paths: this.generatePaths(),components: { schemas: this.generateSchemas(),},}} private generatePaths() { const paths: Record<string,any> = {}; this.endpoints.forEach((endpoint) => { if (!paths[endpoint.path]) { paths[endpoint.path] = {}} paths[endpoint.path][endpoint.method.toLowerCase()] = { summary: endpoint.description,parameters: endpoint.parameters?.map((param) => ({ name: param.name,in: param.location,required: param.required,schema: { type: param.type },description: param.description,})) ?? [],responses: (endpoint.responses ?? []).reduce((acc,response) => { acc[response.status] = { description: response.description,content: response.schema ? { 'application/json': { schema: response.schema,},} : undefined,}; return acc},{} as Record<string,any>),}}); return paths} private generateSchemas() { return { Error: { type: 'object',properties: { error: { type: 'object',properties: { message: { type: 'string' },statusCode: { type: 'number' },timestamp: { type: 'string',format: 'date-time' },},},},},Success: { type: 'object',properties: { success: { type: 'boolean' },data: { type: 'object' },message: { type: 'string' },},},}} public generateMarkdown() { let markdown = '# API Documentation\n\n'; this.endpoints.forEach((endpoint) => { markdown += `## ${endpoint.method.toUpperCase()} ${endpoint.path}\n\n`; markdown += `${endpoint.description}\n\n`; if (endpoint.parameters && endpoint.parameters.length > 0) { markdown += '### Parameters\n\n'; markdown += '| Name | Type | Required | Location | Description |\n'; markdown += '|------|------|----------|----------|-------------|\n'; endpoint.parameters.forEach((param) => { markdown += `| ${param.name} | ${param.type} | ${param.required ? 'Yes' : 'No'} | ${param.location} | ${param.description} |\n`}); markdown += '\n'} if (endpoint.responses && endpoint.responses.length > 0) { markdown += '### Responses\n\n'; endpoint.responses.forEach((response) => { markdown += `- **${response.status}**: ${response.description}\n`}); markdown += '\n'} if (endpoint.examples && endpoint.examples.length > 0) { markdown += '### Examples\n\n'; endpoint.examples.forEach((example) => { markdown += `#### ${example.name}\n\n`; markdown += `**Request:**\n`; markdown += `\`\`\`json\n${JSON.stringify(example.request,null,2)}\n\`\`\`\n\n`; markdown += `**Response:**\n`; markdown += `\`\`\`json\n${JSON.stringify(example.response,null,2)}\n\`\`\`\n\n`})} markdown += '---\n\n'}); return markdown} } export const apiDocGenerator = new ApiDocumentationGenerator(); export default function handler(req: NextApiRequest,res: NextApiResponse) { if (req.method === 'GET') { const format = (req.query.format as string) || 'json'; if (format === 'markdown') { res.setHeader('Content-Type','text/markdown'); res.status(200).send(apiDocGenerator.generateMarkdown())} else { res.setHeader('Content-Type','application/json'); res.status(200).json(apiDocGenerator.generateOpenAPISpec())} } else { res.setHeader('Allow',['GET']); res.status(405).json({ error: 'Method not allowed' })} }
>>>>>>> origin/cursor/integrate-build-improve-and-re-verify-c7b5
>>>>>>> origin/cursor/integrate-build-improve-and-re-verify-8b20
