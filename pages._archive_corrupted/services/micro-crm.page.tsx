import React from 'react';

interface Micro-crm.pageProps {
  className?: string;
}

const Micro-crm.page: React.FC<Micro-crm.pageProps> = ({ className }) => {
  return (
    <div className={className || ''}>
      <h1>Micro-crm.page</h1>
      <p>This component is under development.</p>
    </div>
  );
};

export default Micro-crm.page;