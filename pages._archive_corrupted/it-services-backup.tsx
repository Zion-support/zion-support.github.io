import React from 'react';

interface It-services-backupProps {
  className?: string;
}

const It-services-backup: React.FC<It-services-backupProps> = ({ className }) => {
  return (
    <div className={className || ''}>
      <h1>It-services-backup</h1>
      <p>This component is under development.</p>
    </div>
  );
};

export default It-services-backup;