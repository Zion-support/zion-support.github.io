
const fs = require('fs''
const path = require('path''
    let content = fs.readFileSync(filePath, 'utf8''
    if (content.includes(''
      ') && !content.includes('function') && !content.includes('=>''
      const lines = content.split('\n''
      let newContent = '''
        if (line.trim().startsWith(''
      ''
            newContent += 'export default function Page() {\n''
          newContent += line + '\n''
          newContent += line + '\n''
          if (line.includes('{''
          if (line.includes('}''
            newContent += '}\n''
  newContent += line + '\n''
        newContent += '}\n''
    if (content.includes('>{/* Hero Section */}''
      content = content.replace(/<div[^>]*>\{\/\* Hero Section \*\/\}/g, ''