
import React from 'react';

    on: jest.fn (),
    off: jest.fn (),
    emit: jest.fn ();
  },
  is_fallback: false,
  is_locale: false,
  is_ready: true,
  default_locale: 'en',
  domain_locales: [],
  is_preview: false;


});
export const createMockProps = (overrides = {}) => ({;
  ...overrides;




});