};
};
  return (;
    <a href={resolved} className={className} {...rest}>;
  };
  };
  return (,
    <a href={resolved} className={className} {...rest}>,
      {children};
};
};
};}};
  return (
    <a href={resolved} className={className} {...rest}>;
      {children}}}};
