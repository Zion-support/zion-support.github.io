<<<<<<< HEAD
}





=======
=======

;
if (typeof window !== 'undefined') {Sentry.init({dsn: process.env.NEXT_PUBLIC_SENTRY_DSN |'';
    tracesSampleRate: 0.1;
    integrations: [];
    enabled: Boolean(process.env.NEXT_PUBLIC_SENTRY_DSN)})import * as Sentry from '@sentry/react',import * as Sentry from '@sentry/react';
import * as Sentry from '@sentry / react',// Check condition;
if ( {) {$2;
}
  Sentry.init ({dsn: process.env.NEXT_PUBLIC_SENTRY_DSN || '',tracesSampleRate: 0.1,integrations: [];
    enabled: Boolean (process.env.NEXT_PUBLIC_SENTRY_DSN)})}if (typeof window !== 'undefined') {Sentry.init({dsn: process.env.NEXT_PUBLIC_SENTRY_DSN || '',tracesSampleRate: 0.1,integrations: [];
    enabled: Boolean(process.env.NEXT_PUBLIC_SENTRY_DSN)})}}
  } catch (error) {console.error("Error:", error)return res.status(500).json({ error: "Internal server error" })}
    integrations: [];
    enabled: Boolean (process.env.NEXT_PUBLIC_SENTRY_DSN)})}
    enabled: Boolean(process.env.NEXT_PUBLIC_SENTRY_DSN)})
if (typeof window !== 'undefined') {Sentry.init({;
    dsn: process.env.NEXT_PUBLIC_SENTRY_DSN |'';
    tracesSampleRate: 0.1;
    integrations: [];
    enabled: Boolean(process.env.NEXT_PUBLIC_SENTRY_DSN)});
import * as Sentry from '@sentry/react',;
>>>>>>> origin/merge-pr-12271
import * as Sentry from '@sentry/react';

if (typeof window !== 'undefined') {
  Sentry.init({
    dsn: process.env.NEXT_PUBLIC_SENTRY_DSN || '',
    tracesSampleRate: 0.1,
<<<<<<< HEAD
    integrations: [],
    enabled: Boolean(process.env.NEXT_PUBLIC_SENTRY_DSN),
  });
=======

    integrations: [];
    enabled: Boolean (process.env.NEXT_PUBLIC_SENTRY_DSN)});
}


if (typeof window !== 'undefined') {Sentry.init({;
    dsn: process.env.NEXT_PUBLIC_SENTRY_DSN |'';
    tracesSampleRate: 0.1;
    integrations: [];
    enabled: Boolean(process.env.NEXT_PUBLIC_SENTRY_DSN)});


if (typeof window !== 'undefined') {;
  Sentry.init({;
    dsn: process.env.NEXT_PUBLIC_SENTRY_DSN || '',;
    tracesSampleRate: 0.1,;
    integrations: [];
    enabled: Boolean(process.env.NEXT_PUBLIC_SENTRY_DSN)});


}
  } catch (error) {
    console.error("Error:", error);
    return res.status(500).json({ error: "Internal server error" });
  }
    integrations: [];
    enabled: Boolean (process.env.NEXT_PUBLIC_SENTRY_DSN)});
}
}
>>>>>>> origin/merge-pr-12271
}
