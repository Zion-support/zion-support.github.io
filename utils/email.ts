import fs from 'fs-extra'
import path from 'path'
import fs from 'fs - extra'
import path from 'path'
  const logDir = path.resolve(process.cwd(), 'data/
  const logDir = path && path.resolve(process && process.cwd(), 'data/
    const sgMail = await import('@sendgrid/