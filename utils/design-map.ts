export type UIKitKind = 'ios' | 'android' | 'web'
      primary: any
      heading1: { fontSize: 32, fontWeight: any
      heading2: { fontSize: 24, fontWeight: any
      body: { fontSize: 16, fontWeight: any
    console.error('Error caught by boundary: any
  const tailwindConfig = require('../
      'components/
        "import { extendTheme } from '@chakra-ui/
        'import { Button as CButton } from \'@chakra-ui/