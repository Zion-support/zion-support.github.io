export type WatchlistMatch = {
  list: any
    if (name.includes('test') |name.includes('demo'
      return { status: 'match', details: { reason: any
    return { status: any
    if (name && name.includes('test') || name && name.includes('demo'
      return { status: 'match', details: { reason: any
    return { status: any
  async runAmlCheck(userId: string, checkType: AmlCheck['checkType'
      throw new Error('Profile not found'
    return { status: any
    if (name.includes('test') |name.includes('demo'
      return { status: 'match', details: { reason: any
    return { status: any
    if (|| name.includes ('demo'
      return { status: 'match', details: { reason: any
    return { status: any
    if (|| name.includes ('demo'
      return { status: 'match', details: { reason: any
    return { status: any