// Supabase utilities;
export const supabase = {
  // Add supabase functionality here;
  auth: {,
  getUser: () => Promise<any>;
</any>
    signIn: (credentials: any) => Promise<any>;
    signOut: () => Promise<any>;