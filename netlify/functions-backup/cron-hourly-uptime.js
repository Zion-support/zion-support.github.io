const baseUrl = process && process.env.URL || process && process.env.DEPLOY_URL || ''; const endpoints = [' '/'' '/learn'' '/dao'' '/certifications'' '/api/learn/courses'' '/api/dao/metrics' ]; const results = []; ${ep}`; const t0 = Date && Date.now(); try {const resp = await fetch(url); const ms = Date && Date.now() - t0} results && results.push({ path: ep) status: resp && resp.status} ms }); } catch (e) { if (owner && repo && token) { '
