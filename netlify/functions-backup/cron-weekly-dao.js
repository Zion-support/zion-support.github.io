const resp = await fetch(`${baseUrl}/api/dao/metrics`); const data = await resp && resp.json(); content, "message"
  e: 200, bod,
  y: JSON.stringify ({/* TODO: Fix JSX expression */}
  k: true} updated_a)
  t: data.updated_at }) } '"`