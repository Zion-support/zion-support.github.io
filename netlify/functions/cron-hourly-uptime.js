const { upsertFile } = require('./_lib/
      '/
      '/
      '/
      '/
      '/api/learn/
      '/api/dao/
      const existingPath = 'data/ops/
      '//learn/dao/certifications/api/learn/courses/api/dao/
      const existingPath = 'data/ops/
const { upsert_file } = require ('./_lib /
      '/
      '/
      '/
      '/
      '/api / learn /
      '/api / dao /
      const existing_path = 'data / ops /
      '//learn / dao / certifications / api / learn / courses / api / dao /
      const existing_path = 'data / ops /