const path = require('path');
const { spawnSync } = require('child_process');

function runNode(relPath, args = []) {
  const abs = path.resolve(__dirname, '..', '..', relPath);
<<<<<<<< HEAD:netlify/functions/automation-insights-runner.js.backup.1755991529
  const res = spawnSync('node', [abs, ...args], { stdio: 'pipe', encoding: 'utf8' });
========
  const res = spawnSync('node', [abs, ...args], { stdio: 'pipe', encoding: 'utf8', shell: true });
>>>>>>>> 5fe4d4a516412daaecc6e2e3437df4b5fdac83d3:netlify/functions/automation-guardian-runner.js
  return { status: res.status || 0, stdout: res.stdout || '', stderr: res.stderr || '' };
}

exports.config = {
<<<<<<<< HEAD:netlify/functions/automation-insights-runner.js.backup.1755991529
  schedule: '*/20 * * * *', // every 20 minutes
========
  schedule: '*/10 * * * *',
>>>>>>>> 5fe4d4a516412daaecc6e2e3437df4b5fdac83d3:netlify/functions/automation-guardian-runner.js
};

exports.handler = async () => {
  const logs = [];
  function logStep(name, fn) {
    logs.push(`\n=== ${name} ===`);
    const { status, stdout, stderr } = fn();
    if (stdout) logs.push(stdout);
    if (stderr) logs.push(stderr);
    logs.push(`exit=${status}`);
    return status;
  }

<<<<<<<< HEAD:netlify/functions/automation-insights-runner.js.backup.1755991529
<<<<<<< HEAD:netlify/functions/automation-insights-runner.js
  logStep('automation:insights', () => runNode('automation/automation-insights-digest.cjs'));
  logStep('git:sync', () => runNode('automation/advanced-git-sync.cjs'));
=======
  // Generate sitemap for crawling
  logStep('sitemap:generate', () => runNode('scripts/generate-sitemap.js'));

  // Build search index if available
  try {
    logStep('search:index', () => runNode('scripts/generate-search-index.js'));
  } catch (error) {
    logs.push(`Search index generation skipped: ${String(error)}`);
  }

  // Commit and push
  logStep('git:sync', () => runNode('automation/git-sync.cjs'));
>>>>>>> origin/cursor/develop-autonomous-cloud-agents-for-automation-orchestration-ded0:netlify/functions/site-crawler.js
========
  // Run the automation guardian
  logStep('automation:guardian', () => runNode('automation/automation-guardian-10min.cjs'));

  // Attempt to push any changes
  logStep('git:sync', () => runNode('automation/advanced-git-sync.cjs'));
>>>>>>>> 5fe4d4a516412daaecc6e2e3437df4b5fdac83d3:netlify/functions/automation-guardian-runner.js

  return { statusCode: 200, body: logs.join('\n') };
};