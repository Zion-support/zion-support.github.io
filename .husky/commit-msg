#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Make commit-msg non-blocking to avoid CI/editor constraints
if command -v npx >/dev/null 2>&1; then
  npx --yes commitlint --edit "$1" || true
fi

exit 0
