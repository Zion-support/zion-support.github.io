#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Keep commit-msg quick and non-blocking if commitlint is unavailable
if command -v npx >/dev/null 2>&1; then
  npx --yes commitlint --edit "$1" || true
fi

exit 0
