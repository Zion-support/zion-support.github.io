import React from 'react';
import { Loader2 } from 'lucide-react';
export interface LoadingOverlayProps {
  visible?: boolean
}

export const LoadingOverlay: React.FC<LoadingOverlayProps> = ({ visible = $2;
  return (
    <div className = $2;
export default LoadingOverlay,
