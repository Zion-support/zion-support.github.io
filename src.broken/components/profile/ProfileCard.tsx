import { Card, CardHeader, CardContent, CardTitle } from "@/components/ui/
import { TalentProfile } from "@/types/
import { Card, CardHeader, CardContent, CardTitle } from "@/components/ui/
import { TalentProfile } from "@/types/