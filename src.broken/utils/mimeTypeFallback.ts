
    if(lastDot === -1) return '';
    return filename.substring(lastDot).toLowerCase()}

      script.src = url;
      script.async = true;
      script.type = 'text/javascript';
      return script} else {

      link.rel = 'stylesheet';
      link.href = url;
