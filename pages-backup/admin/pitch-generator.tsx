" " " " " " " " // Added useCallback" " " " interface Slide { id: string; title: string;" content: string;" type: string;" chartType?: "bar" | "funnel" | "timeline"; } const PitchGeneratorPage: NextPage = () => { const { user, isLoading: loading } = useAuth(); const router = useRouter(); const { errorToast } = useToast(); const [currentStep, setCurrentStep] = useState<"inputs" | "data" | "editor">( "inputs" ); 