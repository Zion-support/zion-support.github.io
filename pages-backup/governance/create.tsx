// pages/governance/create.tsx // Assuming shadcn/ui components are available and auto-imported or aliased via @/components/ui // // If exists // // If frontend auth is needed for API calls // // If wallet info is needed const proposalTypes = [ "FEATURE", "BUDGET", "COMMUNITY_GRANT", "GENERAL", ] as const; const proposalSchema = z.object({ title: z.string().min(5, { message: "Title must be at least 5 characters." }), summary: z .string() .min(20, { message: "Summary must be at least 20 characters." }), proposal_type: z.enum(proposalTypes), voting_starts_at: z.string().optional().nullable(), // Using string for datetime-local voting_ends_at: z.string().optional().nullable(), // Using string for datetime-local quorum_percentage: z.preprocess( val => val === " || val === undefined || val === null ? undefined : parseFloat(String(val)), z.number().min(0).max(1).optional().nullable() ), funding_ask_amount: z.preprocess( val => val === " || val === undefined || val === null ? undefined : parseFloat(String(val)), 