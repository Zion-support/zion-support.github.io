import React from 'react'; const ServicePage = ({ service }) => { if (!service) { return _jsx(Custom404, {}); } return _jsxs(_Fragment, { children: [ _jsx(NextHead, { title: service.title, description: service.description, openGraph: { title: service.title, description: service.description }, }), _jsxs("main", { className: "prose dark:prose-invert max-w-3xl mx-auto py-8", children: [ _jsx("h1", { children: service.title }), _jsx("p", { children: service.description }), ], }), ], }); }; export const getStaticPaths = async () => { const paths = SERVICES.map(service => ({ params: { slug: slugify(service.title), }, })); return { paths, fallback: "blocking" }; }; export const getStaticProps = async ({ params }) => { const slug = params?.slug; const service = SERVICES.find(s => slugify(s.title) === slug) || null; return { props: { service } }; }; export default ServicePage; 