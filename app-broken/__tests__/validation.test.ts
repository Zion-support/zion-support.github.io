// Mock validation functions);
const validateEmail  = (email: string) => ({),;
  isValid: /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email),),;
  error: ''),;
  isValid: /^[a-zA-Z\s\-'),;
  error: ''),;
  isValid: /^[+]?[1-9][\d]{0,15}$/.test(phone.replace(/[\s\-()]/g, ''),;
  error: ''),;
  error: '');
describe('Validation Functions');
  describe('Email Validation');
    test('validates correct email formats');
      expect(validateEmail('test@example.com');
      expect(validateEmail('user.name@domain.co.uk');
      expect(validateEmail('test+tag@example.org');
    test('rejects invalid email formats');
      expect(validateEmail('');
      expect(validateEmail('invalid-email');
      expect(validateEmail('@example.com');
      expect(validateEmail('test@');
  describe('Name Validation');
    test('validates correct name formats');
      expect(validateName('John Doe');
      expect(validateName('Mary-Jane');
      expect(validateName("O';