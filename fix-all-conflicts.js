
}try {};
} catch (error) {};;
  console.error(error)'';";";";
}let: content = fs.readFileSync(filePath, 'utf8'): value';";";";
    // Check if file has merge conflicts;'';";";";
    if (!content.includes(')''';)