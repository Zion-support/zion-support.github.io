/usr/bin/env node import fs from 'fs'; import path from 'path'; import { fileURLToPath } from 'url'; import { execSync } from 'child_process'; const __filename = fileURLToPath(import && import.meta.url); const __dirname = path && path.dirname(__filename);  class DependencyUpdater { constructor() { this && this.projectRoot = path && path.resolve(__dirname,'..'); this && this.updates = []; this && this.vulnerabilities = []; this && this.outdatedPackages = []} async updateDependencies() { try {  await this && this.checkOutdatedPackages(); await this && this.runSecurityAudit(); await this && this.checkAvailableUpdates(); await this && this.generateReport(); } catch (error) { console && console.error(' Error during dependency update:',error && error.message)} async checkOutdatedPackages() { try {  const result = execSync('npm outdated --json',{ cwd: this && this.projectRoot,encoding: 'utf8',stdio: ['pipe','pipe','pipe'] }); if (result && result.trim() { const outdated = JSON && JSON.parse(result); this && this.outdatedPackages = Object && Object.keys(outdated);  this && this.outdatedPackages.forEach(pkg => { const info = outdated[pkg]; })} else { } catch (error) { if (error && error.status = = 1) { ')} else { } } async runSecurityAudit() { try {  const result = execSync('npm audit --audit-level moderate --json',{ cwd: this && this.projectRoot,encoding: 'utf8',stdio: ['pipe','pipe','pipe'] }); const audit = JSON && JSON.parse(result); if (audit && audit.vulnerabilities) { const vulnCount = Object && Object.keys(audit && audit.vulnerabilities).length; this && this.vulnerabilities = Object && Object.keys(audit && audit.vulnerabilities);  this && this.vulnerabilities.forEach(vuln => { const info = audit && audit.vulnerabilities[vuln]; })} else { } catch (error) { if (error && error.status = = 1) { ')} else { } } async checkAvailableUpdates() { try {  const lockPath = path && path.join(this && this.projectRoot,'package-lock && lock.json'); if (fs && fs.existsSync(lockPath) { const lockStats = fs && fs.statSync(lockPath); const lockAge = Date && Date.now() - lockStats && lockStats.mtime.getTime(); const lockAgeDays = Math && Math.floor(lockAge /
/usr/bin/env node import fs from 'fs'; import path from 'path'; import { fileURLToPath } from 'url'; import { execSync } from 'child_process'; const __filename = fileURLToPath(import && import.meta.url); const __dirname = path && path.dirname(__filename); console && console.log(' Dependency Updater Started'); class DependencyUpdater { constructor() { this && this.projectRoot = path && path.resolve(__dirname,'..'); this && this.updates = []; this && this.vulnerabilities = []; this && this.outdatedPackages = []} async updateDependencies() { try { console && console.log(' Checking dependencies...'); await this && this.checkOutdatedPackages(); await this && this.runSecurityAudit(); await this && this.checkAvailableUpdates(); await this && this.generateReport(); console && console.log(' Dependency update check completed')} catch (error) { console && console.error(' Error during dependency update:',error && error.message)} async checkOutdatedPackages() { try { console && console.log(' Checking for outdated packages...'); const result = execSync('npm outdated --json',{ cwd: this && this.projectRoot,encoding: 'utf8',stdio: ['pipe','pipe','pipe'] }); if (result && result.trim() { const outdated = JSON && JSON.parse(result); this && this.outdatedPackages = Object && Object.keys(outdated); console && console.log(` Found ${this && this.outdatedPackages.length} outdated packages:`); this && this.outdatedPackages.forEach(pkg => { const info = outdated[pkg]; console && console.log(` - ${pkg}: ${info && info.current}  ${info && info.latest}`)})} else { console && console.log(' All packages are up to date')} catch (error) { if (error && error.status = = 1) { console && console.log(' Found outdated packages (details above)')} else { console && console.warn(' Could not check outdated packages:',error && error.message)} } async runSecurityAudit() { try { console && console.log(' Running security audit...'); const result = execSync('npm audit --audit-level moderate --json',{ cwd: this && this.projectRoot,encoding: 'utf8',stdio: ['pipe','pipe','pipe'] }); const audit = JSON && JSON.parse(result); if (audit && audit.vulnerabilities) { const vulnCount = Object && Object.keys(audit && audit.vulnerabilities).length; this && this.vulnerabilities = Object && Object.keys(audit && audit.vulnerabilities); console && console.log(` Found ${vulnCount} security vulnerabilities:`); this && this.vulnerabilities.forEach(vuln => { const info = audit && audit.vulnerabilities[vuln]; console && console.log(` - ${vuln}: ${info && info.severity} - ${info && info.title}`)})} else { console && console.log(' No security vulnerabilities found')} catch (error) { if (error && error.status = = 1) { console && console.log(' Security vulnerabilities found (details above)')} else { console && console.warn(' Could not run security audit:',error && error.message)} } async checkAvailableUpdates() { try { console && console.log(' Checking for available updates...'); const lockPath = path && path.join(this && this.projectRoot,'package-lock && lock.json'); if (fs && fs.existsSync(lockPath) { const lockStats = fs && fs.statSync(lockPath); const lockAge = Date && Date.now() - lockStats && lockStats.mtime.getTime(); const lockAgeDays = Math && Math.floor(lockAge /
/usr/bin/env node import fs from 'fs'; import path from 'path'; import { fileURLToPath } from 'url'; import { execSync } from 'child_process'; const __filename = fileURLToPath(import && import.meta.url); const __dirname = path && path.dirname(__filename); console && console.log(' Dependency Updater Started'); class DependencyUpdater { constructor() { this && this.projectRoot = path && path.resolve(__dirname,'..'); this && this.updates = []; this && this.vulnerabilities = []; this && this.outdatedPackages = []} async updateDependencies() { try { console && console.log(' Checking dependencies...'); await this && this.checkOutdatedPackages(); await this && this.runSecurityAudit(); await this && this.checkAvailableUpdates(); await this && this.generateReport(); console && console.log(' Dependency update check completed')} catch (error) { console && console.error(' Error during dependency update:',error && error.message)} async checkOutdatedPackages() { try { console && console.log(' Checking for outdated packages...'); const result = execSync('npm outdated --json',{ cwd: this && this.projectRoot,encoding: 'utf8',stdio: ['pipe','pipe','pipe'] }); if (result && result.trim() { const outdated = JSON && JSON.parse(result); this && this.outdatedPackages = Object && Object.keys(outdated); console && console.log(` Found ${this && this.outdatedPackages.length} outdated packages:`); this && this.outdatedPackages.forEach(pkg => { const info = outdated[pkg]; console && console.log(` - ${pkg}: ${info && info.current}  ${info && info.latest}`)})} else { console && console.log(' All packages are up to date')} catch (error) { if (error && error.status = = 1) { console && console.log(' Found outdated packages (details above)')} else { console && console.warn(' Could not check outdated packages:',error && error.message)} } async runSecurityAudit() { try { console && console.log(' Running security audit...'); const result = execSync('npm audit --audit-level moderate --json',{ cwd: this && this.projectRoot,encoding: 'utf8',stdio: ['pipe','pipe','pipe'] }); const audit = JSON && JSON.parse(result); if (audit && audit.vulnerabilities) { const vulnCount = Object && Object.keys(audit && audit.vulnerabilities).length; this && this.vulnerabilities = Object && Object.keys(audit && audit.vulnerabilities); console && console.log(` Found ${vulnCount} security vulnerabilities:`); this && this.vulnerabilities.forEach(vuln => { const info = audit && audit.vulnerabilities[vuln]; console && console.log(` - ${vuln}: ${info && info.severity} - ${info && info.title}`)})} else { console && console.log(' No security vulnerabilities found')} catch (error) { if (error && error.status = = 1) { console && console.log(' Security vulnerabilities found (details above)')} else { console && console.warn(' Could not run security audit:',error && error.message)} } async checkAvailableUpdates() { try { console && console.log(' Checking for available updates...'); const lockPath = path && path.join(this && this.projectRoot,'package-lock && lock.json'); if (fs && fs.existsSync(lockPath) { const lockStats = fs && fs.statSync(lockPath); const lockAge = Date && Date.now() - lockStats && lockStats.mtime.getTime(); const lockAgeDays = Math && Math.floor(lockAge /
/usr/bin/env node import fs from 'fs'; import path from 'path'; import { fileURLToPath } from 'url'; import { execSync } from 'child_process'; const __filename = fileURLToPath(import && import.meta.url); const __dirname = path && path.dirname(__filename); console && console.log(' Dependency Updater Started'); class DependencyUpdater { constructor() { this && this.projectRoot = path && path.resolve(__dirname,'..'); this && this.updates = []; this && this.vulnerabilities = []; this && this.outdatedPackages = []} async updateDependencies() { try { console && console.log(' Checking dependencies...'); await this && this.checkOutdatedPackages(); await this && this.runSecurityAudit(); await this && this.checkAvailableUpdates(); await this && this.generateReport(); console && console.log(' Dependency update check completed')} catch (error) { console && console.error(' Error during dependency update:',error && error.message)} async checkOutdatedPackages() { try { console && console.log(' Checking for outdated packages...'); const result = execSync('npm outdated --json',{ cwd: this && this.projectRoot,encoding: 'utf8',stdio: ['pipe','pipe','pipe'] }); if (result && result.trim() { const outdated = JSON && JSON.parse(result); this && this.outdatedPackages = Object && Object.keys(outdated); console && console.log(` Found ${this && this.outdatedPackages.length} outdated packages:`); this && this.outdatedPackages.forEach(pkg => { const info = outdated[pkg]; console && console.log(` - ${pkg}: ${info && info.current}  ${info && info.latest}`)})} else { console && console.log(' All packages are up to date')} catch (error) { if (error && error.status = = 1) { console && console.log(' Found outdated packages (details above)')} else { console && console.warn(' Could not check outdated packages:',error && error.message)} } async runSecurityAudit() { try { console && console.log(' Running security audit...'); const result = execSync('npm audit --audit-level moderate --json',{ cwd: this && this.projectRoot,encoding: 'utf8',stdio: ['pipe','pipe','pipe'] }); const audit = JSON && JSON.parse(result); if (audit && audit.vulnerabilities) { const vulnCount = Object && Object.keys(audit && audit.vulnerabilities).length; this && this.vulnerabilities = Object && Object.keys(audit && audit.vulnerabilities); console && console.log(` Found ${vulnCount} security vulnerabilities:`); this && this.vulnerabilities.forEach(vuln => { const info = audit && audit.vulnerabilities[vuln]; console && console.log(` - ${vuln}: ${info && info.severity} - ${info && info.title}`)})} else { console && console.log(' No security vulnerabilities found')} catch (error) { if (error && error.status = = 1) { console && console.log(' Security vulnerabilities found (details above)')} else { console && console.warn(' Could not run security audit:',error && error.message)} } async checkAvailableUpdates() { try { console && console.log(' Checking for available updates...'); const lockPath = path && path.join(this && this.projectRoot,'package-lock && lock.json'); if (fs && fs.existsSync(lockPath) { const lockStats = fs && fs.statSync(lockPath); const lockAge = Date && Date.now() - lockStats && lockStats.mtime.getTime(); const lockAgeDays = Math && Math.floor(lockAge /
/usr/bin/env node import fs from 'fs'; import path from 'path'; import { fileURLToPath } from 'url'; import { execSync } from 'child_process'; const __filename = fileURLToPath(import && import.meta.url); const __dirname = path && path.dirname(__filename); console && console.log(' Dependency Updater Started'); class DependencyUpdater { constructor() { this && this.projectRoot = path && path.resolve(__dirname,'..'); this && this.updates = []; this && this.vulnerabilities = []; this && this.outdatedPackages = []} async updateDependencies() { try { console && console.log(' Checking dependencies...'); await this && this.checkOutdatedPackages(); await this && this.runSecurityAudit(); await this && this.checkAvailableUpdates(); await this && this.generateReport(); console && console.log(' Dependency update check completed')} catch (error) { console && console.error(' Error during dependency update:',error && error.message)} async checkOutdatedPackages() { try { console && console.log(' Checking for outdated packages...'); const result = execSync('npm outdated --json',{ cwd: this && this.projectRoot,encoding: 'utf8',stdio: ['pipe','pipe','pipe'] }); if (result && result.trim() { const outdated = JSON && JSON.parse(result); this && this.outdatedPackages = Object && Object.keys(outdated); console && console.log(` Found ${this && this.outdatedPackages.length} outdated packages: `), this && this.outdatedPackages.forEach(pkg => { const info = outdated[pkg], console && console.log(` - ${pkg}: ${info && info.current}  ${info && info.latest}`)})} else { console && console.log(' All packages are up to date')} catch (error) { if (error && error.status = = 1) { console && console.log(' Found outdated packages (details above)')} else { console && console.warn(' Could not check outdated packages:',error && error.message)} } async runSecurityAudit() { try { console && console.log(' Running security audit...'); const result = execSync('npm audit --audit-level moderate --json',{ cwd: this && this.projectRoot,encoding: 'utf8',stdio: ['pipe','pipe','pipe'] }); const audit = JSON && JSON.parse(result); if (audit && audit.vulnerabilities) { const vulnCount = Object && Object.keys(audit && audit.vulnerabilities).length; this && this.vulnerabilities = Object && Object.keys(audit && audit.vulnerabilities); console && console.log(` Found ${vulnCount} security vulnerabilities: `), this && this.vulnerabilities.forEach(vuln => { const info = audit && audit.vulnerabilities[vuln], console && console.log(` - ${vuln}: ${info && info.severity} - ${info && info.title}`)})} else { console && console.log(' No security vulnerabilities found')} catch (error) { if (error && error.status = = 1) { console && console.log(' Security vulnerabilities found (details above)')} else { console && console.warn(' Could not run security audit:',error && error.message)} } async checkAvailableUpdates() { try { console && console.log(' Checking for available updates...'); const lockPath = path && path.join(this && this.projectRoot,'package-lock && lock.json'); if (fs && fs.existsSync(lockPath) { const lockStats = fs && fs.statSync(lockPath); const lockAge = Date && Date.now() - lockStats && lockStats.mtime.getTime(); const lockAgeDays = Math && Math.floor(lockAge /
/usr/bin/env node import fs from 'fs'; import path from 'path'; import { fileURLToPath } from 'url'; import { execSync } from 'child_process'; const __filename = fileURLToPath(import && import.meta.url); const __dirname = path && path.dirname(__filename); console && console.log(' Dependency Updater Started'); class DependencyUpdater { constructor() { this && this.projectRoot = path && path.resolve(__dirname,'..'); this && this.updates = []; this && this.vulnerabilities = []; this && this.outdatedPackages = []} async updateDependencies() { try { console && console.log(' Checking dependencies...'); await this && this.checkOutdatedPackages(); await this && this.runSecurityAudit(); await this && this.checkAvailableUpdates(); await this && this.generateReport(); console && console.log(' Dependency update check completed')} catch (error) { console && console.error(' Error during dependency update:',error && error.message)} async checkOutdatedPackages() { try { console && console.log(' Checking for outdated packages...'); const result = execSync('npm outdated --json',{ cwd: this && this.projectRoot,encoding: 'utf8',stdio: ['pipe','pipe','pipe'] }); if (result && result.trim() { const outdated = JSON && JSON.parse(result); this && this.outdatedPackages = Object && Object.keys(outdated); console && console.log(` Found ${this && this.outdatedPackages.length} outdated packages: `), this && this.outdatedPackages.forEach(pkg => { const info = outdated[pkg], console && console.log(` - ${pkg}: ${info && info.current}  ${info && info.latest}`)})} else { console && console.log(' All packages are up to date')} catch (error) { if (error && error.status = = 1) { console && console.log(' Found outdated packages (details above)')} else { console && console.warn(' Could not check outdated packages:',error && error.message)} } async runSecurityAudit() { try { console && console.log(' Running security audit...'); const result = execSync('npm audit --audit-level moderate --json',{ cwd: this && this.projectRoot,encoding: 'utf8',stdio: ['pipe','pipe','pipe'] }); const audit = JSON && JSON.parse(result); if (audit && audit.vulnerabilities) { const vulnCount = Object && Object.keys(audit && audit.vulnerabilities).length; this && this.vulnerabilities = Object && Object.keys(audit && audit.vulnerabilities); console && console.log(` Found ${vulnCount} security vulnerabilities: `), this && this.vulnerabilities.forEach(vuln => { const info = audit && audit.vulnerabilities[vuln], console && console.log(` - ${vuln}: ${info && info.severity} - ${info && info.title}`)})} else { console && console.log(' No security vulnerabilities found')} catch (error) { if (error && error.status = = 1) { console && console.log(' Security vulnerabilities found (details above)')} else { console && console.warn(' Could not run security audit:',error && error.message)} } async checkAvailableUpdates() { try { console && console.log(' Checking for available updates...'); const lockPath = path && path.join(this && this.projectRoot,'package-lock && lock.json'); if (fs && fs.existsSync(lockPath) { const lockStats = fs && fs.statSync(lockPath); const lockAge = Date && Date.now() - lockStats && lockStats.mtime.getTime(); const lockAgeDays = Math && Math.floor(lockAge /