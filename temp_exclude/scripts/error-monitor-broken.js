#!/usr/bin/env node/usr/bin/env node/usr/bin/env node const __filename = fileURLToPath(import && import.meta.url); const __dirname = path && path.dirname(__filename); class ErrorMonitor { constructor() {this && this.logDir = path && path.join(__dirname,".","logs"),this && this.errorReportDir = path && path.join(__dirname,".","error-reports"),this && this.maxLogSize = 10 * 1024 * 1024,""
#!/usr/bin/env node   class ErrorMonitor { constructor() {this && this.logDir = path && path.join(__dirname,"..","logs"),this && this.errorReportDir = path && path.join(__dirname,"..","error-reports"),this && this.maxLogSize = 10 * 1024 * 1024,
#!/usr/bin/env node/usr/bin/env node/usr/bin/env node const __filename = fileURLToPath(import && import.meta.url); const __dirname = path && path.dirname(__filename); class ErrorMonitor { constructor() {this && this.logDir = path && path.join(__dirname,".","logs"),this && this.errorReportDir = path && path.join(__dirname,".","error-reports"),this && this.maxLogSize = 10 * 1024 * 1024,""
#!/usr/bin/env node   class ErrorMonitor { constructor() {this && this.logDir = path && path.join(__dirname,"..","logs"),this && this.errorReportDir = path && path.join(__dirname,"..","error-reports"),this && this.maxLogSize = 10 * 1024 * 1024,
#!/usr/bin/env node/usr/bin/env node/usr/bin/env node const __filename = fileURLToPath(import && import.meta.url); const __dirname = path && path.dirname(__filename); class ErrorMonitor { constructor() {this && this.logDir = path && path.join(__dirname,".","logs"),this && this.errorReportDir = path && path.join(__dirname,".","error-reports"),this && this.maxLogSize = 10 * 1024 * 1024,""
#!/usr/bin/env node   class ErrorMonitor { constructor() {this && this.logDir = path && path.join(__dirname,"..","logs"),this && this.errorReportDir = path && path.join(__dirname,"..","error-reports"),this && this.maxLogSize = 10 * 1024 * 1024,
#!/usr/bin/env node/usr/bin/env node/usr/bin/env node const __filename = fileURLToPath(import && import.meta.url); const __dirname = path && path.dirname(__filename); class ErrorMonitor { constructor() {this && this.logDir = path && path.join(__dirname,".","logs"),this && this.errorReportDir = path && path.join(__dirname,".","error-reports"),this && this.maxLogSize = 10 * 1024 * 1024,""
#!/usr/bin/env node   class ErrorMonitor { constructor() {this && this.logDir = path && path.join(__dirname,"..","logs"),this && this.errorReportDir = path && path.join(__dirname,"..","error-reports"),this && this.maxLogSize = 10 * 1024 * 1024,
