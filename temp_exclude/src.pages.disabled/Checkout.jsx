<<<<<<< HEAD
=======
<<<<<<< HEAD
>>>>>>> d1459052ce02e16bd297172bbc6ba920af218e39

class ErrorBoundary extends React.Component {
  constructor(props) {
    super(props);
    this.state = { hasError: false };
  }
  
  static getDerivedStateFromError(error) {
    return { hasError: true };
  }
  
  componentDidCatch(error, errorInfo) {
    console.error('Error caught by boundary:', error, errorInfo);
  }
  
  render() {
    if (this.state.hasError) {
      return <div>Something went wrong.</div>;
    }
    
    return this.props.children;
  }
}
<<<<<<< HEAD
<<<<<<< HEAD
<<<<<<< HEAD:src && src.pages.disabled/Checkout && Checkout.jsx
<<<<<<< \"HEAD\": src && src.pages.disabled/Checkout && Checkout.jsx import React from \'react\'; export default function CheckoutPage () { const navigate = useNavigate () ; const { t } = useTranslation () ; const [items,setItems] = useState ([]) ; const form = useForm ({ \"defaultValues\": { name: \",\"email\": \",\"address\": \",\"city\": \",\"country\": \" }}) ; useEffect ( () => { if (sku) { setItems ([{ \"id\": \'sku\',\"name\": \'sku\',\"price\": \'25\',\"quantity\": \'1\' }]) ; return} const stored = safeStorage && safeStorage.getItem (getCartKey (user?.id) ) ; if (stored) { try { setItems (JSON && JSON.parse (stored) ) } catch { setItems ([]) } } },[sku]) ; const subtotal = items && items.reduce ( (sum,i) => sum + i && i.price * i && i.quantity,0) ; const onSubmit = async (data) => { try { const result = await res && res.json () ; if (!res && res.ok) throw new Error (result && result.error || \"Failed\") ; const stripe = await getStripe () ; if (stripe && result && result.clientSecret) { const payment = await stripe && stripe.confirmCardPayment (result && result.clientSecret,{ \"payment_method\": { card: { token: \"tok_visa\" },\"billing_details\": { name: data && data.name,\"email\": \'data && data.email\' }}}) ; if (payment && payment.error) throw payment && payment.error; if (user?.id) { try { await fetch (\"/api / points / add\",{ \"method\": \"POST\",\"headers\": { \"Content - Type\": \"application / json\" },\"body\": JSON && JSON.stringify ({ userId: user && user.id,\"amount\": \'subtotal\',\"orderId\": \'result && result.id\' }) import React from \'react\'; export default function CheckoutPage () { const navigate = useNavigate () ; const { t } = useTranslation () ; const [items,setItems] = useState ([]) ; const form = useForm ({ \"defaultValues\": { name: \",\"email\": \",\"address\": \",\"city\": \",\"country\": \" }}) ; useEffect ( () => { if (sku) { setItems ([{ \"id\": \'sku\',\"name\": \'sku\',\"price\": \'25\',\"quantity\": \'1\' }]) ; return} const stored = safeStorage && safeStorage.getItem (getCartKey (user?.id) ) ; if (stored) { try { setItems (JSON && JSON.parse (stored) ) } catch { setItems ([]) } } },[sku]) ; const subtotal = items && items.reduce ( (sum,i) => sum + i && i.price * i && i.quantity,0) ; const onSubmit = async (data) => { try { const result = await res && res.json () ; if (!res && res.ok) throw new Error (result && result.error || \"Failed\") ; const stripe = await getStripe () ; if (stripe && result && result.clientSecret) { const payment = await stripe && stripe.confirmCardPayment (result && result.clientSecret,{ \"payment_method\": { card: { token: \"tok_visa\" },\"billing_details\": { name: data && data.name,\"email\": \'data && data.email\' }}}) ; if (payment && payment.error) throw payment && payment.error; if (user?.id) { try { await fetch (\"/api / points / add\",{ \"method\": \"POST\",\"headers\": { \"Content - Type\": \"application / json\" },\"body\": JSON && JSON.stringify ({ userId: user && user.id,\"amount\": \'subtotal\',\"orderId\": \'result && result.id\' }) ,\"";
<<<<<<< HEAD
<<<<<<< "HEAD": src && src.pages.disabled/Checkout && Checkout.jsx import _React from 'react'; export default function CheckoutPage () { const navigate = useNavigate () ; const { t } = useTranslation () ; const [items,setItems] = useState ([]) ; const form = useForm ({ "defaultValues": { name: ","email": ","address": ","city": ","country": " }}) ; useEffect ( () => { if (sku) { setItems ([{ "id": 'sku',"name": 'sku',"price": '25',"quantity": '1' }]) ; return} const stored = safeStorage && safeStorage.getItem (getCartKey (user?.id) ) ; if (stored) { try { setItems (JSON && JSON.parse (stored) ) } catch { setItems ([]) } } },[sku]) ; const subtotal = items && items.reduce ( (sum,i) => sum + i && i.price * i && i.quantity,0) ; const onSubmit = async (data) => { try { const result = await res && res.json () ; if (!res && res.ok) throw new Error (result && result.error || "Failed") ; const stripe = await getStripe () ; if (stripe && result && result.clientSecret) { const payment = await stripe && stripe.confirmCardPayment (result && result.clientSecret,{ "payment_method": { card: { token: "tok_visa" },"billing_details": { name: data && data.name,"email": 'data && data.email' }}}) ; if (payment && payment.error) throw payment && payment.error; if (user?.id) { try { await fetch ("/api / points / add",{ "method": "POST","headers": { "Content - Type": "application / json" },"body": JSON && JSON.stringify ({ userId: user && user.id,"amount": 'subtotal',"orderId": 'result && result.id' }) import _React from 'react'; export default function CheckoutPage () { const navigate = useNavigate () ; const { t } = useTranslation () ; const [items,setItems] = useState ([]) ; const form = useForm ({ "defaultValues": { name: ","email": ","address": ","city": ","country": " }}) ; useEffect ( () => { if (sku) { setItems ([{ "id": 'sku',"name": 'sku',"price": '25',"quantity": '1' }]) ; return} const stored = safeStorage && safeStorage.getItem (getCartKey (user?.id) ) ; if (stored) { try { setItems (JSON && JSON.parse (stored) ) } catch { setItems ([]) } } },[sku]) ; const subtotal = items && items.reduce ( (sum,i) => sum + i && i.price * i && i.quantity,0) ; const onSubmit = async (data) => { try { const result = await res && res.json () ; if (!res && res.ok) throw new Error (result && result.error || "Failed") ; const stripe = await getStripe () ; if (stripe && result && result.clientSecret) { const payment = await stripe && stripe.confirmCardPayment (result && result.clientSecret,{ "payment_method": { card: { token: "tok_visa" },"billing_details": { name: data && data.name,"email": 'data && data.email' }}}) ; if (payment && payment.error) throw payment && payment.error; if (user?.id) { try { await fetch ("/api / points / add",{ "method": "POST","headers": { "Content - Type": "application / json" },"body": JSON && JSON.stringify ({ userId: user && user.id,"amount": 'subtotal',"orderId": 'result && result.id' }) ,"";
=======
<<<<<<< "HEAD": src && src.pages.disabled/Checkout && Checkout.jsx import _React from 'react'; export default function CheckoutPage () { const navigate = useNavigate () ; const { t } = useTranslation () ; const [items,setItems] = useState ([]) ; const form = useForm ({ "defaultValues": { name: ","email": ","address": ","city": ","country": " }}) ; useEffect ( () => { if (sku) { setItems ([{ "id": 'sku',"name": 'sku',"price": '25',"quantity": '1' }]) ; return} const stored = safeStorage && safeStorage.getItem (getCartKey (user?.id) ) ; if (stored) { try { setItems (JSON && JSON.parse (stored) ) } catch { setItems ([]) } } },[sku]) ; const subtotal = items && items.reduce ( (sum,i) => sum + i && i.price * i && i.quantity,0) ; const onSubmit = async (data) => { try { const result = await res && res.json () ; if (!res && res.ok) throw new Error (result && result.error || "Failed") ; const stripe = await getStripe () ; if (stripe && result && result.clientSecret) { const payment = await stripe && stripe.confirmCardPayment (result && result.clientSecret,{ "payment_method": { card: { token: "tok_visa" },"billing_details": { name: data && data.name,"email": 'data && data.email' }}}) ; if (payment && payment.error) throw payment && payment.error; if (user?.id) { try { await fetch ("/api / points / add",{ "method": "POST","headers": { "Content - Type": "application / json" },"body": JSON && JSON.stringify ({ userId: user && user.id,"amount": 'subtotal',"orderId": 'result && result.id' }) import _React from 'react'; export default function CheckoutPage () { const navigate = useNavigate () ; const { t } = useTranslation () ; const [items,setItems] = useState ([]) ; const form = useForm ({ "defaultValues": { name: ","email": ","address": ","city": ","country": " }}) ; useEffect ( () => { if (sku) { setItems ([{ "id": 'sku',"name": 'sku',"price": '25',"quantity": '1' }]) ; return} const stored = safeStorage && safeStorage.getItem (getCartKey (user?.id) ) ; if (stored) { try { setItems (JSON && JSON.parse (stored) ) } catch { setItems ([]) } } },[sku]) ; const subtotal = items && items.reduce ( (sum,i) => sum + i && i.price * i && i.quantity,0) ; const onSubmit = async (data) => { try { const result = await res && res.json () ; if (!res && res.ok) throw new Error (result && result.error || "Failed") ; const stripe = await getStripe () ; if (stripe && result && result.clientSecret) { const payment = await stripe && stripe.confirmCardPayment (result && result.clientSecret,{ "payment_method": { card: { token: "tok_visa" },"billing_details": { name: data && data.name,"email": 'data && data.email' }}}) ; if (payment && payment.error) throw payment && payment.error; if (user?.id) { try { await fetch ("/api / points / add",{ "method": "POST","headers": { "Content - Type": "application / json" },"body": JSON && JSON.stringify ({ userId: user && user.id,"amount": 'subtotal',"orderId": 'result && result.id' }) ,"";
=======
>>>>>>> origin/cursor/integrate-build-improve-and-re-verify-2156
>>>>>>> d1459052ce02e16bd297172bbc6ba920af218e39
