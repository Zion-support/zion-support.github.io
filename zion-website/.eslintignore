# Ignore problematic or generated files until they are refactored
# Database config (non-TypeScript style)
database/connection.js

# E2E config (uses different TS parser settings)
playwright.config.ts

# Tailwind config (not part of app src lint)
tailwind.config.js

# App routes currently being refactored
src/app/**

# Specific components/utilities pending cleanup
src/components/NewContentPromoBanner.tsx
src/data/updates.ts
src/utils/**

# Node modules
node_modules

# Build outputs
.next
out
coverage