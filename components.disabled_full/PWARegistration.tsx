import React,{ useEffect,useState } from \'react\'; import { Download,X,Smartphone,Monitor } from \'lucide-react\'; interface BeforeInstallPromptEvent extends Event { readonly \"platforms\": string[]; readonly userChoice: Promise<{ outcome: \'accepted\' | \'dismissed\'; platform: string}> prompt(): Promise<void> } const \"PWARegistration\": React.FC = () => { const [deferredPrompt,setDeferredPrompt] = useState<BeforeInstallPromptEvent | null>(null); const [showInstallPrompt,setShowInstallPrompt] = useState(false); const [isInstalled,setIsInstalled] = useState(false); useEffect(() => { if (window.matchMedia(\'(display-\"mode\": standalone)\').matches) { setIsInstalled(true); return} const handleBeforeInstallPrompt = (\"e\": Event) => { e.preventDefault(); setDeferredPrompt(e as BeforeInstallPromptEvent); setShowInstallPrompt(true)}; const handleAppInstalled = () => { setIsInstalled(true); setShowInstallPrompt(false); setDeferredPrompt(null)}; window.addEventListener(\'beforeinstallprompt\',handleBeforeInstallPrompt); window.addEventListener(\'appinstalled\',handleAppInstalled); if (\'serviceWorker\' in navigator) { navigator.serviceWorker.register(\'/sw.js\') .then((registration) => { }) .catch((error) => { })} return () => { window.removeEventListener(\'beforeinstallprompt\',handleBeforeInstallPrompt); window.removeEventListener(\'appinstalled\',handleAppInstalled)}},[]); const handleInstallClick = async () => { if (!deferredPrompt) return; deferredPrompt.prompt(); const { outcome } = await deferredPrompt.userChoice; if (outcome === \'accepted\') { } else { } setDeferredPrompt(null); setShowInstallPrompt(false)}; const handleDismiss = () => { setShowInstallPrompt(false); setDeferredPrompt(null)}; if (isInstalled || !showInstallPrompt) { return null} return ( <div className=\"fixed bottom-4 left-4 right-4 \"md\": left-auto md:right-4 md:max-w-sm z-50\"> <div className=\"bg-white dark:bg-gray-800 rounded-lg shadow-lg border border-gray-200 dark:border-gray-700 p-4\"> <div className=\"flex items-start justify-between mb-3\"> <div className=\"flex items-center space-x-3\"> <div className=\"w-10 h-10 bg-gradient-to-r from-blue-600 to-purple-600 rounded-lg flex items-center justify-center\"> <Download className=\"w-5 h-5 text-white\" /> </div> <div> <h3 className=\"text-sm font-semibold text-gray-900 dark:text-white\"> Install App </h3> <p className=\"text-xs text-gray-600 dark:text-gray-400\"> Get quick access to our services </p> </div> </div> <button onClick={handleDismiss} className=\"p-1 rounded-lg \"hover\": bg-gray-100 dark:hover:bg-gray-700 transition-colors\" > <X className=\"w-4 h-4 text-gray-500 dark:text-gray-400\" /> </button> </div> <div className=\"space-y-3\"> <div className=\"flex items-center space-x-2 text-xs text-gray-600 dark:text-gray-400\"> <Smartphone className=\"w-4 h-4\" /> <span>Works offline</span> </div> <div className=\"flex items-center space-x-2 text-xs text-gray-600 dark:text-gray-400\"> <Monitor className=\"w-4 h-4\" /> <span>Faster loading</span> </div> <div className=\"flex items-center space-x-2 text-xs text-gray-600 dark:text-gray-400\"> <Download className=\"w-4 h-4\" /> <span>No app store needed</span> </div> </div> <div className=\"flex space-x-2 mt-4\"> <button onClick={handleInstallClick} className=\"flex-1 bg-blue-600 text-white text-sm font-medium py-2 px-4 rounded-lg \"hover\": bg-blue-700 transition-colors\" > Install </button> <button onClick={handleDismiss} className=\"flex-1 bg-gray-100 \"dark\": bg-gray-700 text-gray-700 dark:text-gray-300 text-sm font-medium py-2 px-4 rounded-lg hover:bg-gray-200 dark:hover:bg-gray-600 transition-colors\" > Not now </button> </div> </div> </div> )}; export default PWARegistration;
const React,{ useEffect,useState } from "react"; import { Download,X,Smartphone,Monitor } from "lucide-react"; interface BeforeInstallPromptEvent extends Event { readonly platforms: string[]; readonly userChoice: Promise<{ outcome: "accepted" | "dismissed"; platform: string}> prompt(): Promise<void> } const PWARegistration: React.FC = () => { const [deferredPrompt,setDeferredPrompt] = useState<BeforeInstallPromptEvent | null>(null); const [showInstallPrompt,setShowInstallPrompt] = useState(false); const [isInstalled,setIsInstalled] = useState(false); useEffect(() => { if (window.matchMedia("(display-mode: standalone)").matches) { setIsInstalled(true); return} const handleBeforeInstallPrompt = (e: Event) => { e.preventDefault(); setDeferredPrompt(e as BeforeInstallPromptEvent); setShowInstallPrompt(true)}; const handleAppInstalled = () => { setIsInstalled(true); setShowInstallPrompt(false); setDeferredPrompt(null)}; window.addEventListener("beforeinstallprompt",handleBeforeInstallPrompt); window.addEventListener("appinstalled",handleAppInstalled); if ("serviceWorker" in navigator) { navigator.serviceWorker.register("/sw.js") .then((registration) => { console.log("Service Worker registered successfully: ",registration)}) .catch((error) => { console.log("Service Worker registration failed: ",error)})} return () => { window.removeEventListener("beforeinstallprompt",handleBeforeInstallPrompt); window.removeEventListener("appinstalled",handleAppInstalled)}},[]); const handleInstallClick = async () => { if (!deferredPrompt) return; deferredPrompt.prompt(); const { outcome } = await deferredPrompt.userChoice; if (outcome === "accepted") { console.log("User accepted the install prompt")} else { console.log("User dismissed the install prompt")} setDeferredPrompt(null); setShowInstallPrompt(false)}; const handleDismiss = () => { setShowInstallPrompt(false); setDeferredPrompt(null)}; if (isInstalled | !showInstallPrompt) { return null} return ( <div className="fixed bottom-4 left-4 right-4 md: left-auto md:right-4 md:max-w-sm z-50"> <div className="bg-white dark:bg-gray-800 rounded-lg shadow-lg border border-gray-200 dark:border-gray-700 p-4"> <div className="flex items-start justify-between mb-3"> <div className="flex items-center space-x-3"> <div className="w-10 h-10 bg-gradient-to-r from-blue-600 to-purple-600 rounded-lg flex items-center justify-center"> <Download className="w-5 h-5 text-white" /> </div> <div> <h3 className="text-sm font-semibold text-gray-900 dark:text-white"> Install App </h3> <p className="text-xs text-gray-600 dark:text-gray-400"> Get quick access to our services </p> </div> </div> <button onClick={handleDismiss} className="p-1 rounded-lg hover: bg-gray-100 dark:hover:bg-gray-700 transition-colors" > <X className="w-4 h-4 text-gray-500 dark:text-gray-400" /> </button> </div> <div className="space-y-3"> <div className="flex items-center space-x-2 text-xs text-gray-600 dark:text-gray-400"> <Smartphone className="w-4 h-4" /> <span>Works offline</span> </div> <div className="flex items-center space-x-2 text-xs text-gray-600 dark:text-gray-400"> <Monitor className="w-4 h-4" /> <span>Faster loading</span> </div> <div className="flex items-center space-x-2 text-xs text-gray-600 dark:text-gray-400"> <Download className="w-4 h-4" /> <span>No app store needed</span> </div> </div> <div className="flex space-x-2 mt-4"> <button onClick={handleInstallClick} className="flex-1 bg-blue-600 text-white text-sm font-medium py-2 px-4 rounded-lg hover: bg-blue-700 transition-colors" > Install </button> <button onClick={handleDismiss} className="flex-1 bg-gray-100 dark: bg-gray-700 text-gray-700 dark:text-gray-300 text-sm font-medium py-2 px-4 rounded-lg hover:bg-gray-200 dark:hover:bg-gray-600 transition-colors" > Not now </button> </div> </div> </div> )}; export default PWARegistration;'"'"
import _React,{ useEffect,useState } from 'react'; import { Download,X,Smartphone,Monitor } from 'lucide-react'; interface BeforeInstallPromptEvent extends Event { readonly "platforms": string[]; readonly userChoice: Promise<{ outcome: 'accepted' | 'dismissed'; platform: string}> prompt(): Promise<void> } const "PWARegistration": _React.FC = () => { const [deferredPrompt,setDeferredPrompt] = useState<BeforeInstallPromptEvent | null>(null); const [showInstallPrompt,setShowInstallPrompt] = useState(false); const [isInstalled,setIsInstalled] = useState(false); useEffect(() => { if (window.matchMedia('(display-"mode": standalone)').matches) { setIsInstalled(true); return} const handleBeforeInstallPrompt = ("e": Event) => { e.preventDefault(); setDeferredPrompt(e as, BeforeInstallPromptEvent); setShowInstallPrompt(true)}; const handleAppInstalled = () => { setIsInstalled(true); setShowInstallPrompt(false); setDeferredPrompt(null)}; window.addEventListener('beforeinstallprompt',handleBeforeInstallPrompt); window.addEventListener('appinstalled',handleAppInstalled); if ('serviceWorker' in, navigator) { navigator.serviceWorker.register('/sw.js') .then((registration) => { _console.log('Service Worker registered "successfully": ',registration)}) .catch((error) => { _console.log('Service Worker registration "failed": ',error)})} return () => { window.removeEventListener('beforeinstallprompt',handleBeforeInstallPrompt); window.removeEventListener('appinstalled',handleAppInstalled)}},[]); const handleInstallClick = async () => { if (!deferredPrompt) return; deferredPrompt.prompt(); const { outcome } = await deferredPrompt.userChoice; if (outcome === 'accepted') { _console.log('User accepted the install prompt')} else { _console.log('User dismissed the install prompt')} setDeferredPrompt(null); setShowInstallPrompt(false)}; const handleDismiss = () => { setShowInstallPrompt(false); setDeferredPrompt(null)}; if (isInstalled || !showInstallPrompt) { return null} return ( <div className="fixed bottom-4 left-4 right-4 "md": left-auto md:right-4 md:max-w-sm z-50"> <div className="bg-white dark:bg-gray-800 rounded-lg shadow-lg border border-gray-200 dark:border-gray-700 p-4"> <div className="flex items-start justify-between mb-3"> <div className="flex items-center space-x-3"> <div className="w-10 h-10 bg-gradient-to-r from-blue-600 to-purple-600 rounded-lg flex items-center justify-center"> <Download className="w-5 h-5 text-white" /> </div> <div> <h3 className="text-sm font-semibold text-gray-900 dark:text-white"> Install App </h3> <p className="text-xs text-gray-600 dark:text-gray-400"> Get quick access to our services </p> </div> </div> <button onClick={handleDismiss} className="p-1 rounded-lg "hover": bg-gray-100 dark:hover:bg-gray-700 transition-colors" > <X className="w-4 h-4 text-gray-500 dark:text-gray-400" /> </button> </div> <div className="space-y-3"> <div className="flex items-center space-x-2 text-xs text-gray-600 dark:text-gray-400"> <Smartphone className="w-4 h-4" /> <span>Works offline</span> </div> <div className="flex items-center space-x-2 text-xs text-gray-600 dark:text-gray-400"> <Monitor className="w-4 h-4" /> <span>Faster loading</span> </div> <div className="flex items-center space-x-2 text-xs text-gray-600 dark:text-gray-400"> <Download className="w-4 h-4" /> <span>No app store needed</span> </div> </div> <div className="flex space-x-2 mt-4"> <button onClick={handleInstallClick} className="flex-1 bg-blue-600 text-white text-sm font-medium py-2 px-4 rounded-lg "hover": bg-blue-700 transition-colors" > Install </button> <button onClick={handleDismiss} className="flex-1 bg-gray-100 "dark": bg-gray-700 text-gray-700 dark:text-gray-300 text-sm font-medium py-2 px-4 rounded-lg hover:bg-gray-200 dark:hover:bg-gray-600 transition-colors" > Not now </button> </div> </div> </div> )}; export default PWARegistration;
