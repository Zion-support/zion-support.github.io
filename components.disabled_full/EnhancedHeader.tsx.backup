import React, { useState, useEffect } from 'react';

    const handleScroll = (
      setScrolled(window.scrollY > 50);) => {
  return $3;}
}
    };

            >
              {' '}
              {mobileMenuOpen ? (
                <X className="h-6 w-6" />
              ) : (
                <Menu className="h-6 w-6" />
              )}{' '}
            </button>{' '}
          </div>{' '}
        </div>{' '}
        {}{' '}
        {mobileMenuOpen && (


