import React,{ useEffect } from \"react\"; interface AccessibilityEnhancerProps { \"children\": \'React.ReactNode; skipToContent?: boolean; focusManagement?: boolean; keyboardNavigation?: boolean;\' } } const \"AccessibilityEnhancer\": React.FC<AccessibilityEnhancerProps> = ({ children,skipToContent = true,focusManagement = true,keyboardNavigation = true }) => { useEffect(() => { if (skipToContent) { const handleSkipToContent = (\"e\": KeyboardEvent) => { if (e.key === \"Tab\" && !e.shiftKey) { const skipLink = document.getElementById(\"skip-to-content\"); if (skipLink && document.activeElement === document.body) { skipLink.focus()} } }; document.addEventListener(\"keydown\",handleSkipToContent); return () => document.removeEventListener(\"keydown\",handleSkipToContent)} },[skipToContent]); useEffect(() => { if (focusManagement) { const handleFocusManagement = () => { const modals = document.querySelectorAll(\"[role=\'dialog\']\"); modals.forEach(modal => { const focusableElements = modal.querySelectorAll( \"button,[href],input,select,textarea,[tabindex]:not([tabindex=\'-1\'])\" ); if (focusableElements.length > 0) { const firstElement = focusableElements[0] as HTMLElement; const lastElement = focusableElements[focusableElements.length - 1] as HTMLElement; const handleTabKey = (\"e\": KeyboardEvent) => { if (e.key === \"Tab\") { if (e.shiftKey) { if (document.activeElement === firstElement) { e.preventDefault(); lastElement.focus()} } else { if (document.activeElement === lastElement) { e.preventDefault(); firstElement.focus()} } } }; modal.addEventListener(\"keydown\",handleTabKey)} })}; handleFocusManagement()} },[focusManagement]); useEffect(() => { if (keyboardNavigation) { const handleKeyboardNavigation = (\"e\": KeyboardEvent) => { if (e.key === \"ArrowDown\" || e.key === \"ArrowUp\") { const currentElement = document.activeElement; if (currentElement && currentElement.getAttribute(\"data-navigable\") === \"true\") { e.preventDefault(); const navigableElements = document.querySelectorAll(\"[data-navigable=\'true\']\"); const currentIndex = Array.from(navigableElements).indexOf(currentElement); if (e.key === \"ArrowDown\" && currentIndex < navigableElements.length - 1) { (navigableElements[currentIndex + 1] as HTMLElement).focus()} else if (e.key === \"ArrowUp\" && currentIndex > 0) { (navigableElements[currentIndex - 1] as HTMLElement).focus()} } } if (e.key === \"Escape\") { const openModal = document.querySelector(\"[role=\'dialog\'][aria-hidden=\'false\']\"); if (openModal) { const closeButton = openModal.querySelector(\"[data-close-modal]\") as HTMLElement; if (closeButton) { closeButton.click()} } } }; document.addEventListener(\"keydown\",handleKeyboardNavigation); return () => document.removeEventListener(\"keydown\",handleKeyboardNavigation)} },[keyboardNavigation]); return ( <> {skipToContent && ( <a id=\"skip-to-content\" href=\"#main-content\" className=\"sr-only \"focus\": not-sr-only focus:absolute focus:top-4 focus:left-4 bg-blue-600 text-white px-4 py-2 rounded-lg z-50\" > Skip to main content </a> )} {children} </> )}; export default AccessibilityEnhancer;
const React,{ useEffect } from "react"; interface AccessibilityEnhancerProps { children: "React.ReactNode; skipToContent?: boolean; focusManagement?: boolean; keyboardNavigation?: boolean;" } } const AccessibilityEnhancer: React.FC<AccessibilityEnhancerProps> = ({ children,skipToContent = true,focusManagement = true,keyboardNavigation = true }) => { useEffect(() => { if (skipToContent) { const handleSkipToContent = (e: KeyboardEvent) => { if (e.key === "Tab" && !e.shiftKey) { const skipLink = document.getElementById("skip-to-content"); if (skipLink && document.activeElement === document.body) { skipLink.focus()} } }; document.addEventListener("keydown",handleSkipToContent); return () => document.removeEventListener("keydown",handleSkipToContent)} },[skipToContent]); useEffect(() => { if (focusManagement) { const handleFocusManagement = () => { const modals = document.querySelectorAll("[role="dialog"]"); modals.forEach(modal => { const focusableElements = modal.querySelectorAll( "button,[href],input,select,textarea,[tabindex]:not([tabindex="-1"])" ); if (focusableElements.length > 0) { const firstElement = focusableElements[0] as HTMLElement; const lastElement = focusableElements[focusableElements.length - 1] as HTMLElement; const handleTabKey = (e: KeyboardEvent) => { if (e.key === "Tab") { if (e.shiftKey) { if (document.activeElement === firstElement) { e.preventDefault(); lastElement.focus()} } else { if (document.activeElement === lastElement) { e.preventDefault(); firstElement.focus()} } } }; modal.addEventListener("keydown",handleTabKey)} })}; handleFocusManagement()} },[focusManagement]); useEffect(() => { if (keyboardNavigation) { const handleKeyboardNavigation = (e: KeyboardEvent) => { if (e.key === "ArrowDown" | e.key === "ArrowUp") { const currentElement = document.activeElement; if (currentElement && currentElement.getAttribute("data-navigable") === "true") { e.preventDefault(); const navigableElements = document.querySelectorAll("[data-navigable="true"]"); const currentIndex = Array.from(navigableElements).indexOf(currentElement); if (e.key === "ArrowDown" && currentIndex < navigableElements.length - 1) { (navigableElements[currentIndex + 1] as HTMLElement).focus()} else if (e.key === "ArrowUp" && currentIndex > 0) { (navigableElements[currentIndex - 1] as HTMLElement).focus()} } } if (e.key === "Escape") { const openModal = document.querySelector("[role="dialog"][aria-hidden="false"]"); if (openModal) { const closeButton = openModal.querySelector("[data-close-modal]") as HTMLElement; if (closeButton) { closeButton.click()} } } }; document.addEventListener("keydown",handleKeyboardNavigation); return () => document.removeEventListener("keydown",handleKeyboardNavigation)} },[keyboardNavigation]); return ( <> {skipToContent && ( <a id="skip-to-content" href="#main-content" className="sr-only focus: not-sr-only focus:absolute focus:top-4 focus:left-4 bg-blue-600 text-white px-4 py-2 rounded-lg z-50" > Skip to main content </a> )} {children} </> )}; export default AccessibilityEnhancer;'"'"
import _React,{ useEffect } from "react"; interface AccessibilityEnhancerProps { "children": '_React.ReactNode; skipToContent?: boolean; focusManagement?: boolean; keyboardNavigation?: boolean;' } } const "AccessibilityEnhancer": _React.FC<AccessibilityEnhancerProps> = ({ children,skipToContent = true,focusManagement = true,keyboardNavigation = true }) => { useEffect(() => { if (skipToContent) { const handleSkipToContent = ("e": KeyboardEvent) => { if (e.key === "Tab" && !e.shiftKey) { const skipLink = document.getElementById("skip-to-content"); if (skipLink && document.activeElement === document.body) { skipLink.focus()} } }; document.addEventListener("keydown",handleSkipToContent); return () => document.removeEventListener("keydown",handleSkipToContent)} },[skipToContent]); useEffect(() => { if (focusManagement) { const handleFocusManagement = () => { const modals = document.querySelectorAll("[role='dialog']"); modals.forEach(modal => { const focusableElements = modal.querySelectorAll( "button,[href],input,select,textarea,[tabindex]:not([tabindex='-1'])" ); if (focusableElements.length > 0) { const firstElement = focusableElements[0] as HTMLElement; const lastElement = focusableElements[focusableElements.length - 1] as HTMLElement; const handleTabKey = ("e": KeyboardEvent) => { if (e.key === "Tab") { if (e.shiftKey) { if (document.activeElement === firstElement) { e.preventDefault(); lastElement.focus()} } else { if (document.activeElement === lastElement) { e.preventDefault(); firstElement.focus()} } } }; modal.addEventListener("keydown",handleTabKey)} })}; handleFocusManagement()} },[focusManagement]); useEffect(() => { if (keyboardNavigation) { const handleKeyboardNavigation = ("e": KeyboardEvent) => { if (e.key === "ArrowDown" || e.key === "ArrowUp") { const currentElement = document.activeElement; if (currentElement && currentElement.getAttribute("data-navigable") === "true") { e.preventDefault(); const navigableElements = document.querySelectorAll("[data-navigable='true']"); const currentIndex = Array.from(navigableElements).indexOf(currentElement); if (e.key === "ArrowDown" && currentIndex < navigableElements.length - 1) { (navigableElements[currentIndex + 1] as, HTMLElement).focus()} else if (e.key === "ArrowUp" && currentIndex > 0) { (navigableElements[currentIndex - 1] as, HTMLElement).focus()} } } if (e.key === "Escape") { const openModal = document.querySelector("[role='dialog'][aria-hidden='false']"); if (openModal) { const closeButton = openModal.querySelector("[data-close-modal]") as HTMLElement; if (closeButton) { closeButton.click()} } } }; document.addEventListener("keydown",handleKeyboardNavigation); return () => document.removeEventListener("keydown",handleKeyboardNavigation)} },[keyboardNavigation]); return ( <> {skipToContent && ( <a id="skip-to-content" href="#main-content" className="sr-only "focus": not-sr-only focus:absolute focus:top-4 focus:left-4 bg-blue-600 text-white px-4 py-2 rounded-lg z-50" > Skip to main content </a> )} {children} </> )}; export default AccessibilityEnhancer;
