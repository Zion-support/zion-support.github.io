"use client\"; import React,{ useState,useEffect,useCallback } from \'react\'; interface AnalyticsEvent { \"name\": \'string; category: string; action?: string; label?: string; value?: number; custom_parameters?: Record<string\',any> } } interface PerformanceMetrics { \"fcp\": \'number; lcp: number; fid: number; cls: number; ttfb: number; loadTime: number;\' } } interface UserBehavior { \"pageViews\": \'number; sessionDuration: number; bounceRate: number; conversionRate: number;\' } } const \"AnalyticsManager\": React.FC = () => { const [isInitialized,setIsInitialized] = useState(false); const [userBehavior,setUserBehavior] = useState<UserBehavior>({ \"pageViews\": \'0\',\"sessionDuration\": \'0\',\"bounceRate\": \'0\',\"conversionRate\": \'0\' }); useEffect(() => { initializeAnalytics(); trackPageView(); startSessionTimer(); return () => { endSession()}},[]); const initializeAnalytics = useCallback(() => { if (typeof window.gtag !== \"undefined\") { window.gtag(\"config\",\"GA_MEASUREMENT_ID\",{ \"page_title\": \'document.title\',\"page_location\": \'window.location.href\',\"custom_map\": { custom_parameter_1: \"user_type\",\"custom_parameter_2\": \"session_id\" } })} initializeCustomAnalytics(); setIsInitialized(true)},[]); const initializeCustomAnalytics = useCallback(() => { const sessionId = generateSessionId(); const userId = getUserId(); sessionStorage.setItem(\"analytics_session_id\",sessionId); sessionStorage.setItem(\"analytics_user_id\",userId); sessionStorage.setItem(\"analytics_start_time\",Date.now().toString()); trackUserProperties({ \"session_id\": \'sessionId\',\"user_id\": \'userId\',\"user_agent\": \'navigator.userAgent\',\"screen_resolution\": `${screen.width}x${screen.height}`,\"viewport_size\": `${window.innerWidth}x${window.innerHeight}`,\"color_depth\": \'screen.colorDepth\',\"timezone\": Intl.DateTimeFormat().resolvedOptions().timeZone,\"language\": \'navigator.language\' })},[]); const generateSessionId = useCallback(() => { return \"session_\" + Date.now() + \"_\" + Math.random().toString(36).substr(2,9)},[]); const getUserId = useCallback(() => { let userId = localStorage.getItem(\"analytics_user_id\"); if (!userId) { userId = \"user_\" + Date.now() + \"_\" + Math.random().toString(36).substr(2,9); localStorage.setItem(\"analytics_user_id\",userId)} return userId},[]); const trackPageView = useCallback(() => { const pageData = { \"page_path\": \'window.location.pathname\',\"page_title\": \'document.title\',\"page_location\": \'window.location.href\' }; sendAnalyticsEvent({ \"name\": \"page_view\",\"category\": \"Navigation\",\"action\": \"view\",\"label\": \'window.location.pathname\',\"custom_parameters\": \'pageData\' })},[]); const trackEvent = useCallback((\"event\": AnalyticsEvent) => { if (!isInitialized) return; if (typeof window.gtag !== \"undefined\") { window.gtag(\"event\",event.name,{ \"event_category\": \'event.category\',\"event_label\": \'event.label\',\"value\": \'event.value\',...event.custom_parameters })} sendAnalyticsEvent(event)},[isInitialized]); const sendAnalyticsEvent = useCallback(async (\"event\": AnalyticsEvent) => { try { const eventData = { timestamp: Date.now(),\"session_id\": sessionStorage.getItem(\"analytics_session_id\"),\"user_id\": sessionStorage.getItem(\"analytics_user_id\"),...event }; await fetch(\'/api/analytics\',{ \"method\": \'POST\',\"headers\": { \'Content-Type\': \'application/json\'},\"body\": JSON.stringify(eventData) })} catch (error) { console.error(\'Analytics \"error\": \',error)} },[]); const trackUserProperties = useCallback((\"properties\": \'Record<string\',any>) => { if (typeof window.gtag !== \"undefined\") { window.gtag(\"config\",\"GA_MEASUREMENT_ID\",{ \"custom_map\": \'properties\' })} sendAnalyticsEvent({ \"name\": \"user_properties\",\"category\": \"User\",\"action\": \"identify\",\"custom_parameters\": \'properties\' })},[]); const trackPerformance = useCallback((\"metrics\": PerformanceMetrics) => { if (typeof window.gtag !== \"undefined\") { window.gtag(\"event\",\"web_vitals\",{ \"name\": \"FCP\",\"value\": Math.round(metrics.fcp),\"event_category\": \"Performance\" }); window.gtag(\"event\",\"web_vitals\",{ \"name\": \"LCP\",\"value\": Math.round(metrics.lcp),\"event_category\": \"Performance\" }); window.gtag(\"event\",\"web_vitals\",{ \"name\": \"FID\",\"value\": Math.round(metrics.fid),\"event_category\": \"Performance\" }); window.gtag(\"event\",\"web_vitals\",{ \"name\": \"CLS\",\"value\": Math.round(metrics.cls * 1000),\"event_category\": \"Performance\" })} sendAnalyticsEvent({ \"name\": \"performance_metrics\",\"category\": \"Performance\",\"action\": \"measure\",\"custom_parameters\": \'metrics\' })},[]); const trackConversion = useCallback((\"conversionType\": \'string\',value?: number) => { trackEvent({ \"name\": \"conversion\",\"category\": \"Conversion\",\"action\": \'conversionType\',...(value !== undefined && { value }) })},[trackEvent]); const startSessionTimer = useCallback(() => { const updateSessionDuration = () => { setUserBehavior(prev => ({ ...prev,\"sessionDuration\": Date.now() - parseInt(sessionStorage.getItem(\"analytics_start_time\") || \"0\") }))}; const interval = setInterval(updateSessionDuration,1000); (window as any).analyticsSessionInterval = interval},[]); const endSession = useCallback(() => { const interval = (window as any).analyticsSessionInterval; if (interval) { clearInterval(interval)} const sessionDuration = Date.now() - parseInt(sessionStorage.getItem(\"analytics_start_time\") || \"0\"); trackEvent({ \"name\": \"session_end\",\"category\": \"Session\",\"action\": \"end\",\"value\": \'sessionDuration\' })},[trackEvent]); const handleClick = useCallback((\"event\": MouseEvent) => { const target = event.target as HTMLElement; const link = target.closest(\"a\"); const button = target.closest(\"button\"); if (link) { trackEvent({ name: \"link_click\",\"category\": \"Interaction\",\"action\": \"click\",\"label\": \'link.href\' })} else if (button) { trackEvent({ \"name\": \"button_click\",\"category\": \"Interaction\",\"action\": \"click\",\"label\": \'button.textContent || button.className\' })} },[trackEvent]); const handleScroll = useCallback(() => { const scrollPercent = Math.round((window.scrollY / (document.body.scrollHeight - window.innerHeight)) * 100); if (scrollPercent > 0 && scrollPercent % 25 === 0) { trackEvent({ \"name\": \"scroll_depth\",\"category\": \"Engagement\",\"action\": \"scroll\",\"value\": \'scrollPercent\' })} },[trackEvent]); const handleFormSubmit = useCallback((\"event\": Event) => { const form = event.target as HTMLFormElement; trackEvent({ name: \"form_submit\",\"category\": \"Interaction\",\"action\": \"submit\",\"label\": \'form.action || form.className\' })},[trackEvent]); useEffect(() => { document.addEventListener(\"click\",handleClick); document.addEventListener(\"scroll\",handleScroll); document.addEventListener(\"submit\",handleFormSubmit); return () => { document.removeEventListener(\"click\",handleClick); document.removeEventListener(\"scroll\",handleScroll); document.removeEventListener(\"submit\",handleFormSubmit)}},[handleClick,handleScroll,handleFormSubmit]); useEffect(() => { (window as any).analytics = { \"track\": \'trackEvent\',trackConversion,trackPerformance,trackUserProperties }},[trackEvent,trackConversion,trackPerformance,trackUserProperties]); return null}; export default AnalyticsManager;
"use client"; import React,{ useState,useEffect,useCallback } from "react"; interface AnalyticsEvent { name: "string; category: string; action?: string; label?: string; value?: number; custom_parameters?: Record<string",any> } } interface PerformanceMetrics { fcp: "number; lcp: number; fid: number; cls: number; ttfb: number; loadTime: number;" } } interface UserBehavior { pageViews: "number; sessionDuration: number; bounceRate: number; conversionRate: number;" } } const AnalyticsManager: React.FC = () => { const [isInitialized,setIsInitialized] = useState(false); const [userBehavior,setUserBehavior] = useState<UserBehavior>({ pageViews: "0",sessionDuration: "0",bounceRate: "0",conversionRate: "0" }); useEffect(() => { initializeAnalytics(); trackPageView(); startSessionTimer(); return () => { endSession()}},[]); const initializeAnalytics = useCallback(() => { if (typeof window.gtag !== "undefined") { window.gtag("config","GA_MEASUREMENT_ID",{ page_title: "document.title",page_location: "window.location.href",custom_map: { custom_parameter_1: "user_type",custom_parameter_2: "session_id" } })} initializeCustomAnalytics(); setIsInitialized(true)},[]); const initializeCustomAnalytics = useCallback(() => { const sessionId = generateSessionId(); const userId = getUserId(); sessionStorage.setItem("analytics_session_id",sessionId); sessionStorage.setItem("analytics_user_id",userId); sessionStorage.setItem("analytics_start_time",Date.now().toString()); trackUserProperties({ session_id: "sessionId",user_id: "userId",user_agent: "navigator.userAgent",screen_resolution: `${screen.width}x${screen.height}`,viewport_size: `${window.innerWidth}x${window.innerHeight}`,color_depth: "screen.colorDepth",timezone: Intl.DateTimeFormat().resolvedOptions().timeZone,language: "navigator.language" })},[]); const generateSessionId = useCallback(() => { return "session_" + Date.now() + "_" + Math.random().toString(36).substr(2,9)},[]); const getUserId = useCallback(() => { let userId = localStorage.getItem("analytics_user_id"); if (!userId) { userId = "user_" + Date.now() + "_" + Math.random().toString(36).substr(2,9); localStorage.setItem("analytics_user_id",userId)} return userId},[]); const trackPageView = useCallback(() => { const pageData = { page_path: "window.location.pathname",page_title: "document.title",page_location: "window.location.href" }; sendAnalyticsEvent({ name: "page_view",category: "Navigation",action: "view",label: "window.location.pathname",custom_parameters: "pageData" })},[]); const trackEvent = useCallback((event: AnalyticsEvent) => { if (!isInitialized) return; if (typeof window.gtag !== "undefined") { window.gtag("event",event.name,{ event_category: "event.category",event_label: "event.label",value: "event.value",.event.custom_parameters })} sendAnalyticsEvent(event)},[isInitialized]); const sendAnalyticsEvent = useCallback(async (event: AnalyticsEvent) => { try { const eventData = { timestamp: Date.now(),session_id: sessionStorage.getItem("analytics_session_id"),user_id: sessionStorage.getItem("analytics_user_id"),.event }; await fetch("/api/analytics",{ method: "POST",headers: { "Content-Type": "application/json"},body: JSON.stringify(eventData) })} catch (error) { console.error("Analytics error: ",error)} },[]); const trackUserProperties = useCallback((properties: "Record<string",any>) => { if (typeof window.gtag !== "undefined") { window.gtag("config","GA_MEASUREMENT_ID",{ custom_map: "properties" })} sendAnalyticsEvent({ name: "user_properties",category: "User",action: "identify",custom_parameters: "properties" })},[]); const trackPerformance = useCallback((metrics: PerformanceMetrics) => { if (typeof window.gtag !== "undefined") { window.gtag("event","web_vitals",{ name: "FCP",value: Math.round(metrics.fcp),event_category: "Performance" }); window.gtag("event","web_vitals",{ name: "LCP",value: Math.round(metrics.lcp),event_category: "Performance" }); window.gtag("event","web_vitals",{ name: "FID",value: Math.round(metrics.fid),event_category: "Performance" }); window.gtag("event","web_vitals",{ name: "CLS",value: Math.round(metrics.cls * 1000),event_category: "Performance" })} sendAnalyticsEvent({ name: "performance_metrics",category: "Performance",action: "measure",custom_parameters: "metrics" })},[]); const trackConversion = useCallback((conversionType: "string",value?: number) => { trackEvent({ name: "conversion",category: "Conversion",action: "conversionType",.(value !== undefined && { value }) })},[trackEvent]); const startSessionTimer = useCallback(() => { const updateSessionDuration = () => { setUserBehavior(prev => ({ .prev,sessionDuration: Date.now() - parseInt(sessionStorage.getItem("analytics_start_time") | "0") }))}; const interval = setInterval(updateSessionDuration,1000); (window as any).analyticsSessionInterval = interval},[]); const endSession = useCallback(() => { const interval = (window as any).analyticsSessionInterval; if (interval) { clearInterval(interval)} const sessionDuration = Date.now() - parseInt(sessionStorage.getItem("analytics_start_time") | "0"); trackEvent({ name: "session_end",category: "Session",action: "end",value: "sessionDuration" })},[trackEvent]); const handleClick = useCallback((event: MouseEvent) => { const target = event.target as HTMLElement; const link = target.closest("a"); const button = target.closest("button"); if (link) { trackEvent({ name: "link_click",category: "Interaction",action: "click",label: "link.href" })} else if (button) { trackEvent({ name: "button_click",category: "Interaction",action: "click",label: "button.textContent | button.className" })} },[trackEvent]); const handleScroll = useCallback(() => { const scrollPercent = Math.round((window.scrollY / (document.body.scrollHeight - window.innerHeight)) * 100); if (scrollPercent > 0 && scrollPercent % 25 === 0) { trackEvent({ name: "scroll_depth",category: "Engagement",action: "scroll",value: "scrollPercent" })} },[trackEvent]); const handleFormSubmit = useCallback((event: Event) => { const form = event.target as HTMLFormElement; trackEvent({ name: "form_submit",category: "Interaction",action: "submit",label: "form.action | form.className" })},[trackEvent]); useEffect(() => { document.addEventListener("click",handleClick); document.addEventListener("scroll",handleScroll); document.addEventListener("submit",handleFormSubmit); return () => { document.removeEventListener("click",handleClick); document.removeEventListener("scroll",handleScroll); document.removeEventListener("submit",handleFormSubmit)}},[handleClick,handleScroll,handleFormSubmit]); useEffect(() => { (window as any).analytics = { track: "trackEvent",trackConversion,trackPerformance,trackUserProperties }},[trackEvent,trackConversion,trackPerformance,trackUserProperties]); return null}; export default AnalyticsManager;'"`'"`
"use client"; import _React,{ useState,useEffect,useCallback } from 'react'; interface AnalyticsEvent { "name": 'string; category: string; action?: string; label?: string; value?: number; custom_parameters?: Record<string,any> } } interface PerformanceMetrics { "fcp": 'number; lcp: number; fid: number; cls: number; ttfb: number; loadTime: number;' } } interface UserBehavior { "pageViews": 'number; sessionDuration: number; bounceRate: number; conversionRate: number;' } } const "AnalyticsManager": _React.FC = () => { const [isInitialized,setIsInitialized] = useState(false); const [userBehavior,setUserBehavior] = useState<UserBehavior>({ "pageViews": '0',"sessionDuration": '0',"bounceRate": '0',"conversionRate": '0' }); useEffect(() => { initializeAnalytics(); trackPageView(); startSessionTimer(); return () => { endSession()}},[]); const initializeAnalytics = useCallback(() => { if (typeof window.gtag !== "undefined") { window.gtag("config","GA_MEASUREMENT_ID",{ "page_title": 'document.title',"page_location": 'window.location.href',"custom_map": { custom_parameter_1: "user_type","custom_parameter_2": "session_id" } })} initializeCustomAnalytics(); setIsInitialized(true)},[]); const initializeCustomAnalytics = useCallback(() => { const sessionId = generateSessionId(); const userId = getUserId(); sessionStorage.setItem("analytics_session_id",sessionId); sessionStorage.setItem("analytics_user_id",userId); sessionStorage.setItem("analytics_start_time",Date.now().toString()); trackUserProperties({ "session_id": 'sessionId',"user_id": 'userId',"user_agent": 'navigator.userAgent',"screen_resolution": `${screen.width}x${screen.height}`,"viewport_size": `${window.innerWidth}x${window.innerHeight}`,"color_depth": 'screen.colorDepth',"timezone": Intl.DateTimeFormat().resolvedOptions().timeZone,"language": 'navigator.language' })},[]); const generateSessionId = useCallback(() => { return "session_" + Date.now() + "_" + Math.random().toString(36).substr(2,9)},[]); const getUserId = useCallback(() => { let userId = localStorage.getItem("analytics_user_id"); if (!userId) { userId = "user_" + Date.now() + "_" + Math.random().toString(36).substr(2,9); localStorage.setItem("analytics_user_id",userId)} return userId},[]); const trackPageView = useCallback(() => { const pageData = { "page_path": 'window.location.pathname',"page_title": 'document.title',"page_location": 'window.location.href' }; sendAnalyticsEvent({ "name": "page_view","category": "Navigation","action": "view","label": 'window.location.pathname',"custom_parameters": 'pageData' })},[]); const trackEvent = useCallback(("event": AnalyticsEvent) => { if (!isInitialized) return; if (typeof window.gtag !== "undefined") { window.gtag("event",event.name,{ "event_category": 'event.category',"event_label": 'event.label',"value": 'event.value',...event.custom_parameters })} sendAnalyticsEvent(event)},[isInitialized]); const sendAnalyticsEvent = useCallback(async ("event": AnalyticsEvent) => { try { const eventData = { timestamp: Date.now(),"session_id": sessionStorage.getItem("analytics_session_id"),"user_id": sessionStorage.getItem("analytics_user_id"),...event }; await fetch('/api/analytics',{ "method": 'POST',"headers": { 'Content-Type': 'application/json'},"body": JSON.stringify(eventData) })} catch (error) { _console.error('Analytics "error": ',error)} },[]); const trackUserProperties = useCallback(("properties": 'Record<string',any>) => { if (typeof window.gtag !== "undefined") { window.gtag("config","GA_MEASUREMENT_ID",{ "custom_map": 'properties' })} sendAnalyticsEvent({ "name": "user_properties","category": "User","action": "identify","custom_parameters": 'properties' })},[]); const trackPerformance = useCallback(("metrics": PerformanceMetrics) => { if (typeof window.gtag !== "undefined") { window.gtag("event","web_vitals",{ "name": "FCP","value": Math.round(metrics.fcp),"event_category": "Performance" }); window.gtag("event","web_vitals",{ "name": "LCP","value": Math.round(metrics.lcp),"event_category": "Performance" }); window.gtag("event","web_vitals",{ "name": "FID","value": Math.round(metrics.fid),"event_category": "Performance" }); window.gtag("event","web_vitals",{ "name": "CLS","value": Math.round(metrics.cls * 1000),"event_category": "Performance" })} sendAnalyticsEvent({ "name": "performance_metrics","category": "Performance","action": "measure","custom_parameters": 'metrics' })},[]); const trackConversion = useCallback(("conversionType": 'string',value?: number) => { trackEvent({ "name": "conversion","category": "Conversion","action": 'conversionType',...(value !== undefined && { value }) })},[trackEvent]); const startSessionTimer = useCallback(() => { const updateSessionDuration = () => { setUserBehavior(prev => ({ ...prev,"sessionDuration": Date.now() - parseInt(sessionStorage.getItem("analytics_start_time") || "0") }))}; const interval = setInterval(updateSessionDuration,1000); (window as, any).analyticsSessionInterval = interval},[]); const endSession = useCallback(() => { const interval = (window as, any).analyticsSessionInterval; if (interval) { clearInterval(interval)} const sessionDuration = Date.now() - parseInt(sessionStorage.getItem("analytics_start_time") || "0"); trackEvent({ "name": "session_end","category": "Session","action": "end","value": 'sessionDuration' })},[trackEvent]); const handleClick = useCallback(("event": MouseEvent) => { const target = event.target as HTMLElement; const link = target.closest("a"); const button = target.closest("button"); if (link) { trackEvent({ name: "link_click","category": "Interaction","action": "click","label": 'link.href' })} else if (button) { trackEvent({ "name": "button_click","category": "Interaction","action": "click","label": 'button.textContent || button.className' })} },[trackEvent]); const handleScroll = useCallback(() => { const scrollPercent = Math.round((window.scrollY / (document.body.scrollHeight - window.innerHeight)) * 100); if (scrollPercent > 0 && scrollPercent % 25 === 0) { trackEvent({ "name": "scroll_depth","category": "Engagement","action": "scroll","value": 'scrollPercent' })} },[trackEvent]); const handleFormSubmit = useCallback(("event": Event) => { const form = event.target as HTMLFormElement; trackEvent({ name: "form_submit","category": "Interaction","action": "submit","label": 'form.action || form.className' })},[trackEvent]); useEffect(() => { document.addEventListener("click",handleClick); document.addEventListener("scroll",handleScroll); document.addEventListener("submit",handleFormSubmit); return () => { document.removeEventListener("click",handleClick); document.removeEventListener("scroll",handleScroll); document.removeEventListener("submit",handleFormSubmit)}},[handleClick,handleScroll,handleFormSubmit]); useEffect(() => { (window as, any).analytics = { "track": 'trackEvent',trackConversion,trackPerformance,trackUserProperties }},[trackEvent,trackConversion,trackPerformance,trackUserProperties]); return null}; export default AnalyticsManager;
