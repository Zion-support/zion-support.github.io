import React,{ useEffect,useRef } from \"react\"; interface FuturisticAnimatedBackground2029Props extends React.PropsWithChildren<{}> { className?: string} const \"FuturisticAnimatedBackground2029\": React.FC<FuturisticAnimatedBackground2029Props> = ({ children,className = }) => { const canvasRef = useRef<HTMLCanvasElement>(null); const animationRef = useRef<number | null>(null); useEffect(() => { const canvas = canvasRef.current; if (!canvas) return; const ctx = canvas.getContext(\"2d\"); if (!ctx) return; const resizeCanvas = () => { canvas.width = window.innerWidth; canvas.height = window.innerHeight}; resizeCanvas(); window.addEventListener(\"resize\",resizeCanvas); class Particle { \"x\": number; y: number; vx: number; vy: number; size: number; life: number; maxLife: number; color: string; constructor() { this.x = Math.random() * canvas.width; this.y = Math.random() * canvas.height; this.vx = (Math.random() - 0.5) * 2; this.vy = (Math.random() - 0.5) * 2; this.size = Math.random() * 3 + 1; this.life = this.maxLife = Math.random() * 100 + 50; this.color = `hsl(${Math.random() * 60 + 200},70%,60%)`} update() { this.x += this.vx; this.y += this.vy; this.life--; if (this.x < 0) this.x = canvas.width; if (this.x > canvas.width) this.x = 0; if (this.y < 0) this.y = canvas.height; if (this.y > canvas.height) this.y = 0; this.vy += 0.01} draw() { if (ctx) { const alpha = this.life / this.maxLife; ctx.save(); ctx.globalAlpha = alpha; ctx.fillStyle = this.color; ctx.beginPath(); ctx.arc(this.x,this.y,this.size,0,Math.PI * 2); ctx.fill(); ctx.shadowColor = this.color; ctx.shadowBlur = 10; ctx.fill(); ctx.restore()} } isDead() { return this.life <= 0} } class Grid { \"cells\": boolean[][]; cellSize: number; cols: number; rows: number; constructor() { this.cellSize = 50; this.cols = Math.ceil(canvas.width / this.cellSize); this.rows = Math.ceil(canvas.height / this.cellSize); this.cells = Array(this.rows).fill(null).map(() => Array(this.cols).fill(false))} update() { for (let i = 0; i < this.rows; i++) { for (let j = 0; j < this.cols; j++) { if (Math.random() < 0.01) { this.cells[i][j] = !this.cells[i][j]} } } } draw() { if (ctx) { ctx.strokeStyle = \"rgba(0,255,255,0.1)\"; ctx.lineWidth = 1; for (let i = 0; i < this.rows; i++) { for (let j = 0; j < this.cols; j++) { if (this.cells[i][j]) { ctx.fillStyle = \"rgba(0,255,255,0.05)\"; ctx.fillRect(j * this.cellSize,i * this.cellSize,this.cellSize,this.cellSize)} ctx.strokeRect(j * this.cellSize,i * this.cellSize,this.cellSize,this.cellSize)} } } } } const \"particles\": Particle[] = []; const grid = new Grid(); for (let i = 0; i < 50; i++) { particles.push(new Particle())} const animate = () => { ctx.clearRect(0,0,canvas.width,canvas.height); grid.update(); grid.draw(); for (let i = particles.length - 1; i >= 0; i--) { const particle = particles[i]; particle.update(); particle.draw(); if (particle.isDead()) { particles.splice(i,1); particles.push(new Particle())} } animationRef.current = requestAnimationFrame(animate)}; animate(); return () => { window.removeEventListener(\"resize\",resizeCanvas); if (animationRef.current) { cancelAnimationFrame(animationRef.current)} }},[]); return ( <div className={`relative overflow-hidden ${className}`}> <canvas ref={canvasRef} className=\"absolute inset-0 w-full h-full\" style={{ \"zIndex\": \'-1\' }} /> <div className=\"relative z-10\"> {children} </div> </div> )}; export default FuturisticAnimatedBackground2029;
const React,{ useEffect,useRef } from "react"; interface FuturisticAnimatedBackground2029Props extends React.PropsWithChildren<{}> { className?: string} const FuturisticAnimatedBackground2029: React.FC<FuturisticAnimatedBackground2029Props> = ({ children,className = }) => { const canvasRef = useRef<HTMLCanvasElement>(null); const animationRef = useRef<number | null>(null); useEffect(() => { const canvas = canvasRef.current; if (!canvas) return; const ctx = canvas.getContext("2d"); if (!ctx) return; const resizeCanvas = () => { canvas.width = window.innerWidth; canvas.height = window.innerHeight}; resizeCanvas(); window.addEventListener("resize",resizeCanvas); class Particle { x: number; y: number; vx: number; vy: number; size: number; life: number; maxLife: number; color: string; constructor() { this.x = Math.random() * canvas.width; this.y = Math.random() * canvas.height; this.vx = (Math.random() - 0.5) * 2; this.vy = (Math.random() - 0.5) * 2; this.size = Math.random() * 3 + 1; this.life = this.maxLife = Math.random() * 100 + 50; this.color = `hsl(${Math.random() * 60 + 200},70%,60%)`} update() { this.x += this.vx; this.y += this.vy; this.life--; if (this.x < 0) this.x = canvas.width; if (this.x > canvas.width) this.x = 0; if (this.y < 0) this.y = canvas.height; if (this.y > canvas.height) this.y = 0; this.vy += 0.01} draw() { if (ctx) { const alpha = this.life / this.maxLife; ctx.save(); ctx.globalAlpha = alpha; ctx.fillStyle = this.color; ctx.beginPath(); ctx.arc(this.x,this.y,this.size,0,Math.PI * 2); ctx.fill(); ctx.shadowColor = this.color; ctx.shadowBlur = 10; ctx.fill(); ctx.restore()} } isDead() { return this.life <= 0} } class Grid { cells: boolean[][]; cellSize: number; cols: number; rows: number; constructor() { this.cellSize = 50; this.cols = Math.ceil(canvas.width / this.cellSize); this.rows = Math.ceil(canvas.height / this.cellSize); this.cells = Array(this.rows).fill(null).map(() => Array(this.cols).fill(false))} update() { for (let i = 0; i < this.rows; i++) { for (let j = 0; j < this.cols; j++) { if (Math.random() < 0.01) { this.cells[i][j] = !this.cells[i][j]} } } } draw() { if (ctx) { ctx.strokeStyle = "rgba(0,255,255,0.1)"; ctx.lineWidth = 1; for (let i = 0; i < this.rows; i++) { for (let j = 0; j < this.cols; j++) { if (this.cells[i][j]) { ctx.fillStyle = "rgba(0,255,255,0.05)"; ctx.fillRect(j * this.cellSize,i * this.cellSize,this.cellSize,this.cellSize)} ctx.strokeRect(j * this.cellSize,i * this.cellSize,this.cellSize,this.cellSize)} } } } } const particles: Particle[] = []; const grid = new Grid(); for (let i = 0; i < 50; i++) { particles.push(new Particle())} const animate = () => { ctx.clearRect(0,0,canvas.width,canvas.height); grid.update(); grid.draw(); for (let i = particles.length - 1; i >= 0; i--) { const particle = particles[i]; particle.update(); particle.draw(); if (particle.isDead()) { particles.splice(i,1); particles.push(new Particle())} } animationRef.current = requestAnimationFrame(animate)}; animate(); return () => { window.removeEventListener("resize",resizeCanvas); if (animationRef.current) { cancelAnimationFrame(animationRef.current)} }},[]); return ( <div className={`relative overflow-hidden ${className}`}> <canvas ref={canvasRef} className="absolute inset-0 w-full h-full" style={{ zIndex: "-1" }} /> <div className="relative z-10"> {children} </div> </div> )}; export default FuturisticAnimatedBackground2029;'"`'"`
import _React,{ useEffect,useRef } from "react"; interface FuturisticAnimatedBackground2029Props extends _React.PropsWithChildren<{}> { className?: string} const "FuturisticAnimatedBackground2029": _React.FC<FuturisticAnimatedBackground2029Props> = ({ children,className = }) => { const canvasRef = useRef<HTMLCanvasElement>(null); const animationRef = useRef<number | null>(null); useEffect(() => { const canvas = canvasRef.current; if (!canvas) return; const ctx = canvas.getContext("2d"); if (!ctx) return; const resizeCanvas = () => { canvas.width = window.innerWidth; canvas.height = window.innerHeight}; resizeCanvas(); window.addEventListener("resize",resizeCanvas); class Particle { "x": number; y: number; vx: number; vy: number; size: number; life: number; maxLife: number; color: string; constructor() { this.x = Math.random() * canvas.width; this.y = Math.random() * canvas.height; this.vx = (Math.random() - 0.5) * 2; this.vy = (Math.random() - 0.5) * 2; this.size = Math.random() * 3 + 1; this.life = this.maxLife = Math.random() * 100 + 50; this.color = `hsl(${Math.random() * 60 + 200},70%,60%)`} update() { this.x += this.vx; this.y += this.vy; this.life--; if (this.x < 0) this.x = canvas.width; if (this.x > canvas.width) this.x = 0; if (this.y < 0) this.y = canvas.height; if (this.y > canvas.height) this.y = 0; this.vy += 0.01} draw() { if (ctx) { const alpha = this.life / this.maxLife; ctx.save(); ctx.globalAlpha = alpha; ctx.fillStyle = this.color; ctx.beginPath(); ctx.arc(this.x,this.y,this.size,0,Math.PI * 2); ctx.fill(); ctx.shadowColor = this.color; ctx.shadowBlur = 10; ctx.fill(); ctx.restore()} } isDead() { return this.life <= 0} } class Grid { "cells": boolean[][]; cellSize: number; cols: number; rows: number; constructor() { this.cellSize = 50; this.cols = Math.ceil(canvas.width / this.cellSize); this.rows = Math.ceil(canvas.height / this.cellSize); this.cells = Array(this.rows).fill(null).map(() => Array(this.cols).fill(false))} update() { for (let i = 0; i < this.rows; i++) { for (let j = 0; j < this.cols; j++) { if (Math.random() < 0.01) { this.cells[i][j] = !this.cells[i][j]} } } } draw() { if (ctx) { ctx.strokeStyle = "rgba(0,255,255,0.1)"; ctx.lineWidth = 1; for (let i = 0; i < this.rows; i++) { for (let j = 0; j < this.cols; j++) { if (this.cells[i][j]) { ctx.fillStyle = "rgba(0,255,255,0.05)"; ctx.fillRect(j * this.cellSize,i * this.cellSize,this.cellSize,this.cellSize)} ctx.strokeRect(j * this.cellSize,i * this.cellSize,this.cellSize,this.cellSize)} } } } } const "particles": Particle[] = []; const grid = new Grid(); for (let i = 0; i < 50; i++) { particles.push(new Particle())} const animate = () => { ctx.clearRect(0,0,canvas.width,canvas.height); grid.update(); grid.draw(); for (let i = particles.length - 1; i >= 0; i--) { const particle = particles[i]; particle.update(); particle.draw(); if (particle.isDead()) { particles.splice(i,1); particles.push(new Particle())} } animationRef.current = requestAnimationFrame(animate)}; animate(); return () => { window.removeEventListener("resize",resizeCanvas); if (animationRef.current) { cancelAnimationFrame(animationRef.current)} }},[]); return ( <div className={`relative overflow-hidden ${className}`}> <canvas ref={canvasRef} className="absolute inset-0 w-full h-full" style={{ "zIndex": '-1' }} /> <div className="relative z-10"> {children} </div> </div> )}; export default FuturisticAnimatedBackground2029;
