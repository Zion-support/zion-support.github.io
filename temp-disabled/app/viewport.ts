import { Viewport } from 'next';

export const viewport: Viewport = {
  colorScheme: 'light',
  themeColor: '#000000',
  width: 'device-width',
  initialScale: 1,
  maximumScale: 5,
  userScalable: true,
};