import React from \'react\';

interface SEOProps {
  className?: string;}
  children?: React.ReactNode;}
}

const SEO: React.FC<SEOProps    /> = ({ className = \"\", children }) => {}
  return (}
    <div data-testid=\"eo\" className={className}    />
      {children || <div    />SEO Component</div>}
    </div>
  );
};

export default SEO;
