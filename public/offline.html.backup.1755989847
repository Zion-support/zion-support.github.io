<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Offline - Zion Tech Group</title>
    <meta name="description" content="You're currently offline. Zion Tech Group services are temporarily unavailable.">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            background: linear-gradient(135deg, #0f0f23 0%, #1a1a2e 50%, #16213e 100%);
            color: #ffffff;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            padding: 20px;
        }

        .offline-container {
            max-width: 500px;
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(20px);
            border-radius: 24px;
            padding: 60px 40px;
            border: 1px solid rgba(255, 255, 255, 0.1);
            box-shadow: 0 25px 50px rgba(0, 0, 0, 0.5);
        }

        .offline-icon {
            width: 120px;
            height: 120px;
            margin: 0 auto 30px;
            background: linear-gradient(135deg, #ff6b6b, #ee5a24);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 48px;
            animation: pulse 2s infinite;
        }

        @keyframes pulse {
            0%, 100% { transform: scale(1); opacity: 1; }
            50% { transform: scale(1.05); opacity: 0.8; }
        }

        .offline-title {
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: 20px;
            background: linear-gradient(135deg, #ff6b6b, #ee5a24);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .offline-message {
            font-size: 1.2rem;
            color: #b8b8b8;
            margin-bottom: 30px;
            line-height: 1.6;
        }

        .status-indicator {
            display: inline-flex;
            align-items: center;
            gap: 10px;
            padding: 12px 24px;
            background: rgba(255, 107, 107, 0.1);
            border: 1px solid rgba(255, 107, 107, 0.3);
            border-radius: 50px;
            margin-bottom: 40px;
            font-size: 0.9rem;
            color: #ff6b6b;
        }

        .status-dot {
            width: 8px;
            height: 8px;
            background: #ff6b6b;
            border-radius: 50%;
            animation: blink 1.5s infinite;
        }

        @keyframes blink {
            0%, 50% { opacity: 1; }
            51%, 100% { opacity: 0.3; }
        }

        .features {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin: 40px 0;
        }

        .feature {
            padding: 20px;
            background: rgba(255, 255, 255, 0.03);
            border-radius: 16px;
            border: 1px solid rgba(255, 255, 255, 0.05);
            transition: all 0.3s ease;
        }

        .feature:hover {
            background: rgba(255, 255, 255, 0.08);
            transform: translateY(-2px);
        }

        .feature-icon {
            font-size: 2rem;
            margin-bottom: 15px;
            color: #4ecdc4;
        }

        .feature-title {
            font-weight: 600;
            margin-bottom: 8px;
            color: #ffffff;
        }

        .feature-desc {
            font-size: 0.9rem;
            color: #b8b8b8;
            line-height: 1.5;
        }

        .actions {
            margin-top: 40px;
        }

        .btn {
            display: inline-block;
            padding: 16px 32px;
            margin: 0 10px;
            background: linear-gradient(135deg, #4ecdc4, #44a08d);
            color: white;
            text-decoration: none;
            border-radius: 50px;
            font-weight: 600;
            transition: all 0.3s ease;
            border: none;
            cursor: pointer;
            font-size: 1rem;
        }

        .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 25px rgba(78, 205, 196, 0.3);
        }

        .btn-secondary {
            background: transparent;
            border: 2px solid #4ecdc4;
            color: #4ecdc4;
        }

        .btn-secondary:hover {
            background: #4ecdc4;
            color: white;
        }

        .network-info {
            margin-top: 30px;
            padding: 20px;
            background: rgba(255, 255, 255, 0.03);
            border-radius: 16px;
            border: 1px solid rgba(255, 255, 255, 0.05);
        }

        .network-status {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
            margin-bottom: 15px;
        }

        .retry-btn {
            background: none;
            border: none;
            color: #4ecdc4;
            cursor: pointer;
            text-decoration: underline;
            font-size: 0.9rem;
        }

        .retry-btn:hover {
            color: #ffffff;
        }

        @media (max-width: 768px) {
            .offline-container {
                padding: 40px 20px;
            }
            
            .offline-title {
                font-size: 2rem;
            }

            .features {
                grid-template-columns: 1fr;
            }

            .btn {
                display: block;
                margin: 10px 0;
                text-align: center;
            }
        }
    </style>
</head>
<body>
    <div class="offline-container">
        <div class="offline-icon">
            📡
        </div>

        <h1>You're Offline</h1>
        
        <p class="subtitle">
            It looks like you've lost your connection to the internet. Don't worry - 
            Zion Tech Group is still here to help you explore the future of technology.
        </p>

        <div class="status-indicator">
            <div class="status-dot"></div>
            <span>Network Connection Lost</span>
        </div>

        <div class="features">
            <div class="feature">
                <div class="feature-icon">🚀</div>
                <div class="feature-title">Revolutionary Technology</div>
                <div class="feature-desc">
                    Our AI consciousness and quantum computing solutions are still accessible offline
                </div>
            </div>

            <div class="feature">
                <div class="feature-icon">💡</div>
                <div class="feature-title">Smart Caching</div>
                <div class="feature-desc">
                    Key content and features are cached for offline access
                </div>
            </div>

            <div class="feature">
                <div class="feature-icon">🔒</div>
                <div class="feature-title">Secure & Reliable</div>
                <div class="feature-desc">
                    Your data and preferences remain secure even offline
                </div>
            </div>
        </div>

        <div class="actions">
            <button class="btn" onclick="retryConnection()">
                🔄 Retry Connection
            </button>
            <button class="btn btn-secondary" onclick="goHome()">
                🏠 Go Home
            </button>
        </div>

        <div class="network-info">
            <div class="network-status">
                <span>Network Status:</span>
                <span id="networkStatus">Checking...</span>
            </div>
            <p style="text-align: center; color: #b8b8b8; font-size: 0.9rem;">
                We'll automatically reconnect when your internet is restored.
                <br>
                <button class="retry-btn" onclick="retryConnection()">
                    Click here to retry now
                </button>
            </p>
        </div>
    </div>

    <script>
        // Check network status
        function checkNetworkStatus() {
            const statusElement = document.getElementById('networkStatus');
            
            if (navigator.onLine) {
                statusElement.textContent = 'Connected';
                statusElement.style.color = '#4ecdc4';
                // Redirect to home page when back online
                setTimeout(() => {
                    window.location.href = '/';
                }, 2000);
            } else {
                statusElement.textContent = 'Disconnected';
                statusElement.style.color = '#ff6b6b';
            }
        }

        // Retry connection
        function retryConnection() {
            const statusElement = document.getElementById('networkStatus');
            statusElement.textContent = 'Checking...';
            statusElement.style.color = '#ffd93d';
            
            // Simulate connection check
            setTimeout(() => {
                checkNetworkStatus();
            }, 1000);
        }

        // Go home (will work if cached)
        function goHome() {
            window.location.href = '/';
        }

        // Listen for network changes
        window.addEventListener('online', () => {
            checkNetworkStatus();
        });

        window.addEventListener('offline', () => {
            checkNetworkStatus();
        });

        // Check status on load
        document.addEventListener('DOMContentLoaded', () => {
            checkNetworkStatus();
            
            // Check status every 5 seconds
            setInterval(checkNetworkStatus, 5000);
        });

        // Service Worker registration
        if ('serviceWorker' in navigator) {
            navigator.serviceWorker.register('/sw.js')
                .then((registration) => {
                    console.log('Service Worker registered:', registration);
                })
                .catch((error) => {
                    console.log('Service Worker registration failed:', error);
                });
        }

        // Add some interactive elements
        document.addEventListener('DOMContentLoaded', () => {
            // Add hover effects to features
            const features = document.querySelectorAll('.feature');
            features.forEach(feature => {
                feature.addEventListener('mouseenter', () => {
                    feature.style.transform = 'translateY(-5px) scale(1.02)';
                });
                
                feature.addEventListener('mouseleave', () => {
                    feature.style.transform = 'translateY(0) scale(1)';
                });
            });
        });
    </script>
</body>
</html>