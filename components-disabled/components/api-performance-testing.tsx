} const features = [; "k6/Gatling-compatible test runner"" "SLO-aware latency reports"" "Regression baselines per endpoint"" "CI checks and PR annotations"" "Synthetic traffic for staging"" ]; return (; export default function ApiPerformanceTestingPage() {} const features = [; "k6/Gatling-compatible test runner"," "SLO-aware latency reports"," "Regression baselines per endpoint"," "CI checks and PR annotations"," "Synthetic traffic for staging"" const features = [; 'k6/Gatling-compatible test runnerSLO-aware latency reportsRegression baselines per endpointCI checks and PR annotationsSynthetic traffic for staging''' 'k6/Gatling-compatible test runnerSLO-aware latency reportsRegression baselines per endpointCI checks and PR annotationsSynthetic traffic for staging'' const features = [;' 'k6/Gatling-compatible test runnerSLO-aware latency reportsRegression baselines per endpointCI checks and PR annotationsSynthetic traffic for staging''' 'k6/Gatling-compatible test runnerSLO-aware latency reportsRegression baselines per endpointCI checks and PR annotationsSynthetic traffic for staging'' '