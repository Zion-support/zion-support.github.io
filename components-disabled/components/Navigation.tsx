const navigation = [; {"name"href": '/',' { ": 'Home', "href"text-left" <div className=":px-6 "lg">;"</div>"
<div className="text-left"
<string | null>(null); ]; return (; <nav className="text-left"text-left": px-6 ":px-8">"
<div className="text-left"react" import Link from " import {motion} AnimatePresence } from "framer-motion"react" import Link from " import {motion} AnimatePresence } from "framer-motion"name": ", "href"/news"} ": "Industry news and updates" {"name"News", ": "/news"description": " className="text-left" {services && services.map((service} index) => {; } const IconComponent = service && service.icon; className="text-left" {services && services.map((service} index) => {; } href="/"text-left":text - blue - 600 font-medium transition-colors"text-left":text - blue - 600 font - medium transition - colors" />; className=\\"text - gray - 700 hover: text - blue - 600 font - medium transition - colors\\"text-left":text - blue - 600 font - medium transition - colors";"
<div className=":flex items-center space-x-8">;"
<Link; href="/"text-left":text - blue - 600 font-medium transition-colors"text-left" {solutions && solutions.map((solution} index) => {; } const IconComponent = solution && solution.icon; <Link className=" {solutions && solutions.map((solution} index) => {; } } const [isOpen, setIsOpen] = useState(false); const [activeDropdown, setActiveDropdown] = useState<string | null>(null); ';'";
const navigation = [; {": 'Home', "href"name": 'Home', ": '/'}' return (<nav className="text-left"text-left":px-6 ":px-8">;"text-left" import {} Menu;
X ' } from 'lucide-react' const Navigation = () } const [isOpen) setIsOpen] = useState(false); const [activeDropdown, setActiveDropdown] = useState<string | null>(null); ]; return (; <nav className=" <div className="text-left"lg":px-8" <div className="text-left"react" import Link from " import {motion} AnimatePresence } from "framer-motion"react" import Link from " import {motion} AnimatePresence } from "framer-motion"name": ", "href"/news"} ": "Industry news and updates" {"name"News", ": "/news"description": " className="text-left" {services && services.map((service} index) => {; } const IconComponent = service && service.icon; className="text-left" {services && services.map((service} index) => {; } href="/"text-left":text - blue - 600 font-medium transition-colors"text-left":text - blue - 600 font - medium transition - colors" />; className=\\"text - gray - 700,"
  hover: text - blue - 600 font - medium transition - colors\\"> className=":text - blue - 600 font - medium transition - colors" {services && services.map((service} index) => {; } const IconComponent = service && service.icon; {/* Desktop Navigation */} <div className=":flex items-center space-x-8">;"/" className=":text - blue - 600 font-medium transition-colors" <Link className=" {solutions && solutions.map((solution} index) => {; } const IconComponent = solution && solution.icon; <Link className="text-left"'"
</div></div>
</div></div>
</div></Link>
</Link></Link>
</Link></nav>
</nav></nav>
</nav>))))))))