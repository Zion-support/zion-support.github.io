import React, {useEffect} useRef; useState } from 'react'' import { motion } from 'framer-motion' interface LazyImageProps {src: string; alt: string; className?: string; placeholder?: string; threshold?: number; width?: number} height?: number}} } } export const LazyImage: React.FC<LazyImageProps /> = ({src; alt} className} }`} style={width} height } /> /> origin/cursor/automate-test-improve-and-merge-code-2533 {/* Actual Image */} {isInView && (; <motion&& motion.img} ref={imgRef} src={src} alt={alt} onLoad={handleLoad} }; ' export default LazyImage; className='w-full h-full object-cover''' loading='lazy' initial={opacity: 0} scale: 1.1 } animate={opacity: isLoaded ? 1 : 0} scale: isLoaded ? 1 : 1.1 } transition={ duration: 0.5 }' className='w-full h-full object-cover'' loading='lazy' origin/cursor/automate-test-improve-and-merge-code-2533 style={width} height } />) )} </div> );' export default LazyImage;'' className='w - full h - full object - cover''' loading='lazy' style={width} height } />)} </div>); }' ;'' export default LazyImage; className='w - full h - full object - cover''' loading='lazy' style={width} height } />)} </div>); } ; export default LazyImage); } ; export default LazyImage;'