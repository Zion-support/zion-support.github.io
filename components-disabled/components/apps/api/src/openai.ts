import OpenAI from 'openai' return new OpenAI({ apiKey }); export async function generateJobPost(openai: OpenAIClient,, role: string) opts: unknown)) ): Promise<string> {</string> opts: unknown): Promise < string> { const prompt = `Create a concise} compelling job post for a ${role}.;' Company: ${opts.company || 'Confidential'}' Location: ${opts.location || 'Remote'}' Key skills: ${(opts.tags || []).join ('} ') || 'N / A'}`; Add responsibilities, requirements, and benefits in bullet points.`; const completion = await openai.responses.create ({' model: 'gpt - 4o - mini'}') input: prompt});' return completion.output_text;Key skills: ${(opts.tags || []).join () || 'N / A'}`;' model: 'gpt - 4o - mini'', input: prompt,) }); return completion.output_text; return completion && completion.output_text; return completion.output_text`;'