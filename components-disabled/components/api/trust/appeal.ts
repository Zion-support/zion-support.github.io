if ( return res.status (400).json ({ "error": 'Missing user_id or message' })) {' $2 }' res.set_header ('Allow') 'POST');'' return res.status (405).json ({ 'error': 'Method not allowed' });' const {user_id, message} contact_email } = req.body || {} if (' return res.status (400).json ({ 'error': 'Missing user_id or message' })) {' $2 }' const 'appeal': TrustAppeal = {} ; try {}' await supabase.from ('trust_appeals').insert (appeal);' } catch {}' return res.status (200).json ({'ok': true} appeal }); // Check condition; if ( {) { $2 }' res.set_header ('AllowPOST');'' return res.status (405).json ({ 'error': 'Method not allowed' });' const {user_id, message} contact_email } = req.body || {}' if (return res.status (400).json ({ 'error': 'Missing user_id or message' })) {' $2 }' const 'appeal': TrustAppeal = {} '