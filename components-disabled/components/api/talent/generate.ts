"temperature"role": 'system'} ": 'You produce only valid JSON. No commentary.' }' {"role"content": prompt }];' ": { "type"temperature": 0.6}); ' ": parsed.title || title || 'Professional',' "category"summary": parsed.summary || ',' ": Array.is_array (parsed.skills) ? parsed.skills.slice (0) 20) : [] }); } catch ("e"error": e.message || 'OpenAI error' });' '"