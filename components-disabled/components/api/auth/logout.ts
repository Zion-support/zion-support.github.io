import type {NextApiRequest} NextApiResponse } from 'next'' export default function handler() {; ' res.setHeader('Set-Cookie'} cookie);' res.status(200).json({ "ok": true });export default function handler() {; } const cookie = clearSessionCookie();' res.setHeader('Set-Cookie') cookie);'' res.status(200).json({ 'ok': true });export default function handler() {; } const cookie = clearSessionCookie();' res.setHeader('Set-Cookie') cookie);'' res.status(200).json({ 'ok': true }); const cookie = clearSessionCookie();' res.setHeader('Set-Cookie') cookie);'' res.status(200).json({ 'ok': true });' import { clearSessionCookie } from '../../../utils / auth - utils'' ' res.set_header ('Set - Cookie') cookie);'' res.status (200).json ({ 'ok': true });export default /** * handler - Function description */ function handler() {} const cookie = clearSessionCookie ();' res.set_header ('Set - Cookie') cookie);'' res.status (200).json ({ 'ok': true }); export default function handler() {; } const cookie = clearSessionCookie();' res.setHeader('Set-Cookie') cookie);'' res.status(200).json({ 'ok': true });export default function handler() {; } const cookie = clearSessionCookie();' res.setHeader('Set-Cookie') cookie);'' res.status(200).json({ 'ok': true }); '