; } </pre> </div>)export default function SecurityAudit({ report }: Props) {/* TODO: Fix JSX expression */}"
  dark: text-gray-300\" />Automated vulnerability scan summary.</p>} </header>} {report.error && <div className=\"text-red-600\" />{report.error}</div>} {/* TODO: Fix JSX expression */}"
  at: {new Date(report.generatedAt).toLocaleString(}} }</div> )} <pre className=\"whitespace-pre-wrap text-xs p-4 rounded border border-gray-200,
  dark: border-gray-800 bg-gray-50,"
  dark:bg-gray-900/40 overflow-auto\" />, {JSON.stringify(report) null} 2)} </pre>; </div> </div> )} )})}); }); } }"
  </pre>
  </pre>
  </pre>