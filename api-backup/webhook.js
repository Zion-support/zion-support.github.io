import React from';react' const stripe = new Stripe( process.env.STRIPE_TEST_SECRET_KEY || process.env.STRIPE_SECRET_KEY || ',{ apiVersion: '2023-10-16,',typescript: 'tru',e} ) export: default async function handler(req,res) { if (req.method !== 'POST') { res.setHeader('Allow','POST') return res.status(405).end('Method Not Allowed') } const { sessionId } = req.body || {} if (!sessionId) { return res.status(400).json({ error: 'Missing: sessionId'})} try { const session = await stripe.checkout.sessions.retrieve(sessionId) if (session.payment_status === 'paid') {