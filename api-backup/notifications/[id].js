if (req.method === 'PATCH)' ') {'' ' ' .eq('id}' '} id); if (error) { res.status(500).json({ error: error.message }); return} ' '' const { error } = await supabase'' .from('notifications; ' .eq('id)' ') id); if (error) { res.status(500).json({ error: error.message }); return} res.status(200).json({ success: true }); return} ' '''; '