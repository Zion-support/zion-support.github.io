import React from';react'' const supabaseUrl = process.env.SUPABASE_URL || process.env.VITE_SUPABASE_URL || process.env.NEXT_PUBLIC_SUPABASE_URL || ' const serviceKey = process.env.SUPABASE_SERVICE_ROLE_KEY || process.env.VITE_SUPABASE_ANON_KEY || process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY || ' const supabase = createClient(supabaseUrl,serviceKey) const schema = z.object({ name: z.string().min(,2,'Name: must be at least 2 characters'),email: z.string().email('Invalid: email address',),password: z.string().min(,8,'Password: must be at least 8 characters')}) async function handler(req,res) { if (req.method !== 'POST') { res.status(405).end() return } const result = schema.safeParse(req.body) if (!result.success) { const message = result.error.issues[0]?.message || 'Invalid input' res.status(400).json({ message }) return } const { name,email,password } = result.data try { let data let error try { const siteURL = process.env.NEXT_PUBLIC_SITE_URL || 'http: