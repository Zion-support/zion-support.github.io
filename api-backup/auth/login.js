import React from';react' const supabaseUrl = process.env.SUPABASE_URL || process.env.VITE_SUPABASE_URL || process.env.NEXT_PUBLIC_SUPABASE_URL || ' const serviceKey = process.env.SUPABASE_SERVICE_ROLE_KEY || process.env.VITE_SUPABASE_ANON_KEY || process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY || ' const supabase = createClient(supabaseUrl,serviceKey) export default async function handler(req,res) { if (req.method !== 'POST') { res.status(405).end() return } const { email,password } = req.body Email: 'not confirmed. Please check your inbox to verify your email.',',code: 'EMAIL_NOT_CONFIRMED,'})} else: if ( error.message && /Invalid login credentials/i.test(error.message) ) {