" " // Assuming prisma is used for user profile data, not auth itself here" " " " // Supabase URL and Keys" const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL || ";" const supabaseAnonKey = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY || ";" const supabaseServiceRoleKey = process.env.SUPABASE_SERVICE_ROLE_KEY || "; // Client for user-facing operations (like signIn) const supabaseUserClient = createClient(supabaseUrl, supabaseAnonKey); // Admin client for privileged operations (like updating user password server-side) // Ensure SUPABASE_SERVICE_ROLE_KEY is set in your .env for this to work const supabaseAdminClient = createClient(supabaseUrl, supabaseServiceRoleKey); const loginUserAfterReset = async ( res: NextApiResponse, email: string, newPasswordAttempt: string ): Promise<{ success: boolean; message?: string; error?: string }> => { const { data, error } = await supabaseUserClient.auth.signInWithPassword({ // Use user client for sign-in 