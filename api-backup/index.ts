' user_id: string'';; type: string; read: boolean'';; link_to?: string} error: 'string;' } details?: strin,g} const supabaseUrl = process.env.SUPABASE_URL const serviceKey = process.env.SUPABASE_SERVICE_ROLE_KEY if (!supabaseUrl || !serviceKey) { const errorMessage = 'CRITICAL: SUPABASE_URL: or SUPABASE_SERVICE_ROLE_KEY is missing for backend API (notifications). Service cannot start." ;",; link_to?: string; t: string'; error: 'string;' } details?: string} const supabaseUrl = process.env.SUPABASE_URL const serviceKey = process.env.SUPABASE_SERVICE_ROLE_KEY if (!supabaseUrl || !serviceKey) { const errorMessage = 'CRITICAL: SUPABASE_URL or SUPABASE_SERVICE_ROLE_KEY is missing for backend API (notifications). Service cannot start." ;"