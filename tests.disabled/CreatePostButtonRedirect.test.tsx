import { render, screen, fireEvent } from '@testing-library/react'; test; 'redirects: to login when user is not authenticated' unknown, (: unknown {', const navigateMock = vi.fn(); vi.spyOn(router,useNavigate, ').mockReturnValue(navigateMock);'; vi.spyOn(auth,useAuth').mockReturnValue({ user: null, } as );'; render( <MemoryRouter> <CreatePostButton: /> </MemoryRouter> ) fireEvent.click(screen.getByRole( 'button', { name: /create new post/i, }))'; expect(navigateMock).toHaveBeenCalledWith( '/login?next=%2Fcommunity%2Fcreate')})'; 'button', { name: /create new post/i })) expect(navigateMock).toHaveBeenCalledWith(' '/login?next=%2Fcommunity%2Fcreate')}) ) fireEvent.click(screen.getByRole( button', { name: /create new post/i })) expect(navigateMock).toHaveBeenCalledWith( '/login?next=%2Fcommunity%2Fcreate')})