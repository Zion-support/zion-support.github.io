

<<<<<<< HEAD
ursor/fix-syntax-push-and-merge-to-main-40de
origin/cursor/integrate-build-improve-and-re-verify-c7b5
=======
=======


>>>>>>> cursor/fix-website-loading-errors-and-merge-6662
>>>>>>> cursor/expand-services-advertise-and-build-project-4b36
#!/usr/bin/env node import fs from 'fs'; import path from 'path'; import { fileURLToPath } from 'url'; import { execSync } from 'child_process'; const __filename = fileURLToPath(import.meta.url); const __dirname = path.dirname(__filename);  class DependencyUpdater { constructor() { this.projectRoot = path.resolve(__dirname,'..'); this.updates = []; this.vulnerabilities = []; this.outdatedPackages = []} async updateDependencies() { try {  await this.checkOutdatedPackages(); await this.runSecurityAudit(); await this.checkAvailableUpdates(); await this.generateReport(); } catch (error) { console.error('❌ Error during dependency update:',error.message)} } async checkOutdatedPackages() { try {  const result = execSync('npm outdated --json',{ cwd: this.projectRoot,encoding: 'utf8',stdio: ['pipe','pipe','pipe'] }); if (result.trim()) { const outdated = JSON.parse(result); this.outdatedPackages = Object.keys(outdated);  this.outdatedPackages.forEach(pkg => { const info = outdated[pkg]; })} else { } } catch (error) { if (error.status === 1) { ')} else { } } } async runSecurityAudit() { try {  const result = execSync('npm audit --audit-level moderate --json',{ cwd: this.projectRoot,encoding: 'utf8',stdio: ['pipe','pipe','pipe'] }); const audit = JSON.parse(result); if (audit.vulnerabilities) { const vulnCount = Object.keys(audit.vulnerabilities).length; this.vulnerabilities = Object.keys(audit.vulnerabilities);  this.vulnerabilities.forEach(vuln => { const info = audit.vulnerabilities[vuln]; })} else { } } catch (error) { if (error.status === 1) { ')} else { } } } async checkAvailableUpdates() { try {  const lockPath = path.join(this.projectRoot,'package-lock.json'); if (fs.existsSync(lockPath)) { const lockStats = fs.statSync(lockPath); const lockAge = Date.now() - lockStats.mtime.getTime(); const lockAgeDays = Math.floor(lockAge / (1000 * 60 * 60 * 24)); if (lockAgeDays > 30) { this.updates.push(`Package lock is ${lockAgeDays} days old - consider updating`)} } if (this.outdatedPackages.length > 0) { this.updates.push(`${this.outdatedPackages.length} packages have updates available`)} if (this.vulnerabilities.length > 0) { this.updates.push(`${this.vulnerabilities.length} security vulnerabilities need attention`)} } catch (error) { } } async generateReport() { const report = { timestamp: new Date().toISOString(),summary: { outdatedPackages: this.outdatedPackages.length,vulnerabilities: this.vulnerabilities.length,updates: this.updates.length },outdatedPackages: this.outdatedPackages,vulnerabilities: this.vulnerabilities,updateSuggestions: this.updates,recommendations: this.generateRecommendations() }; const reportPath = path.join(this.projectRoot,'logs','dependency-update-report.json'); try { fs.writeFileSync(reportPath,JSON.stringify(report,null,2)); } catch (error) { }  );    if (this.updates.length > 0) {  this.updates.forEach((update,index) => { })} )} generateRecommendations() { const recommendations = []; if (this.outdatedPackages.length > 0) { recommendations.push('Run "npm update" to update packages within version constraints'); recommendations.push('Review major version updates with "npm outdated"')} if (this.vulnerabilities.length > 0) { recommendations.push('Run "npm audit fix" to automatically fix vulnerabilities'); recommendations.push('Review and manually fix remaining vulnerabilities')} if (this.outdatedPackages.length === 0 && this.vulnerabilities.length === 0) { recommendations.push('Dependencies are up to date and secure')} return recommendations} } const updater = new DependencyUpdater(); updater.updateDependencies().then(() => {  process.exit(0)}).catch((error) => { console.error('❌ Dependency Updater Failed:',error); process.exit(1)});
#!/usr/bin/env node
/**
 * Dependency Updater Script
 * Replaces GitHub Actions dependencies workflow
 * Checks and updates project dependencies
 */
import fs from 'fs';
import path from 'path';
<<<<<<< HEAD
import { fileURLToPath } from 'url';
import { execSync } from 'child_process';
const __filename = fileURLToPath(import.meta.url);
const __dirname = path.dirname(__filename);
console.log('📦 Dependency Updater Started');
=======

import {fileURLToPath} from 'url';
import {execSync} from 'child_process';
const __filename = fileURLToPath(import && import.meta.url);
const __dirname = path && path.dirname(__filename);
console && console.log('📦 Dependency Updater Started');

>>>>>>> cursor/expand-services-advertise-and-build-project-4b36
class DependencyUpdater {
  constructor() {
    this.projectRoot = path.resolve(__dirname, '..');
    this.updates = [];
    this.vulnerabilities = [];
    this.outdatedPackages = [];
  }
  async updateDependencies() {
    try {
      console.log('🔍 Checking dependencies...');
      // Check for outdated packages
      await this.checkOutdatedPackages();
      // Run security audit
      await this.runSecurityAudit();
      // Check for available updates
      await this.checkAvailableUpdates();
      // Generate update report
      await this.generateReport();
      console.log('✅ Dependency update check completed');
    } catch (error) {
      console.error('❌ Error during dependency "update": ', error.message);
    }
  }
  async checkOutdatedPackages() {
    try {
      console.log('📋 Checking for outdated packages...');
      const result = execSync('npm outdated --json', {
        "cwd": this.projectRoot,
        "encoding": 'utf8',
        "stdio": ['pipe', 'pipe', 'pipe']
      });
      if (result.trim()) {
        const outdated = JSON.parse(result);
        this.outdatedPackages = Object.keys(outdated);
        console.log(`📦 Found ${this.outdatedPackages.length} outdated "packages": `);
        this.outdatedPackages.forEach(pkg => {
          const info = outdated[pkg];
          console.log(`   - ${pkg}: ${info.current} → ${info.latest}`);
        });
      } else {
        console.log('✅ All packages are up to date');
      }
    } catch (error) {
      if (error.status === 1) {
        // npm outdated returns 1 when there are outdated packages
        console.log('📦 Found outdated packages (details above)');
      } else {
        console.warn('⚠️  Could not check outdated "packages": ', error.message);
      }
    }
  }
  async runSecurityAudit() {
    try {
      console.log('🔒 Running security audit...');
      const result = execSync('npm audit --audit-level moderate --json', {
        "cwd": this.projectRoot,
        "encoding": 'utf8',
        "stdio": ['pipe', 'pipe', 'pipe']
      });
      const audit = JSON.parse(result);
      if (audit.vulnerabilities) {
        const vulnCount = Object.keys(audit.vulnerabilities).length;
        this.vulnerabilities = Object.keys(audit.vulnerabilities);
        console.log(`⚠️  Found ${vulnCount} security "vulnerabilities": `);
        this.vulnerabilities.forEach(vuln => {
          const info = audit.vulnerabilities[vuln];
          console.log(`   - ${vuln}: ${info.severity} - ${info.title}`);
        });
      } else {
        console.log('✅ No security vulnerabilities found');
      }
    } catch (error) {
      if (error.status === 1) {
        // npm audit returns 1 when vulnerabilities are found
        console.log('⚠️  Security vulnerabilities found (details above)');
      } else {
        console.warn('⚠️  Could not run security "audit": ', error.message);
      }
    }
  }
  async checkAvailableUpdates() {
    try {
      console.log('🔄 Checking for available updates...');
      // Check if package-lock.json exists
      const lockPath = path.join(this.projectRoot, 'package-lock.json');
      if (fs.existsSync(lockPath)) {
        const lockStats = fs.statSync(lockPath);
        const lockAge = Date.now() - lockStats.mtime.getTime();
        const lockAgeDays = Math.floor(lockAge / (1000 * 60 * 60 * 24));
        if (lockAgeDays > 30) {
          this.updates.push(`Package lock is ${lockAgeDays} days old - consider updating`);
        }
      }
      // Check for major version updates
      if (this.outdatedPackages.length > 0) {
        this.updates.push(`${this.outdatedPackages.length} packages have updates available`);
      }
      // Check for security updates
      if (this.vulnerabilities.length > 0) {
        this.updates.push(`${this.vulnerabilities.length} security vulnerabilities need attention`);
      }
    } catch (error) {
      console.warn('⚠️  Could not check available "updates": ', error.message);
    }
  }
  async generateReport() {
    const report = {
      "timestamp": new Date().toISOString(),
      "summary": {
        outdatedPackages: this.outdatedPackages.length,
        "vulnerabilities": this.vulnerabilities.length,
        "updates": this.updates.length
      },
      "outdatedPackages": this.outdatedPackages,
      "vulnerabilities": this.vulnerabilities,
      "updateSuggestions": this.updates,
      "recommendations": this.generateRecommendations()
    };
    // Save report to file
    const reportPath = path.join(this.projectRoot, 'logs', 'dependency-update-report.json');
    try {
      fs.writeFileSync(reportPath, JSON.stringify(report, null, 2));
      console.log(`📊 Report saved "to": ${reportPath}`);
    } catch (error) {
      console.warn('⚠️  Could not save "report": ', error.message);
    }
    // Display summary
    console.log('\n📋 Dependency Update "Summary": ');
    console.log('─'.repeat(50));
    console.log(`📦 Outdated packages: ${this.outdatedPackages.length}`);
    console.log(`🔒 Security "vulnerabilities": ${this.vulnerabilities.length}`);
    console.log(`🔄 Update "suggestions": ${this.updates.length}`);
    if (this.updates.length > 0) {
      console.log('\n💡 "Recommendations": ');
      this.updates.forEach((update, index) => {
        console.log(`   ${index + 1}. ${update}`);
      });
    }
    console.log('─'.repeat(50));
  }
  generateRecommendations() {
    const recommendations = [];
<<<<<<< HEAD
    if (this.outdatedPackages.length > 0) {
      recommendations.push('Run "npm update" to update packages within version constraints');
      recommendations.push('Review major version updates with "npm outdated"');
    }
    if (this.vulnerabilities.length > 0) {
      recommendations.push('Run "npm audit fix" to automatically fix vulnerabilities');
      recommendations.push('Review and manually fix remaining vulnerabilities');
    }
    if (this.outdatedPackages.length === 0 && this.vulnerabilities.length === 0) {
      recommendations.push('Dependencies are up to date and secure');
=======

    if (this && this.outdatedPackages.length > 0) {
    recommendations && recommendations.push('Run "npm update" to update packages within version constraints'),
    recommendations && recommendations.push('Review major version updates with "npm outdated"')
  }
    if (this && this.vulnerabilities.length > 0) {
    recommendations && recommendations.push('Run "npm audit fix" to automatically fix vulnerabilities'),
    recommendations && recommendations.push('Review and manually fix remaining vulnerabilities')
  }
    if (this && this.outdatedPackages.length === 0 && this && this.vulnerabilities.length === 0) {
      recommendations && recommendations.push('Dependencies are up to date and secure');

>>>>>>> cursor/expand-services-advertise-and-build-project-4b36
    }
    return recommendations;
  }


}

origin/cursor/integrate-build-improve-and-re-verify-c7b5
// Run the dependency updater
const updater = new DependencyUpdater();
<<<<<<< HEAD
=======

#!/usr/bin/env node import fs from 'fs'; import path from 'path'; import { fileURLToPath } from 'url'; import { execSync } from 'child_process'; const __filename = fileURLToPath(import.meta.url); const __dirname = path.dirname(__filename); console.log('📦 Dependency Updater Started'); class DependencyUpdater { constructor() { this.projectRoot = path.resolve(__dirname,'..'); this.updates = []; this.vulnerabilities = []; this.outdatedPackages = []} async updateDependencies() { try { console.log('🔍 Checking dependencies...'); await this.checkOutdatedPackages(); await this.runSecurityAudit(); await this.checkAvailableUpdates(); await this.generateReport(); console.log('✅ Dependency update check completed')} catch (error) { console.error('❌ Error during dependency update:',error.message)} } async checkOutdatedPackages() { try { console.log('📋 Checking for outdated packages...'); const result = execSync('npm outdated --json',{ cwd: this.projectRoot,encoding: 'utf8',stdio: ['pipe','pipe','pipe'] }); if (result.trim()) { const outdated = JSON.parse(result); this.outdatedPackages = Object.keys(outdated); console.log(`📦 Found ${this.outdatedPackages.length} outdated packages:`); this.outdatedPackages.forEach(pkg => { const info = outdated[pkg]; console.log(` - ${pkg}: ${info.current} → ${info.latest}`)})} else { console.log('✅ All packages are up to date')} } catch (error) { if (error.status === 1) { console.log('📦 Found outdated packages (details above)')} else { console.warn('⚠️ Could not check outdated packages:',error.message)} } } async runSecurityAudit() { try { console.log('🔒 Running security audit...'); const result = execSync('npm audit --audit-level moderate --json',{ cwd: this.projectRoot,encoding: 'utf8',stdio: ['pipe','pipe','pipe'] }); const audit = JSON.parse(result); if (audit.vulnerabilities) { const vulnCount = Object.keys(audit.vulnerabilities).length; this.vulnerabilities = Object.keys(audit.vulnerabilities); console.log(`⚠️ Found ${vulnCount} security vulnerabilities:`); this.vulnerabilities.forEach(vuln => { const info = audit.vulnerabilities[vuln]; console.log(` - ${vuln}: ${info.severity} - ${info.title}`)})} else { console.log('✅ No security vulnerabilities found')} } catch (error) { if (error.status === 1) { console.log('⚠️ Security vulnerabilities found (details above)')} else { console.warn('⚠️ Could not run security audit:',error.message)} } } async checkAvailableUpdates() { try { console.log('🔄 Checking for available updates...'); const lockPath = path.join(this.projectRoot,'package-lock.json'); if (fs.existsSync(lockPath)) { const lockStats = fs.statSync(lockPath); const lockAge = Date.now() - lockStats.mtime.getTime(); const lockAgeDays = Math.floor(lockAge / (1000 * 60 * 60 * 24)); if (lockAgeDays > 30) { this.updates.push(`Package lock is ${lockAgeDays} days old - consider updating`)} } if (this.outdatedPackages.length > 0) { this.updates.push(`${this.outdatedPackages.length} packages have updates available`)} if (this.vulnerabilities.length > 0) { this.updates.push(`${this.vulnerabilities.length} security vulnerabilities need attention`)} } catch (error) { console.warn('⚠️ Could not check available updates:',error.message)} } async generateReport() { const report = { timestamp: new Date().toISOString(),summary: { outdatedPackages: this.outdatedPackages.length,vulnerabilities: this.vulnerabilities.length,updates: this.updates.length },outdatedPackages: this.outdatedPackages,vulnerabilities: this.vulnerabilities,updateSuggestions: this.updates,recommendations: this.generateRecommendations() }; const reportPath = path.join(this.projectRoot,'logs','dependency-update-report.json'); try { fs.writeFileSync(reportPath,JSON.stringify(report,null,2)); console.log(`📊 Report saved to: ${reportPath}`)} catch (error) { console.warn('⚠️ Could not save report:',error.message)} console.log('\n📋 Dependency Update Summary:'); console.log('─'.repeat(50)); console.log(`📦 Outdated packages: ${this.outdatedPackages.length}`); console.log(`🔒 Security vulnerabilities: ${this.vulnerabilities.length}`); console.log(`🔄 Update suggestions: ${this.updates.length}`); if (this.updates.length > 0) { console.log('\n💡 Recommendations:'); this.updates.forEach((update,index) => { console.log(` ${index + 1}. ${update}`)})} console.log('─'.repeat(50))} generateRecommendations() { const recommendations = []; if (this.outdatedPackages.length > 0) { recommendations.push('Run "npm update" to update packages within version constraints'); recommendations.push('Review major version updates with "npm outdated"')} if (this.vulnerabilities.length > 0) { recommendations.push('Run "npm audit fix" to automatically fix vulnerabilities'); recommendations.push('Review and manually fix remaining vulnerabilities')} if (this.outdatedPackages.length === 0 && this.vulnerabilities.length === 0) { recommendations.push('Dependencies are up to date and secure')} return recommendations} } const updater = new DependencyUpdater(); updater.updateDependencies().then(() => { console.log('📦 Dependency Updater Completed'); process.exit(0)}).catch((error) => { console.error('❌ Dependency Updater Failed:',error); process.exit(1)});
#!/usr/bin/env node import fs from 'fs'; import path from 'path'; import { fileURLToPath } from 'url'; import { execSync } from 'child_process'; const __filename = fileURLToPath(import.meta.url); const __dirname = path.dirname(__filename); console.log('📦 Dependency Updater Started'); class DependencyUpdater { constructor() { this.projectRoot = path.resolve(__dirname,'..'); this.updates = []; this.vulnerabilities = []; this.outdatedPackages = []} async updateDependencies() { try { console.log('🔍 Checking dependencies...'); await this.checkOutdatedPackages(); await this.runSecurityAudit(); await this.checkAvailableUpdates(); await this.generateReport(); console.log('✅ Dependency update check completed')} catch (error) { console.error('❌ Error during dependency update:',error.message)} } async checkOutdatedPackages() { try { console.log('📋 Checking for outdated packages...'); const result = execSync('npm outdated --json',{ cwd: this.projectRoot,encoding: 'utf8',stdio: ['pipe','pipe','pipe'] }); if (result.trim()) { const outdated = JSON.parse(result); this.outdatedPackages = Object.keys(outdated); console.log(`📦 Found ${this.outdatedPackages.length} outdated packages:`); this.outdatedPackages.forEach(pkg => { const info = outdated[pkg]; console.log(` - ${pkg}: ${info.current} → ${info.latest}`)})} else { console.log('✅ All packages are up to date')} } catch (error) { if (error.status === 1) { console.log('📦 Found outdated packages (details above)')} else { console.warn('⚠️ Could not check outdated packages:',error.message)} } } async runSecurityAudit() { try { console.log('🔒 Running security audit...'); const result = execSync('npm audit --audit-level moderate --json',{ cwd: this.projectRoot,encoding: 'utf8',stdio: ['pipe','pipe','pipe'] }); const audit = JSON.parse(result); if (audit.vulnerabilities) { const vulnCount = Object.keys(audit.vulnerabilities).length; this.vulnerabilities = Object.keys(audit.vulnerabilities); console.log(`⚠️ Found ${vulnCount} security vulnerabilities:`); this.vulnerabilities.forEach(vuln => { const info = audit.vulnerabilities[vuln]; console.log(` - ${vuln}: ${info.severity} - ${info.title}`)})} else { console.log('✅ No security vulnerabilities found')} } catch (error) { if (error.status === 1) { console.log('⚠️ Security vulnerabilities found (details above)')} else { console.warn('⚠️ Could not run security audit:',error.message)} } } async checkAvailableUpdates() { try { console.log('🔄 Checking for available updates...'); const lockPath = path.join(this.projectRoot,'package-lock.json'); if (fs.existsSync(lockPath)) { const lockStats = fs.statSync(lockPath); const lockAge = Date.now() - lockStats.mtime.getTime(); const lockAgeDays = Math.floor(lockAge / (1000 * 60 * 60 * 24)); if (lockAgeDays > 30) { this.updates.push(`Package lock is ${lockAgeDays} days old - consider updating`)} } if (this.outdatedPackages.length > 0) { this.updates.push(`${this.outdatedPackages.length} packages have updates available`)} if (this.vulnerabilities.length > 0) { this.updates.push(`${this.vulnerabilities.length} security vulnerabilities need attention`)} } catch (error) { console.warn('⚠️ Could not check available updates:',error.message)} } async generateReport() { const report = { timestamp: new Date().toISOString(),summary: { outdatedPackages: this.outdatedPackages.length,vulnerabilities: this.vulnerabilities.length,updates: this.updates.length },outdatedPackages: this.outdatedPackages,vulnerabilities: this.vulnerabilities,updateSuggestions: this.updates,recommendations: this.generateRecommendations() }; const reportPath = path.join(this.projectRoot,'logs','dependency-update-report.json'); try { fs.writeFileSync(reportPath,JSON.stringify(report,null,2)); console.log(`📊 Report saved to: ${reportPath}`)} catch (error) { console.warn('⚠️ Could not save report:',error.message)} console.log('\n📋 Dependency Update Summary:'); console.log('─'.repeat(50)); console.log(`📦 Outdated packages: ${this.outdatedPackages.length}`); console.log(`🔒 Security vulnerabilities: ${this.vulnerabilities.length}`); console.log(`🔄 Update suggestions: ${this.updates.length}`); if (this.updates.length > 0) { console.log('\n💡 Recommendations:'); this.updates.forEach((update,index) => { console.log(` ${index + 1}. ${update}`)})} console.log('─'.repeat(50))} generateRecommendations() { const recommendations = []; if (this.outdatedPackages.length > 0) { recommendations.push('Run "npm update" to update packages within version constraints'); recommendations.push('Review major version updates with "npm outdated"')} if (this.vulnerabilities.length > 0) { recommendations.push('Run "npm audit fix" to automatically fix vulnerabilities'); recommendations.push('Review and manually fix remaining vulnerabilities')} if (this.outdatedPackages.length === 0 && this.vulnerabilities.length === 0) { recommendations.push('Dependencies are up to date and secure')} return recommendations} } const updater = new DependencyUpdater(); updater.updateDependencies().then(() => { console.log('📦 Dependency Updater Completed'); process.exit(0)}).catch((error) => { console.error('❌ Dependency Updater Failed:',error); process.exit(1)});

=======
>>>>>>> cursor/expand-services-advertise-and-build-project-4b36
updater && updater.updateDependencies().then(() => {
    console && console.log('📦 Dependency Updater Completed'),
    process && process.exit(0)
  }).catch((error) => {
  console && console.error('❌ Dependency Updater "Failed": ', error);
  process && process.exit(1);
});
#!/usr/bin/env node import fs from 'fs'; import path from 'path'; import { fileURLToPath } from 'url'; import { execSync } from 'child_process'; const __filename = fileURLToPath(import && import.meta.url); const __dirname = path && path.dirname(__filename); console && console.log('📦 Dependency Updater Started'); class DependencyUpdater { constructor() { this && this.projectRoot = path && path.resolve(__dirname,'..'); this && this.updates = []; this && this.vulnerabilities = []; this && this.outdatedPackages = []} async updateDependencies() { try { console && console.log('🔍 Checking dependencies...'); await this && this.checkOutdatedPackages(); await this && this.runSecurityAudit(); await this && this.checkAvailableUpdates(); await this && this.generateReport(); console && console.log('✅ Dependency update check completed')} catch (error) { console && console.error('❌ Error during dependency update:',error && error.message)} } async checkOutdatedPackages() { try { console && console.log('📋 Checking for outdated packages...'); const result = execSync('npm outdated --json',{ cwd: this && this.projectRoot,encoding: 'utf8',stdio: ['pipe','pipe','pipe'] }); if (result && result.trim()) { const outdated = JSON && JSON.parse(result); this && this.outdatedPackages = Object && Object.keys(outdated); console && console.log(`📦 Found ${this && this.outdatedPackages.length} outdated packages:`); this && this.outdatedPackages.forEach(pkg => { const info = outdated[pkg]; console && console.log(` - ${pkg}: ${info && info.current} → ${info && info.latest}`)})} else { console && console.log('✅ All packages are up to date')} } catch (error) { if (error && error.status === 1) { console && console.log('📦 Found outdated packages (details above)')} else { console && console.warn('⚠️ Could not check outdated packages:',error && error.message)} } } async runSecurityAudit() { try { console && console.log('🔒 Running security audit...'); const result = execSync('npm audit --audit-level moderate --json',{ cwd: this && this.projectRoot,encoding: 'utf8',stdio: ['pipe','pipe','pipe'] }); const audit = JSON && JSON.parse(result); if (audit && audit.vulnerabilities) { const vulnCount = Object && Object.keys(audit && audit.vulnerabilities).length; this && this.vulnerabilities = Object && Object.keys(audit && audit.vulnerabilities); console && console.log(`⚠️ Found ${vulnCount} security vulnerabilities:`); this && this.vulnerabilities.forEach(vuln => { const info = audit && audit.vulnerabilities[vuln]; console && console.log(` - ${vuln}: ${info && info.severity} - ${info && info.title}`)})} else { console && console.log('✅ No security vulnerabilities found')} } catch (error) { if (error && error.status === 1) { console && console.log('⚠️ Security vulnerabilities found (details above)')} else { console && console.warn('⚠️ Could not run security audit:',error && error.message)} } } async checkAvailableUpdates() { try { console && console.log('🔄 Checking for available updates...'); const lockPath = path && path.join(this && this.projectRoot,'package-lock && lock.json'); if (fs && fs.existsSync(lockPath)) { const lockStats = fs && fs.statSync(lockPath); const lockAge = Date && Date.now() - lockStats && lockStats.mtime.getTime(); const lockAgeDays = Math && Math.floor(lockAge / (1000 * 60 * 60 * 24)); if (lockAgeDays > 30) { this && this.updates.push(`Package lock is ${lockAgeDays} days old - consider updating`)} } if (this && this.outdatedPackages.length > 0) { this && this.updates.push(`${this && this.outdatedPackages.length} packages have updates available`)} if (this && this.vulnerabilities.length > 0) { this && this.updates.push(`${this && this.vulnerabilities.length} security vulnerabilities need attention`)} } catch (error) { console && console.warn('⚠️ Could not check available updates:',error && error.message)} } async generateReport() { const report = { timestamp: new Date().toISOString(),summary: { outdatedPackages: this && this.outdatedPackages.length,vulnerabilities: this && this.vulnerabilities.length,updates: this && this.updates.length },outdatedPackages: this && this.outdatedPackages,vulnerabilities: this && this.vulnerabilities,updateSuggestions: this && this.updates,recommendations: this && this.generateRecommendations() }; const reportPath = path && path.join(this && this.projectRoot,'logs','dependency-update-report && report.json'); try { fs && fs.writeFileSync(reportPath,JSON && JSON.stringify(report,null,2)); console && console.log(`📊 Report saved to: ${reportPath}`)} catch (error) { console && console.warn('⚠️ Could not save report:',error && error.message)} console && console.log('\n📋 Dependency Update Summary:'); console && console.log('─'.repeat(50)); console && console.log(`📦 Outdated packages: ${this && this.outdatedPackages.length}`); console && console.log(`🔒 Security vulnerabilities: ${this && this.vulnerabilities.length}`); console && console.log(`🔄 Update suggestions: ${this && this.updates.length}`); if (this && this.updates.length > 0) { console && console.log('\n💡 Recommendations:'); this && this.updates.forEach((update,index) => { console && console.log(` ${index + 1}. ${update}`)})} console && console.log('─'.repeat(50))} generateRecommendations() { const recommendations = []; if (this && this.outdatedPackages.length > 0) { recommendations && recommendations.push('Run "npm update" to update packages within version constraints'); recommendations && recommendations.push('Review major version updates with "npm outdated"')} if (this && this.vulnerabilities.length > 0) { recommendations && recommendations.push('Run "npm audit fix" to automatically fix vulnerabilities'); recommendations && recommendations.push('Review and manually fix remaining vulnerabilities')} if (this && this.outdatedPackages.length === 0 && this && this.vulnerabilities.length === 0) { recommendations && recommendations.push('Dependencies are up to date and secure')} return recommendations} } const updater = new DependencyUpdater(); updater && updater.updateDependencies().then(() => { console && console.log('📦 Dependency Updater Completed'); process && process.exit(0)}).catch((error) => { console && console.error('❌ Dependency Updater Failed:',error); process && process.exit(1)});
#!/usr/bin/env node import fs from 'fs'; import path from 'path'; import { fileURLToPath } from 'url'; import { execSync } from 'child_process'; const __filename = fileURLToPath(import && import.meta.url); const __dirname = path && path.dirname(__filename); console && console.log('📦 Dependency Updater Started'); class DependencyUpdater { constructor() { this && this.projectRoot = path && path.resolve(__dirname,'..'); this && this.updates = []; this && this.vulnerabilities = []; this && this.outdatedPackages = []} async updateDependencies() { try { console && console.log('🔍 Checking dependencies...'); await this && this.checkOutdatedPackages(); await this && this.runSecurityAudit(); await this && this.checkAvailableUpdates(); await this && this.generateReport(); console && console.log('✅ Dependency update check completed')} catch (error) { console && console.error('❌ Error during dependency update:',error && error.message)} } async checkOutdatedPackages() { try { console && console.log('📋 Checking for outdated packages...'); const result = execSync('npm outdated --json',{ cwd: this && this.projectRoot,encoding: 'utf8',stdio: ['pipe','pipe','pipe'] }); if (result && result.trim()) { const outdated = JSON && JSON.parse(result); this && this.outdatedPackages = Object && Object.keys(outdated); console && console.log(`📦 Found ${this && this.outdatedPackages.length} outdated packages:`); this && this.outdatedPackages.forEach(pkg => { const info = outdated[pkg]; console && console.log(` - ${pkg}: ${info && info.current} → ${info && info.latest}`)})} else { console && console.log('✅ All packages are up to date')} } catch (error) { if (error && error.status === 1) { console && console.log('📦 Found outdated packages (details above)')} else { console && console.warn('⚠️ Could not check outdated packages:',error && error.message)} } } async runSecurityAudit() { try { console && console.log('🔒 Running security audit...'); const result = execSync('npm audit --audit-level moderate --json',{ cwd: this && this.projectRoot,encoding: 'utf8',stdio: ['pipe','pipe','pipe'] }); const audit = JSON && JSON.parse(result); if (audit && audit.vulnerabilities) { const vulnCount = Object && Object.keys(audit && audit.vulnerabilities).length; this && this.vulnerabilities = Object && Object.keys(audit && audit.vulnerabilities); console && console.log(`⚠️ Found ${vulnCount} security vulnerabilities:`); this && this.vulnerabilities.forEach(vuln => { const info = audit && audit.vulnerabilities[vuln]; console && console.log(` - ${vuln}: ${info && info.severity} - ${info && info.title}`)})} else { console && console.log('✅ No security vulnerabilities found')} } catch (error) { if (error && error.status === 1) { console && console.log('⚠️ Security vulnerabilities found (details above)')} else { console && console.warn('⚠️ Could not run security audit:',error && error.message)} } } async checkAvailableUpdates() { try { console && console.log('🔄 Checking for available updates...'); const lockPath = path && path.join(this && this.projectRoot,'package-lock && lock.json'); if (fs && fs.existsSync(lockPath)) { const lockStats = fs && fs.statSync(lockPath); const lockAge = Date && Date.now() - lockStats && lockStats.mtime.getTime(); const lockAgeDays = Math && Math.floor(lockAge / (1000 * 60 * 60 * 24)); if (lockAgeDays > 30) { this && this.updates.push(`Package lock is ${lockAgeDays} days old - consider updating`)} } if (this && this.outdatedPackages.length > 0) { this && this.updates.push(`${this && this.outdatedPackages.length} packages have updates available`)} if (this && this.vulnerabilities.length > 0) { this && this.updates.push(`${this && this.vulnerabilities.length} security vulnerabilities need attention`)} } catch (error) { console && console.warn('⚠️ Could not check available updates:',error && error.message)} } async generateReport() { const report = { timestamp: new Date().toISOString(),summary: { outdatedPackages: this && this.outdatedPackages.length,vulnerabilities: this && this.vulnerabilities.length,updates: this && this.updates.length },outdatedPackages: this && this.outdatedPackages,vulnerabilities: this && this.vulnerabilities,updateSuggestions: this && this.updates,recommendations: this && this.generateRecommendations() }; const reportPath = path && path.join(this && this.projectRoot,'logs','dependency-update-report && report.json'); try { fs && fs.writeFileSync(reportPath,JSON && JSON.stringify(report,null,2)); console && console.log(`📊 Report saved to: ${reportPath}`)} catch (error) { console && console.warn('⚠️ Could not save report:',error && error.message)} console && console.log('\n📋 Dependency Update Summary:'); console && console.log('─'.repeat(50)); console && console.log(`📦 Outdated packages: ${this && this.outdatedPackages.length}`); console && console.log(`🔒 Security vulnerabilities: ${this && this.vulnerabilities.length}`); console && console.log(`🔄 Update suggestions: ${this && this.updates.length}`); if (this && this.updates.length > 0) { console && console.log('\n💡 Recommendations:'); this && this.updates.forEach((update,index) => { console && console.log(` ${index + 1}. ${update}`)})} console && console.log('─'.repeat(50))} generateRecommendations() { const recommendations = []; if (this && this.outdatedPackages.length > 0) { recommendations && recommendations.push('Run "npm update" to update packages within version constraints'); recommendations && recommendations.push('Review major version updates with "npm outdated"')} if (this && this.vulnerabilities.length > 0) { recommendations && recommendations.push('Run "npm audit fix" to automatically fix vulnerabilities'); recommendations && recommendations.push('Review and manually fix remaining vulnerabilities')} if (this && this.outdatedPackages.length === 0 && this && this.vulnerabilities.length === 0) { recommendations && recommendations.push('Dependencies are up to date and secure')} return recommendations} } const updater = new DependencyUpdater(); updater && updater.updateDependencies().then(() => { console && console.log('📦 Dependency Updater Completed'); process && process.exit(0)}).catch((error) => { console && console.error('❌ Dependency Updater Failed:',error); process && process.exit(1)});
#!/usr/bin/env node import fs from 'fs'; import path from 'path'; import { fileURLToPath } from 'url'; import { execSync } from 'child_process'; const __filename = fileURLToPath(import && import.meta.url); const __dirname = path && path.dirname(__filename); console && console.log('📦 Dependency Updater Started'); class DependencyUpdater { constructor() { this && this.projectRoot = path && path.resolve(__dirname,'..'); this && this.updates = []; this && this.vulnerabilities = []; this && this.outdatedPackages = []} async updateDependencies() { try { console && console.log('🔍 Checking dependencies...'); await this && this.checkOutdatedPackages(); await this && this.runSecurityAudit(); await this && this.checkAvailableUpdates(); await this && this.generateReport(); console && console.log('✅ Dependency update check completed')} catch (error) { console && console.error('❌ Error during dependency update:',error && error.message)} } async checkOutdatedPackages() { try { console && console.log('📋 Checking for outdated packages...'); const result = execSync('npm outdated --json',{ cwd: this && this.projectRoot,encoding: 'utf8',stdio: ['pipe','pipe','pipe'] }); if (result && result.trim()) { const outdated = JSON && JSON.parse(result); this && this.outdatedPackages = Object && Object.keys(outdated); console && console.log(`📦 Found ${this && this.outdatedPackages.length} outdated packages:`); this && this.outdatedPackages.forEach(pkg => { const info = outdated[pkg]; console && console.log(` - ${pkg}: ${info && info.current} → ${info && info.latest}`)})} else { console && console.log('✅ All packages are up to date')} } catch (error) { if (error && error.status === 1) { console && console.log('📦 Found outdated packages (details above)')} else { console && console.warn('⚠️ Could not check outdated packages:',error && error.message)} } } async runSecurityAudit() { try { console && console.log('🔒 Running security audit...'); const result = execSync('npm audit --audit-level moderate --json',{ cwd: this && this.projectRoot,encoding: 'utf8',stdio: ['pipe','pipe','pipe'] }); const audit = JSON && JSON.parse(result); if (audit && audit.vulnerabilities) { const vulnCount = Object && Object.keys(audit && audit.vulnerabilities).length; this && this.vulnerabilities = Object && Object.keys(audit && audit.vulnerabilities); console && console.log(`⚠️ Found ${vulnCount} security vulnerabilities:`); this && this.vulnerabilities.forEach(vuln => { const info = audit && audit.vulnerabilities[vuln]; console && console.log(` - ${vuln}: ${info && info.severity} - ${info && info.title}`)})} else { console && console.log('✅ No security vulnerabilities found')} } catch (error) { if (error && error.status === 1) { console && console.log('⚠️ Security vulnerabilities found (details above)')} else { console && console.warn('⚠️ Could not run security audit:',error && error.message)} } } async checkAvailableUpdates() { try { console && console.log('🔄 Checking for available updates...'); const lockPath = path && path.join(this && this.projectRoot,'package-lock && lock.json'); if (fs && fs.existsSync(lockPath)) { const lockStats = fs && fs.statSync(lockPath); const lockAge = Date && Date.now() - lockStats && lockStats.mtime.getTime(); const lockAgeDays = Math && Math.floor(lockAge / (1000 * 60 * 60 * 24)); if (lockAgeDays > 30) { this && this.updates.push(`Package lock is ${lockAgeDays} days old - consider updating`)} } if (this && this.outdatedPackages.length > 0) { this && this.updates.push(`${this && this.outdatedPackages.length} packages have updates available`)} if (this && this.vulnerabilities.length > 0) { this && this.updates.push(`${this && this.vulnerabilities.length} security vulnerabilities need attention`)} } catch (error) { console && console.warn('⚠️ Could not check available updates:',error && error.message)} } async generateReport() { const report = { timestamp: new Date().toISOString(),summary: { outdatedPackages: this && this.outdatedPackages.length,vulnerabilities: this && this.vulnerabilities.length,updates: this && this.updates.length },outdatedPackages: this && this.outdatedPackages,vulnerabilities: this && this.vulnerabilities,updateSuggestions: this && this.updates,recommendations: this && this.generateRecommendations() }; const reportPath = path && path.join(this && this.projectRoot,'logs','dependency-update-report && report.json'); try { fs && fs.writeFileSync(reportPath,JSON && JSON.stringify(report,null,2)); console && console.log(`📊 Report saved to: ${reportPath}`)} catch (error) { console && console.warn('⚠️ Could not save report:',error && error.message)} console && console.log('\n📋 Dependency Update Summary:'); console && console.log('─'.repeat(50)); console && console.log(`📦 Outdated packages: ${this && this.outdatedPackages.length}`); console && console.log(`🔒 Security vulnerabilities: ${this && this.vulnerabilities.length}`); console && console.log(`🔄 Update suggestions: ${this && this.updates.length}`); if (this && this.updates.length > 0) { console && console.log('\n💡 Recommendations:'); this && this.updates.forEach((update,index) => { console && console.log(` ${index + 1}. ${update}`)})} console && console.log('─'.repeat(50))} generateRecommendations() { const recommendations = []; if (this && this.outdatedPackages.length > 0) { recommendations && recommendations.push('Run "npm update" to update packages within version constraints'); recommendations && recommendations.push('Review major version updates with "npm outdated"')} if (this && this.vulnerabilities.length > 0) { recommendations && recommendations.push('Run "npm audit fix" to automatically fix vulnerabilities'); recommendations && recommendations.push('Review and manually fix remaining vulnerabilities')} if (this && this.outdatedPackages.length === 0 && this && this.vulnerabilities.length === 0) { recommendations && recommendations.push('Dependencies are up to date and secure')} return recommendations} } const updater = new DependencyUpdater(); updater && updater.updateDependencies().then(() => { console && console.log('📦 Dependency Updater Completed'); process && process.exit(0)}).catch((error) => { console && console.error('❌ Dependency Updater Failed:',error); process && process.exit(1)});
#!/usr/bin/env node import fs from 'fs'; import path from 'path'; import { fileURLToPath } from 'url'; import { execSync } from 'child_process'; const __filename = fileURLToPath(import && import.meta.url); const __dirname = path && path.dirname(__filename); console && console.log('📦 Dependency Updater Started'); class DependencyUpdater { constructor() { this && this.projectRoot = path && path.resolve(__dirname,'..'); this && this.updates = []; this && this.vulnerabilities = []; this && this.outdatedPackages = []} async updateDependencies() { try { console && console.log('🔍 Checking dependencies...'); await this && this.checkOutdatedPackages(); await this && this.runSecurityAudit(); await this && this.checkAvailableUpdates(); await this && this.generateReport(); console && console.log('✅ Dependency update check completed')} catch (error) { console && console.error('❌ Error during dependency update:',error && error.message)} } async checkOutdatedPackages() { try { console && console.log('📋 Checking for outdated packages...'); const result = execSync('npm outdated --json',{ cwd: this && this.projectRoot,encoding: 'utf8',stdio: ['pipe','pipe','pipe'] }); if (result && result.trim()) { const outdated = JSON && JSON.parse(result); this && this.outdatedPackages = Object && Object.keys(outdated); console && console.log(`📦 Found ${this && this.outdatedPackages.length} outdated packages: `), this && this.outdatedPackages.forEach(pkg => { const info = outdated[pkg], console && console.log(` - ${pkg}: ${info && info.current} → ${info && info.latest}`)})} else { console && console.log('✅ All packages are up to date')} } catch (error) { if (error && error.status === 1) { console && console.log('📦 Found outdated packages (details above)')} else { console && console.warn('⚠️ Could not check outdated packages:',error && error.message)} } } async runSecurityAudit() { try { console && console.log('🔒 Running security audit...'); const result = execSync('npm audit --audit-level moderate --json',{ cwd: this && this.projectRoot,encoding: 'utf8',stdio: ['pipe','pipe','pipe'] }); const audit = JSON && JSON.parse(result); if (audit && audit.vulnerabilities) { const vulnCount = Object && Object.keys(audit && audit.vulnerabilities).length; this && this.vulnerabilities = Object && Object.keys(audit && audit.vulnerabilities); console && console.log(`⚠️ Found ${vulnCount} security vulnerabilities: `), this && this.vulnerabilities.forEach(vuln => { const info = audit && audit.vulnerabilities[vuln], console && console.log(` - ${vuln}: ${info && info.severity} - ${info && info.title}`)})} else { console && console.log('✅ No security vulnerabilities found')} } catch (error) { if (error && error.status === 1) { console && console.log('⚠️ Security vulnerabilities found (details above)')} else { console && console.warn('⚠️ Could not run security audit:',error && error.message)} } } async checkAvailableUpdates() { try { console && console.log('🔄 Checking for available updates...'); const lockPath = path && path.join(this && this.projectRoot,'package-lock && lock.json'); if (fs && fs.existsSync(lockPath)) { const lockStats = fs && fs.statSync(lockPath); const lockAge = Date && Date.now() - lockStats && lockStats.mtime.getTime(); const lockAgeDays = Math && Math.floor(lockAge / (1000 * 60 * 60 * 24)); if (lockAgeDays > 30) { this && this.updates.push(`Package lock is ${lockAgeDays} days old - consider updating`)} } if (this && this.outdatedPackages.length > 0) { this && this.updates.push(`${this && this.outdatedPackages.length} packages have updates available`)} if (this && this.vulnerabilities.length > 0) { this && this.updates.push(`${this && this.vulnerabilities.length} security vulnerabilities need attention`)} } catch (error) { console && console.warn('⚠️ Could not check available updates:',error && error.message)} } async generateReport() { const report = { timestamp: new Date().toISOString(),summary: { outdatedPackages: this && this.outdatedPackages.length,vulnerabilities: this && this.vulnerabilities.length,updates: this && this.updates.length },outdatedPackages: this && this.outdatedPackages,vulnerabilities: this && this.vulnerabilities,updateSuggestions: this && this.updates,recommendations: this && this.generateRecommendations() }; const reportPath = path && path.join(this && this.projectRoot,'logs','dependency-update-report && report.json'); try { fs && fs.writeFileSync(reportPath,JSON && JSON.stringify(report,null,2)); console && console.log(`📊 Report saved to: ${reportPath}`)} catch (error) { console && console.warn('⚠️ Could not save report:',error && error.message)} console && console.log('\n📋 Dependency Update Summary: '), console && console.log('─'.repeat(50)), console && console.log(`📦 Outdated packages: ${this && this.outdatedPackages.length}`); console && console.log(`🔒 Security vulnerabilities: ${this && this.vulnerabilities.length}`); console && console.log(`🔄 Update suggestions: ${this && this.updates.length}`); if (this && this.updates.length > 0) { console && console.log('\n💡 Recommendations:'); this && this.updates.forEach((update,index) => { console && console.log(` ${index + 1}. ${update}`)})} console && console.log('─'.repeat(50))} generateRecommendations() { const recommendations = []; if (this && this.outdatedPackages.length > 0) { recommendations && recommendations.push('Run "npm update" to update packages within version constraints'); recommendations && recommendations.push('Review major version updates with "npm outdated"')} if (this && this.vulnerabilities.length > 0) { recommendations && recommendations.push('Run "npm audit fix" to automatically fix vulnerabilities'); recommendations && recommendations.push('Review and manually fix remaining vulnerabilities')} if (this && this.outdatedPackages.length === 0 && this && this.vulnerabilities.length === 0) { recommendations && recommendations.push('Dependencies are up to date and secure')} return recommendations} } const updater = new DependencyUpdater(); updater && updater.updateDependencies().then(() => { console && console.log('📦 Dependency Updater Completed'); process && process.exit(0)}).catch((error) => { console && console.error('❌ Dependency Updater Failed:',error); process && process.exit(1)});
#!/usr/bin/env node import fs from 'fs'; import path from 'path'; import { fileURLToPath } from 'url'; import { execSync } from 'child_process'; const __filename = fileURLToPath(import && import.meta.url); const __dirname = path && path.dirname(__filename); console && console.log('📦 Dependency Updater Started'); class DependencyUpdater { constructor() { this && this.projectRoot = path && path.resolve(__dirname,'..'); this && this.updates = []; this && this.vulnerabilities = []; this && this.outdatedPackages = []} async updateDependencies() { try { console && console.log('🔍 Checking dependencies...'); await this && this.checkOutdatedPackages(); await this && this.runSecurityAudit(); await this && this.checkAvailableUpdates(); await this && this.generateReport(); console && console.log('✅ Dependency update check completed')} catch (error) { console && console.error('❌ Error during dependency update:',error && error.message)} } async checkOutdatedPackages() { try { console && console.log('📋 Checking for outdated packages...'); const result = execSync('npm outdated --json',{ cwd: this && this.projectRoot,encoding: 'utf8',stdio: ['pipe','pipe','pipe'] }); if (result && result.trim()) { const outdated = JSON && JSON.parse(result); this && this.outdatedPackages = Object && Object.keys(outdated); console && console.log(`📦 Found ${this && this.outdatedPackages.length} outdated packages: `), this && this.outdatedPackages.forEach(pkg => { const info = outdated[pkg], console && console.log(` - ${pkg}: ${info && info.current} → ${info && info.latest}`)})} else { console && console.log('✅ All packages are up to date')} } catch (error) { if (error && error.status === 1) { console && console.log('📦 Found outdated packages (details above)')} else { console && console.warn('⚠️ Could not check outdated packages:',error && error.message)} } } async runSecurityAudit() { try { console && console.log('🔒 Running security audit...'); const result = execSync('npm audit --audit-level moderate --json',{ cwd: this && this.projectRoot,encoding: 'utf8',stdio: ['pipe','pipe','pipe'] }); const audit = JSON && JSON.parse(result); if (audit && audit.vulnerabilities) { const vulnCount = Object && Object.keys(audit && audit.vulnerabilities).length; this && this.vulnerabilities = Object && Object.keys(audit && audit.vulnerabilities); console && console.log(`⚠️ Found ${vulnCount} security vulnerabilities: `), this && this.vulnerabilities.forEach(vuln => { const info = audit && audit.vulnerabilities[vuln], console && console.log(` - ${vuln}: ${info && info.severity} - ${info && info.title}`)})} else { console && console.log('✅ No security vulnerabilities found')} } catch (error) { if (error && error.status === 1) { console && console.log('⚠️ Security vulnerabilities found (details above)')} else { console && console.warn('⚠️ Could not run security audit:',error && error.message)} } } async checkAvailableUpdates() { try { console && console.log('🔄 Checking for available updates...'); const lockPath = path && path.join(this && this.projectRoot,'package-lock && lock.json'); if (fs && fs.existsSync(lockPath)) { const lockStats = fs && fs.statSync(lockPath); const lockAge = Date && Date.now() - lockStats && lockStats.mtime.getTime(); const lockAgeDays = Math && Math.floor(lockAge / (1000 * 60 * 60 * 24)); if (lockAgeDays > 30) { this && this.updates.push(`Package lock is ${lockAgeDays} days old - consider updating`)} } if (this && this.outdatedPackages.length > 0) { this && this.updates.push(`${this && this.outdatedPackages.length} packages have updates available`)} if (this && this.vulnerabilities.length > 0) { this && this.updates.push(`${this && this.vulnerabilities.length} security vulnerabilities need attention`)} } catch (error) { console && console.warn('⚠️ Could not check available updates:',error && error.message)} } async generateReport() { const report = { timestamp: new Date().toISOString(),summary: { outdatedPackages: this && this.outdatedPackages.length,vulnerabilities: this && this.vulnerabilities.length,updates: this && this.updates.length },outdatedPackages: this && this.outdatedPackages,vulnerabilities: this && this.vulnerabilities,updateSuggestions: this && this.updates,recommendations: this && this.generateRecommendations() }; const reportPath = path && path.join(this && this.projectRoot,'logs','dependency-update-report && report.json'); try { fs && fs.writeFileSync(reportPath,JSON && JSON.stringify(report,null,2)); console && console.log(`📊 Report saved to: ${reportPath}`)} catch (error) { console && console.warn('⚠️ Could not save report:',error && error.message)} console && console.log('\n📋 Dependency Update Summary: '), console && console.log('─'.repeat(50)), console && console.log(`📦 Outdated packages: ${this && this.outdatedPackages.length}`); console && console.log(`🔒 Security vulnerabilities: ${this && this.vulnerabilities.length}`); console && console.log(`🔄 Update suggestions: ${this && this.updates.length}`); if (this && this.updates.length > 0) { console && console.log('\n💡 Recommendations:'); this && this.updates.forEach((update,index) => { console && console.log(` ${index + 1}. ${update}`)})} console && console.log('─'.repeat(50))} generateRecommendations() { const recommendations = []; if (this && this.outdatedPackages.length > 0) { recommendations && recommendations.push('Run "npm update" to update packages within version constraints'); recommendations && recommendations.push('Review major version updates with "npm outdated"')} if (this && this.vulnerabilities.length > 0) { recommendations && recommendations.push('Run "npm audit fix" to automatically fix vulnerabilities'); recommendations && recommendations.push('Review and manually fix remaining vulnerabilities')} if (this && this.outdatedPackages.length === 0 && this && this.vulnerabilities.length === 0) { recommendations && recommendations.push('Dependencies are up to date and secure')} return recommendations} } const updater = new DependencyUpdater(); updater && updater.updateDependencies().then(() => { console && console.log('📦 Dependency Updater Completed'); process && process.exit(0)}).catch((error) => { console && console.error('❌ Dependency Updater Failed:',error); process && process.exit(1)});
<<<<<<< HEAD
updater.updateDependencies().then(() => {
  console.log('📦 Dependency Updater Completed');
  process.exit(0);
}).catch((error) => {
  console.error('❌ Dependency Updater "Failed": ', error);
  process.exit(1);
});
#!/usr/bin/env node import fs from 'fs'; import path from 'path'; import { fileURLToPath } from 'url'; import { execSync } from 'child_process'; const __filename = fileURLToPath(import.meta.url); const __dirname = path.dirname(__filename); console.log('📦 Dependency Updater Started'); class DependencyUpdater { constructor() { this.projectRoot = path.resolve(__dirname,'..'); this.updates = []; this.vulnerabilities = []; this.outdatedPackages = []} async updateDependencies() { try { console.log('🔍 Checking dependencies...'); await this.checkOutdatedPackages(); await this.runSecurityAudit(); await this.checkAvailableUpdates(); await this.generateReport(); console.log('✅ Dependency update check completed')} catch (error) { console.error('❌ Error during dependency update:',error.message)} } async checkOutdatedPackages() { try { console.log('📋 Checking for outdated packages...'); const result = execSync('npm outdated --json',{ cwd: this.projectRoot,encoding: 'utf8',stdio: ['pipe','pipe','pipe'] }); if (result.trim()) { const outdated = JSON.parse(result); this.outdatedPackages = Object.keys(outdated); console.log(`📦 Found ${this.outdatedPackages.length} outdated packages:`); this.outdatedPackages.forEach(pkg => { const info = outdated[pkg]; console.log(` - ${pkg}: ${info.current} → ${info.latest}`)})} else { console.log('✅ All packages are up to date')} } catch (error) { if (error.status === 1) { console.log('📦 Found outdated packages (details above)')} else { console.warn('⚠️ Could not check outdated packages:',error.message)} } } async runSecurityAudit() { try { console.log('🔒 Running security audit...'); const result = execSync('npm audit --audit-level moderate --json',{ cwd: this.projectRoot,encoding: 'utf8',stdio: ['pipe','pipe','pipe'] }); const audit = JSON.parse(result); if (audit.vulnerabilities) { const vulnCount = Object.keys(audit.vulnerabilities).length; this.vulnerabilities = Object.keys(audit.vulnerabilities); console.log(`⚠️ Found ${vulnCount} security vulnerabilities:`); this.vulnerabilities.forEach(vuln => { const info = audit.vulnerabilities[vuln]; console.log(` - ${vuln}: ${info.severity} - ${info.title}`)})} else { console.log('✅ No security vulnerabilities found')} } catch (error) { if (error.status === 1) { console.log('⚠️ Security vulnerabilities found (details above)')} else { console.warn('⚠️ Could not run security audit:',error.message)} } } async checkAvailableUpdates() { try { console.log('🔄 Checking for available updates...'); const lockPath = path.join(this.projectRoot,'package-lock.json'); if (fs.existsSync(lockPath)) { const lockStats = fs.statSync(lockPath); const lockAge = Date.now() - lockStats.mtime.getTime(); const lockAgeDays = Math.floor(lockAge / (1000 * 60 * 60 * 24)); if (lockAgeDays > 30) { this.updates.push(`Package lock is ${lockAgeDays} days old - consider updating`)} } if (this.outdatedPackages.length > 0) { this.updates.push(`${this.outdatedPackages.length} packages have updates available`)} if (this.vulnerabilities.length > 0) { this.updates.push(`${this.vulnerabilities.length} security vulnerabilities need attention`)} } catch (error) { console.warn('⚠️ Could not check available updates:',error.message)} } async generateReport() { const report = { timestamp: new Date().toISOString(),summary: { outdatedPackages: this.outdatedPackages.length,vulnerabilities: this.vulnerabilities.length,updates: this.updates.length },outdatedPackages: this.outdatedPackages,vulnerabilities: this.vulnerabilities,updateSuggestions: this.updates,recommendations: this.generateRecommendations() }; const reportPath = path.join(this.projectRoot,'logs','dependency-update-report.json'); try { fs.writeFileSync(reportPath,JSON.stringify(report,null,2)); console.log(`📊 Report saved to: ${reportPath}`)} catch (error) { console.warn('⚠️ Could not save report:',error.message)} console.log('\n📋 Dependency Update Summary:'); console.log('─'.repeat(50)); console.log(`📦 Outdated packages: ${this.outdatedPackages.length}`); console.log(`🔒 Security vulnerabilities: ${this.vulnerabilities.length}`); console.log(`🔄 Update suggestions: ${this.updates.length}`); if (this.updates.length > 0) { console.log('\n💡 Recommendations:'); this.updates.forEach((update,index) => { console.log(` ${index + 1}. ${update}`)})} console.log('─'.repeat(50))} generateRecommendations() { const recommendations = []; if (this.outdatedPackages.length > 0) { recommendations.push('Run "npm update" to update packages within version constraints'); recommendations.push('Review major version updates with "npm outdated"')} if (this.vulnerabilities.length > 0) { recommendations.push('Run "npm audit fix" to automatically fix vulnerabilities'); recommendations.push('Review and manually fix remaining vulnerabilities')} if (this.outdatedPackages.length === 0 && this.vulnerabilities.length === 0) { recommendations.push('Dependencies are up to date and secure')} return recommendations} } const updater = new DependencyUpdater(); updater.updateDependencies().then(() => { console.log('📦 Dependency Updater Completed'); process.exit(0)}).catch((error) => { console.error('❌ Dependency Updater Failed:',error); process.exit(1)});
#!/usr/bin/env node import fs from 'fs'; import path from 'path'; import { fileURLToPath } from 'url'; import { execSync } from 'child_process'; const __filename = fileURLToPath(import.meta.url); const __dirname = path.dirname(__filename); console.log('📦 Dependency Updater Started'); class DependencyUpdater { constructor() { this.projectRoot = path.resolve(__dirname,'..'); this.updates = []; this.vulnerabilities = []; this.outdatedPackages = []} async updateDependencies() { try { console.log('🔍 Checking dependencies...'); await this.checkOutdatedPackages(); await this.runSecurityAudit(); await this.checkAvailableUpdates(); await this.generateReport(); console.log('✅ Dependency update check completed')} catch (error) { console.error('❌ Error during dependency update:',error.message)} } async checkOutdatedPackages() { try { console.log('📋 Checking for outdated packages...'); const result = execSync('npm outdated --json',{ cwd: this.projectRoot,encoding: 'utf8',stdio: ['pipe','pipe','pipe'] }); if (result.trim()) { const outdated = JSON.parse(result); this.outdatedPackages = Object.keys(outdated); console.log(`📦 Found ${this.outdatedPackages.length} outdated packages:`); this.outdatedPackages.forEach(pkg => { const info = outdated[pkg]; console.log(` - ${pkg}: ${info.current} → ${info.latest}`)})} else { console.log('✅ All packages are up to date')} } catch (error) { if (error.status === 1) { console.log('📦 Found outdated packages (details above)')} else { console.warn('⚠️ Could not check outdated packages:',error.message)} } } async runSecurityAudit() { try { console.log('🔒 Running security audit...'); const result = execSync('npm audit --audit-level moderate --json',{ cwd: this.projectRoot,encoding: 'utf8',stdio: ['pipe','pipe','pipe'] }); const audit = JSON.parse(result); if (audit.vulnerabilities) { const vulnCount = Object.keys(audit.vulnerabilities).length; this.vulnerabilities = Object.keys(audit.vulnerabilities); console.log(`⚠️ Found ${vulnCount} security vulnerabilities:`); this.vulnerabilities.forEach(vuln => { const info = audit.vulnerabilities[vuln]; console.log(` - ${vuln}: ${info.severity} - ${info.title}`)})} else { console.log('✅ No security vulnerabilities found')} } catch (error) { if (error.status === 1) { console.log('⚠️ Security vulnerabilities found (details above)')} else { console.warn('⚠️ Could not run security audit:',error.message)} } } async checkAvailableUpdates() { try { console.log('🔄 Checking for available updates...'); const lockPath = path.join(this.projectRoot,'package-lock.json'); if (fs.existsSync(lockPath)) { const lockStats = fs.statSync(lockPath); const lockAge = Date.now() - lockStats.mtime.getTime(); const lockAgeDays = Math.floor(lockAge / (1000 * 60 * 60 * 24)); if (lockAgeDays > 30) { this.updates.push(`Package lock is ${lockAgeDays} days old - consider updating`)} } if (this.outdatedPackages.length > 0) { this.updates.push(`${this.outdatedPackages.length} packages have updates available`)} if (this.vulnerabilities.length > 0) { this.updates.push(`${this.vulnerabilities.length} security vulnerabilities need attention`)} } catch (error) { console.warn('⚠️ Could not check available updates:',error.message)} } async generateReport() { const report = { timestamp: new Date().toISOString(),summary: { outdatedPackages: this.outdatedPackages.length,vulnerabilities: this.vulnerabilities.length,updates: this.updates.length },outdatedPackages: this.outdatedPackages,vulnerabilities: this.vulnerabilities,updateSuggestions: this.updates,recommendations: this.generateRecommendations() }; const reportPath = path.join(this.projectRoot,'logs','dependency-update-report.json'); try { fs.writeFileSync(reportPath,JSON.stringify(report,null,2)); console.log(`📊 Report saved to: ${reportPath}`)} catch (error) { console.warn('⚠️ Could not save report:',error.message)} console.log('\n📋 Dependency Update Summary:'); console.log('─'.repeat(50)); console.log(`📦 Outdated packages: ${this.outdatedPackages.length}`); console.log(`🔒 Security vulnerabilities: ${this.vulnerabilities.length}`); console.log(`🔄 Update suggestions: ${this.updates.length}`); if (this.updates.length > 0) { console.log('\n💡 Recommendations:'); this.updates.forEach((update,index) => { console.log(` ${index + 1}. ${update}`)})} console.log('─'.repeat(50))} generateRecommendations() { const recommendations = []; if (this.outdatedPackages.length > 0) { recommendations.push('Run "npm update" to update packages within version constraints'); recommendations.push('Review major version updates with "npm outdated"')} if (this.vulnerabilities.length > 0) { recommendations.push('Run "npm audit fix" to automatically fix vulnerabilities'); recommendations.push('Review and manually fix remaining vulnerabilities')} if (this.outdatedPackages.length === 0 && this.vulnerabilities.length === 0) { recommendations.push('Dependencies are up to date and secure')} return recommendations} } const updater = new DependencyUpdater(); updater.updateDependencies().then(() => { console.log('📦 Dependency Updater Completed'); process.exit(0)}).catch((error) => { console.error('❌ Dependency Updater Failed:',error); process.exit(1)});
ursor/add-new-services-and-deploy-updates-0462
ursor/fix-syntax-push-and-merge-to-main-40de
#!/usr/bin/env node import fs from 'fs'; import path from 'path'; import { fileURLToPath } from 'url'; import { execSync } from 'child_process'; const __filename = fileURLToPath(import.meta.url); const __dirname = path.dirname(__filename); console.log('📦 Dependency Updater Started'); class DependencyUpdater { constructor() { this.projectRoot = path.resolve(__dirname,'..'); this.updates = []; this.vulnerabilities = []; this.outdatedPackages = []} async updateDependencies() { try { console.log('🔍 Checking dependencies...'); await this.checkOutdatedPackages(); await this.runSecurityAudit(); await this.checkAvailableUpdates(); await this.generateReport(); console.log('✅ Dependency update check completed')} catch (error) { console.error('❌ Error during dependency update:',error.message)} } async checkOutdatedPackages() { try { console.log('📋 Checking for outdated packages...'); const result = execSync('npm outdated --json',{ cwd: this.projectRoot,encoding: 'utf8',stdio: ['pipe','pipe','pipe'] }); if (result.trim()) { const outdated = JSON.parse(result); this.outdatedPackages = Object.keys(outdated); console.log(`📦 Found ${this.outdatedPackages.length} outdated packages:`); this.outdatedPackages.forEach(pkg => { const info = outdated[pkg]; console.log(` - ${pkg}: ${info.current} → ${info.latest}`)})} else { console.log('✅ All packages are up to date')} } catch (error) { if (error.status === 1) { console.log('📦 Found outdated packages (details above)')} else { console.warn('⚠️ Could not check outdated packages:',error.message)} } } async runSecurityAudit() { try { console.log('🔒 Running security audit...'); const result = execSync('npm audit --audit-level moderate --json',{ cwd: this.projectRoot,encoding: 'utf8',stdio: ['pipe','pipe','pipe'] }); const audit = JSON.parse(result); if (audit.vulnerabilities) { const vulnCount = Object.keys(audit.vulnerabilities).length; this.vulnerabilities = Object.keys(audit.vulnerabilities); console.log(`⚠️ Found ${vulnCount} security vulnerabilities:`); this.vulnerabilities.forEach(vuln => { const info = audit.vulnerabilities[vuln]; console.log(` - ${vuln}: ${info.severity} - ${info.title}`)})} else { console.log('✅ No security vulnerabilities found')} } catch (error) { if (error.status === 1) { console.log('⚠️ Security vulnerabilities found (details above)')} else { console.warn('⚠️ Could not run security audit:',error.message)} } } async checkAvailableUpdates() { try { console.log('🔄 Checking for available updates...'); const lockPath = path.join(this.projectRoot,'package-lock.json'); if (fs.existsSync(lockPath)) { const lockStats = fs.statSync(lockPath); const lockAge = Date.now() - lockStats.mtime.getTime(); const lockAgeDays = Math.floor(lockAge / (1000 * 60 * 60 * 24)); if (lockAgeDays > 30) { this.updates.push(`Package lock is ${lockAgeDays} days old - consider updating`)} } if (this.outdatedPackages.length > 0) { this.updates.push(`${this.outdatedPackages.length} packages have updates available`)} if (this.vulnerabilities.length > 0) { this.updates.push(`${this.vulnerabilities.length} security vulnerabilities need attention`)} } catch (error) { console.warn('⚠️ Could not check available updates:',error.message)} } async generateReport() { const report = { timestamp: new Date().toISOString(),summary: { outdatedPackages: this.outdatedPackages.length,vulnerabilities: this.vulnerabilities.length,updates: this.updates.length },outdatedPackages: this.outdatedPackages,vulnerabilities: this.vulnerabilities,updateSuggestions: this.updates,recommendations: this.generateRecommendations() }; const reportPath = path.join(this.projectRoot,'logs','dependency-update-report.json'); try { fs.writeFileSync(reportPath,JSON.stringify(report,null,2)); console.log(`📊 Report saved to: ${reportPath}`)} catch (error) { console.warn('⚠️ Could not save report:',error.message)} console.log('\n📋 Dependency Update Summary:'); console.log('─'.repeat(50)); console.log(`📦 Outdated packages: ${this.outdatedPackages.length}`); console.log(`🔒 Security vulnerabilities: ${this.vulnerabilities.length}`); console.log(`🔄 Update suggestions: ${this.updates.length}`); if (this.updates.length > 0) { console.log('\n💡 Recommendations:'); this.updates.forEach((update,index) => { console.log(` ${index + 1}. ${update}`)})} console.log('─'.repeat(50))} generateRecommendations() { const recommendations = []; if (this.outdatedPackages.length > 0) { recommendations.push('Run "npm update" to update packages within version constraints'); recommendations.push('Review major version updates with "npm outdated"')} if (this.vulnerabilities.length > 0) { recommendations.push('Run "npm audit fix" to automatically fix vulnerabilities'); recommendations.push('Review and manually fix remaining vulnerabilities')} if (this.outdatedPackages.length === 0 && this.vulnerabilities.length === 0) { recommendations.push('Dependencies are up to date and secure')} return recommendations} } const updater = new DependencyUpdater(); updater.updateDependencies().then(() => { console.log('📦 Dependency Updater Completed'); process.exit(0)}).catch((error) => { console.error('❌ Dependency Updater Failed:',error); process.exit(1)});
#!/usr/bin/env node import fs from 'fs'; import path from 'path'; import { fileURLToPath } from 'url'; import { execSync } from 'child_process'; const __filename = fileURLToPath(import.meta.url); const __dirname = path.dirname(__filename); console.log('📦 Dependency Updater Started'); class DependencyUpdater { constructor() { this.projectRoot = path.resolve(__dirname,'..'); this.updates = []; this.vulnerabilities = []; this.outdatedPackages = []} async updateDependencies() { try { console.log('🔍 Checking dependencies...'); await this.checkOutdatedPackages(); await this.runSecurityAudit(); await this.checkAvailableUpdates(); await this.generateReport(); console.log('✅ Dependency update check completed')} catch (error) { console.error('❌ Error during dependency update:',error.message)} } async checkOutdatedPackages() { try { console.log('📋 Checking for outdated packages...'); const result = execSync('npm outdated --json',{ cwd: this.projectRoot,encoding: 'utf8',stdio: ['pipe','pipe','pipe'] }); if (result.trim()) { const outdated = JSON.parse(result); this.outdatedPackages = Object.keys(outdated); console.log(`📦 Found ${this.outdatedPackages.length} outdated packages:`); this.outdatedPackages.forEach(pkg => { const info = outdated[pkg]; console.log(` - ${pkg}: ${info.current} → ${info.latest}`)})} else { console.log('✅ All packages are up to date')} } catch (error) { if (error.status === 1) { console.log('📦 Found outdated packages (details above)')} else { console.warn('⚠️ Could not check outdated packages:',error.message)} } } async runSecurityAudit() { try { console.log('🔒 Running security audit...'); const result = execSync('npm audit --audit-level moderate --json',{ cwd: this.projectRoot,encoding: 'utf8',stdio: ['pipe','pipe','pipe'] }); const audit = JSON.parse(result); if (audit.vulnerabilities) { const vulnCount = Object.keys(audit.vulnerabilities).length; this.vulnerabilities = Object.keys(audit.vulnerabilities); console.log(`⚠️ Found ${vulnCount} security vulnerabilities:`); this.vulnerabilities.forEach(vuln => { const info = audit.vulnerabilities[vuln]; console.log(` - ${vuln}: ${info.severity} - ${info.title}`)})} else { console.log('✅ No security vulnerabilities found')} } catch (error) { if (error.status === 1) { console.log('⚠️ Security vulnerabilities found (details above)')} else { console.warn('⚠️ Could not run security audit:',error.message)} } } async checkAvailableUpdates() { try { console.log('🔄 Checking for available updates...'); const lockPath = path.join(this.projectRoot,'package-lock.json'); if (fs.existsSync(lockPath)) { const lockStats = fs.statSync(lockPath); const lockAge = Date.now() - lockStats.mtime.getTime(); const lockAgeDays = Math.floor(lockAge / (1000 * 60 * 60 * 24)); if (lockAgeDays > 30) { this.updates.push(`Package lock is ${lockAgeDays} days old - consider updating`)} } if (this.outdatedPackages.length > 0) { this.updates.push(`${this.outdatedPackages.length} packages have updates available`)} if (this.vulnerabilities.length > 0) { this.updates.push(`${this.vulnerabilities.length} security vulnerabilities need attention`)} } catch (error) { console.warn('⚠️ Could not check available updates:',error.message)} } async generateReport() { const report = { timestamp: new Date().toISOString(),summary: { outdatedPackages: this.outdatedPackages.length,vulnerabilities: this.vulnerabilities.length,updates: this.updates.length },outdatedPackages: this.outdatedPackages,vulnerabilities: this.vulnerabilities,updateSuggestions: this.updates,recommendations: this.generateRecommendations() }; const reportPath = path.join(this.projectRoot,'logs','dependency-update-report.json'); try { fs.writeFileSync(reportPath,JSON.stringify(report,null,2)); console.log(`📊 Report saved to: ${reportPath}`)} catch (error) { console.warn('⚠️ Could not save report:',error.message)} console.log('\n📋 Dependency Update Summary:'); console.log('─'.repeat(50)); console.log(`📦 Outdated packages: ${this.outdatedPackages.length}`); console.log(`🔒 Security vulnerabilities: ${this.vulnerabilities.length}`); console.log(`🔄 Update suggestions: ${this.updates.length}`); if (this.updates.length > 0) { console.log('\n💡 Recommendations:'); this.updates.forEach((update,index) => { console.log(` ${index + 1}. ${update}`)})} console.log('─'.repeat(50))} generateRecommendations() { const recommendations = []; if (this.outdatedPackages.length > 0) { recommendations.push('Run "npm update" to update packages within version constraints'); recommendations.push('Review major version updates with "npm outdated"')} if (this.vulnerabilities.length > 0) { recommendations.push('Run "npm audit fix" to automatically fix vulnerabilities'); recommendations.push('Review and manually fix remaining vulnerabilities')} if (this.outdatedPackages.length === 0 && this.vulnerabilities.length === 0) { recommendations.push('Dependencies are up to date and secure')} return recommendations} } const updater = new DependencyUpdater(); updater.updateDependencies().then(() => { console.log('📦 Dependency Updater Completed'); process.exit(0)}).catch((error) => { console.error('❌ Dependency Updater Failed:',error); process.exit(1)});
origin/cursor/integrate-build-improve-and-re-verify-c7b5
=======
>>>>>>> origin/cursor/automate-test-improve-and-merge-code-382a:temp_exclude/scripts/dependency-updater.js
>>>>>>> cursor/expand-services-advertise-and-build-project-4b36
