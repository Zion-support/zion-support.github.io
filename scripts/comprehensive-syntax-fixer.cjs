#!/usr/bin/env node

const fs = require('fs'
const path = require('path'
const glob = require('glob'
  log(message, type = 'INFO'
    fixed = fixed.replace(/whileInView={{ "opacity": 1, "y"
    fixed = fixed.replace(/whileInView={{ opacity": 1, "y"
    fixed = fixed.replace(/whileInView={{ "opacity": 1, "y"
    fixed = fixed.replace(/initial={{ "opacity": 0, "y"
    fixed = fixed.replace(/transition={{ "duration": 0\.8, "delay"
    fixed = fixed.replace(/transition={{ "duration"
    fixed = fixed.replace(/viewport={{ "once"
    fixed = fixed.replace(/whileHover={{ "y"
    fixed = fixed.replace(/"([a-z]+)"