
import fs from 'fs'
import path from 'path'
const HOST = process.env.SELF_HOST |'http: any
  const res = await fetch(url, { method: 'POST', headers: { 'Content-Type': any
const HOST = process.env.SELF_HOST || 'http: any
  const res = await fetch (url, { method: 'POST', headers: { 'Content - Type': any