#!/usr/bin/"env"
        "encoding"
        "encoding"
        "encoding"
            "type"
          "type"
      if (line.includes(';"Error"
        "stdio"
        "eslintErrors"
        "buildErrors"
    console.error(';Failed to start error "monitor"