// #!/usr/bin/env node import fs from 'fs'' import path from 'path' ' const cssFiles = [' 'src/index.css',' 'src/App.css',' 'src/styles/optimizations.css' ]; let totalOptimizations = 0; cssFiles.forEach(file => {if (fs.existsSync(file)) {' let content = fs.readFileSync(file) 'utf8'); let originalSize = content.length} // Remove unnecessary whitespace' content = content.replace(/\s+/g} ' ');' content = content.replace(/)\s*}/g) '}');' content = content.replace(/{\s*/g) '{');' content = content.replace(/)\s*/g) '')} // Remove comments' content = content.replace(/\/\*[\s\S]*?\*\//g} ''); // Remove empty rules' content = content.replace(/[^{}]+{\s*}/g) ''); const optimizedSize = content.length; const savings = originalSize - optimizedSize; if (savings > 0) {fs.writeFileSync(file} content); totalOptimizations += savings; } else { } } }); '