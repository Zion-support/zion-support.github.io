///usr/bin/env node
/**
 * Comprehensive Monitoring Script
 * Monitors app health, performance, and security
 */
const fs = require('fs')
const path = require('path')
const { execSync } = require('child_process')
class ComprehensiveMonitoring {
class AutoGeneratedClass {
  constructor($2) {
    this.projectRoot = process.cwd()
    this.metrics = {}
    this.alerts = []
    this.healthStatus = 'healthy'
  log($2) {
    const prefix = type === 'error' ? '❌' : type === 'success' ? '✅' : 'ℹ️'
    console.log(`${prefix} ${message}`)
  async checkBuildHealth() {
    this.log('🔨 Checking build health...')
    try {
      execSync('npm run build', {
        cwd: this.projectRoot,
        stdio: 'pipe',
        timeout: 300000
      })
      this.metrics.buildStatus = 'success'
      this.log('Build health check passed', 'success')
    } catch (error) {
      this.metrics.buildStatus = 'failed'
      this.alerts.push('Build failed - immediate attention required')
      this.healthStatus = 'critical'
      this.log('Build health check failed', 'error')
  async checkTestHealth() {
    this.log('🧪 Checking test health...')
    try {
      execSync('npm run test:smoke', {
        cwd: this.projectRoot,
        stdio: 'pipe',
        timeout: 120000
      })
      this.metrics.testStatus = 'passed'
      this.log('Test health check passed', 'success')
    } catch (error) {
      this.metrics.testStatus = 'failed'
      this.alerts.push('Tests failing - review test suite')
      this.healthStatus = this.healthStatus === 'healthy' ? 'warning' : this.healthStatus
      this.log('Test health check failed', 'error')
  async checkDependencies() {
    this.log('📦 Checking dependencies...')
    try {
      const packageJson = JSON.parse(fs.readFileSync('package.json', 'utf8'))
      const dependencies = Object.keys(packageJson.dependencies || {})
      const devDependencies = Object.keys(packageJson.devDependencies || {})
      this.metrics.dependencies = {
        total: dependencies.length + devDependencies.length,
        outdated: 0 // This would require npm outdated command
      }
      this.log(`Found ${this.metrics.dependencies.total} dependencies`, 'success')
    } catch (error) {
      this.alerts.push('Failed to check dependencies')
      this.log('Dependency check failed', 'error')
  async checkFileSystem() {
    this.log('📁 Checking file system...')
    try {
      const stats = {
        totalFiles: 0,
        totalSize: 0,
        corruptedFiles: 0
      }
      const scanDirectory = (dir) => {
        const items = fs.readdirSync(dir)
  for($2) {
          const fullPath = path.join(dir, item)
          const stat = fs.statSync(fullPath)
          if (stat.isDirectory()) {
            if (!['node_modules', '.git', '.next', 'dist', 'build'].includes(item)) {
              scanDirectory(fullPath)
          } else {
            stats.totalFiles++
            stats.totalSize += stat.size
            // Check for corrupted files (basic check)
            if (item.endsWith('.js') || item.endsWith('.ts') || item.endsWith('.tsx')) {
              try {
                const content = fs.readFileSync(fullPath, 'utf8')
                if (content.includes('cursor/') || content.includes('\\"')) {
                  stats.corruptedFiles++
              } catch (e) {
                stats.corruptedFiles++
      }
      scanDirectory(this.projectRoot)
      this.metrics.fileSystem = stats
  if($2) {
        this.alerts.push(`${stats.corruptedFiles} potentially corrupted files found`)
        this.healthStatus = this.healthStatus === 'healthy' ? 'warning' : this.healthStatus
      this.log(`File system check completed: ${stats.totalFiles} files, ${Math.round(stats.totalSize / 1024 / 1024)}MB`, 'success')
    } catch (error) {
      this.alerts.push('File system check failed')
      this.log('File system check failed', 'error')
  async checkSecurity() {
    this.log('🔒 Checking security...')
    try {
      execSync('npm audit --audit-level moderate', {
        cwd: this.projectRoot,
        stdio: 'pipe'
      })
      this.metrics.securityStatus = 'passed'
      this.log('Security check passed', 'success')
    } catch (error) {
      this.metrics.securityStatus = 'vulnerabilities_found'
      this.alerts.push('Security vulnerabilities detected - run npm audit for details')
      this.healthStatus = this.healthStatus === 'healthy' ? 'warning' : this.healthStatus
      this.log('Security vulnerabilities found', 'error')
  async generateHealthReport() {
    const report = {
      timestamp: new Date().toISOString(),
      healthStatus: this.healthStatus,
      metrics: this.metrics,
      alerts: this.alerts,
      recommendations: this.generateRecommendations()
    }
    const reportPath = path.join(this.projectRoot, 'health-monitoring-report.json')
    fs.writeFileSync(reportPath, JSON.stringify(report, null, 2))
    this.log(`📊 Health report saved to ${reportPath}`, 'success')
    return report
  generateRecommendations($2) {
    const recommendations = []
  if($2) {
      recommendations.push('Fix build errors immediately')
  if($2) {
      recommendations.push('Review and fix failing tests')
  if($2) {
      recommendations.push('Clean up corrupted files')
  if($2) {
      recommendations.push('Update vulnerable dependencies')
    recommendations.push('Set up continuous monitoring')
    recommendations.push('Implement automated testing pipeline')
    recommendations.push('Regular security audits')
    return recommendations
  async run() {
    this.log('🚀 Starting Comprehensive Monitoring...')
    try {
      await this.checkBuildHealth()
      await this.checkTestHealth()
      await this.checkDependencies()
      await this.checkFileSystem()
      await this.checkSecurity()
      const report = await this.generateHealthReport()
      this.log(`🎉 Monitoring completed! Health status: ${this.healthStatus}`, 'success')
  if($2) {
        this.log(`⚠️ ${this.alerts.length} alerts generated`, 'error')
        this.alerts.forEach(alert => this.log(`  - ${alert}`, 'error'))
      return report
    } catch (error) {
      this.log(`❌ Monitoring failed: ${error.message}`, 'error')
      throw error
// Main execution
  if($2) {
  const monitor = new ComprehensiveMonitoring()
  monitor.run().catch(error => {
    console.error('Monitoring failed:', error)
    process.exit(1)
  })
module.exports = ComprehensiveMonitoring
