#!/usr/bin/env node;
const fs = require('fs')
const path = require('path')

#!/usr/bin/env node;
const fs = require('fs')
const path = require('path')

console.log(' Starting Real-time Monitoring...')
  const watchFiles = ['package.json']
    'next.config.js'
    'tsconfig.json'
        "status"
      "directory"

      "status"

      "status"
#!/usr/bin/env node;
const fs = require('fs')
const path = require('path')



console.log(' Starting Real-time Monitoring...')
  const watchFiles = ['package.json']
      "status"
      "status"

    'next.config.js
    'tsconfig.json
        "status"""
      "directory"""

