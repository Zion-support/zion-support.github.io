///usr/bin/env node
/**
 * API Monitor Script for PM2
 * Monitors API endpoints for performance and reliability
 */

const { execSync } = require('child_process')
const fs = require('fs')
const path = require('path')
const http = require('http')
const https = require('https')
class APIMonitor {
class AutoGeneratedClass {
  constructor($2) {
    this.processName = process.env.PM2_PROCESS_NAME || 'api-monitor'
    this.baseUrl = process.env.API_BASE_URL || 'http://localhost:3000'
    this.endpoints = process.env.API_ENDPOINTS ? process.env.API_ENDPOINTS.split(',') : [
      '/api/health',
      '/api/status',
      '/api/version'
    ]
    this.timeout = parseInt(process.env.API_TIMEOUT) || 5000
    this.logFile = path.join(__dirname, '../../logs/pm2/api-monitor.log')
    this.ensureLogDir()
  ensureLogDir($2) {
    const logDir = path.dirname(this.logFile)
    if (!fs.existsSync(logDir)) {
      fs.mkdirSync(logDir, { recursive: true })
  log($2) {
    const timestamp = new Date().toISOString()
    const logMessage = `[${timestamp}] [${this.processName}] ${message}\n`
    console.log(logMessage.trim())
    fs.appendFileSync(this.logFile, logMessage)
  async checkEndpoint(endpoint) {
    return new Promise((resolve) => {
      const url = `${this.baseUrl}${endpoint}`
      const startTime = Date.now()
      const request = (url.startsWith('https:') ? https : http).request(url, {
        method: 'GET',
        timeout: this.timeout
      }, (res) => {
        const endTime = Date.now()
        const responseTime = endTime - startTime
        resolve({
          endpoint,
          status: res.statusCode,
          responseTime,
          success: res.statusCode >= 200 && res.statusCode < 400,
          timestamp: new Date().toISOString()
        })
      })
      request.on('error', (error) => {
        const endTime = Date.now()
        const responseTime = endTime - startTime
        resolve({
          endpoint,
          status: 0,
          responseTime,
          success: false,
          error: error.message,
          timestamp: new Date().toISOString()
        })
      })
      request.on('timeout', () => {
        request.destroy()
        resolve({
          endpoint,
          status: 0,
          responseTime: this.timeout,
          success: false,
          error: 'Request timeout',
          timestamp: new Date().toISOString()
        })
      })
      request.end()
    })
  async checkAllEndpoints() {
    this.log(`Checking ${this.endpoints.length} API endpoints...`)
    const results = []
  for($2) {
      const result = await this.checkEndpoint(endpoint)
      results.push(result)
  if($2) {
        this.log(`✅ ${endpoint} - ${result.status} (${result.responseTime}ms)`)
      } else {
        this.log(`❌ ${endpoint} - ${result.status || 'ERROR'} (${result.responseTime}ms) - ${result.error || 'Unknown error'}`)
    return results
  async analyzePerformance(results) {
    const successfulResults = results.filter(r => r.success)
    const failedResults = results.filter(r => !r.success)
    const avgResponseTime = successfulResults.length > 0
      ? successfulResults.reduce((sum, r) => sum + r.responseTime, 0) / successfulResults.length
      : 0
    const slowEndpoints = successfulResults.filter(r => r.responseTime > 1000)
    const verySlowEndpoints = successfulResults.filter(r => r.responseTime > 3000)
    return {
      totalEndpoints: results.length,
      successful: successfulResults.length,
      failed: failedResults.length,
      successRate: (successfulResults.length / results.length) * 100,
      averageResponseTime: Math.round(avgResponseTime),
      slowEndpoints: slowEndpoints.length,
      verySlowEndpoints: verySlowEndpoints.length,
      issues: [
        ...failedResults.map(r => ({ endpoint: r.endpoint, issue: 'Endpoint failed', severity: 'high' })),
        ...slowEndpoints.map(r => ({ endpoint: r.endpoint, issue: 'Slow response', severity: 'medium' })),
        ...verySlowEndpoints.map(r => ({ endpoint: r.endpoint, issue: 'Very slow response', severity: 'high' }))
      ]
    }
  async generateReport() {
    this.log('Generating API monitoring report...')
    const results = await this.checkAllEndpoints()
    const performance = await this.analyzePerformance(results)
    const report = {
      timestamp: new Date().toISOString(),
      processName: this.processName,
      baseUrl: this.baseUrl,
      results,
      performance,
      environment: {
        NODE_ENV: process.env.NODE_ENV,
        API_BASE_URL: this.baseUrl,
        API_TIMEOUT: this.timeout

    }
    const reportFile = path.join(__dirname, '../../logs/pm2/api-monitor-report.json')
    fs.writeFileSync(reportFile, JSON.stringify(report, null, 2))
    this.log(`API monitoring report saved to ${reportFile}`)
    return report
  async start() {
    this.log(`${this.processName} started`)
    try {
      const report = await this.generateReport()
  if($2) {
        this.log('API monitoring completed - all endpoints healthy')
      } else {
        this.log(`API monitoring completed - ${report.performance.failed} failed, ${report.performance.slowEndpoints} slow endpoints`)
        report.performance.issues.forEach(issue => {
          this.log(`${issue.severity.toUpperCase()}: ${issue.endpoint} - ${issue.issue}`)
        })
    } catch (error) {
      this.log(`API monitoring error: ${error.message}`)
// Start the service
  if($2) {
  const apiMonitor = new APIMonitor()
  apiMonitor.start().catch(console.error)
module.exports = APIMonitor
