}};
; async checkSystemResources() {_; try {; this.log('💻 Checking system resources...');
; const _memInfo = execSync('free -m', _{ encoding: 'utf8'}); const _diskInfo = execSync('df -h', {_encoding: 'utf8'}); const _cpuInfo = execSync('top -bn1 | grep "Cpu(s)"', {_encoding: 'utf8'});
; // Parse memory info; const _memLines = memInfo.split('\n'); const _memTotal = memLines[1].split(/\s+/)[1]; const _memUsed = memLines[1].split(/\s+/)[2]; const _memFree = memLines[1].split(/\s+/)[3];
; // Parse disk info; const _diskLines = diskInfo.split('\n'); const _rootDisk = diskLines.find(line = > line.includes('/')); const _diskUsage = rootDisk ? rootDisk.split(/\s+/)[4].replace('%', ''): '0';
; // Parse CPU info; const _cpuUsage = cpuInfo.includes('id') ?; (100 - parseFloat(cpuInfo.split('id')[0].split(',')[3].replace('%id', '').trim())): 0;
; return {_; success: true; memory: {; total: parseInt(memTotal); used: parseInt(memUsed); free: parseInt(memFree); usagePercent: Math.round((parseInt(memUsed) / parseInt(memTotal)) * 100)}; disk: {_; usagePercent: parseInt(diskUsage)}; cpu: {_; usagePercent: Math.round(cpuUsage)}}} catch (error) {_; return {; success: false; error: error.message; memory: null; disk: null; cpu: null}}};
; async checkProcessHealth() {_; try {; this.log('🔄 Checking process health...');
; const _pm2List = execSync('pm2 list --json', _{ encoding: 'utf8'}); const _processes = JSON.parse(pm2List);
; const _processHealth = {_; total: processes.length; online: 0; stopped: 0; errored: 0; processes: []};
; processes.forEach(proc = > {_; const _status = proc.pm2_env?.status || 'unknown'; processHealth.processes.push({; name: proc.name; status: status; memory: proc.monit?.memory || 0; cpu: proc.monit?.cpu || 0; uptime: proc.pm2_env?.uptime || 0});
; if (status = = = 'online') processHealth.online++; else if (status = = = 'stopped') processHealth.stopped++; else if (status = = = 'errored') processHealth.errored++});
; return {_; success: true; health: processHealth}} catch (error) {_; return {; success: false; error: error.message; health: null}}};
; async checkApplicationHealth() {_; try {; this.log('🌐 Checking application health...');
; const _healthChecks = [];
; // Check if the application is running; try {; const _pm2List = execSync('pm2 list', _{ encoding: 'utf8'}); const _hasRunningApp = pm2List.includes('bolt-zion-app') && pm2List.includes('online');
; healthChecks.push({_; name: 'PM2 App Status'; status: hasRunningApp ? 'healthy': 'unhealthy'; message: hasRunningApp ? 'Application is running': 'Application is not running'})} catch (error) {_; healthChecks.push({; name: 'PM2 App Status'; status: 'error'; message: 'Could not check PM2 status'})};
; // Check if build files exist; const _buildExists = fs.existsSync('dist') || fs.existsSync('.next'); healthChecks.push({_; name: 'Build Files'; status: buildExists ? 'healthy': 'unhealthy'; message: buildExists ? 'Build files exist': 'Build files missing'});
; // Check if package.json exists and is valid; try {_; const _packageJson = JSON.parse(fs.readFileSync('package.json', _'utf8')); healthChecks.push({; name: 'Package.json'; status: 'healthy'; message: 'Package.json is valid'})} catch (error) {_; healthChecks.push({; name: 'Package.json'; status: 'unhealthy'; message: 'Package.json is invalid or missing'})};
; return {_; success: true; checks: healthChecks}} catch (error) {_; return {; success: false; error: error.message; checks: []}}};
; async checkLogHealth() {_; try {; this.log('📝 Checking log health...');
; const _logsDir = path.join(this.projectRoot, _'logs/pm2'); const _logFiles = [];
; if (fs.existsSync(logsDir)) {; const _files = fs.readdirSync(logsDir); files.forEach(file = > {; if (file.endsWith('.log')) {; const _filePath = path.join(logsDir, _file); const _stats = fs.statSync(filePath); logFiles.push({; name: file; size: stats.size; sizeMB: Math.round(stats.size / (1024 * 1024) * 100) / 100; lastModified: stats.mtime})}})};
; return {_; success: true; logFiles: logFiles; totalSize: logFiles.reduce((sum, _file) = > sum + file.size, _0)}} catch (error) {_; return {; success: false; error: error.message; logFiles: []; totalSize: 0}}};
; async generateReport(systemInfo, processInfo, appInfo, logInfo) {_; const _report = {; timestamp: new Date().toISOString(); summary: {; systemHealth: 'unknown'; processHealth: 'unknown'; applicationHealth: 'unknown'; logHealth: 'unknown'; overallHealth: 'unknown'; healthScore: 0}; details: {_; system: systemInfo; processes: processInfo; application: appInfo; logs: logInfo}; recommendations: []};
; // Calculate health scores; let _totalScore = 0; let _maxScore = 0;
; // System health; if (systemInfo.success) {_; const _memUsage = systemInfo.memory?.usagePercent || 0; const _diskUsage = systemInfo.disk?.usagePercent || 0; const _cpuUsage = systemInfo.cpu?.usagePercent || 0;
; if (memUsage < 80 && diskUsage < 80 && cpuUsage < 80) {; report.summary.systemHealth = 'healthy'; totalScore + = 25} else if (memUsage < 90 && diskUsage < 90 && cpuUsage < 90) {_; report.summary.systemHealth = 'warning'; totalScore + = 15} else {_; report.summary.systemHealth = 'unhealthy'; totalScore + = 5}}; maxScore + = 25;
; // Process health; if (processInfo.success && processInfo.health) {_; const _health = processInfo.health; if (health.errored = = = 0 && health.stopped = = = 0) {; report.summary.processHealth = 'healthy'; totalScore + = 25} else if (health.errored = = = 0) {_; report.summary.processHealth = 'warning'; totalScore + = 15} else {_; report.summary.processHealth = 'unhealthy'; totalScore + = 5}}; maxScore + = 25;
; // Application health; if (appInfo.success) {_; const _healthyChecks = appInfo.checks.filter(check = > check.status = = = 'healthy').length; const _totalChecks = appInfo.checks.length;
; if (healthyChecks = = = totalChecks) {; report.summary.applicationHealth = 'healthy'; totalScore + = 25} else if (healthyChecks > = totalChecks * 0.5) {_; report.summary.applicationHealth = 'warning'; totalScore + = 15} else {_; report.summary.applicationHealth = 'unhealthy'; totalScore + = 5}}; maxScore + = 25;
; // Log health; if (logInfo.success) {_; const _totalLogSize = logInfo.totalSize / (1024 * 1024), _// MB; if (totalLogSize < 100) {; report.summary.logHealth = 'healthy'; totalScore + = 25} else if (totalLogSize < 500) {_; report.summary.logHealth = 'warning'; totalScore + = 15} else {_; report.summary.logHealth = 'unhealthy'; totalScore + = 5}}; maxScore + = 25;
; report.summary.healthScore = Math.round((totalScore / maxScore) * 100);
; // Determine overall health; if (report.summary.healthScore > = 80) {_; report.summary.overallHealth = 'healthy'} else if (report.summary.healthScore > = 60) {_; report.summary.overallHealth = 'warning'} else {_; report.summary.overallHealth = 'unhealthy'};
; // Generate recommendations; if (systemInfo.success) {_; if (systemInfo.memory?.usagePercent > 80) {; report.recommendations.push({; priority: 'high'; message: 'High memory usage detected'; action: 'Consider restarting processes or increasing memory'})}; if (systemInfo.disk?.usagePercent > 80) {_; report.recommendations.push({; priority: 'high'; message: 'High disk usage detected'; action: 'Clean up logs and temporary files'})}};
; if (processInfo.success && processInfo.health?.errored > 0) {_; report.recommendations.push({; priority: 'critical'; message: 'Errored processes detected'; action: 'Restart errored processes and investigate logs'})};
; if (logInfo.success && logInfo.totalSize > 500 * 1024 * 1024) {_; report.recommendations.push({; priority: 'medium'; message: 'Large log files detected'; action: 'Implement log rotation or cleanup'})};
; return report};
; async saveReport(report) {_; try {; const _reportDir = path.dirname(this.reportFile); if (!fs.existsSync(reportDir)) {; fs.mkdirSync(reportDir, _{ recursive: true})};
; fs.writeFileSync(this.reportFile, JSON.stringify(report, null, 2)); this.log(`Report saved to: ${_this.reportFile}`)} catch (error) {_; this.log(`Error saving report: ${error.message}`)}};
; async run() {_; this.log('🏥 Starting Health Monitor...'); this.log(`Project root: ${this.projectRoot}`);
; try {_; // Create logs directory if it doesn't exist; const _logsDir = path.dirname(this.logFile); if (!fs.existsSync(logsDir)) {; fs.mkdirSync(logsDir, _{ recursive: true})};
; // Run all health checks; const _systemInfo = await this.checkSystemResources(); const _processInfo = await this.checkProcessHealth(); const _appInfo = await this.checkApplicationHealth(); const _logInfo = await this.checkLogHealth();
; // Generate report; this.log('📊 Generating health report...'); const _report = await this.generateReport(systemInfo, processInfo, appInfo, logInfo);
; // Save report; await this.saveReport(report);
; const _duration = Date.now() - this.startTime;
; // Log summary; this.log('\n📊 Health Monitor Summary: '); this.log(`System: ${_report.summary.systemHealth}`); this.log(`Processes: ${_report.summary.processHealth}`); this.log(`Application: ${_report.summary.applicationHealth}`); this.log(`Logs: ${_report.summary.logHealth}`); this.log(`Overall: ${_report.summary.overallHealth}`); this.log(`Health Score: ${_report.summary.healthScore}/100`); this.log(`Duration: ${_duration}ms`);
; if (report.recommendations.length > 0) {_; this.log('\n💡 Recommendations: '); report.recommendations.forEach(rec = > {; this.log(` [${rec.priority.toUpperCase()}] ${_rec.message}`); this.log(` Action: ${_rec.action}`)})} else {_; this.log('\n✨ All systems are healthy!')};
} catch (error) {_; this.log(`❌ Error running health monitor: ${error.message}`); process.exit(1)}}};
;
// Run the health monitor;
const _healthMonitor = new HealthMonitor();
healthMonitor.run().catch(error = > {_; process.exit(1)});
};
};
;
  async checkSystemResources() {_;
    try {;
      this.log('💻 Checking system resources...');
;
      const _memInfo = execSync('free -m', _{ encoding: 'utf8'});
      const _diskInfo = execSync('df -h', {_encoding: 'utf8'});
      const _cpuInfo = execSync('top -bn1 | grep "Cpu(s)"', {_encoding: 'utf8'});
;
      // Parse memory info;
      const _memLines = memInfo.split('\n');
      const _memTotal = memLines[1].split(/\s+/)[1];
      const _memUsed = memLines[1].split(/\s+/)[2];
      const _memFree = memLines[1].split(/\s+/)[3];
;
      // Parse disk info;
      const _diskLines = diskInfo.split('\n');
      const _rootDisk = diskLines.find(line => line.includes('/'));
      const _diskUsage = rootDisk ? rootDisk.split(/\s+/)[4].replace('%', '') : '0';
;
      // Parse CPU info;
      const _cpuUsage = cpuInfo.includes('id') ?;
        (100 - parseFloat(cpuInfo.split('id')[0].split(',')[3].replace('%id', '').trim())) : 0;
;
      return {_;
        success: true;
        memory: {;
          total: parseInt(memTotal);
          used: parseInt(memUsed);
          free: parseInt(memFree);
          usagePercent: Math.round((parseInt(memUsed) / parseInt(memTotal)) * 100);
        ;
        disk: {;
          usagePercent: parseInt(diskUsage);
        ;
        cpu: {;
          usagePercent: Math.round(cpuUsage);
        ;};
    } catch (error) {_;
      return {;
        success: false;
        error: error.message;
        memory: null;
        disk: null;
        cpu: null;
      ;};
};
;
  async checkProcessHealth() {_;
    try {;
      this.log('🔄 Checking process health...');
;
      const _pm2List = execSync('pm2 list --json', _{ encoding: 'utf8'});
      const _processes = JSON.parse(pm2List);
;
      const _processHealth = {_;
        total: processes.length;
        online: 0;
        stopped: 0;
        errored: 0;
        processes: [];
      ;
;
      processes.forEach(proc => {;
        const _status = proc.pm2_env?.status || 'unknown';
        processHealth.processes.push({;
          name: proc.name;
          status: status;
          memory: proc.monit?.memory || 0;
          cpu: proc.monit?.cpu || 0;
          uptime: proc.pm2_env?.uptime || 0;});
;
        if (status === 'online') processHealth.online++;
        else if (status === 'stopped') processHealth.stopped++;
        else if (status === 'errored') processHealth.errored++;
      });
;
      return {_;
        success: true;
        health: processHealth;
      ;} catch (error) {_;
      return {;
        success: false;
        error: error.message;
        health: null;
      ;};
};
;
  async checkApplicationHealth() {_;
    try {;
      this.log('🌐 Checking application health...');
;
      const _healthChecks = [];
;
      // Check if the application is running;
      try {;
        const _pm2List = execSync('pm2 list', _{ encoding: 'utf8'});
        const _hasRunningApp = pm2List.includes('bolt-zion-app') && pm2List.includes('online');
;
        healthChecks.push({_;
          name: 'PM2 App Status';
          status: hasRunningApp ? 'healthy' : 'unhealthy';
          message: hasRunningApp ? 'Application is running' : 'Application is not running';});
      } catch (error) {_;
        healthChecks.push({;
          name: 'PM2 App Status';
          status: 'error';
          message: 'Could not check PM2 status';});
      };
;
      // Check if build files exist;
      const _buildExists = fs.existsSync('dist') || fs.existsSync('.next');
      healthChecks.push({_;
        name: 'Build Files';
        status: buildExists ? 'healthy' : 'unhealthy';
        message: buildExists ? 'Build files exist' : 'Build files missing';});
;
      // Check if package.json exists and is valid;
      try {_;
        const _packageJson = JSON.parse(fs.readFileSync('package.json', _'utf8'));
        healthChecks.push({;
          name: 'Package.json';
          status: 'healthy';
          message: 'Package.json is valid';});
      } catch (error) {_;
        healthChecks.push({;
          name: 'Package.json';
          status: 'unhealthy';
          message: 'Package.json is invalid or missing';});
      };
;
      return {_;
        success: true;
        checks: healthChecks;
      ;} catch (error) {_;
      return {;
        success: false;
        error: error.message;
        checks: [];
      ;};
};
;
  async checkLogHealth() {_;
    try {;
      this.log('📝 Checking log health...');
;
      const _logsDir = path.join(this.projectRoot, _'logs/pm2');
      const _logFiles = [];
;
      if (fs.existsSync(logsDir)) {;
        const _files = fs.readdirSync(logsDir);
        files.forEach(file => {;
          if (file.endsWith('.log')) {;
            const _filePath = path.join(logsDir, _file);
            const _stats = fs.statSync(filePath);
            logFiles.push({;
              name: file;
              size: stats.size;
              sizeMB: Math.round(stats.size / (1024 * 1024) * 100) / 100;
              lastModified: stats.mtime;});
          };
        });
      };
;
      return {_;
        success: true;
        logFiles: logFiles;
        totalSize: logFiles.reduce(_(sum, _file) => sum + file.size, _0);};
    } catch (error) {_;
      return {;
        success: false;
        error: error.message;
        logFiles: [];
        totalSize: 0;
      ;};
};
;
  async generateReport(systemInfo, processInfo, appInfo, logInfo) {_;
    const _report = {;
      timestamp: new Date().toISOString();
      summary: {;
        systemHealth: 'unknown';
        processHealth: 'unknown';
        applicationHealth: 'unknown';
        logHealth: 'unknown';
        overallHealth: 'unknown';
        healthScore: 0;
      ;
      details: {;
        system: systemInfo;
        processes: processInfo;
        application: appInfo;
        logs: logInfo;
      ;
      recommendations: [];
    ;
;
    // Calculate health scores;
    let _totalScore = 0;
    let _maxScore = 0;
;
    // System health;
    if (systemInfo.success) {;
      const _memUsage = systemInfo.memory?.usagePercent || 0;
      const _diskUsage = systemInfo.disk?.usagePercent || 0;
      const _cpuUsage = systemInfo.cpu?.usagePercent || 0;
;
      if (memUsage < 80 && diskUsage < 80 && cpuUsage < 80) {;
        report.summary.systemHealth = 'healthy';
        totalScore += 25;} else if (memUsage < 90 && diskUsage < 90 && cpuUsage < 90) {_;
        report.summary.systemHealth = 'warning';
        totalScore += 15;} else {_;
        report.summary.systemHealth = 'unhealthy';
        totalScore += 5;};
};
    maxScore += 25;
;
    // Process health;
    if (processInfo.success && processInfo.health) {_;
      const _health = processInfo.health;
      if (health.errored === 0 && health.stopped === 0) {;
        report.summary.processHealth = 'healthy';
        totalScore += 25;} else if (health.errored === 0) {_;
        report.summary.processHealth = 'warning';
        totalScore += 15;} else {_;
        report.summary.processHealth = 'unhealthy';
        totalScore += 5;};
};
    maxScore += 25;
;
    // Application health;
    if (appInfo.success) {_;
      const _healthyChecks = appInfo.checks.filter(check => check.status === 'healthy').length;
      const _totalChecks = appInfo.checks.length;
;
      if (healthyChecks === totalChecks) {;
        report.summary.applicationHealth = 'healthy';
        totalScore += 25;} else if (healthyChecks >= totalChecks * 0.5) {_;
        report.summary.applicationHealth = 'warning';
        totalScore += 15;} else {_;
        report.summary.applicationHealth = 'unhealthy';
        totalScore += 5;};
};
    maxScore += 25;
;
    // Log health;
    if (logInfo.success) {_;
      const _totalLogSize = logInfo.totalSize / (1024 * 1024), _// MB;
      if (totalLogSize < 100) {;
        report.summary.logHealth = 'healthy';
        totalScore += 25;} else if (totalLogSize < 500) {_;
        report.summary.logHealth = 'warning';
        totalScore += 15;} else {_;
        report.summary.logHealth = 'unhealthy';
        totalScore += 5;};
};
    maxScore += 25;
;
    report.summary.healthScore = Math.round((totalScore / maxScore) * 100);
;
    // Determine overall health;
    if (report.summary.healthScore >= 80) {_;
      report.summary.overallHealth = 'healthy';} else if (report.summary.healthScore >= 60) {_;
      report.summary.overallHealth = 'warning';} else {_;
      report.summary.overallHealth = 'unhealthy';};
;
    // Generate recommendations;
    if (systemInfo.success) {_;
      if (systemInfo.memory?.usagePercent > 80) {;
        report.recommendations.push({;
          priority: 'high';
          message: 'High memory usage detected';
          action: 'Consider restarting processes or increasing memory';});
      };
      if (systemInfo.disk?.usagePercent > 80) {_;
        report.recommendations.push({;
          priority: 'high';
          message: 'High disk usage detected';
          action: 'Clean up logs and temporary files';});
      };
};
;
    if (processInfo.success && processInfo.health?.errored > 0) {_;
      report.recommendations.push({;
        priority: 'critical';
        message: 'Errored processes detected';
        action: 'Restart errored processes and investigate logs';});
};
;
    if (logInfo.success && logInfo.totalSize > 500 * 1024 * 1024) {_;
      report.recommendations.push({;
        priority: 'medium';
        message: 'Large log files detected';
        action: 'Implement log rotation or cleanup';});
};
;
    return report;
};
;
  async saveReport(report) {_;
    try {;
      const _reportDir = path.dirname(this.reportFile);
      if (!fs.existsSync(reportDir)) {;
        fs.mkdirSync(reportDir, _{ recursive: true});
      };
;
      fs.writeFileSync(this.reportFile, JSON.stringify(report, null, 2));
      this.log(`Report saved to: ${_this.reportFile}`);
    } catch (error) {_;
      this.log(`Error saving report: ${error.message}`);
};
};
;
  async run() {_;
    this.log('🏥 Starting Health Monitor...');
    this.log(`Project root: ${this.projectRoot}`);
;
    try {_;
      // Create logs directory if it doesn't exist;
      const _logsDir = path.dirname(this.logFile);
      if (!fs.existsSync(logsDir)) {;
        fs.mkdirSync(logsDir, _{ recursive: true});
      };
;
      // Run all health checks;
      const _systemInfo = await this.checkSystemResources();
      const _processInfo = await this.checkProcessHealth();
      const _appInfo = await this.checkApplicationHealth();
      const _logInfo = await this.checkLogHealth();
;
      // Generate report;
      this.log('📊 Generating health report...');
      const _report = await this.generateReport(systemInfo, processInfo, appInfo, logInfo);
;
      // Save report;
      await this.saveReport(report);
;
      const _duration = Date.now() - this.startTime;
;
      // Log summary;
      this.log('\n📊 Health Monitor Summary: ');
      this.log(`System: ${_report.summary.systemHealth}`);
      this.log(`Processes: ${_report.summary.processHealth}`);
      this.log(`Application: ${_report.summary.applicationHealth}`);
      this.log(`Logs: ${_report.summary.logHealth}`);
      this.log(`Overall: ${_report.summary.overallHealth}`);
      this.log(`Health Score: ${_report.summary.healthScore}/100`);
      this.log(`Duration: ${_duration}ms`);
;
      if (report.recommendations.length > 0) {_;
        this.log('\n💡 Recommendations: ');
        report.recommendations.forEach(rec => {;
          this.log(`  [${rec.priority.toUpperCase()}] ${_rec.message}`);
          this.log(`    Action: ${_rec.action}`);
        });
      } else {_;
        this.log('\n✨ All systems are healthy!');};
;
    } catch (error) {_;
      this.log(`❌ Error running health monitor: ${error.message}`);
      process.exit(1);
};
};
};
;
// Run the health monitor;
const _healthMonitor = new HealthMonitor();
healthMonitor.run().catch(error => {_;
  process.exit(1);});
