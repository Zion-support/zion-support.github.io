/usr/bin/
class SEOAccessibility { constructor () { this.project_root = process.cwd (); this.log_file = path.join (this.project_root, 'logs / pm2 / seo - accessibility.log'); this.report_file = path.join (this.project_root, 'logs / pm2 /
    this.logFile = path.join(this.projectRoot, 'logs/pm2/
    this.reportFile = path.join(this.projectRoot, 'logs/pm2/
class SEOAccessibility {constructor() {; this.projectRoot = process.cwd(); this.logFile = path.join(this.projectRoot, 'logs/pm2/seo-accessibility.log'); this.reportFile = path.join(this.projectRoot, 'logs/pm2/