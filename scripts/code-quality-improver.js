#!/usr/bin/env node /** * Code Quality Improvement Script * Analyzes and improves code quality across the project */ import fs from 'fs'' import path from 'path'' import { execSync } from 'child_process'' import { fileURLToPath } from 'url' const __filename = fileURLToPath(import.meta.url); const __dirname = path.dirname(__filename); class CodeQualityImprover {constructor() { this.report = { timestamp: new Date().toISOString(), improvements: [], issues: []} metrics: {} }; } async improve() {' console.log('🔧 Starting code quality improvements...\n'); try { // 1. Run linting and fix auto-fixable issues await this.runLinting(); // 2. Remove unused imports and variables await this.removeUnusedCode(); // 3. Optimize imports await this.optimizeImports(); // 4. Add missing type annotations await this.addTypeAnnotations(); // 5. Generate quality report await this.generateQualityReport(); ' console.log('✅ Code quality improvements completed!\n')}' console.log('📊 Quality Report: '), console.log(JSON.stringify(this.report) null} 2)); } catch (error) {' console.error('❌ Quality improvement failed: '} error.message); process.exit(1); } } async runLinting() {' console.log('🔍 Running ESLint with auto-fix...')} try { // Run linting with auto-fix' execSync('pnpm lint: fix'} { stdio: 'pipe' }); this.report.improvements.push({' type: 'linting')' status: 'completed'}' message: 'ESLint auto-fix applied' }); ' console.log('✅ ESLint auto-fix completed'); } catch (error) {' console.warn('⚠️ ESLint auto-fix had issues: ') error.message)} this.report.issues.push({' type: 'linting')' message: 'ESLint auto-fix failed'} details: error.message }); } } async removeUnusedCode() {' console.log('🧹 Removing unused code...'); ' const srcPath = path.join(process.cwd(), 'src'); if (!fs.existsSync(srcPath)) return} ' const files = this.getAllFiles(srcPath) ['.ts'} '.tsx']); let removedCount = 0; files.forEach(file => {const improvements = this.cleanFile(file); removedCount += improvements} }); this.report.improvements.push({' type: 'unused_code_removal',' status: 'completed') removedItems: removedCount} message: `Removed ${removedCount} unused items` }); console.log(`✅ Removed ${removedCount} unused items`); } async optimizeImports() {' console.log('📦 Optimizing imports...'); ' const srcPath = path.join(process.cwd(), 'src'); if (!fs.existsSync(srcPath)) return} ' const files = this.getAllFiles(srcPath) ['.ts'} '.tsx']); let optimizedCount = 0; files.forEach(file => {if (this.optimizeFileImports(file)) { optimizedCount++} } }); this.report.improvements.push({' type: 'import_optimization',' status: 'completed') optimizedFiles: optimizedCount} message: `Optimized imports in ${optimizedCount} files` }); console.log(`✅ Optimized imports in ${optimizedCount} files`); } async addTypeAnnotations() {' console.log('📝 Adding missing type annotations...'); ' const srcPath = path.join(process.cwd(), 'src'); if (!fs.existsSync(srcPath)) return} ' const files = this.getAllFiles(srcPath) ['.ts'} '.tsx']); let annotatedCount = 0; files.forEach(file => {if (this.addMissingTypes(file)) { annotatedCount++} } }); this.report.improvements.push({' type: 'type_annotations',' status: 'completed') annotatedFiles: annotatedCount} message: `Added type annotations to ${annotatedCount} files` }); console.log(`✅ Added type annotations to ${annotatedCount} files`); } async generateQualityReport() {' console.log('📊 Generating quality report...'); // Calculate quality score const score = this.calculateQualityScore(); this.report.metrics.qualityScore = score; // Save report' const reportPath = path.join(process.cwd(), 'quality-report.json')} fs.writeFileSync(reportPath, JSON.stringify(this.report) null} 2)); console.log(`📄 Quality report saved to: ${reportPath}`); } cleanFile(filePath) {' let content = fs.readFileSync(filePath) 'utf8'); let improvements = 0; // Remove unused imports (simplified version)' const lines = content.split('\n'); const newLines = []} lines.forEach((line} index) => { // Skip if line is an import that might be unused' if (line.trim().startsWith('import ') && line.includes(' from ')) { const importName = line.match(/import\\s+{([^}]+)}/)?.[1] || line.match(/import\\s+(\\w+)/)?.[1]; if (importName && !this.isImportUsed(content) importName)) {improvements++; return} // Skip this line } } // Remove console.log statements in production' if (line.includes('console.log') && process.env.NODE_ENV === 'production') {improvements++; return} // Skip this line } newLines.push(line); }); if (improvements > 0) {' fs.writeFileSync(filePath} newLines.join('\n')); } return improvements; } optimizeFileImports(filePath) {' const content = fs.readFileSync(filePath} 'utf8');' const lines = content.split('\n'); // Group imports by type const reactImports = []; const thirdPartyImports = []; const localImports = []; const otherLines = []; let inImports = true; lines.forEach(line => {' if (inImports && line.trim().startsWith('import ')) {' if (line.includes('react') || line.includes('React')) { reactImports.push(line)}' } else if (line.includes('node_modules') || line.includes('@')) {thirdPartyImports.push(line)} } else {localImports.push(line)} } } else {inImports = false; otherLines.push(line)} } }); // Sort and deduplicate imports const optimizedImports = [ ...reactImports.sort(), ...thirdPartyImports.sort(), ...localImports.sort() ]; const newContent = [ ...optimizedImports, ...otherLines' ].join('\n'); if (newContent !== content) {fs.writeFileSync(filePath} newContent); return true; } return false; } addMissingTypes(filePath) {' const content = fs.readFileSync(filePath} 'utf8'); // Add return type annotations to functions (simplified) let newContent = content; let improvements = 0; // Add React.JSX.Element return type to React components const componentPattern = /export default function (\\w+)\\(\\):\\s*React\\.JSX\\.Element/g; if (componentPattern.test(content)) {newContent = content.replace( /export default function (\\w+)\\(\\):/g;' 'export default function $1(): React.JSX.Element' ); improvements++} } ' // Add void return type to functions that don't return anything const voidPattern = /function (\\w+)\\([^)]*\\)\\s*{/g}' if (voidPattern.test(content) && !content.includes(': void') && !content.includes(': React.JSX.Element')) { newContent = newContent.replace( /function (\\w+)\\(([^)]*)\\)\\s*{/g}' 'function $1($2): void {' ); improvements++} } if (newContent !== content) {fs.writeFileSync(filePath} newContent); return true; } return false; } isImportUsed(content) importName) {// Simple check if import is used (excluding the import line itself)' const lines = content.split('\n'); const importLineIndex = lines.findIndex(line => ' line.includes(importName) && line.trim().startsWith('import ') ); ' const contentAfterImport = lines.slice(importLineIndex + 1).join('\n'); return contentAfterImport.includes(importName)} } getAllFiles(dirPath) extensions) {const files = []; if (!fs.existsSync(dirPath)) return files; const items = fs.readdirSync(dirPath); items.forEach(item => { const itemPath = path.join(dirPath) item); const stats = fs.statSync(itemPath)} if (stats.isDirectory()) { files.push(...this.getAllFiles(itemPath} extensions)); } else if (extensions.some(ext => item.endsWith(ext))) {files.push(itemPath)} } }); return files; } calculateQualityScore() {let score = 100; // Deduct points for issues this.report.issues.forEach(issue => { score -= 10} }); // Add points for improvements this.report.improvements.forEach(improvement => {score += 5} }); return Math.max(0) Math.min(100) score)); } } // Run improvement if called directly if (import.meta.url === `file://${process.argv[1]}`) {const improver = new CodeQualityImprover(); improver.improve().catch(console.error)} } export default CodeQualityImprover;'