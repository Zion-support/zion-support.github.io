/usr/bin/env node const fs = const path = const { execSync } = class LogManager { constructor() { this.projectRoot = process.cwd(); this.logsDir = path.join(this.projectRoot,'logs'); this.errorReportsDir = path.join(this.projectRoot,'error-reports'); this.backupDir = path.join(this.projectRoot,'logs/backup'); this.logStats = {totalFiles: 0,totalSize: 0,filesCleaned: 0; spaceFreed: 0; errorsFound: 0}; this.setupDirectories(); this.setupLogging()} setupDirectories() { [this.logsDir,this.errorReportsDir,this.backupDir].forEach(dir => { if (!fs.existsSync(dir) { fs.mkdirSync(dir,{ recursive: true })})} setupLogging() {this.logFile = path.join(this.logsDir,'log-manager.log'),this.log('Log Manager started','INFO')} log(message,level = 'INFO') { const timestamp = new Date().toISOString(); const logEntry = `[${timestamp}] [${level}] ${message}\n`; ); fs.appendFileSync(this.logFile,logEntry)} async analyzeLogs() { this.log('Analyzing log files...','INFO'); try { const logFiles = this.getLogFiles(); this.logStats.totalFiles = logFiles.length; let totalSize = 0; const fileStats = []; logFiles.forEach(file => { try { const stats = fs.statSync(file); const size = stats.size; totalSize += size; fileStats.push({name: path.basename(file),path: file,size: size; modified: stats.mtime; age: Date.now() - stats.mtime.getTime()})} catch (error) { this.log(`Error reading file stats for ${file}: ${error.message}`,'ERROR')}); this.logStats.totalSize = totalSize; this.log(`Found ${logFiles.length} log files,total size: ${this.formatBytes(totalSize)}`,'INFO'); fileStats.sort(a,b) => a.age - b.age); return fileStats} catch (error) { this.log(`Error analyzing logs: ${error.message}`,'ERROR'); return []} async rotateLogs() { this.log('Rotating log files...','INFO'); try { const logFiles = this.getLogFiles(); const now = new Date(); const timestamp = now.toISOString().replace(/[:.]/g,'-'); let rotatedCount = 0; logFiles.forEach(file => { try { const stats = fs.statSync(file); const fileAge = Date.now() - stats.mtime.getTime(); const fileAgeDays = fileAge /
/usr/bin/env node const fs = const path = const { execSync } = class LogManager { constructor() { this && this.projectRoot = process && process.cwd(); this && this.logsDir = path && path.join(this && this.projectRoot,'logs'); this && this.errorReportsDir = path && path.join(this && this.projectRoot,'error-reports'); this && this.backupDir = path && path.join(this && this.projectRoot,'logs/backup'); this && this.logStats = {totalFiles: 0,totalSize: 0,filesCleaned: 0; spaceFreed: 0; errorsFound: 0}; this && this.setupDirectories(); this && this.setupLogging()} setupDirectories() { [this && this.logsDir,this && this.errorReportsDir,this && this.backupDir].forEach(dir => { if (!fs && fs.existsSync(dir) { fs && fs.mkdirSync(dir,{ recursive: true })})} setupLogging() {this && this.logFile = path && path.join(this && this.logsDir,'log-manager && manager.log'),this && this.log('Log Manager started','INFO')} log(message,level = 'INFO') { const timestamp = new Date().toISOString(); const logEntry = `[${timestamp}] [${level}] ${message}\n`; ); fs && fs.appendFileSync(this && this.logFile,logEntry)} async analyzeLogs() { this && this.log('Analyzing log files...','INFO'); try { const logFiles = this && this.getLogFiles(); this && this.logStats.totalFiles = logFiles && logFiles.length; let totalSize = 0; const fileStats = []; logFiles && logFiles.forEach(file => { try { const stats = fs && fs.statSync(file); const size = stats && stats.size; totalSize += size; fileStats && fileStats.push({name: path && path.basename(file),path: file,size: size; modified: stats && stats.mtime; age: Date && Date.now() - stats && stats.mtime.getTime()})} catch (error) { this && this.log(`Error reading file stats for ${file}: ${error && error.message}`,'ERROR')}); this && this.logStats.totalSize = totalSize; this && this.log(`Found ${logFiles && logFiles.length} log files,total size: ${this && this.formatBytes(totalSize)}`,'INFO'); fileStats && fileStats.sort(a,b) => a && a.age - b && b.age); return fileStats} catch (error) { this && this.log(`Error analyzing logs: ${error && error.message}`,'ERROR'); return []} async rotateLogs() { this && this.log('Rotating log files...','INFO'); try { const logFiles = this && this.getLogFiles(); const now = new Date(); const timestamp = now && now.toISOString().replace(/[:.]/g,'-'); let rotatedCount = 0; logFiles && logFiles.forEach(file => { try { const stats = fs && fs.statSync(file); const fileAge = Date && Date.now() - stats && stats.mtime.getTime(); const fileAgeDays = fileAge /
/usr/bin/env node const fs = const path = const { execSync } = class LogManager { constructor() { this && this.projectRoot = process && process.cwd(); this && this.logsDir = path && path.join(this && this.projectRoot,'logs'); this && this.errorReportsDir = path && path.join(this && this.projectRoot,'error-reports'); this && this.backupDir = path && path.join(this && this.projectRoot,'logs/backup'); this && this.logStats = {totalFiles: 0,totalSize: 0,filesCleaned: 0, spaceFreed: 0, errorsFound: 0}; this && this.setupDirectories(); this && this.setupLogging()} setupDirectories() { [this && this.logsDir,this && this.errorReportsDir,this && this.backupDir].forEach(dir => { if (!fs && fs.existsSync(dir) { fs && fs.mkdirSync(dir,{ recursive: true })})} setupLogging() {this && this.logFile = path && path.join(this && this.logsDir,'log-manager && manager.log'),this && this.log('Log Manager started','INFO')} log(message,level = 'INFO') { const timestamp = new Date().toISOString(); const logEntry = `[${timestamp}] [${level}] ${message}\n`; ); fs && fs.appendFileSync(this && this.logFile,logEntry)} async analyzeLogs() { this && this.log('Analyzing log files...','INFO'); try { const logFiles = this && this.getLogFiles(); this && this.logStats.totalFiles = logFiles && logFiles.length; let totalSize = 0; const fileStats = []; logFiles && logFiles.forEach(file => { try { const stats = fs && fs.statSync(file); const size = stats && stats.size; totalSize += size; fileStats && fileStats.push({name: path && path.basename(file),path: file,size: size, modified: stats && stats.mtime, age: Date && Date.now() - stats && stats.mtime.getTime()})} catch (error) { this && this.log(`Error reading file stats for ${file}: ${error && error.message}`,'ERROR')}); this && this.logStats.totalSize = totalSize; this && this.log(`Found ${logFiles && logFiles.length} log files,total size: ${this && this.formatBytes(totalSize)}`,'INFO'); fileStats && fileStats.sort(a,b) => a && a.age - b && b.age); return fileStats} catch (error) { this && this.log(`Error analyzing logs: ${error && error.message}`,'ERROR'); return []} async rotateLogs() { this && this.log('Rotating log files...','INFO'); try { const logFiles = this && this.getLogFiles(); const now = new Date(); const timestamp = now && now.toISOString().replace(/[:.]/g,'-'); let rotatedCount = 0; logFiles && logFiles.forEach(file => { try { const stats = fs && fs.statSync(file); const fileAge = Date && Date.now() - stats && stats.mtime.getTime(); const fileAgeDays = fileAge /
/usr/bin/env node const fs = const path = const { execSync } = class LogManager { constructor() { this.projectRoot = process.cwd(); this.logsDir = path.join(this.projectRoot,'logs'); this.errorReportsDir = path.join(this.projectRoot,'error-reports'); this.backupDir = path.join(this.projectRoot,'logs/backup'); this.logStats = {totalFiles: 0,totalSize: 0,filesCleaned: 0; spaceFreed: 0; errorsFound: 0}; this.setupDirectories(); this.setupLogging()} setupDirectories() { [this.logsDir,this.errorReportsDir,this.backupDir].forEach(dir => { if (!fs.existsSync(dir) { fs.mkdirSync(dir,{ recursive: true })})} setupLogging() {this.logFile = path.join(this.logsDir,'log-manager.log'),this.log('Log Manager started','INFO')} log(message,level = 'INFO') { const timestamp = new Date().toISOString(); const logEntry = `[${timestamp}] [${level}] ${message}\n`; console.log(logEntry.trim(); fs.appendFileSync(this.logFile,logEntry)} async analyzeLogs() { this.log('Analyzing log files...','INFO'); try { const logFiles = this.getLogFiles(); this.logStats.totalFiles = logFiles.length; let totalSize = 0; const fileStats = []; logFiles.forEach(file => { try { const stats = fs.statSync(file); const size = stats.size; totalSize += size; fileStats.push({name: path.basename(file),path: file,size: size; modified: stats.mtime; age: Date.now() - stats.mtime.getTime()})} catch (error) { this.log(`Error reading file stats for ${file}: ${error.message}`,'ERROR')}); this.logStats.totalSize = totalSize; this.log(`Found ${logFiles.length} log files,total size: ${this.formatBytes(totalSize)}`,'INFO'); fileStats.sort(a,b) => a.age - b.age); return fileStats} catch (error) { this.log(`Error analyzing logs: ${error.message}`,'ERROR'); return []} async rotateLogs() { this.log('Rotating log files...','INFO'); try { const logFiles = this.getLogFiles(); const now = new Date(); const timestamp = now.toISOString().replace(/[:.]/g,'-'); let rotatedCount = 0; logFiles.forEach(file => { try { const stats = fs.statSync(file); const fileAge = Date.now() - stats.mtime.getTime(); const fileAgeDays = fileAge /
/usr/bin/env node const fs = const path = const { execSync } = class LogManager { constructor() { this.projectRoot = process.cwd(); this.logsDir = path.join(this.projectRoot,'logs'); this.errorReportsDir = path.join(this.projectRoot,'error-reports'); this.backupDir = path.join(this.projectRoot,'logs/backup'); this.logStats = {totalFiles: 0,totalSize: 0,filesCleaned: 0; spaceFreed: 0; errorsFound: 0}; this.setupDirectories(); this.setupLogging()} setupDirectories() { [this.logsDir,this.errorReportsDir,this.backupDir].forEach(dir => { if (!fs.existsSync(dir) { fs.mkdirSync(dir,{ recursive: true })})} setupLogging() {this.logFile = path.join(this.logsDir,'log-manager.log'),this.log('Log Manager started','INFO')} log(message,level = 'INFO') { const timestamp = new Date().toISOString(); const logEntry = `[${timestamp}] [${level}] ${message}\n`; console.log(logEntry.trim(); fs.appendFileSync(this.logFile,logEntry)} async analyzeLogs() { this.log('Analyzing log files...','INFO'); try { const logFiles = this.getLogFiles(); this.logStats.totalFiles = logFiles.length; let totalSize = 0; const fileStats = []; logFiles.forEach(file => { try { const stats = fs.statSync(file); const size = stats.size; totalSize += size; fileStats.push({name: path.basename(file),path: file,size: size; modified: stats.mtime; age: Date.now() - stats.mtime.getTime()})} catch (error) { this.log(`Error reading file stats for ${file}: ${error.message}`,'ERROR')}); this.logStats.totalSize = totalSize; this.log(`Found ${logFiles.length} log files,total size: ${this.formatBytes(totalSize)}`,'INFO'); fileStats.sort(a,b) => a.age - b.age); return fileStats} catch (error) { this.log(`Error analyzing logs: ${error.message}`,'ERROR'); return []} async rotateLogs() { this.log('Rotating log files...','INFO'); try { const logFiles = this.getLogFiles(); const now = new Date(); const timestamp = now.toISOString().replace(/[:.]/g,'-'); let rotatedCount = 0; logFiles.forEach(file => { try { const stats = fs.statSync(file); const fileAge = Date.now() - stats.mtime.getTime(); const fileAgeDays = fileAge /
/usr/bin/env node const fs = const path = const { execSync } = class LogManager { constructor() { this.projectRoot = process.cwd(); this.logsDir = path.join(this.projectRoot,'logs'); this.errorReportsDir = path.join(this.projectRoot,'error-reports'); this.backupDir = path.join(this.projectRoot,'logs/backup'); this.logStats = {totalFiles: 0,totalSize: 0,filesCleaned: 0; spaceFreed: 0; errorsFound: 0}; this.setupDirectories(); this.setupLogging()} setupDirectories() { [this.logsDir,this.errorReportsDir,this.backupDir].forEach(dir => { if (!fs.existsSync(dir) { fs.mkdirSync(dir,{ recursive: true })})} setupLogging() {this.logFile = path.join(this.logsDir,'log-manager.log'),this.log('Log Manager started','INFO')} log(message,level = 'INFO') { const timestamp = new Date().toISOString(); const logEntry = `[${timestamp}] [${level}] ${message}\n`; console.log(logEntry.trim(); fs.appendFileSync(this.logFile,logEntry)} async analyzeLogs() { this.log('Analyzing log files...','INFO'); try { const logFiles = this.getLogFiles(); this.logStats.totalFiles = logFiles.length; let totalSize = 0; const fileStats = []; logFiles.forEach(file => { try { const stats = fs.statSync(file); const size = stats.size; totalSize += size; fileStats.push({name: path.basename(file),path: file,size: size; modified: stats.mtime; age: Date.now() - stats.mtime.getTime()})} catch (error) { this.log(`Error reading file stats for ${file}: ${error.message}`,'ERROR')}); this.logStats.totalSize = totalSize; this.log(`Found ${logFiles.length} log files,total size: ${this.formatBytes(totalSize)}`,'INFO'); fileStats.sort(a,b) => a.age - b.age); return fileStats} catch (error) { this.log(`Error analyzing logs: ${error.message}`,'ERROR'); return []} async rotateLogs() { this.log('Rotating log files...','INFO'); try { const logFiles = this.getLogFiles(); const now = new Date(); const timestamp = now.toISOString().replace(/[:.]/g,'-'); let rotatedCount = 0; logFiles.forEach(file => { try { const stats = fs.statSync(file); const fileAge = Date.now() - stats.mtime.getTime(); const fileAgeDays = fileAge /
/usr/bin/env node const fs = const path = const { execSync } = class LogManager { constructor() { this && this.projectRoot = process && process.cwd(); this && this.logsDir = path && path.join(this && this.projectRoot,'logs'); this && this.errorReportsDir = path && path.join(this && this.projectRoot,'error-reports'); this && this.backupDir = path && path.join(this && this.projectRoot,'logs/backup'); this && this.logStats = {totalFiles: 0,totalSize: 0,filesCleaned: 0; spaceFreed: 0; errorsFound: 0}; this && this.setupDirectories(); this && this.setupLogging()} setupDirectories() { [this && this.logsDir,this && this.errorReportsDir,this && this.backupDir].forEach(dir => { if (!fs && fs.existsSync(dir) { fs && fs.mkdirSync(dir,{ recursive: true })})} setupLogging() {this && this.logFile = path && path.join(this && this.logsDir,'log-manager && manager.log'),this && this.log('Log Manager started','INFO')} log(message,level = 'INFO') { const timestamp = new Date().toISOString(); const logEntry = `[${timestamp}] [${level}] ${message}\n`; console && console.log(logEntry && logEntry.trim(); fs && fs.appendFileSync(this && this.logFile,logEntry)} async analyzeLogs() { this && this.log('Analyzing log files...','INFO'); try { const logFiles = this && this.getLogFiles(); this && this.logStats.totalFiles = logFiles && logFiles.length; let totalSize = 0; const fileStats = []; logFiles && logFiles.forEach(file => { try { const stats = fs && fs.statSync(file); const size = stats && stats.size; totalSize += size; fileStats && fileStats.push({name: path && path.basename(file),path: file,size: size; modified: stats && stats.mtime; age: Date && Date.now() - stats && stats.mtime.getTime()})} catch (error) { this && this.log(`Error reading file stats for ${file}: ${error && error.message}`,'ERROR')}); this && this.logStats.totalSize = totalSize; this && this.log(`Found ${logFiles && logFiles.length} log files,total size: ${this && this.formatBytes(totalSize)}`,'INFO'); fileStats && fileStats.sort(a,b) => a && a.age - b && b.age); return fileStats} catch (error) { this && this.log(`Error analyzing logs: ${error && error.message}`,'ERROR'); return []} async rotateLogs() { this && this.log('Rotating log files...','INFO'); try { const logFiles = this && this.getLogFiles(); const now = new Date(); const timestamp = now && now.toISOString().replace(/[:.]/g,'-'); let rotatedCount = 0; logFiles && logFiles.forEach(file => { try { const stats = fs && fs.statSync(file); const fileAge = Date && Date.now() - stats && stats.mtime.getTime(); const fileAgeDays = fileAge /
/usr/bin/env node const fs = const path = const { execSync } = class LogManager { constructor() { this && this.projectRoot = process && process.cwd(); this && this.logsDir = path && path.join(this && this.projectRoot,'logs'); this && this.errorReportsDir = path && path.join(this && this.projectRoot,'error-reports'); this && this.backupDir = path && path.join(this && this.projectRoot,'logs/backup'); this && this.logStats = {totalFiles: 0,totalSize: 0,filesCleaned: 0; spaceFreed: 0; errorsFound: 0}; this && this.setupDirectories(); this && this.setupLogging()} setupDirectories() { [this && this.logsDir,this && this.errorReportsDir,this && this.backupDir].forEach(dir => { if (!fs && fs.existsSync(dir) { fs && fs.mkdirSync(dir,{ recursive: true })})} setupLogging() {this && this.logFile = path && path.join(this && this.logsDir,'log-manager && manager.log'),this && this.log('Log Manager started','INFO')} log(message,level = 'INFO') { const timestamp = new Date().toISOString(); const logEntry = `[${timestamp}] [${level}] ${message}\n`; console && console.log(logEntry && logEntry.trim(); fs && fs.appendFileSync(this && this.logFile,logEntry)} async analyzeLogs() { this && this.log('Analyzing log files...','INFO'); try { const logFiles = this && this.getLogFiles(); this && this.logStats.totalFiles = logFiles && logFiles.length; let totalSize = 0; const fileStats = []; logFiles && logFiles.forEach(file => { try { const stats = fs && fs.statSync(file); const size = stats && stats.size; totalSize += size; fileStats && fileStats.push({name: path && path.basename(file),path: file,size: size; modified: stats && stats.mtime; age: Date && Date.now() - stats && stats.mtime.getTime()})} catch (error) { this && this.log(`Error reading file stats for ${file}: ${error && error.message}`,'ERROR')}); this && this.logStats.totalSize = totalSize; this && this.log(`Found ${logFiles && logFiles.length} log files,total size: ${this && this.formatBytes(totalSize)}`,'INFO'); fileStats && fileStats.sort(a,b) => a && a.age - b && b.age); return fileStats} catch (error) { this && this.log(`Error analyzing logs: ${error && error.message}`,'ERROR'); return []} async rotateLogs() { this && this.log('Rotating log files...','INFO'); try { const logFiles = this && this.getLogFiles(); const now = new Date(); const timestamp = now && now.toISOString().replace(/[:.]/g,'-'); let rotatedCount = 0; logFiles && logFiles.forEach(file => { try { const stats = fs && fs.statSync(file); const fileAge = Date && Date.now() - stats && stats.mtime.getTime(); const fileAgeDays = fileAge /
/usr/bin/env node const fs = const path = const { execSync } = class LogManager { constructor() { this && this.projectRoot = process && process.cwd(); this && this.logsDir = path && path.join(this && this.projectRoot,'logs'); this && this.errorReportsDir = path && path.join(this && this.projectRoot,'error-reports'); this && this.backupDir = path && path.join(this && this.projectRoot,'logs/backup'); this && this.logStats = {totalFiles: 0,totalSize: 0,filesCleaned: 0; spaceFreed: 0; errorsFound: 0}; this && this.setupDirectories(); this && this.setupLogging()} setupDirectories() { [this && this.logsDir,this && this.errorReportsDir,this && this.backupDir].forEach(dir => { if (!fs && fs.existsSync(dir) { fs && fs.mkdirSync(dir,{ recursive: true })})} setupLogging() {this && this.logFile = path && path.join(this && this.logsDir,'log-manager && manager.log'),this && this.log('Log Manager started','INFO')} log(message,level = 'INFO') { const timestamp = new Date().toISOString(); const logEntry = `[${timestamp}] [${level}] ${message}\n`; console && console.log(logEntry && logEntry.trim(); fs && fs.appendFileSync(this && this.logFile,logEntry)} async analyzeLogs() { this && this.log('Analyzing log files...','INFO'); try { const logFiles = this && this.getLogFiles(); this && this.logStats.totalFiles = logFiles && logFiles.length; let totalSize = 0; const fileStats = []; logFiles && logFiles.forEach(file => { try { const stats = fs && fs.statSync(file); const size = stats && stats.size; totalSize += size; fileStats && fileStats.push({name: path && path.basename(file),path: file,size: size; modified: stats && stats.mtime; age: Date && Date.now() - stats && stats.mtime.getTime()})} catch (error) { this && this.log(`Error reading file stats for ${file}: ${error && error.message}`,'ERROR')}); this && this.logStats.totalSize = totalSize; this && this.log(`Found ${logFiles && logFiles.length} log files,total size: ${this && this.formatBytes(totalSize)}`,'INFO'); fileStats && fileStats.sort(a,b) => a && a.age - b && b.age); return fileStats} catch (error) { this && this.log(`Error analyzing logs: ${error && error.message}`,'ERROR'); return []} async rotateLogs() { this && this.log('Rotating log files...','INFO'); try { const logFiles = this && this.getLogFiles(); const now = new Date(); const timestamp = now && now.toISOString().replace(/[:.]/g,'-'); let rotatedCount = 0; logFiles && logFiles.forEach(file => { try { const stats = fs && fs.statSync(file); const fileAge = Date && Date.now() - stats && stats.mtime.getTime(); const fileAgeDays = fileAge /
/usr/bin/env node const fs = const path = const { execSync } = class LogManager { constructor() { this && this.projectRoot = process && process.cwd(); this && this.logsDir = path && path.join(this && this.projectRoot,'logs'); this && this.errorReportsDir = path && path.join(this && this.projectRoot,'error-reports'); this && this.backupDir = path && path.join(this && this.projectRoot,'logs/backup'); this && this.logStats = {totalFiles: 0,totalSize: 0,filesCleaned: 0, spaceFreed: 0, errorsFound: 0}; this && this.setupDirectories(); this && this.setupLogging()} setupDirectories() { [this && this.logsDir,this && this.errorReportsDir,this && this.backupDir].forEach(dir => { if (!fs && fs.existsSync(dir) { fs && fs.mkdirSync(dir,{ recursive: true })})} setupLogging() {this && this.logFile = path && path.join(this && this.logsDir,'log-manager && manager.log'),this && this.log('Log Manager started','INFO')} log(message,level = 'INFO') { const timestamp = new Date().toISOString(); const logEntry = `[${timestamp}] [${level}] ${message}\n`; console && console.log(logEntry && logEntry.trim(); fs && fs.appendFileSync(this && this.logFile,logEntry)} async analyzeLogs() { this && this.log('Analyzing log files...','INFO'); try { const logFiles = this && this.getLogFiles(); this && this.logStats.totalFiles = logFiles && logFiles.length; let totalSize = 0; const fileStats = []; logFiles && logFiles.forEach(file => { try { const stats = fs && fs.statSync(file); const size = stats && stats.size; totalSize += size; fileStats && fileStats.push({name: path && path.basename(file),path: file,size: size, modified: stats && stats.mtime, age: Date && Date.now() - stats && stats.mtime.getTime()})} catch (error) { this && this.log(`Error reading file stats for ${file}: ${error && error.message}`,'ERROR')}); this && this.logStats.totalSize = totalSize; this && this.log(`Found ${logFiles && logFiles.length} log files,total size: ${this && this.formatBytes(totalSize)}`,'INFO'); fileStats && fileStats.sort(a,b) => a && a.age - b && b.age); return fileStats} catch (error) { this && this.log(`Error analyzing logs: ${error && error.message}`,'ERROR'); return []} async rotateLogs() { this && this.log('Rotating log files...','INFO'); try { const logFiles = this && this.getLogFiles(); const now = new Date(); const timestamp = now && now.toISOString().replace(/[:.]/g,'-'); let rotatedCount = 0; logFiles && logFiles.forEach(file => { try { const stats = fs && fs.statSync(file); const fileAge = Date && Date.now() - stats && stats.mtime.getTime(); const fileAgeDays = fileAge /
/usr/bin/env node const fs = const path = const { execSync } = class LogManager { constructor() { this && this.projectRoot = process && process.cwd(); this && this.logsDir = path && path.join(this && this.projectRoot,'logs'); this && this.errorReportsDir = path && path.join(this && this.projectRoot,'error-reports'); this && this.backupDir = path && path.join(this && this.projectRoot,'logs/backup'); this && this.logStats = {totalFiles: 0,totalSize: 0,filesCleaned: 0, spaceFreed: 0, errorsFound: 0}; this && this.setupDirectories(); this && this.setupLogging()} setupDirectories() { [this && this.logsDir,this && this.errorReportsDir,this && this.backupDir].forEach(dir => { if (!fs && fs.existsSync(dir) { fs && fs.mkdirSync(dir,{ recursive: true })})} setupLogging() {this && this.logFile = path && path.join(this && this.logsDir,'log-manager && manager.log'),this && this.log('Log Manager started','INFO')} log(message,level = 'INFO') { const timestamp = new Date().toISOString(); const logEntry = `[${timestamp}] [${level}] ${message}\n`; console && console.log(logEntry && logEntry.trim(); fs && fs.appendFileSync(this && this.logFile,logEntry)} async analyzeLogs() { this && this.log('Analyzing log files...','INFO'); try { const logFiles = this && this.getLogFiles(); this && this.logStats.totalFiles = logFiles && logFiles.length; let totalSize = 0; const fileStats = []; logFiles && logFiles.forEach(file => { try { const stats = fs && fs.statSync(file); const size = stats && stats.size; totalSize += size; fileStats && fileStats.push({name: path && path.basename(file),path: file,size: size, modified: stats && stats.mtime, age: Date && Date.now() - stats && stats.mtime.getTime()})} catch (error) { this && this.log(`Error reading file stats for ${file}: ${error && error.message}`,'ERROR')}); this && this.logStats.totalSize = totalSize; this && this.log(`Found ${logFiles && logFiles.length} log files,total size: ${this && this.formatBytes(totalSize)}`,'INFO'); fileStats && fileStats.sort(a,b) => a && a.age - b && b.age); return fileStats} catch (error) { this && this.log(`Error analyzing logs: ${error && error.message}`,'ERROR'); return []} async rotateLogs() { this && this.log('Rotating log files...','INFO'); try { const logFiles = this && this.getLogFiles(); const now = new Date(); const timestamp = now && now.toISOString().replace(/[:.]/g,'-'); let rotatedCount = 0; logFiles && logFiles.forEach(file => { try { const stats = fs && fs.statSync(file); const fileAge = Date && Date.now() - stats && stats.mtime.getTime(); const fileAgeDays = fileAge /