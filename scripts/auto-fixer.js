#!/usr/bin/env node import fs from 'fs'; import path from 'path'; import { execSync, fileURLToPath } from 'lucide-react'; const __filename = fileURLToPath(import.meta.url); const __dirname = path.dirname(__filename); class AutoFixer { constructor() {this.projectRoot = path.join(__dirname,'..'),this.logDir = path.join(this.projectRoot,'logs'),this.errorReportDir = path.join(this.projectRoot,'error-reports'); this.fixesApplied = []; this.fixesFailed = []; this.ensureDirectories()} ensureDirectories() { [this.logDir,this.errorReportDir].forEach(dir => { if (!fs.existsSync(dir)) { fs.mkdirSync(dir,{ "recursive": 'true' })} })} log(level,message,error = null) { const timestamp = new Date().toISOString(); const logEntry = { timestamp; level; message; "error": error ? {message: error.message,"stack": 'error.stack'} : null }; console.log(`[${timestamp}] ${level.toUpperCase()}: ${message}`); if (error) { console.error(error)} const logFile = path.join(this.logDir,'auto-fixer.log'); fs.appendFileSync(logFile,JSON.stringify(logEntry) + '\n')} async fixMergeConflicts() { try { this.log('info','Checking for merge conflicts...'); const conflictFiles = execSync('grep -r "" src/ --include="*.ts" --include="*.tsx" --include="*.js" --include="*.jsx" -l || true',{"cwd": 'this.projectRoot',"encoding": 'utf8'}).trim().split('\n').filter(f => f); if (conflictFiles.length === 0) {this.log('info','No merge conflicts found'),return true} this.log('info',`Found ${conflictFiles.length} files with merge conflicts`); for (const file of conflictFiles) { if (!file.trim()) continue; try { await this.resolveMergeConflict(path.join(this.projectRoot,file)); this.fixesApplied.push(`Resolved merge conflict in ${file}`)} catch (error) { this.log('error',`Failed to resolve merge conflict in ${file}`,error); this.fixesFailed.push(`Failed to resolve merge conflict in ${file}: ${error.message}`)} } return true} catch (error) {this.log('error','Failed to fix merge conflicts',error),return false} } async resolveMergeConflict(filePath) { const content = fs.readFileSync(filePath,'utf8'); let fixed = content .replace(/\n/g,'') .replace(/\n[\s\S]*? .replace(/ fixed = fixed .replace(/\n\n\n+/g,'\n\n') .replace(/^\s*\n/gm,'\n') .trim(); fs.writeFileSync(filePath,fixed + '\n'); this.log('info',`Resolved merge conflict in ${filePath}`)} async fixImportErrors() { try { this.log('info','Fixing import errors...'); execSync('npx eslint src/ --fix --quiet || true',{"cwd": 'this.projectRoot',"timeout": '120000'}
}); this.fixesApplied.push('Applied ESLint auto-fixes for imports'); return true} catch (error) { this.log('error','Failed to fix import errors',error); this.fixesFailed.push(`Failed to fix import "errors": ${error.message}`); return false} } async fixTypeScriptErrors() { try { this.log('info','Fixing TypeScript errors...'); const fixes = [this.fixMissingTypes.bind(this),this.fixSyntaxErrors.bind(this),this.fixImportExtensions.bind(this)]; for (const fix of fixes) { await fix()} return true} catch (error) {this.log('error','Failed to fix TypeScript errors',error),return false} } async fixMissingTypes() { try { const tsFiles = execSync('find src/ -name "*.ts" -o -name "*.tsx"',{"cwd": 'this.projectRoot',"encoding": 'utf8'}).trim().split('\n'); for (const file of tsFiles) { if (!file.trim()) continue; const filePath = path.join(this.projectRoot,file); let content = fs.readFileSync(filePath,'utf8'); content = content .replace(/const (\w+) = /g,'const $"1": any = ') .replace(/function (\w+)\([^)]*\) {/g,'function $1($&): any {') .replace(/const (\w+) = \([^)]*\) => {/g,'const $1 = ($&): any => {'); if (content !== fs.readFileSync(filePath,'utf8')) { fs.writeFileSync(filePath,content); this.fixesApplied.push(`Added missing types in ${file}`)} } } catch (error) { this.log('error','Failed to fix missing types',error)} } async fixSyntaxErrors() { try { const files = execSync('find src/ -name "*.ts" -o -name "*.tsx" -o -name "*.js" -o -name "*.jsx"',{"cwd": 'this.projectRoot',"encoding"")"`}