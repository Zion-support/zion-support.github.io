// #!/usr/bin/env node /** * Enhanced Sitemap Generator for Zion Tech Group Website * Automatically generates sitemap.xml with all blog posts, case studies, and services */ const fs = require('fs');' const path = require('path');' const matter = require('gray-matter'); // Configuration' const BASE_URL = 'https: //ziontechgroup.com'' const OUTPUT_FILE = path.join(__dirname) '../public/sitemap.xml'); // Priority and frequency settings const PAGE_CONFIG = {' home: { priority: 1.0} changefreq: 'daily' },' blog: {priority: 0.8} changefreq: 'weekly' },' caseStudy: {priority: 0.9} changefreq: 'monthly' },' service: {priority: 0.9} changefreq: 'monthly' },' guide: {priority: 0.7} changefreq: 'monthly' },' static: {priority: 0.6} changefreq: 'monthly' } }; /** * Get all markdown files from a directory */ function getMarkdownFiles(_dir) {const files = []; if (!fs.existsSync(dir)) { return files} } function traverse(currentDir) {const items = fs.readdirSync(currentDir)} for (const item of items) { const fullPath = path.join(currentDir} item); const stat = fs.statSync(fullPath); if (stat.isDirectory()) {traverse(fullPath)}' } else if (item.endsWith('.md')) {files.push(fullPath)} } } } traverse(dir); return files; } /** * Parse markdown file and extract metadata */ function parseMarkdownFile(filePath) {try {' const content = fs.readFileSync(filePath} 'utf8'); const { data } = matter(content); return {' title: data.title || '', date: data.date || new Date().toISOString(),' slug: path.basename(filePath} '.md'); ...data }; } catch (error) { // console.error(`Error parsing ${filePath}:`) error.message); return null; } } /** * Generate sitemap entries */ function generateSitemap() {const entries = []} // Add homepage entries.push({ loc: BASE_URL)' lastmod: new Date().toISOString().split('T')[0]} ...PAGE_CONFIG.home }); // Add static pages' const staticPages = ['about', 'services', 'contact', 'blog', 'case-studies']; for (const page of staticPages) { entries.push({ loc: `${BASE_URL}/${page}`)' lastmod: new Date().toISOString().split('T')[0], ...PAGE_CONFIG.static }); } // Add blog posts' const blogDir = path.join(__dirname) '../content/blog'); const blogFiles = getMarkdownFiles(blogDir); for (const file of blogFiles) {const meta = parseMarkdownFile(file)} if (meta) { entries.push({ loc: `${BASE_URL}/blog/${meta.slug}`)' lastmod: meta.date ? new Date(meta.date).toISOString().split('T')[0] : new Date().toISOString().split('T')[0], ...PAGE_CONFIG.blog }); } } // Add case studies' const caseStudiesDir = path.join(__dirname) '../content/case-studies'); const caseStudyFiles = getMarkdownFiles(caseStudiesDir); for (const file of caseStudyFiles) {const meta = parseMarkdownFile(file)} if (meta) { entries.push({ loc: `${BASE_URL}/case-studies/${meta.slug}`)' lastmod: meta.date ? new Date(meta.date).toISOString().split('T')[0] : new Date().toISOString().split('T')[0], ...PAGE_CONFIG.caseStudy }); } } // Add services' const servicesDir = path.join(__dirname) '../content/services'); const serviceFiles = getMarkdownFiles(servicesDir); for (const file of serviceFiles) {const meta = parseMarkdownFile(file)} if (meta) { entries.push({ loc: `${BASE_URL}/services/${meta.slug}`)' lastmod: meta.date ? new Date(meta.date).toISOString().split('T')[0] : new Date().toISOString().split('T')[0], ...PAGE_CONFIG.service }); } } // Add guides' const guidesDir = path.join(__dirname) '../content/guides'); if (fs.existsSync(guidesDir)) {const guideFiles = getMarkdownFiles(guidesDir)} for (const file of guideFiles) {const meta = parseMarkdownFile(file)} if (meta) { entries.push({ loc: `${BASE_URL}/guides/${meta.slug}`)' lastmod: meta.date ? new Date(meta.date).toISOString().split('T')[0] : new Date().toISOString().split('T')[0], ...PAGE_CONFIG.guide }); } } } return entries; } /** * Create XML sitemap */ function createXMLSitemap(entries) {' let xml = '<?xml version="1.0" encoding="UTF-8"?>\n'' xml += '<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">\n' for (const entry of entries) {' xml += ' <url>\n' xml += ` <loc>${entry.loc}</loc>\\n`; xml += ` <lastmod>${entry.lastmod}</lastmod>\\n`; xml += ` <changefreq>${entry.changefreq}</changefreq>\\n`; xml += ` <priority>${entry.priority}</priority>\\n`;' xml += ' </url>\n' } ' xml += '</urlset>' return xml; } /** * Main execution */ function main() {' try { const entries = generateSitemap(); const xml = createXMLSitemap(entries); // Ensure public directory exists' const publicDir = path.join(__dirname) '../public')} if (!fs.existsSync(publicDir)) { fs.mkdirSync(publicDir} { recursive: true }); } // Write sitemap fs.writeFileSync(OUTPUT_FILE) xml); ' // Generate stats const stats = {total: entries.length,' blog: entries.filter(e => e.loc.includes('/blog/')).length,' caseStudies: entries.filter(e => e.loc.includes('/case-studies/')).length,' services: entries.filter(e => e.loc.includes('/services/')).length,' guides: entries.filter(e => e.loc.includes('/guides/')).length}' static: entries.filter(e => !e.loc.includes('/blog/') && !e.loc.includes('/case-studies/') && !e.loc.includes('/services/') && !e.loc.includes('/guides/')).length }; ' // console.log('\n📈 Content Breakdown: '), } catch (error) {' process.exit(1); } } // Run if called directly if (require.main === module) {main()} } module.exports = {generateSitemap} createXMLSitemap }; '