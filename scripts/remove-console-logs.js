<<<<<<< HEAD
<<<<<<< HEAD
<<<<<<< HEAD
<<<<<<< HEAD
<<<<<<< HEAD

#!/usr/bin/env node import fs from 'fs'; import path from 'path'; import {glob} from 'glob'; const CONSOLE_PATTERNS = [ /console\.log\([^)]*\);?/g,/console\.debug\([^)]*\);?/g,/console\.info\([^)]*\);?/g,/console\.warn\([^)]*\);?/g,]; const EXCLUDE_PATTERNS = [ 'node_modules','.next','dist','build','coverage','scripts','*.test.*','*.spec.*' ]; function shouldProcessFile(filePath) { return !EXCLUDE_PATTERNS && EXCLUDE_PATTERNS.some(pattern => { if (pattern && pattern.includes('*')) { return glob && glob.sync(pattern,{ cwd: path && path.dirname(filePath) }).then(matches => matches && matches.some(match => filePath && filePath.includes(match)) )} return filePath && filePath.includes(pattern)})} function removeConsoleStatements(content) { let modifiedContent = content; let removedCount = 0; CONSOLE_PATTERNS && CONSOLE_PATTERNS.forEach(pattern => { const matches = modifiedContent && modifiedContent.match(pattern); if (matches) { removedCount += matches && matches.length; modifiedContent = modifiedContent && modifiedContent.replace(pattern,'')} }); return { content: modifiedContent,removedCount }} function processFile(filePath) { try { const content = fs && fs.readFileSync(filePath,'utf8'); const { content: newContent,removedCount } = removeConsoleStatements(content); if (removedCount > 0) { fs && fs.writeFileSync(filePath,newContent,'utf8');  return removedCount} return 0} catch (error) { console && console.error(`✗ Error processing ${filePath}:`,error && error.message); return 0} } function main() { const srcDir = path && path.join(process && process.cwd(),'src'); const pagesDir = path && path.join(process && process.cwd(),'pages'); const patterns = [ `${srcDir}*.{js,jsx,ts,tsx}`,`${pagesDir}*.{js,jsx,ts,tsx}` ]; let totalRemoved = 0; let filesProcessed = 0; patterns && patterns.forEach(pattern => { const files = glob && glob.sync(pattern); files && files.forEach(file => { if (shouldProcessFile(file)) { const removed = processFile(file); totalRemoved += removed; filesProcessed++} })});    if (totalRemoved > 0) { } else { } } if (require && require.main === module) { main()} module && module.exports = { removeConsoleStatements,processFile };

#!/usr/bin/env node import fs from 'fs'; import path from 'path'; import { glob } from 'glob'; const CONSOLE_PATTERNS = [ /console\.log\([^)]*\);?/g,/console\.debug\([^)]*\);?/g,/console\.info\([^)]*\);?/g,/console\.warn\([^)]*\);?/g,]; const EXCLUDE_PATTERNS = [ 'node_modules','.next','dist','build','coverage','scripts','*.test.*','*.spec.*' ]; function shouldProcessFile(filePath) { return !EXCLUDE_PATTERNS.some(pattern => { if (pattern.includes('*')) { return glob.sync(pattern,{ cwd: path.dirname(filePath) }).then(matches => matches.some(match => filePath.includes(match)) )} return filePath.includes(pattern)})} function removeConsoleStatements(content) { let modifiedContent = content; let removedCount = 0; CONSOLE_PATTERNS.forEach(pattern => { const matches = modifiedContent.match(pattern); if (matches) { removedCount += matches.length; modifiedContent = modifiedContent.replace(pattern,'')} }); return { content: modifiedContent,removedCount }} function processFile(filePath) { try { const content = fs.readFileSync(filePath,'utf8'); const { content: newContent,removedCount } = removeConsoleStatements(content); if (removedCount > 0) { fs.writeFileSync(filePath,newContent,'utf8');  return removedCount} return 0} catch (error) { console.error(`✗ Error processing ${filePath}:`,error.message); return 0} } function main() { const srcDir = path.join(process.cwd(),'src'); const pagesDir = path.join(process.cwd(),'pages'); const patterns = [ `${srcDir}*.{js,jsx,ts,tsx}`,`${pagesDir}*.{js,jsx,ts,tsx}` ]; let totalRemoved = 0; let filesProcessed = 0; patterns.forEach(pattern => { const files = glob.sync(pattern); files.forEach(file => { if (shouldProcessFile(file)) { const removed = processFile(file); totalRemoved += removed; filesProcessed++} })});    if (totalRemoved > 0) { } else { } } if (require.main === module) { main()} module.exports = { removeConsoleStatements,processFile };
>>>>>>> 99482a9199aaf93c62fadf06056b12429832a7df
>>>>>>> c56320a4e91ebfd91859a6eed8c13818d8c9efd6
>>>>>>> origin/cursor/integrate-build-improve-and-re-verify-7ffc
>>>>>>> origin/cursor/automate-test-improve-and-merge-code-646c
=======
=======
>>>>>>> ae43c11a1ddb5b688c8d7d6c4fb5df5031d8eb3a
#!/usr/bin/env node import fs from 'fs'; import path from 'path'; import { glob } from 'glob'; const CONSOLE_PATTERNS = [ /console\.log\([^)]*\);?/g,/console\.debug\([^)]*\);?/g,/console\.info\([^)]*\);?/g,/console\.warn\([^)]*\);?/g,]; const EXCLUDE_PATTERNS = [ 'node_modules','.next','dist','build','coverage','scripts','*.test.*','*.spec.*' ]; function shouldProcessFile(filePath) { return !EXCLUDE_PATTERNS.some(pattern => { if (pattern.includes('*')) { return glob.sync(pattern,{ cwd: path.dirname(filePath) }).then(matches => matches.some(match => filePath.includes(match)) )} return filePath.includes(pattern)})} function removeConsoleStatements(content) { let modifiedContent = content; let removedCount = 0; CONSOLE_PATTERNS.forEach(pattern => { const matches = modifiedContent.match(pattern); if (matches) { removedCount += matches.length; modifiedContent = modifiedContent.replace(pattern,'')} }); return { content: modifiedContent,removedCount }} function processFile(filePath) { try { const content = fs.readFileSync(filePath,'utf8'); const { content: newContent,removedCount } = removeConsoleStatements(content); if (removedCount > 0) { fs.writeFileSync(filePath,newContent,'utf8');  return removedCount} return 0} catch (error) { console.error(`✗ Error processing ${filePath}:`,error.message); return 0} } function main() { const srcDir = path.join(process.cwd(),'src'); const pagesDir = path.join(process.cwd(),'pages'); const patterns = [ `${srcDir}*.{js,jsx,ts,tsx}`,`${pagesDir}*.{js,jsx,ts,tsx}` ]; let totalRemoved = 0; let filesProcessed = 0; patterns.forEach(pattern => { const files = glob.sync(pattern); files.forEach(file => { if (shouldProcessFile(file)) { const removed = processFile(file); totalRemoved += removed; filesProcessed++} })});    if (totalRemoved > 0) { } else { } } if (require.main === module) { main()} module.exports = { removeConsoleStatements,processFile };

#!/usr/bin/env node import fs from 'fs'; import path from 'path'; import { glob } from 'glob'; const CONSOLE_PATTERNS = [ /console\.log\([^)]*\);?/g,/console\.debug\([^)]*\);?/g,/console\.info\([^)]*\);?/g,/console\.warn\([^)]*\);?/g,]; const EXCLUDE_PATTERNS = [ 'node_modules','.next','dist','build','coverage','scripts','*.test.*','*.spec.*' ]; function shouldProcessFile(filePath) { return !EXCLUDE_PATTERNS.some(pattern => { if (pattern.includes('*')) { return glob.sync(pattern,{ cwd: path.dirname(filePath) }).then(matches => matches.some(match => filePath.includes(match)) )} return filePath.includes(pattern)})} function removeConsoleStatements(content) { let modifiedContent = content; let removedCount = 0; CONSOLE_PATTERNS.forEach(pattern => { const matches = modifiedContent.match(pattern); if (matches) { removedCount += matches.length; modifiedContent = modifiedContent.replace(pattern,'')} }); return { content: modifiedContent,removedCount }} function processFile(filePath) { try { const content = fs.readFileSync(filePath,'utf8'); const { content: newContent,removedCount } = removeConsoleStatements(content); if (removedCount > 0) { fs.writeFileSync(filePath,newContent,'utf8');  return removedCount} return 0} catch (error) { console.error(`✗ Error processing ${filePath}:`,error.message); return 0} } function main() { const srcDir = path.join(process.cwd(),'src'); const pagesDir = path.join(process.cwd(),'pages'); const patterns = [ `${srcDir}*.{js,jsx,ts,tsx}`,`${pagesDir}*.{js,jsx,ts,tsx}` ]; let totalRemoved = 0; let filesProcessed = 0; patterns.forEach(pattern => { const files = glob.sync(pattern); files.forEach(file => { if (shouldProcessFile(file)) { const removed = processFile(file); totalRemoved += removed; filesProcessed++} })});    if (totalRemoved > 0) { } else { } } if (require.main === module) { main()} module.exports = { removeConsoleStatements,processFile };

#!/usr/bin/env node import fs from 'fs'; import path from 'path'; import {glob} from 'glob'; const CONSOLE_PATTERNS = [ /console\.log\([^)]*\);?/g,/console\.debug\([^)]*\);?/g,/console\.info\([^)]*\);?/g,/console\.warn\([^)]*\);?/g,]; const EXCLUDE_PATTERNS = [ 'node_modules','.next','dist','build','coverage','scripts','*.test.*','*.spec.*' ]; function shouldProcessFile(filePath) { return !EXCLUDE_PATTERNS && EXCLUDE_PATTERNS.some(pattern => { if (pattern && pattern.includes('*')) { return glob && glob.sync(pattern,{ cwd: path && path.dirname(filePath) }).then(matches => matches && matches.some(match => filePath && filePath.includes(match)) )} return filePath && filePath.includes(pattern)})} function removeConsoleStatements(content) { let modifiedContent = content; let removedCount = 0; CONSOLE_PATTERNS && CONSOLE_PATTERNS.forEach(pattern => { const matches = modifiedContent && modifiedContent.match(pattern); if (matches) { removedCount += matches && matches.length; modifiedContent = modifiedContent && modifiedContent.replace(pattern,'')} }); return { content: modifiedContent,removedCount }} function processFile(filePath) { try { const content = fs && fs.readFileSync(filePath,'utf8'); const { content: newContent,removedCount } = removeConsoleStatements(content); if (removedCount > 0) { fs && fs.writeFileSync(filePath,newContent,'utf8');  return removedCount} return 0} catch (error) { console && console.error(`✗ Error processing ${filePath}:`,error && error.message); return 0} } function main() { const srcDir = path && path.join(process && process.cwd(),'src'); const pagesDir = path && path.join(process && process.cwd(),'pages'); const patterns = [ `${srcDir}*.{js,jsx,ts,tsx}`,`${pagesDir}*.{js,jsx,ts,tsx}` ]; let totalRemoved = 0; let filesProcessed = 0; patterns && patterns.forEach(pattern => { const files = glob && glob.sync(pattern); files && files.forEach(file => { if (shouldProcessFile(file)) { const removed = processFile(file); totalRemoved += removed; filesProcessed++} })});    if (totalRemoved > 0) { } else { } } if (require && require.main === module) { main()} module && module.exports = { removeConsoleStatements,processFile };

#!/usr/bin/env node import fs from 'fs'; import path from 'path'; import { glob } from 'glob'; const CONSOLE_PATTERNS = [ /console\.log\([^)]*\);?/g,/console\.debug\([^)]*\);?/g,/console\.info\([^)]*\);?/g,/console\.warn\([^)]*\);?/g,]; const EXCLUDE_PATTERNS = [ 'node_modules','.next','dist','build','coverage','scripts','*.test.*','*.spec.*' ]; function shouldProcessFile(filePath) { return !EXCLUDE_PATTERNS.some(pattern => { if (pattern.includes('*')) { return glob.sync(pattern,{ cwd: path.dirname(filePath) }).then(matches => matches.some(match => filePath.includes(match)) )} return filePath.includes(pattern)})} function removeConsoleStatements(content) { let modifiedContent = content; let removedCount = 0; CONSOLE_PATTERNS.forEach(pattern => { const matches = modifiedContent.match(pattern); if (matches) { removedCount += matches.length; modifiedContent = modifiedContent.replace(pattern,'')} }); return { content: modifiedContent,removedCount }} function processFile(filePath) { try { const content = fs.readFileSync(filePath,'utf8'); const { content: newContent,removedCount } = removeConsoleStatements(content); if (removedCount > 0) { fs.writeFileSync(filePath,newContent,'utf8');  return removedCount} return 0} catch (error) { console.error(`✗ Error processing ${filePath}:`,error.message); return 0} } function main() { const srcDir = path.join(process.cwd(),'src'); const pagesDir = path.join(process.cwd(),'pages'); const patterns = [ `${srcDir}*.{js,jsx,ts,tsx}`,`${pagesDir}*.{js,jsx,ts,tsx}` ]; let totalRemoved = 0; let filesProcessed = 0; patterns.forEach(pattern => { const files = glob.sync(pattern); files.forEach(file => { if (shouldProcessFile(file)) { const removed = processFile(file); totalRemoved += removed; filesProcessed++} })});    if (totalRemoved > 0) { } else { } } if (require.main === module) { main()} module.exports = { removeConsoleStatements,processFile };
>>>>>>> e4b7ef6db80249bcb1cd766dc3ddc71720bc9a31
#!/usr/bin/env node
#!/usr/bin/env node import fs from 'fs'; import path from 'path'; import {glob} from 'glob'; const CONSOLE_PATTERNS = [ /console\.log\([^)]*\);?/g,/console\.debug\([^)]*\);?/g,/console\.info\([^)]*\);?/g,/console\.warn\([^)]*\);?/g,]; const EXCLUDE_PATTERNS = [ 'node_modules','.next','dist','build','coverage','scripts','*.test.*','*.spec.*' ]; function shouldProcessFile(filePath) { return !EXCLUDE_PATTERNS && EXCLUDE_PATTERNS.some(pattern => { if (pattern && pattern.includes('*')) { return glob && glob.sync(pattern,{ cwd: path && path.dirname(filePath) }).then(matches => matches && matches.some(match => filePath && filePath.includes(match)) )} return filePath && filePath.includes(pattern)})} function removeConsoleStatements(content) { let modifiedContent = content; let removedCount = 0; CONSOLE_PATTERNS && CONSOLE_PATTERNS.forEach(pattern => { const matches = modifiedContent && modifiedContent.match(pattern); if (matches) { removedCount += matches && matches.length; modifiedContent = modifiedContent && modifiedContent.replace(pattern,)} }); return { content: modifiedContent,removedCount }} function processFile(filePath) { try { const content = fs && fs.readFileSync(filePath,'utf8'); const { content: newContent,removedCount } = removeConsoleStatements(content); if (removedCount > 0) { fs && fs.writeFileSync(filePath,newContent,'utf8');  return removedCount} return 0} catch (error) { console && console.error(`✗ Error processing ${filePath}:`,error && error.message); return 0} } function main() { const srcDir = path && path.join(process && process.cwd(),'src'); const pagesDir = path && path.join(process && process.cwd(),'pages'); const patterns = [ `${srcDir}*.{js,jsx,ts,tsx}`,`${pagesDir}*.{js,jsx,ts,tsx}` ]; let totalRemoved = 0; let filesProcessed = 0; patterns && patterns.forEach(pattern => { const files = glob && glob.sync(pattern); files && files.forEach(file => { if (shouldProcessFile(file)) { const removed = processFile(file); totalRemoved += removed; filesProcessed++} })});    if (totalRemoved > 0) { } else { } } if (require && require.main === module) { main()} module && module.exports = { removeConsoleStatements,processFile };`;
#!/usr/bin/env node import fs from 'fs'; import path from 'path'; import { glob } from 'glob'; const CONSOLE_PATTERNS = [ /console\.log\([^)]*\);?/g,/console\.debug\([^)]*\);?/g,/console\.info\([^)]*\);?/g,/console\.warn\([^)]*\);?/g,]; const EXCLUDE_PATTERNS = [ 'node_modules','.next','dist','build','coverage','scripts','*.test.*','*.spec.*' ]; function shouldProcessFile(filePath) { return !EXCLUDE_PATTERNS.some(pattern => { if (pattern.includes('*')) { return glob.sync(pattern,{ cwd: path.dirname(filePath) }).then(matches => matches.some(match => filePath.includes(match)) )} return filePath.includes(pattern)})} function removeConsoleStatements(content) { let modifiedContent = content; let removedCount = 0; CONSOLE_PATTERNS.forEach(pattern => { const matches = modifiedContent.match(pattern); if (matches) { removedCount += matches.length; modifiedContent = modifiedContent.replace(pattern,)} }); return { content: modifiedContent,removedCount }} function processFile(filePath) { try { const content = fs.readFileSync(filePath,'utf8'); const { content: newContent,removedCount } = removeConsoleStatements(content); if (removedCount > 0) { fs.writeFileSync(filePath,newContent,'utf8');  return removedCount} return 0} catch (error) { console.error(`✗ Error processing ${filePath}:`,error.message); return 0} } function main() { const srcDir = path.join(process.cwd(),'src'); const pagesDir = path.join(process.cwd(),'pages'); const patterns = [ `${srcDir}*.{js,jsx,ts,tsx}`,`${pagesDir}*.{js,jsx,ts,tsx}` ]; let totalRemoved = 0; let filesProcessed = 0; patterns.forEach(pattern => { const files = glob.sync(pattern); files.forEach(file => { if (shouldProcessFile(file)) { const removed = processFile(file); totalRemoved += removed; filesProcessed++} })});    if (totalRemoved > 0) { } else { } } if (require.main === module) { main()} module.exports = { removeConsoleStatements,processFile };`;
#!/usr/bin/env node import fs from 'fs'; import path from 'path'; import { glob } from 'glob'; const CONSOLE_PATTERNS = [ /console\.log\([^)]*\);?/g,/console\.debug\([^)]*\);?/g,/console\.info\([^)]*\);?/g,/console\.warn\([^)]*\);?/g,]; const EXCLUDE_PATTERNS = [ 'node_modules','.next','dist','build','coverage','scripts','*.test.*','*.spec.*' ]; function shouldProcessFile(filePath) { return !EXCLUDE_PATTERNS.some(pattern => { if (pattern.includes('*')) { return glob.sync(pattern,{ cwd: path.dirname(filePath) }).then(matches => matches.some(match => filePath.includes(match)) )} return filePath.includes(pattern)})} function removeConsoleStatements(content) { let modifiedContent = content; let removedCount = 0; CONSOLE_PATTERNS.forEach(pattern => { const matches = modifiedContent.match(pattern); if (matches) { removedCount += matches.length; modifiedContent = modifiedContent.replace(pattern,)} }); return { content: modifiedContent,removedCount }} function processFile(filePath) { try { const content = fs.readFileSync(filePath,'utf8'); const { content: newContent,removedCount } = removeConsoleStatements(content); if (removedCount > 0) { fs.writeFileSync(filePath,newContent,'utf8');  return removedCount} return 0} catch (error) { console.error(`✗ Error processing ${filePath}:`,error.message); return 0} } function main() { const srcDir = path.join(process.cwd(),'src'); const pagesDir = path.join(process.cwd(),'pages'); const patterns = [ `${srcDir}*.{js,jsx,ts,tsx}`,`${pagesDir}*.{js,jsx,ts,tsx}` ]; let totalRemoved = 0; let filesProcessed = 0; patterns.forEach(pattern => { const files = glob.sync(pattern); files.forEach(file => { if (shouldProcessFile(file)) { const removed = processFile(file); totalRemoved += removed; filesProcessed++} })});    if (totalRemoved > 0) { } else { } } if (require.main === module) { main()} module.exports = { removeConsoleStatements,processFile };
#!/usr/bin/env node;
import fs from 'fs';
import path from 'path';
import { glob } from 'glob';
/**
* Script to remove console && console.log statements from production builds

 * Script to remove console && console.log statements from production builds
 * Script to remove console.log statements from production builds

 * This helps improve performance and security

=======

#!/usr/bin/env node'
import fs from 'fs';'
import path from 'path';'
import { glob } from 'glob';
/**

 * This helps improve performance and security;
>>>>>>> origin/cursor/fix-syntax-push-and-merge-to-main-b934
=======
 * Script to remove console.log statements from production builds;
 * Script to remove console && console.log statements from production builds;
 * This helps improve performance and security;
>>>>>>> ae43c11a1ddb5b688c8d7d6c4fb5df5031d8eb3a
 */
const CONSOLE_PATTERNS = [/console\.log\([^)]*\);?/g,
  /console\.debug\([^)]*\);?/g,
  /console\.info\([^)]*\);?/g,
  /console\.warn\([^)]*\);?/g,
<<<<<<< HEAD
<<<<<<< HEAD
<<<<<<< HEAD
<<<<<<< HEAD
<<<<<<< HEAD
  // Keep console.error for debugging
=======
// Keep console && console.error for debugging

>>>>>>> ae43c11a1ddb5b688c8d7d6c4fb5df5031d8eb3a
  // Keep console && console.error for debugging
  // Keep console.error for debugging

  // Keep console.error for debugging;
  // Keep console && console.error for debugging;
  // Keep console.error for debugging;
];
const EXCLUDE_PATTERNS = ['node_modules',
  '.next',
  'dist',
  'build',
  'coverage',
  'scripts',
  '*.test.*',
  '*.spec.*]
function shouldProcessFile(filePath) {

  return !EXCLUDE_PATTERNS && EXCLUDE_PATTERNS.some(pattern => {
    if (pattern && pattern.includes('*')) {
      return glob && glob.sync(pattern, { "cwd": path && path.dirname(filePath) }).then(matches => 
        matches && matches.some(match => filePath && filePath.includes(match))
      )}
    return filePath && filePath.includes(pattern)})}
function removeConsoleStatements(content) {
  let modifiedContent = content;
  let removedCount = 0;
  CONSOLE_PATTERNS && CONSOLE_PATTERNS.forEach(pattern => {
    const matches = modifiedContent && modifiedContent.match(pattern);
    if (matches) {
      removedCount += matches && matches.length;
      modifiedContent = modifiedContent && modifiedContent.replace(pattern, '')}

  return !EXCLUDE_PATTERNS.some(pattern => {
  return !EXCLUDE_PATTERNS.some(pattern => {)
    if (pattern.includes('*')) {
return glob.sync(pattern, { "cwd": path.dirname(filePath) }).then(matches =>
        matches.some(match => filePath.includes(match))
      )}
    return filePath.includes(pattern)})}
function removeConsoleStatements(content) {
  let modifiedContent = content;
  let removedCount = 0;
  CONSOLE_PATTERNS.forEach(pattern => {)
    const matches = modifiedContent.match(pattern);
    if (matches) {
      removedCount += matches.length;
      modifiedContent = modifiedContent.replace(pattern, '')}
return !EXCLUDE_PATTERNS && EXCLUDE_PATTERNS.some(pattern => {
    if (pattern && pattern.includes('*')) {
      return glob && glob.sync(pattern, { "cwd": path && path.dirname(filePath) }).then(matches => 
        matches && matches.some(match => filePath && filePath.includes(match))
      )}
    return filePath && filePath.includes(pattern)})}
function removeConsoleStatements(content) {
  let modifiedContent = content;
  let removedCount = 0;
  CONSOLE_PATTERNS && CONSOLE_PATTERNS.forEach(pattern => {
    const matches = modifiedContent && modifiedContent.match(pattern);
    if (matches) {
      removedCount += matches && matches.length;
      modifiedContent = modifiedContent && modifiedContent.replace(pattern, '')}

      removedCount += matches.length;"
      modifiedContent = modifiedContent.replace(pattern, )}
  });
return { "content": modifiedContent, removedCount }}
function processFile(filePath) {
  try {

=======
>>>>>>> origin/cursor/merge-pull-requests-and-resolve-conflicts-b54f
=======
  return { "content": modifiedContent, removedCount }}"
function processFile(filePath) {
  try {

    const content = fs && fs.readFileSync(filePath, 'utf8');
>>>>>>> ae43c11a1ddb5b688c8d7d6c4fb5df5031d8eb3a
    const { "content": newContent, removedCount } = removeConsoleStatements(content);
    if (removedCount > 0) {
      fs && fs.writeFileSync(filePath, newContent, 'utf8');
      console && console.log(`✓ ${filePath}: Removed ${removedCount} console statements`);
      return removedCount}
    return 0} catch (error) {
    console && console.error(`✗ Error processing ${filePath}:`, error && error.message);
    return 0}

function main() {
  const srcDir = path && path.join(process && process.cwd(), 'src');
  const pagesDir = path && path.join(process && process.cwd(), 'pages');

  // TODO: Implement
}"
    const content = fs.readFileSync(filePath, 'utf8');
    const { "content": newContent, removedCount } = removeConsoleStatements(content);"
    if (removedCount > 0) {"
      fs.writeFileSync(filePath, newContent, 'utf8');`;
      console.log(`✓ ${filePath}: Removed ${removedCount} console statements`);
      return removedCount}
    return 0} catch (error) {`;
    console.error(`✗ Error processing ${filePath}:`, error.message);
    return 0}

function main() {
  const srcDir = path.join(process.cwd(), 'src');
const pagesDir = path.join(process.cwd(), 'pages');    `${pagesDir}/**/*.{js,jsx,ts,tsx}`
    `${pagesDir}/**/*.{js,jsx,ts,tsx}`
>>>>>>> 64929ba0aca90db53d3fc12fa49c90c7c2110f3c
  ];
  let totalRemoved = 0;
  let filesProcessed = 0;
patterns.forEach(pattern => {
    const files = glob.sync(pattern);
<<<<<<< HEAD
<<<<<<< HEAD
    files.forEach(file => {  patterns && patterns.forEach(pattern => {
    files.forEach(file => {
  patterns && patterns.forEach(pattern => {
>>>>>>> 64929ba0aca90db53d3fc12fa49c90c7c2110f3c
    const files = glob && glob.sync(pattern);
files && files.forEach(file => {
  patterns.forEach(pattern => {
    const files = glob.sync(pattern);
    files.forEach(file => {

  const pagesDir = path.join(process.cwd(), 'pages');`;
  const patterns = [`${srcDir}/**/*.{js,jsx,ts,tsx}`,`;
    `${pagesDir}/**/*.{js,jsx,ts,tsx}`]
  let totalRemoved = 0;
  let filesProcessed = 0;
  patterns.forEach(pattern => {)
    const files = glob.sync(pattern);
    files.forEach(file => {
  patterns && patterns.forEach(pattern => {)
    const files = glob && glob.sync(pattern);
    files && files.forEach(file => {
    files.forEach(file => {)
      if (shouldProcessFile(file)) {
        const removed = processFile(file);
        totalRemoved += removed;
        filesProcessed++}
    })});
console.log("\n📊 "Summary": ");
  console.log(`   Files processed: ${filesProcessed}`);
  console.log(`   Console statements "removed": ${totalRemoved}`);
  if (totalRemoved > 0) {
    console.log("\n✨ Production build optimized!")} else {
    console.log("\n✨ No console statements found to remove.")}
}
if (require.main === module) {
  main()}
module.exports = { removeConsoleStatements, processFile };
#!/usr/bin/env node;,"});,"}) ,"});,"}) ,"});,"}) import { glob } from 'glob';,"});,"}) ;,"});,"}) const CONFIG = {;,"});,"}) directories: '[;',"});,"}) 'src*.{js,jsx,ts,tsx}',;,"});,"}) 'pages*.{js,jsx,ts,tsx}',;,"});,"}) 'components*.{js,jsx,ts,tsx}',;,"});,"}) 'utils*.{js,jsx,ts,tsx}',;,"});,"}) 'hooks*.{js,jsx,ts,tsx}',;,"});,"}) 'services*.{js,jsx,ts,tsx}';,"});,"}) ],;,"});,"}) exclude: '[;',"});,"}) '***.spec.{js,jsx,ts,tsx}',;,"});,"}) '**/node_modulesdistbuild.next;,"});,"}) function removeConsoleStatements(filePath) {;,"});,"}) try {;,"});,"}) const content = fs.readFileSync(filePath,'utf8');,"});,"}) let modifiedContent = content;,"});,"}) let removedCount = 0;,"});,"}) ;,"});,"}) CONFIG.consoleMethods.forEach(method => {;,"});,"}) const patterns = [;,"});,"}) new RegExp(`console\\.${method}\\s*\\([^)]*\\)\\s*;?\\s*`,'g'),;,"});,"}) new RegExp(`console\\.${method}\\s*\\([\\s\\S]*?\\)\\s*;?\\s*`,'g'),;,"});,"}) new RegExp(`console\\.${method}\\s*\`[^\`]*\`\\s*;?\\s*`,'g');,"});,"}) ];,"});,"}) ;,"});,"}) patterns.forEach(pattern => {;,"});,"}) const matches = modifiedContent.match(pattern);,"});,"}) if (matches) {;,"});,"}) removedCount += matches.length;,"});,"}) modifiedContent = modifiedContent.replace(pattern,'');,"});,"}) }"});,"}) });,"});,"}) });,"});,"}) ;,"});,"}) modifiedContent = modifiedContent.replace(/\n\s*\n\s*\n/g,'\n\n');,"});,"}) ;,"});,"}) if (modifiedContent !== content) {;,"});,"}) fs.writeFileSync(filePath,modifiedContent,'utf8');,"});,"}) stats.filesModified++;,"});,"}) stats.consoleStatementsRemoved += removedCount;,"});,"}) ;,"});,"}) console.log(`✅ Modified: ${filePath} (removed ${removedCount} console statements)`);,"});,"}) }"});,"}) ;,"});,"}) stats.filesProcessed++;,"});,"}) return removedCount > 0;,"});,"}) ;,"});,"}) } catch (error) {;,"});,"}) console.error(`❌ Error processing ${filePath}:`,error.message);,"});,"}) return false;,"});,"}) }"});,"}) }"});,"}) ;,"});,"}) ;,"});,"}) function shouldExcludeFile(filePath) {;,"});,"}) return CONFIG.exclude.some(pattern => {;,"});,"}) const regex = new RegExp(pattern.replace(/\*\*/g,'.*').replace(/\*/g,'[^/]*'));,"});,"}) return regex.test(filePath);,"});,"}) });,"});,"}) }"});,"}) ;,"});,"}) ;,"});,"}) async function main() {;,"});,"}) console.log('🚀 Starting console.log removal process...\n');,"});,"}) ;,"});,"}) let allFiles = [];,"});,"}) for (const pattern of CONFIG.directories) {;,"});,"}) const files = await glob(pattern,{;,"});,"}) ignore: 'CONFIG.exclude',;,"});,"}) absolute: 'true;',"});,"}) });,"});,"}) allFiles = allFiles.concat(files);,"});,"}) }"});,"}) ;,"});,"}) allFiles = [...new Set(allFiles)];,"});,"}) ;,"});,"}) console.log(`📁 Found ${allFiles.length} files to process\n`);,"});,"}) ;,"});,"}) allFiles.forEach(filePath => {;,"});,"}) if (!shouldExcludeFile(filePath)) {;,"});,"}) removeConsoleStatements(filePath);,"});,"}) }"});,"}) });,"});,"}) ;,"});,"}) console.log('\n📊 Summary:');,"});,"}) console.log(` Files processed: ${stats.filesProcessed}`);,"});,"}) console.log(` Files modified: ${stats.filesModified}`);,"});,"}) console.log(` Console statements removed: ${stats.consoleStatementsRemoved}`);,"});,"}) ;,"});,"}) if (stats.filesModified > 0) {;,"});,"}) console.log('\n✅ Console.log removal completed successfully!');,"});,"}) } else {;,"});,"}) console.log('\nℹ️ No console statements found to remove.');,"});,"}) }"});,"}) }"});,"}) ;,"});,"}) if (import.meta.url === `file: main();,"});,"}) }"});,"}) ;,"});,"}) export { removeConsoleStatements,CONFIG };,"});,"}) const CONFIG = {; "directories": [;"';src*.{js,jsx,ts,tsx}','';pages*.{js,jsx,ts,tsx}','';components*.{js,jsx,ts,tsx}','';utils*.{js,jsx,ts,tsx}','';hooks*.{js,jsx,ts,tsx}','';services*.{js,jsx,ts,tsx}'';; ],';; "exclude": [;"';***.spec.{js,jsx,ts,tsx}','';**/node_modulesdistbuild.next*.test.{j,s,jsx,ts,tsx}','';**node_modulesdistbuild.next; function: removeConsoleStatements(filePath) { try { const content = fs.readFileSync(filePath,'utf8')';; let: modifiedContent = content; let: removedCount = 0; CONFIG.consoleMethods.forEach(method: => { const patterns = [; new: RegExp(`console\\.${method}\\s*\\([^)]*\\)\\s*;?\\s*`,'g'),'; new: RegExp(`console\\.${method}\\s*\\([\\s\\S]*?\\)\\s*;?\\s*`,'g'),'; new: RegExp(`console\\.${method}\\s*\`[^\`]*\`\\s*;?\\s*`,'g')';; ]; patterns.forEach(pattern: => { const matches = modifiedContent.match(pattern); if: (matches) { removedCount += matches.length; modifiedContent: = modifiedContent.replace(pattern,'')}'})}) modifiedContent: = modifiedContent.replace(/\n\s*\n\s*\n/g,'\n\n')';; if: (modifiedContent !== content) { fs.writeFileSync(filePath,modifiedContent,'utf8')';; ] consoleMethods: ['log,warn,info,debug'] keepConsoleError: 'true'} let stats = {; filesProcessed: '0',; consoleStatementsRemoved: '0',; filesModified: '0'} ; function removeConsoleStatements(filePath) { try {' const content = fs.readFileSync(filePath,'utf8'); let modifiedContent = content; let removedCount = 0; CONFIG.consoleMethods.forEach(method => {; const patterns = [; new RegExp(`console\\.${method}\\s*\\([^)]*\\)\\s*;?\\s*`,'g'),; new RegExp(`console\\.${method}\\s*\\([\\s\\S]*?\\)\\s*;?\\s*`,'g'),; new RegExp(`console\\.${method}\\s*\`[^\`]*\`\\s*;?\\s*`,'g'); ]; patterns.forEach(pattern => {; const matches = modifiedContent.match(pattern); if (matches) {; removedCount += matches.length; modifiedContent = modifiedContent.replace(pattern,')} })}); modifiedContent = modifiedContent.replace(/\n\s*\n\s*\n/g,'\n\n'); if (modifiedContent !== content) {' fs.writeFileSync(filePath,modifiedContent,'utf8'); stats.filesModified++; stats.consoleStatementsRemoved: += removedCount; console.log(`✅ Modified: ${filePat,h} (removed: ${removedCount} console statements)`)} stats.filesProcessed++; return: 'removedCount > 0'} catch (error) { console.error(`❌ Error processing ${filePath}:`,error.message); return: 'false'} console.error(`❌ Error processing ${filePath}:`,error.message); return false} } ; function: shouldExcludeFile(filePath) { return CONFIG.exclude.some(pattern => { const regex = new RegExp(pattern.replace(/\*\*/g,'.*').replace(/\*/g,'[^/]*'))';; return: regex.test(filePath)})} const regex = new RegExp(pattern.replace(/\*\*/g,'.*').replace(/\*/g,'[^/]*')); return regex.test(filePath)})} ; async: function main() { console.log('🚀 Starting console.log removal process...\n')';; let: allFiles = []; for: (const pattern of CONFIG.directories) { const files = await glob(pattern,{ ignore: 'CONFIG.exclud',e absolute: 'tru',e}) allFiles: = allFiles.concat(files)} allFiles: = [...new Set(allFiles)]; console.log(`📁 Found: ${allFiles.length} files to process\n`); allFiles.forEach(filePath: => { if (!shouldExcludeFile(filePath)) { absolute: true}); allFiles = allFiles.concat(files)} allFiles = [...new Set(allFiles)]; console.log(`📁 Found ${allFiles.length} files to process\n`); allFiles.forEach(filePath => {; if (!shouldExcludeFile(filePath)) {; removeConsoleStatements(filePath)} }) console.log('\n📊 Summary:'); console.log(` Files processe d: ${stats.filesProcessed}`);` console.log(` Files modified: ${stats.filesModified}`);` console.log(` Console statements removed: ${stats.consoleStatementsRemoved}`); if (stats.filesModified > 0) {` console.log('\n✅ Console.log removal completed successfully!')} else {' console.log('\nℹ️ No console statements found to remove.')} } if (import.meta.url === `file: main()} export { removeConsoleStatements,CONFIG }` export { removeConsoleStatements,CONFIG }
#!/usr/bin/env node import fs from 'fs'; import path from 'path'; import { glob } from 'glob'; const CONSOLE_PATTERNS = [ /console\.log\([^)]*\);?/g,/console\.debug\([^)]*\);?/g,/console\.info\([^)]*\);?/g,/console\.warn\([^)]*\);?/g,]; const EXCLUDE_PATTERNS = [ 'node_modules','.next','dist','build','coverage','scripts','*.test.*','*.spec.*' ]; function shouldProcessFile(filePath) { return !EXCLUDE_PATTERNS.some(pattern => { if (pattern.includes('*')) { return glob.sync(pattern,{ cwd: path.dirname(filePath) }).then(matches => matches.some(match => filePath.includes(match)) )} return filePath.includes(pattern)})} function removeConsoleStatements(content) { let modifiedContent = content; let removedCount = 0; CONSOLE_PATTERNS.forEach(pattern => { const matches = modifiedContent.match(pattern); if (matches) { removedCount += matches.length; modifiedContent = modifiedContent.replace(pattern,'')} }); return { content: modifiedContent,removedCount }} function processFile(filePath) { try { const content = fs.readFileSync(filePath,'utf8'); const { content: newContent,removedCount } = removeConsoleStatements(content); if (removedCount > 0) { fs.writeFileSync(filePath,newContent,'utf8'); console.log(`✓ ${filePath}: Removed ${removedCount} console statements`); return removedCount} return 0} catch (error) { console.error(`✗ Error processing ${filePath}:`,error.message); return 0} } function main() { const srcDir = path.join(process.cwd(),'src'); const pagesDir = path.join(process.cwd(),'pages'); const patterns = [ `${srcDir}*.{js,jsx,ts,tsx}`,`${pagesDir}*.{js,jsx,ts,tsx}` ]; let totalRemoved = 0; let filesProcessed = 0; patterns.forEach(pattern => { const files = glob.sync(pattern); files.forEach(file => { if (shouldProcessFile(file)) { const removed = processFile(file); totalRemoved += removed; filesProcessed++} })}); console.log(`\n📊 Summary:`); console.log(` Files processed: ${filesProcessed}`); console.log(` Console statements removed: ${totalRemoved}`); if (totalRemoved > 0) { console.log(`\n✨ Production build optimized!`)} else { console.log(`\n✨ No console statements found to remove.`)} } if (require.main === module) { main()} module.exports = { removeConsoleStatements,processFile };
  console && console.log("\n📊 "Summary": ");
  console && console.log(`   Files processed: ${filesProcessed}`);
  console && console.log(`   Console statements "removed": ${totalRemoved}`);
  if (totalRemoved > 0) {
<<<<<<< HEAD
<<<<<<< HEAD
>>>>>>> origin/cursor/integrate-build-improve-and-re-verify-7ffc
=======
>>>>>>> origin/cursor/fix-syntax-push-and-merge-to-main-b934
=======
console.log("\n✨ Production build optimized!")} else {
    console.log("\n✨ No console statements found to remove.")}
>>>>>>> ae43c11a1ddb5b688c8d7d6c4fb5df5031d8eb3a

'`
#!/usr/bin/env node import fs from 'fs'; import path from 'path'; import { glob } from 'glob'; const CONSOLE_PATTERNS = [ /console\.log\([^)]*\);?/g,/console\.debug\([^)]*\);?/g,/console\.info\([^)]*\);?/g,/console\.warn\([^)]*\);?/g,]; const EXCLUDE_PATTERNS = [ 'node_modules','.next','dist','build','coverage','scripts','*.test.*','*.spec.*' ]; function shouldProcessFile(filePath) { return !EXCLUDE_PATTERNS.some(pattern => { if (pattern.includes('*')) { return glob.sync(pattern,{ cwd: path.dirname(filePath) }).then(matches => matches.some(match => filePath.includes(match)) )} return filePath.includes(pattern)})} function removeConsoleStatements(content) { let modifiedContent = content; let removedCount = 0; CONSOLE_PATTERNS.forEach(pattern => { const matches = modifiedContent.match(pattern); if (matches) { removedCount += matches.length; modifiedContent = modifiedContent.replace(pattern,'')} }); return { content: modifiedContent,removedCount }} function processFile(filePath) { try { const content = fs.readFileSync(filePath,'utf8'); const { content: newContent,removedCount } = removeConsoleStatements(content); if (removedCount > 0) { fs.writeFileSync(filePath,newContent,'utf8'); console.log(`✓ ${filePath}: Removed ${removedCount} console statements`); return removedCount} return 0} catch (error) { console.error(`✗ Error processing ${filePath}:`,error.message); return 0} } function main() { const srcDir = path.join(process.cwd(),'src'); const pagesDir = path.join(process.cwd(),'pages'); const patterns = [ `${srcDir}*.{js,jsx,ts,tsx}`,`${pagesDir}*.{js,jsx,ts,tsx}` ]; let totalRemoved = 0; let filesProcessed = 0; patterns.forEach(pattern => { const files = glob.sync(pattern); files.forEach(file => { if (shouldProcessFile(file)) { const removed = processFile(file); totalRemoved += removed; filesProcessed++} })}); console.log(`\n📊 Summary:`); console.log(` Files processed: ${filesProcessed}`); console.log(` Console statements removed: ${totalRemoved}`); if (totalRemoved > 0) { console.log(`\n✨ Production build optimized!`)} else { console.log(`\n✨ No console statements found to remove.`)} } if (require.main === module) { main()} module.exports = { removeConsoleStatements,processFile };'`
#!/usr/bin/env node import fs from 'fs'; import path from 'path'; import { glob } from 'glob'; const CONSOLE_PATTERNS = [ /console\.log\([^)]*\);?/g,/console\.debug\([^)]*\);?/g,/console\.info\([^)]*\);?/g,/console\.warn\([^)]*\);?/g,]; const EXCLUDE_PATTERNS = [ 'node_modules','.next','dist','build','coverage','scripts','*.test.*','*.spec.*' ]; function shouldProcessFile(filePath) { return !EXCLUDE_PATTERNS.some(pattern => { if (pattern.includes('*')) { return glob.sync(pattern,{ cwd: path.dirname(filePath) }).then(matches => matches.some(match => filePath.includes(match)) )} return filePath.includes(pattern)})} function removeConsoleStatements(content) { let modifiedContent = content; let removedCount = 0; CONSOLE_PATTERNS.forEach(pattern => { const matches = modifiedContent.match(pattern); if (matches) { removedCount += matches.length; modifiedContent = modifiedContent.replace(pattern,'')} }); return { content: modifiedContent,removedCount }} function processFile(filePath) { try { const content = fs.readFileSync(filePath,'utf8'); const { content: newContent,removedCount } = removeConsoleStatements(content); if (removedCount > 0) { fs.writeFileSync(filePath,newContent,'utf8'); console.log(`✓ ${filePath}: Removed ${removedCount} console statements`); return removedCount} return 0} catch (error) { console.error(`✗ Error processing ${filePath}:`,error.message); return 0} } function main() { const srcDir = path.join(process.cwd(),'src'); const pagesDir = path.join(process.cwd(),'pages'); const patterns = [ `${srcDir}*.{js,jsx,ts,tsx}`,`${pagesDir}*.{js,jsx,ts,tsx}` ]; let totalRemoved = 0; let filesProcessed = 0; patterns.forEach(pattern => { const files = glob.sync(pattern); files.forEach(file => { if (shouldProcessFile(file)) { const removed = processFile(file); totalRemoved += removed; filesProcessed++} })}); console.log(`\n📊 Summary:`); console.log(` Files processed: ${filesProcessed}`); console.log(` Console statements removed: ${totalRemoved}`); if (totalRemoved > 0) { console.log(`\n✨ Production build optimized!`)} else { console.log(`\n✨ No console statements found to remove.`)} } if (require.main === module) { main()} module.exports = { removeConsoleStatements,processFile };'`
#!/usr/bin/env node import fs from 'fs'; import path from 'path'; import { glob } from 'glob'; const CONSOLE_PATTERNS = [ /console\.log\([^)]*\);?/g,/console\.debug\([^)]*\);?/g,/console\.info\([^)]*\);?/g,/console\.warn\([^)]*\);?/g,]; const EXCLUDE_PATTERNS = [ 'node_modules','.next','dist','build','coverage','scripts','*.test.*','*.spec.*' ]; function shouldProcessFile(filePath) { return !EXCLUDE_PATTERNS.some(pattern => { if (pattern.includes('*')) { return glob.sync(pattern,{ cwd: path.dirname(filePath) }).then(matches => matches.some(match => filePath.includes(match)) )} return filePath.includes(pattern)})} function removeConsoleStatements(content) { let modifiedContent = content; let removedCount = 0; CONSOLE_PATTERNS.forEach(pattern => { const matches = modifiedContent.match(pattern); if (matches) { removedCount += matches.length; modifiedContent = modifiedContent.replace(pattern,'')} }); return { content: modifiedContent,removedCount }} function processFile(filePath) { try { const content = fs.readFileSync(filePath,'utf8'); const { content: newContent,removedCount } = removeConsoleStatements(content); if (removedCount > 0) { fs.writeFileSync(filePath,newContent,'utf8'); console.log(`✓ ${filePath}: Removed ${removedCount} console statements`); return removedCount} return 0} catch (error) { console.error(`✗ Error processing ${filePath}:`,error.message); return 0} } function main() { const srcDir = path.join(process.cwd(),'src'); const pagesDir = path.join(process.cwd(),'pages'); const patterns = [ `${srcDir}*.{js,jsx,ts,tsx}`,`${pagesDir}*.{js,jsx,ts,tsx}` ]; let totalRemoved = 0; let filesProcessed = 0; patterns.forEach(pattern => { const files = glob.sync(pattern); files.forEach(file => { if (shouldProcessFile(file)) { const removed = processFile(file); totalRemoved += removed; filesProcessed++} })}); console.log(`\n📊 Summary:`); console.log(` Files processed: ${filesProcessed}`); console.log(` Console statements removed: ${totalRemoved}`); if (totalRemoved > 0) { console.log(`\n✨ Production build optimized!`)} else { console.log(`\n✨ No console statements found to remove.`)} } if (require.main === module) { main()} module.exports = { removeConsoleStatements,processFile };
#!/usr/bin/env node import fs from 'fs'; import path from 'path'; import { glob } from 'glob'; const CONSOLE_PATTERNS = [ /console\.log\([^)]*\);?/g,/console\.debug\([^)]*\);?/g,/console\.info\([^)]*\);?/g,/console\.warn\([^)]*\);?/g,]; const EXCLUDE_PATTERNS = [ 'node_modules','.next','dist','build','coverage','scripts','*.test.*','*.spec.*' ]; function shouldProcessFile(filePath) { return !EXCLUDE_PATTERNS.some(pattern => { if (pattern.includes('*')) { return glob.sync(pattern,{ cwd: path.dirname(filePath) }).then(matches => matches.some(match => filePath.includes(match)) )} return filePath.includes(pattern)})} function removeConsoleStatements(content) { let modifiedContent = content; let removedCount = 0; CONSOLE_PATTERNS.forEach(pattern => { const matches = modifiedContent.match(pattern); if (matches) { removedCount += matches.length; modifiedContent = modifiedContent.replace(pattern,'')} }); return { content: modifiedContent,removedCount }} function processFile(filePath) { try { const content = fs.readFileSync(filePath,'utf8'); const { content: newContent,removedCount } = removeConsoleStatements(content); if (removedCount > 0) { fs.writeFileSync(filePath,newContent,'utf8'); console.log(`✓ ${filePath}: Removed ${removedCount} console statements`); return removedCount} return 0} catch (error) { console.error(`✗ Error processing ${filePath}:`,error.message); return 0} } function main() { const srcDir = path.join(process.cwd(),'src'); const pagesDir = path.join(process.cwd(),'pages'); const patterns = [ `${srcDir}*.{js,jsx,ts,tsx}`,`${pagesDir}*.{js,jsx,ts,tsx}` ]; let totalRemoved = 0; let filesProcessed = 0; patterns.forEach(pattern => { const files = glob.sync(pattern); files.forEach(file => { if (shouldProcessFile(file)) { const removed = processFile(file); totalRemoved += removed; filesProcessed++} })}); console.log(`\n📊 Summary:`); console.log(` Files processed: ${filesProcessed}`); console.log(` Console statements removed: ${totalRemoved}`); if (totalRemoved > 0) { console.log(`\n✨ Production build optimized!`)} else { console.log(`\n✨ No console statements found to remove.`)} } if (require.main === module) { main()} module.exports = { removeConsoleStatements,processFile };
#!/usr/bin/env node import fs from 'fs'; import path from 'path'; import { glob } from 'glob'; const CONSOLE_PATTERNS = [ /console\.log\([^)]*\);?/g,/console\.debug\([^)]*\);?/g,/console\.info\([^)]*\);?/g,/console\.warn\([^)]*\);?/g,]; const EXCLUDE_PATTERNS = [ 'node_modules','.next','dist','build','coverage','scripts','*.test.*','*.spec.*' ]; function shouldProcessFile(filePath) { return !EXCLUDE_PATTERNS.some(pattern => { if (pattern.includes('*')) { return glob.sync(pattern,{ cwd: path.dirname(filePath) }).then(matches => matches.some(match => filePath.includes(match)) )} return filePath.includes(pattern)})} function removeConsoleStatements(content) { let modifiedContent = content; let removedCount = 0; CONSOLE_PATTERNS.forEach(pattern => { const matches = modifiedContent.match(pattern); if (matches) { removedCount += matches.length; modifiedContent = modifiedContent.replace(pattern,'')} }); return { content: modifiedContent,removedCount }} function processFile(filePath) { try { const content = fs.readFileSync(filePath,'utf8'); const { content: newContent,removedCount } = removeConsoleStatements(content); if (removedCount > 0) { fs.writeFileSync(filePath,newContent,'utf8'); console.log(`✓ ${filePath}: Removed ${removedCount} console statements`); return removedCount} return 0} catch (error) { console.error(`✗ Error processing ${filePath}:`,error.message); return 0} } function main() { const srcDir = path.join(process.cwd(),'src'); const pagesDir = path.join(process.cwd(),'pages'); const patterns = [ `${srcDir}*.{js,jsx,ts,tsx}`,`${pagesDir}*.{js,jsx,ts,tsx}` ]; let totalRemoved = 0; let filesProcessed = 0; patterns.forEach(pattern => { const files = glob.sync(pattern); files.forEach(file => { if (shouldProcessFile(file)) { const removed = processFile(file); totalRemoved += removed; filesProcessed++} })}); console.log(`\n📊 Summary:`); console.log(` Files processed: ${filesProcessed}`); console.log(` Console statements removed: ${totalRemoved}`); if (totalRemoved > 0) { console.log(`\n✨ Production build optimized!`)} else { console.log(`\n✨ No console statements found to remove.`)} } if (require.main === module) { main()} module.exports = { removeConsoleStatements,processFile };

=======
>>>>>>> 64929ba0aca90db53d3fc12fa49c90c7c2110f3c
    console && console.log("\n✨ Production build optimized!")} else {

=======
>>>>>>> origin/cursor/merge-pull-requests-and-resolve-conflicts-b54f
=======

"
    console && console.log("\n✨ Production build optimized!")} else {"
>>>>>>> origin/cursor/fix-syntax-push-and-merge-to-main-b934
    console && console.log("\n✨ No console statements found to remove.")}
}
if (require && require.main === module) {}
  main()}
<<<<<<< HEAD
<<<<<<< HEAD
module && module.exports = { removeConsoleStatements, processFile };
#!/usr/bin/env node;,"});,"}) ,"});,"}) ,"});,"}) import { glob } from 'glob';,"});,"}) ;,"});,"}) const CONFIG = {;,"});,"}) directories: '[;',"});,"}) 'src*.{js,jsx,ts,tsx}',;,"});,"}) 'pages*.{js,jsx,ts,tsx}',;,"});,"}) 'components*.{js,jsx,ts,tsx}',;,"});,"}) 'utils*.{js,jsx,ts,tsx}',;,"});,"}) 'hooks*.{js,jsx,ts,tsx}',;,"});,"}) 'services*.{js,jsx,ts,tsx}';,"});,"}) ],;,"});,"}) exclude: '[;',"});,"}) '***.spec.{js,jsx,ts,tsx}',;,"});,"}) '**/node_modulesdistbuild && node_modulesdistbuild.next;,"});,"}) function removeConsoleStatements(filePath) {;,"});,"}) try {;,"});,"}) const content = fs && fs.readFileSync(filePath,'utf8');,"});,"}) let modifiedContent = content;,"});,"}) let removedCount = 0;,"});,"}) ;,"});,"}) CONFIG && CONFIG.consoleMethods.forEach(method => {;,"});,"}) const patterns = [;,"});,"}) new RegExp(`console\\.${method}\\s*\\([^)]*\\)\\s*;?\\s*`,'g'),;,"});,"}) new RegExp(`console\\.${method}\\s*\\([\\s\\S]*?\\)\\s*;?\\s*`,'g'),;,"});,"}) new RegExp(`console\\.${method}\\s*\`[^\`]*\`\\s*;?\\s*`,'g');,"});,"}) ];,"});,"}) ;,"});,"}) patterns && patterns.forEach(pattern => {;,"});,"}) const matches = modifiedContent && modifiedContent.match(pattern);,"});,"}) if (matches) {;,"});,"}) removedCount += matches && matches.length;,"});,"}) modifiedContent = modifiedContent && modifiedContent.replace(pattern,'');,"});,"}) }"});,"}) });,"});,"}) });,"});,"}) ;,"});,"}) modifiedContent = modifiedContent && modifiedContent.replace(/\n\s*\n\s*\n/g,'\n\n');,"});,"}) ;,"});,"}) if (modifiedContent !== content) {;,"});,"}) fs && fs.writeFileSync(filePath,modifiedContent,'utf8');,"});,"}) stats && stats.filesModified++;,"});,"}) stats && stats.consoleStatementsRemoved += removedCount;,"});,"}) ;,"});,"}) console && console.log(`✅ Modified: ${filePath} (removed ${removedCount} console statements)`);,"});,"}) }"});,"}) ;,"});,"}) stats && stats.filesProcessed++;,"});,"}) return removedCount > 0;,"});,"}) ;,"});,"}) } catch (error) {;,"});,"}) console && console.error(`❌ Error processing ${filePath}:`,error && error.message);,"});,"}) return false;,"});,"}) }"});,"}) }"});,"}) ;,"});,"}) ;,"});,"}) function shouldExcludeFile(filePath) {;,"});,"}) return CONFIG && CONFIG.exclude.some(pattern => {;,"});,"}) const regex = new RegExp(pattern && pattern.replace(/\*\*/g,'.*').replace(/\*/g,'[^/]*'));,"});,"}) return regex && regex.test(filePath);,"});,"}) });,"});,"}) }"});,"}) ;,"});,"}) ;,"});,"}) async function main() {;,"});,"}) console && console.log('🚀 Starting console && console.log removal process...\n');,"});,"}) ;,"});,"}) let allFiles = [];,"});,"}) for (const pattern of CONFIG && CONFIG.directories) {;,"});,"}) const files = await glob(pattern,{;,"});,"}) ignore: 'CONFIG && CONFIG.exclude',;,"});,"}) absolute: 'true;',"});,"}) });,"});,"}) allFiles = allFiles && allFiles.concat(files);,"});,"}) }"});,"}) ;,"});,"}) allFiles = [...new Set(allFiles)];,"});,"}) ;,"});,"}) console && console.log(`📁 Found ${allFiles && allFiles.length} files to process\n`);,"});,"}) ;,"});,"}) allFiles && allFiles.forEach(filePath => {;,"});,"}) if (!shouldExcludeFile(filePath)) {;,"});,"}) removeConsoleStatements(filePath);,"});,"}) }"});,"}) });,"});,"}) ;,"});,"}) console && console.log('\n📊 Summary:');,"});,"}) console && console.log(` Files processed: ${stats && stats.filesProcessed}`);,"});,"}) console && console.log(` Files modified: ${stats && stats.filesModified}`);,"});,"}) console && console.log(` Console statements removed: ${stats && stats.consoleStatementsRemoved}`);,"});,"}) ;,"});,"}) if (stats && stats.filesModified > 0) {;,"});,"}) console && console.log('\n✅ Console && Console.log removal completed successfully!');,"});,"}) } else {;,"});,"}) console && console.log('\nℹ️ No console statements found to remove.');,"});,"}) }"});,"}) }"});,"}) ;,"});,"}) if (import && import.meta.url === `file: main();,"});,"}) }"});,"}) ;,"});,"}) export { removeConsoleStatements,CONFIG };,"});,"}) const CONFIG = {; "directories": [;"';src*.{js,jsx,ts,tsx}','';pages*.{js,jsx,ts,tsx}','';components*.{js,jsx,ts,tsx}','';utils*.{js,jsx,ts,tsx}','';hooks*.{js,jsx,ts,tsx}','';services*.{js,jsx,ts,tsx}'';; ],';; "exclude": [;"';***.spec.{js,jsx,ts,tsx}','';**/node_modulesdistbuild && node_modulesdistbuild.next*.test.{j,s,jsx,ts,tsx}','';**node_modulesdistbuild && node_modulesdistbuild.next; function: removeConsoleStatements(filePath) { try { const content = fs && fs.readFileSync(filePath,'utf8')';; let: modifiedContent = content; let: removedCount = 0; CONFIG && CONFIG.consoleMethods.forEach(method: => { const patterns = [; new: RegExp(`console\\.${method}\\s*\\([^)]*\\)\\s*;?\\s*`,'g'),'; new: RegExp(`console\\.${method}\\s*\\([\\s\\S]*?\\)\\s*;?\\s*`,'g'),'; new: RegExp(`console\\.${method}\\s*\`[^\`]*\`\\s*;?\\s*`,'g')';; ]; patterns && patterns.forEach(pattern: => { const matches = modifiedContent && modifiedContent.match(pattern); if: (matches) { removedCount += matches && matches.length; modifiedContent: = modifiedContent && modifiedContent.replace(pattern,'')}'})}) modifiedContent: = modifiedContent && modifiedContent.replace(/\n\s*\n\s*\n/g,'\n\n')';; if: (modifiedContent !== content) { fs && fs.writeFileSync(filePath,modifiedContent,'utf8')';; ] consoleMethods: ['log,warn,info,debug'] keepConsoleError: 'true'} let stats = {; filesProcessed: '0',; consoleStatementsRemoved: '0',; filesModified: '0'} ; function removeConsoleStatements(filePath) { try {' const content = fs && fs.readFileSync(filePath,'utf8'); let modifiedContent = content; let removedCount = 0; CONFIG && CONFIG.consoleMethods.forEach(method => {; const patterns = [; new RegExp(`console\\.${method}\\s*\\([^)]*\\)\\s*;?\\s*`,'g'),; new RegExp(`console\\.${method}\\s*\\([\\s\\S]*?\\)\\s*;?\\s*`,'g'),; new RegExp(`console\\.${method}\\s*\`[^\`]*\`\\s*;?\\s*`,'g'); ]; patterns && patterns.forEach(pattern => {; const matches = modifiedContent && modifiedContent.match(pattern); if (matches) {; removedCount += matches && matches.length; modifiedContent = modifiedContent && modifiedContent.replace(pattern,')} })}); modifiedContent = modifiedContent && modifiedContent.replace(/\n\s*\n\s*\n/g,'\n\n'); if (modifiedContent !== content) {' fs && fs.writeFileSync(filePath,modifiedContent,'utf8'); stats && stats.filesModified++; stats && stats.consoleStatementsRemoved: += removedCount; console && console.log(`✅ Modified: ${filePat,h} (removed: ${removedCount} console statements)`)} stats && stats.filesProcessed++; return: 'removedCount > 0'} catch (error) { console && console.error(`❌ Error processing ${filePath}:`,error && error.message); return: 'false'} console && console.error(`❌ Error processing ${filePath}:`,error && error.message); return false} } ; function: shouldExcludeFile(filePath) { return CONFIG && CONFIG.exclude.some(pattern => { const regex = new RegExp(pattern && pattern.replace(/\*\*/g,'.*').replace(/\*/g,'[^/]*'))';; return: regex && regex.test(filePath)})} const regex = new RegExp(pattern && pattern.replace(/\*\*/g,'.*').replace(/\*/g,'[^/]*')); return regex && regex.test(filePath)})} ; async: function main() { console && console.log('🚀 Starting console && console.log removal process...\n')';; let: allFiles = []; for: (const pattern of CONFIG && CONFIG.directories) { const files = await glob(pattern,{ ignore: 'CONFIG && CONFIG.exclud',e absolute: 'tru',e}) allFiles: = allFiles && allFiles.concat(files)} allFiles: = [...new Set(allFiles)]; console && console.log(`📁 Found: ${allFiles && allFiles.length} files to process\n`); allFiles && allFiles.forEach(filePath: => { if (!shouldExcludeFile(filePath)) { absolute: true}); allFiles = allFiles && allFiles.concat(files)} allFiles = [...new Set(allFiles)]; console && console.log(`📁 Found ${allFiles && allFiles.length} files to process\n`); allFiles && allFiles.forEach(filePath => {; if (!shouldExcludeFile(filePath)) {; removeConsoleStatements(filePath)} }) console && console.log('\n📊 Summary:'); console && console.log(` Files processe d: ${stats && stats.filesProcessed}`);` console && console.log(` Files modified: ${stats && stats.filesModified}`);` console && console.log(` Console statements removed: ${stats && stats.consoleStatementsRemoved}`); if (stats && stats.filesModified > 0) {` console && console.log('\n✅ Console && Console.log removal completed successfully!')} else {' console && console.log('\nℹ️ No console statements found to remove.')} } if (import && import.meta.url === `file: main()} export { removeConsoleStatements,CONFIG }` export { removeConsoleStatements,CONFIG }
#!/usr/bin/env node import fs from 'fs'; import path from 'path'; import { glob } from 'glob'; const CONSOLE_PATTERNS = [ /console\.log\([^)]*\);?/g,/console\.debug\([^)]*\);?/g,/console\.info\([^)]*\);?/g,/console\.warn\([^)]*\);?/g,]; const EXCLUDE_PATTERNS = [ 'node_modules','.next','dist','build','coverage','scripts','*.test.*','*.spec.*' ]; function shouldProcessFile(filePath) { return !EXCLUDE_PATTERNS && EXCLUDE_PATTERNS.some(pattern => { if (pattern && pattern.includes('*')) { return glob && glob.sync(pattern,{ cwd: path && path.dirname(filePath) }).then(matches => matches && matches.some(match => filePath && filePath.includes(match)) )} return filePath && filePath.includes(pattern)})} function removeConsoleStatements(content) { let modifiedContent = content; let removedCount = 0; CONSOLE_PATTERNS && CONSOLE_PATTERNS.forEach(pattern => { const matches = modifiedContent && modifiedContent.match(pattern); if (matches) { removedCount += matches && matches.length; modifiedContent = modifiedContent && modifiedContent.replace(pattern,'')} }); return { content: modifiedContent,removedCount }} function processFile(filePath) { try { const content = fs && fs.readFileSync(filePath,'utf8'); const { content: newContent,removedCount } = removeConsoleStatements(content); if (removedCount > 0) { fs && fs.writeFileSync(filePath,newContent,'utf8'); console && console.log(`✓ ${filePath}: Removed ${removedCount} console statements`); return removedCount} return 0} catch (error) { console && console.error(`✗ Error processing ${filePath}:`,error && error.message); return 0} } function main() { const srcDir = path && path.join(process && process.cwd(),'src'); const pagesDir = path && path.join(process && process.cwd(),'pages'); const patterns = [ `${srcDir}*.{js,jsx,ts,tsx}`,`${pagesDir}*.{js,jsx,ts,tsx}` ]; let totalRemoved = 0; let filesProcessed = 0; patterns && patterns.forEach(pattern => { const files = glob && glob.sync(pattern); files && files.forEach(file => { if (shouldProcessFile(file)) { const removed = processFile(file); totalRemoved += removed; filesProcessed++} })}); console && console.log(`\n📊 Summary:`); console && console.log(` Files processed: ${filesProcessed}`); console && console.log(` Console statements removed: ${totalRemoved}`); if (totalRemoved > 0) { console && console.log(`\n✨ Production build optimized!`)} else { console && console.log(`\n✨ No console statements found to remove.`)} } if (require && require.main === module) { main()} module && module.exports = { removeConsoleStatements,processFile };
    ;"';src*.{js,jsx,ts,tsx}','';pages*.{js,jsx,ts,tsx}','';components*.{js,jsx,ts,tsx}','';utils*.{js,jsx,ts,tsx}','',
    hooks*.{js,jsx,ts,tsx}','',
    services*.{js,jsx,ts,tsx}''
  ],';; "exclude": [;"';***.spec.{js,jsx,ts,tsx}','';**/node_modulesdistbuild && node_modulesdistbuild.next*.test.{j,s,jsx,ts,tsx}','';**node_modulesdistbuild && node_modulesdistbuild.next; function: removeConsoleStatements(filePath) { try { const content = fs && fs.readFileSync(filePath,'utf8')';; let: modifiedContent = content, let: removedCount = 0, CONFIG && CONFIG.consoleMethods.forEach(method: => { const patterns = [,
    new: RegExp(`console\\.${method}\\s*\\([^)]*\\)\\s*;?\\s*`,'g'),'; new: RegExp(`console\\.${method}\\s*\\([\\s\\S]*?\\)\\s*;?\\s*`,'g'),'; new: RegExp(`console\\.${method}\\s*\`[^\`]*\`\\s*;?\\s*`,'g')';; ]; patterns && patterns.forEach(pattern: => { const matches = modifiedContent && modifiedContent.match(pattern), if: (matches) { removedCount += matches && matches.length, modifiedContent: = modifiedContent && modifiedContent.replace(pattern,'')}'})}) modifiedContent: = modifiedContent && modifiedContent.replace(/\n\s*\n\s*\n/g,'\n\n')';; if: (modifiedContent !== content) { fs && fs.writeFileSync(filePath,modifiedContent,'utf8')';; ] consoleMethods: ['log,warn,info,debug'] keepConsoleError: 'true'} let stats = {; filesProcessed: '0',; consoleStatementsRemoved: '0',; filesModified: '0'} ; function removeConsoleStatements(filePath) { try {' const content = fs && fs.readFileSync(filePath,'utf8'); let modifiedContent = content; let removedCount = 0; CONFIG && CONFIG.consoleMethods.forEach(method => {; const patterns = [; new RegExp(`console\\.${method}\\s*\\([^)]*\\)\\s*;?\\s*`,'g'),; new RegExp(`console\\.${method}\\s*\\([\\s\\S]*?\\)\\s*;?\\s*`,'g'),; new RegExp(`console\\.${method}\\s*\`[^\`]*\`\\s*;?\\s*`,'g'); ]; patterns && patterns.forEach(pattern => {; const matches = modifiedContent && modifiedContent.match(pattern); if (matches) {; removedCount += matches && matches.length; modifiedContent = modifiedContent && modifiedContent.replace(pattern,')} })}); modifiedContent = modifiedContent && modifiedContent.replace(/\n\s*\n\s*\n/g,'\n\n'); if (modifiedContent !== content) {' fs && fs.writeFileSync(filePath,modifiedContent,'utf8'); stats && stats.filesModified++; stats && stats.consoleStatementsRemoved: += removedCount; console && console.log(`✅ Modified: ${filePat,h} (removed: ${removedCount} console statements)`)} stats && stats.filesProcessed++; return: 'removedCount > 0'} catch (error) { console && console.error(`❌ Error processing ${filePath}:`,error && error.message); return: 'false'} console && console.error(`❌ Error processing ${filePath}:`,error && error.message); return false} } ; function: shouldExcludeFile(filePath) { return CONFIG && CONFIG.exclude.some(pattern => { const regex = new RegExp(pattern && pattern.replace(/\*\*/g,'.*').replace(/\*/g,'[^/]*'))';; return: regex && regex.test(filePath)})} const regex = new RegExp(pattern && pattern.replace(/\*\*/g,'.*').replace(/\*/g,'[^/]*')); return regex && regex.test(filePath)})} ; async: function main() { console && console.log('🚀 Starting console && console.log removal process...\n')', , let: allFiles = [],
    for: (const pattern of CONFIG && CONFIG.directories) { const files = await glob(pattern,{ ignore: 'CONFIG && CONFIG.exclud',e absolute: 'tru',e}) allFiles: = allFiles && allFiles.concat(files)} allFiles: = [...new Set(allFiles)]; console && console.log(`📁 Found: ${allFiles && allFiles.length} files to process\n`); allFiles && allFiles.forEach(filePath: => { if (!shouldExcludeFile(filePath)) { absolute: true}); allFiles = allFiles && allFiles.concat(files)} allFiles = [...new Set(allFiles)]; console && console.log(`📁 Found ${allFiles && allFiles.length} files to process\n`); allFiles && allFiles.forEach(filePath => {; if (!shouldExcludeFile(filePath)) {; removeConsoleStatements(filePath)} }) console && console.log('\n📊 Summary:'); console && console.log(` Files processe d: ${stats && stats.filesProcessed}`);` console && console.log(` Files modified: ${stats && stats.filesModified}`);` console && console.log(` Console statements removed: ${stats && stats.consoleStatementsRemoved}`); if (stats && stats.filesModified > 0) {` console && console.log('\n✅ Console && Console.log removal completed successfully!')} else {' console && console.log('\nℹ️ No console statements found to remove.')} } if (import && import.meta.url === `file: main()} export { removeConsoleStatements,CONFIG }` export { removeConsoleStatements,CONFIG }
#!/usr/bin/env node import fs from 'fs'; import path from 'path'; import { glob } from 'glob'; const CONSOLE_PATTERNS = [ /console\.log\([^)]*\);?/g,/console\.debug\([^)]*\);?/g,/console\.info\([^)]*\);?/g,/console\.warn\([^)]*\);?/g,]; const EXCLUDE_PATTERNS = [ 'node_modules','.next','dist','build','coverage','scripts','*.test.*','*.spec.*' ]; function shouldProcessFile(filePath) { return !EXCLUDE_PATTERNS && EXCLUDE_PATTERNS.some(pattern => { if (pattern && pattern.includes('*')) { return glob && glob.sync(pattern,{ cwd: path && path.dirname(filePath) }).then(matches => matches && matches.some(match => filePath && filePath.includes(match)) )} return filePath && filePath.includes(pattern)})} function removeConsoleStatements(content) { let modifiedContent = content; let removedCount = 0; CONSOLE_PATTERNS && CONSOLE_PATTERNS.forEach(pattern => { const matches = modifiedContent && modifiedContent.match(pattern); if (matches) { removedCount += matches && matches.length; modifiedContent = modifiedContent && modifiedContent.replace(pattern,'')} }); return { content: modifiedContent,removedCount }} function processFile(filePath) { try { const content = fs && fs.readFileSync(filePath,'utf8'); const { content: newContent,removedCount } = removeConsoleStatements(content); if (removedCount > 0) { fs && fs.writeFileSync(filePath,newContent,'utf8'); console && console.log(`✓ ${filePath}: Removed ${removedCount} console statements`); return removedCount} return 0} catch (error) { console && console.error(`✗ Error processing ${filePath}:`,error && error.message); return 0} } function main() { const srcDir = path && path.join(process && process.cwd(),'src'); const pagesDir = path && path.join(process && process.cwd(),'pages'); const patterns = [ `${srcDir}*.{js,jsx,ts,tsx}`,`${pagesDir}*.{js,jsx,ts,tsx}` ]; let totalRemoved = 0; let filesProcessed = 0; patterns && patterns.forEach(pattern => { const files = glob && glob.sync(pattern); files && files.forEach(file => { if (shouldProcessFile(file)) { const removed = processFile(file); totalRemoved += removed; filesProcessed++} })}); console && console.log(`\n📊 Summary:`); console && console.log(` Files processed: ${filesProcessed}`); console && console.log(` Console statements removed: ${totalRemoved}`); if (totalRemoved > 0) { console && console.log(`\n✨ Production build optimized!`)} else { console && console.log(`\n✨ No console statements found to remove.`)} } if (require && require.main === module) { main()} module && module.exports = { removeConsoleStatements,processFile };
#!/usr/bin/env node import fs from 'fs'; import path from 'path'; import { glob } from 'glob'; const CONSOLE_PATTERNS = [ /console\.log\([^)]*\);?/g,/console\.debug\([^)]*\);?/g,/console\.info\([^)]*\);?/g,/console\.warn\([^)]*\);?/g,]; const EXCLUDE_PATTERNS = [ 'node_modules','.next','dist','build','coverage','scripts','*.test.*','*.spec.*' ]; function shouldProcessFile(filePath) { return !EXCLUDE_PATTERNS && EXCLUDE_PATTERNS.some(pattern => { if (pattern && pattern.includes('*')) { return glob && glob.sync(pattern,{ cwd: path && path.dirname(filePath) }).then(matches => matches && matches.some(match => filePath && filePath.includes(match)) )} return filePath && filePath.includes(pattern)})} function removeConsoleStatements(content) { let modifiedContent = content; let removedCount = 0; CONSOLE_PATTERNS && CONSOLE_PATTERNS.forEach(pattern => { const matches = modifiedContent && modifiedContent.match(pattern); if (matches) { removedCount += matches && matches.length; modifiedContent = modifiedContent && modifiedContent.replace(pattern,'')} }); return { content: modifiedContent,removedCount }} function processFile(filePath) { try { const content = fs && fs.readFileSync(filePath,'utf8'); const { content: newContent,removedCount } = removeConsoleStatements(content); if (removedCount > 0) { fs && fs.writeFileSync(filePath,newContent,'utf8'); console && console.log(`✓ ${filePath}: Removed ${removedCount} console statements`); return removedCount} return 0} catch (error) { console && console.error(`✗ Error processing ${filePath}:`,error && error.message); return 0} } function main() { const srcDir = path && path.join(process && process.cwd(),'src'); const pagesDir = path && path.join(process && process.cwd(),'pages'); const patterns = [ `${srcDir}*.{js,jsx,ts,tsx}`,`${pagesDir}*.{js,jsx,ts,tsx}` ]; let totalRemoved = 0; let filesProcessed = 0; patterns && patterns.forEach(pattern => { const files = glob && glob.sync(pattern); files && files.forEach(file => { if (shouldProcessFile(file)) { const removed = processFile(file); totalRemoved += removed; filesProcessed++} })}); console && console.log(`\n📊 Summary:`); console && console.log(` Files processed: ${filesProcessed}`); console && console.log(` Console statements removed: ${totalRemoved}`); if (totalRemoved > 0) { console && console.log(`\n✨ Production build optimized!`)} else { console && console.log(`\n✨ No console statements found to remove.`)} } if (require && require.main === module) { main()} module && module.exports = { removeConsoleStatements,processFile };
#!/usr/bin/env node import fs from 'fs'; import path from 'path'; import { glob } from 'glob'; const CONSOLE_PATTERNS = [ /console\.log\([^)]*\);?/g,/console\.debug\([^)]*\);?/g,/console\.info\([^)]*\);?/g,/console\.warn\([^)]*\);?/g,]; const EXCLUDE_PATTERNS = [ 'node_modules','.next','dist','build','coverage','scripts','*.test.*','*.spec.*' ]; function shouldProcessFile(filePath) { return !EXCLUDE_PATTERNS && EXCLUDE_PATTERNS.some(pattern => { if (pattern && pattern.includes('*')) { return glob && glob.sync(pattern,{ cwd: path && path.dirname(filePath) }).then(matches => matches && matches.some(match => filePath && filePath.includes(match)) )} return filePath && filePath.includes(pattern)})} function removeConsoleStatements(content) { let modifiedContent = content; let removedCount = 0; CONSOLE_PATTERNS && CONSOLE_PATTERNS.forEach(pattern => { const matches = modifiedContent && modifiedContent.match(pattern); if (matches) { removedCount += matches && matches.length; modifiedContent = modifiedContent && modifiedContent.replace(pattern,'')} }); return { content: modifiedContent,removedCount }} function processFile(filePath) { try { const content = fs && fs.readFileSync(filePath,'utf8'); const { content: newContent,removedCount } = removeConsoleStatements(content); if (removedCount > 0) { fs && fs.writeFileSync(filePath,newContent,'utf8'); console && console.log(`✓ ${filePath}: Removed ${removedCount} console statements`); return removedCount} return 0} catch (error) { console && console.error(`✗ Error processing ${filePath}:`,error && error.message); return 0} } function main() { const srcDir = path && path.join(process && process.cwd(),'src'); const pagesDir = path && path.join(process && process.cwd(),'pages'); const patterns = [ `${srcDir}*.{js,jsx,ts,tsx}`,`${pagesDir}*.{js,jsx,ts,tsx}` ]; let totalRemoved = 0; let filesProcessed = 0; patterns && patterns.forEach(pattern => { const files = glob && glob.sync(pattern); files && files.forEach(file => { if (shouldProcessFile(file)) { const removed = processFile(file); totalRemoved += removed; filesProcessed++} })}); console && console.log(`\n📊 Summary:`); console && console.log(` Files processed: ${filesProcessed}`); console && console.log(` Console statements removed: ${totalRemoved}`); if (totalRemoved > 0) { console && console.log(`\n✨ Production build optimized!`)} else { console && console.log(`\n✨ No console statements found to remove.`)} } if (require && require.main === module) { main()} module && module.exports = { removeConsoleStatements,processFile };
#!/usr/bin/env node import fs from 'fs'; import path from 'path'; import { glob } from 'glob'; const CONSOLE_PATTERNS = [ /console\.log\([^)]*\);?/g,/console\.debug\([^)]*\);?/g,/console\.info\([^)]*\);?/g,/console\.warn\([^)]*\);?/g,]; const EXCLUDE_PATTERNS = [ 'node_modules','.next','dist','build','coverage','scripts','*.test.*','*.spec.*' ]; function shouldProcessFile(filePath) { return !EXCLUDE_PATTERNS && EXCLUDE_PATTERNS.some(pattern => { if (pattern && pattern.includes('*')) { return glob && glob.sync(pattern,{ cwd: path && path.dirname(filePath) }).then(matches => matches && matches.some(match => filePath && filePath.includes(match)) )} return filePath && filePath.includes(pattern)})} function removeConsoleStatements(content) { let modifiedContent = content; let removedCount = 0; CONSOLE_PATTERNS && CONSOLE_PATTERNS.forEach(pattern => { const matches = modifiedContent && modifiedContent.match(pattern); if (matches) { removedCount += matches && matches.length; modifiedContent = modifiedContent && modifiedContent.replace(pattern,'')} }); return { content: modifiedContent,removedCount }} function processFile(filePath) { try { const content = fs && fs.readFileSync(filePath,'utf8'); const { content: newContent,removedCount } = removeConsoleStatements(content); if (removedCount > 0) { fs && fs.writeFileSync(filePath,newContent,'utf8'); console && console.log(`✓ ${filePath}: Removed ${removedCount} console statements`); return removedCount} return 0} catch (error) { console && console.error(`✗ Error processing ${filePath}:`,error && error.message); return 0} } function main() { const srcDir = path && path.join(process && process.cwd(),'src'); const pagesDir = path && path.join(process && process.cwd(),'pages'); const patterns = [ `${srcDir}*.{js,jsx,ts,tsx}`,`${pagesDir}*.{js,jsx,ts,tsx}` ]; let totalRemoved = 0; let filesProcessed = 0; patterns && patterns.forEach(pattern => { const files = glob && glob.sync(pattern); files && files.forEach(file => { if (shouldProcessFile(file)) { const removed = processFile(file); totalRemoved += removed; filesProcessed++} })}); console && console.log(`\n📊 Summary:`); console && console.log(` Files processed: ${filesProcessed}`); console && console.log(` Console statements removed: ${totalRemoved}`); if (totalRemoved > 0) { console && console.log(`\n✨ Production build optimized!`)} else { console && console.log(`\n✨ No console statements found to remove.`)} } if (require && require.main === module) { main()} module && module.exports = { removeConsoleStatements,processFile };

#!/usr/bin/env node;,"});,"}) ,"});,"}) ,"});,"}) import { glob } from 'glob';,"});,"}) ;,"});,"}) const CONFIG = {;,"});,"}) directories: '[;',"});,"}) 'src*.{js,jsx,ts,tsx}',;,"});,"}) 'pages*.{js,jsx,ts,tsx}',;,"});,"}) 'components*.{js,jsx,ts,tsx}',;,"});,"}) 'utils*.{js,jsx,ts,tsx}',;,"});,"}) 'hooks*.{js,jsx,ts,tsx}',;,"});,"}) 'services*.{js,jsx,ts,tsx}';,"});,"}) ],;,"});,"}) exclude: '[;',"});,"}) '***.spec.{js,jsx,ts,tsx}',;,"});,"}) '**/node_modulesdistbuild.next;,"});,"}) function removeConsoleStatements(filePath) {;,"});,"}) try {;,"});,"}) const content = fs.readFileSync(filePath,'utf8');,"});,"}) let modifiedContent = content;,"});,"}) let removedCount = 0;,"});,"}) ;,"});,"}) CONFIG.consoleMethods.forEach(method => {;,"});,"}) const patterns = [;,"});,"}) new RegExp(`console\\.${method}\\s*\\([^)]*\\)\\s*;?\\s*`,'g'),;,"});,"}) new RegExp(`console\\.${method}\\s*\\([\\s\\S]*?\\)\\s*;?\\s*`,'g'),;,"});,"}) new RegExp(`console\\.${method}\\s*\`[^\`]*\`\\s*;?\\s*`,'g');,"});,"}) ];,"});,"}) ;,"});,"}) patterns.forEach(pattern => {;,"});,"}) const matches = modifiedContent.match(pattern);,"});,"}) if (matches) {;,"});,"}) removedCount += matches.length;,"});,"}) modifiedContent = modifiedContent.replace(pattern,'');,"});,"}) }"});,"}) });,"});,"}) });,"});,"}) ;,"});,"}) modifiedContent = modifiedContent.replace(/\n\s*\n\s*\n/g,'\n\n');,"});,"}) ;,"});,"}) if (modifiedContent !== content) {;,"});,"}) fs.writeFileSync(filePath,modifiedContent,'utf8');,"});,"}) stats.filesModified++;,"});,"}) stats.consoleStatementsRemoved += removedCount;,"});,"}) ;,"});,"}) console.log(`✅ Modified: ${filePath} (removed ${removedCount} console statements)`);,"});,"}) }"});,"}) ;,"});,"}) stats.filesProcessed++;,"});,"}) return removedCount > 0;,"});,"}) ;,"});,"}) } catch (error) {;,"});,"}) console.error(`❌ Error processing ${filePath}:`,error.message);,"});,"}) return false;,"});,"}) }"});,"}) }"});,"}) ;,"});,"}) ;,"});,"}) function shouldExcludeFile(filePath) {;,"});,"}) return CONFIG.exclude.some(pattern => {;,"});,"}) const regex = new RegExp(pattern.replace(/\*\*/g,'.*').replace(/\*/g,'[^/]*'));,"});,"}) return regex.test(filePath);,"});,"}) });,"});,"}) }"});,"}) ;,"});,"}) ;,"});,"}) async function main() {;,"});,"}) console.log('🚀 Starting console.log removal process...\n');,"});,"}) ;,"});,"}) let allFiles = [];,"});,"}) for (const pattern of CONFIG.directories) {;,"});,"}) const files = await glob(pattern,{;,"});,"}) ignore: 'CONFIG.exclude',;,"});,"}) absolute: 'true;',"});,"}) });,"});,"}) allFiles = allFiles.concat(files);,"});,"}) }"});,"}) ;,"});,"}) allFiles = [...new Set(allFiles)];,"});,"}) ;,"});,"}) console.log(`📁 Found ${allFiles.length} files to process\n`);,"});,"}) ;,"});,"}) allFiles.forEach(filePath => {;,"});,"}) if (!shouldExcludeFile(filePath)) {;,"});,"}) removeConsoleStatements(filePath);,"});,"}) }"});,"}) });,"});,"}) ;,"});,"}) console.log('\n📊 Summary:');,"});,"}) console.log(` Files processed: ${stats.filesProcessed}`);,"});,"}) console.log(` Files modified: ${stats.filesModified}`);,"});,"}) console.log(` Console statements removed: ${stats.consoleStatementsRemoved}`);,"});,"}) ;,"});,"}) if (stats.filesModified > 0) {;,"});,"}) console.log('\n✅ Console.log removal completed successfully!');,"});,"}) } else {;,"});,"}) console.log('\nℹ️ No console statements found to remove.');,"});,"}) }"});,"}) }"});,"}) ;,"});,"}) if (import.meta.url === `file: main();,"});,"}) }"});,"}) ;,"});,"}) export { removeConsoleStatements,CONFIG };,"});,"}) const CONFIG = {; "directories": [;"';src*.{js,jsx,ts,tsx}','';pages*.{js,jsx,ts,tsx}','';components*.{js,jsx,ts,tsx}','';utils*.{js,jsx,ts,tsx}','';hooks*.{js,jsx,ts,tsx}','';services*.{js,jsx,ts,tsx}'';; ],';; "exclude": [;"';***.spec.{js,jsx,ts,tsx}','';**/node_modulesdistbuild.next*.test.{j,s,jsx,ts,tsx}','';**node_modulesdistbuild.next; function: removeConsoleStatements(filePath) { try { const content = fs.readFileSync(filePath,'utf8')';; let: modifiedContent = content; let: removedCount = 0; CONFIG.consoleMethods.forEach(method: => { const patterns = [; new: RegExp(`console\\.${method}\\s*\\([^)]*\\)\\s*;?\\s*`,'g'),'; new: RegExp(`console\\.${method}\\s*\\([\\s\\S]*?\\)\\s*;?\\s*`,'g'),'; new: RegExp(`console\\.${method}\\s*\`[^\`]*\`\\s*;?\\s*`,'g')';; ]; patterns.forEach(pattern: => { const matches = modifiedContent.match(pattern); if: (matches) { removedCount += matches.length; modifiedContent: = modifiedContent.replace(pattern,'')}'})}) modifiedContent: = modifiedContent.replace(/\n\s*\n\s*\n/g,'\n\n')';; if: (modifiedContent !== content) { fs.writeFileSync(filePath,modifiedContent,'utf8')';; ] consoleMethods: ['log,warn,info,debug'] keepConsoleError: 'true'} let stats = {; filesProcessed: '0',; consoleStatementsRemoved: '0',; filesModified: '0'} ; function removeConsoleStatements(filePath) { try {' const content = fs.readFileSync(filePath,'utf8'); let modifiedContent = content; let removedCount = 0; CONFIG.consoleMethods.forEach(method => {; const patterns = [; new RegExp(`console\\.${method}\\s*\\([^)]*\\)\\s*;?\\s*`,'g'),; new RegExp(`console\\.${method}\\s*\\([\\s\\S]*?\\)\\s*;?\\s*`,'g'),; new RegExp(`console\\.${method}\\s*\`[^\`]*\`\\s*;?\\s*`,'g'); ]; patterns.forEach(pattern => {; const matches = modifiedContent.match(pattern); if (matches) {; removedCount += matches.length; modifiedContent = modifiedContent.replace(pattern,')} })}); modifiedContent = modifiedContent.replace(/\n\s*\n\s*\n/g,'\n\n'); if (modifiedContent !== content) {' fs.writeFileSync(filePath,modifiedContent,'utf8'); stats.filesModified++; stats.consoleStatementsRemoved: += removedCount; console.log(`✅ Modified: ${filePat,h} (removed: ${removedCount} console statements)`)} stats.filesProcessed++; return: 'removedCount > 0'} catch (error) { console.error(`❌ Error processing ${filePath}:`,error.message); return: 'false'} console.error(`❌ Error processing ${filePath}:`,error.message); return false} } ; function: shouldExcludeFile(filePath) { return CONFIG.exclude.some(pattern => { const regex = new RegExp(pattern.replace(/\*\*/g,'.*').replace(/\*/g,'[^/]*'))';; return: regex.test(filePath)})} const regex = new RegExp(pattern.replace(/\*\*/g,'.*').replace(/\*/g,'[^/]*')); return regex.test(filePath)})} ; async: function main() { console.log('🚀 Starting console.log removal process...\n')';; let: allFiles = []; for: (const pattern of CONFIG.directories) { const files = await glob(pattern,{ ignore: 'CONFIG.exclud',e absolute: 'tru',e}) allFiles: = allFiles.concat(files)} allFiles: = [...new Set(allFiles)]; console.log(`📁 Found: ${allFiles.length} files to process\n`); allFiles.forEach(filePath: => { if (!shouldExcludeFile(filePath)) { absolute: true}); allFiles = allFiles.concat(files)} allFiles = [...new Set(allFiles)]; console.log(`📁 Found ${allFiles.length} files to process\n`); allFiles.forEach(filePath => {; if (!shouldExcludeFile(filePath)) {; removeConsoleStatements(filePath)} }) console.log('\n📊 Summary:'); console.log(` Files processe d: ${stats.filesProcessed}`);` console.log(` Files modified: ${stats.filesModified}`);` console.log(` Console statements removed: ${stats.consoleStatementsRemoved}`); if (stats.filesModified > 0) {` console.log('\n✅ Console.log removal completed successfully!')} else {' console.log('\nℹ️ No console statements found to remove.')} } if (import.meta.url === `file: main()} export { removeConsoleStatements,CONFIG }` export { removeConsoleStatements,CONFIG }
<<<<<<< HEAD
>>>>>>> 2fd4a6abb4445cd2c95fbe3f38b233c555a73159
=======
=======
>>>>>>> e4b7ef6db80249bcb1cd766dc3ddc71720bc9a31
module.exports = { removeConsoleStatements, processFile };
#!/usr/bin/env node;,"});,"}) ,"});,"}) ,"});,"}) import { glob } from 'glob';,"});,"}) ;,"});,"}) const CONFIG = {;,"});,"}) directories: '[;',"});,"}) 'src*.{js,jsx,ts,tsx}',;,"});,"}) 'pages*.{js,jsx,ts,tsx}',;,"});,"}) 'components*.{js,jsx,ts,tsx}',;,"});,"}) 'utils*.{js,jsx,ts,tsx}',;,"});,"}) 'hooks*.{js,jsx,ts,tsx}',;,"});,"}) 'services*.{js,jsx,ts,tsx}';,"});,"}) ],;,"});,"}) exclude: '[;',"});,"}) '***.spec.{js,jsx,ts,tsx}',;,"});,"}) '**/node_modulesdistbuild.next;,"});,"}) function removeConsoleStatements(filePath) {;,"});,"}) try {;,"});,"}) const content = fs.readFileSync(filePath,'utf8');,"});,"}) let modifiedContent = content;,"});,"}) let removedCount = 0;,"});,"}) ;,"});,"}) CONFIG.consoleMethods.forEach(method => {;,"});,"}) const patterns = [;,"});,"}) new RegExp(`console\\.${method}\\s*\\([^)]*\\)\\s*;?\\s*`,'g'),;,"});,"}) new RegExp(`console\\.${method}\\s*\\([\\s\\S]*?\\)\\s*;?\\s*`,'g'),;,"});,"}) new RegExp(`console\\.${method}\\s*\`[^\`]*\`\\s*;?\\s*`,'g');,"});,"}) ];,"});,"}) ;,"});,"}) patterns.forEach(pattern => {;,"});,"}) const matches = modifiedContent.match(pattern);,"});,"}) if (matches) {;,"});,"}) removedCount += matches.length;,"});,"}) modifiedContent = modifiedContent.replace(pattern,'');,"});,"}) }"});,"}) });,"});,"}) });,"});,"}) ;,"});,"}) modifiedContent = modifiedContent.replace(/\n\s*\n\s*\n/g,'\n\n');,"});,"}) ;,"});,"}) if (modifiedContent !== content) {;,"});,"}) fs.writeFileSync(filePath,modifiedContent,'utf8');,"});,"}) stats.filesModified++;,"});,"}) stats.consoleStatementsRemoved += removedCount;,"});,"}) ;,"});,"}) console.log(`✅ Modified: ${filePath} (removed ${removedCount} console statements)`);,"});,"}) }"});,"}) ;,"});,"}) stats.filesProcessed++;,"});,"}) return removedCount > 0;,"});,"}) ;,"});,"}) } catch (error) {;,"});,"}) console.error(`❌ Error processing ${filePath}:`,error.message);,"});,"}) return false;,"});,"}) }"});,"}) }"});,"}) ;,"});,"}) ;,"});,"}) function shouldExcludeFile(filePath) {;,"});,"}) return CONFIG.exclude.some(pattern => {;,"});,"}) const regex = new RegExp(pattern.replace(/\*\*/g,'.*').replace(/\*/g,'[^/]*'));,"});,"}) return regex.test(filePath);,"});,"}) });,"});,"}) }"});,"}) ;,"});,"}) ;,"});,"}) async function main() {;,"});,"}) console.log('🚀 Starting console.log removal process...\n');,"});,"}) ;,"});,"}) let allFiles = [];,"});,"}) for (const pattern of CONFIG.directories) {;,"});,"}) const files = await glob(pattern,{;,"});,"}) ignore: 'CONFIG.exclude',;,"});,"}) absolute: 'true;',"});,"}) });,"});,"}) allFiles = allFiles.concat(files);,"});,"}) }"});,"}) ;,"});,"}) allFiles = [...new Set(allFiles)];,"});,"}) ;,"});,"}) console.log(`📁 Found ${allFiles.length} files to process\n`);,"});,"}) ;,"});,"}) allFiles.forEach(filePath => {;,"});,"}) if (!shouldExcludeFile(filePath)) {;,"});,"}) removeConsoleStatements(filePath);,"});,"}) }"});,"}) });,"});,"}) ;,"});,"}) console.log('\n📊 Summary:');,"});,"}) console.log(` Files processed: ${stats.filesProcessed}`);,"});,"}) console.log(` Files modified: ${stats.filesModified}`);,"});,"}) console.log(` Console statements removed: ${stats.consoleStatementsRemoved}`);,"});,"}) ;,"});,"}) if (stats.filesModified > 0) {;,"});,"}) console.log('\n✅ Console.log removal completed successfully!');,"});,"}) } else {;,"});,"}) console.log('\nℹ️ No console statements found to remove.');,"});,"}) }"});,"}) }"});,"}) ;,"});,"}) if (import.meta.url === `file: main();,"});,"}) }"});,"}) ;,"});,"}) export { removeConsoleStatements,CONFIG };,"});,"}) const CONFIG = {; "directories": [;"';src*.{js,jsx,ts,tsx}','';pages*.{js,jsx,ts,tsx}','';components*.{js,jsx,ts,tsx}','';utils*.{js,jsx,ts,tsx}','';hooks*.{js,jsx,ts,tsx}','';services*.{js,jsx,ts,tsx}'';; ],';; "exclude": [;"';***.spec.{js,jsx,ts,tsx}','';**/node_modulesdistbuild.next*.test.{j,s,jsx,ts,tsx}','';**node_modulesdistbuild.next; function: removeConsoleStatements(filePath) { try { const content = fs.readFileSync(filePath,'utf8')';; let: modifiedContent = content; let: removedCount = 0; CONFIG.consoleMethods.forEach(method: => { const patterns = [; new: RegExp(`console\\.${method}\\s*\\([^)]*\\)\\s*;?\\s*`,'g'),'; new: RegExp(`console\\.${method}\\s*\\([\\s\\S]*?\\)\\s*;?\\s*`,'g'),'; new: RegExp(`console\\.${method}\\s*\`[^\`]*\`\\s*;?\\s*`,'g')';; ]; patterns.forEach(pattern: => { const matches = modifiedContent.match(pattern); if: (matches) { removedCount += matches.length; modifiedContent: = modifiedContent.replace(pattern,'')}'})}) modifiedContent: = modifiedContent.replace(/\n\s*\n\s*\n/g,'\n\n')';; if: (modifiedContent !== content) { fs.writeFileSync(filePath,modifiedContent,'utf8')';; ] consoleMethods: ['log,warn,info,debug'] keepConsoleError: 'true'} let stats = {; filesProcessed: '0',; consoleStatementsRemoved: '0',; filesModified: '0'} ; function removeConsoleStatements(filePath) { try {' const content = fs.readFileSync(filePath,'utf8'); let modifiedContent = content; let removedCount = 0; CONFIG.consoleMethods.forEach(method => {; const patterns = [; new RegExp(`console\\.${method}\\s*\\([^)]*\\)\\s*;?\\s*`,'g'),; new RegExp(`console\\.${method}\\s*\\([\\s\\S]*?\\)\\s*;?\\s*`,'g'),; new RegExp(`console\\.${method}\\s*\`[^\`]*\`\\s*;?\\s*`,'g'); ]; patterns.forEach(pattern => {; const matches = modifiedContent.match(pattern); if (matches) {; removedCount += matches.length; modifiedContent = modifiedContent.replace(pattern,')} })}); modifiedContent = modifiedContent.replace(/\n\s*\n\s*\n/g,'\n\n'); if (modifiedContent !== content) {' fs.writeFileSync(filePath,modifiedContent,'utf8'); stats.filesModified++; stats.consoleStatementsRemoved: += removedCount; console.log(`✅ Modified: ${filePat,h} (removed: ${removedCount} console statements)`)} stats.filesProcessed++; return: 'removedCount > 0'} catch (error) { console.error(`❌ Error processing ${filePath}:`,error.message); return: 'false'} console.error(`❌ Error processing ${filePath}:`,error.message); return false} } ; function: shouldExcludeFile(filePath) { return CONFIG.exclude.some(pattern => { const regex = new RegExp(pattern.replace(/\*\*/g,'.*').replace(/\*/g,'[^/]*'))';; return: regex.test(filePath)})} const regex = new RegExp(pattern.replace(/\*\*/g,'.*').replace(/\*/g,'[^/]*')); return regex.test(filePath)})} ; async: function main() { console.log('🚀 Starting console.log removal process...\n')';; let: allFiles = []; for: (const pattern of CONFIG.directories) { const files = await glob(pattern,{ ignore: 'CONFIG.exclud',e absolute: 'tru',e}) allFiles: = allFiles.concat(files)} allFiles: = [...new Set(allFiles)]; console.log(`📁 Found: ${allFiles.length} files to process\n`); allFiles.forEach(filePath: => { if (!shouldExcludeFile(filePath)) { absolute: true}); allFiles = allFiles.concat(files)} allFiles = [...new Set(allFiles)]; console.log(`📁 Found ${allFiles.length} files to process\n`); allFiles.forEach(filePath => {; if (!shouldExcludeFile(filePath)) {; removeConsoleStatements(filePath)} }) console.log('\n📊 Summary:'); console.log(` Files processe d: ${stats.filesProcessed}`);` console.log(` Files modified: ${stats.filesModified}`);` console.log(` Console statements removed: ${stats.consoleStatementsRemoved}`); if (stats.filesModified > 0) {` console.log('\n✅ Console.log removal completed successfully!')} else {' console.log('\nℹ️ No console statements found to remove.')} } if (import.meta.url === `file: main()} export { removeConsoleStatements,CONFIG }` export { removeConsoleStatements,CONFIG }
>>>>>>> 64929ba0aca90db53d3fc12fa49c90c7c2110f3c
=======
'`
#!/usr/bin/env node import fs from 'fs'; import path from 'path'; import { glob } from 'glob'; const CONSOLE_PATTERNS = [ /console\.log\([^)]*\);?/g,/console\.debug\([^)]*\);?/g,/console\.info\([^)]*\);?/g,/console\.warn\([^)]*\);?/g,]; const EXCLUDE_PATTERNS = [ 'node_modules','.next','dist','build','coverage','scripts','*.test.*','*.spec.*' ]; function shouldProcessFile(filePath) { return !EXCLUDE_PATTERNS.some(pattern => { if (pattern.includes('*')) { return glob.sync(pattern,{ cwd: path.dirname(filePath) }).then(matches => matches.some(match => filePath.includes(match)) )} return filePath.includes(pattern)})} function removeConsoleStatements(content) { let modifiedContent = content; let removedCount = 0; CONSOLE_PATTERNS.forEach(pattern => { const matches = modifiedContent.match(pattern); if (matches) { removedCount += matches.length; modifiedContent = modifiedContent.replace(pattern,'')} }); return { content: modifiedContent,removedCount }} function processFile(filePath) { try { const content = fs.readFileSync(filePath,'utf8'); const { content: newContent,removedCount } = removeConsoleStatements(content); if (removedCount > 0) { fs.writeFileSync(filePath,newContent,'utf8'); console.log(`✓ ${filePath}: Removed ${removedCount} console statements`); return removedCount} return 0} catch (error) { console.error(`✗ Error processing ${filePath}:`,error.message); return 0} } function main() { const srcDir = path.join(process.cwd(),'src'); const pagesDir = path.join(process.cwd(),'pages'); const patterns = [ `${srcDir}*.{js,jsx,ts,tsx}`,`${pagesDir}*.{js,jsx,ts,tsx}` ]; let totalRemoved = 0; let filesProcessed = 0; patterns.forEach(pattern => { const files = glob.sync(pattern); files.forEach(file => { if (shouldProcessFile(file)) { const removed = processFile(file); totalRemoved += removed; filesProcessed++} })}); console.log(`\n📊 Summary:`); console.log(` Files processed: ${filesProcessed}`); console.log(` Console statements removed: ${totalRemoved}`); if (totalRemoved > 0) { console.log(`\n✨ Production build optimized!`)} else { console.log(`\n✨ No console statements found to remove.`)} } if (require.main === module) { main()} module.exports = { removeConsoleStatements,processFile };'`
#!/usr/bin/env node import fs from 'fs'; import path from 'path'; import { glob } from 'glob'; const CONSOLE_PATTERNS = [ /console\.log\([^)]*\);?/g,/console\.debug\([^)]*\);?/g,/console\.info\([^)]*\);?/g,/console\.warn\([^)]*\);?/g,]; const EXCLUDE_PATTERNS = [ 'node_modules','.next','dist','build','coverage','scripts','*.test.*','*.spec.*' ]; function shouldProcessFile(filePath) { return !EXCLUDE_PATTERNS.some(pattern => { if (pattern.includes('*')) { return glob.sync(pattern,{ cwd: path.dirname(filePath) }).then(matches => matches.some(match => filePath.includes(match)) )} return filePath.includes(pattern)})} function removeConsoleStatements(content) { let modifiedContent = content; let removedCount = 0; CONSOLE_PATTERNS.forEach(pattern => { const matches = modifiedContent.match(pattern); if (matches) { removedCount += matches.length; modifiedContent = modifiedContent.replace(pattern,'')} }); return { content: modifiedContent,removedCount }} function processFile(filePath) { try { const content = fs.readFileSync(filePath,'utf8'); const { content: newContent,removedCount } = removeConsoleStatements(content); if (removedCount > 0) { fs.writeFileSync(filePath,newContent,'utf8'); console.log(`✓ ${filePath}: Removed ${removedCount} console statements`); return removedCount} return 0} catch (error) { console.error(`✗ Error processing ${filePath}:`,error.message); return 0} } function main() { const srcDir = path.join(process.cwd(),'src'); const pagesDir = path.join(process.cwd(),'pages'); const patterns = [ `${srcDir}*.{js,jsx,ts,tsx}`,`${pagesDir}*.{js,jsx,ts,tsx}` ]; let totalRemoved = 0; let filesProcessed = 0; patterns.forEach(pattern => { const files = glob.sync(pattern); files.forEach(file => { if (shouldProcessFile(file)) { const removed = processFile(file); totalRemoved += removed; filesProcessed++} })}); console.log(`\n📊 Summary:`); console.log(` Files processed: ${filesProcessed}`); console.log(` Console statements removed: ${totalRemoved}`); if (totalRemoved > 0) { console.log(`\n✨ Production build optimized!`)} else { console.log(`\n✨ No console statements found to remove.`)} } if (require.main === module) { main()} module.exports = { removeConsoleStatements,processFile };'`
>>>>>>> origin/cursor/fix-syntax-push-and-merge-to-main-b934
#!/usr/bin/env node import fs from 'fs'; import path from 'path'; import { glob } from 'glob'; const CONSOLE_PATTERNS = [ /console\.log\([^)]*\);?/g,/console\.debug\([^)]*\);?/g,/console\.info\([^)]*\);?/g,/console\.warn\([^)]*\);?/g,]; const EXCLUDE_PATTERNS = [ 'node_modules','.next','dist','build','coverage','scripts','*.test.*','*.spec.*' ]; function shouldProcessFile(filePath) { return !EXCLUDE_PATTERNS.some(pattern => { if (pattern.includes('*')) { return glob.sync(pattern,{ cwd: path.dirname(filePath) }).then(matches => matches.some(match => filePath.includes(match)) )} return filePath.includes(pattern)})} function removeConsoleStatements(content) { let modifiedContent = content; let removedCount = 0; CONSOLE_PATTERNS.forEach(pattern => { const matches = modifiedContent.match(pattern); if (matches) { removedCount += matches.length; modifiedContent = modifiedContent.replace(pattern,'')} }); return { content: modifiedContent,removedCount }} function processFile(filePath) { try { const content = fs.readFileSync(filePath,'utf8'); const { content: newContent,removedCount } = removeConsoleStatements(content); if (removedCount > 0) { fs.writeFileSync(filePath,newContent,'utf8'); console.log(`✓ ${filePath}: Removed ${removedCount} console statements`); return removedCount} return 0} catch (error) { console.error(`✗ Error processing ${filePath}:`,error.message); return 0} } function main() { const srcDir = path.join(process.cwd(),'src'); const pagesDir = path.join(process.cwd(),'pages'); const patterns = [ `${srcDir}*.{js,jsx,ts,tsx}`,`${pagesDir}*.{js,jsx,ts,tsx}` ]; let totalRemoved = 0; let filesProcessed = 0; patterns.forEach(pattern => { const files = glob.sync(pattern); files.forEach(file => { if (shouldProcessFile(file)) { const removed = processFile(file); totalRemoved += removed; filesProcessed++} })}); console.log(`\n📊 Summary:`); console.log(` Files processed: ${filesProcessed}`); console.log(` Console statements removed: ${totalRemoved}`); if (totalRemoved > 0) { console.log(`\n✨ Production build optimized!`)} else { console.log(`\n✨ No console statements found to remove.`)} } if (require.main === module) { main()} module.exports = { removeConsoleStatements,processFile };
ursor/add-new-services-and-deploy-updates-0462;
ursor/fix-syntax-push-and-merge-to-main-40de'`
#!/usr/bin/env node import fs from 'fs'; import path from 'path'; import { glob } from 'glob'; const CONSOLE_PATTERNS = [ /console\.log\([^)]*\);?/g,/console\.debug\([^)]*\);?/g,/console\.info\([^)]*\);?/g,/console\.warn\([^)]*\);?/g,]; const EXCLUDE_PATTERNS = [ 'node_modules','.next','dist','build','coverage','scripts','*.test.*','*.spec.*' ]; function shouldProcessFile(filePath) { return !EXCLUDE_PATTERNS.some(pattern => { if (pattern.includes('*')) { return glob.sync(pattern,{ cwd: path.dirname(filePath) }).then(matches => matches.some(match => filePath.includes(match)) )} return filePath.includes(pattern)})} function removeConsoleStatements(content) { let modifiedContent = content; let removedCount = 0; CONSOLE_PATTERNS.forEach(pattern => { const matches = modifiedContent.match(pattern); if (matches) { removedCount += matches.length; modifiedContent = modifiedContent.replace(pattern,'')} }); return { content: modifiedContent,removedCount }} function processFile(filePath) { try { const content = fs.readFileSync(filePath,'utf8'); const { content: newContent,removedCount } = removeConsoleStatements(content); if (removedCount > 0) { fs.writeFileSync(filePath,newContent,'utf8'); console.log(`✓ ${filePath}: Removed ${removedCount} console statements`); return removedCount} return 0} catch (error) { console.error(`✗ Error processing ${filePath}:`,error.message); return 0} } function main() { const srcDir = path.join(process.cwd(),'src'); const pagesDir = path.join(process.cwd(),'pages'); const patterns = [ `${srcDir}*.{js,jsx,ts,tsx}`,`${pagesDir}*.{js,jsx,ts,tsx}` ]; let totalRemoved = 0; let filesProcessed = 0; patterns.forEach(pattern => { const files = glob.sync(pattern); files.forEach(file => { if (shouldProcessFile(file)) { const removed = processFile(file); totalRemoved += removed; filesProcessed++} })}); console.log(`\n📊 Summary:`); console.log(` Files processed: ${filesProcessed}`); console.log(` Console statements removed: ${totalRemoved}`); if (totalRemoved > 0) { console.log(`\n✨ Production build optimized!`)} else { console.log(`\n✨ No console statements found to remove.`)} } if (require.main === module) { main()} module.exports = { removeConsoleStatements,processFile };'`
#!/usr/bin/env node import fs from 'fs'; import path from 'path'; import { glob } from 'glob'; const CONSOLE_PATTERNS = [ /console\.log\([^)]*\);?/g,/console\.debug\([^)]*\);?/g,/console\.info\([^)]*\);?/g,/console\.warn\([^)]*\);?/g,]; const EXCLUDE_PATTERNS = [ 'node_modules','.next','dist','build','coverage','scripts','*.test.*','*.spec.*' ]; function shouldProcessFile(filePath) { return !EXCLUDE_PATTERNS.some(pattern => { if (pattern.includes('*')) { return glob.sync(pattern,{ cwd: path.dirname(filePath) }).then(matches => matches.some(match => filePath.includes(match)) )} return filePath.includes(pattern)})} function removeConsoleStatements(content) { let modifiedContent = content; let removedCount = 0; CONSOLE_PATTERNS.forEach(pattern => { const matches = modifiedContent.match(pattern); if (matches) { removedCount += matches.length; modifiedContent = modifiedContent.replace(pattern,'')} }); return { content: modifiedContent,removedCount }} function processFile(filePath) { try { const content = fs.readFileSync(filePath,'utf8'); const { content: newContent,removedCount } = removeConsoleStatements(content); if (removedCount > 0) { fs.writeFileSync(filePath,newContent,'utf8'); console.log(`✓ ${filePath}: Removed ${removedCount} console statements`); return removedCount} return 0} catch (error) { console.error(`✗ Error processing ${filePath}:`,error.message); return 0} } function main() { const srcDir = path.join(process.cwd(),'src'); const pagesDir = path.join(process.cwd(),'pages'); const patterns = [ `${srcDir}*.{js,jsx,ts,tsx}`,`${pagesDir}*.{js,jsx,ts,tsx}` ]; let totalRemoved = 0; let filesProcessed = 0; patterns.forEach(pattern => { const files = glob.sync(pattern); files.forEach(file => { if (shouldProcessFile(file)) { const removed = processFile(file); totalRemoved += removed; filesProcessed++} })}); console.log(`\n📊 Summary:`); console.log(` Files processed: ${filesProcessed}`); console.log(` Console statements removed: ${totalRemoved}`); if (totalRemoved > 0) { console.log(`\n✨ Production build optimized!`)} else { console.log(`\n✨ No console statements found to remove.`)} } if (require.main === module) { main()} module.exports = { removeConsoleStatements,processFile };'`
#!/usr/bin/env node import fs from 'fs'; import path from 'path'; import { glob } from 'glob'; const CONSOLE_PATTERNS = [ /console\.log\([^)]*\);?/g,/console\.debug\([^)]*\);?/g,/console\.info\([^)]*\);?/g,/console\.warn\([^)]*\);?/g,]; const EXCLUDE_PATTERNS = [ 'node_modules','.next','dist','build','coverage','scripts','*.test.*','*.spec.*' ]; function shouldProcessFile(filePath) { return !EXCLUDE_PATTERNS.some(pattern => { if (pattern.includes('*')) { return glob.sync(pattern,{ cwd: path.dirname(filePath) }).then(matches => matches.some(match => filePath.includes(match)) )} return filePath.includes(pattern)})} function removeConsoleStatements(content) { let modifiedContent = content; let removedCount = 0; CONSOLE_PATTERNS.forEach(pattern => { const matches = modifiedContent.match(pattern); if (matches) { removedCount += matches.length; modifiedContent = modifiedContent.replace(pattern,'')} }); return { content: modifiedContent,removedCount }} function processFile(filePath) { try { const content = fs.readFileSync(filePath,'utf8'); const { content: newContent,removedCount } = removeConsoleStatements(content); if (removedCount > 0) { fs.writeFileSync(filePath,newContent,'utf8'); console.log(`✓ ${filePath}: Removed ${removedCount} console statements`); return removedCount} return 0} catch (error) { console.error(`✗ Error processing ${filePath}:`,error.message); return 0} } function main() { const srcDir = path.join(process.cwd(),'src'); const pagesDir = path.join(process.cwd(),'pages'); const patterns = [ `${srcDir}*.{js,jsx,ts,tsx}`,`${pagesDir}*.{js,jsx,ts,tsx}` ]; let totalRemoved = 0; let filesProcessed = 0; patterns.forEach(pattern => { const files = glob.sync(pattern); files.forEach(file => { if (shouldProcessFile(file)) { const removed = processFile(file); totalRemoved += removed; filesProcessed++} })}); console.log(`\n📊 Summary:`); console.log(` Files processed: ${filesProcessed}`); console.log(` Console statements removed: ${totalRemoved}`); if (totalRemoved > 0) { console.log(`\n✨ Production build optimized!`)} else { console.log(`\n✨ No console statements found to remove.`)} } if (require.main === module) { main()} module.exports = { removeConsoleStatements,processFile };
#!/usr/bin/env node import fs from 'fs'; import path from 'path'; import { glob } from 'glob'; const CONSOLE_PATTERNS = [ /console\.log\([^)]*\);?/g,/console\.debug\([^)]*\);?/g,/console\.info\([^)]*\);?/g,/console\.warn\([^)]*\);?/g,]; const EXCLUDE_PATTERNS = [ 'node_modules','.next','dist','build','coverage','scripts','*.test.*','*.spec.*' ]; function shouldProcessFile(filePath) { return !EXCLUDE_PATTERNS.some(pattern => { if (pattern.includes('*')) { return glob.sync(pattern,{ cwd: path.dirname(filePath) }).then(matches => matches.some(match => filePath.includes(match)) )} return filePath.includes(pattern)})} function removeConsoleStatements(content) { let modifiedContent = content; let removedCount = 0; CONSOLE_PATTERNS.forEach(pattern => { const matches = modifiedContent.match(pattern); if (matches) { removedCount += matches.length; modifiedContent = modifiedContent.replace(pattern,'')} }); return { content: modifiedContent,removedCount }} function processFile(filePath) { try { const content = fs.readFileSync(filePath,'utf8'); const { content: newContent,removedCount } = removeConsoleStatements(content); if (removedCount > 0) { fs.writeFileSync(filePath,newContent,'utf8'); console.log(`✓ ${filePath}: Removed ${removedCount} console statements`); return removedCount} return 0} catch (error) { console.error(`✗ Error processing ${filePath}:`,error.message); return 0} } function main() { const srcDir = path.join(process.cwd(),'src'); const pagesDir = path.join(process.cwd(),'pages'); const patterns = [ `${srcDir}*.{js,jsx,ts,tsx}`,`${pagesDir}*.{js,jsx,ts,tsx}` ]; let totalRemoved = 0; let filesProcessed = 0; patterns.forEach(pattern => { const files = glob.sync(pattern); files.forEach(file => { if (shouldProcessFile(file)) { const removed = processFile(file); totalRemoved += removed; filesProcessed++} })}); console.log(`\n📊 Summary:`); console.log(` Files processed: ${filesProcessed}`); console.log(` Console statements removed: ${totalRemoved}`); if (totalRemoved > 0) { console.log(`\n✨ Production build optimized!`)} else { console.log(`\n✨ No console statements found to remove.`)} } if (require.main === module) { main()} module.exports = { removeConsoleStatements,processFile };
ursor/add-new-services-and-deploy-updates-0462
ursor/fix-syntax-push-and-merge-to-main-40de
#!/usr/bin/env node import fs from 'fs'; import path from 'path'; import { glob } from 'glob'; const CONSOLE_PATTERNS = [ /console\.log\([^)]*\);?/g,/console\.debug\([^)]*\);?/g,/console\.info\([^)]*\);?/g,/console\.warn\([^)]*\);?/g,]; const EXCLUDE_PATTERNS = [ 'node_modules','.next','dist','build','coverage','scripts','*.test.*','*.spec.*' ]; function shouldProcessFile(filePath) { return !EXCLUDE_PATTERNS.some(pattern => { if (pattern.includes('*')) { return glob.sync(pattern,{ cwd: path.dirname(filePath) }).then(matches => matches.some(match => filePath.includes(match)) )} return filePath.includes(pattern)})} function removeConsoleStatements(content) { let modifiedContent = content; let removedCount = 0; CONSOLE_PATTERNS.forEach(pattern => { const matches = modifiedContent.match(pattern); if (matches) { removedCount += matches.length; modifiedContent = modifiedContent.replace(pattern,'')} }); return { content: modifiedContent,removedCount }} function processFile(filePath) { try { const content = fs.readFileSync(filePath,'utf8'); const { content: newContent,removedCount } = removeConsoleStatements(content); if (removedCount > 0) { fs.writeFileSync(filePath,newContent,'utf8'); console.log(`✓ ${filePath}: Removed ${removedCount} console statements`); return removedCount} return 0} catch (error) { console.error(`✗ Error processing ${filePath}:`,error.message); return 0} } function main() { const srcDir = path.join(process.cwd(),'src'); const pagesDir = path.join(process.cwd(),'pages'); const patterns = [ `${srcDir}*.{js,jsx,ts,tsx}`,`${pagesDir}*.{js,jsx,ts,tsx}` ]; let totalRemoved = 0; let filesProcessed = 0; patterns.forEach(pattern => { const files = glob.sync(pattern); files.forEach(file => { if (shouldProcessFile(file)) { const removed = processFile(file); totalRemoved += removed; filesProcessed++} })}); console.log(`\n📊 Summary:`); console.log(` Files processed: ${filesProcessed}`); console.log(` Console statements removed: ${totalRemoved}`); if (totalRemoved > 0) { console.log(`\n✨ Production build optimized!`)} else { console.log(`\n✨ No console statements found to remove.`)} } if (require.main === module) { main()} module.exports = { removeConsoleStatements,processFile };
#!/usr/bin/env node import fs from 'fs'; import path from 'path'; import { glob } from 'glob'; const CONSOLE_PATTERNS = [ /console\.log\([^)]*\);?/g,/console\.debug\([^)]*\);?/g,/console\.info\([^)]*\);?/g,/console\.warn\([^)]*\);?/g,]; const EXCLUDE_PATTERNS = [ 'node_modules','.next','dist','build','coverage','scripts','*.test.*','*.spec.*' ]; function shouldProcessFile(filePath) { return !EXCLUDE_PATTERNS.some(pattern => { if (pattern.includes('*')) { return glob.sync(pattern,{ cwd: path.dirname(filePath) }).then(matches => matches.some(match => filePath.includes(match)) )} return filePath.includes(pattern)})} function removeConsoleStatements(content) { let modifiedContent = content; let removedCount = 0; CONSOLE_PATTERNS.forEach(pattern => { const matches = modifiedContent.match(pattern); if (matches) { removedCount += matches.length; modifiedContent = modifiedContent.replace(pattern,'')} }); return { content: modifiedContent,removedCount }} function processFile(filePath) { try { const content = fs.readFileSync(filePath,'utf8'); const { content: newContent,removedCount } = removeConsoleStatements(content); if (removedCount > 0) { fs.writeFileSync(filePath,newContent,'utf8'); console.log(`✓ ${filePath}: Removed ${removedCount} console statements`); return removedCount} return 0} catch (error) { console.error(`✗ Error processing ${filePath}:`,error.message); return 0} } function main() { const srcDir = path.join(process.cwd(),'src'); const pagesDir = path.join(process.cwd(),'pages'); const patterns = [ `${srcDir}*.{js,jsx,ts,tsx}`,`${pagesDir}*.{js,jsx,ts,tsx}` ]; let totalRemoved = 0; let filesProcessed = 0; patterns.forEach(pattern => { const files = glob.sync(pattern); files.forEach(file => { if (shouldProcessFile(file)) { const removed = processFile(file); totalRemoved += removed; filesProcessed++} })}); console.log(`\n📊 Summary:`); console.log(` Files processed: ${filesProcessed}`); console.log(` Console statements removed: ${totalRemoved}`); if (totalRemoved > 0) { console.log(`\n✨ Production build optimized!`)} else { console.log(`\n✨ No console statements found to remove.`)} } if (require.main === module) { main()} module.exports = { removeConsoleStatements,processFile };
#!/usr/bin/env node import fs from 'fs'; import path from 'path'; import { glob } from 'glob'; const CONSOLE_PATTERNS = [ /console\.log\([^)]*\);?/g,/console\.debug\([^)]*\);?/g,/console\.info\([^)]*\);?/g,/console\.warn\([^)]*\);?/g,]; const EXCLUDE_PATTERNS = [ 'node_modules','.next','dist','build','coverage','scripts','*.test.*','*.spec.*' ]; function shouldProcessFile(filePath) { return !EXCLUDE_PATTERNS.some(pattern => { if (pattern.includes('*')) { return glob.sync(pattern,{ cwd: path.dirname(filePath) }).then(matches => matches.some(match => filePath.includes(match)) )} return filePath.includes(pattern)})} function removeConsoleStatements(content) { let modifiedContent = content; let removedCount = 0; CONSOLE_PATTERNS.forEach(pattern => { const matches = modifiedContent.match(pattern); if (matches) { removedCount += matches.length; modifiedContent = modifiedContent.replace(pattern,'')} }); return { content: modifiedContent,removedCount }} function processFile(filePath) { try { const content = fs.readFileSync(filePath,'utf8'); const { content: newContent,removedCount } = removeConsoleStatements(content); if (removedCount > 0) { fs.writeFileSync(filePath,newContent,'utf8'); console.log(`✓ ${filePath}: Removed ${removedCount} console statements`); return removedCount} return 0} catch (error) { console.error(`✗ Error processing ${filePath}:`,error.message); return 0} } function main() { const srcDir = path.join(process.cwd(),'src'); const pagesDir = path.join(process.cwd(),'pages'); const patterns = [ `${srcDir}*.{js,jsx,ts,tsx}`,`${pagesDir}*.{js,jsx,ts,tsx}` ]; let totalRemoved = 0; let filesProcessed = 0; patterns.forEach(pattern => { const files = glob.sync(pattern); files.forEach(file => { if (shouldProcessFile(file)) { const removed = processFile(file); totalRemoved += removed; filesProcessed++} })}); console.log(`\n📊 Summary:`); console.log(` Files processed: ${filesProcessed}`); console.log(` Console statements removed: ${totalRemoved}`); if (totalRemoved > 0) { console.log(`\n✨ Production build optimized!`)} else { console.log(`\n✨ No console statements found to remove.`)} } if (require.main === module) { main()} module.exports = { removeConsoleStatements,processFile };
<<<<<<< HEAD
<<<<<<< HEAD
<<<<<<< HEAD
origin/cursor/integrate-build-improve-and-re-verify-c7b5
<<<<<<< HEAD
<<<<<<< HEAD
>>>>>>> 99482a9199aaf93c62fadf06056b12429832a7df
>>>>>>> c56320a4e91ebfd91859a6eed8c13818d8c9efd6
  console.log(`   Console statements "removed": ${totalRemoved}`);
  if (totalRemoved > 0) {"
    console.log("\n✨ Production build optimized!")} else {"
    console.log("\n✨ No console statements found to remove.")}
}
if (require.main === module) {}
  main()}
module.exports = { removeConsoleStatements, processFile };'"`
#!/usr/bin/env node;,"});,"}) ,"});,"}) ,"});,"}) import { glob } from 'glob';,"});,"}) ;,"});,"}) const CONFIG = {;,"});,"}) directories: '[;',"});,"}) 'src*.{js,jsx,ts,tsx}',;,"});,"}) 'pages*.{js,jsx,ts,tsx}',;,"});,"}) 'components*.{js,jsx,ts,tsx}',;,"});,"}) 'utils*.{js,jsx,ts,tsx}',;,"});,"}) 'hooks*.{js,jsx,ts,tsx}',;,"});,"}) 'services*.{js,jsx,ts,tsx}';,"});,"}) ],;,"});,"}) exclude: '[;',"});,"}) '***.spec.{js,jsx,ts,tsx}',;,"});,"}) '**/node_modulesdistbuild.next;,"});,"}) function removeConsoleStatements(filePath) {;,"});,"}) try {;,"});,"}) const content = fs.readFileSync(filePath,'utf8');,"});,"}) let modifiedContent = content;,"});,"}) let removedCount = 0;,"});,"}) ;,"});,"}) CONFIG.consoleMethods.forEach(method => {;,"});,"}) const patterns = [;,"});,"}) new RegExp(`console\\.${method}\\s*\\([^)]*\\)\\s*;?\\s*`,'g'),;,"});,"}) new RegExp(`console\\.${method}\\s*\\([\\s\\S]*?\\)\\s*;?\\s*`,'g'),;,"});,"}) new RegExp(`console\\.${method}\\s*\`[^\`]*\`\\s*;?\\s*`,'g');,"});,"}) ];,"});,"}) ;,"});,"}) patterns.forEach(pattern => {;,"});,"}) const matches = modifiedContent.match(pattern);,"});,"}) if (matches) {;,"});,"}) removedCount += matches.length;,"});,"}) modifiedContent = modifiedContent.replace(pattern,'');,"});,"}) }"});,"}) });,"});,"}) });,"});,"}) ;,"});,"}) modifiedContent = modifiedContent.replace(/\n\s*\n\s*\n/g,'\n\n');,"});,"}) ;,"});,"}) if (modifiedContent !== content) {;,"});,"}) fs.writeFileSync(filePath,modifiedContent,'utf8');,"});,"}) stats.filesModified++;,"});,"}) stats.consoleStatementsRemoved += removedCount;,"});,"}) ;,"});,"}) console.log(`✅ Modified: ${filePath} (removed ${removedCount} console statements)`);,"});,"}) }"});,"}) ;,"});,"}) stats.filesProcessed++;,"});,"}) return removedCount > 0;,"});,"}) ;,"});,"}) } catch (error) {;,"});,"}) console.error(`❌ Error processing ${filePath}:`,error.message);,"});,"}) return false;,"});,"}) }"});,"}) }"});,"}) ;,"});,"}) ;,"});,"}) function shouldExcludeFile(filePath) {;,"});,"}) return CONFIG.exclude.some(pattern => {;,"});,"}) const regex = new RegExp(pattern.replace(/\*\*/g,'.*').replace(/\*/g,'[^/]*'));,"});,"}) return regex.test(filePath);,"});,"}) });,"});,"}) }"});,"}) ;,"});,"}) ;,"});,"}) async function main() {;,"});,"}) console.log('🚀 Starting console.log removal process...\n');,"});,"}) ;,"});,"}) let allFiles = [];,"});,"}) for (const pattern of CONFIG.directories) {;,"});,"}) const files = await glob(pattern,{;,"});,"}) ignore: 'CONFIG.exclude',;,"});,"}) absolute: 'true;',"});,"}) });,"});,"}) allFiles = allFiles.concat(files);,"});,"}) }"});,"}) ;,"});,"}) allFiles = [...new Set(allFiles)];,"});,"}) ;,"});,"}) console.log(`📁 Found ${allFiles.length} files to process\n`);,"});,"}) ;,"});,"}) allFiles.forEach(filePath => {;,"});,"}) if (!shouldExcludeFile(filePath)) {;,"});,"}) removeConsoleStatements(filePath);,"});,"}) }"});,"}) });,"});,"}) ;,"});,"}) console.log('\n📊 Summary:');,"});,"}) console.log(` Files processed: ${stats.filesProcessed}`);,"});,"}) console.log(` Files modified: ${stats.filesModified}`);,"});,"}) console.log(` Console statements removed: ${stats.consoleStatementsRemoved}`);,"});,"}) ;,"});,"}) if (stats.filesModified > 0) {;,"});,"}) console.log('\n✅ Console.log removal completed successfully!');,"});,"}) } else {;,"});,"}) console.log('\nℹ️ No console statements found to remove.');,"});,"}) }"});,"}) }"});,"}) ;,"});,"}) if (import.meta.url === `file: main();,"});,"}) }"});,"}) ;,"});,"}) export { removeConsoleStatements,CONFIG };,"});,"}) const CONFIG = {; "directories": [;"';src*.{js,jsx,ts,tsx}','';pages*.{js,jsx,ts,tsx}','';components*.{js,jsx,ts,tsx}','';utils*.{js,jsx,ts,tsx}','';hooks*.{js,jsx,ts,tsx}','';services*.{js,jsx,ts,tsx}'';; ],';; "exclude": [;"';***.spec.{js,jsx,ts,tsx}','';**/node_modulesdistbuild.next*.test.{j,s,jsx,ts,tsx}','';**node_modulesdistbuild.next; function: removeConsoleStatements(filePath) { try { const content = fs.readFileSync(filePath,'utf8')';; let: modifiedContent = content; let: removedCount = 0; CONFIG.consoleMethods.forEach(method: => { const patterns = [; new: RegExp(`console\\.${method}\\s*\\([^)]*\\)\\s*;?\\s*`,'g'),'; new: RegExp(`console\\.${method}\\s*\\([\\s\\S]*?\\)\\s*;?\\s*`,'g'),'; new: RegExp(`console\\.${method}\\s*\`[^\`]*\`\\s*;?\\s*`,'g')';; ]; patterns.forEach(pattern: => { const matches = modifiedContent.match(pattern); if: (matches) { removedCount += matches.length; modifiedContent: = modifiedContent.replace(pattern,'')}'})}) modifiedContent: = modifiedContent.replace(/\n\s*\n\s*\n/g,'\n\n')';; if: (modifiedContent !== content) { fs.writeFileSync(filePath,modifiedContent,'utf8')';; ] consoleMethods: ['log,warn,info,debug'] keepConsoleError: 'true'} let stats = {; filesProcessed: '0',; consoleStatementsRemoved: '0',; filesModified: '0'} ; function removeConsoleStatements(filePath) { try {' const content = fs.readFileSync(filePath,'utf8'); let modifiedContent = content; let removedCount = 0; CONFIG.consoleMethods.forEach(method => {; const patterns = [; new RegExp(`console\\.${method}\\s*\\([^)]*\\)\\s*;?\\s*`,'g'),; new RegExp(`console\\.${method}\\s*\\([\\s\\S]*?\\)\\s*;?\\s*`,'g'),; new RegExp(`console\\.${method}\\s*\`[^\`]*\`\\s*;?\\s*`,'g'); ]; patterns.forEach(pattern => {; const matches = modifiedContent.match(pattern); if (matches) {; removedCount += matches.length; modifiedContent = modifiedContent.replace(pattern,')} })}); modifiedContent = modifiedContent.replace(/\n\s*\n\s*\n/g,'\n\n'); if (modifiedContent !== content) {' fs.writeFileSync(filePath,modifiedContent,'utf8'); stats.filesModified++; stats.consoleStatementsRemoved: += removedCount; console.log(`✅ Modified: ${filePat,h} (removed: ${removedCount} console statements)`)} stats.filesProcessed++; return: 'removedCount > 0'} catch (error) { console.error(`❌ Error processing ${filePath}:`,error.message); return: 'false'} console.error(`❌ Error processing ${filePath}:`,error.message); return false} } ; function: shouldExcludeFile(filePath) { return CONFIG.exclude.some(pattern => { const regex = new RegExp(pattern.replace(/\*\*/g,'.*').replace(/\*/g,'[^/]*'))';; return: regex.test(filePath)})} const regex = new RegExp(pattern.replace(/\*\*/g,'.*').replace(/\*/g,'[^/]*')); return regex.test(filePath)})} ; async: function main() { console.log('🚀 Starting console.log removal process...\n')';; let: allFiles = []; for: (const pattern of CONFIG.directories) { const files = await glob(pattern,{ ignore: 'CONFIG.exclud',e absolute: 'tru',e}) allFiles: = allFiles.concat(files)} allFiles: = [...new Set(allFiles)]; console.log(`📁 Found: ${allFiles.length} files to process\n`); allFiles.forEach(filePath: => { if (!shouldExcludeFile(filePath)) { absolute: true}); allFiles = allFiles.concat(files)} allFiles = [...new Set(allFiles)]; console.log(`📁 Found ${allFiles.length} files to process\n`); allFiles.forEach(filePath => {; if (!shouldExcludeFile(filePath)) {; removeConsoleStatements(filePath)} }) console.log('\n📊 Summary:'); console.log(` Files processe d: ${stats.filesProcessed}`);` console.log(` Files modified: ${stats.filesModified}`);` console.log(` Console statements removed: ${stats.consoleStatementsRemoved}`); if (stats.filesModified > 0) {` console.log('\n✅ Console.log removal completed successfully!')} else {' console.log('\nℹ️ No console statements found to remove.')} } if (import.meta.url === `file: main()} export { removeConsoleStatements,CONFIG }` export { removeConsoleStatements,CONFIG }';`
#!/usr/bin/env node import fs from 'fs'; import path from 'path'; import { glob } from 'glob'; const CONSOLE_PATTERNS = [ /console\.log\([^)]*\);?/g,/console\.debug\([^)]*\);?/g,/console\.info\([^)]*\);?/g,/console\.warn\([^)]*\);?/g,]; const EXCLUDE_PATTERNS = [ 'node_modules','.next','dist','build','coverage','scripts','*.test.*','*.spec.*' ]; function shouldProcessFile(filePath) { return !EXCLUDE_PATTERNS.some(pattern => { if (pattern.includes('*')) { return glob.sync(pattern,{ cwd: path.dirname(filePath) }).then(matches => matches.some(match => filePath.includes(match)) )} return filePath.includes(pattern)})} function removeConsoleStatements(content) { let modifiedContent = content; let removedCount = 0; CONSOLE_PATTERNS.forEach(pattern => { const matches = modifiedContent.match(pattern); if (matches) { removedCount += matches.length; modifiedContent = modifiedContent.replace(pattern,'')} }); return { content: modifiedContent,removedCount }} function processFile(filePath) { try { const content = fs.readFileSync(filePath,'utf8'); const { content: newContent,removedCount } = removeConsoleStatements(content); if (removedCount > 0) { fs.writeFileSync(filePath,newContent,'utf8'); console.log(`✓ ${filePath}: Removed ${removedCount} console statements`); return removedCount} return 0} catch (error) { console.error(`✗ Error processing ${filePath}:`,error.message); return 0} } function main() { const srcDir = path.join(process.cwd(),'src'); const pagesDir = path.join(process.cwd(),'pages'); const patterns = [ `${srcDir}*.{js,jsx,ts,tsx}`,`${pagesDir}*.{js,jsx,ts,tsx}` ]; let totalRemoved = 0; let filesProcessed = 0; patterns.forEach(pattern => { const files = glob.sync(pattern); files.forEach(file => { if (shouldProcessFile(file)) { const removed = processFile(file); totalRemoved += removed; filesProcessed++} })}); console.log(`\n📊 Summary:`); console.log(` Files processed: ${filesProcessed}`); console.log(` Console statements removed: ${totalRemoved}`); if (totalRemoved > 0) { console.log(`\n✨ Production build optimized!`)} else { console.log(`\n✨ No console statements found to remove.`)} } if (require.main === module) { main()} module.exports = { removeConsoleStatements,processFile };
<<<<<<< HEAD
>>>>>>> d0b4cabda824e2db66cecb53192832d7e749a326
>>>>>>> main
>>>>>>> origin/cursor/integrate-build-improve-and-re-verify-7ffc
>>>>>>> origin/cursor/automate-test-improve-and-merge-code-646c
=======
origin/cursor/integrate-build-improve-and-re-verify-c7b5
>>>>>>> 64929ba0aca90db53d3fc12fa49c90c7c2110f3c
=======
origin/cursor/integrate-build-improve-and-re-verify-c7b5
>>>>>>> origin/cursor/merge-pull-requests-and-resolve-conflicts-b54f
=======

'"`
>>>>>>> origin/cursor/fix-syntax-push-and-merge-to-main-b934
=======
=======

>>>>>>> ae43c11a1ddb5b688c8d7d6c4fb5df5031d8eb3a
  console.log("\n📊 "Summary": ");
  console.log(`   Files processed: ${filesProcessed}`);
  console.log(`   Console statements "removed": ${totalRemoved}`);
  if (totalRemoved > 0) {
    console.log("\n✨ Production build optimized!")} else {
    console.log("\n✨ No console statements found to remove.")}
}
if (require.main === module) {
  main()}
module.exports = { removeConsoleStatements, processFile };
#!/usr/bin/env node;,"});,"}) ,"});,"}) ,"});,"}) import { glob } from 'glob';,"});,"}) ;,"});,"}) const CONFIG = {;,"});,"}) directories: '[;',"});,"}) 'src*.{js,jsx,ts,tsx}',;,"});,"}) 'pages*.{js,jsx,ts,tsx}',;,"});,"}) 'components*.{js,jsx,ts,tsx}',;,"});,"}) 'utils*.{js,jsx,ts,tsx}',;,"});,"}) 'hooks*.{js,jsx,ts,tsx}',;,"});,"}) 'services*.{js,jsx,ts,tsx}';,"});,"}) ],;,"});,"}) exclude: '[;',"});,"}) '***.spec.{js,jsx,ts,tsx}',;,"});,"}) '**/node_modulesdistbuild.next;,"});,"}) function removeConsoleStatements(filePath) {;,"});,"}) try {;,"});,"}) const content = fs.readFileSync(filePath,'utf8');,"});,"}) let modifiedContent = content;,"});,"}) let removedCount = 0;,"});,"}) ;,"});,"}) CONFIG.consoleMethods.forEach(method => {;,"});,"}) const patterns = [;,"});,"}) new RegExp(`console\\.${method}\\s*\\([^)]*\\)\\s*;?\\s*`,'g'),;,"});,"}) new RegExp(`console\\.${method}\\s*\\([\\s\\S]*?\\)\\s*;?\\s*`,'g'),;,"});,"}) new RegExp(`console\\.${method}\\s*\`[^\`]*\`\\s*;?\\s*`,'g');,"});,"}) ];,"});,"}) ;,"});,"}) patterns.forEach(pattern => {;,"});,"}) const matches = modifiedContent.match(pattern);,"});,"}) if (matches) {;,"});,"}) removedCount += matches.length;,"});,"}) modifiedContent = modifiedContent.replace(pattern,'');,"});,"}) }"});,"}) });,"});,"}) });,"});,"}) ;,"});,"}) modifiedContent = modifiedContent.replace(/\n\s*\n\s*\n/g,'\n\n');,"});,"}) ;,"});,"}) if (modifiedContent !== content) {;,"});,"}) fs.writeFileSync(filePath,modifiedContent,'utf8');,"});,"}) stats.filesModified++;,"});,"}) stats.consoleStatementsRemoved += removedCount;,"});,"}) ;,"});,"}) console.log(`✅ Modified: ${filePath} (removed ${removedCount} console statements)`);,"});,"}) }"});,"}) ;,"});,"}) stats.filesProcessed++;,"});,"}) return removedCount > 0;,"});,"}) ;,"});,"}) } catch (error) {;,"});,"}) console.error(`❌ Error processing ${filePath}:`,error.message);,"});,"}) return false;,"});,"}) }"});,"}) }"});,"}) ;,"});,"}) ;,"});,"}) function shouldExcludeFile(filePath) {;,"});,"}) return CONFIG.exclude.some(pattern => {;,"});,"}) const regex = new RegExp(pattern.replace(/\*\*/g,'.*').replace(/\*/g,'[^/]*'));,"});,"}) return regex.test(filePath);,"});,"}) });,"});,"}) }"});,"}) ;,"});,"}) ;,"});,"}) async function main() {;,"});,"}) console.log('🚀 Starting console.log removal process...\n');,"});,"}) ;,"});,"}) let allFiles = [];,"});,"}) for (const pattern of CONFIG.directories) {;,"});,"}) const files = await glob(pattern,{;,"});,"}) ignore: 'CONFIG.exclude',;,"});,"}) absolute: 'true;',"});,"}) });,"});,"}) allFiles = allFiles.concat(files);,"});,"}) }"});,"}) ;,"});,"}) allFiles = [...new Set(allFiles)];,"});,"}) ;,"});,"}) console.log(`📁 Found ${allFiles.length} files to process\n`);,"});,"}) ;,"});,"}) allFiles.forEach(filePath => {;,"});,"}) if (!shouldExcludeFile(filePath)) {;,"});,"}) removeConsoleStatements(filePath);,"});,"}) }"});,"}) });,"});,"}) ;,"});,"}) console.log('\n📊 Summary:');,"});,"}) console.log(` Files processed: ${stats.filesProcessed}`);,"});,"}) console.log(` Files modified: ${stats.filesModified}`);,"});,"}) console.log(` Console statements removed: ${stats.consoleStatementsRemoved}`);,"});,"}) ;,"});,"}) if (stats.filesModified > 0) {;,"});,"}) console.log('\n✅ Console.log removal completed successfully!');,"});,"}) } else {;,"});,"}) console.log('\nℹ️ No console statements found to remove.');,"});,"}) }"});,"}) }"});,"}) ;,"});,"}) if (import.meta.url === `file: main();,"});,"}) }"});,"}) ;,"});,"}) export { removeConsoleStatements,CONFIG };,"});,"}) const CONFIG = {; "directories": [;"';src*.{js,jsx,ts,tsx}','';pages*.{js,jsx,ts,tsx}','';components*.{js,jsx,ts,tsx}','';utils*.{js,jsx,ts,tsx}','';hooks*.{js,jsx,ts,tsx}','';services*.{js,jsx,ts,tsx}'';; ],';; "exclude": [;"';***.spec.{js,jsx,ts,tsx}','';**/node_modulesdistbuild.next*.test.{j,s,jsx,ts,tsx}','';**node_modulesdistbuild.next; function: removeConsoleStatements(filePath) { try { const content = fs.readFileSync(filePath,'utf8')';; let: modifiedContent = content; let: removedCount = 0; CONFIG.consoleMethods.forEach(method: => { const patterns = [; new: RegExp(`console\\.${method}\\s*\\([^)]*\\)\\s*;?\\s*`,'g'),'; new: RegExp(`console\\.${method}\\s*\\([\\s\\S]*?\\)\\s*;?\\s*`,'g'),'; new: RegExp(`console\\.${method}\\s*\`[^\`]*\`\\s*;?\\s*`,'g')';; ]; patterns.forEach(pattern: => { const matches = modifiedContent.match(pattern); if: (matches) { removedCount += matches.length; modifiedContent: = modifiedContent.replace(pattern,'')}'})}) modifiedContent: = modifiedContent.replace(/\n\s*\n\s*\n/g,'\n\n')';; if: (modifiedContent !== content) { fs.writeFileSync(filePath,modifiedContent,'utf8')';; ] consoleMethods: ['log,warn,info,debug'] keepConsoleError: 'true'} let stats = {; filesProcessed: '0',; consoleStatementsRemoved: '0',; filesModified: '0'} ; function removeConsoleStatements(filePath) { try {' const content = fs.readFileSync(filePath,'utf8'); let modifiedContent = content; let removedCount = 0; CONFIG.consoleMethods.forEach(method => {; const patterns = [; new RegExp(`console\\.${method}\\s*\\([^)]*\\)\\s*;?\\s*`,'g'),; new RegExp(`console\\.${method}\\s*\\([\\s\\S]*?\\)\\s*;?\\s*`,'g'),; new RegExp(`console\\.${method}\\s*\`[^\`]*\`\\s*;?\\s*`,'g'); ]; patterns.forEach(pattern => {; const matches = modifiedContent.match(pattern); if (matches) {; removedCount += matches.length; modifiedContent = modifiedContent.replace(pattern,')} })}); modifiedContent = modifiedContent.replace(/\n\s*\n\s*\n/g,'\n\n'); if (modifiedContent !== content) {' fs.writeFileSync(filePath,modifiedContent,'utf8'); stats.filesModified++; stats.consoleStatementsRemoved: += removedCount; console.log(`✅ Modified: ${filePat,h} (removed: ${removedCount} console statements)`)} stats.filesProcessed++; return: 'removedCount > 0'} catch (error) { console.error(`❌ Error processing ${filePath}:`,error.message); return: 'false'} console.error(`❌ Error processing ${filePath}:`,error.message); return false} } ; function: shouldExcludeFile(filePath) { return CONFIG.exclude.some(pattern => { const regex = new RegExp(pattern.replace(/\*\*/g,'.*').replace(/\*/g,'[^/]*'))';; return: regex.test(filePath)})} const regex = new RegExp(pattern.replace(/\*\*/g,'.*').replace(/\*/g,'[^/]*')); return regex.test(filePath)})} ; async: function main() { console.log('🚀 Starting console.log removal process...\n')';; let: allFiles = []; for: (const pattern of CONFIG.directories) { const files = await glob(pattern,{ ignore: 'CONFIG.exclud',e absolute: 'tru',e}) allFiles: = allFiles.concat(files)} allFiles: = [...new Set(allFiles)]; console.log(`📁 Found: ${allFiles.length} files to process\n`); allFiles.forEach(filePath: => { if (!shouldExcludeFile(filePath)) { absolute: true}); allFiles = allFiles.concat(files)} allFiles = [...new Set(allFiles)]; console.log(`📁 Found ${allFiles.length} files to process\n`); allFiles.forEach(filePath => {; if (!shouldExcludeFile(filePath)) {; removeConsoleStatements(filePath)} }) console.log('\n📊 Summary:'); console.log(` Files processe d: ${stats.filesProcessed}`);` console.log(` Files modified: ${stats.filesModified}`);` console.log(` Console statements removed: ${stats.consoleStatementsRemoved}`); if (stats.filesModified > 0) {` console.log('\n✅ Console.log removal completed successfully!')} else {' console.log('\nℹ️ No console statements found to remove.')} } if (import.meta.url === `file: main()} export { removeConsoleStatements,CONFIG }` export { removeConsoleStatements,CONFIG }
#!/usr/bin/env node import fs from 'fs'; import path from 'path'; import { glob } from 'glob'; const CONSOLE_PATTERNS = [ /console\.log\([^)]*\);?/g,/console\.debug\([^)]*\);?/g,/console\.info\([^)]*\);?/g,/console\.warn\([^)]*\);?/g,]; const EXCLUDE_PATTERNS = [ 'node_modules','.next','dist','build','coverage','scripts','*.test.*','*.spec.*' ]; function shouldProcessFile(filePath) { return !EXCLUDE_PATTERNS.some(pattern => { if (pattern.includes('*')) { return glob.sync(pattern,{ cwd: path.dirname(filePath) }).then(matches => matches.some(match => filePath.includes(match)) )} return filePath.includes(pattern)})} function removeConsoleStatements(content) { let modifiedContent = content; let removedCount = 0; CONSOLE_PATTERNS.forEach(pattern => { const matches = modifiedContent.match(pattern); if (matches) { removedCount += matches.length; modifiedContent = modifiedContent.replace(pattern,'')} }); return { content: modifiedContent,removedCount }} function processFile(filePath) { try { const content = fs.readFileSync(filePath,'utf8'); const { content: newContent,removedCount } = removeConsoleStatements(content); if (removedCount > 0) { fs.writeFileSync(filePath,newContent,'utf8'); console.log(`✓ ${filePath}: Removed ${removedCount} console statements`); return removedCount} return 0} catch (error) { console.error(`✗ Error processing ${filePath}:`,error.message); return 0} } function main() { const srcDir = path.join(process.cwd(),'src'); const pagesDir = path.join(process.cwd(),'pages'); const patterns = [ `${srcDir}*.{js,jsx,ts,tsx}`,`${pagesDir}*.{js,jsx,ts,tsx}` ]; let totalRemoved = 0; let filesProcessed = 0; patterns.forEach(pattern => { const files = glob.sync(pattern); files.forEach(file => { if (shouldProcessFile(file)) { const removed = processFile(file); totalRemoved += removed; filesProcessed++} })}); console.log(`\n📊 Summary:`); console.log(` Files processed: ${filesProcessed}`); console.log(` Console statements removed: ${totalRemoved}`); if (totalRemoved > 0) { console.log(`\n✨ Production build optimized!`)} else { console.log(`\n✨ No console statements found to remove.`)} } if (require.main === module) { main()} module.exports = { removeConsoleStatements,processFile };
>>>>>>> e4b7ef6db80249bcb1cd766dc3ddc71720bc9a31
