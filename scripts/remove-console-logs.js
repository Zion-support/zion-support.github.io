/usr/bin/env node import fs from 'fs'; import path from 'path'; import { glob } from 'glob'; const CONSOLE_PATTERNS = [ /console\.log\([^)]*\);?/g,/console\.debug\([^)]*\);?/g,/console\.info\([^)]*\);?/g,/console\.warn\([^)]*\);?/
/usr/bin/env node;,"});,"}) ,"});,"}) ,"});,"}) import { glob } from 'glob';,"});,"}) ;,"});,"}) const CONFIG = {;,"});,"}) directories: '[;',"});,"}) 'src*.{js,jsx,ts,tsx}',;,"});,"}) 'pages*.{js,jsx,ts,tsx}',;,"});,"}) 'components*.{js,jsx,ts,tsx}',;,"});,"}) 'utils*.{js,jsx,ts,tsx}',;,"});,"}) 'hooks*.{js,jsx,ts,tsx}',;,"});,"}) 'services*.{js,jsx,ts,tsx}';,"});,"}) ],;,"});,"}) exclude: '[;',"});,"}) '***.spec.{js,jsx,ts,tsx}',;,"});,"}) '**/node_modulesdistbuild.next;,"});,"}) function removeConsoleStatements(filePath) {;,"});,"}) try {;,"});,"}) const content = fs.readFileSync(filePath,'utf8');,"});,"}) let modifiedContent = content;,"});,"}) let removedCount = 0;,"});,"}) ;,"});,"}) CONFIG.consoleMethods.forEach(method => {;,"});,"}) const patterns = [;,"});,"}) new RegExp(`console\\.${method}\\s*\\([^)]*\\)\\s*;?\\s*`,'g'),;,"});,"}) new RegExp(`console\\.${method}\\s*\\([\\s\\S]*?\\)\\s*;?\\s*`,'g'),;,"});,"}) new RegExp(`console\\.${method}\\s*\`[^\`]*\`\\s*;?\\s*`,'g');,"});,"}) ];,"});,"}) ;,"});,"}) patterns.forEach(pattern => {;,"});,"}) const matches = modifiedContent.match(pattern);,"});,"}) if (matches) {;,"});,"}) removedCount += matches.length;,"});,"}) modifiedContent = modifiedContent.replace(pattern,'');,"});,"}) }"});,"}) });,"});,"}) });,"});,"}) ;,"});,"}) modifiedContent = modifiedContent.replace(/\n\s*\n\s*\n/g,'\n\n');,"});,"}) ;,"});,"}) if (modifiedContent != content) {;,"});,"}) fs.writeFileSync(filePath,modifiedContent,'utf8');,"});,"}) stats.filesModified++;,"});,"}) stats.consoleStatementsRemoved += removedCount;,"});,"}) ;,"});,"}) console.log(` Modified: ${filePath} (removed ${removedCount} console statements)`);,"});,"}) }"});,"}) ;,"});,"}) stats.filesProcessed++;,"});,"}) return removedCount > 0;,"});,"}) ;,"});,"}) } catch (error) {;,"});,"}) console.error(` Error processing ${filePath}:`,error.message);,"});,"}) return false;,"});,"}) }"});,"}) }"});,"}) ;,"});,"}) ;,"});,"}) function shouldExcludeFile(filePath) {;,"});,"}) return CONFIG.exclude.some(pattern => {;,"});,"}) const regex = new RegExp(pattern.replace(/\*\*/g,'.*').replace(/\*/g,'[^/
/usr/bin/env node;,"});,"}) ,"});,"}) ,"});,"}) import { glob } from 'glob';,"});,"}) ;,"});,"}) const CONFIG = {;,"});,"}) directories: '[;',"});,"}) 'src*.{js,jsx,ts,tsx}',;,"});,"}) 'pages*.{js,jsx,ts,tsx}',;,"});,"}) 'components*.{js,jsx,ts,tsx}',;,"});,"}) 'utils*.{js,jsx,ts,tsx}',;,"});,"}) 'hooks*.{js,jsx,ts,tsx}',;,"});,"}) 'services*.{js,jsx,ts,tsx}';,"});,"}) ],;,"});,"}) exclude: '[;',"});,"}) '***.spec.{js,jsx,ts,tsx}',;,"});,"}) '**/node_modulesdistbuild && node_modulesdistbuild.next;,"});,"}) function removeConsoleStatements(filePath) {;,"});,"}) try {;,"});,"}) const content = fs && fs.readFileSync(filePath,'utf8');,"});,"}) let modifiedContent = content;,"});,"}) let removedCount = 0;,"});,"}) ;,"});,"}) CONFIG && CONFIG.consoleMethods.forEach(method => {;,"});,"}) const patterns = [;,"});,"}) new RegExp(`console\\.${method}\\s*\\([^)]*\\)\\s*;?\\s*`,'g'),;,"});,"}) new RegExp(`console\\.${method}\\s*\\([\\s\\S]*?\\)\\s*;?\\s*`,'g'),;,"});,"}) new RegExp(`console\\.${method}\\s*\`[^\`]*\`\\s*;?\\s*`,'g');,"});,"}) ];,"});,"}) ;,"});,"}) patterns && patterns.forEach(pattern => {;,"});,"}) const matches = modifiedContent && modifiedContent.match(pattern);,"});,"}) if (matches) {;,"});,"}) removedCount += matches && matches.length;,"});,"}) modifiedContent = modifiedContent && modifiedContent.replace(pattern,'');,"});,"}) }"});,"}) });,"});,"}) });,"});,"}) ;,"});,"}) modifiedContent = modifiedContent && modifiedContent.replace(/\n\s*\n\s*\n/g,'\n\n');,"});,"}) ;,"});,"}) if (modifiedContent != content) {;,"});,"}) fs && fs.writeFileSync(filePath,modifiedContent,'utf8');,"});,"}) stats && stats.filesModified++;,"});,"}) stats && stats.consoleStatementsRemoved += removedCount;,"});,"}) ;,"});,"}) console && console.log(` Modified: ${filePath} (removed ${removedCount} console statements)`);,"});,"}) }"});,"}) ;,"});,"}) stats && stats.filesProcessed++;,"});,"}) return removedCount > 0;,"});,"}) ;,"});,"}) } catch (error) {;,"});,"}) console && console.error(` Error processing ${filePath}:`,error && error.message);,"});,"}) return false;,"});,"}) }"});,"}) }"});,"}) ;,"});,"}) ;,"});,"}) function shouldExcludeFile(filePath) {;,"});,"}) return CONFIG && CONFIG.exclude.some(pattern => {;,"});,"}) const regex = new RegExp(pattern && pattern.replace(/\*\*/g,'.*').replace(/\*/g,'[^/
/usr/bin/env node import fs from 'fs'; import path from 'path'; import { glob } from 'glob'; const CONSOLE_PATTERNS = [ /console\.log\([^)]*\);?/g,/console\.debug\([^)]*\);?/g,/console\.info\([^)]*\);?/g,/console\.warn\([^)]*\);?/