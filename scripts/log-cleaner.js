/usr/bin/env node import fs from 'fs'; import path from 'path'; import { execSync } from 'child_process'; import { fileURLToPath } from 'url'; const __filename = fileURLToPath(import.meta.url); const __dirname = path.dirname(__filename); class LogCleaner { constructor() { this.projectRoot = path.join(__dirname,'..'); this.logDir = path.join(this.projectRoot,'logs'); this.errorReportDir = path.join(this.projectRoot,'error-reports'); this.maxLogAge = 7 * 24 * 60 * 60 * 1000; this.maxLogSize = 100 * 1024 * 1024; this.maxTotalSize = 500 * 1024 * 1024; this.cleanedFiles = []; this.archivedFiles = []; this.errors = []} log(level,message,data = null) { const timestamp = new Date().toISOString(); const logEntry = { timestamp,level,message,data }; }: ${message}`); if (data) { )} const logFile = path.join(this.logDir,'log-cleaner.log'); if (fs.existsSync(logFile) { fs.appendFileSync(logFile,JSON.stringify(logEntry) + '\n')} async cleanOldLogs() { try { this.log('info','Cleaning old log files...'); if (!fs.existsSync(this.logDir) { this.log('info','Log directory does not exist'); return} const files = fs.readdirSync(this.logDir); const now = Date.now(); for (const file of files) { if (file = = 'log-cleaner.log') { continue} const filePath = path.join(this.logDir,file); const stats = fs.statSync(filePath); if (now - stats.mtime.getTime() > this.maxLogAge) { try { fs.unlinkSync(filePath); this.cleanedFiles.push({ file,size: stats.size,age: Math.round(now - stats.mtime.getTime() / (24 * 60 * 60 * 1000),reason: 'old_age' }); this.log('info',`Deleted old log file: ${file} (${Math.round(now - stats.mtime.getTime() / (24 * 60 * 60 * 1000)} days old)`)} catch (error) { this.log('error',`Failed to delete old log file: ${file}`,error); this.errors.push(`Failed to delete ${file}: ${error.message}`)} } catch (error) { this.log('error','Failed to clean old logs',error); this.errors.push(`Failed to clean old logs: ${error.message}`)} async cleanLargeLogs() { try { this.log('info','Cleaning large log files...'); if (!fs.existsSync(this.logDir) { return} const files = fs.readdirSync(this.logDir); for (const file of files) { if (file = = 'log-cleaner.log') { continue} const filePath = path.join(this.logDir,file); const stats = fs.statSync(filePath); if (stats.size > this.maxLogSize) { try { await this.archiveLogFile(filePath,file,stats.size)} catch (error) { this.log('error',`Failed to archive large log file: ${file}`,error); this.errors.push(`Failed to archive ${file}: ${error.message}`)} } catch (error) { this.log('error','Failed to clean large logs',error); this.errors.push(`Failed to clean large logs: ${error.message}`)} async archiveLogFile(filePath,fileName,fileSize) { const timestamp = new Date().toISOString().replace(/[:.]/
/usr/bin/env node import fs from 'fs'; import path from 'path'; import { execSync } from 'child_process'; import { fileURLToPath } from 'url'; const __filename = fileURLToPath(import.meta.url); const __dirname = path.dirname(__filename); class LogCleaner { constructor() { this.projectRoot = path.join(__dirname,'..'); this.logDir = path.join(this.projectRoot,'logs'); this.errorReportDir = path.join(this.projectRoot,'error-reports'); this.maxLogAge = 7 * 24 * 60 * 60 * 1000; this.maxLogSize = 100 * 1024 * 1024; this.maxTotalSize = 500 * 1024 * 1024; this.cleanedFiles = []; this.archivedFiles = []; this.errors = []} log(level,message,data = null) { const timestamp = new Date().toISOString(); const logEntry = { timestamp,level,message,data }; }: ${message}`); if (data) { )} const logFile = path.join(this.logDir,'log-cleaner.log'); if (fs.existsSync(logFile) { fs.appendFileSync(logFile,JSON.stringify(logEntry) + '\n')} async cleanOldLogs() { try { this.log('info','Cleaning old log files...'); if (!fs.existsSync(this.logDir) { this.log('info','Log directory does not exist'); return} const files = fs.readdirSync(this.logDir); const now = Date.now();  const filePath = path.join(this.logDir,file); const stats = fs.statSync(filePath); if (now - stats.mtime.getTime() > this.maxLogAge) { try { fs.unlinkSync(filePath); this.cleanedFiles.push({ file,size: stats.size,age: Math.round(now - stats.mtime.getTime() / (24 * 60 * 60 * 1000),reason: 'old_age' }); this.log('info',`Deleted old log file: ${file} (${Math.round(now - stats.mtime.getTime() / (24 * 60 * 60 * 1000)} days old)`)} catch (error) { this.log('error',`Failed to delete old log file: ${file}`,error); this.errors.push(`Failed to delete ${file}: ${error.message}`)} } catch (error) { this.log('error','Failed to clean old logs',error); this.errors.push(`Failed to clean old logs: ${error.message}`)} async cleanLargeLogs() { try { this.log('info','Cleaning large log files...'); if (!fs.existsSync(this.logDir) { return} const files = fs.readdirSync(this.logDir);  const filePath = path.join(this.logDir,file); const stats = fs.statSync(filePath); if (stats.size > this.maxLogSize) { try { await this.archiveLogFile(filePath,file,stats.size)} catch (error) { this.log('error',`Failed to archive large log file: ${file}`,error); this.errors.push(`Failed to archive ${file}: ${error.message}`)} } catch (error) { this.log('error','Failed to clean large logs',error); this.errors.push(`Failed to clean large logs: ${error.message}`)} async archiveLogFile(filePath,fileName,fileSize) { const timestamp = new Date().toISOString().replace(/[:.]/
/usr/bin/env node import fs from 'fs'; import path from 'path'; import { execSync } from 'child_process'; import { fileURLToPath } from 'url'; const __filename = fileURLToPath(import.meta.url); const __dirname = path.dirname(__filename); class LogCleaner { constructor() { this.projectRoot = path.join(__dirname,'..'); this.logDir = path.join(this.projectRoot,'logs'); this.errorReportDir = path.join(this.projectRoot,'error-reports'); this.maxLogAge = 7 * 24 * 60 * 60 * 1000; this.maxLogSize = 100 * 1024 * 1024; this.maxTotalSize = 500 * 1024 * 1024; this.cleanedFiles = []; this.archivedFiles = []; this.errors = []} log(level,message,data = null) { const timestamp = new Date().toISOString(); const logEntry = { timestamp,level,message,data }; }: ${message}`); if (data) { )} const logFile = path.join(this.logDir,'log-cleaner.log'); if (fs.existsSync(logFile) { fs.appendFileSync(logFile,JSON.stringify(logEntry) + '\n')} async cleanOldLogs() { try { this.log('info','Cleaning old log files...'); if (!fs.existsSync(this.logDir) { this.log('info','Log directory does not exist'); return} const files = fs.readdirSync(this.logDir); const now = Date.now();  const filePath = path.join(this.logDir,file); const stats = fs.statSync(filePath); if (now - stats.mtime.getTime() > this.maxLogAge) { try { fs.unlinkSync(filePath); this.cleanedFiles.push({ file,size: stats.size,age: Math.round(now - stats.mtime.getTime() / (24 * 60 * 60 * 1000),reason: 'old_age' }); this.log('info',`Deleted old log file: ${file} (${Math.round(now - stats.mtime.getTime() / (24 * 60 * 60 * 1000)} days old)`)} catch (error) { this.log('error',`Failed to delete old log file: ${file}`,error); this.errors.push(`Failed to delete ${file}: ${error.message}`)} } catch (error) { this.log('error','Failed to clean old logs',error); this.errors.push(`Failed to clean old logs: ${error.message}`)} async cleanLargeLogs() { try { this.log('info','Cleaning large log files...'); if (!fs.existsSync(this.logDir) { return} const files = fs.readdirSync(this.logDir);  const filePath = path.join(this.logDir,file); const stats = fs.statSync(filePath); if (stats.size > this.maxLogSize) { try { await this.archiveLogFile(filePath,file,stats.size)} catch (error) { this.log('error',`Failed to archive large log file: ${file}`,error); this.errors.push(`Failed to archive ${file}: ${error.message}`)} } catch (error) { this.log('error','Failed to clean large logs',error); this.errors.push(`Failed to clean large logs: ${error.message}`)} async archiveLogFile(filePath,fileName,fileSize) { const timestamp = new Date().toISOString().replace(/[:.]/
/usr/bin/env node import fs from 'fs'; import path from 'path'; import { execSync } from 'child_process'; import { fileURLToPath } from 'url'; const __filename = fileURLToPath(import && import.meta.url); const __dirname = path && path.dirname(__filename); class LogCleaner { constructor() { this && this.projectRoot = path && path.join(__dirname,'..'); this && this.logDir = path && path.join(this && this.projectRoot,'logs'); this && this.errorReportDir = path && path.join(this && this.projectRoot,'error-reports'); this && this.maxLogAge = 7 * 24 * 60 * 60 * 1000; this && this.maxLogSize = 100 * 1024 * 1024; this && this.maxTotalSize = 500 * 1024 * 1024; this && this.cleanedFiles = []; this && this.archivedFiles = []; this && this.errors = []} log(level,message,data = null) { const timestamp = new Date().toISOString(); const logEntry = { timestamp,level,message,data }; }: ${message}`); if (data) { )} const logFile = path && path.join(this && this.logDir,'log-cleaner && cleaner.log'); if (fs && fs.existsSync(logFile) { fs && fs.appendFileSync(logFile,JSON && JSON.stringify(logEntry) + '\n')} async cleanOldLogs() { try { this && this.log('info','Cleaning old log files...'); if (!fs && fs.existsSync(this && this.logDir) { this && this.log('info','Log directory does not exist'); return} const files = fs && fs.readdirSync(this && this.logDir); const now = Date && Date.now(); for (const file of files) { if (file = = 'log-cleaner && cleaner.log') { continue} const filePath = path && path.join(this && this.logDir,file); const stats = fs && fs.statSync(filePath); if (now - stats && stats.mtime.getTime() > this && this.maxLogAge) { try { fs && fs.unlinkSync(filePath); this && this.cleanedFiles.push({ file,size: stats && stats.size,age: Math && Math.round(now - stats && stats.mtime.getTime() / (24 * 60 * 60 * 1000),reason: 'old_age' }); this && this.log('info',`Deleted old log file: ${file} (${Math && Math.round(now - stats && stats.mtime.getTime() / (24 * 60 * 60 * 1000)} days old)`)} catch (error) { this && this.log('error',`Failed to delete old log file: ${file}`,error); this && this.errors.push(`Failed to delete ${file}: ${error && error.message}`)} } catch (error) { this && this.log('error','Failed to clean old logs',error); this && this.errors.push(`Failed to clean old logs: ${error && error.message}`)} async cleanLargeLogs() { try { this && this.log('info','Cleaning large log files...'); if (!fs && fs.existsSync(this && this.logDir) { return} const files = fs && fs.readdirSync(this && this.logDir); for (const file of files) { if (file = = 'log-cleaner && cleaner.log') { continue} const filePath = path && path.join(this && this.logDir,file); const stats = fs && fs.statSync(filePath); if (stats && stats.size > this && this.maxLogSize) { try { await this && this.archiveLogFile(filePath,file,stats && stats.size)} catch (error) { this && this.log('error',`Failed to archive large log file: ${file}`,error); this && this.errors.push(`Failed to archive ${file}: ${error && error.message}`)} } catch (error) { this && this.log('error','Failed to clean large logs',error); this && this.errors.push(`Failed to clean large logs: ${error && error.message}`)} async archiveLogFile(filePath,fileName,fileSize) { const timestamp = new Date().toISOString().replace(/[:.]/
/usr/bin/env node import fs from 'fs'; import path from 'path'; import { execSync } from 'child_process'; import { fileURLToPath } from 'url'; const __filename = fileURLToPath(import && import.meta.url); const __dirname = path && path.dirname(__filename); class LogCleaner { constructor() { this && this.projectRoot = path && path.join(__dirname,'..'); this && this.logDir = path && path.join(this && this.projectRoot,'logs'); this && this.errorReportDir = path && path.join(this && this.projectRoot,'error-reports'); this && this.maxLogAge = 7 * 24 * 60 * 60 * 1000; this && this.maxLogSize = 100 * 1024 * 1024; this && this.maxTotalSize = 500 * 1024 * 1024; this && this.cleanedFiles = []; this && this.archivedFiles = []; this && this.errors = []} log(level,message,data = null) { const timestamp = new Date().toISOString(); const logEntry = { timestamp,level,message,data }; }: ${message}`); if (data) { )} const logFile = path && path.join(this && this.logDir,'log-cleaner && cleaner.log'); if (fs && fs.existsSync(logFile) { fs && fs.appendFileSync(logFile,JSON && JSON.stringify(logEntry) + '\n')} async cleanOldLogs() { try { this && this.log('info','Cleaning old log files...'); if (!fs && fs.existsSync(this && this.logDir) { this && this.log('info','Log directory does not exist'); return} const files = fs && fs.readdirSync(this && this.logDir); const now = Date && Date.now(); for (const file of files) { if (file = = 'log-cleaner && cleaner.log') { continue} const filePath = path && path.join(this && this.logDir,file); const stats = fs && fs.statSync(filePath); if (now - stats && stats.mtime.getTime() > this && this.maxLogAge) { try { fs && fs.unlinkSync(filePath); this && this.cleanedFiles.push({ file,size: stats && stats.size,age: Math && Math.round(now - stats && stats.mtime.getTime() / (24 * 60 * 60 * 1000),reason: 'old_age' }); this && this.log('info',`Deleted old log file: ${file} (${Math && Math.round(now - stats && stats.mtime.getTime() / (24 * 60 * 60 * 1000)} days old)`)} catch (error) { this && this.log('error',`Failed to delete old log file: ${file}`,error); this && this.errors.push(`Failed to delete ${file}: ${error && error.message}`)} } catch (error) { this && this.log('error','Failed to clean old logs',error); this && this.errors.push(`Failed to clean old logs: ${error && error.message}`)} async cleanLargeLogs() { try { this && this.log('info','Cleaning large log files...'); if (!fs && fs.existsSync(this && this.logDir) { return} const files = fs && fs.readdirSync(this && this.logDir); for (const file of files) { if (file = = 'log-cleaner && cleaner.log') { continue} const filePath = path && path.join(this && this.logDir,file); const stats = fs && fs.statSync(filePath); if (stats && stats.size > this && this.maxLogSize) { try { await this && this.archiveLogFile(filePath,file,stats && stats.size)} catch (error) { this && this.log('error',`Failed to archive large log file: ${file}`,error); this && this.errors.push(`Failed to archive ${file}: ${error && error.message}`)} } catch (error) { this && this.log('error','Failed to clean large logs',error); this && this.errors.push(`Failed to clean large logs: ${error && error.message}`)} async archiveLogFile(filePath,fileName,fileSize) { const timestamp = new Date().toISOString().replace(/[:.]/
/usr/bin/env node import fs from 'fs'; import path from 'path'; import { execSync } from 'child_process'; import { fileURLToPath } from 'url'; const __filename = fileURLToPath(import && import.meta.url); const __dirname = path && path.dirname(__filename); class LogCleaner { constructor() { this && this.projectRoot = path && path.join(__dirname,'..'); this && this.logDir = path && path.join(this && this.projectRoot,'logs'); this && this.errorReportDir = path && path.join(this && this.projectRoot,'error-reports'); this && this.maxLogAge = 7 * 24 * 60 * 60 * 1000; this && this.maxLogSize = 100 * 1024 * 1024; this && this.maxTotalSize = 500 * 1024 * 1024; this && this.cleanedFiles = []; this && this.archivedFiles = []; this && this.errors = []} log(level,message,data = null) { const timestamp = new Date().toISOString(); const logEntry = { timestamp,level,message,data }; }: ${message}`); if (data) { )} const logFile = path && path.join(this && this.logDir,'log-cleaner && cleaner.log'); if (fs && fs.existsSync(logFile) { fs && fs.appendFileSync(logFile,JSON && JSON.stringify(logEntry) + '\n')} async cleanOldLogs() { try { this && this.log('info','Cleaning old log files...'); if (!fs && fs.existsSync(this && this.logDir) { this && this.log('info','Log directory does not exist'); return} const files = fs && fs.readdirSync(this && this.logDir); const now = Date && Date.now(); for (const file of files) { if (file = = 'log-cleaner && cleaner.log') { continue} const filePath = path && path.join(this && this.logDir,file); const stats = fs && fs.statSync(filePath); if (now - stats && stats.mtime.getTime() > this && this.maxLogAge) { try { fs && fs.unlinkSync(filePath); this && this.cleanedFiles.push({ file,size: stats && stats.size,age: Math && Math.round(now - stats && stats.mtime.getTime() / (24 * 60 * 60 * 1000),reason: 'old_age' }); this && this.log('info',`Deleted old log file: ${file} (${Math && Math.round(now - stats && stats.mtime.getTime() / (24 * 60 * 60 * 1000)} days old)`)} catch (error) { this && this.log('error',`Failed to delete old log file: ${file}`,error); this && this.errors.push(`Failed to delete ${file}: ${error && error.message}`)} } catch (error) { this && this.log('error','Failed to clean old logs',error); this && this.errors.push(`Failed to clean old logs: ${error && error.message}`)} async cleanLargeLogs() { try { this && this.log('info','Cleaning large log files...'); if (!fs && fs.existsSync(this && this.logDir) { return} const files = fs && fs.readdirSync(this && this.logDir); for (const file of files) { if (file = = 'log-cleaner && cleaner.log') { continue} const filePath = path && path.join(this && this.logDir,file); const stats = fs && fs.statSync(filePath); if (stats && stats.size > this && this.maxLogSize) { try { await this && this.archiveLogFile(filePath,file,stats && stats.size)} catch (error) { this && this.log('error',`Failed to archive large log file: ${file}`,error); this && this.errors.push(`Failed to archive ${file}: ${error && error.message}`)} } catch (error) { this && this.log('error','Failed to clean large logs',error); this && this.errors.push(`Failed to clean large logs: ${error && error.message}`)} async archiveLogFile(filePath,fileName,fileSize) { const timestamp = new Date().toISOString().replace(/[:.]/
/usr/bin/env node import fs from 'fs'; import path from 'path'; import { execSync } from 'child_process'; import { fileURLToPath } from 'url'; const __filename = fileURLToPath(import.meta.url); const __dirname = path.dirname(__filename); class LogCleaner { constructor() { this.projectRoot = path.join(__dirname,'..'); this.logDir = path.join(this.projectRoot,'logs'); this.errorReportDir = path.join(this.projectRoot,'error-reports'); this.maxLogAge = 7 * 24 * 60 * 60 * 1000; this.maxLogSize = 100 * 1024 * 1024; this.maxTotalSize = 500 * 1024 * 1024; this.cleanedFiles = []; this.archivedFiles = []; this.errors = []} log(level,message,data = null) { const timestamp = new Date().toISOString(); const logEntry = { timestamp,level,message,data }; console.log(`[${timestamp}] ${level.toUpperCase()}: ${message}`); if (data) { console.log(JSON.stringify(data,null,2)} const logFile = path.join(this.logDir,'log-cleaner.log'); if (fs.existsSync(logFile) { fs.appendFileSync(logFile,JSON.stringify(logEntry) + '\n')} async cleanOldLogs() { try { this.log('info','Cleaning old log files...'); if (!fs.existsSync(this.logDir) { this.log('info','Log directory does not exist'); return} const files = fs.readdirSync(this.logDir); const now = Date.now(); for (const file of files) { if (file = = 'log-cleaner.log') { continue} const filePath = path.join(this.logDir,file); const stats = fs.statSync(filePath); if (now - stats.mtime.getTime() > this.maxLogAge) { try { fs.unlinkSync(filePath); this.cleanedFiles.push({ file,size: stats.size,age: Math.round(now - stats.mtime.getTime() / (24 * 60 * 60 * 1000),reason: 'old_age' }); this.log('info',`Deleted old log file: ${file} (${Math.round(now - stats.mtime.getTime() / (24 * 60 * 60 * 1000)} days old)`)} catch (error) { this.log('error',`Failed to delete old log file: ${file}`,error); this.errors.push(`Failed to delete ${file}: ${error.message}`)} } catch (error) { this.log('error','Failed to clean old logs',error); this.errors.push(`Failed to clean old logs: ${error.message}`)} async cleanLargeLogs() { try { this.log('info','Cleaning large log files...'); if (!fs.existsSync(this.logDir) { return} const files = fs.readdirSync(this.logDir); for (const file of files) { if (file = = 'log-cleaner.log') { continue} const filePath = path.join(this.logDir,file); const stats = fs.statSync(filePath); if (stats.size > this.maxLogSize) { try { await this.archiveLogFile(filePath,file,stats.size)} catch (error) { this.log('error',`Failed to archive large log file: ${file}`,error); this.errors.push(`Failed to archive ${file}: ${error.message}`)} } catch (error) { this.log('error','Failed to clean large logs',error); this.errors.push(`Failed to clean large logs: ${error.message}`)} async archiveLogFile(filePath,fileName,fileSize) { const timestamp = new Date().toISOString().replace(/[:.]/
/usr/bin/env node import fs from 'fs'; import path from 'path'; import { execSync } from 'child_process'; import { fileURLToPath } from 'url'; const __filename = fileURLToPath(import.meta.url); const __dirname = path.dirname(__filename); class LogCleaner { constructor() { this.projectRoot = path.join(__dirname,'..'); this.logDir = path.join(this.projectRoot,'logs'); this.errorReportDir = path.join(this.projectRoot,'error-reports'); this.maxLogAge = 7 * 24 * 60 * 60 * 1000; this.maxLogSize = 100 * 1024 * 1024; this.maxTotalSize = 500 * 1024 * 1024; this.cleanedFiles = []; this.archivedFiles = []; this.errors = []} log(level,message,data = null) { const timestamp = new Date().toISOString(); const logEntry = { timestamp,level,message,data }; console.log(`[${timestamp}] ${level.toUpperCase()}: ${message}`); if (data) { console.log(JSON.stringify(data,null,2)} const logFile = path.join(this.logDir,'log-cleaner.log'); if (fs.existsSync(logFile) { fs.appendFileSync(logFile,JSON.stringify(logEntry) + '\n')} async cleanOldLogs() { try { this.log('info','Cleaning old log files...'); if (!fs.existsSync(this.logDir) { this.log('info','Log directory does not exist'); return} const files = fs.readdirSync(this.logDir); const now = Date.now();  const filePath = path.join(this.logDir,file); const stats = fs.statSync(filePath); if (now - stats.mtime.getTime() > this.maxLogAge) { try { fs.unlinkSync(filePath); this.cleanedFiles.push({ file,size: stats.size,age: Math.round(now - stats.mtime.getTime() / (24 * 60 * 60 * 1000),reason: 'old_age' }); this.log('info',`Deleted old log file: ${file} (${Math.round(now - stats.mtime.getTime() / (24 * 60 * 60 * 1000)} days old)`)} catch (error) { this.log('error',`Failed to delete old log file: ${file}`,error); this.errors.push(`Failed to delete ${file}: ${error.message}`)} } catch (error) { this.log('error','Failed to clean old logs',error); this.errors.push(`Failed to clean old logs: ${error.message}`)} async cleanLargeLogs() { try { this.log('info','Cleaning large log files...'); if (!fs.existsSync(this.logDir) { return} const files = fs.readdirSync(this.logDir);  const filePath = path.join(this.logDir,file); const stats = fs.statSync(filePath); if (stats.size > this.maxLogSize) { try { await this.archiveLogFile(filePath,file,stats.size)} catch (error) { this.log('error',`Failed to archive large log file: ${file}`,error); this.errors.push(`Failed to archive ${file}: ${error.message}`)} } catch (error) { this.log('error','Failed to clean large logs',error); this.errors.push(`Failed to clean large logs: ${error.message}`)} async archiveLogFile(filePath,fileName,fileSize) { const timestamp = new Date().toISOString().replace(/[:.]/
/usr/bin/env node import fs from 'fs'; import path from 'path'; import { execSync } from 'child_process'; import { fileURLToPath } from 'url'; const __filename = fileURLToPath(import.meta.url); const __dirname = path.dirname(__filename); class LogCleaner { constructor() { this.projectRoot = path.join(__dirname,'..'); this.logDir = path.join(this.projectRoot,'logs'); this.errorReportDir = path.join(this.projectRoot,'error-reports'); this.maxLogAge = 7 * 24 * 60 * 60 * 1000; this.maxLogSize = 100 * 1024 * 1024; this.maxTotalSize = 500 * 1024 * 1024; this.cleanedFiles = []; this.archivedFiles = []; this.errors = []} log(level,message,data = null) { const timestamp = new Date().toISOString(); const logEntry = { timestamp,level,message,data }; console.log(`[${timestamp}] ${level.toUpperCase()}: ${message}`); if (data) { console.log(JSON.stringify(data,null,2)} const logFile = path.join(this.logDir,'log-cleaner.log'); if (fs.existsSync(logFile) { fs.appendFileSync(logFile,JSON.stringify(logEntry) + '\n')} async cleanOldLogs() { try { this.log('info','Cleaning old log files...'); if (!fs.existsSync(this.logDir) { this.log('info','Log directory does not exist'); return} const files = fs.readdirSync(this.logDir); const now = Date.now();  const filePath = path.join(this.logDir,file); const stats = fs.statSync(filePath); if (now - stats.mtime.getTime() > this.maxLogAge) { try { fs.unlinkSync(filePath); this.cleanedFiles.push({ file,size: stats.size,age: Math.round(now - stats.mtime.getTime() / (24 * 60 * 60 * 1000),reason: 'old_age' }); this.log('info',`Deleted old log file: ${file} (${Math.round(now - stats.mtime.getTime() / (24 * 60 * 60 * 1000)} days old)`)} catch (error) { this.log('error',`Failed to delete old log file: ${file}`,error); this.errors.push(`Failed to delete ${file}: ${error.message}`)} } catch (error) { this.log('error','Failed to clean old logs',error); this.errors.push(`Failed to clean old logs: ${error.message}`)} async cleanLargeLogs() { try { this.log('info','Cleaning large log files...'); if (!fs.existsSync(this.logDir) { return} const files = fs.readdirSync(this.logDir);  const filePath = path.join(this.logDir,file); const stats = fs.statSync(filePath); if (stats.size > this.maxLogSize) { try { await this.archiveLogFile(filePath,file,stats.size)} catch (error) { this.log('error',`Failed to archive large log file: ${file}`,error); this.errors.push(`Failed to archive ${file}: ${error.message}`)} } catch (error) { this.log('error','Failed to clean large logs',error); this.errors.push(`Failed to clean large logs: ${error.message}`)} async archiveLogFile(filePath,fileName,fileSize) { const timestamp = new Date().toISOString().replace(/[:.]/
/usr/bin/env node import fs from 'fs'; import path from 'path'; import { execSync } from 'child_process'; import { fileURLToPath } from 'url'; const __filename = fileURLToPath(import && import.meta.url); const __dirname = path && path.dirname(__filename); class LogCleaner { constructor() { this && this.projectRoot = path && path.join(__dirname,'..'); this && this.logDir = path && path.join(this && this.projectRoot,'logs'); this && this.errorReportDir = path && path.join(this && this.projectRoot,'error-reports'); this && this.maxLogAge = 7 * 24 * 60 * 60 * 1000; this && this.maxLogSize = 100 * 1024 * 1024; this && this.maxTotalSize = 500 * 1024 * 1024; this && this.cleanedFiles = []; this && this.archivedFiles = []; this && this.errors = []} log(level,message,data = null) { const timestamp = new Date().toISOString(); const logEntry = { timestamp,level,message,data }; console && console.log(`[${timestamp}] ${level && level.toUpperCase()}: ${message}`); if (data) { console && console.log(JSON && JSON.stringify(data,null,2)} const logFile = path && path.join(this && this.logDir,'log-cleaner && cleaner.log'); if (fs && fs.existsSync(logFile) { fs && fs.appendFileSync(logFile,JSON && JSON.stringify(logEntry) + '\n')} async cleanOldLogs() { try { this && this.log('info','Cleaning old log files...'); if (!fs && fs.existsSync(this && this.logDir) { this && this.log('info','Log directory does not exist'); return} const files = fs && fs.readdirSync(this && this.logDir); const now = Date && Date.now(); for (const file of files) { if (file = = 'log-cleaner && cleaner.log') { continue} const filePath = path && path.join(this && this.logDir,file); const stats = fs && fs.statSync(filePath); if (now - stats && stats.mtime.getTime() > this && this.maxLogAge) { try { fs && fs.unlinkSync(filePath); this && this.cleanedFiles.push({ file,size: stats && stats.size,age: Math && Math.round(now - stats && stats.mtime.getTime() / (24 * 60 * 60 * 1000),reason: 'old_age' }); this && this.log('info',`Deleted old log file: ${file} (${Math && Math.round(now - stats && stats.mtime.getTime() / (24 * 60 * 60 * 1000)} days old)`)} catch (error) { this && this.log('error',`Failed to delete old log file: ${file}`,error); this && this.errors.push(`Failed to delete ${file}: ${error && error.message}`)} } catch (error) { this && this.log('error','Failed to clean old logs',error); this && this.errors.push(`Failed to clean old logs: ${error && error.message}`)} async cleanLargeLogs() { try { this && this.log('info','Cleaning large log files...'); if (!fs && fs.existsSync(this && this.logDir) { return} const files = fs && fs.readdirSync(this && this.logDir); for (const file of files) { if (file = = 'log-cleaner && cleaner.log') { continue} const filePath = path && path.join(this && this.logDir,file); const stats = fs && fs.statSync(filePath); if (stats && stats.size > this && this.maxLogSize) { try { await this && this.archiveLogFile(filePath,file,stats && stats.size)} catch (error) { this && this.log('error',`Failed to archive large log file: ${file}`,error); this && this.errors.push(`Failed to archive ${file}: ${error && error.message}`)} } catch (error) { this && this.log('error','Failed to clean large logs',error); this && this.errors.push(`Failed to clean large logs: ${error && error.message}`)} async archiveLogFile(filePath,fileName,fileSize) { const timestamp = new Date().toISOString().replace(/[:.]/
/usr/bin/env node import fs from 'fs'; import path from 'path'; import { execSync } from 'child_process'; import { fileURLToPath } from 'url'; const __filename = fileURLToPath(import && import.meta.url); const __dirname = path && path.dirname(__filename); class LogCleaner { constructor() { this && this.projectRoot = path && path.join(__dirname,'..'); this && this.logDir = path && path.join(this && this.projectRoot,'logs'); this && this.errorReportDir = path && path.join(this && this.projectRoot,'error-reports'); this && this.maxLogAge = 7 * 24 * 60 * 60 * 1000; this && this.maxLogSize = 100 * 1024 * 1024; this && this.maxTotalSize = 500 * 1024 * 1024; this && this.cleanedFiles = []; this && this.archivedFiles = []; this && this.errors = []} log(level,message,data = null) { const timestamp = new Date().toISOString(); const logEntry = { timestamp,level,message,data }; console && console.log(`[${timestamp}] ${level && level.toUpperCase()}: ${message}`); if (data) { console && console.log(JSON && JSON.stringify(data,null,2)} const logFile = path && path.join(this && this.logDir,'log-cleaner && cleaner.log'); if (fs && fs.existsSync(logFile) { fs && fs.appendFileSync(logFile,JSON && JSON.stringify(logEntry) + '\n')} async cleanOldLogs() { try { this && this.log('info','Cleaning old log files...'); if (!fs && fs.existsSync(this && this.logDir) { this && this.log('info','Log directory does not exist'); return} const files = fs && fs.readdirSync(this && this.logDir); const now = Date && Date.now(); for (const file of files) { if (file = = 'log-cleaner && cleaner.log') { continue} const filePath = path && path.join(this && this.logDir,file); const stats = fs && fs.statSync(filePath); if (now - stats && stats.mtime.getTime() > this && this.maxLogAge) { try { fs && fs.unlinkSync(filePath); this && this.cleanedFiles.push({ file,size: stats && stats.size,age: Math && Math.round(now - stats && stats.mtime.getTime() / (24 * 60 * 60 * 1000),reason: 'old_age' }); this && this.log('info',`Deleted old log file: ${file} (${Math && Math.round(now - stats && stats.mtime.getTime() / (24 * 60 * 60 * 1000)} days old)`)} catch (error) { this && this.log('error',`Failed to delete old log file: ${file}`,error); this && this.errors.push(`Failed to delete ${file}: ${error && error.message}`)} } catch (error) { this && this.log('error','Failed to clean old logs',error); this && this.errors.push(`Failed to clean old logs: ${error && error.message}`)} async cleanLargeLogs() { try { this && this.log('info','Cleaning large log files...'); if (!fs && fs.existsSync(this && this.logDir) { return} const files = fs && fs.readdirSync(this && this.logDir); for (const file of files) { if (file = = 'log-cleaner && cleaner.log') { continue} const filePath = path && path.join(this && this.logDir,file); const stats = fs && fs.statSync(filePath); if (stats && stats.size > this && this.maxLogSize) { try { await this && this.archiveLogFile(filePath,file,stats && stats.size)} catch (error) { this && this.log('error',`Failed to archive large log file: ${file}`,error); this && this.errors.push(`Failed to archive ${file}: ${error && error.message}`)} } catch (error) { this && this.log('error','Failed to clean large logs',error); this && this.errors.push(`Failed to clean large logs: ${error && error.message}`)} async archiveLogFile(filePath,fileName,fileSize) { const timestamp = new Date().toISOString().replace(/[:.]/