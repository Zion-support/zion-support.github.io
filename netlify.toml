[# Unified Netlify build configuration]
[build]
  # Build from source on Netlify
  command = "npm ci --no-audit --no-fund && npm run build"
  publish = "dist"
  functions = "netlify/functions"
  command_timeout = "30m"

[build.environment]
  NODE_VERSION = "20"
  PYTHON_VERSION = "3.11"
  # Install dependencies and build on Netlify
  NETLIFY_SKIP_INSTALL = "false"
  NETLIFY_USE_YARN = "false"
  NPM_FLAGS = "--omit=optional"

[context.production.environment]
  NODE_ENV = "production"

# Vite/React SPA configuration
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Set basic cache headers for static assets for better performance
[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/*"
  [headers.values]
    Cache-Control = "public, max-age=300"
