[build]
  # Use `npm ci` for deterministic installs
  # Generate Prisma client before building to ensure models are up to date
  command = "npm run build:netlify:prepare"
  # Netlify UI sets `publish` to `out` which causes the Next.js plugin to fail
  # because no exported directory exists. Explicitly publish the `.next`
  # directory so the plugin can run correctly.
  publish = ".next"
  # Increase build timeout for 176+ pages
  command_timeout = "30m"

[build.environment]
  NODE_OPTIONS = "--no-deprecation --max-old-space-size=6144 --no-warnings"
  NPM_FLAGS = "--prefer-offline --legacy-peer-deps"
  NEXT_PUBLIC_APP_ENV = "production"
  NODE_VERSION = "22"
  NEXT_TELEMETRY_DISABLED = "1"
  CI = "true"
  SKIP_TYPE_CHECK = "true"
  
  # Performance optimizations for large builds (with Netlify plugin)
  NEXT_DISABLE_CSS_INLINE = "true"
  NEXT_DISABLE_SOURCE_MAPS = "true"
  GENERATE_SOURCEMAP = "false"
  NEXT_BUILD_WORKERS = "1"
  NEXT_PRIVATE_BUILD_CACHE = "false"
  NEXT_PRIVATE_STATIC_OPTIMIZATION = "false"
  
  # CRITICAL FIX: Completely disable build trace collection to prevent hanging
  NEXT_DISABLE_TRACE_COLLECTION = "true"
  NEXT_PRIVATE_OUTPUT_TRACE = "false"
  # NEXT_PRIVATE_OUTPUT_FILE_TRACING = "false" # Allowing Next.js default / plugin to manage this
  UV_THREADPOOL_SIZE = "4"
  NODE_NO_WARNINGS = "1"
  NEXT_PRIVATE_MINIMIZE_BUILD_TIME = "true"
  
  # Configure secrets scanning to omit legitimate public environment variables
  # NEXT_PUBLIC_* variables are meant to be public and embedded in client bundles
  SECRETS_SCAN_OMIT_KEYS = "NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME,SENTRY_DSN"
  

[[plugins]]
  package = "netlify-plugin-cloudinary"

# Scheduled Functions (new automations replacing some GitHub Actions cadence)
[[scheduled.functions]]
  name = "homepage_advertiser"
  schedule = "*/1 * * * *" # every minute

[[headers]]
  for = "/_next/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[scheduled.functions]]
  name = "front-enhancer"
  schedule = "*/1 * * * *" # every minute

[[scheduled.functions]]
  name = "frontpage-enhancer"
  schedule = "*/1 * * * *" # every minute

[[scheduled.functions]]
  name = "marketing-and-features-promo"
  schedule = "*/15 * * * *" # every 15 minutes

[[redirects]]
  from = "/services/ai-customer-success-platform"
  to = "/ai-customer-success-platform/"
  status = 301
  force = true

[[redirects]]
  from = "/services/intelligent-supply-chain-optimization"
  to = "/intelligent-supply-chain-optimization/"
  status = 301
  force = true

[[scheduled.functions]]
  name = "fast-front-promoter"
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/sitemap.xml"
  [headers.values]
  schedule = "*/1 * * * *" # every minute

[[scheduled.functions]]
  name = "hyper-front-runner"
  schedule = "*/1 * * * *" # every minute
=======
    Referrer-Policy = "strict-origin-when-cross-origin"
    X-Content-Type-Options = "nosniff"
    X-Frame-Options = "DENY"
    Permissions-Policy = "geolocation=(), microphone=(), camera=()"
    Strict-Transport-Security = "max-age=31536000; includeSubDomains; preload"
    X-DNS-Prefetch-Control = "on"
    Content-Security-Policy = "default-src 'self'; script-src 'self' 'unsafe-inline' https://plausible.io; style-src 'self' 'unsafe-inline'; img-src 'self' data: https:; font-src 'self' data:; connect-src 'self' https://plausible.io; frame-ancestors 'none'; base-uri 'self'; form-action 'self'"

[[headers]]
  for = "/favicon.svg"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/og-image.svg"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/site.webmanifest"
  [headers.values]
    Cache-Control = "public, max-age=604800"
