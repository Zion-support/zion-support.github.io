[build]
  command = "npm ci --ignore-scripts --omit=optional || npm install --ignore-scripts --omit=optional && npm run build"
  publish = "dist"
  command_timeout = "30m"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
