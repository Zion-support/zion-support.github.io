[build]
<<<<<<< HEAD
  # Build command
  command = "npm run build:netlify"
=======
  # Pre-build command to handle dependencies
  command = "bash scripts/netlify-pre-build.sh && npm run build"
>>>>>>> origin/main
  
  # Directory to publish
  publish = "dist"
  
  # Functions directory
  functions = "netlify/functions"

[build.environment]
  # Node.js version
  NODE_VERSION = "20"
  
  # Yarn version
  YARN_VERSION = "1.22.22"
  
  # Build environment
  NODE_OPTIONS = "--max-old-space-size=6144"
  NODE_ENV = "production"
  
<<<<<<< HEAD
  # Yarn configuration
  YARN_CACHE_FOLDER = "/opt/buildhome/.yarn_cache"
  YARN_ENABLE_IMMUTABLE_INSTALLS = "false"
=======
  # Yarn configuration - optimized for reliable builds
  YARN_CACHE_FOLDER = "/opt/buildhome/.yarn_cache"
  YARN_ENABLE_IMMUTABLE_INSTALLS = "false"
  YARN_NETWORK_TIMEOUT = "1000000"
  YARN_PREFER_OFFLINE = "true"
  YARN_IGNORE_ENGINES = "true"
  YARN_IGNORE_PLATFORM = "true"
>>>>>>> origin/main
  
  # Install devDependencies for build tools
  YARN_INSTALL_DEV = "true"
  
  # Force install all dependencies including devDependencies
  NPM_CONFIG_INCLUDE = "dev"
  
  # Clear cache and force clean install
  YARN_CACHE_CLEAR = "true"
  
  # Ensure Vite is available and handle peer dependencies
  NPM_CONFIG_LEGACY_PEER_DEPS = "true"
<<<<<<< HEAD
  
  # Disable Next.js auto-detection
  NETLIFY_NEXT_PLUGIN_SKIP = "true"
  # Force Vite build mode
  NETLIFY_USE_NEXT_PLUGIN = "false"
=======
  NPM_CONFIG_AUDIT = "false"
  NPM_CONFIG_FUND = "false"
  NPM_CONFIG_FORCE = "true"
  
  # Disable Next.js auto-detection since this is a Vite project
  NETLIFY_USE_NEXTJS = "false"
  NETLIFY_NEXT_PLUGIN_SKIP = "true"
  NETLIFY_USE_NEXT_PLUGIN = "false"
  
  # Additional build optimizations
  CI = "true"
  GENERATE_SOURCEMAP = "false"
>>>>>>> origin/main

# Cache configuration to prevent dependency issues
[build.cache]
  directories = ["node_modules/.cache"]

# Plugin configurations
[[plugins]]
  package = "netlify-plugin-cloudinary"

# Disable Next.js auto-detection since this is a Vite project
[build.processing]
  skip_processing = false

# Explicitly disable Next.js plugin
[build.processing.css]
  bundle = true
  minify = true

[build.processing.js]
  bundle = true
  minify = true

# Force Vite build detection

# Static redirects for exported site
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Headers for security and performance
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "camera=(), microphone=(), geolocation=()"

# Cache static assets
[[headers]]
  for = "/static/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# Cache API routes
[[headers]]
  for = "/api/*"
  [headers.values]
    Cache-Control = "public, max-age=0, must-revalidate"