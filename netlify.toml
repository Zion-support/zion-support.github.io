[build]
  # Publish prebuilt dist without running a build
  command = "npm run build"
  publish = "dist"
  command_timeout = "30m"

[build.environment]
  NODE_VERSION = "20.18.1"




[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
