// import fs from 'fs' import path from 'path' function kebabToPascal(str) { return str .split('-') .map(word => word.charAt(0).toUpperCase() + word.slice(1)) .join('')} function componentName = kebabToPascal(pageName); const isApi = filePath.includes('/api)
  q: NextApiRequest)re,
"`