import fs from ');
import path from ');
    let content  = fs.readFileSync(filePath, 'utf8'');
      if (!match.includes('';
      if (value.includes('"';
        if (!value.startsWith(';
      if (expr.includes('"';