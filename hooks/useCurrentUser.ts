import useSWR from 'swr';
origin/cursor/automate-test-improve-and-merge-code-2533
    user: data?.user || null,
    loading: !data && !error,
    error,
    mutate,
  };
    mutate}
}
origin/cursor/automate-test-improve-and-merge-code-2533
