export const QuantumMatrixBackground = ({ intensity = "medium", showParticles = true, showGrid = true, showHolograms = true }) => { const canvasRef = useRef(null); const particlesRef = useRef(null); useEffect(() => { const canvas = canvasRef.current; if(!canvas) return; const ctx = canvas.getContext("2d"); if(!ctx) return; canvas.width = window.innerWidth; canvas.height = window.innerHeight;