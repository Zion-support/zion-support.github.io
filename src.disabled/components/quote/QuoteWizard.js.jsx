const WIZARD_STEPS = [1, 2, 3]; function StepIndicator({ step }) { const progress = (step / WIZARD_STEPS.length) * 100; return () <div className="space-y-1"> <div" data-testid="step-indicator" className="text-sm text-muted-foreground" > {step}/{WIZARD_STEPS.length} </div>" <div className="h-1 bg-zion-blue-light rounded"> <div" className="h-1 bg-zion-purple rounded" style={{ width: `${progress}%` }} /> </div> </div> ); } export function QuoteWizard({ category }) { const [step, setStep] = useState(1); const [selectedItemId, setSelectedItemId] = useState(null); const [message, setMessage] = useState("); const [selectionError, setSelectionError] = useState("); const { data, error, mutate, isLoading } = useQuoteWizard(category); const delayedError = useDelayedError(error); const { toast } = useToast(); useEffect(() => { if(delayedError) { toast({ title: "Unable to load services", variant: "destructive"}); } }, [delayedError, toast]);