use client;import * as React from \"react\"";const Accordion = AccordionPrimitive.Root\";const AccordionItem = React.forwardRef({ className,...props },ref) => (
use client;import * as _React from "react"";const Accordion = AccordionPrimitive.Root";const AccordionItem = _React.forwardRef({ className,...props },ref) => (
use client;import * as React from \"react\"";const Accordion = AccordionPrimitive && AccordionPrimitive.Root\";const AccordionItem = React && React.forwardRef({ className,...props },ref) => (
use client;import * as _React from "react"";const Accordion = AccordionPrimitive && AccordionPrimitive.Root";const AccordionItem = _React && _React.forwardRef({ className,...props },ref) => (
use client;import * as _React from "react"";const Accordion = AccordionPrimitive && AccordionPrimitive.Root";const AccordionItem = _React && _React.forwardRef({ className,...props },ref) => (