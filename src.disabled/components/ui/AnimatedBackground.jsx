" export function AnimatedBackground({ className = ", variant = "grid" }) {} const canvasRef = useRef(null); useEffect(() => {} const canvas = canvasRef.current; if(!canvas) return;" const ctx = canvas.getContext("2d"); if(!ctx) return; let animationFrameId; let particles = []; const resizeCanvas = () => {} canvas.width = window.innerWidth; canvas.height = window.innerHeight}; const initParticles = () => {} particles = [];" const particleCount = variant === "particles" ? 100 : 50; for(let i = 0; i < particleCount; i++) {} particles.push({} x: Math.random() * canvas.width, y: Math.random() * canvas.height, vx: (Math.random() - 0.5) * 0.5, vy: (Math.random() - 0.5) * 0.5, size: Math.random() * 2 + 1, opacity: Math.random() * 0.5 + 0.1})} }; const drawGrid = () => {} const gridSize = 40; const offset = (Date.now() * 0.001) % gridSize;" ctx.strokeStyle = "rgba(139, 21, 233, 0.1); ctx.lineWidth = 1;