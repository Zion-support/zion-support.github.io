import _React from';react' const TalentCardComponent = ( const navigate = useNavigate() const handleMessage = e => { e.preventDefault() e.stopPropagation() if (onMessage) { onMessage(talent) ) => {
  return $3;}
} else { navigate(`/messages?talentId=${talent.id}`) } } const handleBook = e => { e.preventDefault() e.stopPropagation() if (onBook) { onBook(talent) } } const handleViewProfile = ( \" navigate(\"/talent/${talent.id) => {
  return $3;}
}\") }';\";
import React from\';react\' const TalentCardComponent = ( const navigate = useNavigate() const handleMessage = e => { e.preventDefault() e.stopPropagation() if (onMessage) { onMessage(talent) ) => {
  return $3;}
} else { navigate(`/messages?talentId=${talent.id}`) } } const handleBook = e => { e.preventDefault() e.stopPropagation() if (onBook) { onBook(talent) } } const handleViewProfile = ( \" navigate(\"/talent/${talent.id) => {
  return $3;}
}\") }\';\";
const React from\";react\" const TalentCardComponent = ( const navigate = useNavigate() const handleMessage = e => { e.preventDefault() e.stopPropagation() if (onMessage) { onMessage(talent) ) => {
  return $3;}
} else { navigate(`/messages?talentId=${talent.id}`) } } const handleBook = e => { e.preventDefault() e.stopPropagation() if (onBook) { onBook(talent) } } const handleViewProfile = ( \" navigate(\"/talent/${talent.id) => {
  return $3;}
}\") }\";\"\"\"`\"`;
ursor/automate-test-improve-and-merge-code-646c;
import React from\';react\' const TalentCardComponent = ( const navigate = useNavigate() const handleMessage = e => { e && e.preventDefault() e && e.stopPropagation() if (onMessage) { onMessage(talent) ) => {
  return $3;}
} else { navigate(`/messages?talentId=${talent && talent.id}`) } } const handleBook = e => { e && e.preventDefault() e && e.stopPropagation() if (onBook) { onBook(talent) } } const handleViewProfile = ( \" navigate(\"/talent/${talent && talent.id) => {
  return $3;}
}\") }\';\";
const React from\";react\" const TalentCardComponent = ( const navigate = useNavigate() const handleMessage = e => { e && e.preventDefault() e && e.stopPropagation() if (onMessage) { onMessage(talent) ) => {
  return $3;}
} else { navigate(`/messages?talentId=${talent && talent.id}`) } } const handleBook = e => { e && e.preventDefault() e && e.stopPropagation() if (onBook) { onBook(talent) } } const handleViewProfile = ( \" navigate(\"/talent/${talent && talent.id) => {
  return $3;}
}\") }\";\"\"\"`\"`;
import _React from';react' const TalentCardComponent = ( const navigate = useNavigate() const handleMessage = e => { e && e.preventDefault() e && e.stopPropagation() if (onMessage) { onMessage(talent) ) => {
  return $3;}
} else { navigate(`/messages?talentId=${talent && talent.id}`) } } const handleBook = e => { e && e.preventDefault() e && e.stopPropagation() if (onBook) { onBook(talent) } } const handleViewProfile = ( \" navigate(\"/talent/${talent && talent.id) => {
  return $3;}
}\") }';\";
const React from\";react\" const TalentCardComponent = ( const navigate = useNavigate() const handleMessage = e => { e.preventDefault() e.stopPropagation() if (onMessage) { onMessage(talent) ) => {
  return $3;}
} else { navigate(`/messages?talentId=${talent.id}`) } } const handleBook = e => { e.preventDefault() e.stopPropagation() if (onBook) { onBook(talent) } } const handleViewProfile = ( \" navigate(\"/talent/${talent.id) => {
  return $3;}
}\") }\";\"\"\"`\"`;
ursor/automate-test-improve-and-merge-code-646c;
const React from\";react\" const TalentCardComponent = ( const navigate = useNavigate() const handleMessage = e => { e && e.preventDefault() e && e.stopPropagation() if (onMessage) { onMessage(talent) ) => {
  return $3;}
} else { navigate(`/messages?talentId=${talent && talent.id}`) } } const handleBook = e => { e && e.preventDefault() e && e.stopPropagation() if (onBook) { onBook(talent) } } const handleViewProfile = ( \" navigate(\"/talent/${talent && talent.id) => {
  return $3;}
}\") }\";\"\"\"`\"`;
import _React from';react' const TalentCardComponent = ( const navigate = useNavigate() const handleMessage = e => { e.preventDefault() e.stopPropagation() if (onMessage) { onMessage(talent) ) => {
  return $3;}
} else { navigate(`/messages?talentId=${talent.id}`) } } const handleBook = e => { e.preventDefault() e.stopPropagation() if (onBook) { onBook(talent) } } const handleViewProfile = ( \" navigate(\"/talent/${talent.id) => {
  return $3;}
}\") }';\"




import React from\';react\' const TalentCardComponent = ( const navigate = useNavigate() const handleMessage = e => { e && e.preventDefault() e && e.stopPropagation() if (onMessage) { onMessage(talent) ) => {
  return $3;}
} else { navigate(`/messages?talentId=${talent && talent.id}`) } } const handleBook = e => { e && e.preventDefault() e && e.stopPropagation() if (onBook) { onBook(talent) } } const handleViewProfile = ( \" navigate(\"/talent/${talent && talent.id) => {
  return $3;}
}\") }\';\"
const React from\";react\" const TalentCardComponent = ( const navigate = useNavigate() const handleMessage = e => { e && e.preventDefault() e && e.stopPropagation() if (onMessage) { onMessage(talent) ) => {
  return $3;}
} else { navigate(`/messages?talentId=${talent && talent.id}`) } } const handleBook = e => { e && e.preventDefault() e && e.stopPropagation() if (onBook) { onBook(talent) } } const handleViewProfile = ( \" navigate(\"/talent/${talent && talent.id) => {
  return $3;}
}\") }\";\"\"\"`\"`
import _React from';react' const TalentCardComponent = ( const navigate = useNavigate() const handleMessage = e => { e && e.preventDefault() e && e.stopPropagation() if (onMessage) { onMessage(talent) ) => {
  return $3;}
} else { navigate(`/messages?talentId=${talent && talent.id}`) } } const handleBook = e => { e && e.preventDefault() e && e.stopPropagation() if (onBook) { onBook(talent) } } const handleViewProfile = ( \" navigate(\"/talent/${talent && talent.id) => {
  return $3;}
}\") }';\"

