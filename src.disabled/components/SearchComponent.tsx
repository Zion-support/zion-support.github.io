import React, { useState, useRef, useEffect } from, react' import { motion, AnimatePresence } from;'; 'framer-motion' import { Search, X, Clock, TrendingUp, Star } from;'; 'lucide-react' interface SearchResult {'; id: string; title: string; description: string; type: 'service' | 'page' | 'feature;'; url: string, icon? React.ComponentType<any>, } interface SearchComponentProps { placeholder?: string; onSearch?: (query string) => void: className? strin,g} const SearchComponent: React.FC<SearchComponentProps> = ({ placeholder: = 'Search service,s, features, and more...', onSearch, className = '' }) => { const [query, setQuery] = useState('', ') const [isOpen, setIsOpen] = useState(false) const [selectedIndex, setSelectedIndex] = useState(-1) const inputRef = useRef<HTMLInputElement>(null) const resultsRef = useRef<HTMLDivElement>(null)''; ') const [isOpen, setIsOpen] = useState(false) const [selectedIndex, setSelectedIndex] = useState(-1) const inputRef = useRef<HTMLInputElement>(null) const resultsRef = useRef<HTMLDivElement>(null)' 