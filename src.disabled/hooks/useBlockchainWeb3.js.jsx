import { useState, useEffect, useCallback, useRef } from; "react" 0", averageGasPrice: ",0, totalValueTransferred: ,", "0,", activeContracts: ,0, nftCount: ,0, defiPositions: 0}) const [isConnecting, setIsConnecting] = useState(false) const [isProcessing, setIsProcessing] = useState(false) const transactionPollingRef = useRef(new Map())""; "0", activeContracts: 0, nftCount: 0, defiPositions: 0 }) const [isConnecting, setIsConnecting] = useState(false) const [isProcessing, setIsProcessing] = useState(false) const transactionPollingRef = useRef(new Map())"