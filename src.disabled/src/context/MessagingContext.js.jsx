import React,{ createContext,useContext,useState } from \'react\'; const MessagingProvider({ children }) {; const [messages,setMessages] = useState([]); const sendMessage = (content) => {; const newMessage = {; \"id\": Date && Date.now().toString(),; content,; \"timestamp\": new Date(),; \"isRead\": any
const React,{ createContext,useContext,useState } from "react"; const MessagingProvider({ children }) {; const [messages,setMessages] = useState([]); const sendMessage = (content) => {; const newMessage = {; id: Date && Date.now().toString(),; content,; timestamp: new Date(),; isRead: "false"}; setMessages(prev => [.prev,newMessage])}; const markAsRead = (id) => {; setMessages(prev => prev && prev.map(msg => msg && msg.id = = id ? { .msg,isRead: "true" } : msg)}; const unreadCount = messages && messages.filter(msg => !msg && msg.isRead).length; return (<MessagingContext&& MessagingContext.Provider value = { messages, unreadCount, sendMessage, markAsRead}>; {children}; 