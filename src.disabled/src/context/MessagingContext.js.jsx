<<<<<<< HEAD
=======
<<<<<<< HEAD
import React,{ createContext,useContext,useState } from \'react\'; const MessagingProvider({ children }) {; const [messages,setMessages] = useState([]); const sendMessage = (content) => {; const newMessage = {; \"id\": Date.now().toString(),; content,; \"timestamp\": new Date(),; \"isRead\": \'false\'}; setMessages(prev => [...prev,newMessage])}; const markAsRead = (id) => {; setMessages(prev => prev.map(msg => msg.id === id ? { ...msg,\"isRead\": \'true\' } : msg))}; const unreadCount = messages.filter(msg => !msg.isRead).length; return(<MessagingContext.Provider value = {; {; messages,; unreadCount,; sendMessage,; markAsRead}}>; {children}; </MessagingContext.Provider>)}; export function useMessaging() {; const context = useContext(MessagingContext); if(context === null) {; throw new Error(\'useMessaging must be used within a MessagingProvider\')}; return context};
const React,{ createContext,useContext,useState } from "react"; const MessagingProvider({ children }) {; const [messages,setMessages] = useState([]); const sendMessage = (content) => {; const newMessage = {; id: Date.now().toString(),; content,; timestamp: new Date(),; isRead: "false"}; setMessages(prev => [.prev,newMessage])}; const markAsRead = (id) => {; setMessages(prev => prev.map(msg => msg.id === id ? { .msg,isRead: "true" } : msg))}; const unreadCount = messages.filter(msg => !msg.isRead).length; return(<MessagingContext.Provider value = {; {; messages,; unreadCount,; sendMessage,; markAsRead}}>; {children}; </MessagingContext.Provider>)}; export function useMessaging() {; const context = useContext(MessagingContext); if(context === null) {; throw new Error("useMessaging must be used within a MessagingProvider")}; return context};'"'"
import _React,{ createContext,useContext,useState } from 'react'; const MessagingProvider({ children }) {; const [messages,setMessages] = useState([]); const sendMessage = (content) => {; const newMessage = {; "id": Date.now().toString(),; content,; "timestamp": new Date(),; "isRead": 'false'}; setMessages(prev => [...prev,newMessage])}; const markAsRead = (id) => {; setMessages(prev => prev.map(msg => msg.id === id ? { ...msg,"isRead": 'true' } : msg))}; const unreadCount = messages.filter(msg => !msg.isRead).length; return(<MessagingContext.Provider value = {; {; messages,; unreadCount,; sendMessage,; markAsRead}}>; {children}; </MessagingContext.Provider>)}; export function useMessaging() {; const context = useContext(MessagingContext); if(context === null) {; throw new Error('useMessaging must be used within a MessagingProvider')}; return context};
=======
import React,{ createContext,useContext,useState } from \'react\'; const MessagingProvider({ children }) {; const [messages,setMessages] = useState([]); const sendMessage = (content) => {; const newMessage = {; \"id\": Date && Date.now().toString(),; content,; \"timestamp\": new Date(),; \"isRead\": \'false\'}; setMessages(prev => [...prev,newMessage])}; const markAsRead = (id) => {; setMessages(prev => prev && prev.map(msg => msg && msg.id === id ? { ...msg,\"isRead\": \'true\' } : msg))}; const unreadCount = messages && messages.filter(msg => !msg && msg.isRead).length; return (<MessagingContext&& MessagingContext.Provider value = { { messages, unreadCount, sendMessage, markAsRead}}>; {children}; </MessagingContext && MessagingContext.Provider>)}; export function useMessaging() {; const context = useContext(MessagingContext); if(context === null) {; throw new Error(\'useMessaging must be used within a MessagingProvider\')}; return context};
const React,{ createContext,useContext,useState } from "react"; const MessagingProvider({ children }) {; const [messages,setMessages] = useState([]); const sendMessage = (content) => {; const newMessage = {; id: Date && Date.now().toString(),; content,; timestamp: new Date(),; isRead: "false"}; setMessages(prev => [.prev,newMessage])}; const markAsRead = (id) => {; setMessages(prev => prev && prev.map(msg => msg && msg.id === id ? { .msg,isRead: "true" } : msg))}; const unreadCount = messages && messages.filter(msg => !msg && msg.isRead).length; return (<MessagingContext&& MessagingContext.Provider value = { { messages, unreadCount, sendMessage, markAsRead}}>; {children}; </MessagingContext && MessagingContext.Provider>)}; export function useMessaging() {; const context = useContext(MessagingContext); if(context === null) {; throw new Error("useMessaging must be used within a MessagingProvider")}; return context};'"'"
>>>>>>> origin/cursor/fix-website-loading-errors-and-merge-8339

import _React,{ createContext,useContext,useState } from 'react; const MessagingProvider({ children }) {const [messages,setMessages] = useState([])const sendMessage = (content) => {const newMessage = {"id: Date.now().toString()content; timestamp": new Date()"isRead: false'}setMessages(prev => [...prev,newMessage])}const markAsRead = (id) => {setMessages(prev => prev.map(msg => msg.id === id ? { ...msg,isRead": 'true } : msg))}const unreadCount = messages.filter(msg => !msg.isRead).length; return(<MessagingContext.Provider value = {{messages,unreadCount,sendMessage,markAsRead}}>; {children}</MessagingContext.Provider>)}export function useMessaging() {const context = useContext(MessagingContext)if(context === null) {throw new Error(useMessaging must be used within a MessagingProvider')}return context}import React,{ createContext,useContext,useState } from \'react\; const MessagingProvider({ children }) {const [messages,setMessages] = useState([])const sendMessage = (content) => {const newMessage = {\"id\: Date && Date.now().toString(),content,\timestamp\": new Date(),\"isRead\: \false\'}setMessages(prev => [...prev,newMessage])}const markAsRead = (id) => {setMessages(prev => prev && prev.map(msg => msg && msg.id === id ? { ...msg,\isRead\": \'true\ } : msg))}const unreadCount = messages && messages.filter(msg => !msg && msg.isRead).length; return (<MessagingContext&& MessagingContext.Provider value = { { messages, unreadCount, sendMessage, markAsRead}}>; {children}</MessagingContext && MessagingContext.Provider>)}export function useMessaging() {const context = useContext(MessagingContext)if(context === null) {throw new Error(\useMessaging must be used within a MessagingProvider\')}return context}const React,{ createContext,useContext,useState } from "react; const MessagingProvider({ children }) {const [messages,setMessages] = useState([])const sendMessage = (content) => {const newMessage = {id: Date && Date.now().toString(),content,timestamp: new Date(),isRead: false"}setMessages(prev => [.prev,newMessage])}const markAsRead = (id) => {setMessages(prev => prev && prev.map(msg => msg && msg.id === id ? { .msg,isRead: "true } : msg))}const unreadCount = messages && messages.filter(msg => !msg && msg.isRead).length; return (<MessagingContext&& MessagingContext.Provider value = { { messages, unreadCount, sendMessage, markAsRead}}>; {children}</MessagingContext && MessagingContext.Provider>)}export function useMessaging() {const context = useContext(MessagingContext)if(context === null) {throw new Error(useMessaging must be used within a MessagingProvider")}return context}'";
ursor/automate-test-improve-and-merge-code-646c;
import _React,{ createContext,useContext,useState } from react'; const MessagingProvider({ children }) {const [messages,setMessages] = useState([])const sendMessage = (content) => {const newMessage = {id": Date.now().toString(),content,"timestamp: new Date(),isRead": 'false}setMessages(prev => [...prev,newMessage])}const markAsRead = (id) => {setMessages(prev => prev.map(msg => msg.id === id ? { ...msg,"isRead: true' } : msg))}const unreadCount = messages.filter(msg => !msg.isRead).length; return(<MessagingContext.Provider value = {{messages,unreadCount,sendMessage,markAsRead}}>; {children}</MessagingContext.Provider>)}export function useMessaging() {const context = useContext(MessagingContext)if(context === null) {throw new Error('useMessaging must be used within a MessagingProvider)}return context}import React,{ createContext,useContext,useState } from \react\'; const MessagingProvider({ children }) {const [messages,setMessages] = useState([])const sendMessage = (content) => {const newMessage = {\id\": Date.now().toString(),content,\"timestamp\: new Date(),\isRead\": \'false\}setMessages(prev => [...prev,newMessage])}const markAsRead = (id) => {setMessages(prev => prev.map(msg => msg.id === id ? { ...msg,\"isRead\: \true\' } : msg))}const unreadCount = messages.filter(msg => !msg.isRead).length; return(<MessagingContext.Provider value = {{messages,unreadCount,sendMessage,markAsRead}}>; {children}</MessagingContext.Provider>)}export function useMessaging() {const context = useContext(MessagingContext)if(context === null) {throw new Error(\'useMessaging must be used within a MessagingProvider\)}return context}const React,{ createContext,useContext,useState } from react"; const MessagingProvider({ children }) {const [messages,setMessages] = useState([])const sendMessage = (content) => {const newMessage = {id: Date.now().toString(),content,timestamp: new Date(),isRead: "false}setMessages(prev => [.prev,newMessage])}const markAsRead = (id) => {setMessages(prev => prev.map(msg => msg.id === id ? { .msg,isRead: true" } : msg))}const unreadCount = messages.filter(msg => !msg.isRead).length; return(<MessagingContext.Provider value = {{messages,unreadCount,sendMessage,markAsRead}}>; {children}</MessagingContext.Provider>)}export function useMessaging() {const context = useContext(MessagingContext)if(context === null) {throw new Error("useMessaging must be used within a MessagingProvider)}return context}'";
ursor/automate-test-improve-and-merge-code-646c;
import _React,{ createContext,useContext,useState } from 'react; const MessagingProvider({ children }) {const [messages,setMessages] = useState([])const sendMessage = (content) => {const newMessage = {"id: Date && Date.now().toString(),content,timestamp": new Date(),"isRead: false'}setMessages(prev => [...prev,newMessage])}const markAsRead = (id) => {setMessages(prev => prev && prev.map(msg => msg && msg.id === id ? { ...msg,isRead": 'true } : msg))}const unreadCount = messages && messages.filter(msg => !msg && msg.isRead).length; return (<MessagingContext&& MessagingContext.Provider value = { { messages, unreadCount, sendMessage, markAsRead}}>; {children}</MessagingContext && MessagingContext.Provider>)}export function useMessaging() {const context = useContext(MessagingContext)if(context === null) {throw new Error(useMessaging must be used within a MessagingProvider')}return context}const React,{ createContext,useContext,useState } from "react; const MessagingProvider({ children }) {const [messages,setMessages] = useState([])const sendMessage = (content) => {const newMessage = {id: Date.now().toString(),content,timestamp: new Date(),isRead: false"}setMessages(prev => [.prev,newMessage])}const markAsRead = (id) => {setMessages(prev => prev.map(msg => msg.id === id ? { .msg,isRead: "true } : msg))}const unreadCount = messages.filter(msg => !msg.isRead).length; return(<MessagingContext.Provider value = {{messages,unreadCount,sendMessage,markAsRead}}>; {children}</MessagingContext.Provider>)}export function useMessaging() {const context = useContext(MessagingContext)if(context === null) {throw new Error(useMessaging must be used within a MessagingProvider")}return context}'";
import _React,{ createContext,useContext,useState } from react'; const MessagingProvider({ children }) { const [messages,setMessages] = useState([]); const sendMessage = (content) => { const newMessage = { id": Date.now().toString(); content, "timestamp: new Date(), isRead": 'false} setMessages(prev => [...prev,newMessage])} const markAsRead = (id) => { setMessages(prev => prev.map(msg => msg.id === id ? { ...msg,"isRead: true' } : msg))} const unreadCount = messages.filter(msg => !msg.isRead).length; return(<MessagingContext.Provider value = { { messages, unreadCount, sendMessage, markAsRead}}>; {children} </MessagingContext.Provider>)} export function useMessaging() { const context = useContext(MessagingContext); if(context === null) { throw new Error('useMessaging must be used within a MessagingProvider)} return context}

<<<<<<< HEAD
import React,{ createContext,useContext,useState } from \react\'; const MessagingProvider({ children }) { const [messages,setMessages] = useState([]); const sendMessage = (content) => { const newMessage = { \id\": Date && Date.now().toString(), content, \"timestamp\: new Date(), \isRead\": \'false\} setMessages(prev => [...prev,newMessage])} const markAsRead = (id) => { setMessages(prev => prev && prev.map(msg => msg && msg.id === id ? { ...msg,\"isRead\: \true\' } : msg))} const unreadCount = messages && messages.filter(msg => !msg && msg.isRead).length; return (<MessagingContext&& MessagingContext.Provider value = { { messages, unreadCount, sendMessage, markAsRead}}>; {children} </MessagingContext && MessagingContext.Provider>)} export function useMessaging() { const context = useContext(MessagingContext); if(context === null) { throw new Error(\'useMessaging must be used within a MessagingProvider\)} return context}
const React,{ createContext,useContext,useState } from react"; const MessagingProvider({ children }) { const [messages,setMessages] = useState([]); const sendMessage = (content) => { const newMessage = { id: Date && Date.now().toString(), content, timestamp: new Date(), isRead: "false} setMessages(prev => [.prev,newMessage])} const markAsRead = (id) => { setMessages(prev => prev && prev.map(msg => msg && msg.id === id ? { .msg,isRead: true" } : msg))} const unreadCount = messages && messages.filter(msg => !msg && msg.isRead).length; return (<MessagingContext&& MessagingContext.Provider value = { { messages, unreadCount, sendMessage, markAsRead}}>; {children} </MessagingContext && MessagingContext.Provider>)} export function useMessaging() { const context = useContext(MessagingContext); if(context === null) { throw new Error("useMessaging must be used within a MessagingProvider)} return context}'"

import _React,{ createContext,useContext,useState } from 'react; const MessagingProvider({ children }) { const [messages,setMessages] = useState([]); const sendMessage = (content) => { const newMessage = { "id: Date && Date.now().toString(), content, timestamp": new Date(), "isRead: false'} setMessages(prev => [...prev,newMessage])} const markAsRead = (id) => { setMessages(prev => prev && prev.map(msg => msg && msg.id === id ? { ...msg,isRead": 'true } : msg))} const unreadCount = messages && messages.filter(msg => !msg && msg.isRead).length; return (<MessagingContext&& MessagingContext.Provider value = { { messages, unreadCount, sendMessage, markAsRead}}>; {children} </MessagingContext && MessagingContext.Provider>)} export function useMessaging() { const context = useContext(MessagingContext); if(context === null) { throw new Error(useMessaging must be used within a MessagingProvider')} return context}


=======
import _React,{ createContext,useContext,useState } from 'react; const MessagingProvider({ children }) { const [messages,setMessages] = useState([]); const sendMessage = (content) => { const newMessage = { "id: Date.now().toString(), content, timestamp": new Date(), "isRead: false'} setMessages(prev => [...prev,newMessage])} const markAsRead = (id) => { setMessages(prev => prev.map(msg => msg.id === id ? { ...msg,isRead": 'true } : msg))} const unreadCount = messages.filter(msg => !msg.isRead).length; return(<MessagingContext.Provider value = { { messages, unreadCount, sendMessage, markAsRead}}>; {children} </MessagingContext.Provider>)} export function useMessaging() { const context = useContext(MessagingContext); if(context === null) { throw new Error(useMessaging must be used within a MessagingProvider')} return context}
>>>>>>> origin/cursor/fix-netlify-build-and-merge-to-main-2a0c
=======
>>>>>>> 89e5074e89029fee0b574fe9cfff0a488d2ce422
import React,{ createContext,useContext,useState } from \'react\; const MessagingProvider({ children }) { const [messages,setMessages] = useState([]); const sendMessage = (content) => { const newMessage = { \"id\: Date.now().toString(), content, \timestamp\": new Date(), \"isRead\: \false\'} setMessages(prev => [...prev,newMessage])} const markAsRead = (id) => { setMessages(prev => prev.map(msg => msg.id === id ? { ...msg,\isRead\": \'true\ } : msg))} const unreadCount = messages.filter(msg => !msg.isRead).length; return(<MessagingContext.Provider value = { { messages, unreadCount, sendMessage, markAsRead}}>; {children} </MessagingContext.Provider>)} export function useMessaging() { const context = useContext(MessagingContext); if(context === null) { throw new Error(\useMessaging must be used within a MessagingProvider\')} return context}
const React,{ createContext,useContext,useState } from "react; const MessagingProvider({ children }) { const [messages,setMessages] = useState([]); const sendMessage = (content) => { const newMessage = { id: Date.now().toString(), content, timestamp: new Date(), isRead: false"} setMessages(prev => [.prev,newMessage])} const markAsRead = (id) => { setMessages(prev => prev.map(msg => msg.id === id ? { .msg,isRead: "true } : msg))} const unreadCount = messages.filter(msg => !msg.isRead).length; return(<MessagingContext.Provider value = { { messages, unreadCount, sendMessage, markAsRead}}>; {children} </MessagingContext.Provider>)} export function useMessaging() { const context = useContext(MessagingContext); if(context === null) { throw new Error(useMessaging must be used within a MessagingProvider")} return context}'"'"
>>>>>>> cursor/automate-test-improve-and-merge-code-18b6
=======
>>>>>>> cursor/expand-services-advertise-and-build-project-4b36
>>>>>>> origin/cursor/fix-website-loading-errors-and-merge-8339
