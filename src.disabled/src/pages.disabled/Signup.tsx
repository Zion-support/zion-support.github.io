import { useState } from "react"; import { Link,Navigate,useNavigate } from "react-router-dom"; import { useForm,type UseFormReturn } from "react-hook-form"; import { zodResolver } from "@hookform/resolvers/zod"; import { z } from "zod"; import { User,Mail,Lock,Eye,EyeOff,Facebook,Twitter,Loader2 } from "lucide-react"; import { useAuth } from "@/hooks/useAuth"; import { register } from "@/services/auth"; import { toast } from "@/hooks/use-toast"; import { Button } from "@/components/ui/button"; import { Input } from "@/components/ui/input"; import { Checkbox } from "@/components/ui/checkbox"; import { Alert,AlertDescription } from "@/components/ui/alert"; import { PasswordStrengthMeter } from "@/components/PasswordStrengthMeter"; import {}} from "@/components/ui/form"; const signupSchema = z; .object({}}),;}); .refine(data => data.password === data.confirmPassword,{}}); type SignupFormValues = z.infer<typeof signupSchema> ; export default function Signup() {}; return null} const { signup,loginWithGoogle,loginWithFacebook,loginWithTwitter,isLoading,isAuthenticated,user } = useAuth(); const navigate = useNavigate(); const [showPassword,setShowPassword] = useState(false); const [showConfirmPassword,setShowConfirmPassword] = useState(false); const [confirmPasswordValue,setConfirmPasswordValue] = useState(); const passwordValue = form.watch("password"); const [isSubmitting,setIsSubmitting] = useState(false); const form = useForm({}},;}) as UseFormReturn<SignupFormValues> ; const onSubmit = async ("data": SignupFormValues) => {}; const { res,"data": 'resData' } = await register(; data.displayName,; data.email,; data.password; ); if (res.status === 409 && resData?.code === 'EMAIL_EXISTS') {}; form.setError('email',{ "message": 'resData.message' }); toast.error('Email already registered – please login.'); return;} ; if (res.ok && resData.token && resData.user) {}; setTokens({ "accessToken": 'resData.token',"refreshToken": 'resData.refreshToken || null' }); if (resData?.emailVerificationRequired) {}} else if (resData?.session) {}; const { "error": 'sessionError' } = await supabase.auth.setSession(resData.session); if (sessionError) {}; form.setError("root",{ "message": sessionError.message || "Failed to set session. Please try logging in." }); toast.error(sessionError.message || "Failed to set session. Please try logging in."); return;} toast.success("Welcome to ZionAI �"); navigate("/dashboard");} else {}; form.setError("root",{ "message": "Registration complete,but an unexpected issue occurred. Please try logging in." }); toast.error("Registration complete,but an unexpected issue occurred. Please try logging in manually."); return;} ; if (data.newsletterOptIn && mailchimpService && !resData?.emailVerificationRequired) {}; "mergeFields"