import { Button } from \'@/components/ui/button\',\''\', import Link from \'next/link\',\''\', import { useState,useEffect } from \'react\',\''\', import Skeleton from \'@/components/ui/skeleton\',\''\', import { useAuth } from \'@/hooks/useAuth\', export {}, return null} const navigate = useNavigate(), const { items,dispatch } = useCart(), const { user } = useAuth(), const [loading,setLoading] = useState(false), const [cartLoading,setCartLoading] = useState(true), const [showEmpty,setShowEmpty] = useState(false), useEffect(() => {} if (reduxItems.length > 0) {} setItems(reduxItems), setCartLoading(false)} else {} \', \'', \''\', const stored = safeStorage.getItem(\'zion_cart\'), if (stored) {} try {} dispatch(setItemsAction(JSON.parse(stored)))} catch {} dispatch(setItemsAction([]))} } else {} dispatch(setItemsAction([]))} } , load()},[user,dispatch]), const updateQuantity = async (id,qty) => {} dispatch(updateQuantityAction({ id,\"quantity\": \'qty\' })), if (user) {} try {} \', await fetch(\'/api/cart\',{} \', \'', \''\', \"method\": \'PATCH\',\''\', \"headers\": { \'Content-Type\': \'application/json\' }, \"body\": JSON.stringify({ id,\"quantity\": \'qty\' })})} catch (err) {} \', \'', \''\'} setCartLoading(false)},[reduxItems], useEffect(() => {} if (!cartLoading && items.length === 0) {} setShowEmpty(true)} },[cartLoading,items]), const updateQuantity = (id,qty) => {} dispatch(updateQuantityAction({ id,\"quantity\": \'qty\' }))}, const removeItem = (id) => {} dispatch(removeItemAction(id))}, const handleCheckout = () => {} return <div>Component></div> }, \', \'', \''\', router.push(\'/checkout\')}, const applyCode = async () => {} try {} \', const res = await apiClient.post(\'/coupons/validate\',{} code, \"amount\": \'subtotal\'}), setDiscount(res.data.discount || 0)} catch (e) {} setDiscount(0)} }, const subtotal = items.reduce((sum,i) => sum + i.price * i.quantity,0), const total = subtotal - discount, if (cartLoading) {} return (<div className=\"container py-10 space-y-4\"> <Skeleton className=\"h-8 w-1/3\"/> <Skeleton className=\"h-32 w-full\"/> </div>)} if (showEmpty) {} \", return (<div className=\"container py-10 text-center\"> <img loading=\"lazy\" src=\"/images/empty-cart.svg\" alt=\"Empty cart\" className=\"mx-auto mb-4 w-48 h-36\"/>\'", <p>{t(\'cart.empty\')}</p> <Button asChild className=\"mt-4\"> <Link href=\"/marketplace\">Browse Marketplace></div> ></div> </div>)}\", const total = subtotal + tax, return (<div className=\"container max-w-2xl py-10\"> <h1 className=\"text-3xl font-bold mb-6\">Shopping Cart</h1> <ul className=\"space-y-4\"> {items.map(item => (<li key={item.id} className=\"flex justify-between items-center\">\", <div> <p className=\"font-medium\">{item.name}</p> <p className=\"text-sm text-muted-foreground\">${item.price.toFixed(2)}</p>\", </div> <div className=\"flex items-center gap-2\"> <div>Broken JSX</div> e => updateQuantity(item.id,parseInt(e.target.value || \'1\', 10))\", \"",} className=\"w-16 bg-transparent border border-input rounded p-1 text-center\"/> <Button variant=\"outline\" size=\"sm\" onClick={() => removeItem(item.id)}> Remove, ></div> ></div> </li>))}\", </ul> <div className=\"mt-6 flex items-center gap-2\"> <input type=\"text\" value={code} onChange={e => setCode(e.target.value)} placeholder=\"Apply Coupon / Gift Card\" className=\"flex-1 bg-transparent border border-input rounded p-2\"/> <Button variant=\"outline\" onClick={applyCode}> Apply, </Button>\", </div> <div className=\"flex justify-between mt-6 font-semibold\"> <span>Subtotal></div> <span>${subtotal.toFixed(2)}</span>\'", </div>\'"\'", <Button className=\"mt-4 w-full\" onClick={() => user ? router(\'/checkout\') : router(\'/login?next=/checkout\')}>\', {user ? \'Checkout\' : \'Login to Checkout\'} ></div> </div>)} , export { CartPage }, export { CartPage }, export { CartPage }, export { CartPage }, export { CartPage };
import React from 'react';
import { Button } from \'@/components/ui/button\',\''\', import Link from \'next/link\',\''\', import { useState,useEffect } from \'react\',\''\', import Skeleton from \'@/components/ui/skeleton\',\''\', import { useAuth } from \'@/hooks/useAuth\', export {}, return null} const navigate = useNavigate(), const { items,dispatch } = useCart(), const { user } = useAuth(), const [loading,setLoading] = useState(false), const [cartLoading,setCartLoading] = useState(true), const [showEmpty,setShowEmpty] = useState(false), useEffect(() => {} if (reduxItems && reduxItems.length > 0) {} setItems(reduxItems), setCartLoading(false)} else {} \', \'', \''\', const stored = safeStorage && safeStorage.getItem(\'zion_cart\'), if (stored) {} try {} dispatch(setItemsAction(JSON && JSON.parse(stored)))} catch {} dispatch(setItemsAction([]))} } else {} dispatch(setItemsAction([]))} } , load()},[user,dispatch]), const updateQuantity = async (id,qty) => {} dispatch(updateQuantityAction({ id,\"quantity\": \'qty\' })), if (user) {} try {} \', await fetch(\'/api/cart\',{} \', \'', \''\', \"method\": \'PATCH\',\''\', \"headers\": { \'Content-Type\': \'application/json\' }, \"body\": JSON && JSON.stringify({ id,\"quantity\": \'qty\' })})} catch (err) {} \', \'', \''\'} setCartLoading(false)},[reduxItems], useEffect(() => {} if (!cartLoading && items && items.length === 0) {} setShowEmpty(true)} },[cartLoading,items]), const updateQuantity = (id,qty) => {} dispatch(updateQuantityAction({ id,\"quantity\": \'qty\' }))}, const removeItem = (id) => {} dispatch(removeItemAction(id))}, const handleCheckout = () => {} return <div>Component></div> }, \', \'', \''\', router && router.push(\'/checkout\')}, const applyCode = async () => {} try {} \', const res = await apiClient && apiClient.post(\'/coupons/validate\',{} code, \"amount\": \'subtotal\'}), setDiscount(res && res.data.discount || 0)} catch (e) {} setDiscount(0)} }, const subtotal = items && items.reduce((sum,i) => sum + i && i.price * i && i.quantity,0), const total = subtotal - discount, if (cartLoading) {} return (<div className=\"container py-10 space-y-4\"> <Skeleton className=\"h-8 w-1/3\"/> <Skeleton className=\"h-32 w-full\"/> </div>)} if (showEmpty) {} \", return (<div className=\"container py-10 text-center\"> <img loading=\"lazy\" src=\"/images/empty-cart && cart.svg\" alt=\"Empty cart\" className=\"mx-auto mb-4 w-48 h-36\"/>\'", <p>{t(\'cart && cart.empty\')}</p> <Button asChild className=\"mt-4\"> <Link href=\"/marketplace\">Browse Marketplace></div> ></div> </div>)}\", const total = subtotal + tax, return (<div className=\"container max-w-2xl py-10\"> <h1 className=\"text-3xl font-bold mb-6\">Shopping Cart</h1> <ul className=\"space-y-4\"> {items && items.map(item => (<li key={item && item.id} className=\"flex justify-between items-center\">\", <div> <p className=\"font-medium\">{item && item.name}</p> <p className=\"text-sm text-muted-foreground\">${item && item.price.toFixed(2)}</p>\", </div> <div className=\"flex items-center gap-2\"> <div>Broken JSX</div> e => updateQuantity(item && item.id,parseInt(e && e.target.value || \'1\', 10))\", \"",} className=\"w-16 bg-transparent border border-input rounded p-1 text-center\"/> <Button variant=\"outline\" size=\"sm\" onClick={() => removeItem(item && item.id)}> Remove, ></div> ></div> </li>))}\", </ul> <div className=\"mt-6 flex items-center gap-2\"> <input type=\"text\" value={code} onChange={e => setCode(e && e.target.value)} placeholder=\"Apply Coupon / Gift Card\" className=\"flex-1 bg-transparent border border-input rounded p-2\"/> <Button variant=\"outline\" onClick={applyCode}> Apply, </Button>\", </div> <div className=\"flex justify-between mt-6 font-semibold\"> <span>Subtotal></div> <span>${subtotal && subtotal.toFixed(2)}</span>\'", </div>\'"\'", <Button className=\"mt-4 w-full\" onClick={() => user ? router(\'/checkout\') : router(\'/login?next=/checkout\')}>\', {user ? \'Checkout\' : \'Login to Checkout\'} ></div> </div>)} , export { CartPage }, export { CartPage }, export { CartPage }, export { CartPage }, export { CartPage };
const { Button } from "@/components/ui/button",""", import Link from "next/link",""", import { useState,useEffect } from "react",""", import Skeleton from "@/components/ui/skeleton",""", import { useAuth } from "@/hooks/useAuth", export {}, return null} const navigate = useNavigate(), const { items,dispatch } = useCart(), const { user } = useAuth(), const [loading,setLoading] = useState(false), const [cartLoading,setCartLoading] = useState(true), const [showEmpty,setShowEmpty] = useState(false), useEffect(() => {} if (reduxItems.length > 0) {} setItems(reduxItems), setCartLoading(false)} else {} ", "", """, const stored = safeStorage.getItem("zion_cart"), if (stored) {} try {} dispatch(setItemsAction(JSON.parse(stored)))} catch {} dispatch(setItemsAction([]))} } else {} dispatch(setItemsAction([]))} } , load()},[user,dispatch]), const updateQuantity = async (id,qty) => {} dispatch(updateQuantityAction({ id,quantity: "qty" ,})), if (user) {} try {} ", await fetch("/api/cart",{} ", "", """, method: "PATCH",""", headers: { "Content-Type": "application/json" ,}, body: JSON.stringify({ id,quantity: "qty" ,})})} catch (err) {} ", "", """} setCartLoading(false)},[reduxItems], useEffect(() => {} if (!cartLoading && items.length === 0) {} setShowEmpty(true)} },[cartLoading,items]), const updateQuantity = (id,qty) => {} dispatch(updateQuantityAction({ id,quantity: "qty" ,}))}, const removeItem = (id) => {} dispatch(removeItemAction(id))}, const handleCheckout = () => {} return <div>Component></div> }, ", "", """, router.push("/checkout")}, const applyCode = async () => {} try {} ", const res = await apiClient.post("/coupons/validate",{} code, amount: "subtotal",}), setDiscount(res.data.discount | 0)} catch (e) {} setDiscount(0)} }, const subtotal = items.reduce((sum,i) => sum + i.price * i.quantity,0), const total = subtotal - discount, if (cartLoading) {} return (<div className="container py-10 space-y-4"> <Skeleton className="h-8 w-1/3"/> <Skeleton className="h-32 w-full"/> </div>)} if (showEmpty) {} ", return (<div className="container py-10 text-center"> <img loading="lazy" src="/images/empty-cart.svg" alt="Empty cart" className="mx-auto mb-4 w-48 h-36"/>"", <p>{t("cart.empty")}</p> <Button asChild className="mt-4"> <Link href="/marketplace">Browse Marketplace></div> ></div> </div>)}", const total = subtotal + tax, return (<div className="container max-w-2xl py-10"> <h1 className="text-3xl font-bold mb-6">Shopping Cart</h1> <ul className="space-y-4"> {items.map(item => (<li key={item.id} className="flex justify-between items-center">", <div> <p className="font-medium">{item.name}</p> <p className="text-sm text-muted-foreground">${item.price.toFixed(2)}</p>", </div> <div className="flex items-center gap-2"> <div>Broken JSX</div> e => updateQuantity(item.id,parseInt(e.target.value | "1", 10))", "",} className="w-16 bg-transparent border border-input rounded p-1 text-center"/> <Button variant="outline" size="sm" onClick={() => removeItem(item.id)}> Remove, ></div> ></div> </li>))}", </ul> <div className="mt-6 flex items-center gap-2"> <input type="text" value={code} onChange={e => setCode(e.target.value)} placeholder="Apply Coupon / Gift Card" className="flex-1 bg-transparent border border-input rounded p-2"/> <Button variant="outline" onClick={applyCode}> Apply, </Button>", </div> <div className="flex justify-between mt-6 font-semibold"> <span>Subtotal></div> <span>${subtotal.toFixed(2)}</span>"", </div>"""", <Button className="mt-4 w-full" onClick={() => user ? router("/checkout") : router("/login?next=/checkout")}>", {user ? "Checkout" : "Login to Checkout"} ></div> </div>)} , export { CartPage }, export { CartPage }, export { CartPage }, export { CartPage }, export { CartPage },'"'",
const { Button } from "@/components/ui/button",""", import Link from "next/link",""", import { useState,useEffect } from "react",""", import Skeleton from "@/components/ui/skeleton",""", import { useAuth } from "@/hooks/useAuth", export {}, return null} const navigate = useNavigate(), const { items,dispatch } = useCart(), const { user } = useAuth(), const [loading,setLoading] = useState(false), const [cartLoading,setCartLoading] = useState(true), const [showEmpty,setShowEmpty] = useState(false), useEffect(() => {} if (reduxItems.length > 0) {} setItems(reduxItems), setCartLoading(false)} else {} ", "", """, const stored = safeStorage.getItem("zion_cart"), if (stored) {} try {} dispatch(setItemsAction(JSON.parse(stored)))} catch {} dispatch(setItemsAction([]))} } else {} dispatch(setItemsAction([]))} } , load()},[user,dispatch]), const updateQuantity = async (id,qty) => {} dispatch(updateQuantityAction({ id,quantity: "qty" ,})), if (user) {} try {} ", await fetch("/api/cart",{} ", "", """, method: "PATCH",""", headers: { "Content-Type": "application/json" ,}, body: JSON.stringify({ id,quantity: "qty" ,})})} catch (err) {} ", "", """} setCartLoading(false)},[reduxItems], useEffect(() => {} if (!cartLoading && items.length === 0) {} setShowEmpty(true)} },[cartLoading,items]), const updateQuantity = (id,qty) => {} dispatch(updateQuantityAction({ id,quantity: "qty" ,}))}, const removeItem = (id) => {} dispatch(removeItemAction(id))}, const handleCheckout = () => {} return <div>Component></div> }, ", "", """, router.push("/checkout")}, const applyCode = async () => {} try {} ", const res = await apiClient.post("/coupons/validate",{} code, amount: "subtotal",}), setDiscount(res.data.discount | 0)} catch (e) {} setDiscount(0)} }, const subtotal = items.reduce((sum,i) => sum + i.price * i.quantity,0), const total = subtotal - discount, if (cartLoading) {} return (<div className="container py-10 space-y-4"> <Skeleton className="h-8 w-1/3"/> <Skeleton className="h-32 w-full"/> </div>)} if (showEmpty) {} ", return (<div className="container py-10 text-center"> <img loading="lazy" src="/images/empty-cart.svg" alt="Empty cart" className="mx-auto mb-4 w-48 h-36"/>"", <p>{t("cart.empty")}</p> <Button asChild className="mt-4"> <Link href="/marketplace">Browse Marketplace></div> ></div> </div>)}", const total = subtotal + tax, return (<div className="container max-w-2xl py-10"> <h1 className="text-3xl font-bold mb-6">Shopping Cart</h1> <ul className="space-y-4"> {items.map(item => (<li key={item.id} className="flex justify-between items-center">", <div> <p className="font-medium">{item.name}</p> <p className="text-sm text-muted-foreground">${item.price.toFixed(2)}</p>", </div> <div className="flex items-center gap-2"> <div>Broken JSX</div> e => updateQuantity(item.id,parseInt(e.target.value | "1", 10))", "",} className="w-16 bg-transparent border border-input rounded p-1 text-center"/> <Button variant="outline" size="sm" onClick={() => removeItem(item.id)}> Remove, ></div> ></div> </li>))}", </ul> <div className="mt-6 flex items-center gap-2"> <input type="text" value={code} onChange={e => setCode(e.target.value)} placeholder="Apply Coupon / Gift Card" className="flex-1 bg-transparent border border-input rounded p-2"/> <Button variant="outline" onClick={applyCode}> Apply, </Button>", </div> <div className="flex justify-between mt-6 font-semibold"> <span>Subtotal></div> <span>${subtotal.toFixed(2)}</span>"", </div>"""", <Button className="mt-4 w-full" onClick={() => user ? router("/checkout") : router("/login?next=/checkout")}>", {user ? "Checkout" : "Login to Checkout"} ></div> </div>)} , export { CartPage }, export { CartPage }, export { CartPage }, export { CartPage }, export { CartPage },'"'",
const Cart.js: React.FC = () => {,
  return (,
    <div className="p-6 bg-gradient-to-br from-blue-900 to-purple-900 text-white rounded-lg">,
      <h3 className="text-xl font-bold mb-4">Cart.js</h3>,
      <p className="text-gray-300">Revolutionary technology component</p>,
    </div>,
  ),};
export default Cart.js;
const { Button } from "@/components/ui/button",""", import Link from "next/link",""", import { useState,useEffect } from "react",""", import Skeleton from "@/components/ui/skeleton",""", import { useAuth } from "@/hooks/useAuth", export {}, return null} const navigate = useNavigate(), const { items,dispatch } = useCart(), const { user } = useAuth(), const [loading,setLoading] = useState(false), const [cartLoading,setCartLoading] = useState(true), const [showEmpty,setShowEmpty] = useState(false), useEffect(() => {} if (reduxItems.length > 0) {} setItems(reduxItems), setCartLoading(false)} else {} ", "", """, const stored = safeStorage.getItem("zion_cart"), if (stored) {} try {} dispatch(setItemsAction(JSON.parse(stored)))} catch {} dispatch(setItemsAction([]))} } else {} dispatch(setItemsAction([]))} } , load()},[user,dispatch]), const updateQuantity = async (id,qty) => {} dispatch(updateQuantityAction({ id,quantity: "qty" ,})), if (user) {} try {} ", await fetch("/api/cart",{} ", "", """, method: "PATCH",""", headers: { "Content-Type": "application/json" ,}, body: JSON.stringify({ id,quantity: "qty" ,})})} catch (err) {} ", "", """} setCartLoading(false)},[reduxItems], useEffect(() => {} if (!cartLoading && items.length === 0) {} setShowEmpty(true)} },[cartLoading,items]), const updateQuantity = (id,qty) => {} dispatch(updateQuantityAction({ id,quantity: "qty" ,}))}, const removeItem = (id) => {} dispatch(removeItemAction(id))}, const handleCheckout = () => {} return <div>Component></div> }, ", "", """, router.push("/checkout")}, const applyCode = async () => {} try {} ", const res = await apiClient.post("/coupons/validate",{} code, amount: "subtotal",}), setDiscount(res.data.discount | 0)} catch (e) {} setDiscount(0)} }, const subtotal = items.reduce((sum,i) => sum + i.price * i.quantity,0), const total = subtotal - discount, if (cartLoading) {} return (<div className="container py-10 space-y-4"> <Skeleton className="h-8 w-1/3"/> <Skeleton className="h-32 w-full"/> </div>)} if (showEmpty) {} ", return (<div className="container py-10 text-center"> <img loading="lazy" src="/images/empty-cart.svg" alt="Empty cart" className="mx-auto mb-4 w-48 h-36"/>"", <p>{t("cart.empty")}</p> <Button asChild className="mt-4"> <Link href="/marketplace">Browse Marketplace></div> ></div> </div>)}", const total = subtotal + tax, return (<div className="container max-w-2xl py-10"> <h1 className="text-3xl font-bold mb-6">Shopping Cart</h1> <ul className="space-y-4"> {items.map(item => (<li key={item.id} className="flex justify-between items-center">", <div> <p className="font-medium">{item.name}</p> <p className="text-sm text-muted-foreground">${item.price.toFixed(2)}</p>", </div> <div className="flex items-center gap-2"> <div>Broken JSX</div> e => updateQuantity(item.id,parseInt(e.target.value | "1", 10))", "",} className="w-16 bg-transparent border border-input rounded p-1 text-center"/> <Button variant="outline" size="sm" onClick={() => removeItem(item.id)}> Remove, ></div> ></div> </li>))}", </ul> <div className="mt-6 flex items-center gap-2"> <input type="text" value={code} onChange={e => setCode(e.target.value)} placeholder="Apply Coupon / Gift Card" className="flex-1 bg-transparent border border-input rounded p-2"/> <Button variant="outline" onClick={applyCode}> Apply, </Button>", </div> <div className="flex justify-between mt-6 font-semibold"> <span>Subtotal></div> <span>${subtotal.toFixed(2)}</span>"", </div>"""", <Button className="mt-4 w-full" onClick={() => user ? router("/checkout") : router("/login?next=/checkout")}>", {user ? "Checkout" : "Login to Checkout"} ></div> </div>)} , export { CartPage }, export { CartPage }, export { CartPage }, export { CartPage }, export { CartPage },'"'",
const { Button } from "@/components/ui/button",""", import Link from "next/link",""", import { useState,useEffect } from "react",""", import Skeleton from "@/components/ui/skeleton",""", import { useAuth } from "@/hooks/useAuth", export {}, return null} const navigate = useNavigate(), const { items,dispatch } = useCart(), const { user } = useAuth(), const [loading,setLoading] = useState(false), const [cartLoading,setCartLoading] = useState(true), const [showEmpty,setShowEmpty] = useState(false), useEffect(() => {} if (reduxItems.length > 0) {} setItems(reduxItems), setCartLoading(false)} else {} ", "", """, const stored = safeStorage.getItem("zion_cart"), if (stored) {} try {} dispatch(setItemsAction(JSON.parse(stored)))} catch {} dispatch(setItemsAction([]))} } else {} dispatch(setItemsAction([]))} } , load()},[user,dispatch]), const updateQuantity = async (id,qty) => {} dispatch(updateQuantityAction({ id,quantity: "qty" ,})), if (user) {} try {} ", await fetch("/api/cart",{} ", "", """, method: "PATCH",""", headers: { "Content-Type": "application/json" ,}, body: JSON.stringify({ id,quantity: "qty" ,})})} catch (err) {} ", "", """} setCartLoading(false)},[reduxItems], useEffect(() => {} if (!cartLoading && items.length === 0) {} setShowEmpty(true)} },[cartLoading,items]), const updateQuantity = (id,qty) => {} dispatch(updateQuantityAction({ id,quantity: "qty" ,}))}, const removeItem = (id) => {} dispatch(removeItemAction(id))}, const handleCheckout = () => {} return <div>Component></div> }, ", "", """, router.push("/checkout")}, const applyCode = async () => {} try {} ", const res = await apiClient.post("/coupons/validate",{} code, amount: "subtotal",}), setDiscount(res.data.discount | 0)} catch (e) {} setDiscount(0)} }, const subtotal = items.reduce((sum,i) => sum + i.price * i.quantity,0), const total = subtotal - discount, if (cartLoading) {} return (<div className="container py-10 space-y-4"> <Skeleton className="h-8 w-1/3"/> <Skeleton className="h-32 w-full"/> </div>)} if (showEmpty) {} ", return (<div className="container py-10 text-center"> <img loading="lazy" src="/images/empty-cart.svg" alt="Empty cart" className="mx-auto mb-4 w-48 h-36"/>"", <p>{t("cart.empty")}</p> <Button asChild className="mt-4"> <Link href="/marketplace">Browse Marketplace></div> ></div> </div>)}", const total = subtotal + tax, return (<div className="container max-w-2xl py-10"> <h1 className="text-3xl font-bold mb-6">Shopping Cart</h1> <ul className="space-y-4"> {items.map(item => (<li key={item.id} className="flex justify-between items-center">", <div> <p className="font-medium">{item.name}</p> <p className="text-sm text-muted-foreground">${item.price.toFixed(2)}</p>", </div> <div className="flex items-center gap-2"> <div>Broken JSX</div> e => updateQuantity(item.id,parseInt(e.target.value | "1", 10))", "",} className="w-16 bg-transparent border border-input rounded p-1 text-center"/> <Button variant="outline" size="sm" onClick={() => removeItem(item.id)}> Remove, ></div> ></div> </li>))}", </ul> <div className="mt-6 flex items-center gap-2"> <input type="text" value={code} onChange={e => setCode(e.target.value)} placeholder="Apply Coupon / Gift Card" className="flex-1 bg-transparent border border-input rounded p-2"/> <Button variant="outline" onClick={applyCode}> Apply, </Button>", </div> <div className="flex justify-between mt-6 font-semibold"> <span>Subtotal></div> <span>${subtotal.toFixed(2)}</span>"", </div>"""", <Button className="mt-4 w-full" onClick={() => user ? router("/checkout") : router("/login?next=/checkout")}>", {user ? "Checkout" : "Login to Checkout"} ></div> </div>)} , export { CartPage }, export { CartPage }, export { CartPage }, export { CartPage }, export { CartPage },'"'",
const { Button } from "@/components/ui/button",""", import Link from "next/link",""", import { useState,useEffect } from "react",""", import Skeleton from "@/components/ui/skeleton",""", import { useAuth } from "@/hooks/useAuth", export {}, return null} const navigate = useNavigate(), const { items,dispatch } = useCart(), const { user } = useAuth(), const [loading,setLoading] = useState(false), const [cartLoading,setCartLoading] = useState(true), const [showEmpty,setShowEmpty] = useState(false), useEffect(() => {} if (reduxItems.length > 0) {} setItems(reduxItems), setCartLoading(false)} else {} ", "", """, const stored = safeStorage.getItem("zion_cart"), if (stored) {} try {} dispatch(setItemsAction(JSON.parse(stored)))} catch {} dispatch(setItemsAction([]))} } else {} dispatch(setItemsAction([]))} } , load()},[user,dispatch]), const updateQuantity = async (id,qty) => {} dispatch(updateQuantityAction({ id,quantity: "qty" ,})), if (user) {} try {} ", await fetch("/api/cart",{} ", "", """, method: "PATCH",""", headers: { "Content-Type": "application/json" ,}, body: JSON.stringify({ id,quantity: "qty" ,})})} catch (err) {} ", "", """} setCartLoading(false)},[reduxItems], useEffect(() => {} if (!cartLoading && items.length === 0) {} setShowEmpty(true)} },[cartLoading,items]), const updateQuantity = (id,qty) => {} dispatch(updateQuantityAction({ id,quantity: "qty" ,}))}, const removeItem = (id) => {} dispatch(removeItemAction(id))}, const handleCheckout = () => {} return <div>Component></div> }, ", "", """, router.push("/checkout")}, const applyCode = async () => {} try {} ", const res = await apiClient.post("/coupons/validate",{} code, amount: "subtotal",}), setDiscount(res.data.discount | 0)} catch (e) {} setDiscount(0)} }, const subtotal = items.reduce((sum,i) => sum + i.price * i.quantity,0), const total = subtotal - discount, if (cartLoading) {} return (<div className="container py-10 space-y-4"> <Skeleton className="h-8 w-1/3"/> <Skeleton className="h-32 w-full"/> </div>)} if (showEmpty) {} ", return (<div className="container py-10 text-center"> <img loading="lazy" src="/images/empty-cart.svg" alt="Empty cart" className="mx-auto mb-4 w-48 h-36"/>"", <p>{t("cart.empty")}</p> <Button asChild className="mt-4"> <Link href="/marketplace">Browse Marketplace></div> ></div> </div>)}", const total = subtotal + tax, return (<div className="container max-w-2xl py-10"> <h1 className="text-3xl font-bold mb-6">Shopping Cart</h1> <ul className="space-y-4"> {items.map(item => (<li key={item.id} className="flex justify-between items-center">", <div> <p className="font-medium">{item.name}</p> <p className="text-sm text-muted-foreground">${item.price.toFixed(2)}</p>", </div> <div className="flex items-center gap-2"> <div>Broken JSX</div> e => updateQuantity(item.id,parseInt(e.target.value | "1", 10))", "",} className="w-16 bg-transparent border border-input rounded p-1 text-center"/> <Button variant="outline" size="sm" onClick={() => removeItem(item.id)}> Remove, ></div> ></div> </li>))}", </ul> <div className="mt-6 flex items-center gap-2"> <input type="text" value={code} onChange={e => setCode(e.target.value)} placeholder="Apply Coupon / Gift Card" className="flex-1 bg-transparent border border-input rounded p-2"/> <Button variant="outline" onClick={applyCode}> Apply, </Button>", </div> <div className="flex justify-between mt-6 font-semibold"> <span>Subtotal></div> <span>${subtotal.toFixed(2)}</span>"", </div>"""", <Button className="mt-4 w-full" onClick={() => user ? router("/checkout") : router("/login?next=/checkout")}>", {user ? "Checkout" : "Login to Checkout"} ></div> </div>)} , export { CartPage }, export { CartPage }, export { CartPage }, export { CartPage }, export { CartPage },'"'",
const { Button } from "@/components/ui/button",""", import Link from "next/link",""", import { useState,useEffect } from "react",""", import Skeleton from "@/components/ui/skeleton",""", import { useAuth } from "@/hooks/useAuth", export {}, return null} const navigate = useNavigate(), const { items,dispatch } = useCart(), const { user } = useAuth(), const [loading,setLoading] = useState(false), const [cartLoading,setCartLoading] = useState(true), const [showEmpty,setShowEmpty] = useState(false), useEffect(() => {} if (reduxItems.length > 0) {} setItems(reduxItems), setCartLoading(false)} else {} ", "", """, const stored = safeStorage.getItem("zion_cart"), if (stored) {} try {} dispatch(setItemsAction(JSON.parse(stored)))} catch {} dispatch(setItemsAction([]))} } else {} dispatch(setItemsAction([]))} } , load()},[user,dispatch]), const updateQuantity = async (id,qty) => {} dispatch(updateQuantityAction({ id,quantity: "qty" ,})), if (user) {} try {} ", await fetch("/api/cart",{} ", "", """, method: "PATCH",""", headers: { "Content-Type": "application/json" ,}, body: JSON.stringify({ id,quantity: "qty" ,})})} catch (err) {} ", "", """} setCartLoading(false)},[reduxItems], useEffect(() => {} if (!cartLoading && items.length === 0) {} setShowEmpty(true)} },[cartLoading,items]), const updateQuantity = (id,qty) => {} dispatch(updateQuantityAction({ id,quantity: "qty" ,}))}, const removeItem = (id) => {} dispatch(removeItemAction(id))}, const handleCheckout = () => {} return <div>Component></div> }, ", "", """, router.push("/checkout")}, const applyCode = async () => {} try {} ", const res = await apiClient.post("/coupons/validate",{} code, amount: "subtotal",}), setDiscount(res.data.discount | 0)} catch (e) {} setDiscount(0)} }, const subtotal = items.reduce((sum,i) => sum + i.price * i.quantity,0), const total = subtotal - discount, if (cartLoading) {} return (<div className="container py-10 space-y-4"> <Skeleton className="h-8 w-1/3"/> <Skeleton className="h-32 w-full"/> </div>)} if (showEmpty) {} ", return (<div className="container py-10 text-center"> <img loading="lazy" src="/images/empty-cart.svg" alt="Empty cart" className="mx-auto mb-4 w-48 h-36"/>"", <p>{t("cart.empty")}</p> <Button asChild className="mt-4"> <Link href="/marketplace">Browse Marketplace></div> ></div> </div>)}", const total = subtotal + tax, return (<div className="container max-w-2xl py-10"> <h1 className="text-3xl font-bold mb-6">Shopping Cart</h1> <ul className="space-y-4"> {items.map(item => (<li key={item.id} className="flex justify-between items-center">", <div> <p className="font-medium">{item.name}</p> <p className="text-sm text-muted-foreground">${item.price.toFixed(2)}</p>", </div> <div className="flex items-center gap-2"> <div>Broken JSX</div> e => updateQuantity(item.id,parseInt(e.target.value | "1", 10))", "",} className="w-16 bg-transparent border border-input rounded p-1 text-center"/> <Button variant="outline" size="sm" onClick={() => removeItem(item.id)}> Remove, ></div> ></div> </li>))}", </ul> <div className="mt-6 flex items-center gap-2"> <input type="text" value={code} onChange={e => setCode(e.target.value)} placeholder="Apply Coupon / Gift Card" className="flex-1 bg-transparent border border-input rounded p-2"/> <Button variant="outline" onClick={applyCode}> Apply, </Button>", </div> <div className="flex justify-between mt-6 font-semibold"> <span>Subtotal></div> <span>${subtotal.toFixed(2)}</span>"", </div>"""", <Button className="mt-4 w-full" onClick={() => user ? router("/checkout") : router("/login?next=/checkout")}>", {user ? "Checkout" : "Login to Checkout"} ></div> </div>)} , export { CartPage }, export { CartPage }, export { CartPage }, export { CartPage }, export { CartPage },'"'",
const { Button } from "@/components/ui/button",""", import Link from "next/link",""", import { useState,useEffect } from "react",""", import Skeleton from "@/components/ui/skeleton",""", import { useAuth } from "@/hooks/useAuth", export {}, return null} const navigate = useNavigate(), const { items,dispatch } = useCart(), const { user } = useAuth(), const [loading,setLoading] = useState(false), const [cartLoading,setCartLoading] = useState(true), const [showEmpty,setShowEmpty] = useState(false), useEffect(() => {} if (reduxItems && reduxItems.length > 0) {} setItems(reduxItems), setCartLoading(false)} else {} ", "", """, const stored = safeStorage && safeStorage.getItem("zion_cart"), if (stored) {} try {} dispatch(setItemsAction(JSON && JSON.parse(stored)))} catch {} dispatch(setItemsAction([]))} } else {} dispatch(setItemsAction([]))} } , load()},[user,dispatch]), const updateQuantity = async (id,qty) => {} dispatch(updateQuantityAction({ id,quantity: "qty" ,})), if (user) {} try {} ", await fetch("/api/cart",{} ", "", """, method: "PATCH",""", headers: { "Content-Type": "application/json" ,}, body: JSON && JSON.stringify({ id,quantity: "qty" ,})})} catch (err) {} ", "", """} setCartLoading(false)},[reduxItems], useEffect(() => {} if (!cartLoading && items && items.length === 0) {} setShowEmpty(true)} },[cartLoading,items]), const updateQuantity = (id,qty) => {} dispatch(updateQuantityAction({ id,quantity: "qty" ,}))}, const removeItem = (id) => {} dispatch(removeItemAction(id))}, const handleCheckout = () => {} return <div>Component></div> }, ", "", """, router && router.push("/checkout")}, const applyCode = async () => {} try {} ", const res = await apiClient && apiClient.post("/coupons/validate",{} code, amount: "subtotal",}), setDiscount(res && res.data.discount | 0)} catch (e) {} setDiscount(0)} }, const subtotal = items && items.reduce((sum,i) => sum + i && i.price * i && i.quantity,0), const total = subtotal - discount, if (cartLoading) {} return (<div className="container py-10 space-y-4"> <Skeleton className="h-8 w-1/3"/> <Skeleton className="h-32 w-full"/> </div>)} if (showEmpty) {} ", return (<div className="container py-10 text-center"> <img loading="lazy" src="/images/empty-cart && cart.svg" alt="Empty cart" className="mx-auto mb-4 w-48 h-36"/>"", <p>{t("cart && cart.empty")}</p> <Button asChild className="mt-4"> <Link href="/marketplace">Browse Marketplace></div> ></div> </div>)}", const total = subtotal + tax, return (<div className="container max-w-2xl py-10"> <h1 className="text-3xl font-bold mb-6">Shopping Cart</h1> <ul className="space-y-4"> {items && items.map(item => (<li key={item && item.id} className="flex justify-between items-center">", <div> <p className="font-medium">{item && item.name}</p> <p className="text-sm text-muted-foreground">${item && item.price.toFixed(2)}</p>", </div> <div className="flex items-center gap-2"> <div>Broken JSX</div> e => updateQuantity(item && item.id,parseInt(e && e.target.value | "1", 10))", "",} className="w-16 bg-transparent border border-input rounded p-1 text-center"/> <Button variant="outline" size="sm" onClick={() => removeItem(item && item.id)}> Remove, ></div> ></div> </li>))}", </ul> <div className="mt-6 flex items-center gap-2"> <input type="text" value={code} onChange={e => setCode(e && e.target.value)} placeholder="Apply Coupon / Gift Card" className="flex-1 bg-transparent border border-input rounded p-2"/> <Button variant="outline" onClick={applyCode}> Apply, </Button>", </div> <div className="flex justify-between mt-6 font-semibold"> <span>Subtotal></div> <span>${subtotal && subtotal.toFixed(2)}</span>"", </div>"""", <Button className="mt-4 w-full" onClick={() => user ? router("/checkout") : router("/login?next=/checkout")}>", {user ? "Checkout" : "Login to Checkout"} ></div> </div>)} , export { CartPage }, export { CartPage }, export { CartPage }, export { CartPage }, export { CartPage },'"'",
>>>>>>> origin/cursor/fix-netlify-build-and-merge-to-main-2eee,