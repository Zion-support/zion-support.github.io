const { useParams,Link } from "react-router-dom";"""; import { Button } from "@/components/ui/button";"""; import { Skeleton } from "@/components/ui/skeleton";"""; import { useGetOrderQuery } from "@/hooks/useOrder";"""; import { generateInvoicePdf } from "@/utils/generateInvoicePdf";"""; import { useAuth } from "@/hooks/useAuth";"""; import { supabase } from "@/integrations/supabase/client";"""; import { toast } from "@/hooks/use-toast"; export {}; return null} const { orderId } = useParams(); const { user } = useAuth(); const { data: "order",isLoading } = useGetOrderQuery(orderId); const handleDownload = async () => {} if (!order) return; const blob = await generateInvoicePdf(order); const url = URL.createObjectURL(blob);"; const link = document.createElement("a"); link.href = url; link.download = `invoice-${order.orderId}.pdf`; document.body.appendChild(link); link.click(); document.body.removeChild(link); URL.revokeObjectURL(url)
