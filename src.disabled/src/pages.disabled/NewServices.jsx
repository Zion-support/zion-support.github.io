import _React,{ useState } from 'react';'''; import { motion } from 'framer-motion'; import { COMPREHENSIVE_SERVICES } from "../data/comprehensiveServices.jsx"; import { EMERGING_TECH_SERVICES } from "../data/emergingTechServices.jsx"; export function NewServices() {}; return null} const filteredServices = allServices.filter(service => {} const matchesSearch = service.title.toLowerCase().includes(searchTerm.toLowerCase()) ||'; service.description.toLowerCase().includes(searchTerm.toLowerCase()) ||''; (('tags' in service && 'tags' in service && service.tags && service.tags.some(tag => tag.toLowerCase().includes(searchTerm.toLowerCase()))) || false); return matchesCategory && matchesSearch}); const containerVariants = {} "hidden": { opacity: 0 },; "visible": {} "y": '0',; "opacity": '1',; "transition": {} "duration": 0.5; "
