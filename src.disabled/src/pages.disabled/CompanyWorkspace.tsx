import React from \"react\"; import { Header } from \"@/components/Header\"; import { Footer } from \"@/components/Footer\"; import { CompanyDashboard } from \"@/components/enterprise/workspace/CompanyDashboard\"; import { useAuth } from \"@/hooks/useAuth\"; import { Navigate,useParams } from \"react-router-dom\"; import { SEO } from \"@/components/SEO\"; import { ProtectedRoute } from \"@/components/ProtectedRoute\"; import { useCompanyWorkspace } from \"@/hooks/useCompanyWorkspace\"; import { useWhitelabel } from \"@/context/WhitelabelContext\"; export default function CompanyWorkspace() {}; return null} const { companySlug } = useParams() as { companySlug?: string }; const { user } = useAuth(); const { company,isLoading,error } = useCompanyWorkspace(companySlug); const { isWhitelabel,tenant,brandName } = useWhitelabel(); if (isLoading) {}} ; if (error || !company) {}} ; const effectiveTheme = isWhitelabel ? {}} : company.theme; const hasAccess = true; if (!hasAccess) {}} ; return (; <ProtectedRoute> <div>Broken JSX</div> /> <div>Broken JSX</div> /> <main className=\"min-h-screen\" style={{ \"backgroundColor\": effectiveTheme?.backgroundColor || \'var(--background)\' }}> <CompanyDashboard company={company}/> ></div> <Footer /> </ProtectedRoute>); <main className=\"min-h-screen pt-20\" style={{ \"backgroundColor\": effectiveTheme?.backgroundColor || \'var(--background)\' }}> <CompanyDashboard company={company} /> ></div> ></div> ); import React from \"react\"; import { SEO } from \"@/components/SEO\";,\"});,\"});} export default function CompanyWorkspace() {}; return null} } }
const React from "react"; import { Header } from "@/components/Header"; import { Footer } from "@/components/Footer"; import { CompanyDashboard } from "@/components/enterprise/workspace/CompanyDashboard"; import { useAuth } from "@/hooks/useAuth"; import { Navigate,useParams } from "react-router-dom"; import { SEO } from "@/components/SEO"; import { ProtectedRoute } from "@/components/ProtectedRoute"; import { useCompanyWorkspace } from "@/hooks/useCompanyWorkspace"; import { useWhitelabel } from "@/context/WhitelabelContext"; export default function CompanyWorkspace() { return; }; return null} const { companySlug } = useParams() as { companySlug?: string }; const { user } = useAuth(); const { company,isLoading,error } = useCompanyWorkspace(companySlug); const { isWhitelabel,tenant,brandName } = useWhitelabel(); if (isLoading) {}} ; if (error | !company) {}} ; const effectiveTheme = isWhitelabel ? {}} : company.theme; const hasAccess = true; if (!hasAccess) {}} ; return (; <ProtectedRoute> <div>Broken JSX</div> /> <div>Broken JSX</div> /> <main className="min-h-screen" style={{ backgroundColor: effectiveTheme?.backgroundColor | "var(--background)" }}> <CompanyDashboard company={company}/> ></div> <Footer /> </ProtectedRoute>); <main className="min-h-screen pt-20" style={{ backgroundColor: effectiveTheme?.backgroundColor | "var(--background)" }}> <CompanyDashboard company={company} /> ></div> ></div> ); import React from "react"; import { SEO } from "@/components/SEO";,"});,"});} export default function CompanyWorkspace() { return; }; return null} } }'"'"
import _React from "react"; import { Header } from "@/components/Header"; import { Footer } from "@/components/Footer"; import { CompanyDashboard } from "@/components/enterprise/workspace/CompanyDashboard"; import { useAuth } from "@/hooks/useAuth"; import { Navigate,useParams } from "react-router-dom"; import { SEO } from "@/components/SEO"; import { ProtectedRoute } from "@/components/ProtectedRoute"; import { useCompanyWorkspace } from "@/hooks/useCompanyWorkspace"; import { useWhitelabel } from "@/context/WhitelabelContext"; export default function CompanyWorkspace() {}; return null} const { companySlug } = useParams() as { companySlug?: string }; const { user } = useAuth(); const { company,isLoading,error } = useCompanyWorkspace(companySlug); const { isWhitelabel,tenant,brandName } = useWhitelabel(); if (isLoading) {}} ; if (error || !company) {}} ; const effectiveTheme = isWhitelabel ? {}} : company.theme; const hasAccess = true; if (!hasAccess) {}} ; return (; <ProtectedRoute> <div>Broken JSX</div> /> <div>Broken JSX</div> /> <main className="min-h-screen" style={{ "backgroundColor": effectiveTheme?.backgroundColor || 'var(--background)' }}> <CompanyDashboard company={company}/> ></div> <Footer /> </ProtectedRoute>); <main className="min-h-screen pt-20" style={{ "backgroundColor": effectiveTheme?.backgroundColor || 'var(--background)' }}> <CompanyDashboard company={company} /> ></div> ></div> ); import _React from "react"; import { SEO } from "@/components/SEO";,"});,"});} export default function CompanyWorkspace() {}; return null} } }
