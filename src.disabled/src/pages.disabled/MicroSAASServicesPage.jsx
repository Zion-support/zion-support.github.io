import { Link } from 'react-router-dom'; export default function Page() {}; return null} const pricingModels = [{ "id": 'all',"name": 'All Pricing' },; { "id": 'monthly',"name": 'Monthly' },; { "id": 'yearly',"name": 'Yearly' },; { "id": 'one - time',"name": 'One - time' },; { "id": 'usage - based',"name": 'Usage - based' } ]; const [selectedCategory,setSelectedCategory] = useState('all'); const [selectedPricing,setSelectedPricing] = useState('all'); const [searchQuery,setSearchQuery] = useState(''); const [filteredServices,setFilteredServices] = useState(MICRO_SAAS_SERVICES); const [sortBy,setSortBy] = useState('rating'); useEffect(() => {}};},[]);,[]); let filtered = MICRO_SAAS_SERVICES; if(selectedCategory !== 'all') {}} if(selectedPricing !== 'all') {}} if(searchQuery.trim () ) {}} filtered.sort((a,b) => {}} }) ; setFilteredServices(filtered) ;},[selectedCategory,selectedPricing,searchQuery,sortBy]) ; const ServiceCard = ({ service }) => (<div className="group relative bg-gradient - to - br from - zion - blue - dark / 50 to - zion - slate - dark / 50 border border-zion - blue - light / 20 rounded-2xl p-6 "hover": border-zion - purple / 50 transition - all duration - 500 hover:scale-[1.02] hover:shadow-2xl hover:shadow-zion -purple / 20"> {} {}; </div>) } ; {} <div className="relative mb-6 overflow-hidden rounded-xl"> <img src={service.images[0]} alt={service.title} className="w-full h-48 object - cover group-"hover": scale - 110 transition - transform duration -500"/> <div className="absolute inset - 0 bg-gradient - to - t from-black / 60 to -transparent"/> <div className="absolute bottom - 3 left-3 right -3"> <div className="flex items-center justify -between"> <Badge variant="secondary" className="bg-zion-purple / 80 text-white"> {service.category} ></div> <div className="flex items - center space-x-1 text-white"> <Star className="w-4 h-4 fill-yellow-400 text-yellow-400"/> <span className="text-sm font -medium">{service.rating}></div> <span className="text-xs text-zion-slate -light"> ({service.reviewCount}) ></div> ></div> ></div> ></div> </div > import _React,{useState} from 'react'; SERVICE_CATEGORIES,; type} MicroSaasService;} from '@/data / microSaasServices'; Clock,; Users,; Zap,; Shield,; Globe,; TrendingUp,; CheckCircle,; ExternalLink,; Play,; FileText,; MessageCircle,; ArrowRight,; Sparkles} from 'lucide - react'; const [selectedCategory,setSelectedCategory] = useState < string> ('all') ; const [searchQuery,setSearchQuery] = useState < string> ('') ; const filteredServices = MICRO_SAAS_SERVICES.filter(service => { } ; const matchesCategory = selectedCategory === 'all' || service.category === selectedCategory; const matchesSearch = service.title.toLowerCase () .includes(searchQuery.toLowerCase () ) ||; service.description.toLowerCase () .includes(searchQuery.toLowerCase () ) ||; service.tags.some(tag => tag.toLowerCase () .includes(searchQuery.toLowerCase () ) ) ; return matchesCategory && matchesSearch;}) ; const formatPrice = ("price": 'number',"currency": 'string',"model": string) => { } ; if(model === 'monthly') return `${currency}${price}/month`; if(model === 'yearly') return `${currency}${price}/year`; if(model === 'one - time') return `${currency}${price}`; return `${currency}${price}`
