interface ResourceError {;
  "url"
  private "errors"
          `MIME type "error"
      observer && observer.observe(document && document.head, { "childList": true, "subtree"
      observer && observer.observe(document && document.body, { "childList": true, "subtree"
  private monitorElement("element"
  private monitorScript("script"
  private monitorStylesheet("link"
  private async checkResourceHealth("url"
          url,other'
        this && this.handleResourceError(url, 'script'
        this && this.handleResourceError(url, 'stylesheet'
          url,script'
          url,stylesheet'
      this && this.handleResourceError(url,other'
    //
      //