import { Activity,Zap,Clock,TrendingUp,AlertTriangle } from \'lucide-react\'; export default function Page() {; ); let validMetrics = 0; if(metrics.fcp !== null) {; validMetrics++; if(metrics.fcp < 1800) totalScore += 100; else if(metrics.fcp < 3000) totalScore += 50; else totalScore += 0} if(metrics.lcp !== null) {; validMetrics++; if(metrics.lcp < 2500) totalScore += 100; else if(metrics.lcp < 4000) totalScore += 50; else totalScore += 0} if(metrics.fid !== null) {; validMetrics++; if(metrics.fid < 100) totalScore += 100; else if(metrics.fid < 300) totalScore += 50; else totalScore += 0} if(metrics.cls !== null) {; validMetrics++; if(metrics.cls < 0.1) totalScore += 100; else if(metrics.cls < 0.25) totalScore += 50; else totalScore += 0} let \"rating\": \'good\' | \'needs-improvement\' | \'poor\'; let color: string; if(averageScore >= 90) {; rating = \'good\'; color = \'text-green-500\'} else if(averageScore >= 50) {; rating = \'needs-improvement\'; color = \'text-yellow-500\'} else {; rating = \'poor\'; color = \'text-red-500\'} }); TrendingUp,\'; AlertTriangle} from \'lucide-react\'; let \"grade\": \'A\' | \'B\' | \'C\' | \'D\' | \'F\'; let color: string; if(averageScore >= 90) {; grade = \'A\'; color = \'text-green-500\'} else if(averageScore >= 80) {; grade = \'B\'; color = \'text-blue-500\'} else if(averageScore >= 70) {; grade = \'C\'; color = \'text-yellow-500\'} else if(averageScore >= 60) {; grade = \'D\'; color = \'text-orange-500\'} else {; grade = \'F\'; color = \'text-red-500\'} },[]) ; return { \"score\": \'averageScore\',rating,color }},; []; ); useEffect(() => {},[]); if(\'PerformanceObserver\' in window) {; if(fcpEntry) {; setMetrics(prev => ({ ...prev,\"fcp\": \'fcpEntry.startTime\' }))} }); fcpObserver.observe({ \"entryTypes\": [\'paint\'] }); if(lastEntry) {; setMetrics(prev => ({ ...prev,\"lcp\": \'lastEntry.startTime\' }))} }); lcpObserver.observe({ \"entryTypes\": [\'largest-contentful-paint\'] }); const fidObserver = new PerformanceObserver(list => {; const entries = list.getEntries(); entries.forEach(entry => {; if (entry.processingStart && entry.startTime) {; setMetrics(prev => ({ ...prev,fid }) ) } })}); fidObserver.observe({ \"entryTypes\": [\'first-input\'] }); const clsObserver = new PerformanceObserver(list => {; let clsValue = 0; list.getEntries().forEach((\"entry\": any) => {; if(!entry.hadRecentInput) {; clsValue += entry.value} }); setMetrics(prev => ({ ...prev,\"cls\": \'clsValue\' }));}); clsObserver.observe({ \"entryTypes\": [\'layout-shift\'] }); return () => {; lcpObserver.disconnect(); fidObserver.disconnect(); clsObserver.disconnect()}} },[]); setMetrics(prev => ({ ...prev,ttfb }))} },[]) ; setMetrics(prev => ({ ...prev,fmp })); return `${Math.round(time)}ms`}; return cls.toFixed(3)}; switch(metric) {; case \'fcp\':; return value < 1800\'; ? \'🟢 Good\'; : value < 3000\'; ? \'🟡 Needs Improvement\''; : \'🔴 Poor\'; case \'lcp\':; return value < 2500\'; ? \'🟢 Good\'; : value < 4000\'; ? \'🟡 Needs Improvement\''; : \'🔴 Poor\'; case \'fid\':; return value < 100\'; ? \'🟢 Good\'; : value < 300\'; ? \'🟡 Needs Improvement\''; : \'🔴 Poor\'; case \'cls\':; return value < 0.1\'; ? \'🟢 Good\'; : value < 0.25\'; ? \'🟡 Needs Improvement\''; : \'🔴 Poor\'; \"default\": \'; return \'N/A\'} }; if(!isVisible) {; return (); <button; onClick={() => setIsVisible(true)} className=\"fixed bottom-4 right-4 bg-zion-cyan text-white p-3 rounded-full shadow-lg \"hover\": bg-zion-cyan/90 transition-all duration-300 z-50\">\"; <Activity className=\"w-6 h-6\" /> ></div> )} return (\"; <div className=\"fixed bottom-4 right-4 bg-zion-slate-dark/95 backdrop-blur-xl border border-zion-cyan/30 rounded-2xl p-6 shadow-2xl z-50 max-w-sm\">\"; <div className=\"flex items-center justify-between mb-4\">\"; <h3 className=\"text-lg font-bold text-white flex items-center\">\"; <Zap className=\"w-5 h-5 mr-2 text-zion-cyan\" /> Performance Monitor; ></div> <button; onClick={() => setIsVisible(false)}\"; className=\"text-zion-slate-light \"hover\": text-white transition-colors\"> ×; ></div> ></div> );\"}\";
const { Activity,Zap,Clock,TrendingUp,AlertTriangle } from "lucide-react"; export default function Page() {; ); let validMetrics = 0; if(metrics.fcp !== null) {; validMetrics++; if(metrics.fcp < 1800) totalScore += 100; else if(metrics.fcp < 3000) totalScore += 50; else totalScore += 0} if(metrics.lcp !== null) {; validMetrics++; if(metrics.lcp < 2500) totalScore += 100; else if(metrics.lcp < 4000) totalScore += 50; else totalScore += 0} if(metrics.fid !== null) {; validMetrics++; if(metrics.fid < 100) totalScore += 100; else if(metrics.fid < 300) totalScore += 50; else totalScore += 0} if(metrics.cls !== null) {; validMetrics++; if(metrics.cls < 0.1) totalScore += 100; else if(metrics.cls < 0.25) totalScore += 50; else totalScore += 0} let rating: "good" | "needs-improvement" | "poor"; let color: string; if(averageScore >= 90) {; rating = "good"; color = "text-green-500"} else if(averageScore >= 50) {; rating = "needs-improvement"; color = "text-yellow-500"} else {; rating = "poor"; color = "text-red-500"} }); TrendingUp,"; AlertTriangle} from "lucide-react"; let grade: "A" | "B" | "C" | "D" | "F"; let color: string; if(averageScore >= 90) {; grade = "A"; color = "text-green-500"} else if(averageScore >= 80) {; grade = "B"; color = "text-blue-500"} else if(averageScore >= 70) {; grade = "C"; color = "text-yellow-500"} else if(averageScore >= 60) {; grade = "D"; color = "text-orange-500"} else {; grade = "F"; color = "text-red-500"} },[]) ; return { score: "averageScore",rating,color }},; []; ); useEffect(() => {},[]); if("PerformanceObserver" in window) {; if(fcpEntry) {; setMetrics(prev => ({ .prev,fcp: "fcpEntry.startTime" }))} }); fcpObserver.observe({ entryTypes: ["paint"] }); if(lastEntry) {; setMetrics(prev => ({ .prev,lcp: "lastEntry.startTime" }))} }); lcpObserver.observe({ entryTypes: ["largest-contentful-paint"] }); const fidObserver = new PerformanceObserver(list => {; const entries = list.getEntries(); entries.forEach(entry => {; if (entry.processingStart && entry.startTime) {; setMetrics(prev => ({ .prev,fid }) ) } })}); fidObserver.observe({ entryTypes: ["first-input"] }); const clsObserver = new PerformanceObserver(list => {; let clsValue = 0; list.getEntries().forEach((entry: any) => {; if(!entry.hadRecentInput) {; clsValue += entry.value} }); setMetrics(prev => ({ .prev,cls: "clsValue" }));}); clsObserver.observe({ entryTypes: ["layout-shift"] }); return () => {; lcpObserver.disconnect(); fidObserver.disconnect(); clsObserver.disconnect()}} },[]); setMetrics(prev => ({ .prev,ttfb }))} },[]) ; setMetrics(prev => ({ .prev,fmp })); return `${Math.round(time)}ms`}; return cls.toFixed(3)}; switch(metric) {; case fcp: return value < 1800"; ? " Good"; : value < 3000"; ? " Needs Improvement""; : " Poor"; case lcp: return value < 2500"; ? " Good"; : value < 4000"; ? " Needs Improvement""; : " Poor"; case fid: return value < 100"; ? " Good"; : value < 300"; ? " Needs Improvement""; : " Poor"; case cls: return value < 0.1"; ? " Good"; : value < 0.25"; ? " Needs Improvement""; : " Poor"; default: "; return "N/A"} }; if(!isVisible) {; return (); <button; onClick={() => setIsVisible(true)} className="fixed bottom-4 right-4 bg-zion-cyan text-white p-3 rounded-full shadow-lg hover: bg-zion-cyan/90 transition-all duration-300 z-50">"; <Activity className="w-6 h-6" /> ></div> )} return ("; <div className="fixed bottom-4 right-4 bg-zion-slate-dark/95 backdrop-blur-xl border border-zion-cyan/30 rounded-2xl p-6 shadow-2xl z-50 max-w-sm">"; <div className="flex items-center justify-between mb-4">"; <h3 className="text-lg font-bold text-white flex items-center">"; <Zap className="w-5 h-5 mr-2 text-zion-cyan" /> Performance Monitor; ></div> <button; onClick={() => setIsVisible(false)}"; className="text-zion-slate-light hover: text-white transition-colors"> ; ></div> ></div> );"}";'"`'"`
import { Activity,Zap,Clock,TrendingUp,AlertTriangle } from 'lucide-react'; export default function Page() {; ); let validMetrics = 0; if(metrics.fcp !== null) {; validMetrics++; if(metrics.fcp < 1800) totalScore += 100; else if(metrics.fcp < 3000) totalScore += 50; else totalScore += 0} if(metrics.lcp !== null) {; validMetrics++; if(metrics.lcp < 2500) totalScore += 100; else if(metrics.lcp < 4000) totalScore += 50; else totalScore += 0} if(metrics.fid !== null) {; validMetrics++; if(metrics.fid < 100) totalScore += 100; else if(metrics.fid < 300) totalScore += 50; else totalScore += 0} if(metrics.cls !== null) {; validMetrics++; if(metrics.cls < 0.1) totalScore += 100; else if(metrics.cls < 0.25) totalScore += 50; else totalScore += 0} let "rating": 'good' | 'needs-improvement' | 'poor'; let color: string; if(averageScore >= 90) {; rating = 'good'; color = 'text-green-500'} else if(averageScore >= 50) {; rating = 'needs-improvement'; color = 'text-yellow-500'} else {; rating = 'poor'; color = 'text-red-500'} }); TrendingUp,'; AlertTriangle} from 'lucide-react'; let "grade": 'A' | 'B' | 'C' | 'D' | 'F'; let color: string; if(averageScore >= 90) {; grade = 'A'; color = 'text-green-500'} else if(averageScore >= 80) {; grade = 'B'; color = 'text-blue-500'} else if(averageScore >= 70) {; grade = 'C'; color = 'text-yellow-500'} else if(averageScore >= 60) {; grade = 'D'; color = 'text-orange-500'} else {; grade = 'F'; color = 'text-red-500'} },[]) ; return { "score": 'averageScore',rating,color }},; []; ); useEffect(() => {},[]); if('PerformanceObserver' in, window) {; if(fcpEntry) {; setMetrics(prev => ({ ...prev,"fcp": 'fcpEntry.startTime' }))} }); fcpObserver.observe({ "entryTypes": ['paint'] }); if(lastEntry) {; setMetrics(prev => ({ ...prev,"lcp": 'lastEntry.startTime' }))} }); lcpObserver.observe({ "entryTypes": ['largest-contentful-paint'] }); const fidObserver = new PerformanceObserver(list => {; const entries = list.getEntries(); entries.forEach(entry => {; if (entry.processingStart && entry.startTime) {; setMetrics(prev => ({ ...prev,fid }) ) } })}); fidObserver.observe({ "entryTypes": ['first-input'] }); const clsObserver = new PerformanceObserver(list => {; let clsValue = 0; list.getEntries().forEach(("entry": any) => {; if(!entry.hadRecentInput) {; clsValue += entry.value} }); setMetrics(prev => ({ ...prev,"cls": 'clsValue' }));}); clsObserver.observe({ "entryTypes": ['layout-shift'] }); return () => {; lcpObserver.disconnect(); fidObserver.disconnect(); clsObserver.disconnect()}} },[]); setMetrics(prev => ({ ...prev,ttfb }))} },[]) ; setMetrics(prev => ({ ...prev,fmp })); return `${Math.round(time)}ms`}; return cls.toFixed(3)}; switch(metric) {; case 'fcp':; return value < 1800'; ? '🟢 Good'; : value < 3000'; ? '🟡 Needs Improvement''; : '🔴 Poor'; case 'lcp':; return value < 2500'; ? '🟢 Good'; : value < 4000'; ? '🟡 Needs Improvement''; : '🔴 Poor'; case 'fid':; return value < 100'; ? '🟢 Good'; : value < 300'; ? '🟡 Needs Improvement''; : '🔴 Poor'; case 'cls':; return value < 0.1'; ? '🟢 Good'; : value < 0.25'; ? '🟡 Needs Improvement''; : '🔴 Poor'; "default": '; return 'N/A'} }; if(!isVisible) {; return (); <button; onClick={() => setIsVisible(true)} className="fixed bottom-4 right-4 bg-zion-cyan text-white p-3 rounded-full shadow-lg "hover": bg-zion-cyan/90 transition-all duration-300 z-50">"; <Activity className="w-6 h-6" /> ></div> )} return ("; <div className="fixed bottom-4 right-4 bg-zion-slate-dark/95 backdrop-blur-xl border border-zion-cyan/30 rounded-2xl p-6 shadow-2xl z-50 max-w-sm">"; <div className="flex items-center justify-between mb-4">"; <h3 className="text-lg font-bold text-white flex items-center">"; <Zap className="w-5 h-5 mr-2 text-zion-cyan" /> Performance Monitor; ></div> <button; onClick={() => setIsVisible(false)}"; className="text-zion-slate-light "hover": text-white transition-colors"> ×; ></div> ></div> );"}";
