import React from 'react';

interface CheckoutShippingOptions.testProps {
  className?: string;
}

const CheckoutShippingOptions.test: React.FC<CheckoutShippingOptions.testProps> = ({ className }) => {
  return (
    <div className={className || ''}>
      <h1>CheckoutShippingOptions.test</h1>
      <p>This component is under development.</p>
    </div>
  );
};

export default CheckoutShippingOptions.test;