import React from 'react';

interface GlobalSpinnerProps {
  // Add props here as needed
}

export default function GlobalSpinner({ }: GlobalSpinnerProps) {
  return (
    <div>
      <h1>GlobalSpinner</h1>
      <p>This component is currently under development.</p>
    </div>
  );
}