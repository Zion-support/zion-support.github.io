

=======

=======
import React,{ useEffect,useRef } from \'react\'; export const FuturisticAnimatedBackground = ({ className = \'',intensity = \'medium\',colorScheme = \'multi\' }) => {; const canvasRef = useRef(null) ; const animationRef = useRef () ; useEffect(() => {},[]); const canvas = canvasRef.current; useEffect(() => { const canvas = canvasRef.current; if(!canvas); return; const ctx = canvas.getContext(\'2d\'); if(!ctx); return; const resizeCanvas = () => {; canvas.width = window.innerWidth; canvas.height = window.innerHeight}; resizeCanvas(); window.addEventListener(\'resize\',resizeCanvas); const particles = []; const colors = colorSchemes[colorScheme]; const particleCount = intensity === \'low\' ? 50 : intensity === \'medium\' ? 100 : 200; for(let i = 0; i < particleCount; i++) {; particles.push({; \"x\": Math.random() * canvas.width,; \"y\": Math.random() * canvas.height,; \"vx\": (Math.random() - 0.5) * 2,; \"vy\": (Math.random() - 0.5) * 2,; \"size\": Math.random() * 3 + 1,; \"opacity\": Math.random() * 0.5 + 0.1,; \"color\": colors[Math.floor(Math.random() * colors.length)],; \"life\": Math.random() * 100,; \"maxLife\": \'100;\'})} const gridOpacity = intensity === \'low\' ? 0.1 : intensity === \'medium\' ? 0.2 : 0.3; const animate = () => {; ctx.fillStyle = \'rgba(0,0,0,0.1); ctx.fillRect(0,0,canvas.width,canvas.height); ctx.strokeStyle = `rgba(0,255,255,${gridOpacity}) `; ctx.lineWidth = 1; for(let x = 0; x < canvas.width; x += gridSize) {; ctx.beginPath(); ctx.moveTo(x,0); ctx.lineTo(x,canvas.height); ctx.stroke()} for(let y = 0; y < canvas.height; y += gridSize) {; ctx.beginPath(); ctx.moveTo(0,y); ctx.lineTo(canvas.width,y); ctx.stroke()} particles.forEach((particle,index) => {; particle.x += particle.vx; particle.y += particle.vy; if(particle.x <= 0 || particle.x >= canvas.width) {; particle.vx *= -1} if(particle.y <= 0 || particle.y >= canvas.height) {; particle.vy *= -1} particle.life--; if(particle.life <= 0) {; particle.life = particle.maxLife; particle.x = Math.random () * canvas.width; particle.y = Math.random () * canvas.height} const alpha = (particle.life / particle.maxLife) * particle.opacity;\'"; ctx.fillStyle = \"${particle.color}${Math.floor(alpha * 255).toString(16).padStart(2,0\')}\"; ctx.beginPath(); ctx.arc(particle.x,particle.y,particle.size,0,Math.PI * 2); ctx.fill(); const gradient = ctx.createRadialGradient(particle.x,particle.y,0,particle.x,particle.y,particle.size * 3);\'"; gradient.addColorStop(0,`${particle.color}${Math.floor(alpha * 100).toString(16).padStart(2,0\')}`); gradient.addColorStop(1,transparent\'); ctx.fillStyle = gradient; ctx.beginPath(); ctx.arc(particle.x,particle.y,particle.size * 3,0,Math.PI * 2); ctx.fill()}); ctx.strokeStyle = `rgba(0,255,255,${gridOpacity * 0.5})`; ctx.lineWidth = 0.5; for(let i = 0; i < particles.length; i++) {; for(let j = i + 1; j < particles.length; j++) {; const distance = Math.sqrt(dx * dx + dy * dy); if(distance < 100) {; const alpha = (1 - distance / 100) * gridOpacity * 0.5;\"; ctx.strokeStyle = \"rgba(0,255,255,${alpha})\"; ctx.beginPath(); ctx.moveTo(particles[i].x,particles[i].y); ctx.lineTo(particles[j].x,particles[j].y); ctx.stroke()} } } const time = Date.now () * 0.001; for(let i = 0; i < 3; i++) {; const x = Math.sin(time * 0.5 + i) * canvas.width * 0.3 + canvas.width * 0.5; const y = Math.cos(time * 0.3 + i) * canvas.height * 0.3 + canvas.height * 0.5; const size = Math.sin(time + i) * 20 + 40; const alpha = (Math.sin(time * 2 + i) + 1) * 0.1 + 0.05; const gradient = ctx.createRadialGradient(x,y,0,x,y,size * 2);\"; gradient.addColorStop(0,`rgba(0,255,255,${alpha})`);\"; gradient.addColorStop(0.5,\"rgba(0,255,255,${alpha * 0.5})\"); gradient.addColorStop(1,transparent\'); ctx.fillStyle = gradient; ctx.beginPath(); ctx.arc(x,y,size * 2,0,Math.PI * 2); ctx.fill(); ctx.fillStyle = \"rgba(0,255,255,${alpha * 2})\"; ctx.beginPath(); ctx.arc(x,y,size * 0.3,0,Math.PI * 2); ctx.fill()} const scanGradient = ctx.createLinearGradient(0,scanY - 2,0,scanY + 2); scanGradient.addColorStop(0,transparent\'); scanGradient.addColorStop(0.5,rgba(0,255,255,0.3)); scanGradient.addColorStop(1,transparent\'); ctx.fillStyle = scanGradient; ctx.fillRect(0,scanY - 2,canvas.width,4) ; animationRef.current = requestAnimationFrame(animate) }; animate () ; return () => {; if(animationRef.current) {; cancelAnimationFrame(animationRef.current)} window.removeEventListener(\'resize\',resizeCanvas)}},[intensity,colorScheme]);\"; return (<canvas ref={canvasRef} className={`fixed inset-0 pointer-events-none ${className}`} style={{ \"zIndex\": \'-1\' }}/>)}; export default FuturisticAnimatedBackground;}}}}}}}}}}}\'`;
const React,{ useEffect,useRef } from "react"; export const FuturisticAnimatedBackground = ({ className = "",intensity = "medium",colorScheme = "multi" }) => {; const canvasRef = useRef(null) ; const animationRef = useRef () ; useEffect(() => {},[]); const canvas = canvasRef.current; useEffect(() => { const canvas = canvasRef.current; if(!canvas); return; const ctx = canvas.getContext("2d"); if(!ctx); return; const resizeCanvas = () => {; canvas.width = window.innerWidth; canvas.height = window.innerHeight}; resizeCanvas(); window.addEventListener("resize",resizeCanvas); const particles = []; const colors = colorSchemes[colorScheme]; const particleCount = intensity === "low" ? 50 : intensity === "medium" ? 100 : 200; for(let i = 0; i < particleCount; i++) {; particles.push({; x: Math.random() * canvas.width,; y: Math.random() * canvas.height,; vx: (Math.random() - 0.5) * 2,; vy: (Math.random() - 0.5) * 2,; size: Math.random() * 3 + 1,; opacity: Math.random() * 0.5 + 0.1,; color: colors[Math.floor(Math.random() * colors.length)],; life: Math.random() * 100,; maxLife: "100;"})} const gridOpacity = intensity === "low" ? 0.1 : intensity === "medium" ? 0.2 : 0.3; const animate = () => {; ctx.fillStyle = "rgba(0,0,0,0.1); ctx.fillRect(0,0,canvas.width,canvas.height); ctx.strokeStyle = `rgba(0,255,255,${gridOpacity}) `; ctx.lineWidth = 1; for(let x = 0; x < canvas.width; x += gridSize) {; ctx.beginPath(); ctx.moveTo(x,0); ctx.lineTo(x,canvas.height); ctx.stroke()} for(let y = 0; y < canvas.height; y += gridSize) {; ctx.beginPath(); ctx.moveTo(0,y); ctx.lineTo(canvas.width,y); ctx.stroke()} particles.forEach((particle,index) => {; particle.x += particle.vx; particle.y += particle.vy; if(particle.x <= 0 | particle.x >= canvas.width) {; particle.vx *= -1} if(particle.y <= 0 | particle.y >= canvas.height) {; particle.vy *= -1} particle.life--; if(particle.life <= 0) {; particle.life = particle.maxLife; particle.x = Math.random () * canvas.width; particle.y = Math.random () * canvas.height} const alpha = (particle.life / particle.maxLife) * particle.opacity;""; ctx.fillStyle = "${particle.color}${Math.floor(alpha * 255).toString(16).padStart(2,0")}"; ctx.beginPath(); ctx.arc(particle.x,particle.y,particle.size,0,Math.PI * 2); ctx.fill(); const gradient = ctx.createRadialGradient(particle.x,particle.y,0,particle.x,particle.y,particle.size * 3);""; gradient.addColorStop(0,`${particle.color}${Math.floor(alpha * 100).toString(16).padStart(2,0")}`); gradient.addColorStop(1,transparent"); ctx.fillStyle = gradient; ctx.beginPath(); ctx.arc(particle.x,particle.y,particle.size * 3,0,Math.PI * 2); ctx.fill()}); ctx.strokeStyle = `rgba(0,255,255,${gridOpacity * 0.5})`; ctx.lineWidth = 0.5; for(let i = 0; i < particles.length; i++) {; for(let j = i + 1; j < particles.length; j++) {; const distance = Math.sqrt(dx * dx + dy * dy); if(distance < 100) {; const alpha = (1 - distance / 100) * gridOpacity * 0.5;"; ctx.strokeStyle = "rgba(0,255,255,${alpha})"; ctx.beginPath(); ctx.moveTo(particles[i].x,particles[i].y); ctx.lineTo(particles[j].x,particles[j].y); ctx.stroke()} } } const time = Date.now () * 0.001; for(let i = 0; i < 3; i++) {; const x = Math.sin(time * 0.5 + i) * canvas.width * 0.3 + canvas.width * 0.5; const y = Math.cos(time * 0.3 + i) * canvas.height * 0.3 + canvas.height * 0.5; const size = Math.sin(time + i) * 20 + 40; const alpha = (Math.sin(time * 2 + i) + 1) * 0.1 + 0.05; const gradient = ctx.createRadialGradient(x,y,0,x,y,size * 2);"; gradient.addColorStop(0,`rgba(0,255,255,${alpha})`);"; gradient.addColorStop(0.5,"rgba(0,255,255,${alpha * 0.5})"); gradient.addColorStop(1,transparent"); ctx.fillStyle = gradient; ctx.beginPath(); ctx.arc(x,y,size * 2,0,Math.PI * 2); ctx.fill(); ctx.fillStyle = "rgba(0,255,255,${alpha * 2})"; ctx.beginPath(); ctx.arc(x,y,size * 0.3,0,Math.PI * 2); ctx.fill()} const scanGradient = ctx.createLinearGradient(0,scanY - 2,0,scanY + 2); scanGradient.addColorStop(0,transparent"); scanGradient.addColorStop(0.5,rgba(0,255,255,0.3)); scanGradient.addColorStop(1,transparent"); ctx.fillStyle = scanGradient; ctx.fillRect(0,scanY - 2,canvas.width,4) ; animationRef.current = requestAnimationFrame(animate) }; animate () ; return () => {; if(animationRef.current) {; cancelAnimationFrame(animationRef.current)} window.removeEventListener("resize",resizeCanvas)}},[intensity,colorScheme]);"; return (<canvas ref={canvasRef} className={`fixed inset-0 pointer-events-none ${className}`} style={{ zIndex: "-1" }}/>)}; export default FuturisticAnimatedBackground;}}}}}}}}}}}"`;'"`'"`
=======


=======

=======
import React,{ useEffect,useRef } from \'react\'; export const FuturisticAnimatedBackground = ({ className = \'',intensity = \'medium\',colorScheme = \'multi\' }) => {; const canvasRef = useRef(null) ; const animationRef = useRef () ; useEffect(() => {},[]); const canvas = canvasRef.current; useEffect(() => { const canvas = canvasRef.current; if(!canvas); return; const ctx = canvas.getContext(\'2d\'); if(!ctx); return; const resizeCanvas = () => {; canvas.width = window.innerWidth; canvas.height = window.innerHeight}; resizeCanvas(); window.addEventListener(\'resize\',resizeCanvas); const particles = []; const colors = colorSchemes[colorScheme]; const particleCount = intensity === \'low\' ? 50 : intensity === \'medium\' ? 100 : 200; for(let i = 0; i < particleCount; i++) {; particles.push({; \"x\": Math.random() * canvas.width,; \"y\": Math.random() * canvas.height,; \"vx\": (Math.random() - 0.5) * 2,; \"vy\": (Math.random() - 0.5) * 2,; \"size\": Math.random() * 3 + 1,; \"opacity\": Math.random() * 0.5 + 0.1,; \"color\": colors[Math.floor(Math.random() * colors.length)],; \"life\": Math.random() * 100,; \"maxLife\": \'100;\'})} const gridOpacity = intensity === \'low\' ? 0.1 : intensity === \'medium\' ? 0.2 : 0.3; const animate = () => {; ctx.fillStyle = \'rgba(0,0,0,0.1); ctx.fillRect(0,0,canvas.width,canvas.height); ctx.strokeStyle = `rgba(0,255,255,${gridOpacity}) `; ctx.lineWidth = 1; for(let x = 0; x < canvas.width; x += gridSize) {; ctx.beginPath(); ctx.moveTo(x,0); ctx.lineTo(x,canvas.height); ctx.stroke()} for(let y = 0; y < canvas.height; y += gridSize) {; ctx.beginPath(); ctx.moveTo(0,y); ctx.lineTo(canvas.width,y); ctx.stroke()} particles.forEach((particle,index) => {; particle.x += particle.vx; particle.y += particle.vy; if(particle.x <= 0 || particle.x >= canvas.width) {; particle.vx *= -1} if(particle.y <= 0 || particle.y >= canvas.height) {; particle.vy *= -1} particle.life--; if(particle.life <= 0) {; particle.life = particle.maxLife; particle.x = Math.random () * canvas.width; particle.y = Math.random () * canvas.height} const alpha = (particle.life / particle.maxLife) * particle.opacity;\'"; ctx.fillStyle = \"${particle.color}${Math.floor(alpha * 255).toString(16).padStart(2,0\')}\"; ctx.beginPath(); ctx.arc(particle.x,particle.y,particle.size,0,Math.PI * 2); ctx.fill(); const gradient = ctx.createRadialGradient(particle.x,particle.y,0,particle.x,particle.y,particle.size * 3);\'"; gradient.addColorStop(0,`${particle.color}${Math.floor(alpha * 100).toString(16).padStart(2,0\')}`); gradient.addColorStop(1,transparent\'); ctx.fillStyle = gradient; ctx.beginPath(); ctx.arc(particle.x,particle.y,particle.size * 3,0,Math.PI * 2); ctx.fill()}); ctx.strokeStyle = `rgba(0,255,255,${gridOpacity * 0.5})`; ctx.lineWidth = 0.5; for(let i = 0; i < particles.length; i++) {; for(let j = i + 1; j < particles.length; j++) {; const distance = Math.sqrt(dx * dx + dy * dy); if(distance < 100) {; const alpha = (1 - distance / 100) * gridOpacity * 0.5;\"; ctx.strokeStyle = \"rgba(0,255,255,${alpha})\"; ctx.beginPath(); ctx.moveTo(particles[i].x,particles[i].y); ctx.lineTo(particles[j].x,particles[j].y); ctx.stroke()} } } const time = Date.now () * 0.001; for(let i = 0; i < 3; i++) {; const x = Math.sin(time * 0.5 + i) * canvas.width * 0.3 + canvas.width * 0.5; const y = Math.cos(time * 0.3 + i) * canvas.height * 0.3 + canvas.height * 0.5; const size = Math.sin(time + i) * 20 + 40; const alpha = (Math.sin(time * 2 + i) + 1) * 0.1 + 0.05; const gradient = ctx.createRadialGradient(x,y,0,x,y,size * 2);\"; gradient.addColorStop(0,`rgba(0,255,255,${alpha})`);\"; gradient.addColorStop(0.5,\"rgba(0,255,255,${alpha * 0.5})\"); gradient.addColorStop(1,transparent\'); ctx.fillStyle = gradient; ctx.beginPath(); ctx.arc(x,y,size * 2,0,Math.PI * 2); ctx.fill(); ctx.fillStyle = \"rgba(0,255,255,${alpha * 2})\"; ctx.beginPath(); ctx.arc(x,y,size * 0.3,0,Math.PI * 2); ctx.fill()} const scanGradient = ctx.createLinearGradient(0,scanY - 2,0,scanY + 2); scanGradient.addColorStop(0,transparent\'); scanGradient.addColorStop(0.5,rgba(0,255,255,0.3)); scanGradient.addColorStop(1,transparent\'); ctx.fillStyle = scanGradient; ctx.fillRect(0,scanY - 2,canvas.width,4) ; animationRef.current = requestAnimationFrame(animate) }; animate () ; return () => {; if(animationRef.current) {; cancelAnimationFrame(animationRef.current)} window.removeEventListener(\'resize\',resizeCanvas)}},[intensity,colorScheme]);\"; return (<canvas ref={canvasRef} className={`fixed inset-0 pointer-events-none ${className}`} style={{ \"zIndex\": \'-1\' }}/>)}; export default FuturisticAnimatedBackground;}}}}}}}}}}}\'`;
const React,{ useEffect,useRef } from "react"; export const FuturisticAnimatedBackground = ({ className = "",intensity = "medium",colorScheme = "multi" }) => {; const canvasRef = useRef(null) ; const animationRef = useRef () ; useEffect(() => {},[]); const canvas = canvasRef.current; useEffect(() => { const canvas = canvasRef.current; if(!canvas); return; const ctx = canvas.getContext("2d"); if(!ctx); return; const resizeCanvas = () => {; canvas.width = window.innerWidth; canvas.height = window.innerHeight}; resizeCanvas(); window.addEventListener("resize",resizeCanvas); const particles = []; const colors = colorSchemes[colorScheme]; const particleCount = intensity === "low" ? 50 : intensity === "medium" ? 100 : 200; for(let i = 0; i < particleCount; i++) {; particles.push({; x: Math.random() * canvas.width,; y: Math.random() * canvas.height,; vx: (Math.random() - 0.5) * 2,; vy: (Math.random() - 0.5) * 2,; size: Math.random() * 3 + 1,; opacity: Math.random() * 0.5 + 0.1,; color: colors[Math.floor(Math.random() * colors.length)],; life: Math.random() * 100,; maxLife: "100;"})} const gridOpacity = intensity === "low" ? 0.1 : intensity === "medium" ? 0.2 : 0.3; const animate = () => {; ctx.fillStyle = "rgba(0,0,0,0.1); ctx.fillRect(0,0,canvas.width,canvas.height); ctx.strokeStyle = `rgba(0,255,255,${gridOpacity}) `; ctx.lineWidth = 1; for(let x = 0; x < canvas.width; x += gridSize) {; ctx.beginPath(); ctx.moveTo(x,0); ctx.lineTo(x,canvas.height); ctx.stroke()} for(let y = 0; y < canvas.height; y += gridSize) {; ctx.beginPath(); ctx.moveTo(0,y); ctx.lineTo(canvas.width,y); ctx.stroke()} particles.forEach((particle,index) => {; particle.x += particle.vx; particle.y += particle.vy; if(particle.x <= 0 | particle.x >= canvas.width) {; particle.vx *= -1} if(particle.y <= 0 | particle.y >= canvas.height) {; particle.vy *= -1} particle.life--; if(particle.life <= 0) {; particle.life = particle.maxLife; particle.x = Math.random () * canvas.width; particle.y = Math.random () * canvas.height} const alpha = (particle.life / particle.maxLife) * particle.opacity;""; ctx.fillStyle = "${particle.color}${Math.floor(alpha * 255).toString(16).padStart(2,0")}"; ctx.beginPath(); ctx.arc(particle.x,particle.y,particle.size,0,Math.PI * 2); ctx.fill(); const gradient = ctx.createRadialGradient(particle.x,particle.y,0,particle.x,particle.y,particle.size * 3);""; gradient.addColorStop(0,`${particle.color}${Math.floor(alpha * 100).toString(16).padStart(2,0")}`); gradient.addColorStop(1,transparent"); ctx.fillStyle = gradient; ctx.beginPath(); ctx.arc(particle.x,particle.y,particle.size * 3,0,Math.PI * 2); ctx.fill()}); ctx.strokeStyle = `rgba(0,255,255,${gridOpacity * 0.5})`; ctx.lineWidth = 0.5; for(let i = 0; i < particles.length; i++) {; for(let j = i + 1; j < particles.length; j++) {; const distance = Math.sqrt(dx * dx + dy * dy); if(distance < 100) {; const alpha = (1 - distance / 100) * gridOpacity * 0.5;"; ctx.strokeStyle = "rgba(0,255,255,${alpha})"; ctx.beginPath(); ctx.moveTo(particles[i].x,particles[i].y); ctx.lineTo(particles[j].x,particles[j].y); ctx.stroke()} } } const time = Date.now () * 0.001; for(let i = 0; i < 3; i++) {; const x = Math.sin(time * 0.5 + i) * canvas.width * 0.3 + canvas.width * 0.5; const y = Math.cos(time * 0.3 + i) * canvas.height * 0.3 + canvas.height * 0.5; const size = Math.sin(time + i) * 20 + 40; const alpha = (Math.sin(time * 2 + i) + 1) * 0.1 + 0.05; const gradient = ctx.createRadialGradient(x,y,0,x,y,size * 2);"; gradient.addColorStop(0,`rgba(0,255,255,${alpha})`);"; gradient.addColorStop(0.5,"rgba(0,255,255,${alpha * 0.5})"); gradient.addColorStop(1,transparent"); ctx.fillStyle = gradient; ctx.beginPath(); ctx.arc(x,y,size * 2,0,Math.PI * 2); ctx.fill(); ctx.fillStyle = "rgba(0,255,255,${alpha * 2})"; ctx.beginPath(); ctx.arc(x,y,size * 0.3,0,Math.PI * 2); ctx.fill()} const scanGradient = ctx.createLinearGradient(0,scanY - 2,0,scanY + 2); scanGradient.addColorStop(0,transparent"); scanGradient.addColorStop(0.5,rgba(0,255,255,0.3)); scanGradient.addColorStop(1,transparent"); ctx.fillStyle = scanGradient; ctx.fillRect(0,scanY - 2,canvas.width,4) ; animationRef.current = requestAnimationFrame(animate) }; animate () ; return () => {; if(animationRef.current) {; cancelAnimationFrame(animationRef.current)} window.removeEventListener("resize",resizeCanvas)}},[intensity,colorScheme]);"; return (<canvas ref={canvasRef} className={`fixed inset-0 pointer-events-none ${className}`} style={{ zIndex: "-1" }}/>)}; export default FuturisticAnimatedBackground;}}}}}}}}}}}"`;'"`'"`>>>>>>> 4b01bbd5bc5a9373450c5efad91d38fbaa54fdb4:temp_exclude/src.disabled/src/components/FuturisticAnimatedBackground.jsx
>>>>>>> cursor/fix-website-loading-errors-and-merge-6662
import _React,{ useEffect,useRef } from 'react'; export const FuturisticAnimatedBackground = ({ className = ',intensity = 'medium',colorScheme = 'multi' }) => {; const canvasRef = useRef(null) ; const animationRef = useRef () ; useEffect(() => {},[]); const canvas = canvasRef.current; useEffect(() => { const canvas = canvasRef.current; if(!canvas); return; const ctx = canvas.getContext('2d'); if(!ctx); return; const resizeCanvas = () => {; canvas.width = window.innerWidth; canvas.height = window.innerHeight}; resizeCanvas(); window.addEventListener('resize',resizeCanvas); const particles = []; const colors = colorSchemes[colorScheme]; const particleCount = intensity === 'low' ? 50 : intensity === 'medium' ? 100 : 200; for(let i = 0; i < particleCount; i++) {; particles.push({; "x": Math.random() * canvas.width,; "y": Math.random() * canvas.height,; "vx": (Math.random() - 0.5) * 2,; "vy": (Math.random() - 0.5) * 2,; "size": Math.random() * 3 + 1,; "opacity": Math.random() * 0.5 + 0.1,; "color": colors[Math.floor(Math.random() * colors.length)],; "life": Math.random() * 100,; "maxLife": '100;'})} const gridOpacity = intensity === 'low' ? 0.1 : intensity === 'medium' ? 0.2 : 0.3; const animate = () => {; ctx.fillStyle = 'rgba(0,0,0,0.1); ctx.fillRect(0,0,canvas.width,canvas.height); ctx.strokeStyle = `rgba(0,255,255,${gridOpacity}) `; ctx.lineWidth = 1; for(let x = 0; x < canvas.width; x += gridSize) {; ctx.beginPath(); ctx.moveTo(x,0); ctx.lineTo(x,canvas.height); ctx.stroke()} for(let y = 0; y < canvas.height; y += gridSize) {; ctx.beginPath(); ctx.moveTo(0,y); ctx.lineTo(canvas.width,y); ctx.stroke()} particles.forEach((particle,index) => {; particle.x += particle.vx; particle.y += particle.vy; if(particle.x <= 0 || particle.x >= canvas.width) {; particle.vx *= -1} if(particle.y <= 0 || particle.y >= canvas.height) {; particle.vy *= -1} particle.life--; if(particle.life <= 0) {; particle.life = particle.maxLife; particle.x = Math.random () * canvas.width; particle.y = Math.random () * canvas.height} const alpha = (particle.life / particle.maxLife) * particle.opacity;'"; ctx.fillStyle = "${particle.color}${Math.floor(alpha * 255).toString(16).padStart(2,0')}"; ctx.beginPath(); ctx.arc(particle.x,particle.y,particle.size,0,Math.PI * 2); ctx.fill(); const gradient = ctx.createRadialGradient(particle.x,particle.y,0,particle.x,particle.y,particle.size * 3);'"; gradient.addColorStop(0,`${particle.color}${Math.floor(alpha * 100).toString(16).padStart(2,0')}`); gradient.addColorStop(1,transparent'); ctx.fillStyle = gradient; ctx.beginPath(); ctx.arc(particle.x,particle.y,particle.size * 3,0,Math.PI * 2); ctx.fill()}); ctx.strokeStyle = `rgba(0,255,255,${gridOpacity * 0.5})`; ctx.lineWidth = 0.5; for(let i = 0; i < particles.length; i++) {; for(let j = i + 1; j < particles.length; j++) {; const distance = Math.sqrt(dx * dx + dy * dy); if(distance < 100) {; const alpha = (1 - distance / 100) * gridOpacity * 0.5;"; ctx.strokeStyle = "rgba(0,255,255,${alpha})"; ctx.beginPath(); ctx.moveTo(particles[i].x,particles[i].y); ctx.lineTo(particles[j].x,particles[j].y); ctx.stroke()} } } const time = Date.now () * 0.001; for(let i = 0; i < 3; i++) {; const x = Math.sin(time * 0.5 + i) * canvas.width * 0.3 + canvas.width * 0.5; const y = Math.cos(time * 0.3 + i) * canvas.height * 0.3 + canvas.height * 0.5; const size = Math.sin(time + i) * 20 + 40; const alpha = (Math.sin(time * 2 + i) + 1) * 0.1 + 0.05; const gradient = ctx.createRadialGradient(x,y,0,x,y,size * 2);"; gradient.addColorStop(0,`rgba(0,255,255,${alpha})`);"; gradient.addColorStop(0.5,"rgba(0,255,255,${alpha * 0.5})"); gradient.addColorStop(1,transparent'); ctx.fillStyle = gradient; ctx.beginPath(); ctx.arc(x,y,size * 2,0,Math.PI * 2); ctx.fill(); ctx.fillStyle = "rgba(0,255,255,${alpha * 2})"; ctx.beginPath(); ctx.arc(x,y,size * 0.3,0,Math.PI * 2); ctx.fill()} const scanGradient = ctx.createLinearGradient(0,scanY - 2,0,scanY + 2); scanGradient.addColorStop(0,transparent'); scanGradient.addColorStop(0.5,rgba(0,255,255,0.3)); scanGradient.addColorStop(1,transparent'); ctx.fillStyle = scanGradient; ctx.fillRect(0,scanY - 2,canvas.width,4) ; animationRef.current = requestAnimationFrame(animate) }; animate () ; return () => {; if(animationRef.current) {; cancelAnimationFrame(animationRef.current)} window.removeEventListener('resize',resizeCanvas)}},[intensity,colorScheme]);"; return (<canvas ref={canvasRef} className={`fixed inset-0 pointer-events-none ${className}`} style={{ "zIndex": '-1' }}/>)}; export default FuturisticAnimatedBackground;}}}}}}}}}}}'`;
=======
=======
import _React,{ useEffect,useRef } from 'react'; export const FuturisticAnimatedBackground = ({ className = ',intensity = 'medium',colorScheme = 'multi' }) => {; const canvasRef = useRef(null) ; const animationRef = useRef () ; useEffect(() => {},[]); const canvas = canvasRef && canvasRef.current; useEffect(() => { const canvas = canvasRef && canvasRef.current; if(!canvas); return; const ctx = canvas && canvas.getContext('2d'); if(!ctx); return; const resizeCanvas = () => {; canvas && canvas.width = window && window.innerWidth; canvas && canvas.height = window && window.innerHeight}; resizeCanvas(); window && window.addEventListener('resize',resizeCanvas); const particles = []; const colors = colorSchemes[colorScheme]; const particleCount = intensity === 'low' ? 50 : intensity === 'medium' ? 100 : 200; for(let i = 0; i < particleCount; i++) {; particles && particles.push({; "x": Math && Math.random() * canvas && canvas.width,; "y": Math && Math.random() * canvas && canvas.height,; "vx": (Math && Math.random() - 0 && 0.5) * 2,; "vy": (Math && Math.random() - 0 && 0.5) * 2,; "size": Math && Math.random() * 3 + 1,; "opacity": Math && Math.random() * 0 && 0.5 + 0 && 0.1,; "color": colors[Math && Math.floor(Math && Math.random() * colors && colors.length)],; "life": Math && Math.random() * 100,; "maxLife": '100;'})} const gridOpacity = intensity === 'low' ? 0 && 0.1 : intensity === 'medium' ? 0 && 0.2 : 0 && 0.3; const animate = () => {; ctx && ctx.fillStyle = 'rgba(0,0,0,0 && 0.1); ctx && ctx.fillRect(0,0,canvas && canvas.width,canvas && canvas.height); ctx && ctx.strokeStyle = `rgba(0,255,255,${gridOpacity}) `; ctx && ctx.lineWidth = 1; for(let x = 0; x < canvas && canvas.width; x += gridSize) {; ctx && ctx.beginPath(); ctx && ctx.moveTo(x,0); ctx && ctx.lineTo(x,canvas && canvas.height); ctx && ctx.stroke()} for(let y = 0; y < canvas && canvas.height; y += gridSize) {; ctx && ctx.beginPath(); ctx && ctx.moveTo(0,y); ctx && ctx.lineTo(canvas && canvas.width,y); ctx && ctx.stroke()} particles && particles.forEach((particle,index) => {; particle && particle.x += particle && particle.vx; particle && particle.y += particle && particle.vy; if(particle && particle.x <= 0 || particle && particle.x >= canvas && canvas.width) {; particle && particle.vx *= -1} if(particle && particle.y <= 0 || particle && particle.y >= canvas && canvas.height) {; particle && particle.vy *= -1} particle && particle.life--; if(particle && particle.life <= 0) {; particle && particle.life = particle && particle.maxLife; particle && particle.x = Math && Math.random () * canvas && canvas.width; particle && particle.y = Math && Math.random () * canvas && canvas.height} const alpha = (particle && particle.life / particle && particle.maxLife) * particle && particle.opacity;'"; ctx && ctx.fillStyle = "${particle && particle.color}${Math && Math.floor(alpha * 255).toString(16).padStart(2,0')}"; ctx && ctx.beginPath(); ctx && ctx.arc(particle && particle.x,particle && particle.y,particle && particle.size,0,Math && Math.PI * 2); ctx && ctx.fill(); const gradient = ctx && ctx.createRadialGradient(particle && particle.x,particle && particle.y,0,particle && particle.x,particle && particle.y,particle && particle.size * 3);'"; gradient && gradient.addColorStop(0,`${particle && particle.color}${Math && Math.floor(alpha * 100).toString(16).padStart(2,0')}`); gradient && gradient.addColorStop(1,transparent'); ctx && ctx.fillStyle = gradient; ctx && ctx.beginPath(); ctx && ctx.arc(particle && particle.x,particle && particle.y,particle && particle.size * 3,0,Math && Math.PI * 2); ctx && ctx.fill()}); ctx && ctx.strokeStyle = `rgba(0,255,255,${gridOpacity * 0 && 0.5})`; ctx && ctx.lineWidth = 0 && 0.5; for(let i = 0; i < particles && particles.length; i++) {; for(let j = i + 1; j < particles && particles.length; j++) {; const distance = Math && Math.sqrt(dx * dx + dy * dy); if(distance < 100) {; const alpha = (1 - distance / 100) * gridOpacity * 0 && 0.5;"; ctx && ctx.strokeStyle = "rgba(0,255,255,${alpha})"; ctx && ctx.beginPath(); ctx && ctx.moveTo(particles[i].x,particles[i].y); ctx && ctx.lineTo(particles[j].x,particles[j].y); ctx && ctx.stroke()} } } const time = Date && Date.now () * 0 && 0.001; for(let i = 0; i < 3; i++) {; const x = Math && Math.sin(time * 0 && 0.5 + i) * canvas && canvas.width * 0 && 0.3 + canvas && canvas.width * 0 && 0.5; const y = Math && Math.cos(time * 0 && 0.3 + i) * canvas && canvas.height * 0 && 0.3 + canvas && canvas.height * 0 && 0.5; const size = Math && Math.sin(time + i) * 20 + 40; const alpha = (Math && Math.sin(time * 2 + i) + 1) * 0 && 0.1 + 0 && 0.05; const gradient = ctx && ctx.createRadialGradient(x,y,0,x,y,size * 2);"; gradient && gradient.addColorStop(0,`rgba(0,255,255,${alpha})`);"; gradient && gradient.addColorStop(0 && 0.5,"rgba(0,255,255,${alpha * 0 && 0.5})"); gradient && gradient.addColorStop(1,transparent'); ctx && ctx.fillStyle = gradient; ctx && ctx.beginPath(); ctx && ctx.arc(x,y,size * 2,0,Math && Math.PI * 2); ctx && ctx.fill(); ctx && ctx.fillStyle = "rgba(0,255,255,${alpha * 2})"; ctx && ctx.beginPath(); ctx && ctx.arc(x,y,size * 0 && 0.3,0,Math && Math.PI * 2); ctx && ctx.fill()} const scanGradient = ctx && ctx.createLinearGradient(0,scanY - 2,0,scanY + 2); scanGradient && scanGradient.addColorStop(0,transparent'); scanGradient && scanGradient.addColorStop(0 && 0.5,rgba(0,255,255,0 && 0.3)); scanGradient && scanGradient.addColorStop(1,transparent'); ctx && ctx.fillStyle = scanGradient; ctx && ctx.fillRect(0,scanY - 2,canvas && canvas.width,4) ; animationRef && animationRef.current = requestAnimationFrame(animate) }; animate () ; return () => {; if(animationRef && animationRef.current) {; cancelAnimationFrame(animationRef && animationRef.current)} window && window.removeEventListener('resize',resizeCanvas)}},[intensity,colorScheme]);"; return (<canvas ref={canvasRef} className={`fixed inset-0 pointer-events-none ${className}`} style={{ "zIndex": '-1' }}/>)}; export default FuturisticAnimatedBackground;}}}}}}}}}}}'`;

<<<<<<< HEAD
<<<<<<< HEAD
import _React,{ useEffect,useRef } from 'react'; export const FuturisticAnimatedBackground = ({ className = ',intensity = 'medium',colorScheme = 'multi' }) => {; const canvasRef = useRef(null) ; const animationRef = useRef () ; useEffect(() => {},[]); const canvas = canvasRef.current; useEffect(() => { const canvas = canvasRef.current; if(!canvas); return; const ctx = canvas.getContext('2d'); if(!ctx); return; const resizeCanvas = () => {; canvas.width = window.innerWidth; canvas.height = window.innerHeight}; resizeCanvas(); window.addEventListener('resize',resizeCanvas); const particles = []; const colors = colorSchemes[colorScheme]; const particleCount = intensity === 'low' ? 50 : intensity === 'medium' ? 100 : 200; for(let i = 0; i < particleCount; i++) {; particles.push({; "x": Math.random() * canvas.width,; "y": Math.random() * canvas.height,; "vx": (Math.random() - 0.5) * 2,; "vy": (Math.random() - 0.5) * 2,; "size": Math.random() * 3 + 1,; "opacity": Math.random() * 0.5 + 0.1,; "color": colors[Math.floor(Math.random() * colors.length)],; "life": Math.random() * 100,; "maxLife": '100;'})} const gridOpacity = intensity === 'low' ? 0.1 : intensity === 'medium' ? 0.2 : 0.3; const animate = () => {; ctx.fillStyle = 'rgba(0,0,0,0.1); ctx.fillRect(0,0,canvas.width,canvas.height); ctx.strokeStyle = `rgba(0,255,255,${gridOpacity}) `; ctx.lineWidth = 1; for(let x = 0; x < canvas.width; x += gridSize) {; ctx.beginPath(); ctx.moveTo(x,0); ctx.lineTo(x,canvas.height); ctx.stroke()} for(let y = 0; y < canvas.height; y += gridSize) {; ctx.beginPath(); ctx.moveTo(0,y); ctx.lineTo(canvas.width,y); ctx.stroke()} particles.forEach((particle,index) => {; particle.x += particle.vx; particle.y += particle.vy; if(particle.x <= 0 || particle.x >= canvas.width) {; particle.vx *= -1} if(particle.y <= 0 || particle.y >= canvas.height) {; particle.vy *= -1} particle.life--; if(particle.life <= 0) {; particle.life = particle.maxLife; particle.x = Math.random () * canvas.width; particle.y = Math.random () * canvas.height} const alpha = (particle.life / particle.maxLife) * particle.opacity;'"; ctx.fillStyle = "${particle.color}${Math.floor(alpha * 255).toString(16).padStart(2,0')}"; ctx.beginPath(); ctx.arc(particle.x,particle.y,particle.size,0,Math.PI * 2); ctx.fill(); const gradient = ctx.createRadialGradient(particle.x,particle.y,0,particle.x,particle.y,particle.size * 3);'"; gradient.addColorStop(0,`${particle.color}${Math.floor(alpha * 100).toString(16).padStart(2,0')}`); gradient.addColorStop(1,transparent'); ctx.fillStyle = gradient; ctx.beginPath(); ctx.arc(particle.x,particle.y,particle.size * 3,0,Math.PI * 2); ctx.fill()}); ctx.strokeStyle = `rgba(0,255,255,${gridOpacity * 0.5})`; ctx.lineWidth = 0.5; for(let i = 0; i < particles.length; i++) {; for(let j = i + 1; j < particles.length; j++) {; const distance = Math.sqrt(dx * dx + dy * dy); if(distance < 100) {; const alpha = (1 - distance / 100) * gridOpacity * 0.5;"; ctx.strokeStyle = "rgba(0,255,255,${alpha})"; ctx.beginPath(); ctx.moveTo(particles[i].x,particles[i].y); ctx.lineTo(particles[j].x,particles[j].y); ctx.stroke()} } } const time = Date.now () * 0.001; for(let i = 0; i < 3; i++) {; const x = Math.sin(time * 0.5 + i) * canvas.width * 0.3 + canvas.width * 0.5; const y = Math.cos(time * 0.3 + i) * canvas.height * 0.3 + canvas.height * 0.5; const size = Math.sin(time + i) * 20 + 40; const alpha = (Math.sin(time * 2 + i) + 1) * 0.1 + 0.05; const gradient = ctx.createRadialGradient(x,y,0,x,y,size * 2);"; gradient.addColorStop(0,`rgba(0,255,255,${alpha})`);"; gradient.addColorStop(0.5,"rgba(0,255,255,${alpha * 0.5})"); gradient.addColorStop(1,transparent'); ctx.fillStyle = gradient; ctx.beginPath(); ctx.arc(x,y,size * 2,0,Math.PI * 2); ctx.fill(); ctx.fillStyle = "rgba(0,255,255,${alpha * 2})"; ctx.beginPath(); ctx.arc(x,y,size * 0.3,0,Math.PI * 2); ctx.fill()} const scanGradient = ctx.createLinearGradient(0,scanY - 2,0,scanY + 2); scanGradient.addColorStop(0,transparent'); scanGradient.addColorStop(0.5,rgba(0,255,255,0.3)); scanGradient.addColorStop(1,transparent'); ctx.fillStyle = scanGradient; ctx.fillRect(0,scanY - 2,canvas.width,4) ; animationRef.current = requestAnimationFrame(animate) }; animate () ; return () => {; if(animationRef.current) {; cancelAnimationFrame(animationRef.current)} window.removeEventListener('resize',resizeCanvas)}},[intensity,colorScheme]);"; return (<canvas ref={canvasRef} className={`fixed inset-0 pointer-events-none ${className}`} style={{ "zIndex": '-1' }}/>)}; export default FuturisticAnimatedBackground;}}}}}}}}}}}'`;
==============
>>>>>>> f8e247744ae2f2b9a6ba0423164ce0dcdffb9f6a
