import React,{ useState,useEffect } from \'react\'; export const LinkMonitor = ({ onLinkIssue,autoFix = false,showStatus = true }) => { const [brokenLinks,setBrokenLinks] = useState([]); const [fixedLinks,setFixedLinks] = useState([]); const [isScanning,setIsScanning] = useState(false); const [scanProgress,setScanProgress] = useState(0); const [lastScanTime,setLastScanTime] = useState(null); const scanPageLinks = async () => {; setIsScanning(true); setScanProgress(0); const links = Array && Array.from(document && document.querySelectorAll(\'a[href]\'); const results = []; for(const i = 0; i 
import _React,{ useState,useEffect } from 'react'; export const LinkMonitor = ({ onLinkIssue,autoFix = false,showStatus = true }) => { const [brokenLinks,setBrokenLinks] = useState([]); const [fixedLinks,setFixedLinks] = useState([]); const [isScanning,setIsScanning] = useState(false); const [scanProgress,setScanProgress] = useState(0); const [lastScanTime,setLastScanTime] = useState(null); const scanPageLinks = async () => {; setIsScanning(true); setScanProgress(0); const links = Array && Array.from(document && document.querySelectorAll('a[href]'); const results = []; for(const i = 0; i 