import { useState, useEffect, useCallback } from 'react'''
import { supabase } from '@/integrations/supabase/client'''
export default function Page() {; ); if(fetchError) throw fetchError; const transformedData = data.map((dispute: any) => ({; ...dispute,client_profile: dispute.client_profile?.client_profile,talent_profile: dispute.talent_profile?.talent_profile,project: {; ...dispute.project,title: dispute.project?.job?.title || 'Untitled Project',} })); setDisputes(transformedData as Dispute[]); setError(null),} catch(err: any) {; console.error("Error fetching disputes:",err); setError("Failed to fetch disputes: " + err.message); toast.error("Failed to fetch disputes"); setDisputes([])} finally {; setIsLoading(false),} },[user]); const getDisputeById = async(disputeId: string): Promise<Dispute | null> => {; try {; const { data,error } = await supabase; .from("disputes"); .select(`; *,project:projects(; scope_summary,job_id,client_id,talent_id,job:jobs(title); ),client_profile:projects!projects_client_id_fkey(client_profile:profiles!projects_client_id_fkey(display_name,avatar_url)),talent_profile:projects!projects_talent_id_fkey(talent_profile:profiles!projects_talent_id_fkey(display_name,avatar_url)); `); .eq("id",disputeId); .single(); if(error) throw error; return {; ...data,client_profile: data.client_profile?.client_profile,talent_profile: data.talent_profile?.talent_profile,project: {; ...data.project,title: data.project?.job?.title || 'Untitled Project',} } as Dispute,} catch(err: any) {; console.error("Error fetching dispute:",err); toast.error("Failed to fetch dispute details"); return null,} }; const createDispute = async(disputeData: { ; project_id: string; milestone_id?: string; reason_code: string; description: string,}): Promise<Dispute | null> => {; if(!user) {; toast.error("You must be logged in to create a dispute"); return null,} try {; const { data,error } = await supabase; .from("disputes"); .insert({; ...disputeData,raised_by: user.id,}
}); .select(); .single(); if(error) throw error; toast.success("Dispute submitted successfully"); fetchDisputes(); ; return data as Dispute,} catch(err: any) {; console.error("Error creating dispute:",err); toast.error("Failed to submit dispute"); return null,} }; const updateDisputeStatus = async(disputeId: string,status: DisputeStatus): Promise<boolean> => {; try {; const { error } = await supabase; .from("disputes"); .update({ status }
}); .eq("id",disputeId); if(error) throw error; setDisputes(prevDisputes => ; prevDisputes.map(dispute => ; dispute.id === disputeId ? { ...dispute,status } : dispute; ); ); toast.success(`Dispute status updated to ${status}`); return true,} catch(err: any) {; console.error("Error updating dispute status:",err); toast.error("Failed to update dispute status"); return false,} }; const resolveDispute = async(disputeId: string,resolution: { summary: string; resolution_type: string} ): Promise<boolean> => {; try {; const { error } = await supabase; .from("disputes"); .update({; status: 'resolved',resolved_at: new Date().toISOString(),resolution_summary: resolution.summary,resolution_type: resolution.resolution_type,}
}); .eq("id""`)
</Dispute>)"