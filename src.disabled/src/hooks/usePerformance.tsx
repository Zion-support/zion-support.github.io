:src/hooks/usePerformance.tsx; interface PerformanceMetrics {}} interface PerformanceOptions {; enableRealUserMonitoring?: boolean; enableWebVitals?: boolean; enableResourceTiming?: boolean; enableNavigationTiming?: boolean; logToConsole?: boolean; :src/hooks/usePerformance.tsx; sendToAnalytics?: boolean} sendToAnalytics?: boolean} export function usePerformance(\"options\": PerformanceOptions = {}) {; const {; enableRealUserMonitoring = true,; enableWebVitals = true,; enableResourceTiming = true,; enableNavigationTiming = true,; logToConsole = false,; sendToAnalytics = false} = options; const [metrics,setMetrics] = useState<PerformanceMetrics>({; \"fcp\": \'null\',; \"lcp\": \'null\',; \"fid\": \'null\',; \"cls\": \'null\',; \"ttfb\": \'null\',; \"domLoad\": \'null\',; \"windowLoad\": \'null\',; \"navigationStart\": \'null\'}); const [isMonitoring,setIsMonitoring] = useState(false); return; if(fcpEntry) {}; setMetrics(prev => ({ ...prev,\"fcp\": \'fcpEntry.startTime\' }))}); if(lcpEntry) {}; setMetrics(prev => ({ ...prev,\"lcp\": \'lcpEntry.startTime\' }))}); if(fidEntry && \'processingStart\' in fidEntry) {}; setMetrics(prev => ({ ...prev,\"fid\": \'fidEntry.processingStart - fidEntry.startTime\' }))}); for(const entry of list.getEntries()) {; return; if(fcpEntry) {; setMetrics(prev => ({ ...prev,\"fcp\": \'fcpEntry.startTime\' }))}); if(lcpEntry) {}; setMetrics(prev => ({ ...prev,\"lcp\": \'lcpEntry.startTime\' }))}); if(fidEntry && \'processingStart\' in fidEntry) {}; setMetrics(prev => ({ ...prev,\"fid\": \'fidEntry.processingStart - fidEntry.startTime\' }))}); for(const entry of list.getEntries()) {; if(!layoutShiftEntry.hadRecentInput) {; clsValue += layoutShiftEntry.value; setMetrics(prev => ({ ...prev,\"cls\": \'clsValue\' }));}); try {}; fcpObserver.observe({ \"entryTypes\": [\'paint\'] }); lcpObserver.observe({ \"entryTypes\": [\'largest-contentful-paint\'] }); fidObserver.observe({ \"entryTypes\": [\'first-input\'] }); clsObserver.observe({ \"entryTypes\": [\'layout-shift\'] })} catch(error) {}; fcpObserver.observe({ \"entryTypes\": [\'paint\'] }); lcpObserver.observe({ \"entryTypes\": [\'largest-contentful-paint\'] }); fidObserver.observe({ \"entryTypes\": [\'first-input\'] }); clsObserver.observe({ \"entryTypes\": [\'layout-shift\'] })} catch(error) {}; :src/hooks/usePerformance.tsx; if(navigationEntry) {}})); \"duration\": \'entry.duration\',; \"startTime\": \'entry.startTime\',; \"name\": \'entry.name\'})})}); try {}; longTaskObserver.observe({ \"entryTypes\": [\'longtask\'] })} catch(error) {}; return : unknown longTaskObserver.disconnect(); if(logToConsole) {}; longTaskObserver.observe({ \"entryTypes\": [\'longtask\'] })} catch(error) {}; return () => longTaskObserver.disconnect(); if(logToConsole) {}; } } },[enableNavigationTiming,logToConsole]); :src/hooks/usePerformance.tsx; if(slowResources.length > 0 && logToConsole) {}; if(slowResources.length > 0 && logToConsole) {; } return resources},[enableResourceTiming,logToConsole]); :src/hooks/usePerformance.tsx; try {}; setMetrics(prev => ({ ...prev,fcp })); if(logToConsole) \"FCP\": \',fcp)} })}); observerRef.current.observe({ \"entryTypes\": [\'paint\'] })} catch(e) {}; setMetrics(prev => ({ ...prev,fcp })); if(logToConsole) } })}); observerRef.current.observe({ \"entryTypes\": [\'paint\'] })} catch(e) {}; } try {}; setMetrics(prev => ({ ...prev,lcp })); if(logToConsole) }); lcpObserverRef.current.observe({ \"entryTypes\": [\'largest-contentful-paint\'] })} catch(e) {}; if(lastEntry) {; setMetrics(prev => ({ ...prev,lcp })); if(logToConsole) } }); lcpObserverRef.current.observe({ \"entryTypes\": [\'largest-contentful-paint\'] })} catch(e) {}; } try {}; clsValue += (entry as any).value} } setMetrics(prev => ({ ...prev,\"cls\": \'clsValue\' })); if(logToConsole) clsObserverRef.current.observe({ \"entryTypes\": [\'layout-shift\'] })} catch(e) {}},[enableWebVitals,logToConsole]); let \"firstInputTime\": number | null = null; let firstInputDelay: number | null = null; clsValue += (entry as any).value} } setMetrics(prev => ({ ...prev,\"cls\": \'clsValue\' })); if(logToConsole) }); clsObserverRef.current.observe({ \"entryTypes\": [\'layout-shift\'] })} catch(e) {}; } },[enableWebVitals,logToConsole]); let \"firstInputTime\": number | null = null; let firstInputDelay: number | null = null; firstInputTime = performance.now(); firstInputDelay = firstInputTime - (performance.timeOrigin || 0); setMetrics(prev => ({ ...prev,\"fid\": \'firstInputDelay\' })); if(logToConsole) :src/hooks/usePerformance.tsx; document.removeEventListener(\'pointerdown\',firstInputHandler); document.removeEventListener(\'keydown\',firstInputHandler); document.removeEventListener(\'mousedown\',firstInputHandler); document.removeEventListener(\'touchstart\',firstInputHandler)}; document.addEventListener(\'pointerdown\',firstInputHandler); document.addEventListener(\'keydown\',firstInputHandler); document.addEventListener(\'mousedown\',firstInputHandler); document.addEventListener(\'touchstart\',firstInputHandler)},[enableWebVitals,logToConsole]); document.removeEventListener(\'pointerdown\',firstInputHandler); document.removeEventListener(\'keydown\',firstInputHandler); document.removeEventListener(\'mousedown\',firstInputHandler); document.removeEventListener(\'touchstart\',firstInputHandler)}; document.addEventListener(\'pointerdown\',firstInputHandler); document.addEventListener(\'keydown\',firstInputHandler); document.addEventListener(\'mousedown\',firstInputHandler); document.addEventListener(\'touchstart\',firstInputHandler)},[enableWebVitals,logToConsole]); setIsMonitoring(true); monitorWebVitals(); monitorFirstInput(); getNavigationTiming(); :src/hooks/usePerformance.tsx; getResourceTiming()} else {; window.addEventListener(\'load\',() => {; getNavigationTiming(); getResourceTiming()})} if(document.readyState === \'loading\') {}; document.addEventListener(\'DOMContentLoaded\',getNavigationTiming)} else {}; getNavigationTiming()} },[isMonitoring,monitorWebVitals,monitorFirstInput,getNavigationTiming,getResourceTiming]); const stopMonitoring = useCallback(() => {}} if(lcpObserverRef.current) {}} if(clsObserverRef.current) {}; clsObserverRef.current = null} },[]); const getPerformanceScore = useCallback(() => {; let score = 100; if(metrics.fcp !== null) {; if(metrics.fcp < 1800) score -= 0; else if(metrics.fcp < 3000) score -= 10; else score -= 25;} if(metrics.lcp !== null) {; if(metrics.lcp < 2500) score -= 0; else if(metrics.lcp < 4000) score -= 10; else score -= 25;} if(metrics.fid !== null) {; if(metrics.fid < 100) score -= 0; else if(metrics.fid < 300) score -= 10; else score -= 25;} if(metrics.cls !== null) {; if(metrics.cls < 0.1) score -= 0; else if(metrics.cls < 0.25) score -= 10; else score -= 25;} return Math.max(0,score);},[metrics]); :src/hooks/usePerformance.tsx; if(window.gtag) {; window.gtag(\'event\',performance_metrics\',analyticsData)} if(window.gtag) {; window.gtag(\'event\',\'performance_metrics\',analyticsData)} fetch(\'/api/analytics/performance\',{; \"method\": \'POST\',; \"headers\": { \'Content-Type\': \'application/json\' },; \"body\": JSON.stringify(analyticsData);}).catch(console.error)},[metrics,sendToAnalytics,getPerformanceScore]); useEffect(: unknown {; if (enableRealUserMonitoring) {; :src/hooks/usePerformance.tsx; startMonitoring()} return : unknown {; stopMonitoring()}},[enableRealUserMonitoring,startMonitoring,stopMonitoring]); \'; startMonitoring()} return () => {; stopMonitoring()}},[enableRealUserMonitoring,startMonitoring,stopMonitoring]); useEffect(: unknown {}; sendMetricsToAnalytics()} sendMetricsToAnalytics()} },[metrics,sendToAnalytics,sendMetricsToAnalytics]); return {; metrics,; isMonitoring,; \"performanceScore\": getPerformanceScore(),; startMonitoring,; stopMonitoring,; getResourceTiming,; sendMetricsToAnalytics;}; :src/hooks/usePerformance.tsx; return : unknown observer.disconnect()},[eventName,callback]); export function useRenderTime(...\"args\": unknown[]): unknown {}; return () => observer.disconnect()},[eventName,callback]); export function useRenderTime() {}; return null} };},[]);,[]); setRenderTime(time); renderStart.current = renderEnd}); return renderTime} export function useComponentPerformance(\"componentName\": string) {; const [renderTime,setRenderTime] = useState<number>(0); const [mountTime,setMountTime] = useState<number>(0); :src/hooks/usePerformance.tsx; useEffect(: unknown {; startTime.current = performance.now(); setMountTime(startTime.current); return : unknown {; setRenderTime(totalTime); if(totalTime > 16) { }},[componentName]); useEffect(() => {}};},[]);,[]); startTime.current = performance.now(); setMountTime(startTime.current); return () => {; setRenderTime(totalTime); if(totalTime > 16) { }ms`)} }},[componentName]); return { renderTime,mountTime }} export function useAPIPerformance(...\"args\": unknown[]): unknown {; const [apiMetrics,setApiMetrics] = useState<Map<string,number[]>>(new Map()); :src/hooks/usePerformance.tsx; newMap.set(endpoint,[...existing,duration]); return newMap})},[]); :src/hooks/usePerformance.tsx; if(!metrics || metrics.length === 0) return 0; return metrics.reduce(\"sum\": \'unknown\',\"time\": \'unknown sum + time\',0) / metrics.length},[apiMetrics]); if(!metrics || metrics.length === 0) return 0; return metrics.reduce((sum,time) => sum + time,0) / metrics.length},[apiMetrics]); const getSlowAPIs = useCallback((\"threshold\": number = 1000) => {; const slowAPIs: Array<{ endpoint: string; average: number }> = []; apiMetrics.forEach((times,endpoint) => {}; slowAPIs.push({ endpoint,average })} }); return slowAPIs.sort(\"a\": \'unknown\',\"b\": \'unknown b.average - a.average)\'},[apiMetrics]); if(average > threshold) {}; slowAPIs.push({ endpoint,average })} }); return slowAPIs.sort((a,b) => b.average - a.average)},[apiMetrics]); return {; apiMetrics,; trackAPICall,; getAPIAverage,; getSlowAPIs; :src/hooks/usePerformance.tsx;}} \'`;}}
:src/hooks/usePerformance.tsx; interface PerformanceMetrics {}} interface PerformanceOptions {; enableRealUserMonitoring?: boolean; enableWebVitals?: boolean; enableResourceTiming?: boolean; enableNavigationTiming?: boolean; logToConsole?: boolean; :src/hooks/usePerformance.tsx; sendToAnalytics?: boolean} sendToAnalytics?: boolean} export function usePerformance(options: PerformanceOptions = {}) {; const {; enableRealUserMonitoring = true,; enableWebVitals = true,; enableResourceTiming = true,; enableNavigationTiming = true,; logToConsole = false,; sendToAnalytics = false} = options; const [metrics,setMetrics] = useState<PerformanceMetrics>({; fcp: "null",; lcp: "null",; fid: "null",; cls: "null",; ttfb: "null",; domLoad: "null",; windowLoad: "null",; navigationStart: "null"}); const [isMonitoring,setIsMonitoring] = useState(false); return; if(fcpEntry) {}; setMetrics(prev => ({ .prev,fcp: "fcpEntry.startTime" }))}); if(lcpEntry) {}; setMetrics(prev => ({ .prev,lcp: "lcpEntry.startTime" }))}); if(fidEntry && "processingStart" in fidEntry) {}; setMetrics(prev => ({ .prev,fid: "fidEntry.processingStart - fidEntry.startTime" }))}); for(const entry of list.getEntries()) {; return; if(fcpEntry) {; setMetrics(prev => ({ .prev,fcp: "fcpEntry.startTime" }))}); if(lcpEntry) {}; setMetrics(prev => ({ .prev,lcp: "lcpEntry.startTime" }))}); if(fidEntry && "processingStart" in fidEntry) {}; setMetrics(prev => ({ .prev,fid: "fidEntry.processingStart - fidEntry.startTime" }))}); for(const entry of list.getEntries()) {; if(!layoutShiftEntry.hadRecentInput) {; clsValue += layoutShiftEntry.value; setMetrics(prev => ({ .prev,cls: "clsValue" }));}); try {}; fcpObserver.observe({ entryTypes: ["paint"] }); lcpObserver.observe({ entryTypes: ["largest-contentful-paint"] }); fidObserver.observe({ entryTypes: ["first-input"] }); clsObserver.observe({ entryTypes: ["layout-shift"] })} catch(error) {}; fcpObserver.observe({ entryTypes: ["paint"] }); lcpObserver.observe({ entryTypes: ["largest-contentful-paint"] }); fidObserver.observe({ entryTypes: ["first-input"] }); clsObserver.observe({ entryTypes: ["layout-shift"] })} catch(error) {}; :src/hooks/usePerformance.tsx; if(navigationEntry) {}})); duration: "entry.duration",; startTime: "entry.startTime",; name: "entry.name"})})}); try {}; longTaskObserver.observe({ entryTypes: ["longtask"] })} catch(error) {}; return : unknown longTaskObserver.disconnect(); if(logToConsole) {}; longTaskObserver.observe({ entryTypes: ["longtask"] })} catch(error) {}; return () => longTaskObserver.disconnect(); if(logToConsole) {}; console.log("Navigation Timing: ",{ ttfb,domLoad,windowLoad })} } },[enableNavigationTiming,logToConsole]); :src/hooks/usePerformance.tsx; if(slowResources.length > 0 && logToConsole) {}; if(slowResources.length > 0 && logToConsole) {; console.warn("Slow resources detected: ",slowResources)} return resources},[enableResourceTiming,logToConsole]); :src/hooks/usePerformance.tsx; try {}; setMetrics(prev => ({ .prev,fcp })); if(logToConsole) FCP: ",fcp)} })}); observerRef.current.observe({ entryTypes: ["paint"] })} catch(e) {}; setMetrics(prev => ({ .prev,fcp })); if(logToConsole) console.log("FCP: ",fcp)} })}); observerRef.current.observe({ entryTypes: ["paint"] })} catch(e) {}; console.warn("FCP monitoring not supported")} try {}; setMetrics(prev => ({ .prev,lcp })); if(logToConsole) }); lcpObserverRef.current.observe({ entryTypes: ["largest-contentful-paint"] })} catch(e) {}; if(lastEntry) {; setMetrics(prev => ({ .prev,lcp })); if(logToConsole) console.log("LCP: ",lcp)} }); lcpObserverRef.current.observe({ entryTypes: ["largest-contentful-paint"] })} catch(e) {}; console.warn("LCP monitoring not supported")} try {}; clsValue += (entry as any).value} } setMetrics(prev => ({ .prev,cls: "clsValue" })); if(logToConsole) clsObserverRef.current.observe({ entryTypes: ["layout-shift"] })} catch(e) {}},[enableWebVitals,logToConsole]); let firstInputTime: number | null = null; let firstInputDelay: number | null = null; clsValue += (entry as any).value} } setMetrics(prev => ({ .prev,cls: "clsValue" })); if(logToConsole) console.log("CLS: ",clsValue)}); clsObserverRef.current.observe({ entryTypes: ["layout-shift"] })} catch(e) {}; console.warn("CLS monitoring not supported")} },[enableWebVitals,logToConsole]); let firstInputTime: number | null = null; let firstInputDelay: number | null = null; firstInputTime = performance.now(); firstInputDelay = firstInputTime - (performance.timeOrigin | 0); setMetrics(prev => ({ .prev,fid: "firstInputDelay" })); if(logToConsole) :src/hooks/usePerformance.tsx; document.removeEventListener("pointerdown",firstInputHandler); document.removeEventListener("keydown",firstInputHandler); document.removeEventListener("mousedown",firstInputHandler); document.removeEventListener("touchstart",firstInputHandler)}; document.addEventListener("pointerdown",firstInputHandler); document.addEventListener("keydown",firstInputHandler); document.addEventListener("mousedown",firstInputHandler); document.addEventListener("touchstart",firstInputHandler)},[enableWebVitals,logToConsole]); document.removeEventListener("pointerdown",firstInputHandler); document.removeEventListener("keydown",firstInputHandler); document.removeEventListener("mousedown",firstInputHandler); document.removeEventListener("touchstart",firstInputHandler)}; document.addEventListener("pointerdown",firstInputHandler); document.addEventListener("keydown",firstInputHandler); document.addEventListener("mousedown",firstInputHandler); document.addEventListener("touchstart",firstInputHandler)},[enableWebVitals,logToConsole]); setIsMonitoring(true); monitorWebVitals(); monitorFirstInput(); getNavigationTiming(); :src/hooks/usePerformance.tsx; getResourceTiming()} else {; window.addEventListener("load",() => {; getNavigationTiming(); getResourceTiming()})} if(document.readyState === "loading") {}; document.addEventListener("DOMContentLoaded",getNavigationTiming)} else {}; getNavigationTiming()} },[isMonitoring,monitorWebVitals,monitorFirstInput,getNavigationTiming,getResourceTiming]); const stopMonitoring = useCallback(() => {}} if(lcpObserverRef.current) {}} if(clsObserverRef.current) {}; clsObserverRef.current = null} },[]); const getPerformanceScore = useCallback(() => {; let score = 100; if(metrics.fcp !== null) {; if(metrics.fcp < 1800) score -= 0; else if(metrics.fcp < 3000) score -= 10; else score -= 25;} if(metrics.lcp !== null) {; if(metrics.lcp < 2500) score -= 0; else if(metrics.lcp < 4000) score -= 10; else score -= 25;} if(metrics.fid !== null) {; if(metrics.fid < 100) score -= 0; else if(metrics.fid < 300) score -= 10; else score -= 25;} if(metrics.cls !== null) {; if(metrics.cls < 0.1) score -= 0; else if(metrics.cls < 0.25) score -= 10; else score -= 25;} return Math.max(0,score);},[metrics]); :src/hooks/usePerformance.tsx; if(window.gtag) {; window.gtag("event",performance_metrics",analyticsData)} if(window.gtag) {; window.gtag("event","performance_metrics",analyticsData)} fetch("/api/analytics/performance",{; method: "POST",; headers: { "Content-Type": "application/json" },; body: JSON.stringify(analyticsData);}).catch(console.error)},[metrics,sendToAnalytics,getPerformanceScore]); useEffect(: unknown {; if (enableRealUserMonitoring) {; :src/hooks/usePerformance.tsx; startMonitoring()} return : unknown {; stopMonitoring()}},[enableRealUserMonitoring,startMonitoring,stopMonitoring]); "; startMonitoring()} return () => {; stopMonitoring()}},[enableRealUserMonitoring,startMonitoring,stopMonitoring]); useEffect(: unknown {}; sendMetricsToAnalytics()} sendMetricsToAnalytics()} },[metrics,sendToAnalytics,sendMetricsToAnalytics]); return {; metrics,; isMonitoring,; performanceScore: getPerformanceScore(),; startMonitoring,; stopMonitoring,; getResourceTiming,; sendMetricsToAnalytics;}; :src/hooks/usePerformance.tsx; return : unknown observer.disconnect()},[eventName,callback]); export function useRenderTime(.args: unknown[]): unknown {}; return () => observer.disconnect()},[eventName,callback]); export function useRenderTime() { return; }; return null} };},[]);,[]); setRenderTime(time); renderStart.current = renderEnd}); return renderTime} export function useComponentPerformance(componentName: string) {; const [renderTime,setRenderTime] = useState<number>(0); const [mountTime,setMountTime] = useState<number>(0); :src/hooks/usePerformance.tsx; useEffect(: unknown {; startTime.current = performance.now(); setMountTime(startTime.current); return : unknown {; setRenderTime(totalTime); if(totalTime > 16) {}},[componentName]); useEffect(() => {}};},[]);,[]); startTime.current = performance.now(); setMountTime(startTime.current); return () => {; setRenderTime(totalTime); if(totalTime > 16) { console.warn(`Slow component render: ${componentName} took ${totalTime.toFixed(2)}ms`)} }},[componentName]); return { renderTime,mountTime }} export function useAPIPerformance(.args: unknown[]): unknown {; const [apiMetrics,setApiMetrics] = useState<Map<string,number[]>>(new Map()); :src/hooks/usePerformance.tsx; newMap.set(endpoint,[.existing,duration]); return newMap})},[]); :src/hooks/usePerformance.tsx; if(!metrics | metrics.length === 0) return 0; return metrics.reduce(sum: "unknown",time: "unknown sum + time",0) / metrics.length},[apiMetrics]); if(!metrics | metrics.length === 0) return 0; return metrics.reduce((sum,time) => sum + time,0) / metrics.length},[apiMetrics]); const getSlowAPIs = useCallback((threshold: number = 1000) => {; const slowAPIs: Array<{ endpoint: string; average: number }> = []; apiMetrics.forEach((times,endpoint) => {}; slowAPIs.push({ endpoint,average })} }); return slowAPIs.sort(a: "unknown",b: "unknown b.average - a.average)"},[apiMetrics]); if(average > threshold) {}; slowAPIs.push({ endpoint,average })} }); return slowAPIs.sort((a,b) => b.average - a.average)},[apiMetrics]); return {; apiMetrics,; trackAPICall,; getAPIAverage,; getSlowAPIs; :src/hooks/usePerformance.tsx;}} "`;}}'"`'"`
:src/hooks/usePerformance.tsx; interface PerformanceMetrics {}} interface PerformanceOptions {; enableRealUserMonitoring?: boolean; enableWebVitals?: boolean; enableResourceTiming?: boolean; enableNavigationTiming?: boolean; logToConsole?: boolean; :src/hooks/usePerformance.tsx; sendToAnalytics?: boolean} sendToAnalytics?: boolean} export function usePerformance("options": PerformanceOptions = {}) {; const {; enableRealUserMonitoring = true,; enableWebVitals = true,; enableResourceTiming = true,; enableNavigationTiming = true,; logToConsole = false,; sendToAnalytics = false} = options; const [metrics,setMetrics] = useState<PerformanceMetrics>({; "fcp": 'null',; "lcp": 'null',; "fid": 'null',; "cls": 'null',; "ttfb": 'null',; "domLoad": 'null',; "windowLoad": 'null',; "navigationStart": 'null'}); const [isMonitoring,setIsMonitoring] = useState(false); return; if(fcpEntry) {}; setMetrics(prev => ({ ...prev,"fcp": 'fcpEntry.startTime' }))}); if(lcpEntry) {}; setMetrics(prev => ({ ...prev,"lcp": 'lcpEntry.startTime' }))}); if(fidEntry && 'processingStart' in, fidEntry) {}; setMetrics(prev => ({ ...prev,"fid": 'fidEntry.processingStart - fidEntry.startTime' }))}); for(const entry of list.getEntries()) {; return; if(fcpEntry) {; setMetrics(prev => ({ ...prev,"fcp": 'fcpEntry.startTime' }))}); if(lcpEntry) {}; setMetrics(prev => ({ ...prev,"lcp": 'lcpEntry.startTime' }))}); if(fidEntry && 'processingStart' in, fidEntry) {}; setMetrics(prev => ({ ...prev,"fid": 'fidEntry.processingStart - fidEntry.startTime' }))}); for(const entry of list.getEntries()) {; if(!layoutShiftEntry.hadRecentInput) {; clsValue += layoutShiftEntry.value; setMetrics(prev => ({ ...prev,"cls": 'clsValue' }));}); try {}; fcpObserver.observe({ "entryTypes": ['paint'] }); lcpObserver.observe({ "entryTypes": ['largest-contentful-paint'] }); fidObserver.observe({ "entryTypes": ['first-input'] }); clsObserver.observe({ "entryTypes": ['layout-shift'] })} catch(error) {}; fcpObserver.observe({ "entryTypes": ['paint'] }); lcpObserver.observe({ "entryTypes": ['largest-contentful-paint'] }); fidObserver.observe({ "entryTypes": ['first-input'] }); clsObserver.observe({ "entryTypes": ['layout-shift'] })} catch(error) {}; :src/hooks/usePerformance.tsx; if(navigationEntry) {}})); "duration": 'entry.duration',; "startTime": 'entry.startTime',; "name": 'entry.name'})})}); try {}; longTaskObserver.observe({ "entryTypes": ['longtask'] })} catch(error) {}; return : unknown longTaskObserver.disconnect(); if(logToConsole) {}; longTaskObserver.observe({ "entryTypes": ['longtask'] })} catch(error) {}; return () => longTaskObserver.disconnect(); if(logToConsole) {}; _console.log('Navigation "Timing": ',{ ttfb,domLoad,windowLoad })} } },[enableNavigationTiming,logToConsole]); :src/hooks/usePerformance.tsx; if(slowResources.length > 0 && logToConsole) {}; if(slowResources.length > 0 && logToConsole) {; _console.warn('Slow resources "detected": ',slowResources)} return resources},[enableResourceTiming,logToConsole]); :src/hooks/usePerformance.tsx; try {}; setMetrics(prev => ({ ...prev,fcp })); if(logToConsole) "FCP": ',fcp)} })}); observerRef.current.observe({ "entryTypes": ['paint'] })} catch(e) {}; setMetrics(prev => ({ ...prev,fcp })); if(logToConsole) _console.log('"FCP": ',fcp)} })}); observerRef.current.observe({ "entryTypes": ['paint'] })} catch(e) {}; _console.warn('FCP monitoring not supported')} try {}; setMetrics(prev => ({ ...prev,lcp })); if(logToConsole) }); lcpObserverRef.current.observe({ "entryTypes": ['largest-contentful-paint'] })} catch(e) {}; if(lastEntry) {; setMetrics(prev => ({ ...prev,lcp })); if(logToConsole) _console.log('"LCP": ',lcp)} }); lcpObserverRef.current.observe({ "entryTypes": ['largest-contentful-paint'] })} catch(e) {}; _console.warn('LCP monitoring not supported')} try {}; clsValue += (entry as, any).value} } setMetrics(prev => ({ ...prev,"cls": 'clsValue' })); if(logToConsole) clsObserverRef.current.observe({ "entryTypes": ['layout-shift'] })} catch(e) {}},[enableWebVitals,logToConsole]); let "firstInputTime": number | null = null; let firstInputDelay: number | null = null; clsValue += (entry as, any).value} } setMetrics(prev => ({ ...prev,"cls": 'clsValue' })); if(logToConsole) _console.log('"CLS": ',clsValue)}); clsObserverRef.current.observe({ "entryTypes": ['layout-shift'] })} catch(e) {}; _console.warn('CLS monitoring not supported')} },[enableWebVitals,logToConsole]); let "firstInputTime": number | null = null; let firstInputDelay: number | null = null; firstInputTime = performance.now(); firstInputDelay = firstInputTime - (performance.timeOrigin || 0); setMetrics(prev => ({ ...prev,"fid": 'firstInputDelay' })); if(logToConsole) :src/hooks/usePerformance.tsx; document.removeEventListener('pointerdown',firstInputHandler); document.removeEventListener('keydown',firstInputHandler); document.removeEventListener('mousedown',firstInputHandler); document.removeEventListener('touchstart',firstInputHandler)}; document.addEventListener('pointerdown',firstInputHandler); document.addEventListener('keydown',firstInputHandler); document.addEventListener('mousedown',firstInputHandler); document.addEventListener('touchstart',firstInputHandler)},[enableWebVitals,logToConsole]); document.removeEventListener('pointerdown',firstInputHandler); document.removeEventListener('keydown',firstInputHandler); document.removeEventListener('mousedown',firstInputHandler); document.removeEventListener('touchstart',firstInputHandler)}; document.addEventListener('pointerdown',firstInputHandler); document.addEventListener('keydown',firstInputHandler); document.addEventListener('mousedown',firstInputHandler); document.addEventListener('touchstart',firstInputHandler)},[enableWebVitals,logToConsole]); setIsMonitoring(true); monitorWebVitals(); monitorFirstInput(); getNavigationTiming(); :src/hooks/usePerformance.tsx; getResourceTiming()} else {; window.addEventListener('load',() => {; getNavigationTiming(); getResourceTiming()})} if(document.readyState === 'loading') {}; document.addEventListener('DOMContentLoaded',getNavigationTiming)} else {}; getNavigationTiming()} },[isMonitoring,monitorWebVitals,monitorFirstInput,getNavigationTiming,getResourceTiming]); const stopMonitoring = useCallback(() => {}} if(lcpObserverRef.current) {}} if(clsObserverRef.current) {}; clsObserverRef.current = null} },[]); const getPerformanceScore = useCallback(() => {; let score = 100; if(metrics.fcp !== null) {; if(metrics.fcp < 1800) score -= 0; else if(metrics.fcp < 3000) score -= 10; else score -= 25;} if(metrics.lcp !== null) {; if(metrics.lcp < 2500) score -= 0; else if(metrics.lcp < 4000) score -= 10; else score -= 25;} if(metrics.fid !== null) {; if(metrics.fid < 100) score -= 0; else if(metrics.fid < 300) score -= 10; else score -= 25;} if(metrics.cls !== null) {; if(metrics.cls < 0.1) score -= 0; else if(metrics.cls < 0.25) score -= 10; else score -= 25;} return Math.max(0,score);},[metrics]); :src/hooks/usePerformance.tsx; if(window.gtag) {; window.gtag('event',performance_metrics',analyticsData)} if(window.gtag) {; window.gtag('event','performance_metrics',analyticsData)} fetch('/api/analytics/performance',{; "method": 'POST',; "headers": { 'Content-Type': 'application/json' },; "body": JSON.stringify(analyticsData);}).catch(_console.error)},[metrics,sendToAnalytics,getPerformanceScore]); useEffect(: unknown {; if (enableRealUserMonitoring) {; :src/hooks/usePerformance.tsx; startMonitoring()} return : unknown {; stopMonitoring()}},[enableRealUserMonitoring,startMonitoring,stopMonitoring]); '; startMonitoring()} return () => {; stopMonitoring()}},[enableRealUserMonitoring,startMonitoring,stopMonitoring]); useEffect(: unknown {}; sendMetricsToAnalytics()} sendMetricsToAnalytics()} },[metrics,sendToAnalytics,sendMetricsToAnalytics]); return {; metrics,; isMonitoring,; "performanceScore": getPerformanceScore(),; startMonitoring,; stopMonitoring,; getResourceTiming,; sendMetricsToAnalytics;}; :src/hooks/usePerformance.tsx; return : unknown observer.disconnect()},[eventName,callback]); export function useRenderTime(..."args": unknown[]): unknown {}; return () => observer.disconnect()},[eventName,callback]); export function useRenderTime() {}; return null} };},[]);,[]); setRenderTime(time); renderStart.current = renderEnd}); return renderTime} export function useComponentPerformance("componentName": string) {; const [renderTime,setRenderTime] = useState<number>(0); const [mountTime,setMountTime] = useState<number>(0); :src/hooks/usePerformance.tsx; useEffect(: unknown {; startTime.current = performance.now(); setMountTime(startTime.current); return : unknown {; setRenderTime(totalTime); if(totalTime > 16) { }},[componentName]); useEffect(() => {}};},[]);,[]); startTime.current = performance.now(); setMountTime(startTime.current); return () => {; setRenderTime(totalTime); if(totalTime > 16) { _console.warn(`Slow component "render": ${componentName} took ${totalTime.toFixed(2)}ms`)} }},[componentName]); return { renderTime,mountTime }} export function useAPIPerformance(..."args": unknown[]): unknown {; const [apiMetrics,setApiMetrics] = useState<Map<string,number[]>>(new Map()); :src/hooks/usePerformance.tsx; newMap.set(endpoint,[...existing,duration]); return newMap})},[]); :src/hooks/usePerformance.tsx; if(!metrics || metrics.length === 0) return 0; return metrics.reduce("sum": 'unknown',"time": 'unknown sum + time',0) / metrics.length},[apiMetrics]); if(!metrics || metrics.length === 0) return 0; return metrics.reduce((sum,time) => sum + time,0) / metrics.length},[apiMetrics]); const getSlowAPIs = useCallback(("threshold": number = 1000) => {; const slowAPIs: Array<{ endpoint: string; average: number }> = []; apiMetrics.forEach((times,endpoint) => {}; slowAPIs.push({ endpoint,average })} }); return slowAPIs.sort("a": 'unknown',"b": 'unknown b.average - a.average)'},[apiMetrics]); if(average > threshold) {}; slowAPIs.push({ endpoint,average })} }); return slowAPIs.sort((a,b) => b.average - a.average)},[apiMetrics]); return {; apiMetrics,; trackAPICall,; getAPIAverage,; getSlowAPIs; :src/hooks/usePerformance.tsx;}} '`;}}
