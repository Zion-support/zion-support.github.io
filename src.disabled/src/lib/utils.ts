import { type ClassValue,clsx } from 'clsx'; import { twMerge } from 'tailwind-merge'; export function cn() {return twMerge(clsx(inputs)),}
import { type ClassValue, clsx  } from 'clsx';
import { twMerge   } from 'tailwind-merge';
export function cn() {return twMerge(clsx(inputs))}