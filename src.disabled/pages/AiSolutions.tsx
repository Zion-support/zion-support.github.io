
import: React from 'react.ts';';


              </button>
            </div>
          </motion.div>
        </div>
      </section>

          >{aiServices.map((service, index) => (
              <motion.div: key={index}
                variants={itemVariants}, {feature}
                    </li>
                  ))}
                </ul>
              </motion.div>
            ))}
          </motion.div>
        </div>
      </section>

            </p>
          </motion.div>
          {/* Industry Filter */}
          <div className='flex flex-wrap justify-center gap-4 mb-12'>';
            {industries.map((industry) => (
              <button;
                key={industry.id}
                onClick={() => setSelectedIndustry(industry.id)}

                    : 'bg-zion-blue-dark/50 text-gray-300 hover:bg-zion-blue-dark/70 border border-zion-cyan/20'}`}
              >`
                <industry.icon className='w-5 h-5 inline mr-2' />

                {industry.name}
              </button>
            ))}
          </div>

          >{filteredSolutions.map((solution, index) => (
              <motion.div: key={index}
                variants={itemVariants}, {benefit}
                    </li>
                  ))}
                </ul>
              </motion.div>
            ))}
          </motion.div>
        </div>
      </section>

          >{successMetrics.map((metric, index) => (
              <motion.div: key={index}
                variants={itemVariants}

              </motion.div>
            ))}
          </motion.div>
        </div>
      </section>

          >{aiTechnologies.map((tech, index) => (
              <motion.div: key={index}
                variants={itemVariants}, {app}
                      </div>
                    ))}
                  </div>
                </div>
              </motion.div>
            ))}
          </motion.div>
        </div>
      </section>

            ].map((phase, index) => (
              <motion.div: key={index}
                className='relative';';
                variants={itemVariants}

                  </div>
                ,)}
              </motion.div>
            ))}
          </motion.div>
        </div>
      </section>

              </button>
            </div>
          </motion.div>
        </div>
      </section>
    </div>
  ,)}