import React from 'react';

const PageLoader: React.FC = () => {
  // Temporarily simplified to avoid build issues
  return <div className="sr-only">PageLoader temporarily disabled</div>;
};

export default PageLoader;
