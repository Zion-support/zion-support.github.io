// import React { useEffect } from \"react\";
interface ApiResponse<T = unknown> {/* TODO: Fix JSX expression */}";"'"
  l: \'string\'; nam; e: \'string\'} rol}\"admin\" | \" | \"guest\"'"
  l: \'string\'; nam; e: \'string\'} rol}\"admin\" | \" | \"guest\"'"
  n: \'string\'; pric} e: \'number\'}} categor,y: \'string\'} ; interface FormData {; } [ke,'
  y: \'string]: string | number | boolean | File\'} ; interface ComponentProps {; className?: string} children?: React.ReactNode [ke,'
  y: \'string]: unknown\'} ; const,'
  PerformanceMonitor: React.F,
  C: = () => {, useEffect(() => {} i,"
  f: (typeof window = == \"undefined\"
  metrics: PerformanceMetric,
  s: = {} ; const fcpObserver = new PerformanceObserver((list) => {/* TODO: Fix JSX expression */}""}}}\"} })\")"'"paint\"]})\""
  p: \'= lastEntry.startTime\',\" lcpObserver.observe({/* TODO: Fix JSX expression */}")"
  s: [\"largest-contentful-paint\" const fidObserver = new PerformanceObserver((list) => {; metrics.fid = (entry as any).processingStart - entry.startTime,\";}}}\" fidObserver.observe({/* TODO: Fix JSX expression */}")"first-input\"]})\""
  t: \'clsValue = 0\'; const clsObserver = new PerformanceObserver((list) => {\"; if (entry.name === \"first-contentful-paint\"} metrics.fc,"'"}} }\"} ;})\")"'"paint\"]})\""
  p: \'= lastEntry.startTime\',\" lcpObserver.observe({/* TODO: Fix JSX expression */}")"
  s: [\"largest-contentful-paint\" const fidObserver = new PerformanceObserver((list) => {; metrics.fid = (entry as any).processingStart - entry.startTime,\"}}}\" fidObserver.observe({/* TODO: Fix JSX expression */}")"first-input\"]})\""
  t: \'clsValue = 0\'; const clsObserver = new PerformanceObserver((list) => {\"'"
  PerformanceMonitor: React.FC = () => {} if (typeof window === \") return}; const,"
  metrics: PerformanceMetrics = {} ; const metric,s: PerformanceMetrics = {} ; const,"
  metrics: PerformanceMetrics = {} ; for (const entry of list.getEntries()) {; if (entry.name = == \"first-contentful-paint\")"
  s: [\"] });} const fcpObserver = new PerformanceObserver();} const lcpObserver = new PerformanceObserver(list => {) metrics.lcp = lastEntry.startTime}}});} lcpObserver.observe({/* TODO: Fix JSX expression */}")"largest-contentful-paint\"] });} metrics.fcp = entry.startTime,\" lcpObserver.observe();} metrics.fid = (entry as unknown).processingStart - entry.startTime;} ; const fidObserver = new PerformanceObserver(list => {\"} fidObserver.observe({/* TODO: Fix JSX expression */}"
  s: [\"first-input\" clsObserver.observe({/* TODO: Fix JSX expression */}")"layout-shift\"]})\"navigation\")[0] as PerformanceNavigationTiming; i,"
  f: (navigationEntry) {} metrics.ttfb = navigationEntry.responseStart - navigationEntry.requestStart,\"}}\"; if (entry.name === \"first-meaningful-paint\"} metrics.fm,""}}\";});} clsObserver.observe({/* TODO: Fix JSX expression */}"'"
  s: [\"] });} const clsObserver = new PerformanceObserver();} const navigationEntry = performance.getEntriesByType(\" \" )[0] as PerformanceNavigationTiming; if (navigationEntry) {; metrics.ttfb =; navigationEntry.responseStart - navigationEntry.requestStart} metrics.fid = (entry as any).processingStart - entry.startTime}\"; if (!(entry as unknown).hadRecentInput) {; clsValue += (entry as unknown).value}\"first-meaningful-paint\") {; metrics.fmp = entry.startTime}}}\")"
  s: [\"] })})\" fmpObserver.observe({/* TODO: Fix JSX expression */}"
  s: [\"paint\" const sendMetrics = () => {/* TODO: Fix JSX expression */}""} metric_,""fcp,\",metric_,"
  2: \"lcp,\" metric_,""fid,\",metric_,"
  4: \"cls,\" metric_,""ttfb}\"},valu,""
  n: \'tru\',e})}\"'"
  f: (process.env['NODE_ENV'] = == \") {/* TODO: Fix JSX expression */}"'"} \"Content-Type\"application/json}\"},\"
  y: JSON.stringify({} ur)
  l: \'window.location.hre\',f)timestam,""
  p: Date.now()}metrics})}).catch(console.error)} ; window.addEventListener(\"beforeunload\""
  n: () => {\")"
  s: [\"] });} const sendMetrics = () => {/* TODO: Fix JSX expression */}""} metric_,\" 1: \",metric_,""lcp\",\""
  3: \",metric_,""cls\"}\""
  5: \"},valu,"
  e: Math.round(metrics.fcp || 0),non_interactio,"
  n: \'true\'})} ; if (process.env['NODE_ENV'] = == \"production\" \"; fmpObserver.observe()}}} if (typeof window !== \" && (window as any).gtag) {/* TODO: Fix JSX expression */}"'"cls\"}\""
  5: \"}); valu,"
  e: Math.round(metrics.fcp || 0); non_interactio,
  n: \'true\'});} metric_,"'"
  5: \"},\" non_interactio,"
  n: \'true\'}) (window as any).gtag(\"event,web_vitals\"'"
  4: \"}metric_,")"ttfb\"})valu,""
  n: \'true\'; ; if (process.env['NODE_ENV'] = == \") {/* TODO: Fix JSX expression */}"'"beforeunload\")sendMetrics)}\"
  n: \'null,";";
export: default PerformanceMonitor\',\" window.removeEventListener(\")sendMetrics)}},[]);} return null export default PerformanceMonitor;\" return null; export default PerformanceMonitor;; metho,"
  d: \"POST\""
  s: {\"Content-Type\": \"},bod,"
  y: JSON.stringify({) ur} l: \'window.location.href\')timestam,"'"
  p: Date.now()}metrics})}).catch(console.error)} ; return null export default PerformanceMonitor\\"; window.removeEventListener(\\"beforeunload\\" return null; \\"' const React { useEffect } from " interface PerformanceMetrics {; fcp?: number; lcp?: number; fid?: number; cls?: number} ttfb?: number} fmp?: number} ; "; interface ApiResponse<T = unknown> {/* TODO: Fix JSX expression */}"
  a: "T"number"}message?: string} ; interface User {/* TODO: Fix JSX expression */}"
  l: "string"string"} rol}"admin" | " | "guest""
  l: "; nam; e: "string"} e: "admin"user" | "} ; interface Service {/* TODO: Fix JSX expression */}""string"; pric} e: "}} categor,y: "string""
  y: "} ; interface ComponentProps {; className?: string} children?: React.ReactNode [ke,""string]: unknown"} ; const,""
  f: (typeof window = == ") return}const,"
  metrics: PerformanceMetric,
  s: = {} ; const fcpObserver = new PerformanceObserver((list) => {/* TODO: Fix JSX expression */}"
  p: "= entry.startTime"}}}"} })")"
  s: ["]})" const lcpObserver = new PerformanceObserver((list) => {/* TODO: Fix JSX expression */}"
  p: "= lastEntry.startTime"}}})"; lcpObserver.observe({/* TODO: Fix JSX expression */}"
  s: ["largest-contentful-paint" const fidObserver = new PerformanceObserver((list) => {; metrics.fid = (entry as any).processingStart - entry.startTime,";}}}" fidObserver.observe({/* TODO: Fix JSX expression */}")"first-input"]})""
  t: "; const clsObserver = new PerformanceObserver((list) => {"interface PerformanceMetrics {; const fcpObserver = new PerformanceObserver((list) => {; for (const entry of list.getEntries()) {"first-contentful-paint") {""
  p: ","}} }" fcpObserver.observe({/* TODO: Fix JSX expression */}")"paint"]})""
  p: ","}})")"
  s: ["]})" const fidObserver = new PerformanceObserver((list) => {; metrics.fid = (entry as any).processingStart - entry.startTime,"})" fidObserver.observe({/* TODO: Fix JSX expression */}"
  s: ["first-input" le,""clsValue = 0"; const clsObserver = new PerformanceObserver((list) => {""
  PerformanceMonitor: React.FC = () => {} if (typeof window === ") return}; const,"
  metrics: PerformanceMetrics = {} ; const metric,s: PerformanceMetrics = {} ; const,"
  metrics: PerformanceMetrics = {} ; for (const entry of list.getEntries()) {; if (entry.name = == "first-contentful-paint")"
  s: ["] });} const fcpObserver = new PerformanceObserver();} const lcpObserver = new PerformanceObserver(list => {) metrics.lcp = lastEntry.startTime}}});} lcpObserver.observe({/* TODO: Fix JSX expression */}")"largest-contentful-paint"] });} metrics.fcp = entry.startTime," lcpObserver.observe();} metrics.fid = (entry as unknown).processingStart - entry.startTime;} ; const fidObserver = new PerformanceObserver(list => {"} fidObserver.observe({/* TODO: Fix JSX expression */}"
  s: ["first-input" clsObserver.observe({/* TODO: Fix JSX expression */}")"layout-shift"]})"navigation")[0] as PerformanceNavigationTiming; i,"
  f: (navigationEntry) {} metrics.ttfb = navigationEntry.responseStart - navigationEntry.requestStart,"}}"; if (entry.name === "first-meaningful-paint"} metrics.fm,""= entry.startTime",";});} clsObserver.observe({/* TODO: Fix JSX expression */}")"layout-shift"] });} const clsObserver = new PerformanceObserver();} const navigationEntry = performance.getEntriesByType("navigation" )[0] as PerformanceNavigationTiming; if (navigationEntry) {; metrics.ttfb =; navigationEntry.responseStart - navigationEntry.requestStart} metrics.fid = (entry as any).processingStart - entry.startTime}"});} } ; if (entry.name = == "first-meaningful-paint" fmpObserver.observe({/* TODO: Fix JSX expression */}")"paint"] })})")"
  s: ["]})" const sendMetrics = () => {/* TODO: Fix JSX expression */}"
  p: {,"} metric_,"
  1: "fcp,""
  2: "," metric_,"
  3: "fid,""
  4: "," metric_,"
  5: "ttfb}"
  e: Math.round(metrics.fc)
  p: | 0),non_interactio,""tru",e})}""
  f: (process.env['NODE_ENV'] = == ") {/* TODO: Fix JSX expression */}"'"} "Content-Type"application/json}"},"
  y: JSON.stringify({} ur,")"window.location.hre",f)timestam,"
  p: Date.now()}metrics})}).catch(console.error)} ; window.addEventListener("beforeunload""
  n: () => {")"
  s: ["] });} const sendMetrics = () => {/* TODO: Fix JSX expression */}""} metric_," 1: ",metric_,""lcp",""
  3: ",metric_,""cls"}""
  5: "},valu,"
  e: Math.round(metrics.fcp | 0),non_interactio,"
  n: "true"production") {; fetch()}}} const fmpObserver = new PerformanceObserver(list => {) metrics.fmp = entry.startTime)"; fmpObserver.observe()}}} if (typeof window !== "undefined"'"
  4: "}" metric_,"
  5: "ttfb"
  e: Math.round(metrics.fcp | 0); non_interactio,""true"});} metric_,"
  5: "ttfb" non_interactio,""true"}) (window as any).gtag(" {/* TODO: Fix JSX expression */}""cls"}metric_,"
  5: "ttfb"
  e: Math.round(metrics.fcp | 0),non_interactio,""true"; ; if (process.env['NODE_ENV'] = == ") {/* TODO: Fix JSX expression */}"'"beforeunload")sendMetrics)}""
  n: ";";
export: default PerformanceMonitor" window.removeEventListener("beforeunload" return null; export default PerformanceMonitor;; metho,""POST",header,"
  s: {"}; ": "application/json""
  y: JSON.stringify({) ur} l: ")timestam,"" fcpObserver.disconnect();} lcpObserver.disconnect();} fidObserver.disconnect();} clsObserver.disconnect();} fmpObserver.disconnect()"; window.removeEventListener(")sendMetrics);} },[]);} " return null; "'"' import _React { useEffect } from " interface PerformanceMetrics {;
fcp?: number; lcp?: number; fid?: number; cls?: number} ttfb?: number} fmp?: number} ; "; interface ApiResponse<T = unknown> {/* TODO: Fix JSX expression */}"
  a: 'T; statu} s: 'number}message?: string} ; interface User {/* TODO: Fix JSX expression */}"'"} e: "admin"user" | "} ; interface Service {/* TODO: Fix JSX expression */}"'"} e: "admin"user" | "} ; interface Service {/* TODO: Fix JSX expression */}"'""
  f: (typeof window = == ") return}const,"
  metrics: PerformanceMetric,
  s: = {} ; const fcpObserver = new PerformanceObserver((list) => {/* TODO: Fix JSX expression */}"
  p: '= entry.startTime,"}}}" fcpObserver.observe({/* TODO: Fix JSX expression */}")"
  s: ["paint" const lcpObserver = new PerformanceObserver((list) => {/* TODO: Fix JSX expression */}"";}}})" lcpObserver.observe({/* TODO: Fix JSX expression */}"'"
  s: ["]})" const fidObserver = new PerformanceObserver((list) => {; metrics.fid = (entry as) any).processingStart - entry.startTime,"})" fidObserver.observe({/* TODO: Fix JSX expression */}"
  s: ["first-input" le,"";interface PerformanceMetrics {; const fcpObserver = new PerformanceObserver((list) => {; for (const entry of list.getEntries()) {"; if (entry.name === ") {"} metrics.fc,"
  p: '= entry.startTime,"}} }" fcpObserver.observe({/* TODO: Fix JSX expression */}")"
  s: ["paint" const lcpObserver = new PerformanceObserver((list) => {/* TODO: Fix JSX expression */}""}}})" lcpObserver.observe({/* TODO: Fix JSX expression */}"'"
  s: ["]})" const fidObserver = new PerformanceObserver((list) => {; metrics.fid = (entry as) any).processingStart - entry.startTime,"})" fidObserver.observe({/* TODO: Fix JSX expression */}"
  s: ["first-input" le,"";interface PerformanceMetrics {; fcp?: number; lcp?: number; fid?: number; cls?: number} ttfb?: number}} fmp?: number} ; const,"'"undefined") return}; const,""
  metrics: PerformanceMetrics = {} ; for (const entry of list.getEntries()) {; if (entry.name = == ") {; metrics.fcp = entry.startTime}}} ;});} fcpObserver.observe({/* TODO: Fix JSX expression */}")"paint"] });} const fcpObserver = new PerformanceObserver();} const lcpObserver = new PerformanceObserver(list => {) metrics.lcp = lastEntry.startTime}}});} lcpObserver.observe({/* TODO: Fix JSX expression */}"
  s: ["largest-contentful-paint"})" lcpObserver.observe();} metrics.fid = (entry as) unknown).processingStart - entry.startTime;} ; const fidObserver = new PerformanceObserver(list => {")"
  s: ["] });} let clsValue = 0; if (!(entry as) any).hadRecentInput) {; clsValue += (entry as} any).value} ; metrics.cls = clsValue;})" clsObserver.observe({/* TODO: Fix JSX expression */}"
  s: ["layout-shift" const navigationEntry = performance.getEntriesByType("navigation""
  f: (navigationEntry) {} metrics.ttfb = navigationEntry.responseStart - navigationEntry.requestStart,"; const fmpObserver = new PerformanceObserver((list) => {"; if (entry.name === ") {"} metrics.fm,"
  p: '= entry.startTime,"}}")"'"layout-shift"] });} const clsObserver = new PerformanceObserver();} const navigationEntry = performance.getEntriesByType("navigation" )[0] as PerformanceNavigationTiming; if (navigationEntry) {; metrics.ttfb =; navigationEntry.responseStart - navigationEntry.requestStart; metrics.fid = (entry as) any).processingStart - entry.startTime,"});} } ; if (entry.name = == "first-meaningful-paint" fmpObserver.observe({/* TODO: Fix JSX expression */}")"paint"] })})")"
  s: ["]})" const sendMetrics = () => {/* TODO: Fix JSX expression */}"
  p: {,"} metric_,"
  1: "fcp,""
  2: "," metric_,"
  3: "fid,""
  4: "," metric_,"
  5: "ttfb}"
  e: Math.round(metrics.fc)
  p: || 0),non_interactio,"" i,"'"production") {/* TODO: Fix JSX expression */}"
  s: {"} ": "application/json}" bod,"
  y: JSON.stringify({} ur)
  l: 'window.location.hre,f)timestam,"'"
  p: Date.now()}metrics})}).catch(_console.error)} ; window.addEventListener(")sendMetrics);} setTimeout(sendMetrics)5000);} retur,""}; fmpObserver.observe({/* TODO: Fix JSX expression */}")"paint"] });} const sendMetrics = () => {/* TODO: Fix JSX expression */}"
  p: {"} metric_,"fcp",metric_,"
  2: "lcp" metric_,""fid",metric_,"
  4: "cls" metric_,""ttfb"},valu,""
  n: 'true'})} ; if (process.env['NODE_ENV'] = == ") {; fetch()}}} const fmpObserver = new PerformanceObserver(list => {) metrics.fmp = entry.startTime)" "undefined" && (window as) any).gtag) {/* TODO: Fix JSX expression */}"
  4: "cls" metric_,""ttfb"}); valu,""'"ttfb"},""
  n: 'true'}) (window as) any).gtag(" {/* TODO: Fix JSX expression */}"'"cls"}metric_,"
  5: "ttfb"
  e: Math.round(metrics.fcp || 0),non_interactio,""production") {/* TODO: Fix JSX expression */}"
  p: Date.now()} metrics}; metrics})}).catch(_console.error)} ;} ; return () => {; fcpObserver.disconnect()}}} lcpObserver.disconnect();} fidObserver.disconnect();} clsObserver.disconnect();} fmpObserver.disconnect();} window.removeEventListener("beforeunload"},[]);} retur,"
  n: 'null,"'";
export: default PerformanceMonitor,"beforeunload")sendMetrics)}},[]);} return null export default PerformanceMonitor;""
  d: ",header,""}; "Content-Type"application/json"},bod,""'" fcpObserver.disconnect();} lcpObserver.disconnect();} fidObserver.disconnect();} clsObserver.disconnect();} fmpObserver.disconnect()"; window.removeEventListener(")sendMetrics);} },[]);} " return null; "