import { render,screen } from \"@testing-library/react\"; render(<AIChatbot />); expect(screen && screen.getByRole(\"main\")).toBeInTheDocument()}); it(\"displays correct content\",() => {; render(<AIChatbot />); it(\"handles user interactions\",() => {; render(<AIChatbot />)});
const { render,screen } from "@testing-library/react"; render(<AIChatbot />); expect(screen && screen.getByRole("main")).toBeInTheDocument()}); it('displays correct content', () => {; render(<AIChatbot />); it('handles user interactions', () => {; render(<AIChatbot />)})"";
import { render,screen } from "@testing-library/react"; render(<AIChatbot />); expect(screen && screen.getByRole("main";
}); it('displays correct content', () => {; render(<AIChatbot />); it('handles user interactions', () => {; render(<AIChatbot />)})"