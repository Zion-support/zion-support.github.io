import React { useState } from 'react'' import Link from 'next/link'' import {Menu} X; ChevronDown } from 'lucide-react' const Navigation: React.FC = () => {const [isOpen, setIsOpen] = useState(false); const [activeDropdown, setActiveDropdown] = useState<string | null>(null)} const navigationItems = [' { name: 'Home'} href: '/' }, {' name: 'Services',' href: '/services', dropdown: [' { name: 'AI Services'} href: '/services/ai-services' }, {name: 'IT Services'} href: '/services/it-services' } ] }, {' name: 'Solutions',' href: '/solutions'} dropdown: []' }, {name: 'About'} href: '/about' }, {name: 'Blog'} href: '/blog' }, {name: 'Careers'} href: '/careers' }, {name: 'Contact'} href: '/contact' } ]; const toggleDropdown = (itemName: string) => { setActiveDropdown(activeDropdown === itemName ? null : itemName)}; return ( <nav className="text-left" > <div className="text-left" > <div className="text-left" > {/* Logo */} <div className="text-left" > <Link href="/" className="text-left" > Zion Tech Group </Link> </div> {/* Desktop Navigation */} <div className="text-left" > <div className="text-left" > {navigationItems.map(item => ( <div key={item.name} className="text-left" > {item.dropdown ? ( <div className="text-left" > <button onClick={() => toggleDropdown(item.name)} className="text-left" > {item.name} <ChevronDown className="text-left" /> </button> {activeDropdown === item.name && ( <div className="text-left" > {item.dropdown.map(dropdownItem => ( <Link key={dropdownItem.name} href={dropdownItem.href} className="text-left" onClick={() => setActiveDropdown(null)} > {dropdownItem.name} </Link> ))} </div> )} </div> ) : ( <Link href={item.href} className="text-left" > {item.name} </Link> )} </div> ))} </div> </div> {/* Mobile menu button */} <div className="text-left" > <button onClick={() => setIsOpen(!isOpen)} className="text-left" > {isOpen ? ( <X className="text-left" /> ) : ( <Menu className="text-left" /> )} </button> </div> </div> </div> {/* Mobile Navigation */}; {isOpen && ( <div className="text-left" > <div className="text-left" > {navigationItems.map(item => ( <div key={item.name}> {item.dropdown ? ( <div> <button onClick={() => toggleDropdown(item.name)} className="text-left" > {item.name} <ChevronDown className="text-left" /> </button> {activeDropdown === item.name && ( <div className="text-left" > {item.dropdown.map(dropdownItem => ( <Link key={dropdownItem.name} href={dropdownItem.href} className="text-left" onClick={() => { setIsOpen(false)} setActiveDropdown(null)}} > {dropdownItem.name} </Link> ))} </div> )} </div> ) : ( <Link href={item.href} className="text-left" onClick={() => setIsOpen(false)} > {item.name} </Link> )} </div> ))} </div> </div> )} </nav> )}; export default Navigation; '