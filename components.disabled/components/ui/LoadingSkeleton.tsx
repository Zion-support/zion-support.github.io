import React from 'react';

const LoadingSkeleton: React.FC = () => {
  // Temporarily simplified to avoid build issues
  return <div className="sr-only">LoadingSkeleton temporarily disabled</div>;
};

export default LoadingSkeleton;
