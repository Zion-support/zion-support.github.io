import React from 'react';'
// import { useEffect } from \'react\' declare global {/* TODO: Fix JSX expression */}';'
  d: string)\"targetId\"PerformanceMonitor\": React.FC = () => {/* TODO: Fix JSX expression */}"
  t: unknown) => { for (const entry of list.getEntries()) { if (entry.entryType === \'largest-contentful-paint\') { if (typeof console !== \'undefined\') {} } } }); try { observer.observe({ \"entryTypes\"list\": unknown) => { for (const entry of list.getEntries()) { if (entry.entryType === \'first-input\') { if (typeof console !== \'undefined\') {} } } }); try { fidObserver.observe({ \": [\'first-input\'] })} catch () {}let clsValue = 0; const clsObserver = new (window as any).PerformanceObserver((\"list\"entryTypes\": [\'layout-shift\'] })} catch () {}return () => {observer.disconnect(); fidObserver.disconnect()} clsObserver.disconnect()}} const fidObserver = new PerformanceObserver((list) => {for (const entry of list.getEntries()) { const fid = (entry as any).processingStart - entry.startTime; metrics.fid = fid} if (process.env['NODE_ENV'] === \'development\') {} } }); try { fidObserver.observe({ \": [\'first-input\'] })} catch () {}let clsValue = 0; const clsObserver = new PerformanceObserver((list) => { for (const entry of list.getEntries()) { if (!(entry as any).hadRecentInput) { clsValue += (entry as any).value} } metrics.cls = clsValue; if (process.env['NODE_ENV'] === \'development\') {} }); try { clsObserver.observe({ \"entryTypes\"entryTypes\": [\'paint\'] })} catch () {}const navigationEntry = performance.getEntriesByType(\'navigation\')[0] as any; if (navigationEntry) {metrics.ttfb = navigationEntry.responseStart - navigationEntry.requestStart} if (process.env['NODE_ENV'] === \'development\') {} } const sendMetrics = () => {if (process.env['NODE_ENV'] === \'production\' && Object.keys(metrics).length > 0) { try { fetch(\'/api/performance-metrics\',{ \": \'POST\'}\"headers\"body\": JSON.stringify({...metrics)\": new Date().toISOString(),\"url\"userAgent\": \'navigator.userAgent\' }) }).catch(() => {})} catch () {}} }; window.addEventListener(\'load\')() => {setTimeout(sendMetrics}2000)}); return () => {lcpObserver.disconnect(); fidObserver.disconnect(); clsObserver.disconnect()} fcpObserver.disconnect()}},[]); return null}; export default PerformanceMonitor;' const { useEffect } from " declare global {/* TODO: Fix JSX expression */}"'"string"}config?: unknown) => void} } } const,""
  P:""
  s: ["] })} catch () {}const fidObserver = new (window as any).PerformanceObserver((lis)"
  t: unknown) => {/* TODO: Fix JSX expression */}"
  D:"}entry.processingStart - entry.startTime)} } } }); try {/* TODO: Fix JSX expression */}"
  s: ["first-input")"
  t: unknown) => {for (const entry of list.getEntries()) { const entryAny = entry as any} if (!entryAny.hadRecentInput) { clsValue += entryAny.value} } if (typeof console !== ") {/* TODO: Fix JSX expression */}""}clsValue)} }); try {/* TODO: Fix JSX expression */}""layout-shift"] })} catch () {}return () => {observer.disconnect(); fidObserver.disconnect()} clsObserver.disconnect()}} const fidObserver = new PerformanceObserver((list) => {for (const entry of list.getEntries()) { const fid = (entry as any).processingStart - entry.startTime; metrics.fid = fid} if (process.env['NODE_ENV'] === ") {/* TODO: Fix JSX expression */}"'"}fid)} } }); try {/* TODO: Fix JSX expression */}""first-input"] })} catch () {}let clsValue = 0; const clsObserver = new PerformanceObserver((list) => { for (const entry of list.getEntries()) { if (!(entry as any).hadRecentInput) { clsValue += (entry as any).value} } metrics.cls = clsValue; if (process.env['NODE_ENV'] === ") {/* TODO: Fix JSX expression */}"'"}clsValue)} }); try {/* TODO: Fix JSX expression */}""layout-shift"] })} catch () {}const fcpObserver = new PerformanceObserver((list) => {for (const entry of list.getEntries()) { if (entry.name === ") { metrics.fcp = entry.startTime} if (process.env['NODE_ENV'] === "development"'"
  P: ""
  s: ["] })} catch () {}const navigationEntry = performance.getEntriesByType("navigation"development") {/* TODO: Fix JSX expression */}"
  B: "}metrics.ttfb)} } const sendMetrics = () => {/* TODO: Fix JSX expression */}"
  d: "POST""
  s: { ": "application/json"
  y: JSON.stringify({/* TODO: Fix JSX expression */}""window.location.href"}userAgen,"
  t: "navigator.userAgent"load")() => {setTimeout(sendMetrics}2000)}); return () => {lcpObserver.disconnect(); fidObserver.disconnect(); clsObserver.disconnect()} fcpObserver.disconnect()}},[]); return null}; export default PerformanceMonitor;'"' import { useEffect } from 'react' declare global {/* TODO: Fix JSX expression */}";'"targetId": 'string'}config?: unknown) => void} } } const ": _React.FC = () => {/* TODO: Fix JSX expression */}"'"entryTypes": ['largest-contentful-paint'] })} catch () {}const fidObserver = new (window as) any).PerformanceObserver((": unknown) => {/* TODO: Fix JSX expression */}"'"entryTypes": ['first-input'] })} catch () {}let clsValue = 0; const clsObserver = new (window as) any).PerformanceObserver((": unknown) => {for (const entry of list.getEntries()) { const entryAny = entry as any} if (!entryAny.hadRecentInput) { clsValue += entryAny.value} } if (typeof _console !== 'undefined') {_console.log('"CLS"entryTypes": ['layout-shift'] })} catch () {}return () => {observer.disconnect(); fidObserver.disconnect()} clsObserver.disconnect()}} const fidObserver = new PerformanceObserver((list) => {for (const entry of list.getEntries()) { const fid = (entry as) any).processingStart - entry.startTime; metrics.fid = fid} if (process.env['NODE_ENV'] === 'development') { _console.log('": '}fid)} } }); try { fidObserver.observe({ "entryTypes"CLS": '}clsValue)} }); try { clsObserver.observe({ ": ['layout-shift'] })} catch () {}const fcpObserver = new PerformanceObserver((list) => {for (const entry of list.getEntries()) { if (entry.name === 'first-contentful-paint') { metrics.fcp = entry.startTime} if (process.env['NODE_ENV'] === 'development') { _console.log('"FCP"entryTypes": ['paint'] })} catch () {}const navigationEntry = performance.getEntriesByType('navigation')[0] as any; if (navigationEntry) {metrics.ttfb = navigationEntry.responseStart - navigationEntry.requestStart} if (process.env['NODE_ENV'] === 'development') { _console.log('": '}metrics.ttfb)} } const sendMetrics = () => {if (process.env['NODE_ENV'] === 'production' && Object.keys(metrics).length > 0) { try { fetch('/api/performance-metrics',{ "method"headers": { 'Content-Type': 'application/json' },": JSON.stringify({...metrics)"timestamp"url": 'window.location.href'}": 'navigator.userAgent' }) }).catch(() => {})} catch () {}} }; window.addEventListener('load')() => {setTimeout(sendMetrics}2000)}); return () => {lcpObserver.disconnect(); fidObserver.disconnect(); clsObserver.disconnect()} fcpObserver.disconnect()}},[]); return null}; export default PerformanceMonitor;''"
// import { useEffect } from \'react\' declare global {/* TODO: Fix JSX expression */}"
  d: string)\"targetId\"PerformanceMonitor\": React.FC = () => {/* TODO: Fix JSX expression */}"
  t: unknown) => { for (const entry of list.getEntries()) { if (entry.entryType === \'largest-contentful-paint\') { if (typeof console !== \'undefined\') {} } } });
try { observer.observe({ \"entryTypes\"list\": unknown) => { for (const entry of list.getEntries()) { if (entry.entryType === \'first-input\') { if (typeof console !== \'undefined\') {} } } }); try { fidObserver.observe({ \": [\'first-input\'] })} catch (e) {} let clsValue = 0; const clsObserver = new (window as any).PerformanceObserver((\"list\"entryTypes\": [\'layout-shift\'] })} catch (e) {} return () => {observer.disconnect(); fidObserver.disconnect()} clsObserver.disconnect()}} const fidObserver = new PerformanceObserver((list) => {for (const entry of list.getEntries()) { const fid = (entry as any).processingStart - entry.startTime; metrics.fid = fid} if (process.env['NODE_ENV'] === \'development\') {} } }); try { fidObserver.observe({ \": [\'first-input\'] })} catch (e) {} let clsValue = 0; const clsObserver = new PerformanceObserver((list) => { for (const entry of list.getEntries()) { if (!(entry as any).hadRecentInput) { clsValue += (entry as any).value} } metrics.cls = clsValue; if (process.env['NODE_ENV'] === \'development\') {} }); try { clsObserver.observe({ \"entryTypes\"entryTypes\": [\'paint\'] })} catch (e) {} const navigationEntry = performance.getEntriesByType(\'navigation\')[0] as any; if (navigationEntry) {metrics.ttfb = navigationEntry.responseStart - navigationEntry.requestStart} if (process.env['NODE_ENV'] === \'development\') {} } const sendMetrics = () => {if (process.env['NODE_ENV'] === \'production\' && Object.keys(metrics).length > 0) { try { fetch(\'/api/performance-metrics\',{ \": \'POST\'}\"headers\"body\": JSON.stringify({...metrics)\": new Date().toISOString(),\"url\"userAgent\": \'navigator.userAgent\' }) }).catch(() => {})} catch (e) {} } }; window.addEventListener(\'load\')() => {setTimeout(sendMetrics}2000)}); return () => {lcpObserver.disconnect(); fidObserver.disconnect(); clsObserver.disconnect()} fcpObserver.disconnect()}},[]); return null}; export default PerformanceMonitor;' const { useEffect } from " declare global {/* TODO: Fix JSX expression */}"'"string"}config?: unknown) => void} } } const,""
  P:""
  s: ["] })} catch (e) {} const fidObserver = new (window as any).PerformanceObserver((lis)"
  t: unknown) => {/* TODO: Fix JSX expression */}"
  D:"}entry.processingStart - entry.startTime)} } } }); try {/* TODO: Fix JSX expression */}"
  s: ["first-input")"
  t: unknown) => {for (const entry of list.getEntries()) { const entryAny = entry as any} if (!entryAny.hadRecentInput) { clsValue += entryAny.value} } if (typeof console !== ") {/* TODO: Fix JSX expression */}""}clsValue)} }); try {/* TODO: Fix JSX expression */}""layout-shift"] })} catch (e) {} return () => {observer.disconnect(); fidObserver.disconnect()} clsObserver.disconnect()}} const fidObserver = new PerformanceObserver((list) => {for (const entry of list.getEntries()) { const fid = (entry as any).processingStart - entry.startTime; metrics.fid = fid} if (process.env['NODE_ENV'] === ") {/* TODO: Fix JSX expression */}"'"}fid)} } }); try {/* TODO: Fix JSX expression */}""first-input"] })} catch (e) {} let clsValue = 0; const clsObserver = new PerformanceObserver((list) => { for (const entry of list.getEntries()) { if (!(entry as any).hadRecentInput) { clsValue += (entry as any).value} } metrics.cls = clsValue; if (process.env['NODE_ENV'] === ") {/* TODO: Fix JSX expression */}"'"}clsValue)} }); try {/* TODO: Fix JSX expression */}""layout-shift"] })} catch (e) {} const fcpObserver = new PerformanceObserver((list) => {for (const entry of list.getEntries()) { if (entry.name === ") { metrics.fcp = entry.startTime} if (process.env['NODE_ENV'] === "development"'"
  P: ""
  s: ["] })} catch (e) {} const navigationEntry = performance.getEntriesByType("navigation"development") {/* TODO: Fix JSX expression */}"
  B: "}metrics.ttfb)} } const sendMetrics = () => {/* TODO: Fix JSX expression */}"
  d: "POST""
  s: { ": "application/json"
  y: JSON.stringify({/* TODO: Fix JSX expression */}""window.location.href"}userAgen,"
  t: "navigator.userAgent"load")() => {setTimeout(sendMetrics}2000)}); return () => {lcpObserver.disconnect(); fidObserver.disconnect(); clsObserver.disconnect()} fcpObserver.disconnect()}},[]); return null}; export default PerformanceMonitor;'"' import { useEffect } from 'react' declare global {/* TODO: Fix JSX expression */}";'"targetId": 'string'}config?: unknown) => void} } } const ": _React.FC = () => {/* TODO: Fix JSX expression */}"'"entryTypes": ['largest-contentful-paint'] })} catch (e) {} const fidObserver = new (window as) any).PerformanceObserver((": unknown) => {/* TODO: Fix JSX expression */}"'"entryTypes": ['first-input'] })} catch (e) {} let clsValue = 0; const clsObserver = new (window as) any).PerformanceObserver((": unknown) => {for (const entry of list.getEntries()) { const entryAny = entry as any} if (!entryAny.hadRecentInput) { clsValue += entryAny.value} } if (typeof _console !== 'undefined') {_// console.log('"CLS"entryTypes": ['layout-shift'] })} catch (e) {} return () => {observer.disconnect(); fidObserver.disconnect()} clsObserver.disconnect()}} const fidObserver = new PerformanceObserver((list) => {for (const entry of list.getEntries()) { const fid = (entry as) any).processingStart - entry.startTime; metrics.fid = fid} if (process.env['NODE_ENV'] === 'development') { _// console.log('": '}fid)} } }); try { fidObserver.observe({ "entryTypes"CLS": '}clsValue)} }); try { clsObserver.observe({ ": ['layout-shift'] })} catch (e) {} const fcpObserver = new PerformanceObserver((list) => {for (const entry of list.getEntries()) { if (entry.name === 'first-contentful-paint') { metrics.fcp = entry.startTime} if (process.env['NODE_ENV'] === 'development') { _// console.log('"FCP"entryTypes": ['paint'] })} catch (e) {} const navigationEntry = performance.getEntriesByType('navigation')[0] as any; if (navigationEntry) {metrics.ttfb = navigationEntry.responseStart - navigationEntry.requestStart} if (process.env['NODE_ENV'] === 'development') { _// console.log('": '}metrics.ttfb)} } const sendMetrics = () => {if (process.env['NODE_ENV'] === 'production' && Object.keys(metrics).length > 0) { try { fetch('/api/performance-metrics',{ "method"headers": { 'Content-Type': 'application/json' },": JSON.stringify({...metrics)"timestamp"url": 'window.location.href'}": 'navigator.userAgent' }) }).catch(() => {})} catch (e) {} } }; window.addEventListener('load')() => {setTimeout(sendMetrics}2000)}); return () => {lcpObserver.disconnect(); fidObserver.disconnect(); clsObserver.disconnect()} fcpObserver.disconnect()}},[]); return null}; export default PerformanceMonitor;''"
"