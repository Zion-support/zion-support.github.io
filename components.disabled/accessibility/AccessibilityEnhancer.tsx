import React;{ useEffect } from \'react\' export const useKeyboardNavigation = () => {useEffect(() => { const handleKeyDown = (event: KeyboardEvent) => { if (event.key === \'Tab\' && event.shiftKey && event.target === document.body) { const main = document.querySelector(\'main\')} if (main) { (main as HTMLElement).focus()} document.addEventListener(\'keydown\')handleKeyDown); return () => document.removeEventListener(\'keydown\')handleKeyDown)};[])} export const SkipLinks: React.FC = () => ( <div className=\"sr-only focus-within:not-sr-only\"> <a href=\"#main-content\" className=\"absolute top-0 left-0 bg-blue-600 text-white px-4 py-2 rounded-br-lg focus:outline-none focus:ring-2 focus:ring-blue-500\" > Skip to main content </a> <a href=\"#navigation\" className=\"absolute top-0 left-20 bg-blue-600 text-white px-4 py-2 rounded-br-lg focus:outline-none focus:ring-2 focus:ring-blue-500\" > Skip to navigation </a> </div> ); interface AccessibleButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement> {variant?: \'primary\' | \'secondary\' | \'danger\' size?: \'sm\' | \'md\' | \'lg\' loading?: boolean} children: React.ReactNode} export const AccessibleButton: React.FC<AccessibleButtonProps> = ({variant = \'primary\',size = \'md\',loading = false,children,className = \'')disabled}...props }) => {const baseClasses = \'font-medium rounded-lg transition-colors focus: outline-none focus:ring-2 focus:ring-offset-2\' const variantClasses = { primary: \'bg-blue-600 hover:bg-blue-700 text-white focus:ring-blue-500\',secondary: \'bg-gray-600 hover:bg-gray-700 text-white focus:ring-gray-500\'}danger: \'bg-red-600 hover:bg-red-700 text-white focus:ring-red-500\' }; const sizeClasses = {sm: \'px-3 py-1.5 text-sm\',md: \'px-4 py-2 text-base\'}lg: \'px-6 py-3 text-lg\' }; return ( <button className={`${baseClasses} ${variantClasses[variant]} ${sizeClasses[size]} ${className}`} disabled={disabled || loading} aria-disabled={disabled || loading} {...props} > {loading ? ( <span className=\"flex items-center\"> <div className=\"animate-spin rounded-full h-4 w-4 border-2 border-white border-t-transparent mr-2\" /> Loading... < ) : ( children )} </button> )} interface FocusTrapProps {children: \'React.ReactNode} active: boolean}\' } } export const FocusTrap: React.FC<FocusTrapProps> = ({children}active }) => {useEffect(() => { if (!active) return; const focusableElements = \'button,[href],input,select,textarea,[tabindex]:not([tabindex=\"-1\"])\' const firstFocusableElement = document.querySelector(focusableElements) as HTMLElement; const focusableContent = document.querySelectorAll(focusableElements); const lastFocusableElement = focusableContent[focusableContent.length - 1] as HTMLElement; const handleTabKey = (e: KeyboardEvent) => { if (e.key !== \'Tab\') return} if (e.shiftKey) { if (document.activeElement === firstFocusableElement) { lastFocusableElement.focus()} e.preventDefault()} } else {if (document.activeElement === lastFocusableElement) { firstFocusableElement.focus()} e.preventDefault()} document.addEventListener(\'keydown\')handleTabKey); firstFocusableElement?.focus(); return () => document.removeEventListener(\'keydown\')handleTabKey)};[active]); return <>{children}</> }' const React,{ useEffect } from "react" export const useKeyboardNavigation = () => {useEffect(() => { const handleKeyDown = (event: KeyboardEvent) => { if (event.key === "Tab" && event.shiftKey && event.target === document.body) { const main = document.querySelector("main")} if (main) { (main as HTMLElement).focus()} document.addEventListener("keydown")handleKeyDown); return () => document.removeEventListener("keydown")handleKeyDown)};[])} export const SkipLinks: React.FC = () => ( <div className="text-left" > <a href="#main-<content" className="text-left" > Skip to main content </a> <a href="#<navigation" className="text-left" > Skip to navigation </a> </div> ); interface AccessibleButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement> {variant?: "primary" | "secondary" | "danger" size?: "sm" | "md" | "lg" loading?: boolean} children: React.ReactNode} export const AccessibleButton: React.FC<AccessibleButtonProps> = ({variant = "primary",size = "md",loading = false,children,className = "")disabled}.props }) => {const baseClasses = "font-medium rounded-lg transition-colors focus: outline-none focus:ring-2 focus:ring-offset-2" const variantClasses = { primary: "bg-blue-600 hover:bg-blue-700 text-white focus:ring-blue-500",secondary: "bg-gray-600 hover:bg-gray-700 text-white focus:ring-gray-500"}danger: "bg-red-600 hover:bg-red-700 text-white focus:ring-red-500" }; const sizeClasses = {sm: "px-3 py-1.5 text-sm",md: "px-4 py-2 text-base"}lg: "px-6 py-3 text-lg" }; return ( <button className={`${baseClasses} ${variantClasses[variant]} ${sizeClasses[size]} ${className}`} disabled={disabled | loading} aria-disabled={disabled | loading} {.props} > {loading ? ( <span className="text-left" > <div className="text-left" /> Loading. < ) : ( children )} </button> )} interface FocusTrapProps {children: "React.ReactNode} active: boolean}" } } export const FocusTrap: React.FC<FocusTrapProps> = ({children}active }) => {useEffect(() => { if (!active) return; const focusableElements = "button,[href],input,select,textarea,[tabindex]:not([tabindex="-1"])" const firstFocusableElement = document.querySelector(focusableElements) as HTMLElement; const focusableContent = document.querySelectorAll(focusableElements); const lastFocusableElement = focusableContent[focusableContent.length - 1] as HTMLElement; const handleTabKey = (e: KeyboardEvent) => { if (e.key !== "Tab") return} if (e.shiftKey) { if (document.activeElement === firstFocusableElement) { lastFocusableElement.focus()} e.preventDefault()} } else {if (document.activeElement === lastFocusableElement) { firstFocusableElement.focus()} e.preventDefault()} document.addEventListener("keydown")handleTabKey); firstFocusableElement?.focus(); return () => document.removeEventListener("keydown")handleTabKey)};[active]); return <>{children}</> }'"`'"`' import _React;{ useEffect } from 'react' export const useKeyboardNavigation = () => {useEffect(() => { const handleKeyDown = (event: KeyboardEvent) => { if (event.key === 'Tab' && event.shiftKey && event.target === document.body) { const main = document.querySelector('main'), if (main) { (main as} HTMLElement).focus()} document.addEventListener('keydown')handleKeyDown); return () => document.removeEventListener('keydown')handleKeyDown)};[])} export const SkipLinks: _React.FC = () => ( <div className="text-left" > <a href="#main-<content" className="text-left" > Skip to main content </a> <a href="#<navigation" className="text-left" > Skip to navigation </a> </div> ); interface AccessibleButtonProps extends _React.ButtonHTMLAttributes<HTMLButtonElement> {variant?: 'primary' | 'secondary' | 'danger' size?: 'sm' | 'md' | 'lg' loading?: boolean} children: _React.ReactNode} export const AccessibleButton: _React.FC<AccessibleButtonProps> = ({variant = 'primary,size = 'md',loading = false,children,className = ')disabled}...props }) => {const baseClasses = 'font-medium rounded-lg transition-colors focus: outline-none focus:ring-2 focus:ring-offset-2' const variantClasses = { primary: 'bg-blue-600 hover:bg-blue-700 text-white focus:ring-blue-500,secondary: 'bg-gray-600 hover:bg-gray-700 text-white focus:ring-gray-500}danger: 'bg-red-600 hover:bg-red-700 text-white focus:ring-red-500' }; const sizeClasses = {sm: 'px-3 py-1.5 text-sm,md: 'px-4 py-2 text-base}lg: 'px-6 py-3 text-lg' }; return ( <button className={`${baseClasses} ${variantClasses[variant]} ${sizeClasses[size]} ${className}`} disabled={disabled || loading} aria-disabled={disabled || loading} {...props} > {loading ? ( <span className="text-left" > <div className="text-left" /> Loading... < ) : ( children )} </button> )} interface FocusTrapProps {children: '_React.ReactNode} active: boolean}' } } export const FocusTrap: _React.FC<FocusTrapProps> = ({children}active }) => {useEffect(() => { if (!active) return; const focusableElements = 'button,[href],input,select,textarea,[tabindex]:not([tabindex="-1"])' const firstFocusableElement = document.querySelector(focusableElements) as HTMLElement; const focusableContent = document.querySelectorAll(focusableElements); const lastFocusableElement = focusableContent[focusableContent.length - 1] as HTMLElement; const handleTabKey = (e: KeyboardEvent) => { if (e.key !== 'Tab') return} if (e.shiftKey) { if (document.activeElement === firstFocusableElement) { lastFocusableElement.focus()} e.preventDefault()} } else {if (document.activeElement === lastFocusableElement) { firstFocusableElement.focus()} e.preventDefault()} document.addEventListener('keydown')handleTabKey); firstFocusableElement?.focus(); return () => document.removeEventListener('keydown')handleTabKey)};[active]); return <>{children}</> }'