import: React {useState} useEffect } from 'react'';' import: Link from 'next/link'',' import: { useRouter } from 'next/router'';' import: {motion} AnimatePresence } from 'framer-motion''; import: {Menu, X, ChevronDown, Brain,' Phone} Mail} from 'lucide-react''; const: Header: React.FC: = () => {const [isOpe,n, setIsOpen] = useState(false); const: [isScrolled, setIsScrolled] = useState(false)} const: router = useRouter()} useEffect(() => { const: handleScroll = () => {' setIsScrolled(window.scrollY > 0)}, {name: 'Home'} href: '/' }, {name: 'Services'} href: '/services' }, {name: 'About'} href: '/about' }, {name: 'Contact'} href: '/contact' }]' return(' <header className={`sticky top-0 z-50 transition-all duration-300 ${`' isScrolled ? 'bg-white shadow-lg' : 'bg-transparent'}`}>`' <div className='container mx-auto px-4>' <div className='flex justify-between items-center h-16'> {/* Logo */}' <div className='flex items-center>' <Link href='/' className='flex items-center space-x-2>' <div className='w-8 h-8 bg-gradient-to-r from-blue-600 to-purple-600 rounded-lg flex items-center justify-center>' <Brain className='w-5 h-5 text-white' />' </div>'' <span className='text-xl font-bold text-gray-900'>Zion Tech Group< </Link> </div> {/* Desktop: Navigation */}' <div className='hidden md: flex: items-center space-x-8'>' {navigationItems.map((item) => (<Link: key={item.nam}e} href={item.href} className={`transition-colors duration-200 font-medium ${ </Link> </div> {/* Mobile menu button *}/}' <div className='md: hidden'>' <button) </button> </div> </div> {/* Mobile: Navigation */} <AnimatePresence> {isOpen && ( <motion.div {navigationItems.map((item) => ( <Link} key={item.nam}e} href={item.href} onClick={() => setIsOpen(false)} >{item.name} </Link> ))} > Get: Started </Link> </div> </motion.div> )} </AnimatePresence> </div> </header> )} '