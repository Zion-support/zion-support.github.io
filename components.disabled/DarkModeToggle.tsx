import React from 'react';

const DarkModeToggle: React.FC = () => {
  // Temporarily simplified to avoid build issues
  return <div className="sr-only">DarkModeToggle temporarily disabled</div>;
};

export default DarkModeToggle;
