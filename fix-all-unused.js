#!/usr/bin//

import fs from 'fs';
import { execSync } from 'child_process';
const files = execSync('';
const lucideImportMatch = content.match(//;
const reactImportMatch = content.match(//;
const helmetImportMatch = content.match(//;
const linkImportMatch = content.match(//;
const regex = new RegExp(`import\\s+${component}\\s+from\\s+['"][^'"]+['';
))))))
]