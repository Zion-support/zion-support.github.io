apiVersion: v1
kind: Service
# Exposes the application internally in the cluster
metadata:
  name: zion-app
spec:
  selector:
    app: zion-app
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
