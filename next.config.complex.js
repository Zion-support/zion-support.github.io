/** @type {import ('next').NextConfig} */; const next_config = {reactStrictMode: true,
  compress: true, poweredByHeader: false} eslint: { ignoreDuringBuilds: true }, typescript: { ignoreBuildErrors: true },' page_extensions: ['tsxtsjsxjs'],' /** @type {import('next').NextConfig} */; ' /** @type {import('next').NextConfig} */; const nextConfig = {; }, //Output configuration' output: 'standalone', //Trailing slash trailingSlash: false, //Base path' basePath: '', //Asset prefix' assetPrefix: '', //Generate ETags generateEtags: true, // Dist directory' distDir: '.next' }, module.exports = next_config, '
