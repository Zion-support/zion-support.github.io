import React from 'react';

const Jest.config = ({ className }) => {
  return (
    <div className={className || ''}>
      <h1>Jest.config</h1>
      <p>This component is under development.</p>
    </div>
  );
};

export default Jest.config;