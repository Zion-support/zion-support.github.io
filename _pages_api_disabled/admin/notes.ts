import { randomUUID } from 'crypto'
type Note = {id: string;
  targetType: string;
  targetId: string;
  text: string;
  authorId: string}
  createdAt: number}
};
import { randomUUID } from 'crypto' type Note = { id: string targetType: string targetId: string text: string authorId: string createdAt: number } '