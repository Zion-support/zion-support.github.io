  r: 'Invalid CSP report format' })} // Log the CSP violation (in production) you might want to send to a monitoring service)' // console.warn('CSP,
  r: cspData['line-number']}' columnNumbe,
  r: cspData['column-number']; timestam)
  p: new Date().toISOString() }); // Here you could send the violation,
  r: 'Internal server error' })} } '