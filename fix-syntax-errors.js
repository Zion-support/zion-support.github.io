import fs from 'fs''
import path from 'path''
import { fileURLToPath } from 'url''
    let content = fs.readFileSync(filePath, 'utf8'';
    if (!content.includes(';') || !content.includes('</''
      // Remove trailing semicolons that shouldn''
      .replace(/;\s*$/gm, '''
      .replace(/<\/([^>]+)>;\s*$/gm, '</$1>''
      .replace(/<([^>]+)>;\s*$/gm, '<$1>''
      .replace(/(\w+)="([^"]*)"\s*;\s*$/gm, ''