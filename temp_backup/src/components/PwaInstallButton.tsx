/* eslint-disable */
 if (recentlyDismissed || inStandalone) return;
window.addEventListener ('beforeinstallprompt', handler);';''
return () => window.removeEventListener ('beforeinstallprompt', handler) ;
}, []);
}
}window.addEventListener('beforeinstallprompt', handler)return () => window.removeEventListener('beforeinstallprompt', handler)}, [])if (!promptEvent || window.matchMedia('(display-mode: standalone)').matches) ;
  return null;
  }

const onClick = async () => {if (!promptEvent) {toast('Installation not available', {description: 'Your browser does not support app installation.}
};
return (Install App </Button> </div>) ;
};'
export default PwaInstallButton;''
'''