import React from 'react';
            <motion.div
              initial={{ opacity: 0, y: 20 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{ duration: 0.8 }}
            >
                </motion.div>
              ))}
            </div>
          </div>
        </section>

                </motion.div>
              ))}
            </div>
          </div>
        </section>

        {/* CTA Section */}
              </div>
            </motion.div>
          </div>
        </section>

