import type { NextPage } from 'next';
import Head from 'next/head';

const TalentIndexPage: NextPage = () => (
  <div>
    <Head><title>Talent</title></Head>
    <h1 className="text-2xl font-semibold">Talent</h1>
    <p className="text-gray-600 dark:text-gray-400 mt-2">Discover talent. (placeholder)</p>
  </div>
);

export default TalentIndexPage;
