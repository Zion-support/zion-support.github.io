import React from 'react';

const LoadingState: React.FC = () => {
  // Temporarily simplified to avoid build issues
  return <div className="sr-only">LoadingState temporarily disabled</div>;
};

export default LoadingState;
