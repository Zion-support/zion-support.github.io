import React from 'react';

const LiveChatWidget: React.FC = () => {
  // Temporarily simplified to avoid build issues
  return <div className="sr-only">LiveChatWidget temporarily disabled</div>;
};

export default LiveChatWidget;
