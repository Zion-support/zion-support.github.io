import React from 'react';

const ServiceAds: React.FC = () => {
  // Temporarily simplified to avoid build issues
  return <div className="sr-only">ServiceAds temporarily disabled</div>;
};

export default ServiceAds;
