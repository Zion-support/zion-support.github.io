import React from 'react';

const ServiceLanding: React.FC = () => {
  // Temporarily simplified to avoid build issues
  return <div className="sr-only">ServiceLanding temporarily disabled</div>;
};

export default ServiceLanding;
