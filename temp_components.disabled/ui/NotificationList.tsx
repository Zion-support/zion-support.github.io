import React from 'react';

const NotificationList: React.FC = () => {
  // Temporarily simplified to avoid build issues
  return <div className="sr-only">NotificationList temporarily disabled</div>;
};

export default NotificationList;
