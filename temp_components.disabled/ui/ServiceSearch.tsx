import React from 'react';

const ServiceSearch: React.FC = () => {
  // Temporarily simplified to avoid build issues
  return <div className="sr-only">ServiceSearch temporarily disabled</div>;
};

export default ServiceSearch;
