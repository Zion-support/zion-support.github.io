import React from 'react';

const QuantumCard: React.FC = () => {
  // Temporarily simplified to avoid build issues
  return <div className="sr-only">QuantumCard temporarily disabled</div>;
};

export default QuantumCard;
