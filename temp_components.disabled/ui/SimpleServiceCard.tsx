import React from 'react';

const SimpleServiceCard: React.FC = () => {
  // Temporarily simplified to avoid build issues
  return <div className="sr-only">SimpleServiceCard temporarily disabled</div>;
};

export default SimpleServiceCard;
