import React from 'react';

const StatsSection: React.FC = () => {
  // Temporarily simplified to avoid build issues
  return <div className="sr-only">StatsSection temporarily disabled</div>;
};

export default StatsSection;
