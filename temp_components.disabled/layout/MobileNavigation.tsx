import React from 'react';

const MobileNavigation: React.FC = () => {
  // Temporarily simplified to avoid build issues
  return <div className="sr-only">MobileNavigation temporarily disabled</div>;
};

export default MobileNavigation;
