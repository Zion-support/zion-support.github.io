import React from 'react';

const FeatureGrid: React.FC = () => {
  // Temporarily simplified to avoid build issues
  return <div className="sr-only">FeatureGrid temporarily disabled</div>;
};

export default FeatureGrid;
