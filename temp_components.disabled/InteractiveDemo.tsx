import React from 'react';

const InteractiveDemo: React.FC = () => {
  // Temporarily simplified to avoid build issues
  return <div className="sr-only">InteractiveDemo temporarily disabled</div>;
};

export default InteractiveDemo;
