import React from 'react';

const Analytics: React.FC = () => {
  // Temporarily simplified to avoid build issues
  return <div className="sr-only">Analytics temporarily disabled</div>;
};

export default Analytics;
