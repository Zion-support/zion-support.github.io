export interface BlogPost { id: string;, title: string;, excerpt: string;, date: string;, category: string;, readTime: string;, image: string;, featured?: boolean;, slug: string;, tags: string[]; } export interface CaseStudy { id: string;, title: string;, excerpt: string;, company: string;, industry: string;, challenge: string;, solution: string;, result: string;, metric: string;, featured?: boolean;, slug: string;, tags: string[]; } export interface Service { id: string, title: string, description: string, category: string, pricing: { starting: string, popular?: boolean; }; features: string[], 