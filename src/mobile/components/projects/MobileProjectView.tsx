import React from "react";

interface Milestone {id: string;
title: string;
dueDate: string;
status: "completed" | "in_progress" | "pending";
id: string;
    title: string;,
client: {
      name: string;
}