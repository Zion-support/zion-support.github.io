import React, { useState } from 'react.ts'; import { motion } from 'framer-motion.ts'; import { Mail, CheckCircle, AlertCircle, Send, Zap, Shield, Gift } from 'lucide-react.ts'; interface NewsletterFormData { email: string; firstName: string; interests: string[]; frequency: 'weekly' | 'monthly' | 'quarterly'; } const interests = [; { id: 'ai-solutions', label: 'AI Solutions', icon: Zap },; { id: 'cloud-services', label: 'Cloud Services', icon: Shield },; { id: 'cybersecurity', label: 'Cybersecurity', icon: Shield },; { id: 'digital-transformation', label: 'Digital Transformation', icon: Zap },; { id: 'it-consulting', label: 'IT Consulting', icon: Gift },; { id: 'industry-insights', label: 'Industry Insights', icon: Zap }; ]; const frequencies = [; { value: 'weekly', label: 'Weekly', description: 'Stay updated with latest trends' },; { value: 'monthly', label: 'Monthly', description: 'Monthly digest of insights' },; { value: 'quarterly', label: 'Quarterly', description: 'Quarterly strategic updates' }; ]; const [status, setStatus] = useState<any>('idle'); const [errors, setErrors] = useState<Partial<NewsletterFormData>>({}); const validateForm = (): boolean => {; const newErrors: Partial<NewsletterFormData> = {}; if (!formData.email) { newErrors.email = 'Email is required'} else if (!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(formData.email)) { newErrors.email = 'Please enter a valid email address'} if (!formData.firstName) { newErrors.firstName = 'First name is required'} if (formData.interests.length = == 0) {; newErrors.interests = 'Please select at least one interest'; } setErrors(newErrors); return Object.keys(newErrors).length === 0}; return} setStatus('loading'); try {