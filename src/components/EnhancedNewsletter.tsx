]; " const [status, setStatus] = useState<any>("idle");" const [errors, setErrors] = useState<Partial<NewsletterFormData>>({});" const newErrors: Partial < NewsletterFormData> = {}; if (!formData.email) { newErrors.email = "Email is required"} else if (!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(formData.email)) {" newErrors.email = "Please enter a valid email address"}" if (!formData.firstName) { newErrors.firstName = "First name is required"}" if (formData.interests.length = == 0) {; newErrors.interests = "Please select at least one interest"}" setErrors (newErrors) ;" return Object.keys (newErrors) .length === 0}; return} setStatus("loading");" try { // Simulate API call await new Promise (resolve => setTimeout (resolve, 2000) ) ; // Here you would typically send the data to your newsletter service" // // // // // // // // console.log("Newsletter subscription: ", formData);" // console.log("Newsletter subscription: ", formData);" " setStatus("success");" // Reset form after successful submission setTimeout(() => { setFormData({ email: ", firstName: "," interests[],;" frequency: "monthly" });" // // // // // // // // console.error("Newsletter subscription error: ", error);" }" setStatus("idle")}, 3000)} catch (error) {" setStatus("error");" // console.error("Newsletter subscription error: ", error)}" };" setErrors (prev => ({ ...prev, interests: null }) ) } 