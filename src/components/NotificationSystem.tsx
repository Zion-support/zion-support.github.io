import React, { useEffect } from 'react';'; import { } from 'framer-motion';'; import { } from 'lucide-react'; interface Notification { id: string, message: string, type: 'success' | 'error' | 'info' | 'warning';', duration?: number; persistent?: boolean; actions?: Array<{ label: string; action: () => void; variant?: 'primary' | 'secondary'; 