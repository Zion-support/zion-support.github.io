export const ContentQualityEnhancer = ({ className = ", showAnalysis = true, autoAnalyze = true, targetElements = ["p",h1",h2",h3",h4",h5",h6",div[class*="content"]",article",section"] }) => { const [isOpen, setIsOpen] = useState(false); const [analysis, setAnalysis] = useState(null); const [isAnalyzing, setIsAnalyzing] = useState(false); const [activeTab, setActiveTab] = useState("overview"); const [selectedElement, setSelectedElement] = useState(null); // Analyze content quality const analyzeContent = useCallback(async () => { setIsAnalyzing(true); const issues = []; const suggestions = []; const wordCount = 0; const readabilityScore = 100; const seoScore = 100; const engagementScore = 100; // Get all content elements" const contentElements = document.querySelectorAll(targetElements.join(",)); contentElements.forEach((element, index) => { const text = element.textContent || "; const words = text.trim().split(/\s+/).filter(word => word.length > 0); wordCount += words.length; // Check for empty content if (words.length === 0) { issues.push({ id: `empty-content-${index}`, type: "error", title: "Empty Content", description: "Element contains no meaningful content", severity: "high", element: element, fixable: true, suggestion: "Add relevant content or remove empty element", impact: "seo" }); seoScore -= 10; engagementScore -= 10} // Check for very short content if (words.length > 0 && words.length < 10) { issues.push({ ` id: `short-content-${index}`, type: "warning", title: "Very Short Content",` description: `Element contains only ${words.length} words`, severity: "medium", element: element, fixable: true, suggestion: "Expand content to provide more value to users", impact: "engagement" }); engagementScore -= 5; seoScore -= 3} // Check for very long paragraphs (hard to read) if (words.length > 100) { issues.push({ ` id: `long-paragraph-${index}`, type: "warning", title: "Very Long Paragraph",` description: `Paragraph contains ${words.length} words`, severity: "medium", element: element, fixable: true, suggestion: "Break into smaller paragraphs for better readability", impact: "readability" }); readabilityScore -= 5; engagementScore -= 3} // Check for proper heading structure if (element.tagName.match(/^H[1-6]$/)) { const level = parseInt(element.tagName.charAt(1)); const previousHeadings = Array.from(contentElements).slice(0, index).filter(el => el.tagName.match(/^H[1-6]$/)); if (previousHeadings.length > 0) { const lastLevel = parseInt(previousHeadings[previousHeadings.length - 1].tagName.charAt(1)); if (level - lastLevel > 1) { issues.push({ ` id: `heading-skip-${index}`, type: "warning", title: "Heading Level Skipped",` description: `Heading level jumps from h${lastLevel} to h${level}`, severity: "medium", element: element, fixable: true, suggestion: "Ensure heading levels follow logical sequence", impact: "accessibility" }); seoScore -= 3} } } // Check for keyword stuffing const commonWords = text.toLowerCase().match(/\b\w+\b/g) || []; const wordFrequency = { /* empty */ }; commonWords.forEach(word => { wordFrequency[word] = (wordFrequency[word] || 0) + 1}); Object.entries(wordFrequency).forEach(([word, count]) => { if (count > 5 && word.length > 3) { const density = (count / commonWords.length) * 100; if (density > 3) { issues.push({ ` id: `keyword-stuffing-${index}-${word}`, type: "warning", title: "Potential Keyword Stuffing","` description: `Word "${word}" appears ${count} times (${density.toFixed(1)}% density)`, severity: "medium", element: element, fixable: true, suggestion: "Reduce keyword density for more natural content", impact: "seo" }); seoScore -= 5} } }); // Check for proper meta descriptions" if (element.tagName === "META" && element.getAttribute("name") === "description") { const content = element.getAttribute("content") || "; if (content.length < 50) { issues.push({ ` id: `short-meta-${index}`, type: "warning", title: "Short Meta Description",` description: `Meta description is only ${content.length} characters`, severity: "medium", element: element, fixable: true, suggestion: "Meta descriptions should be 150-160 characters for optimal display", impact: "seo" }); seoScore -= 5} else if (content.length > 160) { issues.push({ ` id: `long-meta-${index}`, type: "warning", title: "Long Meta Description",` description: `Meta description is ${content.length} characters`, severity: "low", element: element, fixable: true, suggestion: "Meta descriptions should be 150-160 characters for optimal display", impact: "seo" }); seoScore -= 2} } // Check for broken links" if (element.tagName === "A") { const href = element.getAttribute("href"); if (href && (href.startsWith("#") || href.startsWith("javascript:"))) { issues.push({ ` id: `broken-link-${index}`, type: "warning", title: "Potential Broken Link","` description: `Link "${href}" may not work properly`, severity: "medium", element: element, fixable: true, suggestion: "Ensure link points to valid URL or page section", impact: "engagement" }); engagementScore -= 3} } // Check for images without alt text" if (element.tagName === "IMG") { const alt = element.getAttribute("alt"); if (!alt || alt.trim() === ") { issues.push({ ` id: `missing-alt-${index}`, type: "error", title: "Missing Alt Text", description: "Image has no alternative text for accessibility", severity: "high", element: element, fixable: true, suggestion: "Add descriptive alt text for better accessibility and SEO", impact: "accessibility" }); seoScore -= 8; engagementScore -= 5} } }); // Generate suggestions based on analysis if (wordCount < 300) { suggestions.push({ id: "increase-content", type: "improvement", title: "Increase Content Length", description: "Consider adding more content to improve SEO and user engagement", priority: "high", implementation: "Add relevant sections, expand existing content, include more examples", expectedImpact: 15 })} if (readabilityScore < 80) { suggestions.push({ id: "improve-readability", type: "optimization", title: "Improve Readability", description: "Content could be more readable for better user experience", priority: "medium", implementation: "Use shorter sentences, break up long paragraphs, simplify complex language", expectedImpact: 12 })} if (seoScore < 80) { suggestions.push({ id: "seo-optimization", type: "enhancement", title: "SEO Optimization", description: "Several SEO improvements could boost search visibility", priority: "high", implementation: "Fix meta descriptions, add alt text, improve heading structure", expectedImpact: 20 })} if (engagementScore < 80) { suggestions.push({ id: "engagement-improvement", type: "improvement", title: "Improve User Engagement", description: "Content could be more engaging for visitors", priority: "medium", implementation: "Add interactive elements, improve content structure, include calls-to-action", expectedImpact: 10 })} // Calculate overall score const overallScore = Math.round((readabilityScore + seoScore + engagementScore) / 3); const analysisResult = { wordCount, readabilityScore: Math.max(0, Math.min(100, readabilityScore)), seoScore: Math.max(0, Math.min(100, seoScore)), engagementScore: Math.max(0, Math.min(100, engagementScore)), issues, suggestions, overallScore: Math.max(0, Math.min(100, overallScore)) }; setAnalysis(analysisResult); setIsAnalyzing(false)}, [targetElements]); // Auto-fix content issues const autoFixIssues = useCallback(() => { if (!analysis) return; const fixedCount = 0; fixableIssues.forEach(issue => { if (issue.element) { switch (issue.id.split("-")[0]) { case "empty-content":" if (issue.element.textContent?.trim() === ") { issue.element.innerHTML = "<em>Content placeholder - please add relevant information</em>; fixedCount++} break; case "missing-alt":" if (issue.element.tagName === "IMG") { const img = issue.element; if (!img.alt) { img.alt = "Image"; fixedCount++} } break; case "short-meta":" case "long-meta": // These would require more complex logic to fix break; case "broken-link": // These would require more complex logic to fix break} } }); if (fixedCount > 0) { // Re-analyze content after fixes setTimeout(analyzeContent, 500)} return fixedCount}, [analysis, analyzeContent]); // Highlight element in page const highlightElement = useCallback((element) => { // Remove previous highlights" document.querySelectorAll(".content-highlight").forEach(el => { el.classList.remove("content-highlight")}); // Add highlight to selected element" element.classList.add("content-highlight"); setSelectedElement(element); // Scroll to element" element.scrollIntoView({ behavior: "smooth", block: "center" }); // Remove highlight after 3 seconds setTimeout(() => { element.classList.remove("content-highlight"); setSelectedElement(null)}, 3000)}, []); // Auto-analyze content useEffect(() => { if (autoAnalyze) { const timer = setTimeout(analyzeContent, 3000); return () => clearTimeout(timer)} }, [autoAnalyze, analyzeContent]); // Get score color const getScoreColor = (score) => { if (score >= 80) return "text-green-600"; if (score >= 60) return "text-yellow-600"; return "text-red-600"}; // Get score background color const getScoreBgColor = (score) => { if (score >= 80) return "bg-green-100 dark:bg-green-900/20"; if (score >= 60) return "bg-yellow-100 dark:bg-yellow-900/20"; return "bg-red-100 dark:bg-red-900/20"}; // Get severity color const getSeverityColor = (severity) => { switch (severity) { case "high": return "text-red-600 bg-red-100 dark:bg-red-900/30"; case "medium": return "text-yellow-600 bg-yellow-100 dark:bg-yellow-900/30"; case "low": return "text-blue-600 bg-blue-100 dark:bg-blue-900/30"; default: return "text-gray-600 bg-gray-100 dark:bg-gray-900/30"} }; // Get priority color const getPriorityColor = (priority) => { switch (priority) { case "high": return "text-red-600 bg-red-100 dark:bg-red-900/30"; case "medium": return "text-yellow-600 bg-yellow-100 dark:bg-yellow-900/30"; case "low": return "text-blue-600 bg-blue-100 dark:bg-blue-900/30"; default: return "text-gray-600 bg-gray-100 dark:bg-gray-900/30"} }; return (<> {/* Content Quality Toggle Button */}"` <motion.button initial={{ scale: 0 }} animate={{ scale: 1 }} whileHover={{ scale: 1.1 }} whileTap={{ scale: 0.9 }} onClick={() => setIsOpen(!isOpen)} className={`fixed bottom-20 right-4 z-50 w-14 h-14 bg-purple-600 hover:bg-purple-700 text-white rounded-full shadow-lg flex items-center justify-center transition-all duration-300 ${className}`} aria-label="Content Quality Analysis" aria-expanded={isOpen}>" <DocumentTextIcon className="w-6 h-6"/> </motion.button> {/* Content Quality Panel */} <AnimatePresence> {isOpen && (<motion.div initial = { { opacity: 0, scale: 0.8, y: 20 }} animate = { { opacity: 1, scale: 1, y: 0 }} exit = { { opacity: 0, scale: 0.8, y: 20 " 