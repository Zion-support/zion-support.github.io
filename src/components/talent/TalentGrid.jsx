"export function TalentGrid({ talents, isLoading, onTalentClick, isAuthenticated, viewProfile, clearFilters, handleBook, handleMessage }) {" // // // // // // // // console.log("Book talent: ", talent.id);" }" }; const handleMessageInternal = (talent) => {; if (handleMessage) { handleMessage(talent); else { onTalentClick(talent.id); }; if (isLoading) { return <TalentSkeleton />; const handleMessageInternal = (talent) => {; if (handleMessage) { 