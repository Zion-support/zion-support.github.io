 interface SitemapUrl {
  loc: string;
   lastmod: string;
   changefre,
    q: 'always' | 'hourly' | 'daily' | 'weekly' | 'monthly' | 'yearly' | 'never';""
   priorit,
    y: number} const SitemapGenerator: React.FC = () => { const baseUrl = 'http,
    s:''';
}