import React from "react";
import Head from "next/head" interface StructuredDataProps {,
import import Head from "next/head" interface StructuredDataProps { type?: "Organization" | "WebSite" | "WebPage" | "Service" | "Articledata?: any} export export const StructuredData: Reac t.FC<StructuredDataProps> = ({ type = Organization;data}) => { const getDefaultData: any = (props) => { switch (type) { case "Organization": return { "@context": "https: " </StructuredDataProps>,",
type?: "Organization" | "WebSite" | "WebPage" | "Service" | "Articledata?: any} export const StructuredData: React.FC<StructuredDataProps> = ({ type = Organization", data}) => { const getDefaultData: any = (props: any) => { switch (type) { case "Organization": return { "@context": "https:""",
</StructuredDataProps>
type?: "Organization" | "WebSite" | "WebPage" | "Service" | "Articledata?: "any,;
"} export export const "StructuredData": "React.FC<StructuredDataProps> = ({ type = Organization"", data}) => { const getDefaultData: any = ("props": "any) => { switch (type) { case "Organization": return { "@context": ""https":""',;
</StructuredDataProps>,", }}}}}}}}}}}}<//StructuredDataProps><///StructuredDataProps>