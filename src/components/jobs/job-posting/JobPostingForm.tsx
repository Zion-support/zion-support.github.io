import React, { useState, useEffect, useCallback } from 'react';
import { _useNavigate  } from 'react-router-dom';
import { _toast } from 'sonner';
import { _Input } from '@/components/ui/input';
import { _Label } from '@/components/ui/label';
import { _Button } from '@/components/ui/button';
import { _Form } from '@/components/ui/form';
import { _useJobForm } from './useJobForm';
import { _BasicInfoFields } from './BasicInfoFields';
import { _DateFields } from './DateFields';
import { _DescriptionFields } from './DescriptionFields';
import { _useJobs } from '@/hooks/useJobs';
import { _JobSchemaType } from './validation';
  return () => {;
    // Cleanup function;
};
}, []); []);    if(jobId) {;
      setIsFormLoading(true);
      getJobById(jobId);
        .then((job) => {;
          if(job) {;
            const _currentValues = getValues(); // Use destructured getValues;
            Object.entries(job).forEach(([key, value]) => {;
              if(key === 'published_date' && value) {;
                setStartDate(new Date(value as string));
                setValue('published_date', value as string);
      if(onSuccess) {;