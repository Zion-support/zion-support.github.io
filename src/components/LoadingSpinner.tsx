import React from 'react';

  size?: 'small' | 'medium' | 'large';
  color?: 'blue' | 'white' | 'gray';
  text?: string;
  fullScreen ?  : boolean;
 }

            cx='12'
            cy='12'
            r='10'
            stroke='currentColor'
            strokeWidth='4'
            fill='none'
          />
          />
        </svg>
      </div>
      {  text  && (
      )}
    </div>
  );
};

exportdefaultLoadingSpinner;
