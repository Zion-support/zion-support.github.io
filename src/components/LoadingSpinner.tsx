import React from 'react';
interface LoadingSpinnerProps {
  <LoadingSpinnerProps> = () => {
  return (
    <div className="loadingspinner">
      {}
    <div>
  );
'use client';
interface LoadingSpinnerProps {<LoadingSpinnerProps> = ({<div className={cn('flex flex-col items-center justify-center gap-3', className)}>
      <div
className={cn()}
<p className="text-sm text-gray-600 dark:text-gray-300 animate-pulse">
          {text}
        <p>
      )}
  if (fullScreen) {<div>Coming Soon<div>
  )
          <div className="fixed inset-0 bg-white/80 dark:bg-slate-900/80 backdrop-blur-sm flex items-center justify-center z-50">
        {spinner}
  }
  return spinner;
};
export default LoadingSpinner;
}}