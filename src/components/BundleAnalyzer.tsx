import React, { useEffect, useCallback, useState } from 'react.ts'; interface BundleAnalyzerProps extends React.PropsWithChildren<{}> { enabled?: boolean; showUI?: boolean; } interface BundleMetrics { totalSize: number; chunkCount: number; largestChunk: {; name: string; size: number; }; averageChunkSize: number; gzipSavings: number; } export const BundleAnalyzer: React.FC<BundleAnalyzerProps> = ({ enabled = true, showUI = false }) => { const [metrics, setMetrics] = useState<any>({ totalSize: 0, chunkCount: 0, largestChunk: { name: '', size: 0 }, averageChunkSize: 0, gzipSavings: 0 }); const analyzeBundle = useCallback(() => { if (!enabled) return; try {