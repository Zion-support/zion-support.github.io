"import { Search, X, ArrowDown export const EnhancedSearchInput = ({ placeholder = "Search for services, talent, or equipment...", onSearch, suggestions = [], className = " }) => {";" const [query, setQuery] = useState(");" const [showSuggestions, setShowSuggestions] = useState(false);" const [filteredSuggestions, setFilteredSuggestions] = useState([]); const [selectedIndex, setSelectedIndex] = useState(-1); const inputRef = useRef(null); const suggestionsRef = useRef(null); useEffect(() => { if (query.trim()) { const filtered = suggestions.filter(suggestion => suggestion.title.toLowerCase().includes(query.toLowerCase()) ||; 