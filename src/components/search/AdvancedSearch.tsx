import React, { useState, useEffect, useRef } from 'react'; import { Search, Filter, X, Clock, TrendingUp, Star } from 'lucide-react'; import { useNavigate } from 'react-router-dom'; export interface SearchFilter { category: string; priceRange: [number, number]; rating: number; tags: string[]; } export interface SearchSuggestion { id: string; text: string; type: 'service' | 'category' | 'feature'; popularity?: number; trending?: boolean; } interface AdvancedSearchProps { suggestions?: SearchSuggestion[]; onSearch?: (query: string, filters?: SearchFilter) => void; placeholder?: string; className?: string; showFilters?: boolean; } export const AdvancedSearch: React.FC<AdvancedSearchProps> = ({ suggestions = [], onSearch, placeholder = "Search services, solutions, features...", className = "", showFilters = true }) => { const [query, setQuery] = useState(''); const [isOpen, setIsOpen] = useState(false); const [selectedFilters, setSelectedFilters] = useState<SearchFilter>({ category: '', priceRange: [0, 10000], rating: 0, tags: [] }); const [recentSearches, setRecentSearches] = useState<string[]>([]); const [filteredSuggestions, setFilteredSuggestions] = useState<SearchSuggestion[]>([]); const navigate = useNavigate(); const inputRef = useRef<HTMLInputElement>(null); const dropdownRef = useRef<HTMLDivElement>(null);