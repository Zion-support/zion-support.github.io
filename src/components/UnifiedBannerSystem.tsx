import React, { useState, useEffect } from 'react';'; import { } from 'framer-motion'; export interface BannerConfig { id: string;, title: string;, subtitle?: string;, description: string;, ctaText: string;, ctaLink: string;, imageUrl?: string;, priority: number;, category: string;, tags: string[];, backgroundColor?: string;, textColor?: string;, animation?: 'slide' | 'fade' | 'scale' | 'none';, duration?: number; // Auto-rotate duration in ms } interface UnifiedBannerSystemProps { banners: BannerConfig[];, autoRotate?: boolean;, rotationInterval?: number;, showNavigation?: boolean;, maxVisible?: number; } const UnifiedBannerSystem: React.FC<UnifiedBannerSystemProps> = ({ banners autoRotate = true rotationInterval = 5000 showNavigation = true 