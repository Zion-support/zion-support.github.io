import React, { useState, useEffect, useMemo } from 'react'; import { motion, AnimatePresence } from 'framer-motion'; import { Search, Filter, X, ChevronDown, Check, Star, Tag, MapPin, Calendar, Users, Zap, Brain, Cloud, Shield, Globe } from 'lucide-react'; interface SearchResult { id: string; title: string; description: string; category: string; tags: string[]; location?: string; date?: string; rating?: number; type: 'service' | 'article' | 'team' | 'technology'} interface FilterOption { id: string; label: string; value: string; count: number} interface SearchAndFilterSystemProps extends React.PropsWithChildren<{}> { data: SearchResult[]; onResultsChange?: (results: SearchResult[]) => void; placeholder?: string; showFilters?: boolean} export const SearchAndFilterSystem: React.FC<SearchAndFilterSystemProps> = ({ data, onResultsChange, placeholder = "Search services, articles, team members...", showFilters = true }) => { const [searchQuery, setSearchQuery] = useState(''); const [isSearchFocused, setIsSearchFocused] = useState(false); const [activeFilters, setActiveFilters] = useState<Set<string>>(new Set()); const [showFilterPanel, setShowFilterPanel] = useState(false); const [selectedCategory, setSelectedCategory] = useState<any>('all'); const [sortBy, setSortBy] = useState<any>('relevance');