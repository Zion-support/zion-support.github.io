"export const QuantumMatrixBackground = ({ intensity = "medium", showParticles = true, showGrid = true, showHolograms = true }) => {"; const canvasRef = useRef(null); const particlesRef = useRef(null); useEffect(() => { const canvas = canvasRef.current; if (!canvas) return; const ctx = canvas.getContext("2d");" if (!ctx)" return; canvas.width = window.innerWidth; canvas.height = window.innerHeight; // Quantum particle system const particles = []; const colors = ["#22ddd2", #8b5cf6",#3b82f6", #22ddd2"];" const intensityMultiplier = intensity === "high" ? 2 : intensity === "medium" ? 1.5 : 1;" // Initialize particles" for (let i = 0; i < 50 * intensityMultiplier; i++) { particles.push({ x: Math.random() * canvas.width, y: Math.random() * canvas.height, vx: (Math.random() - 0.5) * 2, vy: (Math.random() - 0.5) * 2, size: Math.random() * 3 + 1, opacity: Math.random() * 0.8 + 0.2, color: colors[Math.floor(Math.random() * colors.length)], quantumState: Math.random(); })} // Quantum entanglement effect const entangleParticles = () => {; for (let i = 0; i < particles.length; i += 2) { if (i + 1 < particles.length) { 