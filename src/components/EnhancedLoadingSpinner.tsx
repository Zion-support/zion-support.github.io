import React from 'react.ts'; import { motion } from 'framer-motion.ts'; interface LoadingSpinnerProps extends React.PropsWithChildren<{}> { size?: 'sm' | 'md' | 'lg'; variant?: 'default' | 'futuristic' | 'ai' | 'quantum'; text?: string; showProgress?: boolean; progress?: number; } interface EnhancedLoadingSpinnerProps { enabled?: boolean; showProgress?: boolean; showEstimatedTime?: boolean; size?: 'small' | 'medium' | 'large'; variant?: 'default' | 'futuristic' | 'minimal' | 'themed'; onComplete?: () => void; autoComplete?: boolean; autoCompleteDelay?: number; } export function EnhancedLoadingSpinner({ enabled = true, showProgress = true, showEstimatedTime = true, size = 'medium', variant = 'futuristic', onComplete, autoComplete = false, autoCompleteDelay = 3000 }: EnhancedLoadingSpinnerProps) { const [loadingState, setLoadingState] = useState<LoadingState>({ type: 'loading', message: 'Loading...', progress: 0, estimatedTime: 3 }); const [isVisible, setIsVisible] = useState(true); const [currentStep, setCurrentStep] = useState(0);