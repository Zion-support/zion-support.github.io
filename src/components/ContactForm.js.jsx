"const ContactForm = () => {" const [formData, setFormData] = useState({; firstName: ", lastName: "," email: ", comp: "," phone: ", service: "," message: "});" const [isSubmitting, setIsSubmitting] = useState(false);" const [submitStatus, setSubmitStatus] = useState("idle");" const handleInputChange = e => {"; const { name, value } = e.target; setFormData(prev => ({ ...prev, [name]: value})); }; const handleSubmit = async e => {; e.preventDefault(); setIsSubmitting(true); setSubmitStatus("idle");" try {" // Simulate API call await new Promise(resolve => setTimeout(resolve, 2000)); // Reset form on success setFormData({ firstName: ", lastName: "," email: ", comp: "," phone: ", service: "," message: "});" setSubmitStatus("success");" setTimeout(() => setSubmitStatus("idle"), 5000);" } catch (error) {" setSubmitStatus("error");" setTimeout(() => setSubmitStatus("idle"), 5000);" } finally {" 