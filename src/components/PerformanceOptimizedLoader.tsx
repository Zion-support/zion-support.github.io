import React, { memo } from 'react.ts'; import { motion } from 'framer-motion.ts'; interface LoaderProps extends React.PropsWithChildren<{}> { size?: 'sm' | 'md' | 'lg'; color?: 'primary' | 'secondary' | 'white'; text?: string; fullScreen?: boolean; showLogo?: boolean; } export const PerformanceOptimizedLoader = memo<LoadingSpinnerProps>(({ size = 'md', color = 'primary', text, fullScreen = false }: LoaderProps) { const sizeClasses = { sm: 'w-6 h-6', md: 'w-12 h-12',; ; ; ; ; lg: 'w-16 h-16'; ; }; const textSizes = { sm: 'text-xs', md: 'text-sm',; ; ; lg: 'text-base'; ; }; const showLogo = true;