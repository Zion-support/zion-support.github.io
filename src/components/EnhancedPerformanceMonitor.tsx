import React, { useEffect, useState } from 'react';'; import { } from '../utils/performance'; interface PerformanceMetrics { domContentLoaded: number;, loadComplete: number;, firstPaint: number;, firstContentfulPaint: number;, cls?: number;, fid?: number;, lcp?: number;, ttfb?: number; } interface PerformanceAlert { id: string;, type: 'warning' | 'error' | 'info';, message: string;, metric: string;, value: number;, threshold: number; } const EnhancedPerformanceMonitor: React.FC = () => { const [metrics, setMetrics] = useState<PerformanceMetrics | null>(null); const [alerts, setAlerts] = useState<PerformanceAlert[]>([]); const [isMonitoring, setIsMonitoring] = useState(false); // Performance thresholds const thresholds = { domContentLoaded: 1000, // ms, loadComplete: 2000, // ms, firstPaint: 1000, // ms, firstContentfulPaint: 1500, // ms, cls: 0.1, // Cumulative Layout Shift, fid: 100, // ms, lcp: 2500, // ms, ttfb: 600 // ms }; const generateAlert = (metric: string, value: number, threshold: number): PerformanceAlert => { const type = value > threshold ? 'error' : value > threshold * 0.8 ? 'warning' : 'info'; 