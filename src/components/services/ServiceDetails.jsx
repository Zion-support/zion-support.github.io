import { Card,CardContent,CardDescription,CardFooter,CardHeader,CardTitle } from "@/components/ui/card"; import { Server,Clock,MapPin export function ServiceDetails({ country }) { const getDatacenters = (country) => { const dataCenters = { export default ServiceDetails;