<<<<<<< HEAD
import React, { useEffect } from 'react';
const SecurityEnhancer: React.FC = () => {;
  useEffect(() => {
    addSecurityHeaders();
    setupSecurityMonitoring();
}, []);
  const addSecurityHeaders = () => {;
    const meta = document.createElement('meta');
    meta.httpEquiv = 'X-Content-Type-Options';
    meta.content = 'nosniff';
    document.head.appendChild(meta);
};
  const setupSecurityMonitoring = () => {;
    let suspiciousActivityCount = 0;
    const maxSuspiciousActivities = 10;
    const handleSuspiciousActivity = (event: Event) => {;
      suspiciousActivityCount++;
      console.warn('Suspicious activity detected:', event.type);
      if (suspiciousActivityCount > maxSuspiciousActivities) {;
        console.error('Multiple suspicious activities detected.');
}
    };
    document.addEventListener('DOMContentLoaded', () => {;
      const scripts = document.querySelectorAll('script');
      scripts.forEach((script) => {;
        if (script.src && !script.src.startsWith(window.location.origin)) {;
          handleSuspiciousActivity(new Event('external-script'));
}
      });
});
};
  return null;
};
export default SecurityEnhancer;
=======
import React from 'react'; import { import { motion,AnimatePresence } from 'framer-motion'; params - Function parameters * @returns {*} Function return value */ function SecurityEnhancer(props: any) { Shield,Lock,Eye,EyeOff,AlertTriangle,CheckCircle,XCircle,Info,Settings,X,RefreshCw,Key,Fingerprint,Network,Database,Server,Globe,FileText,Download,Upload,Trash2,Search,Filter,BarChart3,ShieldCheck,Bug,Zap,Clock,UserCheck,Activity,ChevronUp,ChevronDown} from 'lucide-react'; interface SecurityStatus { csp: boolean; hsts: boolean; xss: boolean; frameOptions: boolean; contentType: boolean; referrerPolicy: boolean; permissionsPolicy: boolean} interface SecurityEvent { id: string; type: 'info' | 'warning' | 'error' | 'success'; message: string; timestamp: Dat e; source: string; severity: 'low' | 'medium' | 'high' | 'critical'; details?: string} interface SecurityMetrics { totalRequests: number; blockedRequests: number; suspiciousActivity: number; lastScan: Dat e; vulnerabilities: number; complianceScore: number; export function SecurityEnhancer(props: any) { const [isVisible,setIsVisible] = useState<any>(false); const [isExpanded,setIsExpanded] = useState<any>(false); const [securityStatus,setSecurityStatus] = useState<SecurityStatus | null>( null ); const [securityEvents,setSecurityEvents] = useState<SecurityEvent[]>([]); const [securityMetrics,setSecurityMetrics] = useState<SecurityMetrics | null>(null); const [isScanning,setIsScanning] = useState<any>(false); const [activeFilters,setActiveFilters] = useState<string[]>([]); const [searchTerm,setSearchTerm] = useState<any>(''); useEffect(() => { return () => { }},[]);,[]); if(isVisible) { initializeSecurityMonitoring(); runSecurityScan()} },[isVisible]) ; setupSecurityEventListeners()},30000); return () => clearInterval(interval)},[]); cspMeta.httpEquiv = 'Content-Security-Policy'; const cspMeta = document.createElement('meta');' cspMeta.httpEquiv = 'Content-Security-Policy'; cspMeta.content = [' "default-src 'self'"," "script-src 'self' 'unsafe-inline' 'unsafe-eval' https: "style-src 'self' 'unsafe-inline' https: "font-src 'self' https: "img-src 'self' data: http s:"," "connect-src 'self' https: "frame-src 'self'"," "object-src 'none'"," "base-uri 'self'"," "form-action 'self'"," "frame-ancestors 'none'",upgrade-insecure-requests',].join('; '); if(existingCSP) { existingCSP.remove()} document.head.appendChild(cspMeta); const securityMetaTags = [' { name: 'X-Content-Type-Options',content: 'nosniff' },{ name: 'X-Frame-Options',content: 'DENY' },{ name: 'X-XSS-Protection',content: '1; mode=block' },{ name: 'Referrer-Policy',content: 'strict-origin-when-cross-origin' },{ name: 'Permissions-Policy',content: 'camera=(),microphone=(),geolocation=(),interest-cohort=()},]; securityMetaTags.forEach(tag => { metaTag.name = tag.name; metaTag.content = tag.content; document.head.appendChild (metaTag) }) },[]) ; Element.prototype.innerHTML = function(value: string) { if(' typeof value === 'string' &&' (value.includes('<script>) || value.includes('javascript:')) ) { logSecurityEvent(' 'warning',Potential XSS attempt detected',DOM Manipulation',medium' )} return originalInnerHTML.call(this,value) }; window.fetch = function(input: RequestInf o | URL,init?: RequestInit) { if(url.includes('javascript:') || url.includes('data: tex t/html')) { logSecurityEvent(' 'error',Suspicious fetch request blocked',Network Request',high' ); return Promise.reject(new Error('Suspicious request blocked'))} return originalFetch.call(this,input,init) }; console.log = function(...args: an y[]) { if(' args.some(arg => typeof arg === 'string' && arg.includes('password')) ) { logSecurityEvent(' 'warning',Potential sensitive data logging detected',Console Access',medium' )} return originalConsoleLog.apply(this,args) }},[]) ; setSecurityEvents(prev => [event,...prev.slice(0,99)])},[] ); setSecurityStatus(status)} catch(error) { },[]) ; try { await new Promise(resolve => setTimeout(resolve,2000)); const metrics: SecurityMetric s = { totalRequests: Mat h.floor(Math.random() * 1000) + 500,blockedRequests: Mat h.floor(Math.random() * 50) + 10,suspiciousActivity: Mat h.floor(Math.random() * 20) + 5,lastScan: new Date(),vulnerabilities: Mat h.floor(Math.random() * 10) + 2,complianceScore: Mat h.floor(Math.random() * 20) + 80}; setSecurityMetrics(metrics); logSecurityEvent(' 'success',Security scan completed successfully',Security Scanner',low' )} catch(error) { logSecurityEvent(' 'error',Security scan failed',Security Scanner',high' )} finally { setIsScanning(false)} },[logSecurityEvent]) ; return matchesSearch && matchesFilter}) }; case 'warning':" return <AlertTriangle className="w-4 h-4 text-yellow-500" />; case 'error':" return <XCircle className="w-4 h-4 text-red-500" />; case 'success':" return <CheckCircle className="w-4 h-4 text-green-500" />; default:" return <Info className="w-4 h-4 text-gray-500" />} }; case 'high':' return 'border-red-500 bg-red-50 dark: b g-red-900/20'; case 'medium':' return 'border-yellow-500 bg-yellow-50 dark: b g-yellow-900/20'; case 'low':' return 'border-blue-500 bg-blue-50 dark: b g-blue-900/20'; default:' return 'border-gray-500 bg-gray-50 dark: b g-gray-900/20'} }; return () <> {} <motion.button" className="fixed bottom-6 right-24 z-50 bg-zion-blue hover: b g-zion-blue-dark text-white p-3 rounded-full shadow-lg hover: shado w-xl transition-all duration-300" onClick={() => setIsVisible(!isVisible)} whileHover={{ scale: 1.1 }} whileTap={{ scale: 0.9 }}" title="Security Panel" >" <Shield className="w-6 h-6" /> </motion.button> {} <AnimatePresence> {isVisible && (<motion.div initial={{ opacity: 0,x: 30 0 }} animate={{ opacity: 1,x: 0 }} exit={{ opacity: 0,x: 30 0 }}" className="fixed top-0 right-0 h-full w-96 bg-white dark: b g-gray-900 shadow-2xl z-40 overflow-y-auto" role="dialog" >" <div className="p-6"> {}" <div className="flex items-center justify-between mb-6">" <div className="flex items-center space-x-2">" <Shield className="w-6 h-6 text-zion-blue" />" <h2 className="text-xl font-bold text-gray-900 dark: tex t-white"> Security </h2> </div>" <div className="flex items-center space-x-2"> <button onClick={() => setIsExpanded(!isExpanded)}" className="p-1 text-gray-500 hover: tex t-gray-700 dark: tex t-gray-400 dark: hove r:text-gray-200"' aria-label={isExpanded ? 'Collapse panel' : 'Expand panel'} > {isExpanded ? (" <ChevronUp className="w-4 h-4" /> ) : (" <ChevronDown className="w-4 h-4" /> )} </button> <button onClick={() => setIsVisible(false)}" className="p-1 text-gray-500 hover: tex t-gray-700 dark: tex t-gray-400 dark: hove r:text-gray-200" >" <X className="w-5 h-5" /> </button> </div> </div> {} {securityStatus && (" <div className="mb-6">" <h3 className="text-lg font-semibold mb-3 text-gray-900 dark: tex t-white"> Security Status </h3>" <div className="space-y-2"> {[' { key: 'csp',label: 'Content Security Policy' },{ key: 'hsts',label: 'HTTP Strict Transport Security' },{ key: 'xss',label: 'XSS Protection' },{ key: 'frameOptions',label: 'Frame Options' },{ key: 'contentType',label: 'Content Type Options' },{ key: 'referrerPolicy',label: 'Referrer Policy' },{ key: 'permissionsPolicy',label: 'Permissions Policy' },].map(item => ( <div key={item.key}" className="flex items-center justify-between p-2 bg-gray-50 dark: b g-gray-800 rounded-lg" >" <span className="text-sm text-gray-700 dark: tex t-gray-300"> {item.label} </span> {getStatusIcon() securityStatus[item.key as keyof SecurityStatus] )} </div> ))} </div> </div>) } {} {securityMetrics && (" <div className="mb-6">" <h3 className="text-lg font-semibold mb-3 text-gray-900 dark: tex t-white"> Security Metrics </h3>" <div className="grid grid-cols-2 gap-3">" <div className="p-3 bg-blue-50 dark: b g-blue-900/20 rounded-lg">" <p className="text-xs text-blue-600 dark: tex t-blue-400"> Total Requests </p>" <p className="text-lg font-bold text-blue-800 dark: tex t-blue-200"> {securityMetrics.totalRequests.toLocaleString()} </p> </div>" <div className="p-3 bg-red-50 dark: b g-red-900/20 rounded-lg">" <p className="text-xs text-red-600 dark: tex t-red-400"> Blocked Requests </p>" <p className="text-lg font-bold text-red-800 dark: tex t-red-200"> {securityMetrics.blockedRequests.toLocaleString()} </p> </div>" <div className="p-3 bg-yellow-50 dark: b g-yellow-900/20 rounded-lg">" <p className="text-xs text-yellow-600 dark: tex t-yellow-400"> Suspicious Activity </p>" <p className="text-lg font-bold text-yellow-800 dark: tex t-yellow-200"> {securityMetrics.suspiciousActivity.toLocaleString()} </p> </div>" <div className="p-3 bg-green-50 dark: b g-green-900/20 rounded-lg">" <p className="text-xs text-green-600 dark: tex t-green-400"> Compliance Score </p>" <p className="text-lg font-bold text-green-800 dark: tex t-green-200"> {securityMetrics.complianceScore}% </p> </div> </div> </div>) } {}" <div className="mb-6">" <div className="flex items-center justify-between mb-3">" <h3 className="text-lg font-semibold text-gray-900 dark: tex t-white"> Security Events </h3> <button onClick={runSecurityScan} disabled={isScanning}" className="flex items-center space-x-2 px-3 py-1 bg-zion-blue hover: b g-zion-blue-dark text-white text-sm rounded-lg transition-colors disabled: opacit y-50" > {isScanning ? ( <>" <div className="w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin" /> <span>Scanning...</span> </> ) : ( <>" <ShieldCheck className="w-4 h-4" /> <span>Scan</span> </> )} </button> </div> {}" <div className="mb-3 space-y-2">" <div className="flex space-x-2"> {['info',warning',error',success'].map(type => ( <button key={type} onClick={() => { setActiveFilters(prev => prev.includes(type) ? prev.filter(t => t !== type) : [...prev,type] )}}` className={`px-2 py-1 text-xs rounded ${activeFilters.includes(type) ? 'bg-zion-blue text-white'' : 'bg-gray-200 dark: b g-gray-700 text-gray-700 dark: tex t-gray-300'` }`} > {type} </button>) ) } </div> <input" type="text" placeholder="Search events..." value={searchTerm} onChange={e => setSearchTerm(e.target.value)}" className="w-full px-3 py-2 text-sm border border-gray-300 dark: borde r-gray-600 rounded-lg bg-white dark: b g-gray-800 text-gray-900 dark: tex t-white" /> </div> {}" <div className="space-y-2 max-h-64 overflow-y-auto"> {filteredEvents.length > 0 ? ( filteredEvents.map(event => ( <div key={event.id}` className={`p-3 rounded-lg border-l-4 ${getSeverityColor(event.severity)}`} >" <div className="flex items-start space-x-2"> {getEventIcon(event.type)}" <div className="flex-1 min-w-0">" <p className="text-sm font-medium text-gray-900 dark: tex t-white"> {event.message} </p>" <div className="flex items-center space-x-2 mt-1 text-xs text-gray-600 dark: tex t-gray-400"> <span>{event.source}</span> <span>•</span> <span> {event.timestamp.toLocaleTimeString()} </span> <span>•</span>" <span className="capitalize"> {event.severity} </span> </div> {event.details && (" <p className="text-xs text-gray-500 dark: tex t-gray-500 mt-1"> {event.details} </p>) } </div> </div> </div> )) ) : (" <div className="text-center py-4 text-gray-500 dark: tex t-gray-400"> No events found </div>) } </div> </div> {}" <div className="mb-6">" <h3 className="text-lg font-semibold mb-3 text-gray-900 dark: tex t-white"> Quick Actions </h3>" <div className="grid grid-cols-2 gap-3"> <button onClick={() => logSecurityEvent(' 'info',Manual security check initiated',User Action',low' ) }" className="flex items-center justify-center space-x-2 p-3 bg-blue-50 dark: b g-blue-900/20 text-blue-700 dark: tex t-blue-300 rounded-lg hover: b g-blue-100 dark: hove r:bg-blue-900/40 transition-colors" >" <Activity className="w-4 h-4" />" <span className="text-sm">Check Status</span> </button> <button onClick={() => setSecurityEvents([])}" className="flex items-center justify-center space-x-2 p-3 bg-gray-50 dark: b g-gray-800 text-gray-700 dark: tex t-gray-300 rounded-lg hover: b g-gray-100 dark: hove r:bg-gray-700 transition-colors" >" <Trash2 className="w-4 h-4" />" <span className="text-sm">Clear Events</span> </button> </div> </div> {}" <div className="mb-6">" <h3 className="text-lg font-semibold mb-3 text-gray-900 dark: tex t-white"> Security Tips </h3>" <div className="space-y-2 text-sm text-gray-600 dark: tex t-gray-400"> <p>• Keep your browser and extensions updated</p> <p>• Use strong,unique passwords for each account</p> <p>• Enable two - factor authentication when available</p> <p>• Be cautious of suspicious links and downloads</p> <p>• Regularly review your security settings</p> </div> </div> </div> </motion.div>) } </AnimatePresence> </> )} '"` </motion> </motion> </script> </any> </string> </any> </SecurityMetrics> </SecurityEvent> </SecurityStatus> </any> </any>
>>>>>>> origin/automation-improvements
