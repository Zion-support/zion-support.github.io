import React, { useEffect, useCallback } from 'react'; interface SecurityEnhancerProps { enabled?: boolean; onSecurityEvent?: (event: SecurityEvent) => void; } interface SecurityEvent { type: 'csp-violation' | 'xss-attempt' | 'insecure-request' | 'suspicious-activity'; details: any; timestamp: number; } export const SecurityEnhancer: React.FC<SecurityEnhancerProps> = ({ enabled = true, onSecurityEvent }) => { const reportSecurityEvent = useCallback((event: SecurityEvent) => { if (process.env.NODE_ENV === 'development') { console.warn('Security Event:', event); } onSecurityEvent?.(event); }, [onSecurityEvent]); useEffect(() => { if (!enabled || typeof window === 'undefined') return;