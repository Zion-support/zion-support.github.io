<span>{text}</span>
</div>
)
type className,
}: ProfileBadgeProps) {
const badgeConfig = {
verified: {
const {,
icon: Icon  text colors;
}= badgeConfig[type];
}> <Icon className="w-3 h-3" /> <span> {
text,
}</span>
}'"  );
}