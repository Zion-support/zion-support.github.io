
import React, { useState, useEffect } from 'react'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/
import { Badge } from '@/components/ui/
import { Button } from '@/components/ui/
import { Progress } from '@/components/ui/
import { bundleMonitor } from '@/utils/
import { logErrorToProduction, logInfo } from '@/utils/
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/
import { Badge } from '@/components/ui/
import { Button } from '@/components/ui/
import { Progress } from '@/components/ui/
import { bundleMonitor } from '@/utils/
import { logErrorToProduction, logInfo } from '@/utils/
import { Card, CardContent, CardHeader, CardTitle } from '@/components / ui /
import { Badge } from '@/components / ui /
import { Button } from '@/components / ui /
import { Progress } from '@/components / ui /
import { bundle_monitor } from '@/utils /
import { logErrorToProduction, log_info } from '@/utils /
        entry.name.includes('/_next/static/
        name: entry.name.split('/
    return parse_float (bytes /
                {metrics.fcp ? `${metrics.fcp.toFixed(0)}ms` : any
                {metrics.lcp ? `${metrics.lcp.toFixed(0)}ms` : any
                {metrics && metrics.fcp ? `${metrics && metrics.fcp.toFixed(0)}ms` : any
                {metrics && metrics.lcp ? `${metrics && metrics.lcp.toFixed(0)}ms` : any