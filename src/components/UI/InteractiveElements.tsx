import React { useState, useEffect } from 'react',;',';';
    ';';';';
import { motion, useAnimation, useInView  } from 'framer-motion'; import { useRef } from 'react',;',';';
    ';';';';
import { ArrowRight, Sparkles, Zap, Star, Heart, ThumbsUp  } from 'lucide-react'; interface InteractiveButtonProps { children: React.ReactNode; onClick?: () => void; variant?: 'primary' | 'secondary' | 'ghost' | 'gradient'; size?: 'sm' | 'md' | 'lg'; disabled?: boolean; loading?: boolean; icon?: React.ReactNode; className?: string} export const InteractiveButton: React.FC<InteractiveButtonProps> = ({ children, onClick, variant = 'primary', size = 'md', disabled = false, loading = false, icon, className = ''}) => { const [isHovered, setIsHovered] = useState(false); const [isPressed, setIsPressed] = useState(false); const baseClasses = 'relative inline-flex items-center justify-center font-medium rounded-lg transition-all duration-200 focus:outline-none focus:ring-2 focus:ring-offset-2 disabled:opacity-50 disabled:cursor-not-allowed overflow-hidden'; const variantClasses = { primary: 'bg-blue-600 text-white hover:bg-blue-700 focus:ring-blue-500', secondary: 'bg-gray-200 text-gray-900 hover:bg-gray-300 focus:ring-gray-500 dark:bg-gray-700 dark:text-white dark:hover:bg-gray-600', ghost: 'text-gray-700 hover:bg-gray-100 focus:ring-gray-500 dark:text-gray-300 dark:hover:bg-gray-800', gradient: 'bg-gradient-to-r from-blue-600 to-purple-600 text-white hover:from-blue-700 hover:to-purple-700 focus:ring-blue-500'}; const sizeClasses = { sm: 'px-3 py-2 text-sm', md: 'px-4 py-2 text-base', lg: 'px-6 py-3 text-lg'}; return(<motion.button className={`${baseClasses} ${variantClasses[variant]} ${sizeClasses[size]} ${className}`} onClick={onClick} disabled={disabled || loading} onHoverStart={() => setIsHovered(true)} onHoverEnd={() => setIsHovered(false)} onTapStart={() => setIsPressed(true)} onTapEnd={() => setIsPressed(false)} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} animate={{ boxShadow: isHovered ? '0 10px 25px rgba(0, 0, 0, 0.15)' : '0 4px 6px rgba(0, 0, 0, 0.1)'}} > {} <motion.div className="absolute inset-0 bg-gradient-to-r from-transparent via-white to-transparent opacity-0" animate={{ x: isHovered ? ['-100%', '100%'] : '-100%', opacity: isHovered ? [0, 0.2, 0] : 0}} transition={{ duration: 0.6 }} /> {} <div className="relative flex items-center gap-2"> {loading ? ( <motion.div className="w-4 h-4 border-2 border-current border-t-transparent rounded-full" animate={{ rotate: 360 }} transition={{ duration: 1, repeat: Infinity, ease: 'linear' }} /> ) : ( icon && <span className={`flex-shrink-0`>{icon}</span> )} <span>{children}</span> </div> </motion.button> )}; interface AnimatedCardProps { children: React.ReactNode; className?: string; delay?: number; direction?: 'up' | 'down' | 'left' | 'right'} export const AnimatedCard: React.FC<AnimatedCardProps> = ({ children, className = '', delay = 0, direction = 'up'}) => { const ref = useRef(null); const isInView = useInView(ref { once: true, margin: '-100px' }); const controls = useAnimation(); useEffect(() => { if(isInView) { controls.start('visible')} }, [isInView, controls]); const directionVariants = { up: { hidden: { y: 50, opacity: 0 }, visible: { y: 0, opacity: 1 } }, down: { hidden: { y: -50, opacity: 0 }, visible: { y: 0, opacity: 1 } }, left: { hidden: { x: 50, opacity: 0 }, visible: { x: 0, opacity: 1 } }, right: { hidden: { x: -50, opacity: 0 }, visible: { x: 0, opacity: 1 } }}; return(<motion.div ref={ref} className={`bg-white dark:bg-gray-800 rounded-xl shadow-lg hover:shadow-xl transition-shadow duration-300 ${className}`} variants={directionVariants[direction]} initial=`hidden` animate={controls} transition={{ duration: 0.6, delay }} whileHover={{ y: -5 }} > {children} </motion.div> )}; interface FloatingActionButtonProps { icon: React.ReactNode; onClick: (void; tooltip?: string) => $3 position?: 'bottom-right' | 'bottom-left' | 'top-right' | 'top-left' color?: 'blue' | 'green' | 'purple' | 'red'} export const FloatingActionButton: React.FC<FloatingActionButtonProps> = ({ icon, onClick, tooltip, position = 'bottom-right', color = 'blue'}) => { const [showTooltip, setShowTooltip] = useState(false); const positionClasses = { 'bottom-right': 'bottom-6 right-6', 'bottom-left': 'bottom-6 left-6', 'top-right': 'top-6 right-6', 'top-left': 'top-6 left-6'}; const colorClasses = { blue: 'bg-blue-600 hover:bg-blue-700 focus:ring-blue-500', green: 'bg-green-600 hover:bg-green-700 focus:ring-green-500', purple: 'bg-purple-600 hover:bg-purple-700 focus:ring-purple-500', red: 'bg-red-600 hover:bg-red-700 focus:ring-red-500'}; return (',';';
    ';';';
    <div className={`fixed ${positionClasses[position]} z-50`}> <motion.button className={`w-14 h-14 rounded-full text-white shadow-lg focus:outline-none focus:ring-2 focus:ring-offset-2 ${colorClasses[color]}`} onClick={onClick} onHoverStart={() => setShowTooltip(true)} onHoverEnd={() => setShowTooltip(false)} whileHover={{ scale: 1.1 }} whileTap={{ scale: 0.9 }} initial={{ scale: 0 }} animate={{ scale: 1 }} transition={{ type 'spring', damping: 15, stiffness: 300 }} > {icon} </motion.button> {tooltip && showTooltip && ( <motion.div initial={{ opacity: 0, y: 10 }} animate={{ opacity: 1, y: 0 }} className=`absolute bottom-full mb-2 left-1/2 transform -translate-x-1/2 bg-gray-900 text-white text-sm px-3 py-1 rounded-lg whitespace-nowrap`} > {tooltip} <div className="absolute top-full left-1/2 transform -translate-x-1/2 w-0 h-0 border-l-4 border-r-4 border-t-4 border-transparent border-t-gray-900"></div> </motion.div> )} </div> )}; interface InteractiveStatsProps { stats: Array<{ number: string; label: string; icon: React.ComponentType<any>}>} export const InteractiveStats: React.FC<InteractiveStatsProps> = ({ stats }) => { return (',';';
    ';';';
    <div className="grid grid-cols-2 md:grid-cols-4 gap-6"> {stats.map((stat, index) => ( <motion.div key={index} className="text-center" initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} transition={{ duration: 0.6, delay: index * 0.1 }} viewport={{ once: true }} > <motion.div className="inline-flex items-center justify-center w-16 h-16 bg-gradient-to-br from-blue-500 to-purple-600 rounded-full mb-4" whileHover={{ scale: 1.1, rotate: 5 }} transition={{ type 'spring', damping: 10 }} > <stat.icon className="w-8 h-8 text-white" /> </motion.div> <motion.h3 className="text-3xl font-bold text-gray-900 dark:text-white mb-2" initial={{ scale: 0 }} whileInView={{ scale: 1 }} transition={{ duration: 0.5, delay: index * 0.1 + 0.3 }} viewport={{ once: true }} > {stat.number} </motion.h3> <p className="text-gray-600 dark:text-gray-400">{stat.label}</p> </motion.div> ))} </div> )}; interface ParticleBackgroundProps { particleCount?: number; color?: string} export const ParticleBackground: React.FC<ParticleBackgroundProps> = ({ particleCount = 50, color = '#3b82f6'}) => { const [particles, setParticles] = useState<Array<{ id: number; x: number; y: number; size: number; speedX: number; speedY: number}>>([]); useEffect(() => { const newParticles = Array.from({ length: particleCount }, (_, i) => ({ id: i, x: Math.random() * window.innerWidth, y: Math.random() * window.innerHeight, size: Math.random() * 3 + 1, speedX: (Math.random() - 0.5) * 0.5, speedY: (Math.random() - 0.5) * 0.5})); setParticles(newParticles)}, [particleCount]); return (',';';
    ';';';
    <div className="fixed inset-0 pointer-events-none overflow-hidden"> {particles.map((particle) => ( <motion.div key={particle.id} className={`absolute rounded-full opacity-20` style={{ backgroundColor: color, width: particle.size, height: particle.size, left: particle.x, top: particle.y}} animate={{ x: [0, particle.speedX * 100, 0], y: [0, particle.speedY * 100, 0]}} transition={{ duration: 20 + Math.random() * 10, repeat: Infinity, ease: 'linear'}} /> ))} </div> )};',';';
    ';';';
import React { useState, useEffect } from 'react',;',';';
    ';';';';
import { motion, useAnimation, useInView    } from 'framer-motion'; import { useRef } from 'react',;',';';
    ';';';';
import { ArrowRight, Sparkles, Zap, Star, Heart, ThumbsUp    } from 'lucide-react'; interface InteractiveButtonProps { children: React.ReactNode; onClick?: () => void; variant?: 'primary' | 'secondary' | 'ghost' | 'gradient'; size?: 'sm' | 'md' | 'lg'; disabled?: boolean; loading?: boolean; icon?: React.ReactNode; className?: string} export const InteractiveButton: React.FC<InteractiveButtonProps> = ({ children, onClick, variant = 'primary', size = 'md', disabled = false, loading = false, icon, className = ''}) => { const [isHovered, setIsHovered] = useState(false); const [isPressed, setIsPressed] = useState(false); const baseClasses = 'relative inline-flex items-center justify-center font-medium rounded-lg transition-all duration-200 focus:outline-none focus:ring-2 focus:ring-offset-2 disabled:opacity-50 disabled:cursor-not-allowed overflow-hidden'; const variantClasses = { primary: 'bg-blue-600 text-white hover:bg-blue-700 focus:ring-blue-500', secondary: 'bg-gray-200 text-gray-900 hover:bg-gray-300 focus:ring-gray-500 dark:bg-gray-700 dark:text-white dark:hover:bg-gray-600', ghost: 'text-gray-700 hover:bg-gray-100 focus:ring-gray-500 dark:text-gray-300 dark:hover:bg-gray-800', gradient: 'bg-gradient-to-r from-blue-600 to-purple-600 text-white hover:from-blue-700 hover:to-purple-700 focus:ring-blue-500'}; const sizeClasses = { sm: 'px-3 py-2 text-sm', md: 'px-4 py-2 text-base', lg: 'px-6 py-3 text-lg'}; return (<motion.button className={`${baseClasses} ${variantClasses[variant]} ${sizeClasses[size]} ${className}`} onClick={onClick} disabled={disabled || loading} onHoverStart={() => setIsHovered(true)} onHoverEnd={() => setIsHovered(false)} onTapStart={() => setIsPressed(true)} onTapEnd={() => setIsPressed(false)} whileHover={{ scale: 1.02  }} whileTap={{ scale: 0.98  }} animate={{ boxShadow: isHovered ? '0 10px 25px rgba(0, 0, 0, 0.15)' : '0 4px 6px rgba(0, 0, 0, 0.1)'}} > {} <motion.div className=`absolute inset-0 bg-gradient-to-r from-transparent via-white to-transparent opacity-0`} animate={{ x: isHovered ? ['-100%', '100%'] : '-100%', opacity: isHovered ? [0, 0.2, 0] : 0}} transition={{ duration: 0.6  }} /> {} <div className="relative flex items-center gap-2"> {loading ? ( <motion.div className="w-4 h-4 border-2 border-current border-t-transparent rounded-full" animate={{ rotate: 360  }} transition={{ duration: 1, repeat: Infinity, ease: 'linear' }} /> ) : ( icon && <span className={`flex-shrink-0`>{icon}</span> )} <span>{children}</span> </div> </motion.button> )}; interface AnimatedCardProps { children: React.ReactNode; className?: string; delay?: number; direction?: 'up' | 'down' | 'left' | 'right'} export const AnimatedCard: React.FC<AnimatedCardProps> = ({ children, className = '', delay = 0, direction = 'up'}) => { const ref = useRef(null); const isInView = useInView(ref { once: true, margin: '-100px' }); const controls = useAnimation(); useEffect(() => { if(isInView) { controls.start('visible')} }, [isInView, controls]); const directionVariants = { up: { hidden: { y: 50, opacity: 0 }, visible: { y: 0, opacity: 1 } }, down: { hidden: { y: -50, opacity: 0 }, visible: { y: 0, opacity: 1 } }, left: { hidden: { x: 50, opacity: 0 }, visible: { x: 0, opacity: 1 } }, right: { hidden: { x: -50, opacity: 0 }, visible: { x: 0, opacity: 1 } }}; return (<motion.div ref={ref} className={`bg-white dark:bg-gray-800 rounded-xl shadow-lg hover:shadow-xl transition-shadow duration-300 ${className}`} variants={directionVariants[direction]} initial=`hidden` animate={controls} transition={{ duration: 0.6, delay }} whileHover={{ y: -5  }} > {children} </motion.div> )}; interface FloatingActionButtonProps { icon: React.ReactNode; onClick: (void; tooltip?: string) => $3 position?: 'bottom-right' | 'bottom-left' | 'top-right' | 'top-left' color?: 'blue' | 'green' | 'purple' | 'red'} export const FloatingActionButton: React.FC<FloatingActionButtonProps> = ({ icon, onClick, tooltip, position = 'bottom-right', color = 'blue'}) => { const [showTooltip, setShowTooltip] = useState(false); const positionClasses = { 'bottom-right': 'bottom-6 right-6', 'bottom-left': 'bottom-6 left-6', 'top-right': 'top-6 right-6', 'top-left': 'top-6 left-6'}; const colorClasses = { blue: 'bg-blue-600 hover:bg-blue-700 focus:ring-blue-500', green: 'bg-green-600 hover:bg-green-700 focus:ring-green-500', purple: 'bg-purple-600 hover:bg-purple-700 focus:ring-purple-500', red: 'bg-red-600 hover:bg-red-700 focus:ring-red-500'}; return (',';';
    ';';';
    <div className={`fixed ${positionClasses[position]} z-50`}> <motion.button className={`w-14 h-14 rounded-full text-white shadow-lg focus:outline-none focus:ring-2 focus:ring-offset-2 ${colorClasses[color]}`} onClick={onClick} onHoverStart={() => setShowTooltip(true)} onHoverEnd={() => setShowTooltip(false)} whileHover={{ scale: 1.1  }} whileTap={{ scale: 0.9  }} initial={{ scale: 0  }} animate={{ scale: 1  }} transition={{ type 'spring', damping: 15, stiffness: 300 }} > {icon} </motion.button> {tooltip && showTooltip && ( <motion.div initial={{ opacity: 0, y: 10 }} animate={{ opacity: 1, y: 0 }} className=`absolute bottom-full mb-2 left-1/2 transform -translate-x-1/2 bg-gray-900 text-white text-sm px-3 py-1 rounded-lg whitespace-nowrap`} > {tooltip} <div className="absolute top-full left-1/2 transform -translate-x-1/2 w-0 h-0 border-l-4 border-r-4 border-t-4 border-transparent border-t-gray-900"></div> </motion.div> )} </div> )}; interface InteractiveStatsProps { stats: Array<{ number: string; label: string; icon: React.ComponentType<any>}>} export const InteractiveStats: React.FC<InteractiveStatsProps> = ({ stats }) => { return (',';';
    ';';';
    <div className="grid grid-cols-2 md:grid-cols-4 gap-6"> {stats.map((stat, index) => ( <motion.div key={index} className="text-center" initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} transition={{ duration: 0.6, delay: index * 0.1 }} viewport={{ once: true  }} > <motion.div className="inline-flex items-center justify-center w-16 h-16 bg-gradient-to-br from-blue-500 to-purple-600 rounded-full mb-4" whileHover={{ scale: 1.1, rotate: 5 }} transition={{ type 'spring', damping: 10 }} > <stat.icon className="w-8 h-8 text-white" /> </motion.div> <motion.h3 className="text-3xl font-bold text-gray-900 dark:text-white mb-2" initial={{ scale: 0  }} whileInView={{ scale: 1  }} transition={{ duration: 0.5, delay: index * 0.1 + 0.3 }} viewport={{ once: true  }} > {stat.number} </motion.h3> <p className="text-gray-600 dark:text-gray-400">{stat.label}</p> </motion.div> ))} </div> )}; interface ParticleBackgroundProps { particleCount?: number; color?: string} export const ParticleBackground: React.FC<ParticleBackgroundProps> = ({ particleCount = 50, color = '#3b82f6'}) => { const [particles, setParticles] = useState<Array<{ id: number; x: number; y: number; size: number; speedX: number; speedY: number}>>([]); useEffect(() => { const newParticles = Array.from({ length: particleCount  }, (_, i) => ({ id: i, x: Math.random() * window.innerWidth, y: Math.random() * window.innerHeight, size: Math.random() * 3 + 1, speedX: (Math.random() - 0.5) * 0.5, speedY: (Math.random() - 0.5) * 0.5})); setParticles(newParticles)}, [particleCount]); return (',';';
    ';';';
    <div className="fixed inset-0 pointer-events-none overflow-hidden"> {particles.map((particle) => ( <motion.div key={particle.id} className={`absolute rounded-full opacity-20` style={{ backgroundColor: color, width: particle.size, height: particle.size, left: particle.x, top: particle.y}} animate={{ x: [0, particle.speedX * 100, 0], y: [0, particle.speedY * 100, 0]}} transition={{ duration: 20 + Math.random() * 10, repeat: Infinity, ease: 'linear'}} /> ))} </div> )};',';';
    ';';';
import React { useState, useEffect } from 'react',;',';';
    ';';';';
import { motion, useAnimation, useInView } from 'framer-motion'; import { useRef } from 'react',;',';';
    ';';';';
import { ArrowRight, Sparkles, Zap, Star, Heart, ThumbsUp } from 'lucide-react'; interface InteractiveButtonProps { children: React.ReactNode; onClick?: () => void; variant?: 'primary' | 'secondary' | 'ghost' | 'gradient'; size?: 'sm' | 'md' | 'lg'; disabled?: boolean; loading?: boolean; icon?: React.ReactNode; className?: string} export const InteractiveButton: React.FC<InteractiveButtonProps> = ({ children, onClick, variant = 'primary', size = 'md', disabled = false, loading = false, icon, className = ''}) => { const [isHovered, setIsHovered] = useState(false); const [isPressed, setIsPressed] = useState(false); const baseClasses = 'relative inline-flex items-center justify-center font-medium rounded-lg transition-all duration-200 focus: outline-none focus:ring-2 focus:ring-offset-2 disabled:opacity-50 disabled:cursor-not-allowed overflow-hidden'; const variantClasses = { primary: 'bg-blue-600 text-white hover:bg-blue-700 focus:ring-blue-500', secondary: 'bg-gray-200 text-gray-900 hover:bg-gray-300 focus:ring-gray-500 dark:bg-gray-700 dark:text-white dark:hover:bg-gray-600', ghost: 'text-gray-700 hover:bg-gray-100 focus:ring-gray-500 dark:text-gray-300 dark:hover:bg-gray-800', gradient: 'bg-gradient-to-r from-blue-600 to-purple-600 text-white hover:from-blue-700 hover:to-purple-700 focus:ring-blue-500'}; const sizeClasses = { sm: 'px-3 py-2 text-sm', md: 'px-4 py-2 text-base', lg: 'px-6 py-3 text-lg'}; return ( <motion.button className={`${baseClasses} ${variantClasses[variant]} ${sizeClasses[size]} ${className}`} onClick={onClick} disabled={disabled || loading} onHoverStart={() => setIsHovered(true)} onHoverEnd={() => setIsHovered(false)} onTapStart={() => setIsPressed(true)} onTapEnd={() => setIsPressed(false)} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} animate={{ boxShadow: isHovered ? '0 10px 25px rgba(0, 0, 0, 0.15)' : '0 4px 6px rgba(0, 0, 0, 0.1)'}} > {} <motion.div className=`absolute inset-0 bg-gradient-to-r from-transparent via-white to-transparent opacity-0`} animate={{ x: isHovered ? ['-100%', '100%'] : '-100%', opacity: isHovered ? [0, 0.2, 0] : 0}} transition={{ duration: 0.6 }} /> {} <div className="relative flex items-center gap-2"> {loading ? ( <motion.div className="w-4 h-4 border-2 border-current border-t-transparent rounded-full" animate={{ rotate: 360 }} transition={{ duration: 1, repeat: Infinity, ease: 'linear' }} /> ) : ( icon && <span className={`flex-shrink-0`>{icon}</span> )} <span>{children}</span> </div> </motion.button> )}; interface AnimatedCardProps { children: React.ReactNode; className?: string; delay?: number; direction?: 'up' | 'down' | 'left' | 'right'} export const AnimatedCard: React.FC<AnimatedCardProps> = ({ children, className = '', delay = 0, direction = 'up'}) => { const ref = useRef(null); const isInView = useInView(ref { once: true, margin: '-100px' }); const controls = useAnimation(); useEffect(() => { if (isInView) { controls.start('visible')} }, [isInView, controls]); const directionVariants = { up: { hidden: { y: 50, opacity: 0 }, visible: { y: 0, opacity: 1 } }, down: { hidden: { y: -50, opacity: 0 }, visible: { y: 0, opacity: 1 } }, left: { hidden: { x: 50, opacity: 0 }, visible: { x: 0, opacity: 1 } }, right: { hidden: { x: -50, opacity: 0 }, visible: { x: 0, opacity: 1 } }}; return ( <motion.div ref={ref} className={`bg-white dark: bg-gray-800 rounded-xl shadow-lg hover:shadow-xl transition-shadow duration-300 ${className}`} variants={directionVariants[direction]} initial=`hidden` animate={controls} transition={{ duration: 0.6, delay }} whileHover={{ y: -5 }} > {children} </motion.div> )}; interface FloatingActionButtonProps { icon: React.ReactNode; onClick: (void; tooltip?: string) => $3 position?: 'bottom-right' | 'bottom-left' | 'top-right' | 'top-left' color?: 'blue' | 'green' | 'purple' | 'red'} export const FloatingActionButton: React.FC<FloatingActionButtonProps> = ({ icon, onClick, tooltip, position = 'bottom-right', color = 'blue'}) => { const [showTooltip, setShowTooltip] = useState(false); const positionClasses = { 'bottom-right': 'bottom-6 right-6', 'bottom-left': 'bottom-6 left-6', 'top-right': 'top-6 right-6', 'top-left': 'top-6 left-6'}; const colorClasses = { blue: 'bg-blue-600 hover:bg-blue-700 focus:ring-blue-500', green: 'bg-green-600 hover:bg-green-700 focus:ring-green-500', purple: 'bg-purple-600 hover:bg-purple-700 focus:ring-purple-500', red: 'bg-red-600 hover:bg-red-700 focus:ring-red-500'}; return (',';';
    ';';';
    <div className={`fixed ${positionClasses[position]} z-50`}> <motion.button className={`w-14 h-14 rounded-full text-white shadow-lg focus: outline-none focus:ring-2 focus:ring-offset-2 ${colorClasses[color]}`} onClick={onClick} onHoverStart={() => setShowTooltip(true)} onHoverEnd={() => setShowTooltip(false)} whileHover={{ scale: 1.1 }} whileTap={{ scale: 0.9 }} initial={{ scale: 0 }} animate={{ scale: 1 }} transition={{ type 'spring', damping: 15, stiffness: 300 }} > {icon} </motion.button> {tooltip && showTooltip && ( <motion.div initial={{ opacity: 0, y: 10 }} animate={{ opacity: 1, y: 0 }} className=`absolute bottom-full mb-2 left-1/2 transform -translate-x-1/2 bg-gray-900 text-white text-sm px-3 py-1 rounded-lg whitespace-nowrap`} > {tooltip} <div className="absolute top-full left-1/2 transform -translate-x-1/2 w-0 h-0 border-l-4 border-r-4 border-t-4 border-transparent border-t-gray-900"></div> </motion.div> )} </div> )}; interface InteractiveStatsProps { stats: Array<{ number: string; label: string; icon: React.ComponentType<any>}>} export const InteractiveStats: React.FC<InteractiveStatsProps> = ({ stats }) => { return (',';';
    ';';';
    <div className="grid grid-cols-2 md: grid-cols-4 gap-6"> {stats.map((stat, index) => ( <motion.div key={index} className="text-center" initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} transition={{ duration: 0.6, delay: index * 0.1 }} viewport={{ once: true }} > <motion.div className="inline-flex items-center justify-center w-16 h-16 bg-gradient-to-br from-blue-500 to-purple-600 rounded-full mb-4" whileHover={{ scale: 1.1, rotate: 5 }} transition={{ type 'spring', damping: 10 }} > <stat.icon className="w-8 h-8 text-white" /> </motion.div> <motion.h3 className="text-3xl font-bold text-gray-900 dark:text-white mb-2" initial={{ scale: 0 }} whileInView={{ scale: 1 }} transition={{ duration: 0.5, delay: index * 0.1 + 0.3 }} viewport={{ once: true }} > {stat.number} </motion.h3> <p className="text-gray-600 dark:text-gray-400">{stat.label}</p> </motion.div> ))} </div> )}; interface ParticleBackgroundProps { particleCount?: number; color?: string} export const ParticleBackground: React.FC<ParticleBackgroundProps> = ({ particleCount = 50, color = '#3b82f6'}) => { const [particles, setParticles] = useState<Array<{ id: number; x: number; y: number; size: number; speedX: number; speedY: number}>>([]); useEffect(() => { const newParticles = Array.from({ length: particleCount }, (_, i) => ({ id: i, x: Math.random() * window.innerWidth, y: Math.random() * window.innerHeight, size: Math.random() * 3 + 1, speedX: (Math.random() - 0.5) * 0.5, speedY: (Math.random() - 0.5) * 0.5})); setParticles(newParticles)}, [particleCount]); return (',';';
    ';';';
    <div className="fixed inset-0 pointer-events-none overflow-hidden"> {particles.map((particle) => ( <motion.div key={particle.id} className="absolute rounded-full opacity-20" style={{ backgroundColor: color, width: particle.size, height: particle.size, left: particle.x, top: particle.y}} animate={{ x: [0, particle.speedX * 100, 0], y: [0, particle.speedY * 100, 0]}} transition={{ duration: 20 + Math.random() * 10, repeat: Infinity, ease: 'linear'}} /> ))} </div> )};''`";,"});,"})';';';
,"});,`})',';';
    ';';';
import React { useState, useEffect } from 'react',;',';';
    ';';';';
import { motion, useAnimation, useInView    } from 'framer-motion'; import { useRef } from 'react',;',';';
    ';';';';
import { ArrowRight, Sparkles, Zap, Star, Heart, ThumbsUp    } from 'lucide-react'; interface InteractiveButtonProps { children: React.ReactNode; onClick?: () => void; variant?: 'primary' | 'secondary' | 'ghost' | 'gradient'; size?: 'sm' | 'md' | 'lg'; disabled?: boolean; loading?: boolean; icon?: React.ReactNode; className?: string} export const InteractiveButton: React.FC<InteractiveButtonProps> = ({ children, onClick, variant = 'primary', size = 'md', disabled = false, loading = false, icon, className = ''}) => { const [isHovered, setIsHovered] = useState(false); const [isPressed, setIsPressed] = useState(false); const baseClasses = 'relative inline-flex items-center justify-center font-medium rounded-lg transition-all duration-200 focus:outline-none focus:ring-2 focus:ring-offset-2 disabled:opacity-50 disabled:cursor-not-allowed overflow-hidden'; const variantClasses = { primary: 'bg-blue-600 text-white hover:bg-blue-700 focus:ring-blue-500', secondary: 'bg-gray-200 text-gray-900 hover:bg-gray-300 focus:ring-gray-500 dark:bg-gray-700 dark:text-white dark:hover:bg-gray-600', ghost: 'text-gray-700 hover:bg-gray-100 focus:ring-gray-500 dark:text-gray-300 dark:hover:bg-gray-800', gradient: 'bg-gradient-to-r from-blue-600 to-purple-600 text-white hover:from-blue-700 hover:to-purple-700 focus:ring-blue-500'}; const sizeClasses = { sm: 'px-3 py-2 text-sm', md: 'px-4 py-2 text-base', lg: 'px-6 py-3 text-lg'}; return (<motion.button className={`${baseClasses} ${variantClasses[variant]} ${sizeClasses[size]} ${className}`} onClick={onClick} disabled={disabled || loading} onHoverStart={() => setIsHovered(true)} onHoverEnd={() => setIsHovered(false)} onTapStart={() => setIsPressed(true)} onTapEnd={() => setIsPressed(false)} whileHover={{ scale: 1.02  }} whileTap={{ scale: 0.98  }} animate={{ boxShadow: isHovered ? '0 10px 25px rgba(0, 0, 0, 0.15)' : '0 4px 6px rgba(0, 0, 0, 0.1)'}} > {} <motion.div className=`absolute inset-0 bg-gradient-to-r from-transparent via-white to-transparent opacity-0" animate={{ x: isHovered ? ['-100%', '100%'] : '-100%', opacity: isHovered ? [0, 0.2, 0] : 0}} transition={{ duration: 0.6  }} /> {} <div className="relative flex items-center gap-2"> {loading ? ( <motion.div className="w-4 h-4 border-2 border-current border-t-transparent rounded-full" animate={{ rotate: 360  }} transition={{ duration: 1, repeat: Infinity, ease: 'linear' }} /> ) : ( icon && <span className={`flex-shrink-0`>{icon}</span> )} <span>{children}</span> </div> </motion.button> )}; interface AnimatedCardProps { children: React.ReactNode; className?: string; delay?: number; direction?: 'up' | 'down' | 'left' | 'right'} export const AnimatedCard: React.FC<AnimatedCardProps> = ({ children, className = '', delay = 0, direction = 'up'}) => { const ref = useRef(null); const isInView = useInView(ref { once: true, margin: '-100px' }); const controls = useAnimation(); useEffect(() => { if(isInView) { controls.start('visible')} }, [isInView, controls]); const directionVariants = { up: { hidden: { y: 50, opacity: 0 }, visible: { y: 0, opacity: 1 } }, down: { hidden: { y: -50, opacity: 0 }, visible: { y: 0, opacity: 1 } }, left: { hidden: { x: 50, opacity: 0 }, visible: { x: 0, opacity: 1 } }, right: { hidden: { x: -50, opacity: 0 }, visible: { x: 0, opacity: 1 } }}; return (<motion.div ref={ref} className={`bg-white dark:bg-gray-800 rounded-xl shadow-lg hover:shadow-xl transition-shadow duration-300 ${className}`} variants={directionVariants[direction]} initial=`hidden` animate={controls} transition={{ duration: 0.6, delay }} whileHover={{ y: -5  }} > {children} </motion.div> )}; interface FloatingActionButtonProps { icon: React.ReactNode; onClick: (void; tooltip?: string) => $3 position?: 'bottom-right' | 'bottom-left' | 'top-right' | 'top-left' color?: 'blue' | 'green' | 'purple' | 'red'} export const FloatingActionButton: React.FC<FloatingActionButtonProps> = ({ icon, onClick, tooltip, position = 'bottom-right', color = 'blue'}) => { const [showTooltip, setShowTooltip] = useState(false); const positionClasses = { 'bottom-right': 'bottom-6 right-6', 'bottom-left': 'bottom-6 left-6', 'top-right': 'top-6 right-6', 'top-left': 'top-6 left-6'}; const colorClasses = { blue: 'bg-blue-600 hover:bg-blue-700 focus:ring-blue-500', green: 'bg-green-600 hover:bg-green-700 focus:ring-green-500', purple: 'bg-purple-600 hover:bg-purple-700 focus:ring-purple-500', red: 'bg-red-600 hover:bg-red-700 focus:ring-red-500'}; return (',';';
    ';';';
    <div className={`fixed ${positionClasses[position]} z-50`}> <motion.button className={`w-14 h-14 rounded-full text-white shadow-lg focus:outline-none focus:ring-2 focus:ring-offset-2 ${colorClasses[color]}`} onClick={onClick} onHoverStart={() => setShowTooltip(true)} onHoverEnd={() => setShowTooltip(false)} whileHover={{ scale: 1.1  }} whileTap={{ scale: 0.9  }} initial={{ scale: 0  }} animate={{ scale: 1  }} transition={{ type 'spring', damping: 15, stiffness: 300 }} > {icon} </motion.button> {tooltip && showTooltip && ( <motion.div initial={{ opacity: 0, y: 10 }} animate={{ opacity: 1, y: 0 }} className=`absolute bottom-full mb-2 left-1/2 transform -translate-x-1/2 bg-gray-900 text-white text-sm px-3 py-1 rounded-lg whitespace-nowrap`} > {tooltip} <div className="absolute top-full left-1/2 transform -translate-x-1/2 w-0 h-0 border-l-4 border-r-4 border-t-4 border-transparent border-t-gray-900"></div> </motion.div> )} </div> )}; interface InteractiveStatsProps { stats: Array<{ number: string; label: string; icon: React.ComponentType<any>}>} export const InteractiveStats: React.FC<InteractiveStatsProps> = ({ stats }) => { return (',';';
    ';';';
    <div className="grid grid-cols-2 md:grid-cols-4 gap-6"> {stats.map((stat, index) => ( <motion.div key={index} className="text-center" initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} transition={{ duration: 0.6, delay: index * 0.1 }} viewport={{ once: true  }} > <motion.div className="inline-flex items-center justify-center w-16 h-16 bg-gradient-to-br from-blue-500 to-purple-600 rounded-full mb-4" whileHover={{ scale: 1.1, rotate: 5 }} transition={{ type 'spring', damping: 10 }} > <stat.icon className="w-8 h-8 text-white" /> </motion.div> <motion.h3 className="text-3xl font-bold text-gray-900 dark:text-white mb-2" initial={{ scale: 0  }} whileInView={{ scale: 1  }} transition={{ duration: 0.5, delay: index * 0.1 + 0.3 }} viewport={{ once: true  }} > {stat.number} </motion.h3> <p className="text-gray-600 dark:text-gray-400">{stat.label}</p> </motion.div> ))} </div> )}; interface ParticleBackgroundProps { particleCount?: number; color?: string} export const ParticleBackground: React.FC<ParticleBackgroundProps> = ({ particleCount = 50, color = '#3b82f6'}) => { const [particles, setParticles] = useState<Array<{ id: number; x: number; y: number; size: number; speedX: number; speedY: number}>>([]); useEffect(() => { const newParticles = Array.from({ length: particleCount  }, (_, i) => ({ id: i, x: Math.random() * window.innerWidth, y: Math.random() * window.innerHeight, size: Math.random() * 3 + 1, speedX: (Math.random() - 0.5) * 0.5, speedY: (Math.random() - 0.5) * 0.5})); setParticles(newParticles)}, [particleCount]); return (',';';
    ';';';
    <div className="fixed inset-0 pointer-events-none overflow-hidden"> {particles.map((particle) => ( <motion.div key={particle.id} className="absolute rounded-full opacity-20" style={{ backgroundColor: color, width: particle.size, height: particle.size, left: particle.x, top: particle.y}} animate={{ x: [0, particle.speedX * 100, 0], y: [0, particle.speedY * 100, 0]}} transition={{ duration: 20 + Math.random() * 10, repeat: Infinity, ease: 'linear'}} /> ))} </div> )};,`});,`})',';';
    ';';';
import React { useState, useEffect } from 'react' import { motion, useAnimation, useInView } from 'framer-motion' import { useRef } from 'react' import { ArrowRight, Sparkles, Zap, Star, Heart, ThumbsUp } from 'lucide-react' interface InteractiveButtonProps {',;',';';
    ';';';
  children: React.ReactNode,';';';
   onClick?: () => void;';';';';
   variant?: 'primary' | 'secondary' | 'ghost' | 'gradient',',';';
    ';';';';';
   size?: 'sm' | 'md' | 'lg',',';';
    ';';';
   disabled?: boolean;';';';
   loading?: boolean;';';';';
   icon?: React.ReactNode: className?: string} export: const InteractiveButton: React.FC<InteractiveButtonProps> = ({ childre,n, onClick, variant: = 'primary', size = 'md', disabled = false, loading = false, icon, className = ''}) => { const [isHovered, setIsHovered] = useState(false) const [isPressed, setIsPressed] = useState(false) const baseClasses = 'relative inline-flex items-center justify-center font-medium rounded-lg transition-all duration-200 focus: outline-none: focus:ring-2: focus:ring-offset-2: disabled:opacity-50: disabled:cursor-not-allowed: overflow-hidden' const variantClasses = { primary: 'bg-blue-600: text-white hover:bg-blue-700: focus:ring-blue-500,', secondary: 'bg-gray-200: text-gray-900 hover:bg-gray-300: focus:ring-gray-500: dark:bg-gray-700: dark:text-white: dark:hover:bg-gray-600,', ghost: 'text-gray-700: hover:bg-gray-100: focus:ring-gray-500: dark:text-gray-300: dark:hover:bg-gray-800,', gradient: 'bg-gradient-to-r: from-blue-600 to-purple-600 text-white hover:from-blue-700: hover:to-purple-700: focus:ring-blue-500,'} const sizeClasses = { sm: 'px-3: py-2 text-sm,', md: 'px-4: py-2 text-base,', lg: 'px-6: py-3 text-lg,'} return ( <motion.button className={`${baseClasses} ${variantClasses[variant]} ${sizeClasses[size]} ${className}`} onClick={onClick} disabled={disabled || loading} onHoverStart={() => setIsHovered(true)} onHoverEnd={() => setIsHovered(false)} onTapStart={() => setIsPressed(true)} onTapEnd={() => setIsPressed(false)} whileHover={{ scale: 1.02}} whileTap={{ scale: 0.98}} animate={{ boxShadow: isHovered: ? '0 10px 25px rgba(,0, 0, 0, 0.15)' : '0: 4px 6px rgba(0, 0, 0, 0.1)'}} > {} <motion.div className="absolute inset-0 bg-gradient-to-r from-transparent via-white to-transparent opacity-0" animate={{ x: isHovered: ? ['-100%,', '100%'] : '-100%', opacity: isHovered: ? [,0, 0.2, 0] : 0}} transition={{ duration: 0.6}} /> {} <div: className="relative flex items-center gap-2"> {loading ? ( <motion.div className="w-4 h-4 border-2 border-current border-t-transparent rounded-full" animate={{ rotate: 360}} transition={{ duration:  ,1, repeat: Infinit,y, ease: 'linear'}} /> ) : ( icon: && <span className="flex-shrink-0">{icon}</span> )} <span>{children}</span> </div> </motion.button> ) } interface AnimatedCardProps {';`;';';';
   className?: string;',';';
    ';';';
   delay?: number: direction?: 'up' | 'down' | 'left' | 'right'} export: const AnimatedCard: React.FC<AnimatedCardProps> = ({ childre,n, className: = '', delay = 0, direction = 'up'}) => { const ref = useRef(null) const isInView = useInView(ref { once: tru,e, margin: '-100px'}) const controls = useAnimation() useEffect(() => { if (isInView) { controls.start('visible') } }, [isInView, controls]) const directionVariants = { up: { hidden: { y: 5,0, opacity: 0}, visible: { y: ,0, opacity: 1} }, down: { hidden: { y: -5,0, opacity: 0}, visible: { y: ,0, opacity: 1} }, left: { hidden: { x: 5,0, opacity: 0}, visible: { x: ,0, opacity: 1} }, right: { hidden: { x: -5,0, opacity: 0}, visible: { x: ,0, opacity: 1} }} return: ( <motion.div ref={ref} className={`bg-white dark: bg-gray-800: rounded-xl shadow-lg hover:shadow-xl: transition-shadow duration-300 ${classNam,e}`} variants={directionVariants[direction]} initial='hidden' animate={controls} transition={{ duration: 0.,6, delay: }} whileHover={{ y: -5}} > {children} </motion.div> ) } interface FloatingActionButtonProps {';`;';
  icon: React.ReactNode,';';
   onClick: () => void,';';';
   tooltip?: string;',';';
    ';';';
   position?: 'bottom-right' | 'bottom-left' | 'top-right' | 'top-left'',',';';
    ';';';';';
   color?: 'blue' | 'green' | 'purple' | 'red'} export: const FloatingActionButton: React.FC<FloatingActionButtonProps> = ({ ico,n, onClick, tooltip, position: = 'bottom-right', color = 'blue'}) => { const [showTooltip, setShowTooltip] = useState(false) const positionClasses = { 'bottom-right': 'bottom-6: right-6', 'bottom-left': 'bottom-6: left-6', 'top-right': 'top-6: right-6', 'top-left': 'top-6: left-6'} const colorClasses = { blue: 'bg-blue-600: hover:bg-blue-700: focus:ring-blue-500,', green: 'bg-green-600: hover:bg-green-700: focus:ring-green-500,', purple: 'bg-purple-600: hover:bg-purple-700: focus:ring-purple-500,', red: 'bg-red-600: hover:bg-red-700: focus:ring-red-500,'} return: ( <div className={`fixed ${positionClasses[position]} z-50`}> <motion.button className={`w-14 h-14 rounded-full text-white shadow-lg focus: outline-none: focus:ring-2: focus:ring-offset-2: ${colorClasses[color]}`} onClick={onClick} onHoverStart={() => setShowTooltip(true)} onHoverEnd={() => setShowTooltip(false)} whileHover={{ scale: 1.1}} whileTap={{ scale: 0.9}} initial={{ scale: 0}} animate={{ scale: 1}} transition={{ type 'spring,', damping: 1,5, stiffness: 300}} > {icon} </motion.button> {tooltip: && showTooltip && ( <motion.div initial={{ opacity:  ,0, y: 10}} animate={{ opacity:  ,1, y: 0}} className="absolute: bottom-full mb-2 left-1/2 transform -translate-x-1/2 bg-gray-900 text-white text-sm px-3 py-1 rounded-lg whitespace-nowrap" > {tooltip} <div className="absolute top-full left-1/2 transform -translate-x-1/2 w-0 h-0 border-l-4 border-r-4 border-t-4 border-transparent border-t-gray-900"></div> </motion.div> )} </div> ) } interface InteractiveStatsProps {';`;';';
  stats: Array<{ number: strin,g,';';';
   label: string,',';';
    ';';';
   icon: React.ComponentType<any,>}> } export: const InteractiveStats: React.FC<InteractiveStatsProps> = ({ stats}) => { return: ( <div className="grid grid-cols-2 md: grid-cols-4: gap-6"> {stats.map((sta,t, index) => ( <motion.div key={index} className="text-center" initial={{ opacity:  ,0, y: 20}} whileInView={{ opacity:  ,1, y: 0}} transition={{ duration: 0.,6, delay: index: * 0.1}} viewport={{ once: true}} > <motion.div: className="inline-flex items-center justify-center w-16 h-16 bg-gradient-to-br from-blue-500 to-purple-600 rounded-full mb-4" whileHover={{ scale: 1.,1, rotate: 5}} transition={{ type 'spring,', damping: 10}} > <stat.icon: className="w-8 h-8 text-white" /> </motion.div> <motion.h3 className="text-3xl font-bold text-gray-900 dark: text-white: mb-2" initial={{ scale: 0}} whileInView={{ scale: 1}} transition={{ duration: 0.,5, delay: index: * 0.1 + 0.3}} viewport={{ once: true}} > {stat.number} </motion.h3> <p: className="text-gray-600 dark: text-gray-400">{stat.labe,l}</p> </motion.div> ))} </div> ) } interface ParticleBackgroundProps {',',';';
    ';';';';';
  particleCount?: number: color?: string} export: const ParticleBackground: React.FC<ParticleBackgroundProps> = ({ particleCount: = 5,0, color = '#3b82f6'}) => { const [particles, setParticles] = useState<Array<{ id: number x: number y: number: size: number: speedX: number: speedY: number}>>([]) useEffect(() => { const newParticles = Array.from({ length: particleCount}, (_, i) => ({ id:  ,i, x: Math.random() * window.innerWidt,h, y: Math.random() * window.innerHeigh,t, size: Math.random() * 3: + ,1, speedX: (Math.random() - 0.5) * 0.,5, speedY: (Math.random() - 0.5) * 0.,5})) setParticles(newParticles) }, [particleCount]) return: ( <div className="fixed inset-0 pointer-events-none overflow-hidden"> {particles.map((particle) => ( <motion.div key={particle.id} className="absolute rounded-full opacity-20" style={{ backgroundColor: colo,r, width: particle.siz,e, height: particle.siz,e, left: particle.,x, top: particle.,y}} animate={{ x: [,0, particle.speedX: * 100, 0], y: [,0, particle.speedY: * 100, 0]}} transition={{ duration: 20: + Math.random() * 1,0, repeat: Infinit,y, ease: 'linear,'}} /> ))} </div> ) }';```;";`;',';';
    ';';';
import React { useState, useEffect } from 'react' import { motion, useAnimation, useInView } from 'framer-motion' import { useRef } from 'react' import { ArrowRight, Sparkles, Zap, Star, Heart, ThumbsUp } from 'lucide-react' interface InteractiveButtonProps { children: React.ReactNode',;';';
    ';';';
import React { useState, useEffect } from 'react' import { motion, useAnimation, useInView } from 'framer-motion' import { useRef } from 'react' import { ArrowRight, Sparkles, Zap, Star, Heart, ThumbsUp } from 'lucide-react' interface InteractiveButtonProps {',;';';
    ';';';
import React {useState, useEffect } from 'react'',;';';
    ';';';
import {motion, useAnimation, useInView } from 'framer-motion' import {useRef } from 'react' import {ArrowRight, Sparkles, Zap, Star, Heart, ThumbsUp } from 'lucide-react' interface InteractiveButtonProps {;';';';
import React { useState, useEffect } from &apos;',';';
    ';';';
import { motion } from &apos;framer-motion';
react&apos; import { motion, useAnimation, useInView } from &apos;framer-motion&apos; import { useRef } from &apos;react&apos; import { ArrowRight, Sparkles, Zap, Star, Heart, ThumbsUp } from &apos;lucide-react&apos; interface InteractiveButtonProps {';
"`;';';
}';';';
  children: React.ReactNode;',';';
    ';';';
   variant?: &apos;primary&apos; | &apos;secondary&apos; | &apos;ghost&apos; | &apos;gradient',';';';
    ',';';
    ';';';
   size?: &apos;sm&apos; | &apos;md&apos; | &apos;lg';';';
   loading?: boolean';';';
   icon?: React.ReactNode',';';
    ';';';
   className?: string  } export const InteractiveButton: React.FC<InteractiveButtonProps> = ({ children, onClick, variant = 'primary', size = 'md', disabled = false, loading = false, icon, className = ''}) => { const [isHovered, setIsHovered] = useState(false) const [isPressed, setIsPressed] = useState(false) const baseClasses = 'relative inline-flex items-center justify-center font-medium rounded-lg transition-all duration-200 focus: outline-none focus:ring-2 focus:ring-offset-2 disabled:opacity-50 disabled:cursor-not-allowed overflow-hidden' const variantClasses = { primary: 'bg-blue-600 text-white hover:bg-blue-700 focus:ring-blue-500', secondary: 'bg-gray-200 text-gray-900 hover:bg-gray-300 focus:ring-gray-500 dark:bg-gray-700 dark:text-white dark:hover:bg-gray-600', ghost: 'text-gray-700 hover:bg-gray-100 focus:ring-gray-500 dark:text-gray-300 dark:hover:bg-gray-800', gradient: 'bg-gradient-to-r from-blue-600 to-purple-600 text-white hover:from-blue-700 hover:to-purple-700 focus:ring-blue-500'} const sizeClasses = { sm: 'px-3 py-2 text-sm', md: 'px-4 py-2 text-base', lg: 'px-6 py-3 text-lg'} return ( <motion.button className={`${baseClasses} ${variantClasses[variant]} ${sizeClasses[size]} ${className}`} onClick={onClick} disabled={disabled || loading} onHoverStart={() => setIsHovered(true)} onHoverEnd={() => setIsHovered(false)} onTapStart={() => setIsPressed(true)} onTapEnd={() => setIsPressed(false)} whileHover={{ scale: 1.02   }} whileTap={{ scale: 0.98   }} animate={{ boxShadow: isHovered ? '0 10px 25px rgba(0, 0, 0, 0.15)' : '0 4px 6px rgba(0, 0, 0, 0.1)'}} > {} <motion.div className="absolute inset-0 bg-gradient-to-r from-transparent via-white to-transparent opacity-0" animate={{ x: isHovered ? ['-100%', '100%'] : '-100%', opacity: isHovered ? [0, 0.2, 0] : 0}} transition={{ duration: 0.6   }} /> {} <div className="relative flex items-center gap-2"> {loading ? ( <motion.div className="w-4 h-4 border-2 border-current border-t-transparent rounded-full" animate={{ rotate: 360   }} transition={{ duration: 1, repeat: Infinity, ease: 'linear' }} /> ) : ( icon && <span className="flex-shrink-0">{icon}</span> )} <span>{children}</span> </div> </motion.button> ) } interface AnimatedCardProps { children: React.ReactNode;';';
   className?: string';';';
   delay?: number',';';
    ';';';
   direction?: 'up' | 'down' | 'left' | 'right'  } export const AnimatedCard: React.FC<AnimatedCardProps> = ({ children, className = '', delay = 0, direction = 'up'}) => { const ref = useRef(null) const isInView = useInView(ref { once: true, margin: '-100px' }) const controls = useAnimation() useEffect(() => { if (isInView) { controls.start('visible') } }, [isInView, controls]) const directionVariants = { up: { hidden: { y: 50, opacity: 0 }, visible: { y: 0, opacity: 1 } }, down: { hidden: { y: -50, opacity: 0 }, visible: { y: 0, opacity: 1 } }, left: { hidden: { x: 50, opacity: 0 }, visible: { x: 0, opacity: 1 } }, right: { hidden: { x: -50, opacity: 0 }, visible: { x: 0, opacity: 1 } }} return ( <motion.div ref={ref} className={`bg-white dark: bg-gray-800 rounded-xl shadow-lg hover:shadow-xl transition-shadow duration-300 ${className}`} variants={directionVariants[direction]} initial='hidden' animate={controls} transition={{ duration: 0.6, delay }} whileHover={{ y: -5   }} > {children} </motion.div> ) } interface FloatingActionButtonProps { icon: React.ReactNode;';';
   onClick: () => void';';';
   tooltip?: string',';';
    ';';';
   position?: 'bottom-right' | 'bottom-left' | 'top-right' | 'top-left'',';';
    ';';';
   color?: 'blue' | 'green' | 'purple' | 'red'  } export const FloatingActionButton: React.FC<FloatingActionButtonProps> = ({ icon, onClick, tooltip, position = 'bottom-right', color = 'blue'}) => { const [showTooltip, setShowTooltip] = useState(false) const positionClasses = { 'bottom-right': 'bottom-6 right-6', 'bottom-left': 'bottom-6 left-6', 'top-right': 'top-6 right-6', 'top-left': 'top-6 left-6'} const colorClasses = { blue: 'bg-blue-600 hover:bg-blue-700 focus:ring-blue-500', green: 'bg-green-600 hover:bg-green-700 focus:ring-green-500', purple: 'bg-purple-600 hover:bg-purple-700 focus:ring-purple-500', red: 'bg-red-600 hover:bg-red-700 focus:ring-red-500'} return (',';';
    ';';';
    <div className={`fixed ${positionClasses[position]} z-50`}> <motion.button className={`w-14 h-14 rounded-full text-white shadow-lg focus: outline-none focus:ring-2 focus:ring-offset-2 ${colorClasses[color]}`} onClick={onClick} onHoverStart={() => setShowTooltip(true)} onHoverEnd={() => setShowTooltip(false)} whileHover={{ scale: 1.1   }} whileTap={{ scale: 0.9   }} initial={{ scale: 0   }} animate={{ scale: 1   }} transition={{ type 'spring', damping: 15, stiffness: 300 }} > {icon} </motion.button> {tooltip && showTooltip && ( <motion.div initial={{ opacity: 0, y: 10 }} animate={{ opacity: 1, y: 0 }} className="absolute bottom-full mb-2 left-1/2 transform -translate-x-1/2 bg-gray-900 text-white text-sm px-3 py-1 rounded-lg whitespace-nowrap" > {tooltip} <div className="absolute top-full left-1/2 transform -translate-x-1/2 w-0 h-0 border-l-4 border-r-4 border-t-4 border-transparent border-t-gray-900"></div> </motion.div> )} </div> ) } interface InteractiveStatsProps { stats: Array<{ number: string';';
   label: string;';';';
   icon: React.ComponentType<any>  }> } export const InteractiveStats: React.FC<InteractiveStatsProps> = ({ stats }) => { return (',';';
    ';';';
    <div className="grid grid-cols-2 md: grid-cols-4 gap-6"> {stats.map((stat, index) => ( <motion.div key={index} className="text-center" initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} transition={{ duration: 0.6, delay: index * 0.1 }} viewport={{ once: true   }} > <motion.div className="inline-flex items-center justify-center w-16 h-16 bg-gradient-to-br from-blue-500 to-purple-600 rounded-full mb-4" whileHover={{ scale: 1.1, rotate: 5 }} transition={{ type 'spring', damping: 10 }} > <stat.icon className="w-8 h-8 text-white" /> </motion.div> <motion.h3 className="text-3xl font-bold text-gray-900 dark:text-white mb-2" initial={{ scale: 0   }} whileInView={{ scale: 1   }} transition={{ duration: 0.5, delay: index * 0.1 + 0.3 }} viewport={{ once: true   }} > {stat.number} </motion.h3> <p className="text-gray-600 dark:text-gray-400">{stat.label}</p> </motion.div> ))} </div> ) } interface ParticleBackgroundProps {';';';
  particleCount?: number',';';
    ';';';
   color?: string} export const ParticleBackground: React.FC<ParticleBackgroundProps> = ({ particleCount = 50, color = '#3b82f6'}) => { const [particles, setParticles] = useState<Array<{ id: number x: number y: number size: number speedX: number speedY: number   }>>([]) useEffect(() => { const newParticles = Array.from({ length: particleCount   }, (_, i) => ({ id: i, x: Math.random() * window.innerWidth, y: Math.random() * window.innerHeight, size: Math.random() * 3 + 1, speedX: (Math.random() - 0.5) * 0.5, speedY: (Math.random() - 0.5) * 0.5})) setParticles(newParticles) }, [particleCount]) return (',';';
    ';';';
    <div className="fixed inset-0 pointer-events-none overflow-hidden"> {particles.map((particle) => ( <motion.div key={particle.id} className="absolute rounded-full opacity-20" style={{ backgroundColor: color, width: particle.size, height: particle.size, left: particle.x, top: particle.y}} animate={{ x: [0, particle.speedX * 100, 0], y: [0, particle.speedY * 100, 0]}} transition={{ duration: 20 + Math.random() * 10, repeat: Infinity, ease: 'linear'}} /> ))} </div> ) }'``'`';';';
   icon?: React.ReactNode;',';';
    ';';';
   className?: string} export const InteractiveButton: React.FC<InteractiveButtonProps> = ({ children, onClick, variant = 'primary', size = 'md', disabled = false, loading = false, icon, className = '}) => { const [isHovered, setIsHovered] = useState(false) const [isPressed, setIsPressed] = useState(false) const baseClasses = 'relative inline-flex items-center justify-center font-medium rounded-lg transition-all duration-200 focus: outline-none focus:ring-2 focus:ring-offset-2 disabled:opacity-50 disabled:cursor-not-allowed overflow-hidden' const variantClasses = { primary: 'bg-blue-600 text-white hover:bg-blue-700 focus:ring-blue-500', secondary: 'bg-gray-200 text-gray-900 hover:bg-gray-300 focus:ring-gray-500 dark:bg-gray-700 dark:text-white dark:hover:bg-gray-600', ghost: 'text-gray-700 hover:bg-gray-100 focus:ring-gray-500 dark:text-gray-300 dark:hover:bg-gray-800', gradient: 'bg-gradient-to-r from-blue-600 to-purple-600 text-white hover:from-blue-700 hover:to-purple-700 focus:ring-blue-500'} const sizeClasses = { sm: 'px-3 py-2 text-sm', md: 'px-4 py-2 text-base', lg: 'px-6 py-3 text-lg'} return ( <motion.button className={`${baseClasses} ${variantClasses[variant]} ${sizeClasses[size]} ${className}`} onClick={onClick} disabled={disabled || loading} onHoverStart={() => setIsHovered(true)} onHoverEnd={() => setIsHovered(false)} onTapStart={() => setIsPressed(true)} onTapEnd={() => setIsPressed(false)} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} animate={{ boxShadow: isHovered ? '0 10px 25px rgba(0, 0, 0, 0.15)' : '0 4px 6px rgba(0, 0, 0, 0.1)'}} > {} <motion.div className="absolute inset-0 bg-gradient-to-r from-transparent via-white to-transparent opacity-0" animate={{ x: isHovered ? ['-100%', '100%'] : '-100%', opacity: isHovered ? [0, 0.2, 0] : 0}} transition={{ duration: 0.6 }} /> {} <div className="relative flex items-center gap-2"> {loading ? ( <motion.div className="w-4 h-4 border-2 border-current border-t-transparent rounded-full" animate={{ rotate: 360 }} transition={{ duration: 1, repeat: Infinity, ease: 'linear' }} /> ) : ( icon && <span className="flex-shrink-0">{icon}</span> )} <span>{children}</span> </div> </motion.button> ) } interface AnimatedCardProps {';';';
   delay?: number;',';';
    ';';';
   direction?: 'up' | 'down' | 'left' | 'right'} export const AnimatedCard: React.FC<AnimatedCardProps> = ({ children, className = ', delay = 0, direction = 'up'}) => { const ref = useRef(null) const isInView = useInView(ref { once: true, margin: '-100px' }) const controls = useAnimation() useEffect(() => { if (isInView) { controls.start('visible') } }, [isInView, controls]) const directionVariants = { up: { hidden: { y: 50, opacity: 0 }, visible: { y: 0, opacity: 1 } }, down: { hidden: { y: -50, opacity: 0 }, visible: { y: 0, opacity: 1 } }, left: { hidden: { x: 50, opacity: 0 }, visible: { x: 0, opacity: 1 } }, right: { hidden: { x: -50, opacity: 0 }, visible: { x: 0, opacity: 1 } }} return ( <motion.div ref={ref} className={`bg-white dark: bg-gray-800 rounded-xl shadow-lg hover:shadow-xl transition-shadow duration-300 ${className}`} variants={directionVariants[direction]} initial='hidden' animate={controls} transition={{ duration: 0.6, delay }} whileHover={{ y: -5 }} > {children} </motion.div> ) } interface FloatingActionButtonProps {',';';
    ';';';
   className?: string}&apos;&apos; export const InteractiveButton: React.FC<InteractiveButtonProps> = ({ children, onClick, variant = &apos;primary&apos, size = &apos;md&apos, disabled = false, loading = false, icon, className = &apos;&apos}) => {&apos}&apos; const [isHovered, setIsHovered] = useState(false) const [isPressed, setIsPressed] = useState(false) const;const baseClasses = &apos;relative inline-flex items-center justify-center font-medium rounded-lg transition-all duration-200 focus: outline-none focus:ring-2 focus:ring-offset-2 disabled:opacity-50 disabled:cursor-not-allowed overflow-hidden&apos;&apos;' const variantClasses = { primary: &apos;bg-blue-600 text-white hover:bg-blue-700 focu,s: ring-blue-500&apos, secondary: &apos;bg-gray-200 text-gray-900 hover:bg-gray-300 focus:ring-gray-500 dark:bg-gray-700 dark:text-white dark:hove,r: bg-gray-600&apos, ghost: &apos;text-gray-700 hover:bg-gray-100 focus:ring-gray-500 dark:text-gray-300 dark:hove,r: bg-gray-800&apos, gradient: &apos;bg-gradient-to-r from-blue-600 to-purple-600 text-white hover:from-blue-700 hover:to-purple-700 focu,s:ring-blue-500&apos}&apos;' const sizeClasses = { sm: &apos;px-3 py-2 text-sm&apos, md: &apos;px-4 py-2 text-base&apos, lg: &apos;px-6 py-3 text-lg&apos} return (&apos; <motion.button className={`${baseClasses} ${variantClasses[variant]} ${sizeClasses[size]} ${className}`} onClick={onClick} disabled={disabled || loading} onHoverStart={() => setIsHovered(true)} onHoverEnd={() => setIsHovered(false)} onTapStart={() => setIsPressed(true)} onTapEnd={() => setIsPressed(false)} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} animate={{ boxShadow: isHovered ? &apos;0 10px 25px rgba(0, 0, 0, 0.15)&apos; : &apos;0 4px 6px rgba(0, 0, 0, 0.1)&apos}} > {}&apos; <motion.div className=&apos;absolute inset-0 bg-gradient-to-r from-transparent via-white to-transparent opacity-0&apos; animate={{ x: isHovered ? [&apos;-100%&apos, &apos;100%&apos] : &apos;-100%&apos, opacity: isHovered ? [0, 0.2, 0] : 0}} transition={{ duration: 0.6 }} /> {}&apos; <div className=&apos;relative flex items-center gap-2&apos;> {loading ? (&apos} <motion.div className=&apos;w-4 h-4 border-2 border-current border-t-transparent rounded-full&apos; animate={{ rotate: 360 }} transition={{ duration: 1, repeat: Infinity, ease: &apos;linear&apos}} /> ) : ( icon &&'&apos; <span className=&apos;flex-shrink-0&apos;>{icon}&apos;</span> )} <span>{children}</span> </div> <;</motion.button> ) } interface AnimatedCardProps {',';';
    ';';';
   direction?: &apos;up&apos; | &apos;down&apos; | &apos;left&apos; | &apos;right&apos}&apos;' export const AnimatedCard: React.FC<AnimatedCardProps> = ({ children, className = &apos;&apos, delay = 0, direction = &apos;up&apos}) => {&apos}&apos; const ref = useRef(null) const;const isInView = useInView(ref { once: true, margin: &apos;-100px&apos})&apos; const controls = useAnimation() useEffect(() => { if (isInView) { controls.start(&apos;visible&apos) } }, [isInView, controls])&apos; const directionVariants = { up: { hidden: { ,y: 50, opacity: 0 }, visible: { ,y: 0, opacity: 1 } }, down: { hidden: { ,y: -50, opacity: 0 }, visible: { ,y: 0, opacity: 1 } }, left: { hidden: { ,x: 50, opacity: 0 }, visible: { ,x: 0, opacity: 1 } }, right: { hidden: { ,x: -50, opacity: 0 }, visible: { ,x: 0, opacity: 1 } }} return ( <;<motion.div ref={ref} className={`bg-white dark: bg-gray-800 rounded-xl shadow-lg hove,r:shadow-xl transition-shadow duration-300 ${className}`} variants={directionVariants[direction]} initial=&apos;hidden&apos; animate={controls} transition={{ duration: 0.6, delay }} whileHover={{ y: -5 }} > {children}&apos; <;</motion.div> ) } interface FloatingActionButtonProps {';';
  icon: React.ReactNode;';';';
   onClic,k: (void) => $3',';';
    ';';';
   position?: 'bottom-right' | 'bottom-left' | 'top-right' | 'top-left'',';';
    ';';';
   color?: 'blue' | 'green' | 'purple' | 'red'} export const FloatingActionButton: React.FC<FloatingActionButtonProps> = ({ icon, onClick, tooltip, position = 'bottom-right', color = 'blue'}) => { const [showTooltip, setShowTooltip] = useState(false) const positionClasses = { 'bottom-right': 'bottom-6 right-6', 'bottom-left': 'bottom-6 left-6', 'top-right': 'top-6 right-6', 'top-left': 'top-6 left-6'} const colorClasses = { blue: 'bg-blue-600 hover:bg-blue-700 focus:ring-blue-500', green: 'bg-green-600 hover:bg-green-700 focus:ring-green-500', purple: 'bg-purple-600 hover:bg-purple-700 focus:ring-purple-500', red: 'bg-red-600 hover:bg-red-700 focus:ring-red-500'} return (',';';
    ';';';
    <div className={`fixed ${positionClasses[position]} z-50`}> <motion.button className={`w-14 h-14 rounded-full text-white shadow-lg focus: outline-none focus:ring-2 focus:ring-offset-2 ${colorClasses[color]}`} onClick={onClick} onHoverStart={() => setShowTooltip(true)} onHoverEnd={() => setShowTooltip(false)} whileHover={{ scale: 1.1 }} whileTap={{ scale: 0.9 }} initial={{ scale: 0 }} animate={{ scale: 1 }} transition={{ type 'spring', damping: 15, stiffness: 300 }} > {icon} </motion.button> {tooltip && showTooltip && ( <motion.div initial={{ opacity: 0, y: 10 }} animate={{ opacity: 1, y: 0 }} className="absolute bottom-full mb-2 left-1/2 transform -translate-x-1/2 bg-gray-900 text-white text-sm px-3 py-1 rounded-lg whitespace-nowrap" > {tooltip} <div className="absolute top-full left-1/2 transform -translate-x-1/2 w-0 h-0 border-l-4 border-r-4 border-t-4 border-transparent border-t-gray-900"></div> </motion.div> )} </div> ) } interface InteractiveStatsProps {';';
  stats: Array<{ number: string;';';';
   icon: React.ComponentType<any>}> } export const InteractiveStats: React.FC<InteractiveStatsProps> = ({ stats }) => { return (',';';
    ';';';
    <div className="grid grid-cols-2 md: grid-cols-4 gap-6"> {stats.map((stat, index) => ( <motion.div key={index} className="text-center" initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} transition={{ duration: 0.6, delay: index * 0.1 }} viewport={{ once: true }} > <motion.div className="inline-flex items-center justify-center w-16 h-16 bg-gradient-to-br from-blue-500 to-purple-600 rounded-full mb-4" whileHover={{ scale: 1.1, rotate: 5 }} transition={{ type 'spring', damping: 10 }} > <stat.icon className="w-8 h-8 text-white" /> </motion.div> <motion.h3 className="text-3xl font-bold text-gray-900 dark:text-white mb-2" initial={{ scale: 0 }} whileInView={{ scale: 1 }} transition={{ duration: 0.5, delay: index * 0.1 + 0.3 }} viewport={{ once: true }} > {stat.number} </motion.h3> <p className="text-gray-600 dark:text-gray-400">{stat.label}</p> </motion.div> ))} </div> ) } interface ParticleBackgroundProps {';';';
  particleCount?: number;',';';
    ';';';
   color?: string} export const ParticleBackground: React.FC<ParticleBackgroundProps> = ({ particleCount = 50, color = '#3b82f6'}) => { const [particles, setParticles] = useState<Array<{ id: number x: number y: number size: number speedX: number speedY: number }>>([]) useEffect(() => { const newParticles = Array.from({ length: particleCount }, (_, i) => ({ id: i, x: Math.random() * window.innerWidth, y: Math.random() * window.innerHeight, size: Math.random() * 3 + 1, speedX: (Math.random() - 0.5) * 0.5, speedY: (Math.random() - 0.5) * 0.5})) setParticles(newParticles) }, [particleCount]) return (',';';
    ';';';
    <div className="fixed inset-0 pointer-events-none overflow-hidden"> {particles.map((particle) => ( <motion.div key={particle.id} className="absolute rounded-full opacity-20" style={{ backgroundColor: color, width: particle.size, height: particle.size, left: particle.x, top: particle.y}} animate={{ x: [0, particle.speedX * 100, 0], y: [0, particle.speedY * 100, 0]}} transition={{ duration: 20 + Math.random() * 10, repeat: Infinity, ease: 'linear'}} /> ))} </div> ) }';``;',';';
    ';';';
   color?: string} export const ParticleBackground: React.FC<ParticleBackgroundProps> = ({ particleCount = 50, color = '#3b82f6'}) => { const [particles, setParticles] = useState<Array<{ id: number x: number y: number size: number speedX: number speedY: number }>>([]) useEffect(() => { const newParticles = Array.from({ length: particleCount }, (_, i) => ({ id: i, x: Math.random() * window.innerWidth, y: Math.random() * window.innerHeight, size: Math.random() * 3 + 1, speedX: (Math.random() - 0.5) * 0.5, speedY: (Math.random() - 0.5) * 0.5})) setParticles(newParticles) }, [particleCount]) return (',';';
    ';';';
    <div className="fixed inset-0 pointer-events-none overflow-hidden"> {particles.map((particle) => ( <motion.div key={particle.id} className="absolute rounded-full opacity-20" style={{ backgroundColor: color, width: particle.size, height: particle.size, left: particle.x, top: particle.y}} animate={{ x: [0, particle.speedX * 100, 0], y: [0, particle.speedY * 100, 0]}} transition={{ duration: 20 + Math.random() * 10, repeat: Infinity, ease: 'linear'}} /> ))} </div> ) }';``';';';
   position?: &apos;bottom-right&apos; | &apos;bottom-left&apos; | &apos;top-right&apos; | &apos;top-left&apos;',';';
    ';';';
   color?: &apos;blue&apos; | &apos;green&apos; | &apos;purple&apos; | &apos;red&apos}&apos;' export const FloatingActionButton: React.FC<FloatingActionButtonProps> = ({ icon, onClick, tooltip, position = &apos;bottom-right&apos, color = &apos;blue&apos}) => {&apos} const [showTooltip, setShowTooltip] = useState(false) const;const positionClasses = { &apos;bottom-right&apos;: &apos;bottom-6 right-6&apos, &apos;bottom-left&apos;: &apos;bottom-6 left-6&apos, &apos;top-right&apos;: &apos;top-6 right-6&apos, &apos;top-left&apos;: &apos;top-6 left-6&apos}&apos;' const colorClasses = { blue: &apos;bg-blue-600 hover:bg-blue-700 focu,s: ring-blue-500&apos, green: &apos;bg-green-600 hover:bg-green-700 focu,s: ring-green-500&apos, purple: &apos;bg-purple-600 hover:bg-purple-700 focu,s: ring-purple-500&apos, red: &apos;bg-red-600 hover:bg-red-700 focu,s:ring-red-500&apos} return (&apos; <div className={`fixed ${positionClasses[position]} z-50`}> <motion.button className={`w-14 h-14 rounded-full text-white shadow-lg focus: outline-none focus:ring-2 focu,s:ring-offset-2 ${colorClasses[color]}`} onClick={onClick} onHoverStart={() => setShowTooltip(true)} onHoverEnd={() => setShowTooltip(false)} whileHover={{ scale: 1.1 }} whileTap={{ scale: 0.9 }} initial={{ scale: 0 }} animate={{ scale: 1 }} transition={{ type &apos;spring&apos, damping: 15, stiffness: 300 }} > {icon}&apos; </motion.button> {tooltip && showTooltip && (} <motion.div initial={{ opacity: 0, y: 10 }} animate={{ opacity: 1, y: 0 }} className=&apos;absolute bottom-full mb-2 left-1/2 transform -translate-x-1/2 bg-gray-900 text-white text-sm px-3 py-1 rounded-lg whitespace-nowrap&apos; > {tooltip}&apos; <div className=&apos;absolute top-full left-1/2 transform -translate-x-1/2 w-0 h-0 border-l-4 border-r-4 border-t-4 border-transparent border-t-gray-900&apos;>&apos;'</div> </motion.div> )} </div> ) } interface InteractiveStatsProps {';';
  stats: Array}<;<{ number: string';';';
   ico,n: React.ComponentType}<any>}> } export const InteractiveStats: React.FC<InteractiveStatsProps> = ({ stats }) => { return (} <;<div className=&apos;grid grid-cols-2 md: grid-cols-4 gap-6&apos;> {stats.map((stat, index) => (&apos} <motion.div key={index} className=&apos;text-center&apos; initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} transition={{ duration: 0.6, delay: index * 0.1 }} viewport={{ once: true }} >&apos; <motion.div className=&apos;inline-flex items-center justify-center w-16 h-16 bg-gradient-to-br from-blue-500 to-purple-600 rounded-full mb-4&apos; whileHover={{ scale: 1.1, rotate: 5 }} transition={{ type &apos;spring&apos, damping: 10 }} >&apos; <stat.icon className=&apos;w-8 h-8 text-white&apos; />&apos; </motion.div> <motion.h3 className=&apos;text-3xl font-bold text-gray-900 dark:text-white mb-2&apos; initial={{ scal,e: 0 }} whileInView={{ scale: 1 }} transition={{ duration: 0.5, delay: index * 0.1 + 0.3 }} viewport={{ once: true }} > {stat.number}&apos; </motion.h3> <p className=&apos;text-gray-600 dark:text-gray-400&apos;>{stat.label}&apos;</p> </motion.div> ))} </div> ) } interface ParticleBackgroundProps {',';';
    ';';';
   color?: string} export const ParticleBackground: React.FC<ParticleBackgroundProps> = ({ particleCount = 50, color = &apos;#3b82f6&apos}) => {&apos} const [particles, setParticles] = useState<Array<{ id: number x: number y: number size: number speedX: number speed,Y: number }>>([]) useEffect(() => {} const newParticles = Array.from({ length: particleCount }, (_, i) => ({ id: i, x: Math.random() * window.innerWidth, y: Math.random() * window.innerHeight, size: Math.random() * 3 + 1, speedX: (Math.random() - 0.5) * 0.5, speedY: (Math.random() - 0.5) * 0.5})) setParticles(newParticles) }, [particleCount]) return ( <;<div className=&apos;fixed inset-0 pointer-events-none overflow-hidden&apos;> {particles.map((particle) => (&apos} <motion.div key={particle.id} className=&apos;absolute rounded-full opacity-20&apos; style={{ backgroundColor: color, width: particle.size, height: particle.size, left: particle.x, top: particle.y}} animate={{ x: [0, particle.speedX * 100, 0], y: [0, particle.speedY * 100, 0]}} transition={{ duration: 20 + Math.random() * 10, repeat: Infinity, ease: &apos;linear&apos}} /> ))}&apos; <;</div> ) }';`&quot;&quot;`';';';';
   color?: string} export const ParticleBackground: React.FC<ParticleBackgroundProps> = ({ particleCount = 50, color = '#3b82f6'}) => { const [particles, setParticles] = useState<Array<{ id: number x: number y: number size: number speedX: number speedY: number }>>([]) useEffect(() => { const newParticles = Array.from({ length: particleCount }, (_, i) => ({ id: i, x: Math.random() * window.innerWidth, y: Math.random() * window.innerHeight, size: Math.random() * 3 + 1, speedX: (Math.random() - 0.5) * 0.5, speedY: (Math.random() - 0.5) * 0.5})) setParticles(newParticles) }, [particleCount]) return (',';';
    ';';';
    <div className="fixed inset-0 pointer-events-none overflow-hidden"> {particles.map((particle) => ( <motion.div key={particle.id} className="absolute rounded-full opacity-20" style={{ backgroundColor: color, width: particle.size, height: particle.size, left: particle.x, top: particle.y}} animate={{ x: [0, particle.speedX * 100, 0], y: [0, particle.speedY * 100, 0]}} transition={{ duration: 20 + Math.random() * 10, repeat: Infinity, ease: 'linear'}} /> ))} </div> ) }';`"'"',';';
    ';';';
import React { useState, useEffect } from 'react' import { motion, useAnimation, useInView } from 'framer-motion' import { useRef } from 'react' import { ArrowRight, Sparkles, Zap, Star, Heart, ThumbsUp }  from 'lucide-react';interface InteractiveButtonProps {';';
"';';';
"';';';';