import React from 'react';

interface, SEOOptimizerProps {
  children: React.ReactNod, e;
}

const, SEOOptimize, r: React.FC<SEOOptimizerProps> = ({ childre, n }) => { 
  return < > {children }</>;
};

export, default, SEOOptimizer;
