import React, { useState, useRef, useEffect } from 'react'
import React, { useState, useRef, useEffect } from 'react'
import Image from 'next/
import { cn  } from '@/lib/
import { imageOptimization  } from '@/utils/
import { logWarn } from '@/utils/
import Image from 'next /
import { cn } from '@/lib /
import { image_optimization } from '@/utils /
import { log_warn } from '@/utils /
              wasOptimized: src.includes('/_next/
      placeholder = = 'blur' && 'backdrop-blur-sm bg-gray-200/
  aspectRatio = '16/
                