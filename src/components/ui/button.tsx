

  children,
  href,
  on_click,
  type = "button",
  variant = "primary",
  size = "md",
  class_name = "",
  disabled = false,
  style
}) => {


  }
  return (
    <button;
      type={type}
      className={classes}
      on_click={on_click}
      disabled={disabled}


    </button>;
  );
};
export { Button };
export default Button;

      style={style}
    >;
      {content}

