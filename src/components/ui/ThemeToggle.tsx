import React from 'react'; import { Sun, Moon, Monitor, Palette } from 'lucide-react'; import { useTheme, ColorScheme } from '../../context/ThemeContext'; interface ThemeToggleProps { showColorSchemes?: boolean; className?: string; } export const ThemeToggle: React.FC<ThemeToggleProps> = ({ showColorSchemes = false, className = '' }) => { const { theme, colorScheme, setTheme, setColorScheme, toggleTheme, isDark } = useTheme(); const colorSchemes: { value: ColorScheme; name: string; color: string }[] = [ { value: 'blue', name: 'Blue', color: '#0ea5e9' }, { value: 'purple', name: 'Purple', color: '#8b5cf6' }, { value: 'green', name: 'Green', color: '#10b981' }, { value: 'orange', name: 'Orange', color: '#f97316' }, { value: 'red', name: 'Red', color: '#ef4444' }, { value: 'pink', name: 'Pink', color: '#ec4899' }, ]; const getThemeIcon = () => { switch (theme) { case 'light': return <Sun className="w-4 h-4" />; case 'dark': return <Moon className="w-4 h-4" />; case 'system': return <Monitor className="w-4 h-4" />; default: return <Sun className="w-4 h-4" />; } }; return ( <div className={`flex items-center space-x-2 ${className}`}> {} <div className="relative"> <button onClick={toggleTheme} className="flex items-center space-x-2 px-3 py-2 rounded-lg bg-gray-100 dark:bg-gray-800 hover:bg-gray-200 dark:hover:bg-gray-700 transition-colors" title={`Current theme: ${theme}`} > {getThemeIcon()} <span className="text-sm font-medium capitalize">{theme}</span> </button> </div> {} {showColorSchemes && ( <div className="relative group"> <button className="flex items-center space-x-2 px-3 py-2 rounded-lg bg-gray-100 dark:bg-gray-800 hover:bg-gray-200 dark:hover:bg-gray-700 transition-colors" title="Color scheme" > <Palette className="w-4 h-4" /> <div className="w-4 h-4 rounded-full border-2 border-white dark:border-gray-800" style={{ backgroundColor: colorSchemes.find(cs => cs.value === colorScheme)?.color }} /> </button> {} <div className="absolute right-0 top-full mt-2 w-48 bg-white dark:bg-gray-800 rounded-lg shadow-lg border border-gray-200 dark:border-gray-700 opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-200 z-50"> <div className="p-2"> <div className="text-xs font-semibold text-gray-500 dark:text-gray-400 uppercase tracking-wide mb-2"> Color Scheme </div> <div className="grid grid-cols-2 gap-1"> {colorSchemes.map((scheme) => ( <button key={scheme.value} onClick={() => setColorScheme(scheme.value)} className={`flex items-center space-x-2 px-2 py-1 rounded text-sm transition-colors ${ colorScheme === scheme.value ? 'bg-blue-100 dark:bg-blue-900 text-blue-700 dark:text-blue-300' : 'hover:bg-gray-100 dark:hover:bg-gray-700 text-gray-700 dark:text-gray-300' }`} > <div className="w-3 h-3 rounded-full border border-gray-300 dark:border-gray-600" style={{ backgroundColor: scheme.color }} /> <span>{scheme.name}</span> </button> ))} </div> </div> </div> </div> )} </div> ); }; export default ThemeToggle;