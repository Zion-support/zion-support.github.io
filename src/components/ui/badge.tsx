import React from 'react';'
import { cn } from '@/lib/utils';      <div
        ref={ref}
        className={cn()
          baseClasses,
          variantClasses[variant],
          sizeClasses[size],
          className
        )}
        {...props}
      >
        {children}
      </div>
    )}
);
'
Badge.displayName = 'Badge';
;
export { Badge };
'