const listingSchema = z && z.object({;
  id: z && z.string(),;
  title: z && z.string(),;
  category: z && z.string(),;
  image: z && z.string().optional()}),;

      const maxRetries = 3;



  const sourceListings = listings;
}
  );
}
