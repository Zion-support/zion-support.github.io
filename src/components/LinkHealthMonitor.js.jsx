"import {;" Link, ExternalLink, AlertTriangle, CheckCircle, RefreshCw, Zap} from "lucide-react";"const LinkHealthMonitor = () => {" const [isOpen, setIsOpen] = useState(false); const [isMonitoring, setIsMonitoring] = useState(false); const [linkStatuses, setLinkStatuses] = useState([]); const [report, setReport] = useState(null); const [selectedFilter, setSelectedFilter] = useState("all");" // Sample data based on the analysis report" const sampleLinks = [; { url: "https://linkedin.com/company/ziontechgroup", status: "broken"," statusCode: 404, responseTime: 1200," lastChecked: new Date(), parentPage: "Homepage"," linkText: "LinkedIn"}, {" url: "https://twitter.com/ziontechgroup"," status: "external", statusCode: 200," responseTime: 800, lastChecked: new Date(), parentPage: "Homepage"," linkText: "Twitter"}, {" url: "tel:+1 302 464 0950"," status: "healthy", statusCode: 200," responseTime: 50, lastChecked: new Date(), parentPage: "Contact"," linkText: "Phone Number"}, {" url: "mailto:kleber@ziontechgroup.com"," status: "healthy", statusCode: 200," responseTime: 50, lastChecked: new Date(), parentPage: "Contact"," linkText: "Email"}, ];" useEffect(() => {" setLinkStatuses(sampleLinks); generateReport(sampleLinks); }, []); const generateReport = links => {; const totalLinks = links.length; const healthyLinks = links.filter(l => l.status === "healthy").length;" const brokenLinks = links.filter(l => l.status === "broken").length;" const externalLinks = links.filter(l => l.status === "external").length;" const avgResponseTime =" links.reduce((sum, l) => sum + (l.responseTime || 0), 0) / totalLinks; setReport({ totalLinks, healthyLinks, brokenLinks, externalLinks, averageResponseTime: avgResponseTime, lastUpdated: new Date()}); }; const startMonitoring = async () => {; 