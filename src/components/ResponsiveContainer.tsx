import React from 'react';
interface ResponsiveContainerProps {
  <ResponsiveContainerProps> = () => {
interface ResponsiveContainerProps {<ResponsiveContainerProps> = ({<'sm' | 'md' | 'lg' | 'xl' | '2xl'>('lg');
  const isTablet = useMediaQuery({ minWidth: 768, maxWidth: 1023 });
  const isDesktop = useMediaQuery({ minWidth: 1024 });
  const isLargeDesktop = useMediaQuery({ minWidth: 1280 });
  useEffect(() => {<div className={className}>{children}</div>;
  }
  const getResponsiveClasses = () => {<div className="responsivecontainer">
      {}
    <div>
  );
};
<T,>(values: {
}}}}}))