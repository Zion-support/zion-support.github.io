"import {;" AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent, AlertDialogDescription, AlertDialogFooter, AlertDialogHeader, AlertDialogTitle, } from "../components/ui/alert-dialog";"export function LanguageDetectionPopup() { const [open, setOpen] = useState(false); const { t } = useTranslation(); const { changeLanguage, currentLanguage, supportedLanguages } = useLanguage() as LanguageContextType; const [detectedLanguage, setDetectedLanguage] = useState<SupportedLanguage | null>(null); useEffect(() => { const hasVisited = safeStorage.getItem("zion_has_visited");" if (hasVisited) return;" safeStorage.setItem("zion_has_visited", "true");" " const browserLang = navigator.language.substring(0, 2) as SupportedLanguage; const isSupported = supportedLanguages.some(lang => lang.code === browserLang); if (isSupported && browserLang !== currentLanguage) { 