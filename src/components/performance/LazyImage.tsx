import React, { useState, useRef, useEffect } from 'react'; import { _motion  } from 'framer-motion'; interface LazyImageProps {';
 src: string; alt: string; className?: string; width?: number; height?: number; placeholder?: string; priority?: boolean; onLoad?: () => void; onError?: () => void;   ';';
} export const LazyImage: Reac t.FC<LazyImageProps> = ({ src, alt, className = '', width, height, placeholder = 'data: imag e/svg+xml;base64,PHN2ZyB3aWR0aD0iMjAwIiBoZWlnaHQ9IjIwMCIgeG1sbnM9Imh0dHA6Ly93d3cudzMub3JnLzIwMDAvc3ZnIj48cmVjdCB3aWR0aD0iMTAwJSIgaGVpZ2h0PSIxMDAlIiBmaWxsPSIjZjNmNGY2Ii8+PC9zdmc+', priority = false, onLoad, onError }) => { const [isLoaded, setIsLoaded] = useState<any>(false); const [isInView, setIsInView] = useState<any>(priority); const [hasError, setHasError] = useState<any>(false); const _imgRef = useRef<HTMLImageElement>(null); useEffect(() => { if(priority) return; const _observer = new IntersectionObserver(([entry]) => { if(entry.isIntersecting) { setIsInView(true); observer.disconnect(); } }, { threshold: 0.1, rootMargin: '50px' } ); if(imgRef.current) { observer.observe(imgRef.current); } return () => observer.disconnect(); }, [priority]); const _handleLoad = (props: any) => { setIsLoaded(true); onLoad?.(); }; const _handleError = (props: any) => { setHasError(true); onError?.(); }; return (<div ref={imgRef} className={`relative overflow-hidden ${className}`} style={{ width, height }} > {} {!isLoaded && !hasError && ( <motion.div className="absolute inset-0 bg-gray-200 animate-pulse flex items-center justify-center" initial={{ opacity: 1   }} animate={{ opacity: isLoade d ? 0 : 1   }} transition={{ duration: 0.3   }} > <div className="w-8 h-8 border-2 border-gray-300 border-t-blue-500 rounded-full animate-spin"  /> </motion.div> )} {} {hasError && ( <div className="absolute inset-0 bg-gray-100 flex items-center justify-center"> <div className="text-center text-gray-500"> <svg className="w-12 h-12 mx-auto mb-2" fill="none" stroke="currentColor" viewBox="0 0 24 24"> <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"  /> </svg> <p className="text-sm">Failed to load image</p> </div> </div> )} {} {isInView && !hasError && ( <motion.img src={src} alt={alt} className={`w-full h-full object-cover transition-opacity duration-300 ${isLoaded ? 'opacity-100' : 'opacity-0' }`} onLoad={handleLoad} onError={handleError} loading={priority ? 'eager' : 'lazy'} decoding="async" /> )} </div> ); };
</motion>
</HTMLImageElement>
</any>
</any>';
</LazyImageProps>;';;';import React, { useState, useRef, useEffect } from 'react'; import { _motion  } from 'framer-motion'; interface LazyImageProps {;
 "src": "string; "alt": string; className?: string; width?: number; height?: number; placeholder?: string; priority?: boolean; onLoad?: () => void; onError?: () => void; ;
"} export const "LazyImage": "React.FC<LazyImageProps> = ({ src", alt, className = '', width, height, placeholder = '"data": "imag e/svg+xml;base64",PHN2ZyB3aWR0aD0iMjAwIiBoZWlnaHQ9IjIwMCIgeG1sbnM9Imh0dHA6Ly93d3cudzMub3JnLzIwMDAvc3ZnIj48cmVjdCB3aWR0aD0iMTAwJSIgaGVpZ2h0PSIxMDAlIiBmaWxsPSIjZjNmNGY2Ii8+PC9zdmc+', priority = false, onLoad, onError }) => { const [isLoaded, setIsLoaded] = useState<any>(false); const [isInView, setIsInView] = useState<any>(priority); const [hasError, setHasError] = useState<any>(false); const _imgRef = useRef<HTMLImageElement>(null); useEffect(() => { if(priority) return; const _observer = new IntersectionObserver(([entry]) => { if(entry.isIntersecting) { setIsInView(true); observer.disconnect(); } }, { "threshold": "0.1", "rootMargin": '50px' } ); if(imgRef.current) { observer.observe(imgRef.current); } return () => observer.disconnect(); }, [priority]); const _handleLoad = ("props": "any) => { setIsLoaded(true); onLoad?.(); "}; const _handleError = ("props": "any) => { setHasError(true); onError?.(); "}; return (<div ref={imgRef} className={`relative overflow-hidden ${className}`} style={{ width, height }} > {} {!isLoaded && !hasError && ( <motion.div className="absolute inset-0 bg-gray-200 animate-pulse flex items-center justify-center" initial={{ "opacity": "1   "}} animate={{ "opacity": "isLoade d ? 0 : 1   "}} transition={{ "duration": "0.3   "}} > <div className="w-8 h-8 border-2 border-gray-300 border-t-blue-500 rounded-full animate-spin"  /> </motion.div> )} {} {hasError && ( <div className="absolute inset-0 bg-gray-100 flex items-center justify-center"> <div className="text-center text-gray-500"> <svg className="w-12 h-12 mx-auto mb-2" fill="none" stroke="currentColor" viewBox="0 0 24 24"> <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"  /> </svg> <p className="text-sm">Failed to load image</p> </div> </div> )} {} {isInView && !hasError && ( <motion.img src={src} alt={alt} className={`w-full h-full object-cover transition-opacity duration-300 ${isLoaded ? 'opacity-100' : 'opacity-0' }`} onLoad={handleLoad} onError={handleError} loading={priority ? 'eager' : 'lazy'} decoding="async" /> )} </div> ); };
;
</motion>;
</HTMLImageElement>;
</any>;
</any>;
</any>;
</LazyImageProps>;
import React, { useState, useRef, useEffect } from 'react'; import { _motion  } from 'framer-motion'; interface LazyImageProps {
 src: string; alt: string; className?: string; width?: number; height?: number; placeholder?: string; priority?: boolean; onLoad?: () => void; onError?: () => void;   
} export const LazyImage: React.FC<LazyImageProps> = ({ src, alt, className = '', width, height, placeholder = 'data: imag e/svg+xml;base64,PHN2ZyB3aWR0aD0iMjAwIiBoZWlnaHQ9IjIwMCIgeG1sbnM9Imh0dHA6Ly93d3cudzMub3JnLzIwMDAvc3ZnIj48cmVjdCB3aWR0aD0iMTAwJSIgaGVpZ2h0PSIxMDAlIiBmaWxsPSIjZjNmNGY2Ii8+PC9zdmc+', priority = false, onLoad, onError }) => { const [isLoaded, setIsLoaded] = useState<any>(false); const [isInView, setIsInView] = useState<any>(priority); const [hasError, setHasError] = useState<any>(false); const _imgRef = useRef<HTMLImageElement>(null); useEffect(() => { if(priority) return; const _observer = new IntersectionObserver(([entry]) => { if(entry.isIntersecting) { setIsInView(true); observer.disconnect(); } }, { threshold: 0.1, rootMargin: '50px' }
    ); if(imgRef.current) { observer.observe(imgRef.current); } return () => observer.disconnect(); }, [priority]); const _handleLoad = (props: any) => { setIsLoaded(true); onLoad?.(); }; const _handleError = (props: any) => { setHasError(true); onError?.(); }; return (
        <div ref={imgRef} className={`relative overflow-hidden ${className}`} style={{ width, height }} > {} {!isLoaded && !hasError && ( <motion.div className="absolute inset-0 bg-gray-200 animate-pulse flex items-center justify-center" initial={{ opacity: 1   }} animate={{ opacity: isLoade d ? 0 : 1   }} transition={{ duration: 0.3   }} > <div className="w-8 h-8 border-2 border-gray-300 border-t-blue-500 rounded-full animate-spin"  /> </motion.div> )} {} {hasError && ( <div className="absolute inset-0 bg-gray-100 flex items-center justify-center"> <div className="text-center text-gray-500"> <svg className="w-12 h-12 mx-auto mb-2" fill="none" stroke="currentColor" viewBox="0 0 24 24"> <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"  /> </svg> <p className="text-sm">Failed to load image</p> </div> </div> )} {} {isInView && !hasError && ( <motion.img src={src} alt={alt} className={`w-full h-full object-cover transition-opacity duration-300 ${isLoaded ? 'opacity-100' : 'opacity-0' }`} onLoad={handleLoad} onError={handleError} loading={priority ? 'eager' : 'lazy'} decoding="async" /> )} </div>
    ); };
</motion>
</HTMLImageElement>
</any>
</any>
</any>
</LazyImageProps>
import React,{ useState,useRef,useEffect } from &apos;react&apos;; import { _motion } from &apos;framer-motion&apos;; interface LazyImageProps { src: string; alt: string; className?: string; width?: number; height?: number; placeholder?: string; priority?: boolean; onLoad?: () => void; onError?: () => void; &apos;;&apos;} export const LazyImage: Reac t.FC<LazyImageProps> = ({ src,alt,className = &quot;,width,height,placeholder = &apos;data: imag e/svg+xml;base64,PHN2ZyB3aWR0aD0iMjAwIiBoZWlnaHQ9IjIwMCIgeG1sbnM9Imh0dHA6Ly93d3cudzMub3JnLzIwMDAvc3ZnIj48cmVjdCB3aWR0aD0iMTAwJSIgaGVpZ2h0PSIxMDAlIiBmaWxsPSIjZjNmNGY2Ii8+PC9zdmc+&apos;,priority = false,onLoad,onError }) => { const [isLoaded,setIsLoaded] = useState<any>(false); const [isInView,setIsInView] = useState<any>(priority); const [hasError,setHasError] = useState<any>(false); const _imgRef = useRef<HTMLImageElement>(null); useEffect(() => { if(priority) return; const _observer = new IntersectionObserver(([entry]) => { if(entry.isIntersecting) { setIsInView(true); observer.disconnect()} },{ threshold: 0.1,rootMargin: &apos;50px&apos; } ); if(imgRef.current) { observer.observe(imgRef.current)} return () => observer.disconnect()},[priority]); const _handleLoad = (props) => { setIsLoaded(true); onLoad?.()}; const _handleError = (props) => { setHasError(true); onError?.()}; return (<div ref={imgRef} className={`relative overflow-hidden ${className}`} style={{ width,height }} > {} {!isLoaded && !hasError && ( <motion.div className=&quot;absolute inset-0 bg-gray-200 animate-pulse flex items-center justify-center&quot; initial={{ opacity: 1 }} animate={{ opacity: isLoade d ? 0 : 1 }} transition={{ duration: 0.3 }} > <div className=&quot;w-8 h-8 border-2 border-gray-300 border-t-blue-500 rounded-full animate-spin&quot; /> </motion.div> )} {} {hasError && ( <div className=&quot;absolute inset-0 bg-gray-100 flex items-center justify-center&quot;> <div className=&quot;text-center text-gray-500&quot;> <svg className=&quot;w-12 h-12 mx-auto mb-2&quot; fill=&quot;none&quot; stroke=&quot;currentColor&quot; viewBox=&quot;0 0 24 24&quot;> <path strokeLinecap=&quot;round&quot; strokeLinejoin=&quot;round&quot; strokeWidth={2} d=&quot;M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z&quot; /> </svg> <p className=&quot;text-sm&quot;>Failed to load image</p> </div> </div> )} {} {isInView && !hasError && ( <motion.img src={src} alt={alt} className={`w-full h-full object-cover transition-opacity duration-300 ${isLoaded ? &apos;opacity-100&apos; : &apos;opacity-0&apos; }`} onLoad={handleLoad} onError={handleError} loading={priority ? &apos;eager&apos; : &apos;lazy&apos;} decoding=&quot;async&quot; /> )} </div> )}; </motion> </HTMLImageElement> </any> </any> </LazyImageProps>;&apos;;&apos;;
const React,{ useState,useRef,useEffect } from "react"; import { _motion } from "framer-motion"; interface LazyImageProps { src: string; alt: string; className?: string; width?: number; height?: number; placeholder?: string; priority?: boolean; onLoad?: () => void; onError?: () => void; ";"} export const LazyImage: Reac t.FC<LazyImageProps> = ({ src,alt,className = ",width,height,placeholder = "data: imag e/svg+xml;base64,PHN2ZyB3aWR0aD0iMjAwIiBoZWlnaHQ9IjIwMCIgeG1sbnM9Imh0dHA6Ly93d3cudzMub3JnLzIwMDAvc3ZnIj48cmVjdCB3aWR0aD0iMTAwJSIgaGVpZ2h0PSIxMDAlIiBmaWxsPSIjZjNmNGY2Ii8+PC9zdmc+",priority = false,onLoad,onError }) => { const [isLoaded,setIsLoaded] = useState<any>(false); const [isInView,setIsInView] = useState<any>(priority); const [hasError,setHasError] = useState<any>(false); const _imgRef = useRef<HTMLImageElement>(null); useEffect(() => { if(priority) return; const _observer = new IntersectionObserver(([entry]) => { if(entry.isIntersecting) { setIsInView(true); observer.disconnect()} },{ threshold: 0.1,rootMargin: "50px" } ); if(imgRef.current) { observer.observe(imgRef.current)} return () => observer.disconnect()},[priority]); const _handleLoad = (props) => { setIsLoaded(true); onLoad?.()}; const _handleError = (props) => { setHasError(true); onError?.()}; return (<div ref={imgRef} className={`relative overflow-hidden ${className}`} style={{ width,height }} > {} {!isLoaded && !hasError && ( <motion.div className="absolute inset-0 bg-gray-200 animate-pulse flex items-center justify-center" initial={{ opacity: 1 }} animate={{ opacity: isLoade d ? 0 : 1 }} transition={{ duration: 0.3 }} > <div className="w-8 h-8 border-2 border-gray-300 border-t-blue-500 rounded-full animate-spin" /> </motion.div> )} {} {hasError && ( <div className="absolute inset-0 bg-gray-100 flex items-center justify-center"> <div className="text-center text-gray-500"> <svg className="w-12 h-12 mx-auto mb-2" fill="none" stroke="currentColor" viewBox="0 0 24 24"> <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z" /> </svg> <p className="text-sm">Failed to load image</p> </div> </div> )} {} {isInView && !hasError && ( <motion.img src={src} alt={alt} className={`w-full h-full object-cover transition-opacity duration-300 ${isLoaded ? "opacity-100" : "opacity-0" }`} onLoad={handleLoad} onError={handleError} loading={priority ? "eager" : "lazy"} decoding="async" /> )} </div> )}; </motion> </HTMLImageElement> </any> </any> </LazyImageProps>;";";'"`'"`
import { _useEffect } from 'react';,
import { _useState } from 'react';,
import _React,{ useState,useRef,useEffect } from 'react'; import { _motion } from 'framer-motion'; interface LazyImageProps { src: string; alt: string; className?: string; width?: number; height?: number; placeholder?: string; priority?: boolean; onLoad?: () => void; onError?: () => void; ';'} export const LazyImage: Reac t.FC<LazyImageProps> = ({ src,alt,className = ",width,height,placeholder = 'data: imag e/svg+xml;base64,PHN2ZyB3aWR0aD0iMjAwIiBoZWlnaHQ9IjIwMCIgeG1sbnM9Imh0dHA6Ly93d3cudzMub3JnLzIwMDAvc3ZnIj48cmVjdCB3aWR0aD0iMTAwJSIgaGVpZ2h0PSIxMDAlIiBmaWxsPSIjZjNmNGY2Ii8+PC9zdmc+',priority = false,onLoad,onError }) => { const [isLoaded,setIsLoaded] = useState<any>(false); const [isInView,setIsInView] = useState<any>(priority); const [hasError,setHasError] = useState<any>(false); const _imgRef = useRef<HTMLImageElement>(null); useEffect(() => { if(priority) return; const _observer = new IntersectionObserver(([entry]) => { if(entry.isIntersecting) { setIsInView(true); observer.disconnect()} },{ threshold: 0.1,rootMargin: '50px' } ); if(imgRef.current) { observer.observe(imgRef.current)} return () => observer.disconnect()},[priority]); const _handleLoad = (props) => { setIsLoaded(true); onLoad?.()}; const _handleError = (props) => { setHasError(true); onError?.()}; return (<div ref={imgRef} className={`relative overflow-hidden ${className}`} style={{ width,height }} > {} {!isLoaded && !hasError && ( <motion.div className="absolute inset-0 bg-gray-200 animate-pulse flex items-center justify-center" initial={{ opacity: 1 }} animate={{ opacity: isLoade d ? 0 : 1 }} transition={{ duration: 0.3 }} > <div className="w-8 h-8 border-2 border-gray-300 border-t-blue-500 rounded-full animate-spin" /> </motion.div> )} {} {hasError && ( <div className="absolute inset-0 bg-gray-100 flex items-center justify-center"> <div className="text-center text-gray-500"> <svg className="w-12 h-12 mx-auto mb-2" fill="none" stroke="currentColor" viewBox="0 0 24 24"> <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z" /> </svg> <p className="text-sm">Failed to load image</p> </div> </div> )} {} {isInView && !hasError && ( <motion.img src={src} alt={alt} className={`w-full h-full object-cover transition-opacity duration-300 ${isLoaded ? 'opacity-100' : 'opacity-0' }`} onLoad={handleLoad} onError={handleError} loading={priority ? 'eager' : 'lazy'} decoding="async" /> )} </div> )}; </motion> </HTMLImageElement> </any> </any> </LazyImageProps>;';';

import { motion } from 'framer-motion';