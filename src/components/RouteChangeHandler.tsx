import React from 'react';

interface RouteChangeHandlerProps {
  resetScrollOnChange?: boolean;
  forceRerender?: boolean;
}

export function RouteChangeHandler({ resetScrollOnChange = true, forceRerender = false }: RouteChangeHandlerProps) {
  // Placeholder for route change handling
  return null;
}