:src/components/LazyLoader.tsx.disabled
import React, { Suspense, lazy } from 'react';
export const LazyPricing = lazy(() => import('../pages / Pricing') ) ;
import { LoadingSpinner } from './ui / LoadingSpinner';

export default function Page($1) {
export';';';
import React,{ Suspense,lazy } from 'react'; export const LazyPricing = lazy(() => import('../pages / Pricing') ) ; import { LoadingSpinner } from './ui / LoadingSpinner'; export default function Page($1) { export'; ;';';
>>>>>>> origin/cursor/automate-test-improve-and-merge-code-eafe:src/components/LazyLoader.tsx
