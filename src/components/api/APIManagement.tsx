import React, { useState, useEffect } from 'react'; import { Code, Key, Shield, Activity, Clock, CheckCircle, XCircle, AlertTriangle, BarChart3, Settings, Plus, Edit, Trash2, Eye, Copy, Download, Upload, RefreshCw, Zap, Database, Globe, Lock, Unlock, Filter, Search, TrendingUp, Users, Server, Cpu, HardDrive } from 'lucide-react'; import { useTheme } from '../context/ThemeContext'; import { useNotifications } from '../components/ui/NotificationSystem'; interface APIKey { id: string; name: string; key: string; permissions: string[]; status: 'active' | 'inactive' | 'expired'; createdAt: string; lastUsed: string; usage: { requests: number; limit: number; resetDate: string; }; environment: 'development' | 'staging' | 'production'; } interface APIEndpoint { id: string; name: string; path: string; method: 'GET' | 'POST' | 'PUT' | 'DELETE' | 'PATCH'; description: string; status: 'active' | 'deprecated' | 'maintenance'; version: string; rateLimit: { requests: number; period: string; }; authentication: 'none' | 'api-key' | 'oauth' | 'jwt'; responseTime: number; successRate: number; lastUpdated: string; } interface APIMetric { timestamp: string; requests: number; errors: number; responseTime: number; activeUsers: number; } export const APIManagement: React.FC = () => { const [activeTab, setActiveTab] = useState('overview'); const [apiKeys, setApiKeys] = useState<APIKey[]>([]); const [endpoints, setEndpoints] = useState<APIEndpoint[]>([]); const [metrics, setMetrics] = useState<APIMetric[]>([]); const [isLoading, setIsLoading] = useState(true); const [searchTerm, setSearchTerm] = useState(''); const [filterStatus, setFilterStatus] = useState('all'); const { theme, config } = useTheme(); const { addNotification } = useNotifications();