"export const FuturisticAnimatedBackground = ({ className = ", intensity = "medium", colorScheme = "multi" }) => {"; const canvasRef = useRef(null); const animationRef = useRef(); useEffect(() => { const canvas = canvasRef.current; if (!canvas) return; const ctx = canvas.getContext("2d");" if (!ctx)" return; // Set canvas size const resizeCanvas = () => {; canvas.width = window.innerWidth; canvas.height = window.innerHeight}; resizeCanvas(); window.addEventListener("resize", resizeCanvas);" // Particle system" const particles = []; // Color schemes const colors = colorSchemes[colorScheme]; const particleCount = intensity === "low" ? 50 : intensity === "medium" ? 100 : 200;" // Initialize particles" for (let i = 0; i < particleCount; i++) { particles.push({ x: Math.random() * canvas.width, y: Math.random() * canvas.height, vx: (Math.random() - 0.5) * 2, vy: (Math.random() - 0.5) * 2, size: Math.random() * 3 + 1, opacity: Math.random() * 0.5 + 0.1, color: colors[Math.floor(Math.random() * colors.length)], life: Math.random() * 100, maxLife: 100 })} // Grid lines" const gridOpacity = intensity === "low" ? 0.1 : intensity === "medium" ? 0.2 : 0.3;" // Animation loop" const animate = () => {; // Clear canvas with fade effect" ctx.fillStyle = "rgba(0, 0, 0, 0.1);" ctx.fillRect(0, 0, canvas.width, canvas.height);" // Draw grid ctx.strokeStyle = `rgba (0, 255, 255, ${gridOpacity}) `;` ctx.lineWidth = 1; // Vertical lines for (let x = 0; x < canvas.width; x += gridSize) { 