const [isExpanded, setIsExpanded] = useState (false) ; return(); <> {/* Floating Action Button */} <motion.div" className = "fixed bottom-6 right-6 z-50" initial={{ scale: 0 }}" animate={{ scale: 1 }} transition = { { duration: 0.3, delay: 2 }} >" <div className="relative">" {/* Main Button */}" <motion.button onClick={() => setIsExpanded(!isExpanded)}" className="w-16 h-16 bg-gradient-to-r from-blue-600 to-purple-600 rounded-full shadow-lg hover: shadow-xl transition-all duration-300 flex items-center justify-center text-white text-2xl" whileHover={{ scale: 1.1 }}" whileTap={{ scale: 0.9 }} 