  variant?: 'default' | 'premium' | 'featured' | 'compact';
  showCount?: number;
  autoRotate?: boolean;
  rotationInterval?: number;
  className ?  : string;
 }
          </Link>
        </div>
      </div  > );
  }
                  </Link>
                </div>
              </div>
            ))}
          </div>
            </Link>
          </div>
        </div>
      </div  > );
  }
              NEW
            </span>
          </div>
        </div>
                </div>
              </Link>
            ))}
          </div>
        </div>
      </div  > );
  }
            </Link>
          </div>
        ))}
      </div>
    </div>
  );
};
exportdefaultNewArticlesPromoBanner;