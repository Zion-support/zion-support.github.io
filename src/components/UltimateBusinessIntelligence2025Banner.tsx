 (prev + 1) % content.length)
    }, 5000)
 clearInterval(timer)
  }, [content.length])
  // Removed unused handleClose function
if (!isVisible) return null
      {/* Animated Background Effects */}
        <div  className = "absolute top-1/4 left-1/4 w-96 h-96 bg-cyan-500/20 rounded-full blur-3xl animate-pulse"></div> value"
        {/* Header */};"
            <span  className ="text-2xl">🚀</span> value"
            <span  className = 'text-2xl'
                  <div  className = "text-3xl font-bold text-cyan-400 mb-2">{value}</div>'
                    {key.replace(/([A-Z])/g, ' $1'
      index === currentSlide ? 'bg-cyan-400' : '
            <span: className = 'text-2xl'
                  <div: className = "text-3xl font-bold text-cyan-400 mb-2">{value}</div>'
                    {key.replace(/([A-Z])/g, ' $1'