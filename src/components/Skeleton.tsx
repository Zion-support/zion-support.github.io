
        )}
      />;
    )}
;
  return (;
    <div className="space-y-2">;
      {Array.from({ length: lines }).map((_, i) => (;
        <div;

      {Array.from({ length: lines }).map((_, i) => (

      ))}

      ))}
    </div>'
  )}"





