
  const navigation = useMemo(() => [
    { name: 'Home', href: '/' },
    { name: 'Pricing', href: '/pricing' },
    { name: 'Contact', href: '/contact' },
  ], []);

  return (
        </div>

        {/* Mobile Navigation */}
        {isMenuOpen && (
