import React from "react";

export default function EnhancedNewsletterSignup({ title, subtitle, showContentPreview }: { title?: string; subtitle?: string; showContentPreview?: boolean }) {
  return null;
}
