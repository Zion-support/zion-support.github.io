import React, { useState, useEffect, useCallback } from 'react';'; import { } from 'framer-motion'; interface PerformanceMetrics { fcp: number;, lcp: number;, fid: number;, cls: number;, ttfb: number;, inp: number;, bundleSize: number;, loadTime: number; } interface PerformanceAlert { id: string;, type: 'warning' | 'error' | 'info';, message: string;, timestamp: number;, metric: string;, value: number;, threshold: number; } const AdvancedPerformanceMonitor: React.FC = () => { const [metrics, setMetrics] = useState<PerformanceMetrics>({ fcp: 0, lcp: 0, fid: 0, cls: 0, ttfb: 0, inp: 0, bundleSize: 0, loadTime: 0 