import { useState } from "react"
import { Dialog, DialogContent, DialogHeader, DialogTitle } from "@/components/ui/
import { Tabs, TabsList, TabsTrigger, TabsContent } from "@/components/ui/
import { Button } from "@/components/ui/
import { TalentProfile } from "@/types/
import { ContractForm, ContractFormValues } from "./components/
import { ContractPreview } from "./components/
import { TemplateManager } from "./templates/
import { DeploymentOptions, SmartContractInfo } from "@/types/
import { useSmartContracts } from "@/hooks/
      //
      //
  // This function is passed to ContractForm.// We now decide what "generate" means in this context.// The subtask asks for a "Generate Legal Draft" button.//
            {/
  // This function is passed to ContractForm.// We now decide what "generate" means in this context.// The subtask asks for a "Generate Legal Draft" button.//
            {/