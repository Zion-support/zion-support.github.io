
import { ContractTemplate } from "@/types/
import { Button } from "@/components/ui/
import { useContractTemplates } from "@/hooks/
import { Card, CardContent } from "@/components/ui/
import { Separator } from "@/components/ui/
import { Tooltip, TooltipContent, TooltipProvider, TooltipTrigger } from "@/components/ui/
import { useAuth } from "@/hooks/
  AlertDialogTitle} from "@/components/ui/
  AlertDialogTitle} from "@/components/ui/
import { ContractTemplate } from "@/types/
import { Button } from "@/components/ui/
import { useContractTemplates } from "@/hooks/
import { Card, CardContent } from "@/components/ui/
import { Separator } from "@/components/ui/
import { Tooltip, TooltipContent, TooltipProvider, TooltipTrigger } from "@/components/ui/
import { useAuth } from "@/hooks/
  AlertDialogTitle} from "@/components/ui/
                    
  template.is default && (<span className="bg-zion-purple/10 text-zion-purple text-xs px-2 py-0.5 rounded-full">Default
</p> </div> <div className="flex items-center gap-2" > <Button > <Edit className="h-4 w-4" /> 
  !user && (<TooltipContent> Please log in to use this feature 
</Tooltip> </TooltipProvider>) : (<Button variant="ghost" size="icon" disabled aria-label="Default template"> <StarOff className="h-4 w-4" /> 
<Button > <Trash className="h-4 w-4 text-destructive" /> </Button> </div> </div> 
 <AlertDialogContent> <AlertDialogHeader> <AlertDialogTitle>Delete Template</AlertDialogTitle> <AlertDialogDescription> Are you sure you want to delete this template? This action cannot be undone. </AlertDialogDescription> </AlertDialogHeader> <AlertDialogFooter> <AlertDialogCancel>Cancel</AlertDialogCancel> 
  template.is default && (<span className="bg - zion - purple / 10 text - zion - purple text - xs px - 2 py - 0.5 rounded - full">Default
</p> </div> <div className="flex items - center gap - 2" > <Button > <Edit className="h - 4 w - 4" /> 
  !user && (<TooltipContent> Please log in to use this feature 
</Tooltip> </TooltipProvider>) : (<Button variant="ghost" size="icon" disabled aria - label="Default template"> <StarOff className="h - 4 w - 4" /> 
<Button > <Trash className="h - 4 w - 4 text - destructive" /> </Button> </div> </div> 
 <AlertDialogContent> <AlertDialogHeader> <AlertDialogTitle > Delete Template</AlertDialogTitle> <AlertDialogDescription> Are you sure you want to delete this template? This action cannot be undone. </AlertDialogDescription> </AlertDialogHeader> <AlertDialogFooter> <AlertDialogCancel > Cancel</AlertDialogCancel> 