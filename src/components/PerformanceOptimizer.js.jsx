"export const PerformanceOptimizer = ({ children }) => {"; const location = useLocation(); // Preload critical resources useEffect(() => { const preloadCriticalResources = () => {; // Preload critical CSS" const criticalCSS = document.createElement("link");" criticalCSS.rel = "preload";" criticalCSS.as = "style";" criticalCSS.href = "/src/index.css";" document.head.appendChild(criticalCSS);" // Preload critical fonts" const criticalFonts = document.createElement("link");" criticalFonts.rel = "preload";" criticalFonts.as = "font";" criticalFonts.href = "/fonts/inter-var.woff2";" criticalFonts.crossOrigin = "anonymous";" document.head.appendChild(criticalFonts)};" preloadCriticalResources()}, []); // Optimize images on route change useEffect(() => { const optimizeImages = () => {; const images = document.querySelectorAll("img");" images.forEach((img) => {" // Add loading="lazy" to images below the fold" if (img.getBoundingClientRect().top > window.innerHeight) {" img.loading = "lazy"}" // Add decoding="async" for better performance" img.decoding = "async";" // Add error handling" img.onerror = () => { img.style.display = "none"}})};" // Use requestIdleCallback for non-critical optimization" if ("requestIdleCallback" in window) {" requestIdleCallback(optimizeImages)} else { 