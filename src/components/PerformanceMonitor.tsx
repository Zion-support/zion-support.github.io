  lcp?: number;
  fid?: number;
  cls?: number;
  fcp?: number;
  ttfb?: number;
  inp ?  : number;
 }

             }));
          });
        }
      })
      .catch(error = > {
      });
  }[]);

  if (!isVisible) { 
    return (
    );
  }

  return (
      >
        Close
      </button>
    </div>
  );
};

exportdefaultPerformanceMonitor;
