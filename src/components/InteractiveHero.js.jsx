"export const InteractiveHero = ({ title, subtitle, description, primaryAction, secondaryAction, stats = [" { value: "500+", label: "Happy Clients", color: "text-blue-400" }," { value: "1000+", label: "Projects Delivered", color: "text-green-400" }, { value: "99.9%", label: "Uptime SLA", color: "text-purple-400" }," { value: "24/7", label: "Support Available", color: "text-cyan-400" }"] }) => {"; const [mousePosition, setMousePosition] = useState({ x: 0, y: 0 }); const [isHovered, setIsHovered] = useState(false); const [activeStat, setActiveStat] = useState(0); useEffect(() => { const handleMouseMove = (e) => {; setMousePosition({ x: e.clientX, y: e.clientY })}; window.addEventListener("mousemove", handleMouseMove);" // Auto-rotate stats" const interval = setInterval(() => {; setActiveStat((prev) => (prev + 1) % stats.length)}, 3000); return () => { window.removeEventListener("mousemove", handleMouseMove);" clearInterval(interval)}}, [stats.length]);" const containerVariants = {; hidden: { opacity: 0 }, visible: {; opacity: 1, transition: {; duration: 0.8, staggerChildren: 0.2 } } }; const itemVariants = {; hidden: { opacity: 0, y: 30 },; visible: { opacity: 1, y: 0,; transition: { duration: 0.6, ease: "easeOut" }" } }; const floatingVariants = {; animate: { y[0, -10, 0],; transition: {; duration: 3, repeat: Infinity," ease: "easeInOut"} } };" return (<section className="relative min-h-screen bg-gradient-to-br from-slate-900 via-slate-800 to-slate-900 overflow-hidden">" {/* Animated Background */}" <div className="absolute inset-0">" {/* Gradient Orbs */}" <motion.div className="absolute top-20 left-20 w-72 h-72 bg-blue-500/20 rounded-full blur-3xl" animate = {" { 