

  return (
    <Card>
      <CardHeader>

      </CardHeader>
      <CardContent>
        <Dialog open={open} onOpenChange={setOpen}>
          <DialogTrigger asChild>

          </DialogTrigger>
          <DialogContent>
            <DialogHeader>
              <DialogTitle>Available Rewards</DialogTitle>
              <DialogDescription>

                    </Button>
                  </div>
                </div>
              ))}
            </div>

              </Button>
            </div>
          </DialogContent>
        </Dialog>
      </CardContent>
    </Card>

