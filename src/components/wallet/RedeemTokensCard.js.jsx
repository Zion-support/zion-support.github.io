"const RedeemTokensCard() {" const { wallet, spendTokens } = useWallet(); const [open, setOpen] = useState(false); const handleRedeem = async (option) => {; if (!wallet || wallet.balance < option.cost) return; await spendTokens(option.cost, `Redeemed: ${option.title}`);` setOpen(false)}; return (<Card> <CardHeader>" <CardTitle className="flex items-center gap-2">" <Gift aria-hidden="true" className="h-5 w-5"/> Redeem Rewards" </CardTitle>" <CardDescription>Exchange your ZION$ for rewards and perks</CardDescription> </CardHeader> <CardContent> <Dialog open={open} onOpenChange={setOpen}> <DialogTrigger asChild>" <Button className="w-full">View Available Rewards</Button>" </DialogTrigger>" <DialogContent> 