import React, { useState, useEffect, useCallback } from 'react'; import { motion, AnimatePresence } from 'framer-motion'; import { Eye, EyeOff, Volume2, VolumeX, Type, Contrast, ZoomIn, ZoomOut, RotateCcw, CheckCircle, AlertTriangle, XCircle, Info, Keyboard, MousePointer, Monitor, Smartphone, Tablet, Settings, X, Check } from 'lucide-react'; interface AccessibilitySettings { highContrast: boolean; largeText: boolean; reducedMotion: boolean; screenReader: boolean; focusIndicator: boolean; colorBlindness: 'none' | 'protanopia' | 'deuteranopia' | 'tritanopia'; fontSize: 'small' | 'medium' | 'large' | 'extra-large'; zoomLevel: number; audioDescription: boolean; keyboardNavigation: boolean; voiceControl: boolean; } interface EnhancedAccessibilityPanelProps { isOpen: boolean; onClose: () => void; onSettingsChange: (settings: AccessibilitySettings) => void; currentSettings: AccessibilitySettings; } const defaultSettings: AccessibilitySettings = { highContrast: false, largeText: false, reducedMotion: false, screenReader: false, focusIndicator: true, colorBlindness: 'none', fontSize: 'medium', zoomLevel: 100, audioDescription: false, keyboardNavigation: true, voiceControl: false }; export const EnhancedAccessibilityPanel: React.FC<EnhancedAccessibilityPanelProps> = ({ isOpen, onClose, onSettingsChange, currentSettings = defaultSettings }) => { const [settings, setSettings] = useState<AccessibilitySettings>(currentSettings); const [activeTab, setActiveTab] = useState<'visual' | 'audio' | 'motor' | 'cognitive'>('visual'); useEffect(() => { setSettings(currentSettings); }, [currentSettings]); const handleSettingChange = useCallback((key: keyof AccessibilitySettings, value: any) => { const newSettings = { ...settings, [key]: value }; setSettings(newSettings); onSettingsChange(newSettings); }, [settings, onSettingsChange]); const resetSettings = useCallback(() => { setSettings(defaultSettings); onSettingsChange(defaultSettings); }, [onSettingsChange]); const applySettings = useCallback(() => {