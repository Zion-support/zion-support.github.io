import React, { useState } from 'react'; import { motion } from 'framer-motion'; import { Search, X } from 'lucide-react'; interface EnhancedSearchProps { className?: string; onClose?: () => void; } export const EnhancedSearch: React.FC<EnhancedSearchProps> = ({ className, onClose }) => { const [query, setQuery] = useState(''); const handleSubmit = (e: React.FormEvent) => { e.preventDefault();