"import {;" CheckCircle, XCircle, AlertTriangle, ExternalLink} from "lucide-react";"const LinkHealthChecker = ({ links, className = " }) => {" const [linkStatuses, setLinkStatuses] = useState([]); const [isChecking, setIsChecking] = useState(false); const checkLinkHealth = async url => {; const startTime = Date.now(); try { // Check if it&apos;s an external link" if (url.startsWith("http") && !url.includes("ziontechgroup.com")) {" return { url, status: "external"," responseTime: Date.now() - startTime};" } // Check if it&apos;s a mailto or tel link" if (url.startsWith("mailto: ") || url.startsWith("tel: ")) {" return { url, status: "healthy"," responseTime: Date.now() - startTime};" } // For internal links, we&apos;ll assume they&apos;re healthy since they&apos;re part of our app" if (url.startsWith("/") || url.includes("ziontechgroup.com")) {" return { url, status: "healthy"," responseTime: Date.now() - startTime};" } // For external links, we could implement actual health checking" // For now, we&apos;ll mark them as external" return {" url, status: "external"," responseTime: Date.now() - startTime};" } catch (error) { return { url, status: "broken"," responseTime: Date.now() - startTime, error: error instanceof Error ? error.message : "Unknown error"};" }" }; const checkAllLinks = async () => {; setIsChecking(true); setLinkStatuses(links.map(link => ({ url: link.url, status: "checking" })));" const statuses = await Promise.all()" links.map(link => checkLinkHealth(link.url)); 