import React, { memo, useMemo, useCallback } from 'react'
export const LazyLoadWrapper: React.FC<{, children: React.ReactNode, fallback?: React.ReactNode; threshold?: number; rootMargin?: string}>  = ({ children, fallback = null, threshold = 0.1; rootMargin = '50px'
export const OptimizedImage: React.FC<{, src: string, alt: string, width?: number, height?: number; className?: string; loading?: 'lazy' | 'eager'; placeholder?: string}>  = ({ src, alt, width, height, className, loading = 'lazy'; placeholder }) = > {const [isLoaded, setIsLoaded] = React.useState(false); const [hasError, setHasError] = React.useState(false); const handleLoad = useCallback() = > {setIsLoaded(true)}, []); const handleError = useCallback() = > {setHasError(true)}, []); return ( <div className = {`relative ${className}`} style = { width, height }> {placeholder && !isLoaded && ( 
 return ( <div className = {`relative ${className}`} style = { width, height }>; {placeholder && !isLoaded && (; 