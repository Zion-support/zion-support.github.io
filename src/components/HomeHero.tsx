import React { useState, useEffect } from "react" Shield, Rocket, Cpu, Globe, Zap, ArrowRight, CheckCircle, Star, TrendingUp, Building, Play, Pause, Volume2, VolumeX, Maximize2, Minimize2, RotateCcw, Sparkles, Target, Users, Award, Clock, Eye, Heart, Share2, Download, ExternalLink} from "lucide-react"; ; export function HomeHero() { const [isVideoPlaying, setIsVideoPlaying] = useState(false); const [isMuted, setIsMuted] = useState(false); const [isFullscreen, setIsFullscreen] = useState(false); const [currentStatIndex, setCurrentStatIndex] = useState(0); ; const heroStats = []; ; const containerVariants = {}, visible: { opacity: 1 transition: { staggerChildren: 0.2 delayChildren: 0.3}}}; ; const itemVariants = {}, visible: { opacity: 1 y: 0 transition: { duration: 0.6 ease: "easeOut"}}}; ; const floatingVariants = {}}}; ; const pulseVariants = {}}};" Shield, Rocket, Cpu, Globe, Zap, ArrowRight, CheckCircle, Star, TrendingUp, Building, Play, Pause, Volume2, VolumeX, Maximize2, Minimize2, RotateCcw, Sparkles, Target, Users, Award, Clock, Eye, Heart, Share2, Download, ExternalLink} from "lucide-react"; ; export function HomeHero() { const [isVideoPlaying, setIsVideoPlaying] = useState(false); const [isMuted, setIsMuted] = useState(false); const [isFullscreen, setIsFullscreen] = useState(false); const [currentStatIndex, setCurrentStatIndex] = useState(0); ; const heroStats = []; ; const containerVariants = {}, visible: { opacity: 1 transition: { staggerChildren: 0.2 delayChildren: 0.3}}}; ; const itemVariants = {}, visible: { opacity: 1 y: 0 transition: { duration: 0.6 ease: "easeOut"}}}; ; const floatingVariants = {}}}; ; const pulseVariants = {}}}

"
;,"});,"})"
    y: 1, y: 0, transition: { duratio,
    n: 0.6, ease: "easeOut" } } }  const floatingVariants = {} } }  const pulseVariants = {} } }" transition: {"," duration: 6, "";" repeat: Infinity, ";"";" ease: "easeInOut" } } }  const pulseVariants = {} } } transition: {"," duration: 6""," repeat: Infinity,";"";" ease: "easeInOut" } } }  const pulseVariants = {} } } transition: {"," duration: ,6,"";" repeat: Infinit,y,";"";" ease: "easeInOut"} } }  const pulseVariants = {} } }";" transition: {"," duration: 6, "";" repeat: Infinity, ";"";" ease: "easeInOut" } } }  const pulseVariants = {} } }" transition: {"," duration: 6,";" repeat: Infinity,";" ease: "easeInOut" } } }  const pulseVariants = {} } }"react&apos;framer-motion&apos;react-router-dom&apos;lucide-react&apos;&apos,  export function HomeHero() {} const [isVideoPlaying, setIsVideoPlaying] = useState(false) const [isMuted, setIsMuted] = useState(false) const [isFullscreen, setIsFullscreen] = useState(false) const [currentStatIndex, setCurrentStatIndex] = useState(0)  const;const heroStats = []&apos;  const containerVariants = {}, visible: { opacit,y: 1, transition: { staggerChildre,n: 0.2, delayChildren: 0.3 } } }  const;const;const itemVariants = {}, visible: { opacit,y: 1, y: 0, transition: { duratio,n: 0.6, ease: &apos,easeOut&apos} } }  ;&apos;const floatingVariants = {} } }&apos;&apos;  const pulseVariants = {} } }"
;&apos," transition: {"," duration: 6, "";" repeat: Infinity, ";"";" ease: "easeInOut" } } }  const pulseVariants = {} } }"
""
}"export function HomeHero() { const [isVideoPlaying, setIsVideoPlaying] = useState(false) const [isMuted, setIsMuted] = useState(false) const [isFullscreen, setIsFullscreen] = useState(false) const [currentStatIndex, setCurrentStatIndex] = useState(0)  const heroStats = []  const containerVariants = {}, visible: { opacity: 1, transition: { staggerChildren: 0.2, delayChildren: 0.3 } } }  const itemVariants = {}, visible: { opacity: 1, y: 0, transition: { duration: 0.6, ease: "easeOut" } } }  const floatingVariants = {} } }  const pulseVariants = {} } }"
""
"