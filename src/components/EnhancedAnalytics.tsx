import React, { useEffect, useState, useCallback, useRef } from 'react'; import { motion, AnimatePresence } from 'framer-motion'; import { BarChart3, TrendingUp, Users, Eye, MousePointer, Clock, Globe, Smartphone, Monitor, Tablet, Download, Upload, Activity, Zap, AlertTriangle, CheckCircle, Info } from 'lucide-react'; interface AnalyticsData { pageViews: number; uniqueVisitors: number; sessionDuration: number; bounceRate: number; deviceTypes: { desktop: number; mobile: number; tablet: number; }; topPages: Array<{ path: string; views: number; title: string; }>; userEngagement: { scrollDepth: number; timeOnPage: number; interactions: number; }; performance: { loadTime: number; coreWebVitals: { fcp: number; lcp: number; fid: number; cls: number; }; }; } interface EnhancedAnalyticsProps { enabled?: boolean; showDashboard?: boolean; trackingId?: string; } export const EnhancedAnalytics: React.FC<EnhancedAnalyticsProps> = ({ enabled = true, showDashboard = false, trackingId = 'G-XXXXXXXXXX' }) => { const [analyticsData, setAnalyticsData] = useState<AnalyticsData | null>(null); const [isVisible, setIsVisible] = useState(false); const [currentPage, setCurrentPage] = useState(window.location.pathname); const [sessionStart, setSessionStart] = useState(Date.now()); const [userInteractions, setUserInteractions] = useState(0); const [scrollDepth, setScrollDepth] = useState(0); const [timeOnPage, setTimeOnPage] = useState(0); const sessionRef = useRef<NodeJS.Timeout>(); const scrollRef = useRef<NodeJS.Timeout>();