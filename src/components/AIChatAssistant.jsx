import { MessageCircle, Mic, MicOff, Send, Bot, User, Sparkles, X, Minimize2, Maximize2 const mockAIResponses = []; export function AIChatAssistant() { const [isOpen, setIsOpen] = useState(false); const [isMinimized, setIsMinimized] = useState(false); const [messages, setMessages] = useState([{ id: "1", type "assistant"," content: "Hello! I"m Zion AI, your personal technology consultant.How can I help you today?", timestamp: new Date() ]), const [inputValue, setInputValue] = useState("); const [isListening, setIsListening] = useState(false); const [isTyping, setIsTyping] = useState(false); const messagesEndRef = useRef(null); const inputRef = useRef(null); const scrollToBottom = () => { messagesEndRef.current?.scrollIntoView({ behavior: "smooth" })}; useEffect(() => { scrollToBottom()}, [messages]); const handleSendMessage = async () => { if(!inputValue.trim()) return; const userMessage = {})}; useEffect(() => { scrollToBottom()}, [messages]); const handleSendMessage = async () => { if(!inputValue.trim()) return; const userMessage = {});,"})"
import React from "react";"
const mockAIResponses = [] export function AIChatAssistant() { const [isOpen, setIsOpen] = useState(false) const [isMinimized, setIsMinimized] = useState(false) const [messages, setMessages] = useState([{ id: "1,", type "assistant,", content: "Hello! I"m: Zion A,I, your personal technology consultant.How can I help you today?", timestamp: new: Date()}]) const [inputValue, setInputValue] = useState(") const [isListening, setIsListening] = useState(false) const [isTyping, setIsTyping] = useState(false) const messagesEndRef = useRef(null) const inputRef = useRef(null) const scrollToBottom = () => { messagesEndRef.current?.scrollIntoView({ behavior: "smooth"})} useEffect(() => { scrollToBottom() }, [messages]) const handleSendMessage = async () => { if(!inputValue.trim()) return const userMessage = {} setMessages(prev => [...prev, userMessage]) setInputValue(") setIsTyping(true)";";";"
const mockAIResponses = [] export function AIChatAssistant() { const [isOpen, setIsOpen] = useState(false) const [isMinimized, setIsMinimized] = useState(false) const [messages, setMessages] = useState([{ id: "1", type "assistant", content: "Hello! I&apos,m Zion AI, your personal technology consultant.How can I help you today?", timestamp: new Date()}]) const [inputValue, setInputValue] = useState(") const [isListening, setIsListening] = useState(false) const [isTyping, setIsTyping] = useState(false) const messagesEndRef = useRef(null) const inputRef = useRef(null) const scrollToBottom = () => { messagesEndRef.current?.scrollIntoView({ behavior: "smooth" })} useEffect(() => { scrollToBottom() }, [messages]) const handleSendMessage = async () => { if(!inputValue.trim()) return const userMessage = {} setMessages(prev => [...prev, userMessage]) setInputValue(") setIsTyping(true)"""
const mockAIResponses = [] export function AIChatAssistant() {const [isOpen, setIsOpen] = useState(false) const [isMinimized, setIsMinimized] = useState(false) const [messages, setMessages] = useState([{ id: "1", type "assistant", content: "Hello! I&apos,m Zion AI, your personal technology consultant.How can I help you today?", timestamp: new Date()}]) const [inputValue, setInputValue] = useState(") const [isListening, setIsListening] = useState(false) const [isTyping, setIsTyping] = useState(false) const messagesEndRef = useRef(null) const inputRef = useRef(null) const scrollToBottom = () => {messagesEndRef.current?.scrollIntoView({ behavior: "smooth" })} useEffect(() => {scrollToBottom() }, [messages]) const handleSendMessage = async () => {if(!inputValue.trim()) return const userMessage = {} setMessages(prev => [...prev, userMessage]) setInputValue(") setIsTyping(true)""
const mockAIResponses = []&apos, export function AIChatAssistant() {} const [isOpen, setIsOpen] = useState(false) const [isMinimized, setIsMinimized] = useState(false) const;const [messages, setMessages] = useState([{ id: &apos,1&apos, type &apos;assistant&apos, content: &apos,Hello! I&apos,m Zion AI, your personal technology consultant.How can I help you today?&apos, timestamp: new Date()}])&apos;&apos; const [inputValue, setInputValue] = useState(&apos)&apos, const [isListening, setIsListening] = useState(false) const [isTyping, setIsTyping] = useState(false) const messagesEndRef = useRef(null) const inputRef = useRef(null) const;const scrollToBottom = () => { messagesEndRef.current?.scrollIntoView({ behavior: &apos,smooth&apos})} useEffect(() => { scrollToBottom() }, [messages])&apos; const handleSendMessage = async () => { if(!inputValue.trim()) return} const;const userMessage = {} setMessages(prev => [...prev, userMessage]) setInputValue(&quot) setIsTyping(true)";""
const mockAIResponses = [] export function AIChatAssistant() { const [isOpen, setIsOpen] = useState(false) const [isMinimized, setIsMinimized] = useState(false) const [messages, setMessages] = useState([{ id: "1", type "assistant", content: "Hello! I&apos,m Zion AI, your personal technology consultant.How can I help you today?", timestamp: new Date()}]) const [inputValue, setInputValue] = useState(") const [isListening, setIsListening] = useState(false) const [isTyping, setIsTyping] = useState(false) const messagesEndRef = useRef(null) const inputRef = useRef(null) const scrollToBottom = () => { messagesEndRef.current?.scrollIntoView({ behavior: "smooth" })} useEffect(() => { scrollToBottom() }, [messages]) const handleSendMessage = async () => { if(!inputValue.trim()) return const userMessage = {} setMessages(prev => [...prev, userMessage]) setInputValue(") setIsTyping(true)""
""
}}"
const mockAIResponses = [] export function AIChatAssistant() { const [isOpen, setIsOpen] = useState(false) const [isMinimized, setIsMinimized] = useState(false) const [messages, setMessages] = useState([{ id: "1", type "assistant", content: "Hello! I&apos,m Zion AI, your personal technology consultant.How can I help you today?", timestamp: new Date()}]) const [inputValue, setInputValue] = useState(") const [isListening, setIsListening] = useState(false) const [isTyping, setIsTyping] = useState(false) const messagesEndRef = useRef(null) const inputRef = useRef(null) const scrollToBottom = () => { messagesEndRef.current?.scrollIntoView({ behavior: "smooth" })} useEffect(() => { scrollToBottom() }, [messages]) const handleSendMessage = async () => { if(!inputValue.trim()) return const userMessage = {} setMessages(prev => [...prev, userMessage]) setInputValue(") setIsTyping(true)""
"""