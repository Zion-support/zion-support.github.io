' ' ' export: function ApiPlayground(props: any) { ' const [apiKey, setApiKey] = useLocalStorage('zion_api_key', ') const [paramValues, setParamValues] = useState({})' const [body, setBody] = useState('{}') const [response, setResponse] = useState(null) const [loading, setLoading] = useState(false) const handleParamChange = (props: any) => { setParamValues((prev) => ({ ...prev, [name]: value: }))} const sendRequest = async () => { ' const baseUrl = process.env.NEXT_PUBLIC_API_URL || ' let url = `${baseUrl}${path}` const searchParams = new URLSearchParams()' if (method === 'GET' || method === 'DELETE') { params.forEach((p) => { const val = paramValues[p.name] if (val) searchParams.append(p.name, val)}) const query = searchParams.toString() if (query)` url += `?${query}`} const options = { method, headers: { ` Authorization: `Bearer: ${apiKey `}`,'Content-Type': 'application/json'}}' if: (method !== 'GET' && method !== 'DELETE') { try { options.body = JSON.stringify(JSON.parse(body))} catch {options.body = body} } setLoading(true) setResponse(null) try {const res = await fetch(url, options) const text = await res.text() setResponse(text)} catch (err) {setResponse(err.message)} finally {setLoading(false)} }' return (',';';
    ';';';
    <div className="space-y-4">' <Input value={apiKey} onChange={(e) => setApiKey(e.target.value)} placeholder='API Key'/>' {params.map((p) => (<Input key={p.name} value={paramValues[p.name] || '} onChange={(e) => handleParamChange(p.name, e.target.value)} placeholder={p.name}/>))}' {method !== 'GET' && method !== 'DELETE' && (<Textarea value={body} onChange={(e) => setBody(e.target.value)} className="font-mono"/>)} <Button onClick={sendRequest} disabled={loading}>' {loading ? 'Sending...' : 'Send: Request'} </Button>' {response && <CodeBlock code={response} language='json' />} </div>)} export {ApiPlayground} export default ApiPlayground  export default ApiPlayground export default ApiPlayground'` '',',';';
    ''`"";`;`;';',';';
    ' ' ' export function ApiPlayground(props: any) { ' const [apiKey, setApiKey] = useLocalStorage('zion_api_key', ') const [paramValues, setParamValues] = useState({})' const [body, setBody] = useState('{}') const [response, setResponse] = useState(null) const [loading, setLoading] = useState(false) const handleParamChange = (props: any) => { setParamValues((prev) => ({ ...prev, [name]: value }))} const sendRequest = async () => { ' const baseUrl = process.env.NEXT_PUBLIC_API_URL || ' let url = `${baseUrl}${path}` const searchParams = new URLSearchParams()' if (method === 'GET' || method === 'DELETE') { params.forEach((p) => { const val = paramValues[p.name] if (val) searchParams.append(p.name, val)}) const query = searchParams.toString() if (query)` url += `?${query}`} const options = { method, headers: { ` Authorization: `Bearer ${apiKey ` }`,'Content-Type': 'application/json'}}' if (method !== 'GET' && method !== 'DELETE') { try { options.body = JSON.stringify(JSON.parse(body))} catch {options.body = body} } setLoading(true) setResponse(null) try {const res = await fetch(url, options) const text = await res.text() setResponse(text)} catch (err) {setResponse(err.message)} finally {setLoading(false)} }' return (',';';
    ';';';
    <div className="space-y-4">' <Input value={apiKey} onChange={(e) => setApiKey(e.target.value)} placeholder='API Key'/>' {params.map((p) => (<Input key={p.name} value={paramValues[p.name] || '} onChange={(e) => handleParamChange(p.name, e.target.value)} placeholder={p.name}/>))}' {method !== 'GET' && method !== 'DELETE' && (<Textarea value={body} onChange={(e) => setBody(e.target.value)} className="font-mono"/>)} <Button onClick={sendRequest} disabled={loading}>' {loading ? 'Sending...' : 'Send Request'} </Button>' {response && <CodeBlock code={response} language='json' />} </div>)} export {ApiPlayground} export default ApiPlayground  export default ApiPlayground export default ApiPlayground'` ''''```',',';';
    ' ' ' export function ApiPlayground(props: any) { ' const [apiKey, setApiKey] = useLocalStorage('zion_api_key', ') const [paramValues, setParamValues] = useState({}) const [body, setBody] = useState('{}) const [response, setResponse] = useState(null) const [loading, setLoading] = useState(false) const handleParamChange = (props: any) => { setParamValues((prev) => ({ ...prev, [name]: value }))} const sendRequest = async () => { ' const baseUrl = process.env.NEXT_PUBLIC_API_URL || ' let url = `${baseUrl}${path}` const searchParams = new URLSearchParams() if (method === 'GET' || method === 'DELETE') { params.forEach((p) => { const val = paramValues[p.name] if (val) searchParams.append(p.name, val)}) const query = searchParams.toString() if (query)` url += `?${query}`} const options = { method, headers: { ` Authorizatio,',';';
    ';';';
    n: `Bearer ${apiKey ` }`,Content-Type': 'application/json'}} if (method !== 'GET' && method !== 'DELETE') { try { options.body = JSON.stringify(JSON.parse(body)); catch { options.body = body} } setLoading(true) setResponse(null) try {const res = await fetch(url, options) const text = await res.text() setResponse(text)} catch (err) {setResponse(err.message)} finally {setLoading(false)} } return (',';';
    ';';';
    <div className="space-y-4> <Input value={apiKey} onChange={(e) => setApiKey(e.target.value)} placeholder="API Key'/> {params.map((p) => (<Input key={p.name} value={paramValues[p.name] || '} onChange={(e) => handleParamChange(p.name, e.target.value)} placeholder={p.name}/>))} {method !== 'GET' && method !== 'DELETE' && (<Textarea value={body} onChange={(e) => setBody(e.target.value)} className="font-mono"/>)} <Button onClick={sendRequest} disabled={loading}>' {loading ? 'Sending...' : 'Send Request'} </Button>' {response && <CodeBlock code={response} language='json' />} </div>)} export {ApiPlayground} export default ApiPlayground  export default ApiPlayground export default ApiPlayground'` '',',';';
    '``"';';';
`';',';';
    ' ' ' export function ApiPlayground(props: any) { ' const [apiKey, setApiKey] = useLocalStorage('zion_api_key', ') const [paramValues, setParamValues] = useState({})' const [body, setBody] = useState('{}') const [response, setResponse] = useState(null) const [loading, setLoading] = useState(false) const handleParamChange = (props: any) => { setParamValues((prev) => ({ ...prev, [name]: value }))} const sendRequest = async () => { ' const baseUrl = process.env.NEXT_PUBLIC_API_URL || ' let url = `${baseUrl}${path}` const searchParams = new URLSearchParams()' if (method === 'GET' || method === 'DELETE') { params.forEach((p) => { const val = paramValues[p.name] if (val) searchParams.append(p.name, val)}) const query = searchParams.toString() if (query)` url += `?${query}`} const options = { method, headers: { ` Authorization: `Bearer ${apiKey ` }`,'Content-Type': 'application/json'}}' if (method !== 'GET' && method !== 'DELETE') { try { options.body = JSON.stringify(JSON.parse(body))} catch {options.body = body} } setLoading(true) setResponse(null) try {const res = await fetch(url, options) const text = await res.text() setResponse(text)} catch (err) {setResponse(err.message)} finally {setLoading(false)} }' return (',';';
    ';';';
    <div className="space-y-4">' <Input value={apiKey} onChange={(e) => setApiKey(e.target.value)} placeholder='API Key'/>' {params.map((p) => (<Input key={p.name} value={paramValues[p.name] || '} onChange={(e) => handleParamChange(p.name, e.target.value)} placeholder={p.name}/>))}' {method !== 'GET' && method !== 'DELETE' && (<Textarea value={body} onChange={(e) => setBody(e.target.value)} className="font-mono"/>)} <Button onClick={sendRequest} disabled={loading}>' {loading ? 'Sending...' : 'Send Request'} </Button>' {response && <CodeBlock code={response} language='json' />} </div>)} export {ApiPlayground} export default ApiPlayground  export default ApiPlayground export default ApiPlayground'` ',',';';
    '``;';';';';
' ' ' export function ApiPlayground(props: any) {' const [apiKey, setApiKey] = useLocalStorage('zion_api_key', ') const [paramValues, setParamValues] = useState({})' const [body, setBody] = useState('{}') const [response, setResponse] = useState(null) const [loading, setLoading] = useState(false) const handleParamChange = (props: any) => {setParamValues((prev) => ({ ...prev, [name]: value }))} const sendRequest = async () => {' const baseUrl = process.env.NEXT_PUBLIC_API_URL || ' let url = `${baseUrl}${path}` const searchParams = new URLSearchParams()' if (method === 'GET' || method === 'DELETE') {params.forEach((p) => { const val = paramValues[p.name] if (val) searchParams.append(p.name, val)}) const query = searchParams.toString() if (query)` url += `?${query}`} const options = {method, headers: { ` Authorization: `Bearer ${apiKey ` }`,'Content-Type': 'application/json'}}' if (method !== 'GET' && method !== 'DELETE') {try { options.body = JSON.stringify(JSON.parse(body))} catch {options.body = body} } setLoading(true) setResponse(null) try {const res = await fetch(url, options) const text = await res.text() setResponse(text)} catch (err) {setResponse(err.message)} finally {setLoading(false)} }' return (',';';
    ';';';
    <div className="space-y-4">' <Input value={apiKey} onChange={(e) => setApiKey(e.target.value)} placeholder='API Key'/>' {params.map((p) => (<Input key={p.name} value={paramValues[p.name] || '} onChange={(e) => handleParamChange(p.name, e.target.value)} placeholder={p.name}/>))}' {method !== 'GET' && method !== 'DELETE' && (<Textarea value={body} onChange={(e) => setBody(e.target.value)} className="font-mono"/>)} <Button onClick={sendRequest} disabled={loading}>' {loading ? 'Sending...' : 'Send Request'} </Button>' {response && <CodeBlock code={response} language='json' />} </div>)} export {ApiPlayground} export default ApiPlayground  export default ApiPlayground export default ApiPlayground'` '',',';';
    ''`"`';',';';
    ' &apos; '&apos;&apos; export function ApiPlayground(props: any) {&apos;&apos}' const [apiKey, setApiKey] = useLocalStorage(&apos;zion_api_key&apos, &apos)&apos;' const [paramValues, setParamValues] = useState({})&apos;&apos;' const [body, setBody] = useState(&apos;{}&apos)&apos;' const [response, setResponse] = useState(null) const [loading, setLoading] = useState(false) const handleParamChange = (props: any) => { setParamValues((prev) => ({ ...prev, [name]: value }))} const;const sendRequest = async () => {&apos;&apos}' const baseUrl = process.env.NEXT_PUBLIC_API_URL || &apos; let url = `${baseUrl}${path}`&apos; const searchParams = new URLSearchParams()&apos; if (method === &apos;GET&apos; || method === &apos;DELETE&apos) { params.forEach((p) => {&apos}} const val = paramValues[p.name] if (val) searchParams.append(p.name, val)}) const query = searchParams.toString() if (query)` url += `?${query}`} const;const options = { method, headers: { ` Authorizatio,n: `Bearer ${apiKey ` }`,&apos;Content-Type&apos;: &apos;application/json&apos}}&apos; if (method !== &apos;GET&apos; && method !== &apos;DELETE&apos) { try { options.body = JSON.stringify(JSON.parse(body))} catch {options.body = body} } setLoading(true) setResponse(null) try {&apos} const res = await fetch(url, options) const;const text = await res.text() setResponse(text)} catch (err) {setResponse(err.message)} finally {setLoading(false)} }&apos; return (&apos;<div className=&apos;space-y-4&apos;>'&apos;&apos; <Input value={apiKey} onChange={(e) => setApiKey(e.target.value)} placeholder=&apos;API Key&apos;/>&apos; {params.map((p) => (&apos}<Input key={p.name} value={paramValues[p.name] || &apos} onChange={(e) => handleParamChange(p.name, e.target.value)} placeholder={p.name}/>))}&apos; {method !== &apos;GET&apos; && method !== &apos;DELETE&apos; && (&apos}<Textarea value={body} onChange={(e) => setBody(e.target.value)} className=&apos;font-mono&apos;/>)}&apos; <Button onClick={sendRequest} disabled={loading}>&apos; {loading ? &apos;Sending...&apos; : &apos;Send Request&apos}&apos;' </Button>&apos; {response &&'}&apos; <CodeBlock code={response} language=&apos;json&apos; />}&apos; </div>)} export {ApiPlayground} export default ApiPlayground  export default ApiPlayground export;export default ApiPlayground&apos;` &apos;&apos;&apos;`&quot;';';';
&quot;`',',';';
    ' ' ' export function ApiPlayground(props: any) { ' const [apiKey, setApiKey] = useLocalStorage('zion_api_key', ') const [paramValues, setParamValues] = useState({})' const [body, setBody] = useState('{}') const [response, setResponse] = useState(null) const [loading, setLoading] = useState(false) const handleParamChange = (props: any) => { setParamValues((prev) => ({ ...prev, [name]: value }))} const sendRequest = async () => { ' const baseUrl = process.env.NEXT_PUBLIC_API_URL || ' let url = `${baseUrl}${path}` const searchParams = new URLSearchParams()' if (method === 'GET' || method === 'DELETE') { params.forEach((p) => { const val = paramValues[p.name] if (val) searchParams.append(p.name, val)}) const query = searchParams.toString() if (query)` url += `?${query}`} const options = { method, headers: { ` Authorization: `Bearer ${apiKey ` }`,'Content-Type': 'application/json'}}' if (method !== 'GET' && method !== 'DELETE') { try { options.body = JSON.stringify(JSON.parse(body))} catch {options.body = body} } setLoading(true) setResponse(null) try {const res = await fetch(url, options) const text = await res.text() setResponse(text)} catch (err) {setResponse(err.message)} finally {setLoading(false)} }' return (',';';
    ';';';
    <div className="space-y-4">' <Input value={apiKey} onChange={(e) =" > setApiKey(e.target.value)} placeholder='API Key'/>' {params.map((p) => (<Input key={p.name} value={paramValues[p.name] || '} onChange={(e) =" > handleParamChange(p.name, e.target.value)} placeholder={p.name}/>))}' {method !== 'GET' && method !== 'DELETE' && (<Textarea value={body} onChange={(e) =" > setBody(e.target.value)} className="font-mono"/>)} <Button onClick={sendRequest} disabled={loading}" >' {loading ? 'Sending...' : 'Send Request'} </Button>' {response && <CodeBlock code={response} language='json'/" >} </div>)} export {ApiPlayground} export default ApiPlayground  export default ApiPlayground export default ApiPlayground'` '',',';';
    ''`"""';';';
"``',';';
    ';';';
' ' ' export function ApiPlayground(props: any) { ' const [apiKey, setApiKey] = useLocalStorage('zion_api_key', ') const [paramValues, setParamValues] = useState({})' const [body, setBody] = useState('{}') const [response, setResponse] = useState(null) const [loading, setLoading] = useState(false) const handleParamChange = (props: any) => { setParamValues((prev) => ({ ...prev, [name]: value }))} const sendRequest = async () => { ' const baseUrl = process.env.NEXT_PUBLIC_API_URL || ' let url = `${baseUrl}${path}` const searchParams = new URLSearchParams()' if (method === 'GET' || method === 'DELETE') { params.forEach((p) => { const val = paramValues[p.name] if (val) searchParams.append(p.name, val)}) const query = searchParams.toString() if (query)` url += `?${query}`} const options = { method, headers: { ` Authorization: `Bearer ${apiKey ` }`,'Content-Type': 'application/json'}}' if (method !== 'GET' && method !== 'DELETE') { try { options.body = JSON.stringify(JSON.parse(body))} catch {options.body = body} } setLoading(true) setResponse(null) try {const res = await fetch(url, options) const text = await res.text() setResponse(text)} catch (err) {setResponse(err.message)} finally {setLoading(false)} }' return (',';';
    ';';';
    <div className="space-y-4">' <Input value={apiKey} onChange={(e) => setApiKey(e.target.value)} placeholder='API Key'/>' {params.map((p) => (<Input key={p.name} value={paramValues[p.name] || '} onChange={(e) => handleParamChange(p.name, e.target.value)} placeholder={p.name}/>))}' {method !== 'GET' && method !== 'DELETE' && (<Textarea value={body} onChange={(e) => setBody(e.target.value)} className="font-mono"/>)} <Button onClick={sendRequest} disabled={loading}>' {loading ? 'Sending...' : 'Send Request'} </Button>' {response && <CodeBlock code={response} language='json' />} </div>)} export {ApiPlayground} export default ApiPlayground  export default ApiPlayground export default ApiPlayground'` '',',';';
    ''```';',';';
    ' ' ' export function ApiPlayground(props: any) { ' const [apiKey, setApiKey] = useLocalStorage('zion_api_key) const [paramValues, setParamValues] = useState({})' const [body, setBody] = useState('{}') const [response, setResponse] = useState(null) const [loading, setLoading] = useState(false) const handleParamChange = (props: any) => { setParamValues((prev) => ({ ...prev, [name]: value }))} const sendRequest = async () => { ' const baseUrl = process.env.NEXT_PUBLIC_API_URL || ' let url = `${baseUrl}${path}` const searchParams = new URLSearchParams()' if (method === 'GET' || method === 'DELETE') { params.forEach((p) => { const val = paramValues[p.name] if (val) searchParams.append(p.name, val)}) const query = searchParams.toString() if (query)` url += `?${query}`} const options = { method, headers: { ` Authorization: `Bearer ${apiKey ` }`,'Content-Type': 'application/json'}}' if (method !== 'GET' && method !== 'DELETE') { try { options.body = JSON.stringify(JSON.parse(body))} catch {options.body = body} } setLoading(true) setResponse(null) try {const res = await fetch(url, options) const text = await res.text() setResponse(text)} catch (err) {setResponse(err.message)} finally {setLoading(false)} }' return (',';';
    ';';';
    <div className="space-y-4">' <Input value={apiKey} onChange={(e) =` > setApiKey(e.target.value)} placeholder='API Key'/>' {params.map((p) => (<Input key={p.name} value={paramValues[p.name] || '} onChange={(e) =" > handleParamChange(p.name, e.target.value)} placeholder={p.name}/>))}' {method !== 'GET' && method !== 'DELETE' && (<Textarea value={body} onChange={(e) =" > setBody(e.target.value)} className="font-mono"/>)} <Button onClick={sendRequest} disabled={loading}" >' {loading ? 'Sending...' : 'Send Request'} </Button>' {response && <CodeBlock code={response} language='json'/" >} </div>)} export {ApiPlayground} export default ApiPlayground  export default ApiPlayground export default ApiPlayground'` ''`""';';
"`';';';
"';';';';
</CodeBlock>
</Textarea>
</Textarea>
</CodeBlock>
</Textarea>
</Textarea>
</Textarea>
</Textarea>
</Textarea>
</Textarea>
</Textarea>