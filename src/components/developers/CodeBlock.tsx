

          {language}
        </div>;
      )}

    </div>;
  );

    >;
      <pre;
        className={cn ('p - 4 overflow - auto', showLineNumbers && 'pl - 12 relative')}
      >;
        {showLineNumbers && (

export default CodeBlock;
;
}
}
}
}

    </div>;
  );
}
;

