"export function WebhooksManager() {" const { webhooks, loading, testResult, fetchWebhooks, createWebhook, toggleWebhook, deleteWebhook, testWebhook, clearTestResult } = useWebhooks(); const [showCreateDialog, setShowCreateDialog] = useState(false); const [showDeleteConfirm, setShowDeleteConfirm] = useState(null); const [showTestDialog, setShowTestDialog] = useState(null); const [showTestResult, setShowTestResult] = useState(false); // Create webhook form state" const [webhookName, setWebhookName] = useState(");" const [webhookUrl, setWebhookUrl] = useState(");" const [webhookSecret, setWebhookSecret] = useState(");" const [selectedEvents, setSelectedEvents] = useState([]);" const [testEventType, setTestEventType] = useState("new_application");" // Load webhooks on mount" useEffect(() => { fetchWebhooks()}, []); const handleCreateWebhook = async () => {; " if (webhookName.trim() === " || webhookUrl.trim() === " || selectedEvents.length === 0)" return;" await createWebhook(webhookName, webhookUrl, selectedEvents, webhookSecret.trim() === " ? null : webhookSecret);" setShowCreateDialog(false);" resetWebhookForm()}; const handleDeleteWebhook = async (webhookId) => {; await deleteWebhook(webhookId); setShowDeleteConfirm(null)}; const handleTestWebhook = async (webhookId) => {; await testWebhook(webhookId, testEventType); setShowTestResult(true)}; const resetWebhookForm = () => {; " setWebhookName(");" setWebhookUrl(");" setWebhookSecret(");" setSelectedEvents([])};" // Event type options const eventOptions = [" { value: "new_application", label: "New Application", description: "When a talent applies to a job" }, { value: "quote_received", label: "Quote Received", description: "When a quote is received from talent" }," { value: "milestone_approved", label: "Milestone Approved", description: "When a project milestone is approved" }, { value: "talent_hired", label: "Talent Hired", description: "When talent is hired for a project" }," ];" // Toggle an event selection const toggleEvent = (event) => {; setSelectedEvents(prev => prev.includes(event); ? prev.filter(e => e !== event); [...prev, event])};" return (<Card className="bg-zinc-900 border-zinc-800 text-white">" <CardHeader>" <CardTitle className="text-xl flex items-center">" <Webhook className="mr-2" size={20}/> Webhooks" </CardTitle>" <CardDescription className="text-zinc-400">" Set up webhooks to get notified when events happen in your Zion account." </CardDescription> </CardHeader> <CardContent>" <div className="flex justify-between items-center mb-6">" <p className="text-sm text-zinc-400">" You have {webhooks.length} {webhooks.length === 1 ? "webhook" : "webhooks"}" </p>" <Dialog open={showCreateDialog} onOpenChange={setShowCreateDialog}> <DialogTrigger asChild>" <Button variant="default">" <Plus size={16} className="mr-1"/> Add Webhook" </Button>" </DialogTrigger>" <DialogContent className="bg-zinc-900 border-zinc-800 text-white">" <DialogHeader>" <DialogTitle>Create Webhook</DialogTitle>" <DialogDescription className="text-zinc-400">" Add a webhook endpoint to receive event notifications." </DialogDescription> </DialogHeader> " <div className="space-y-4 py-4">" <div className="space-y-2">" <Label htmlFor="webhook-name">Webhook Name</Label>" <Input id="webhook-name" value={webhookName} onChange={(e) => setWebhookName(e.target.value)} placeholder="e.g. Application Notifications" className="bg-zinc-800 border-zinc-700"/>" </div>" " <div className="space-y-2">" <Label htmlFor="webhook-url">Endpoint URL</Label>" <Input id="webhook-url" value={webhookUrl} onChange={(e) => setWebhookUrl(e.target.value)} placeholder="https: //example.com/webhook" className="bg-zinc-800 border-zinc-700"/>" <p className="text-xs text-zinc-500">" The URL where webhook payloads will be sent when events occur." </p> </div> " <div className="space-y-2">" <Label htmlFor="webhook-secret">Secret Key (Optional)</Label>" <Input id="webhook-secret" type="password" value={webhookSecret} onChange={(e) => setWebhookSecret(e.target.value)} placeholder="Enter secret key" aria-label="Secret key" className="bg-zinc-800 border-zinc-700"/>" <p className="text-xs text-zinc-500">" Used to verify webhook payload signatures. Keep it secret and secure." </p> </div> " <div className="space-y-2">" <Label>Event Types</Label>" <div className="grid gap-2 pt-2">" {eventOptions.map((event) => (<div key={event.value} className="flex items-center space-x-2">" <Checkbox id={event.value} checked={selectedEvents.includes(event.value)} onCheckedChange={() => toggleEvent(event.value)}/>" <Label htmlFor={event.value} className="text-sm font-medium leading-none peer-disabled: cursor-not-allowed peer-disabled:opacity-70">" {event.label}" <span className="block text-xs text-zinc-400 mt-1">{event.description}</span>" </Label>" </div>))} </div> </div> </div> <DialogFooter>" <Button variant="outline" onClick={() => {" setShowCreateDialog(false);" resetWebhookForm()}}> Cancel </Button>" <Button onClick={handleCreateWebhook} disabled={webhookName.trim() === " ||" webhookUrl.trim() === " ||" selectedEvents.length === 0}>" Create Webhook 