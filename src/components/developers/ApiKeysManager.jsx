import React from "react"; " " " " " " export { function }; export default function ApiKeysManager() { const { apiKeys, loading, newApiKey, fetchApiKeys, createApiKey, deleteApiKey, toggleApiKey, updateApiKeyScopes, regenerateApiKey, revokeApiKey, clearNewApiKey } = useApiKeys(); const [showCreateDialog, setShowCreateDialog] = useState(false); const [newKeyName, setNewKeyName] = useState("); const [selectedScopes, setSelectedScopes] = useState([]); const [showRegenerateConfirm, setShowRegenerateConfirm] = useState(null); const [showDeleteConfirm, setShowDeleteConfirm] = useState(null); const scopeOptions = []; const handleCreateKey = async () => { if(!newKeyName.trim() || selectedScopes.length === 0) return; await createApiKey(newKeyName.trim(), selectedScopes); setNewKeyName("); setSelectedScopes([]); setShowCreateDialog(false)}; const handleScopeToggle = (scope) => { setSelectedScopes(prev => prev.includes(scope) ? prev.filter(s => s !== scope) [...prev, scope])}; const getExampleCode = (apiKey) => { " return "curl -X GET "https: export default function ApiKeysManager() { const { apiKeys, loading, newApiKey, fetchApiKeys, createApiKey, deleteApiKey, toggleApiKey, updateApiKeyScopes, regenerateApiKey, revokeApiKey, clearNewApiKey } = useApiKeys(); const [showCreateDialog, setShowCreateDialog] = useState(false); const [newKeyName, setNewKeyName] = useState("); const [selectedScopes, setSelectedScopes] = useState([]); const [showRegenerateConfirm, setShowRegenerateConfirm] = useState(null); const [showDeleteConfirm, setShowDeleteConfirm] = useState(null); const scopeOptions = []; const handleCreateKey = async () => { if(!newKeyName.trim() || selectedScopes.length === 0) return; await createApiKey(newKeyName.trim(), selectedScopes); setNewKeyName("); setSelectedScopes([]); setShowCreateDialog(false)}; const handleScopeToggle = (scope) => { setSelectedScopes(prev => prev.includes(scope) ? prev.filter(s => s !== scope) [...prev, scope])}; const getExampleCode = (apiKey) => { " return "curl -X GET "https:  ,
",,
 " " " " " " export { function }; export default function ApiKeysManager() { const { apiKeys, loading, newApiKey, fetchApiKeys, createApiKey, deleteApiKey, toggleApiKey, updateApiKeyScopes, regenerateApiKey, revokeApiKey, clearNewApiKey } = useApiKeys(); const [showCreateDialog, setShowCreateDialog] = useState(false); const [newKeyName, setNewKeyName] = useState("); const [selectedScopes, setSelectedScopes] = useState([]); const [showRegenerateConfirm, setShowRegenerateConfirm] = useState(null); const [showDeleteConfirm, setShowDeleteConfirm] = useState(null); const scopeOptions = []; const handleCreateKey = async () => { if (!newKeyName.trim() || selectedScopes.length === 0) return; await createApiKey(newKeyName.trim(), selectedScopes); setNewKeyName("); setSelectedScopes([]); setShowCreateDialog(false)}; const handleScopeToggle = (scope) => { setSelectedScopes(prev => prev.includes(scope) ? prev.filter(s => s !== scope) [...prev, scope])}; const getExampleCode = (apiKey) => { " return "curl -X GET "https: """"","});,"})"
;,"});,"})"
 " " " " " " export { function }; export default function ApiKeysManager() { const { apiKeys, loading, newApiKey, fetchApiKeys, createApiKey, deleteApiKey, toggleApiKey, updateApiKeyScopes, regenerateApiKey, revokeApiKey, clearNewApiKey } = useApiKeys(); const [showCreateDialog, setShowCreateDialog] = useState(false); const [newKeyName, setNewKeyName] = useState("); const [selectedScopes, setSelectedScopes] = useState([]); const [showRegenerateConfirm, setShowRegenerateConfirm] = useState(null); const [showDeleteConfirm, setShowDeleteConfirm] = useState(null); const scopeOptions = []; const handleCreateKey = async () => { if(!newKeyName.trim() || selectedScopes.length === 0) return; await createApiKey(newKeyName.trim(), selectedScopes); setNewKeyName("); setSelectedScopes([]); setShowCreateDialog(false)}; const handleScopeToggle = (scope) => { setSelectedScopes(prev => prev.includes(scope) ? prev.filter(s => s !== scope) [...prev, scope])}; const getExampleCode = (apiKey) => { " return "curl -X GET "https:;,"});,"})"
" " " " " export { function } export default function ApiKeysManager() { const { apiKeys, loading, newApiKey, fetchApiKeys, createApiKey, deleteApiKey, toggleApiKey, updateApiKeyScopes, regenerateApiKey, revokeApiKey, clearNewApiKey } = useApiKeys() const [showCreateDialog, setShowCreateDialog] = useState(false) const [newKeyName, setNewKeyName] = useState(") const [selectedScopes, setSelectedScopes] = useState([]) const [showRegenerateConfirm, setShowRegenerateConfirm] = useState(null) const [showDeleteConfirm, setShowDeleteConfirm] = useState(null) const scopeOptions = [] const handleCreateKey = async () => { if (!newKeyName.trim() || selectedScopes.length === 0) return await createApiKey(newKeyName.trim(), selectedScopes) setNewKeyName(") setSelectedScopes([]) setShowCreateDialog(false)} const handleScopeToggle = (scope) => { setSelectedScopes(prev => prev.includes(scope) ? prev.filter(s => s !== scope) [...prev, scope])} const getExampleCode = (apiKey) => { " return "curl -X GET "https: """"","
" " " " " export: { function } export default function ApiKeysManager() { const { apiKeys, loading, newApiKey, fetchApiKeys, createApiKey, deleteApiKey, toggleApiKey, updateApiKeyScopes, regenerateApiKey, revokeApiKey, clearNewApiKey } = useApiKeys() const [showCreateDialog, setShowCreateDialog] = useState(false) const [newKeyName, setNewKeyName] = useState(") const [selectedScopes, setSelectedScopes] = useState([]) const [showRegenerateConfirm, setShowRegenerateConfirm] = useState(null) const [showDeleteConfirm, setShowDeleteConfirm] = useState(null) const scopeOptions = [] const handleCreateKey = async () => { if (!newKeyName.trim() || selectedScopes.length === 0) return await createApiKey(newKeyName.trim(), selectedScopes) setNewKeyName(") setSelectedScopes([]) setShowCreateDialog(false)} const handleScopeToggle = (scope) => { setSelectedScopes(prev => prev.includes(scope) ? prev.filter(s => s !== scope) [...prev, scope])} const getExampleCode = (apiKey) => { " return "curl -X GET "https: """"",",",","
" " " " " export { function } export default function ApiKeysManager() { const { apiKeys, loading, newApiKey, fetchApiKeys, createApiKey, deleteApiKey, toggleApiKey, updateApiKeyScopes, regenerateApiKey, revokeApiKey, clearNewApiKey } = useApiKeys() const [showCreateDialog, setShowCreateDialog] = useState(false) const [newKeyName, setNewKeyName] = useState(") const [selectedScopes, setSelectedScopes] = useState([]) const [showRegenerateConfirm, setShowRegenerateConfirm] = useState(null) const [showDeleteConfirm, setShowDeleteConfirm] = useState(null) const scopeOptions = [] const handleCreateKey = async () => { if (!newKeyName.trim() || selectedScopes.length === 0) return await createApiKey(newKeyName.trim(), selectedScopes) setNewKeyName(") setSelectedScopes([]) setShowCreateDialog(false)} const handleScopeToggle = (scope) => { setSelectedScopes(prev => prev.includes(scope) ? prev.filter(s => s !== scope) [...prev, scope])} const getExampleCode = (apiKey) => { " return "curl -X GET "https: """""",
" " " " " export { function } export default function ApiKeysManager() { const { apiKeys, loading, newApiKey, fetchApiKeys, createApiKey, deleteApiKey, toggleApiKey, updateApiKeyScopes, regenerateApiKey, revokeApiKey, clearNewApiKey } = useApiKeys() const [showCreateDialog, setShowCreateDialog] = useState(false) const [newKeyName, setNewKeyName] = useState(") const [selectedScopes, setSelectedScopes] = useState([]) const [showRegenerateConfirm, setShowRegenerateConfirm] = useState(null) const [showDeleteConfirm, setShowDeleteConfirm] = useState(null) const scopeOptions = [] const handleCreateKey = async () => { if (!newKeyName.trim() || selectedScopes.length === 0) return await createApiKey(newKeyName.trim(), selectedScopes) setNewKeyName(") setSelectedScopes([]) setShowCreateDialog(false)} const handleScopeToggle = (scope) => { setSelectedScopes(prev => prev.includes(scope) ? prev.filter(s => s !== scope) [...prev, scope])} const getExampleCode = (apiKey) => { " return "curl -X GET "https: """"","""
" " " " " export { function } export default function ApiKeysManager() { const { apiKeys, loading, newApiKey, fetchApiKeys, createApiKey, deleteApiKey, toggleApiKey, updateApiKeyScopes, regenerateApiKey, revokeApiKey, clearNewApiKey } = useApiKeys() const [showCreateDialog, setShowCreateDialog] = useState(false) const [newKeyName, setNewKeyName] = useState(") const [selectedScopes, setSelectedScopes] = useState([]) const [showRegenerateConfirm, setShowRegenerateConfirm] = useState(null) const [showDeleteConfirm, setShowDeleteConfirm] = useState(null) const scopeOptions = [] const handleCreateKey = async () => { if (!newKeyName.trim() || selectedScopes.length === 0) return await createApiKey(newKeyName.trim(), selectedScopes) setNewKeyName(") setSelectedScopes([]) setShowCreateDialog(false)} const handleScopeToggle = (scope) => { setSelectedScopes(prev => prev.includes(scope) ? prev.filter(s => s !== scope) [...prev, scope])} const getExampleCode = (apiKey) => { " return "curl -X GET "https: """","
" " " " " export {function } export default function ApiKeysManager()   {const { apiKeys, loading, newApiKey, fetchApiKeys, createApiKey, deleteApiKey, toggleApiKey, updateApiKeyScopes, regenerateApiKey, revokeApiKey, clearNewApiKey } = useApiKeys() const [showCreateDialog, setShowCreateDialog] = useState(false) const [newKeyName, setNewKeyName] = useState(") const [selectedScopes, setSelectedScopes] = useState([]) const [showRegenerateConfirm, setShowRegenerateConfirm] = useState(null) const [showDeleteConfirm, setShowDeleteConfirm] = useState(null) const scopeOptions = [] const handleCreateKey = async () => {if (!newKeyName.trim() || selectedScopes.length === 0) return await createApiKey(newKeyName.trim(), selectedScopes) setNewKeyName(") setSelectedScopes([]) setShowCreateDialog(false)} const handleScopeToggle = (scope) => {setSelectedScopes(prev => prev.includes(scope) ? prev.filter(s => s !== scope) [...prev, scope])} const getExampleCode = (apiKey) => { " return "curl -X GET "https: """"","",
" &apos; &apos; &apos; "&apos,&apos, export default function ApiKeysManager() {} const ;{ apiKeys, loading, newApiKey, fetchApiKeys, createApiKey, deleteApiKey, toggleApiKey, updateApiKeyScopes, regenerateApiKey, revokeApiKey, clearNewApiKey } = useApiKeys() const [showCreateDialog, setShowCreateDialog] = useState(false) const;const [newKeyName, setNewKeyName] = useState(&apos)&apos, const [selectedScopes, setSelectedScopes] = useState([]) const [showRegenerateConfirm, setShowRegenerateConfirm] = useState(null) const [showDeleteConfirm, setShowDeleteConfirm] = useState(null) const;const scopeOptions = []&apos; const handleCreateKey = async () => { if (!newKeyName.trim() || selectedScopes.length === 0) return await createApiKey(newKeyName.trim(), selectedScopes) setNewKeyName(&apos) setSelectedScopes([]) setShowCreateDialog(false)}&apos; const handleScopeToggle = (scope) => { setSelectedScopes(prev => prev.includes(scope) ? prev.filter(s => s !== scope) [...prev, scope])} const;const;const getExampleCode = (apiKey) => { &apos; return "curl -X GET &apos;https: &apos,&apos,""","
&quot}""
" " " " " export { function } export default function ApiKeysManager() { const { apiKeys, loading, newApiKey, fetchApiKeys, createApiKey, deleteApiKey, toggleApiKey, updateApiKeyScopes, regenerateApiKey, revokeApiKey, clearNewApiKey } = useApiKeys() const [showCreateDialog, setShowCreateDialog] = useState(false) const [newKeyName, setNewKeyName] = useState(") const [selectedScopes, setSelectedScopes] = useState([]) const [showRegenerateConfirm, setShowRegenerateConfirm] = useState(null) const [showDeleteConfirm, setShowDeleteConfirm] = useState(null) const scopeOptions = [] const handleCreateKey = async () => { if (!newKeyName.trim() || selectedScopes.length === 0) return await createApiKey(newKeyName.trim(), selectedScopes) setNewKeyName(") setSelectedScopes([]) setShowCreateDialog(false)} const handleScopeToggle = (scope) => { setSelectedScopes(prev => prev.includes(scope) ? prev.filter(s => s !== scope) [...prev, scope])} const getExampleCode = (apiKey) => { " return "curl -X GET "https: """"","""
" " " " " export { function } export default function ApiKeysManager() { const { apiKeys, loading, newApiKey, fetchApiKeys, createApiKey, deleteApiKey, toggleApiKey, updateApiKeyScopes, regenerateApiKey, revokeApiKey, clearNewApiKey } = useApiKeys() const [showCreateDialog, setShowCreateDialog] = useState(false) const [newKeyName, setNewKeyName] = useState(") const [selectedScopes, setSelectedScopes] = useState([]) const [showRegenerateConfirm, setShowRegenerateConfirm] = useState(null) const [showDeleteConfirm, setShowDeleteConfirm] = useState(null) const scopeOptions = [] const handleCreateKey = async () => { if (!newKeyName.trim() || selectedScopes.length === 0) return await createApiKey(newKeyName.trim(), selectedScopes) setNewKeyName(") setSelectedScopes([]) setShowCreateDialog(false)} const handleScopeToggle = (scope) => { setSelectedScopes(prev => prev.includes(scope) ? prev.filter(s => s !== scope) [...prev, scope])} const getExampleCode = (apiKey) => { " return "curl -X GET "https: """"""
",
""}}}}""
" " " " " export { function } export default function ApiKeysManager() { const { apiKeys, loading, newApiKey, fetchApiKeys, createApiKey, deleteApiKey, toggleApiKey, updateApiKeyScopes, regenerateApiKey, revokeApiKey, clearNewApiKey } = useApiKeys() const [showCreateDialog, setShowCreateDialog] = useState(false) const [newKeyName, setNewKeyName] = useState(") const [selectedScopes, setSelectedScopes] = useState([]) const [showRegenerateConfirm, setShowRegenerateConfirm] = useState(null) const [showDeleteConfirm, setShowDeleteConfirm] = useState(null) const scopeOptions = [] const handleCreateKey = async () => { if (!newKeyName.trim() || selectedScopes.length === 0) return await createApiKey(newKeyName.trim(), selectedScopes) setNewKeyName(") setSelectedScopes([]) setShowCreateDialog(false)} const handleScopeToggle = (scope) => { setSelectedScopes(prev => prev.includes(scope) ? prev.filter(s => s !== scope) [...prev, scope])} const getExampleCode = (apiKey) => { " return "curl -X GET "https: """"",""
" " " " " export { function } export default function ApiKeysManager() { const { apiKeys, loading, newApiKey, fetchApiKeys, createApiKey, deleteApiKey, toggleApiKey, updateApiKeyScopes, regenerateApiKey, revokeApiKey, clearNewApiKey } = useApiKeys() const [showCreateDialog, setShowCreateDialog] = useState(false) const [newKeyName, setNewKeyName] = useState(") const [selectedScopes, setSelectedScopes] = useState([]) const [showRegenerateConfirm, setShowRegenerateConfirm] = useState(null) const [showDeleteConfirm, setShowDeleteConfirm] = useState(null) const scopeOptions = [] const handleCreateKey = async () => { if (!newKeyName.trim() || selectedScopes.length === 0) return await createApiKey(newKeyName.trim(), selectedScopes) setNewKeyName(") setSelectedScopes([]) setShowCreateDialog(false)} const handleScopeToggle = (scope) => { setSelectedScopes(prev => prev.includes(scope) ? prev.filter(s => s !== scope) [...prev, scope])} const getExampleCode = (apiKey) => { " return "curl -X GET "https: """"""",
" " " " " export { function } export default function ApiKeysManager() { const { apiKeys, loading, newApiKey, fetchApiKeys, createApiKey, deleteApiKey, toggleApiKey, updateApiKeyScopes, regenerateApiKey, revokeApiKey, clearNewApiKey } = useApiKeys() const [showCreateDialog, setShowCreateDialog] = useState(false) const [newKeyName, setNewKeyName] = useState(") const [selectedScopes, setSelectedScopes] = useState([]) const [showRegenerateConfirm, setShowRegenerateConfirm] = useState(null) const [showDeleteConfirm, setShowDeleteConfirm] = useState(null) const scopeOptions = [] const handleCreateKey = async () => { if (!newKeyName.trim() || selectedScopes.length === 0) return await createApiKey(newKeyName.trim(), selectedScopes) setNewKeyName(") setSelectedScopes([]) setShowCreateDialog(false)} const handleScopeToggle = (scope) => { setSelectedScopes(prev => prev.includes(scope) ? prev.filter(s => s !== scope) [...prev, scope])} const getExampleCode = (apiKey) => { " return "curl -X GET "https: ""'"",
",,
"}}}}"""