"const mockCodeSnippets = [" {; id: "1", title: "React Hook for API Calls"," description: "Custom hook for managing API calls with loading states and error handling", language: "typescript"," code: `"interface UseApiOptions<T> {` url: string; method?: "GET" | "POST" | "PUT" | "DELETE";" body?;headers?: Record<string, string>}" export function useApi<T>({ url, method = "GET", body, headers }: UseApiOptions<T>) {" const [data, setData] = useState<T | null>(null); const [loading, setLoading] = useState(false); const [error, setError] = useState<string | null>(null); const execute = async () => {; try { setLoading (true) ; setError (null) ; const response = await fetch(url, {; 