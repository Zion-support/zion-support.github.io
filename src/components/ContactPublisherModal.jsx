"export function ContactPublisherModal({ isOpen, onClose, productId, sellerId }) {" const [subject, setSubject] = useState(");" const [message, setMessage] = useState(");" const [error, setError] = useState(");" const [isLoading, setIsLoading] = useState(false); // New loading state" const firstInputRef = useRef(null); 