export function ContactPublisherModal({ isOpen, onClose, productId, sellerId }) { const [subject, setSubject] = useState("); const [message, setMessage] = useState("); const [error, setError] = useState("); const [isLoading, setIsLoading] = useState(false); // New loading state const firstInputRef = useRef(null); const modalRef = useRef(null); useEffect(() => { if (!isOpen) return; function handleKeyDown(e) { 