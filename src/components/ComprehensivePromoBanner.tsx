import React from 'react';
  variant?: 'hero' | 'compact' | 'featured';
  showCount?: number;
  className ?  : string;
 }
          </Link>
        </div>
      </div  > );
  }
              </Link>
            </div>
          ))}
        </div>
      </div  > );
  }
              ))}
            </div>
          </div>
        </div>
          </Link>
        </div>
      </div>
    </div>
  );
};
exportdefaultComprehensivePromoBanner;