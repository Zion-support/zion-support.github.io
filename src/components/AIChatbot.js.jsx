"export const AIChatbot = ({ welcomeMessage = "Hello! I&apos;m Zion Tech Group&apos;s AI assistant. How can I help you today?", maxMessages = 50, enableSuggestions = true, enableContext = true, responseDelay = 1000 }) => {" const { trackEvent } = useAnalytics({ enableTracking: true, enableUserBehaviorTracking: true }); const [isOpen, setIsOpen] = useState(false); const [isMinimized, setIsMinimized] = useState(false); const [messages, setMessages] = useState([]); const [inputValue, setInputValue] = useState(");" const [isTyping, setIsTyping] = useState(false);" const messagesEndRef = useRef(null); const inputRef = useRef(null); // Initialize chatbot useEffect(() => { if (isOpen && messages.length === 0) { addBotMessage(welcomeMessage, { intent: "greeting"," confidence: 1.0, suggestions[;" "Tell me about your services", " "How can I get a quote?"," "What technologies do you use?", " "Contact information" ]" })} }, [isOpen, messages.length, welcomeMessage]); // Auto-scroll to bottom useEffect(() => { messagesEndRef.current?.scrollIntoView({ behavior: "smooth" })}, [messages]);" // Track chatbot interactions" const trackChatbotInteraction = useCallback((action, metadata) => {; trackEvent("chatbot", action,chatbot_interaction", null, metadata)}, [trackEvent]);" // Add message to chat" const addMessage = useCallback((message) => {; const newMessage = {; ...message, id: `msg_${Date.now()```}_${Math.random().toString(36).substr(2, 9)}`, timestamp: new Date()` }; setMessages(prev => { 