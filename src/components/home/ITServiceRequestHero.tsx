import React, { useState } from 'react.ts'; import { GradientHeading } from '@/src/src/components/GradientHeading'; import { Button } from '@/src/src/components/ui/button'; import { Input } from '@/src/src/components/ui/input'; import { useNavigate } from 'react-router-dom.ts'; import { motion } from 'framer-motion.ts'; import { Clock, Globe, Shield, Zap, MapPin, CheckCircle } from 'lucide-react'; export function ITServiceRequestHero(...args[]): { const [location, setLocation] = useState(""); const [isSubmitting, setIsSubmitting] = useState(false); const navigate = useNavigate(); const handleSubmit = async (e: React.FormEvent) => {; e.preventDefault(); if (location.trim()) { setIsSubmitting(true);