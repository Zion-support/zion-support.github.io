import React from "react";

export default function ContentPromotionBanner({
  title,
  description,
  ctaText,
  ctaLink,
  variant,
  className,
  dismissible,
}: {
  title?: string;
  description?: string;
  ctaText?: string;
  ctaLink?: string;
  variant?: string;
  className?: string;
  dismissible?: boolean;
}) {
  return null;
}