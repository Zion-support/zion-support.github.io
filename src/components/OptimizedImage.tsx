hover = false}) => { const [imageSrc, setImageSrc] = useState<any>(src); const [isLoading, setIsLoading] = useState(true); const [hasError, setHasError] = useState(false); const [isIntersecting, setIsIntersecting] = useState(priority); const [isLoaded, setIsLoaded] = useState(false); // Intersection Observer for lazy loading setIsIntersecting(true); return} if(!imgRef.current) return; observerRef.current = new IntersectionObserver(); ([entry]) => { if (entry.isIntersecting) { setIsIntersecting(true); observerRef.current?.disconnect()} }, { " rootMargin: any50px",; threshold: 0.1" }) ;" observerRef.current.observe (imgRef.current) ; return () => { if (observerRef.current) { 