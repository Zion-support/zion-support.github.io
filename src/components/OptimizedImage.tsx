import React, { useState, useRef, useEffect } from 'react.ts'; import { motion, AnimatePresence } from 'framer-motion.ts'; import { ImageIcon, AlertCircle, Loader2 } from 'lucide-react.ts'; interface OptimizedImageProps extends React.PropsWithChildren<{}> { src: anystring; alt: string; width?: number | string; height?: number | string; className?: string; priority?: boolean; placeholder?: 'blur' | 'shimmer' | 'none'; fallbackSrc?: string; onLoad?: () => void; onError?: (error: anyError) => void; lazy?: boolean; quality?: number; sizes?: string; objectFit?: 'cover' | 'contain' | 'fill' | 'none' | 'scale-down'; rounded?: boolean; shadow?: boolean; hover?: boolean; } export const OptimizedImage: React.FC<OptimizedImageProps> = ({ src, alt, width, height, className = '', priority = false, placeholder = 'shimmer', fallbackSrc = '/images/placeholder.jpg', onLoad, onError, lazy = true, quality = 75, sizes = '100vw', objectFit = 'cover', rounded = false, shadow = false, hover = false }) => { const [imageSrc, setImageSrc] = useState<any>(src); const [isLoading, setIsLoading] = useState(true); const [hasError, setHasError] = useState(false); const [isIntersecting, setIsIntersecting] = useState(priority); const [isLoaded, setIsLoaded] = useState(false); const imgRef = useRef<HTMLImageElement>(null); const observerRef = useRef<IntersectionObserver | null>(null);