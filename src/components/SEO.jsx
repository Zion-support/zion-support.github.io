import React from 'react';

export function SEO({ title, description, keywords, image, canonical, ogImage, url }) {
  // For now, just return null since we're not using react-helmet-async
  // In a production app, you might want to use a different SEO solution
  return null;
}
