import {Helmet} from 'react-helmet-async';

export function SEO($1) {
  return (
    <Helmet>
      <title>{title}</title>';
      <metaname='description' content={description}   />';
      <metaname='keywords' content={keywords}   />';
      <link rel='canonical' href={canonical}   />
    </Helmet>
  )}
export default SEO;';';