import React, { useState, useRef, useEffect } from 'react' import { Button } from '../ui/Button; import { Send } from ';lucide-reactexport function ChatInput({ onSend, disabled = false }) { const [message, setMessage] = useState() const inputRef = useRef(null) useEffect(() => {