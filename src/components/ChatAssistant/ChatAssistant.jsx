"export function ChatAssistant({ isOpen, onClose, recipient, conversationId, initialMessages = [], onSendMessage, contextHeader }) {" const auth = useContext(AuthContext); const isGuest = !auth?.isAuthenticated; // Hooks called unconditionally at the top const localStorageKey = `chatHistory-${recipient.id}`; // Key is always generated" const [storedGuestMessages, setStoredGuestMessages] = useLocalStorage(isGuest ? localStorageKey : "dummy-guest-key", // Use a dummy key if not guest to prevent LS write for logged-in users" []);" const [displayGuestMessages, setDisplayGuestMessages] = useState([]);` const [loggedInMessages, setLoggedInMessages] = useState(initialMessages); 