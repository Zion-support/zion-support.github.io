import React, { useState, useCallback, useEffect } from 'react'; import { Plus, MessageCircle, Phone, Mail, MapPin, ArrowUp, X, Settings, HelpCircle, Star, Share2, Bookmark, Download, Printer } from 'lucide-react'; interface FloatingAction { id: string; icon: React.ComponentType<{ size?: number; className?: string }>; label: string; action: () => void; color: string; priority: 'high' | 'medium' | 'low'; } interface FloatingActionButtonProps { actions?: FloatingAction[]; position?: 'bottom-right' | 'bottom-left' | 'top-right' | 'top-left'; theme?: 'light' | 'dark' | 'auto'; showScrollToTop?: boolean; showContactActions?: boolean; showUtilityActions?: boolean; } const FloatingActionButton: React.FC<FloatingActionButtonProps> = ({ actions = [], position = 'bottom-right', theme = 'auto', showScrollToTop = true, showContactActions = true, showUtilityActions = true }) => { const [isExpanded, setIsExpanded] = useState(false); const [showScrollButton, setShowScrollButton] = useState(false); const [currentTheme, setCurrentTheme] = useState<'light' | 'dark'>('light');