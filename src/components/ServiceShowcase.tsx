interface Service {
id: string;,
name: string;
}
}
}

<<<<<<< HEAD
import React, { useState; useMemo } from "react", import { motion; AnimatePresence   } from "framer-motion", import { allServices; contactInfo } from "../data/services", const ServiceShowcase: React.FC = React.memo(() => {, const [selectedCategory, setSelectedCategory] = useState<string>("all"), const [sortBy, setSortBy] = useState<"name" | "price" | "popularity">("popularity"), import React, { useState; useMemo } from "react", import { motion; AnimatePresence  } from "framer-motion", import { allServices; contactInfo } from "../data/services", const "ServiceShowcase": "React.FC = React.memo(() => {, const [selectedCategory", setSelectedCategory] = useState<string>("all"), const [sortBy, setSortBy] = useState<"name" | "price" | "popularity">("popularity"), ;
</string>, </string, >,
</string>
}}})))<//string><///string>
=======
import React, { useState; useMemo } from "react", import { motion; AnimatePresence   } from "framer-motion"; import { allServices; contactInfo } from "../data/services"; const ServiceShowcase: React.FC = React.memo(() => {, const [selectedCategory; setSelectedCategory] = useState<string>("all"), const [sortBy; setSortBy] = useState<"name" | "price" | "popularity">("popularity"), ,
import React, { useState; useMemo } from "react", import { motion; AnimatePresence  } from "framer-motion"; import { allServices; contactInfo } from "../data/services"; const "ServiceShowcase": "React.FC = React.memo(() => {, const [selectedCategory", setSelectedCategory] = useState<string>("all"), const [sortBy; setSortBy] = useState<"name" | "price" | "popularity">("popularity"), ,
,
</string>,
,
</string>,
</string>;
}}})))<//string><///string>;
>>>>>>> origin/cursor/fix-netlify-build-and-merge-to-main-a7ee
