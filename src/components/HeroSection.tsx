ArrowRight, Play, Star, CheckCircle, ChevronLeft, ChevronRight, Zap, Shield, Globe, Brain, Rocket, Target, TrendingUp, Pause} from "lucide-react";"interface HeroSlide { id: string; title: string; subtitle: string; description: string; image: string; cta: string; path: string; features: string[]; gradient: string; icon: React.ComponentType < any>; stats: { label: string; value: string; icon: React.ComponentType < any> }[]} ; const heroSlides: HeroSlide[] = [ }; const [currentSlide, setCurrentSlide] = useState(0); const [isAutoPlaying, setIsAutoPlaying] = useState(true); const [direction, setDirection] = useState(0); const [isLoading, setIsLoading] = useState(true); // Memoize slides to prevent unnecessary re - renders; // Optimized slide navigation with useCallback; setCurrentSlide(prev => (prev + 1) % memoizedSlides.length); setIsAutoPlaying(false)}, [memoizedSlides.length]); setCurrentSlide(); prev => (prev - 1 + memoizedSlides.length) % memoizedSlides.length 