export function MobileMenu({ className }) { navigationItems.push({ href: "/dashboard", label: t("nav.dashboard"), icon: Settings, matches: (path) => path.startsWith("/dashboard") })} return (<div className = { cn("md:hidden", className) }> const { user, isAuthenticated } = useAuth(); const location = useLocation(); const [isOpen, setIsOpen] = useState(false); const toggleMenu = () => setIsOpen(!isOpen); const navigationItems = [" 