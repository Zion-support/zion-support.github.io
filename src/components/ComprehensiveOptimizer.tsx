import React, { useEffect, useState, useCallback } from 'react'; import { motion, AnimatePresence } from 'framer-motion'; interface PerformanceMetrics { fcp: number; lcp: number; fid: number; cls: number; ttfb: number; memory: number; cpu: number; network: string; } interface ComprehensiveOptimizerProps { enabled?: boolean; showPanel?: boolean; position?: 'top-left' | 'top-right' | 'bottom-left' | 'bottom-right'; autoOptimize?: boolean; } export const ComprehensiveOptimizer: React.FC<ComprehensiveOptimizerProps> = ({ enabled = true, showPanel = false, position = 'top-right', autoOptimize = true }) => { const [metrics, setMetrics] = useState<PerformanceMetrics>({ fcp: 0, lcp: 0, fid: 0, cls: 0, ttfb: 0, memory: 0, cpu: 0, network: 'unknown' }); const [optimizations, setOptimizations] = useState<string[]>([]); const [isOptimizing, setIsOptimizing] = useState(false);