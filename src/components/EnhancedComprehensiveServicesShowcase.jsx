"import {;" Brain, Cloud, Shield, Users, Zap, Atom, TrendingUp, Star, ArrowRight, CheckCircle, Phone, Mail, MapPin} from "lucide-react";"const EnhancedComprehensiveServicesShowcase = () => {; const [activeCategory, setActiveCategory] = useState("all");" const [searchTerm, setSearchTerm] = useState(");" const [selectedService, setSelectedService] = useState(null);" // Transform services for the showcase const transformedServices = [; ...innovativeServices2025.map(service => ({ ...service, category: service.category,; price: service.price, description: service.description,; features: service.features, benefits: service.benefits,; icon: service.icon, color: service.color,; badge: service.badge})), ...ULTIMATE_INNOVATIVE_SERVICES_2027.map(service => ({ ...service, category: service.category || "Innovation", price: service.price || "$2,999", description: " service.description || service.tagline || "Revolutionary service"," features: service.features || [], benefits: service.benefits || [], icon: service.icon || ""," color: service.color || "from-purple-600 to-indigo-700", badge: service.badge || "New"})), ];" const filteredServices = transformedServices.filter(service => {; const matchesSearch = service.name.toLowerCase().includes(searchTerm.toLowerCase()) ||; 