"// Using regular img tag instead of Next.js Image;"export function ProductListingCard({ listing, view = "grid"," onRequestQuote, detailBasePath = "/marketplace/listing"}) {" const isGrid = view === "grid";" const navigate = useNavigate();" const [loading, setLoading] = useState(false); const [imageSrc, setImageSrc] = useState(); listing.images && listing.images.length > 0 ? listing.images[0] : "/placeholder.svg" );" const [imageError, setImageError] = useState(false); const formatPrice = () => {; if (listing.price === null) return "Custom pricing";" return `${listing.currency}${listing.price.toLocaleString()}`;" };` const handleImageError = () => {; if (!imageError) { // Prevent infinite loops if placeholder also fails" setImageSrc("/placeholder.svg");" setImageError(true);" } }; const handleRequestQuote = e => {; 