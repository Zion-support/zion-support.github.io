"// Form validation schema;"const loginSchema = z.object({; " email: z.string().email("Please enter a valid email").min(1, "Email is required"), " password: z.string().min(6, "Password must be at least 6 characters")});"function LoginForm() {" const { login, isLoading } = useAuth(); const navigate = useNavigate(); const [showPassword, setShowPassword] = useState(false); const [isSubmitting, setIsSubmitting] = useState(false); const form = useForm({; resolver: zodResolver(loginSchema), defaultValues: { " email: "," password: "}});" const onSubmit = async (data) => {"; 