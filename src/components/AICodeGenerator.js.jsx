"export const AICodeGenerator = () => {" const { trackEvent } = useAnalytics({; enableTracking: true, enableUserBehaviorTracking: true }); const [activeTab, setActiveTab] = useState("generate");" const [showAdvanced, setShowAdvanced] = useState(false);" const [customCode, setCustomCode] = useState(");" const [copied, setCopied] = useState(false);" const { isGenerating, isAnalyzing, generatedCode, codeAnalysis, suggestions, history, generateCode, analyzeCode, applySuggestion, optimizeCode, generateTests, generateDocs, clearHistory, exportCode } = useAICodeGeneration(); const [form, setForm] = useState({ prompt: ", language: "typescript"," framework: "react", style: "functional"," target: "web", quality: "development"," includeTests: false, includeDocs: false," includeErrorHandling: false, includeLogging: false,; includeMetrics: false }); // Handle form submission const handleSubmit = useCallback(async (e) => {; 