// Added useCallback"// // Assuming this path is correct from earlier exploration"// For the purpose of this subtask, let&apos;s define a local Review type if the import path is uncertain or to ensure self-containment"// In a real scenario, this would be imported from the shared types.;"export interface Review { id: string; rating: number; comment?: string | null; // Ensure this matches your actual type (e.g. review_text) review_text?: string | null; // Alternative field name for comment, created_at: string; // Or Date user?: { // Assuming user is nested and might have a name, id: string | number; // User ID could be string or number 