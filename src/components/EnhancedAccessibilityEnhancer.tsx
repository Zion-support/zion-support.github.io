import React, { useEffect, useState, useRef } from 'react'; import { motion, AnimatePresence } from 'framer-motion'; import { Eye, EyeOff, Volume2, VolumeX, Type, Contrast, Move, Keyboard, Accessibility, CheckCircle, AlertTriangle, Info, Settings, X } from 'lucide-react'; interface AccessibilitySettings { highContrast: boolean; largeText: boolean; reducedMotion: boolean; screenReader: boolean; keyboardNavigation: boolean; focusIndicators: boolean; colorBlindness: 'none' | 'protanopia' | 'deuteranopia' | 'tritanopia'; fontSize: 'small' | 'medium' | 'large' | 'xlarge'; lineHeight: 'tight' | 'normal' | 'loose'; spacing: 'compact' | 'normal' | 'comfortable'; } interface AccessibilityIssue { id: string; type: 'error' | 'warning' | 'info'; message: string; element?: HTMLElement; severity: 'high' | 'medium' | 'low'; wcagLevel: 'A' | 'AA' | 'AAA'; fix?: () => void; } interface EnhancedAccessibilityEnhancerProps { enabled?: boolean; showPanel?: boolean; autoFix?: boolean; complianceLevel?: 'A' | 'AA' | 'AAA'; } export const EnhancedAccessibilityEnhancer: React.FC<EnhancedAccessibilityEnhancerProps> = ({ enabled = true, showPanel = false, autoFix = true, complianceLevel = 'AA' }) => { const [isOpen, setIsOpen] = useState(false); const [settings, setSettings] = useState<AccessibilitySettings>({ highContrast: false, largeText: false, reducedMotion: false, screenReader: false, keyboardNavigation: false, focusIndicators: true, colorBlindness: 'none', fontSize: 'medium', lineHeight: 'normal', spacing: 'normal' }); const [issues, setIssues] = useState<AccessibilityIssue[]>([]); const [currentIssue, setCurrentIssue] = useState<AccessibilityIssue | null>(null); const [isScanning, setIsScanning] = useState(false); const [scanProgress, setScanProgress] = useState(0); const panelRef = useRef<HTMLDivElement>(null);