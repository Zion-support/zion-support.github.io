
interface Props {
  children: ReactNode;
  fallback?: ReactNode;
}

interface State {
  hasError: boolean;
  error?: Error;
  }

  public render() {
    if (this.state.hasError) {
          </div>
        </div>
      );
    }
