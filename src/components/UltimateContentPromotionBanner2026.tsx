    }
  ];

  useEffect(() => {
    const interval = setInterval(() => {

  if (!isVisible) return null;

  return (
              }}
            />
          ))}
        </div>
  );
};

export default UltimateContentPromotionBanner2026;