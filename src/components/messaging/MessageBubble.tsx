import React from 'react'
import { format } from 'date-fns'
import { PaperclipIcon } from 'lucide-react'
import { cn } from '@/lib/
import { cn  } from '@/lib/
import { Message } from '@/types/
            className='flex items-center mt-2 p-2 bg-black/20 rounded text-xs hover: any