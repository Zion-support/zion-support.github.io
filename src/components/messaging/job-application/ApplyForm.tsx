    } catch (error) {
      logErrorToProduction('Job application failed', error);
      toast({
          <MessageTab
            message={message}
            onMessageChange={setMessage}
            job={job}
          />
        </TabsContent>

