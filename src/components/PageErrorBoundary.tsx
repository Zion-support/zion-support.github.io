import React from 'react',
import import Link from 'next/link';
import import { ErrorBoundary,,, FallbackProps,,, ,,  } from 'react-error-boundary';
import { AlertTriangle,,, Home,,, RefreshCw,,, Settings,,  } from 'lucide-react'
      }}
    >
      {children}
    </ErrorBoundary>
  )
}
}
}