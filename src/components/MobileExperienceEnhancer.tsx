import React, { useState, useEffect } from 'react'; import { motion } from 'framer-motion'; import { Smartphone, Tablet, Monitor, Settings, X } from 'lucide-react'; export function MobileExperienceEnhancer() { const [isOpen, setIsOpen] = useState(false); const [deviceType, setDeviceType] = useState('desktop'); useEffect(() => { const detectDevice = () => { const width = window.innerWidth; if (width < 768) { setDeviceType('mobile'); } else if (width < 1024) { setDeviceType('tablet'); } else { setDeviceType('desktop'); } }; detectDevice(); window.addEventListener('resize', detectDevice); return () => window.removeEventListener('resize', detectDevice); }, []); if (!isOpen) { return ( <motion.button initial={{ scale: 0 }} animate={{ scale: 1 }} className="fixed bottom-4 right-4 z-50 bg-blue-600 text-white p-3 rounded-full shadow-lg hover:bg-blue-700 transition-colors" onClick={() => setIsOpen(true)} > <Smartphone className="w-6 h-6" /> </motion.button> ); } return ( <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} className="fixed bottom-4 right-4 z-50 bg-white rounded-xl shadow-xl border border-gray-200 p-4 w-80" > <div className="flex items-center justify-between mb-4"> <h3 className="text-lg font-semibold text-gray-900">Mobile Experience</h3> <button onClick={() => setIsOpen(false)} className="text-gray-400 hover:text-gray-600" > <X className="w-5 h-5" /> </button> </div> <div className="space-y-4"> <div className="flex items-center space-x-3"> {deviceType === 'mobile' && <Smartphone className="w-5 h-5 text-blue-600" />} {deviceType === 'tablet' && <Tablet className="w-5 h-5 text-blue-600" />} {deviceType === 'desktop' && <Monitor className="w-5 h-5 text-blue-600" />} <span className="text-sm text-gray-700 capitalize">{deviceType} detected</span> </div> <div className="text-sm text-gray-600"> <p>Current viewport: {window.innerWidth} x {window.innerHeight}</p> </div> <div className="pt-2 border-t border-gray-200"> <button className="w-full bg-blue-600 text-white py-2 px-4 rounded-lg text-sm font-medium hover:bg-blue-700 transition-colors"> Optimize for {deviceType} </button> </div> </div> </motion.div> ); }