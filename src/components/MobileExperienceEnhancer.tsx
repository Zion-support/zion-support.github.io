import React, { useState, useEffect, useCallback } from 'react'; import { motion, AnimatePresence } from 'framer-motion'; interface MobileExperienceEnhancerProps { children: React.ReactNode; } const MobileExperienceEnhancer: React.FC<MobileExperienceEnhancerProps> = ({ children }) => { const [isMobile, setIsMobile] = useState(false); const [showBackToTop, setShowBackToTop] = useState(false); const [touchStart, setTouchStart] = useState<number | null>(null); const [touchEnd, setTouchEnd] = useState<number | null>(null);