import React, { useEffect, PropsWithChildren } from 'react'; const AccessibilityEnhancer: React.FC<PropsWithChildren> = ({ children }) => { useEffect(() => { // Skip link functionality const handleKeyDown = (event: KeyboardEvent) => { if (event.key === 'Tab') { document.body.classList.add('keyboard-navigation'); } 