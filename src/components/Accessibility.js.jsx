"import {;" Eye, EyeOff, Volume2, VolumeX, Keyboard, Accessibility, X} from "lucide-react";"const AccessibilityContext = createContext(null);"export const useAccessibility = () => {; const context = useContext(AccessibilityContext); if (!context) { throw new Error(" "useAccessibility must be used within an AccessibilityProvider" );" } return context; }; // Accessibility Provider Component; export const AccessibilityProvider = ({ children }) => {; const [highContrast, setHighContrast] = useState(false); const [reducedMotion, setReducedMotion] = useState(false); const [fontSize, setFontSize] = useState("medium");" const [colorBlindMode, setColorBlindMode] = useState("none");" // Load settings from localStorage" useEffect(() => { const savedSettings = localStorage.getItem("zion-accessibility-settings");" if (savedSettings) {" const settings = JSON.parse(savedSettings); setHighContrast(settings.highContrast || false); setReducedMotion(settings.reducedMotion || false); setFontSize(settings.fontSize || "medium");" setColorBlindMode(settings.colorBlindMode || "none");" }" }, []); // Save settings to localStorage useEffect(() => { const settings = {; highContrast, reducedMotion, fontSize, colorBlindMode}; localStorage.setItem(" "zion-accessibility-settings", JSON.stringify(settings)" );" }, [highContrast, reducedMotion, fontSize, colorBlindMode]); // Apply accessibility settings to document useEffect(() => { const root = document.documentElement; // High contrast mode if (highContrast) { root.classList.add("high-contrast");" } else {" root.classList.remove("high-contrast");" }" // Reduced motion if (reducedMotion) { root.classList.add("reduced-motion");" } else {" root.classList.remove("reduced-motion");" }" // Font size root.style.fontSize =" fontSize === "small" ? "14px" : fontSize === "large" ? "18px" : "16px";" // Color blind mode" root.style.filter =" colorBlindMode === "none" ? "none" : colorBlindMode === "protanopia" ? "url(#protanopia)" : colorBlindMode === "deuteranopia" ? "url(#deuteranopia)" : "url(#tritanopia);" }, [highContrast, reducedMotion, fontSize, colorBlindMode]);" const toggleReducedMotion = () => setReducedMotion(!reducedMotion); const value = {; highContrast, reducedMotion, fontSize, colorBlindMode, toggleHighContrast, toggleReducedMotion, setFontSize, setColorBlindMode}; return(); 