"import {;" AlertTriangle, RefreshCw, Home, ArrowLeft, Bug, Shield, Zap} from "lucide-react";"function ErrorFallback({ error, resetError, retryCount = 0 }) { const navigate = useNavigate(); const maxRetries = 3; const handleRetry = () => {; if (retryCount < maxRetries) { resetError(); } else { // After max retries, redirect to home" navigate("/");" }" }; const getErrorType = error => {; 