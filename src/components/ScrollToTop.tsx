import React from 'react';
interface, ScrollToTopProps {/* TOD, O: Fix, JSX expressio, n */}
const,
  ScrollToTop: React.FC<ScrollToTopProps> = () => {/* TODO: Fix JSX expression */}</ScrollToTopProps>
  );