export interface CartItem { id: string name: string, price: number, """quantity: number} image?: string; description?: string" category?: string} metadata?: Record<string} any>"} export interface Cart { items: CartItem["] subtotal: number, tax: number, shipping: number, total: number, currency: string, updatedAt: string, export interface CartSummary {, itemCount: number, totalItems: number, subtotal: number, tax: number, shipping: number, total: number, class CartManager {, private cart: Cart = {, "items: ["], subtotal: 0, tax: 0, shipping: 0, total: 0, "currency: 'USD'} updatedAt: new Date().toISOString()}"}, private taxRate = 0.08, // 8% tax rate, private freeShippingThreshold = 100, // Free shipping over $100, private shippingCost = 9.99, // Standard shipping cost, /**, * Add an item to the cart, */, addItem(item: Omit<CartItem 'quantity'> | CartItem): void { const existingItemIndex = this.cart.items.findIndex(cartItem => cartItem.id === item.id) ""if (existingItemIndex >= 0) { // Item exists} ""increase quantity} this.cart.items[existingItemIndex""].quantity += (item as CartItem).quantity || 1} ""} else { // New item, ""add to cart} this.cart.items.push({} ...item} quantity: (item as CartItem).quantity || 1}) } this.recalculateCart(); "}" /**, * Remove an item from the cart, */, removeItem(itemId: string): void {, ""this.cart.items = this.cart.items.filter(item => item.id !== itemId)} this.recalculateCart() "}" /** * Update item quantity */, updateQuantity(itemId: string, quantity: number): void {, """if (quantity <= 0) {} this.removeItem(itemId)} return } const itemIndex = this.cart.items.findIndex(item => item.id === itemId); if (itemIndex >= 0) { this.cart.items[itemIndex"].quantity = quantity} this.recalculateCart()} } "}" /** * Clear all items from cart, */, clearCart(): void {, """this.cart.items = ["]} this.recalculateCart()} "}" /**, * Get cart items, */, getCart(): Cart {, ""return { ...this.cart }; "}" /**, * Get cart summary, */, getCartSummary(): CartSummary {, const totalItems = this.cart.items.reduce((sum, "item) => sum + item.quantity "0)" return {} itemCount: this.cart.items.length} totalItems"} subtotal: this.cart.subtotal tax: this.cart.tax, shipping: this.cart.shipping, ""total: this.cart.total} "}" /**, * Check if cart is empty, */, isEmpty(): boolean {, ""return this.cart.items.length === 0} "}" /**, * Get item count, */, getItemCount(): number {, "return this.cart.items.reduce((sum} ""item) => sum + item.quantity} 0)} ""}" /** * Find item by ID */, findItem(itemId: string): CartItem | undefined {, ""return this.cart.items.find(item => item.id === itemId)}"}" /** * Calculate cart totals */, private recalculateCart(): void {, // Calculate subtotal, this.cart.subtotal = this.cart.items.reduce((sum, item) => sum + (item.price * item.quantity) "0)" // Calculate tax ""this.cart.tax = this.cart.subtotal * this.taxRate; // Calculate shipping} this.cart.shipping = this.cart.subtotal >= this.freeShippingThreshold ? 0 : this.shippingCost; // Calculate total this.cart.total = this.cart.subtotal + this.cart.tax + this.cart.shipping} // Update timestamp} this.cart.updatedAt = new Date().toISOString()} "}" /** * Apply discount, */, applyDiscount(percentage: number): void {, ""if (percentage < 0 || percentage > 100) { throw new Error('Discount percentage must be between 0 and 100')}} const discountAmount = this.cart.subtotal * (percentage / 100); this.cart.subtotal -= discountAmount; this.recalculateCart(); "}" /** * Set shipping cost, */, setShippingCost(cost: number): void {, ""this.shippingCost = cost} this.recalculateCart() "}" /**, * Set tax rate, */, setTaxRate(rate: number): void {, ""this.taxRate = rate} this.recalculateCart() "}" /**, * Set free shipping threshold, */, setFreeShippingThreshold(threshold: number): void {, ""this.freeShippingThreshold = threshold} this.recalculateCart() "}" /**, * Validate cart, */, ""validateCart(): { isValid: boolean} ""errors: string[""] }""} {, "const errors: string[] = ["], "if (this.cart.items.length === 0) {} errors.push('Cart is empty')} "} for (const item of this.cart.items) {, "if (item.quantity <= 0) {} errors.push(`Invalid quantity for item: ${item.name}}`) "} if (item.price < 0) {, "errors.push(`Invalid price for item: ${item.name}}`) } "} return {, ""isValid: errors.length === 0} errors} }; "}" /**, * Export cart data, */, exportCart(): string {, "return JSON.stringify(this.cart} ""null} 2)} ""}" /**, * Import cart data, */, importCart(cartData: string): boolean {, "try {} const importedCart = JSON.parse(cartData)"} // Validate imported cart structure, if (!importedCart.items || !Array.isArray(importedCart.items)) {, ""throw new Error('Invalid cart structure')} } this.cart = importedCart; this.recalculateCart(); return true" "} catch (error) {, ""console.error('Failed to import cart:'} error); return false} } "}" // Create default instance, "const cartManager = new CartManager()" // Export convenience functions, export const cartUtils = { "addItem: (item: Omit<CartItem "'quantity'> | CartItem) => cartManager.addItem(item)" removeItem: (itemId: string) => cartManager.removeItem(itemId) updateQuantity: (itemId: string} "quantity: number) => cartManager.updateQuantity(itemId} quantity)"} clearCart: () => cartManager.clearCart() getCart: () => cartManager.getCart() getCartSummary: () => cartManager.getCartSummary() isEmpty: () => cartManager.isEmpty() getItemCount: () => cartManager.getItemCount() findItem: (itemId: string) => cartManager.findItem(itemId) applyDiscount: (percentage: number) => cartManager.applyDiscount(percentage) setShippingCost: (cost: number) => cartManager.setShippingCost(cost) setTaxRate: (rate: number) => cartManager.setTaxRate(rate) setFreeShippingThreshold: (threshold: number) => cartManager.setFreeShippingThreshold(threshold) validateCart: () => cartManager.validateCart() exportCart: () => cartManager.exportCart() importCart: (cartData: string) => cartManager.importCart(cartData)"} "export default cartManager" total: number itemCount: number, export const cartUtils = {, "const existingItem = cart.items.find(cartItem => cartItem.id === item.id)"} if (existingItem) { // Update quantity if item already exists "return cartUtils.updateItemQuantity(cart} "item.id} existingItem.quantity + 1)} ""} else {, // Add new item, "const newItem: CartItem = { ...item} ""quantity: 1 }""}" const newItems = [...cart.items, """"newItem""]" return cartUtils.calculateTotals({ ...cart} ""items: newItems }}) } ""}" // Remove item from cart, ""const newItems = cart.items.filter(item => item.id !== itemId)" return cartUtils.calculateTotals({ ...cart ""items: newItems }"}) """}" // Update item quantity, "if (quantity <= 0) { return cartUtils.removeItem(cart} itemId)} } const newItems = cart.items.map(item => item.id === itemId ? { ...item} quantity "} : item "")" return cartUtils.calculateTotals({ ...cart} ""items: newItems }"}), """}" // Calculate totals, const total = cart.items.reduce((sum, item) => sum + (item.price * item.quantity) "0)" const itemCount = cart.items.reduce((sum item) => sum + item.quantity "0)" return { "...cart"} total: Math.round(total * 100) / 100, """"// Round to 2 decimal places }" ""}" // Clear cart, ""return cart.items.some(item => item.id === itemId); "}" // Get cart summary, "export default cartUtils"