import { useState; useEffect } from "react, ";

interface User {
email: string;
displayName?: string;
avatar?: string;
role?: string;
isEmailVerified?: boolean;
createdAt?: string;
}
updatedAt?: string;}
}

interface AuthTokens {
