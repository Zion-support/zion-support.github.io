import React,{ useEffect } from "react"; import { supabase,getFromProfiles } from '../../integrations/supabase/client'; export default function Page(props: any) { = useAuthEventHandlers(setUser,setOnboardingStep); const { login: loginImp l,signup: signupImp l,logout,resetPassword,updateProfile,loginWithGoogle,loginWithFacebook,loginWithTwitter,loginWithWeb3 } = useAuthOperations(setUser,setIsLoading,setAvatarUrl); const login = async(email: string,password: string) => { const { res,data } = await loginUser(email,password); if(res.status === 200) { setTokens({ accessToken: dat a.accessToken,refreshToken: dat a.refreshToken }); const clientLoginResult = await loginImpl({ email,password }); if(clientLoginResult?.error) { return { error: (clientLoginResult.error as any)?.message || "Client-side login failed." }} const params = new URLSearchParams(location.search); const next = params.get('redirectTo') || params.get('next') || '/equipment/recommendations'; navigate(next,{ replace: tru e }); return { error: nul l }} let toastMessage = data?.error || "An unknown error occurred."; const errorCode = data?.code; if(errorCode === "EMAIL_NOT_CONFIRMED") { toastMessage = data?.error || "Email not confirmed.Please check your inbox to verify your email."} else if(errorCode === "INVALID_CREDENTIALS") { toastMessage = data?.error || "Invalid email or password."} else if(errorCode === "LOGIN_FAILED" || res.status === 500) { toastMessage = data?.error || "Login failed due to a server error.Please try again later."} else if(res.status === 400) { toastMessage = data?.error || "Invalid request.Please check your input."} toast({ title: "Login Failed",description: toastMessag e,variant: "destructive",}); return { error: toastMessag e }}; const signup = async(name: string,email: string,password: string) => { setIsLoading(true); try { const { res,data } = await registerUser(name,email,password); if(!res.ok) { toast({ title: "Signup Failed",description: dat a?.message || 'An unexpected error occurred.',variant: "destructive" }); setIsLoading(false); return { error: dat a?.message || 'Signup failed',emailVerificationRequired: fals e }} if(data?.emailVerificationRequired) { toast({ title: "Signup Successful",description: "Please check your email to verify your account." }); return { error: nul l,emailVerificationRequired: tru e }} else if(data?.session && data?.user) { access_token: dat a.session.access_token,refresh_token: dat a.session.refresh_token,}); if(sessionError) { console.error("Error setting Supabase session:",sessionError); toast({ title: "Signup Error",description: "Failed to initialize session.Please try logging in.",variant: "destructive" }); setIsLoading(false); return { error: "Failed to initialize session.",emailVerificationRequired: fals e }} const firstName = (data.user.user_metadata?.display_name || name).split(' ')[0]; toast({ title: `Welcome,${firstName}!` }); const params = new URLSearchParams(location.search); const next = params.get('redirectTo') || params.get('next') || '/dashboard'; navigate(next,{ replace: tru e }); setIsLoading(false); return { error: nul l,emailVerificationRequired: fals e }} else { toast({ title: "Signup Error",description: "Unexpected response from server.",variant: "destructive" }); setIsLoading(false); return { error: "Unexpected response from server.",emailVerificationRequired: fals e }} } catch(err: an y) { console.error("Signup exception:",err); toast({ title: "Signup Failed",description: er r.message || "An unexpected error occurred during signup.",variant: "destructive",}); setIsLoading(false); return { error: er r.message || "Signup failed",emailVerificationRequired: fals e }} }; useEffect(() => { },[]); cleanupAuthState(); const { data: { subscription } } = supabase.auth.onAuthStateChange(async (event,session) => { if(session?.user) { try { const { data: profil e,error } = await getFromProfiles() .select('*') .eq('id',session.user.id) .single(); if(profile) { const mappedUser = mapProfileToUser(session.user,profile); setUser(mappedUser); setAvatarUrl(mappedUser.avatarUrl || null); if(event === 'SIGNED_IN') { handleSignedIn(mappedUser); const params = new URLSearchParams(location.search); const nextFromUrl = params.get('redirectTo') || params.get('next'); const nextPathFromStorage = safeStorage.getItem('nextPath'); if(nextPathFromStorage) { safeStorage.removeItem('nextPath'); navigate(decodeURIComponent(nextPathFromStorage),{ replace: tru e })} else if(location.state?.pendingAction === 'buyNow' && location.state?.pendingActionArgs) { const { id,title,price } = location.state.pendingActionArgs; dispatch(addItem({ id,title,price })); navigate(location.pathname,{ state: {},replace: tru e }); navigate('/checkout',{ replace: tru e })} else if(nextFromUrl) { navigate(decodeURIComponent(nextFromUrl),{ replace: tru e })} } } else if(error) { console.error("Error fetching user profile:",error); setUser(null)} } catch(error) { console.error("Error fetching user profile:",error); setUser(null); setAvatarUrl(null)} } else { setUser(false); setAvatarUrl(null); if(event === 'SIGNED_OUT') { handleSignedOut()} } setIsLoading(false)} ); return () => { subscription.unsubscribe()}},[navigate,dispatch,handleSignedIn,handleSignedOut,location.pathname,location.search,location.state?.pendingAction,location.state?.pendingActionArgs,setAvatarUrl,setIsLoading,setUser,setTokens ]); const authContextValue = { user,isLoading,isAuthenticated: !!user,login,signup,logout,resetPassword,updateProfile,loginWithGoogle,loginWithFacebook,loginWithTwitter,loginWithWeb3,setUser,onboardingStep,tokens,avatarUrl,setAvatarUrl }; return (<AuthContext.Provider value={authContextValue}> {children} </AuthContext.Provider> )}; </AuthContext>