
import Link from 'next/
import { useRouter } from 'next/
import { cn } from '@/lib/
import { useAuth } from '@/hooks/
import { useFavorites } from '@/hooks/
import { useCart } from '@/context/
import { LanguageSelector } from '@/components/header/
 from '@/components/ui/
import { MiniCartPreview } from '@/components/cart/
import { LoginModal } from '@/components/auth/LoginModal'; import { LanguageSelector } from '@/components/header/
import { HoverCard, HoverCardTrigger, HoverCardContent } from '@/components/ui/
import { LoginModal } from '@/components/auth/
      href: any
      matches: (path: string) => path = = '/
      href: any
      matches: (path: string) => path.startsWith('/
      href: any
      matches: (path: string) => path.startsWith('/
      href: any
        path.startsWith('/talent') && !path.includes('/
      href: any
      matches: (path: string) => path.startsWith('/
      href: any
        path.startsWith('/community') |path.startsWith('/
      href: any
        path = = '/
        path = = '/
        path = = '/
      href: any
      matches: (path: string) => path.startsWith('/
                      ? 'bg-zion-purple/
                      : any
                  href='/
                    router.pathname = = '/
                      ? 'bg-zion-purple/
                      : any
                  href='/
                    router.pathname = = '/
                      ? 'bg-zion-purple/
                      : any
                  href='/
                    router.pathname = = '/
                      ? 'bg-zion-purple/
                      : any
                    href='/
                      'inline-flex h-9 items-center justify-center rounded-md px-4 text-sm font-medium transition-colors relative focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary',                      router.pathname.startsWith('/
                        ? 'bg-zion-purple/
                        : any
      matches: (path: string) => path = = '/
      href: any
      matches: (path: string) => path.startsWith('/
        { key: 'cloud-devops', href: '/services/cloud-devops', name: 'Cloud & DevOps', matches: (p: string) => p.startsWith('/services/
        { key: 'digital-twin', href: '/services/digital-twin', name: 'Digital Twin', matches: (p: string) => p.startsWith('/services/
        { key: 'data-analytics', href: '/services/data-analytics', name: 'Data Analytics', matches: (p: string) => p.startsWith('/services/
        { key: 'it-infrastructure', href: '/services/it-infrastructure', name: 'IT Infrastructure', matches: (p: string) => p.startsWith('/services/
        { key: 'ai-business-intelligence', href: '/services/ai-business-intelligence', name: 'AI Business Intelligence', matches: (p: string) => p.startsWith('/services/
        { key: 'ai-sales-copilot', href: '/services/ai-sales-copilot', name: 'AI Sales Copilot', matches: (p: string) => p.startsWith('/services/
        { key: 'cloud-finops-optimizer', href: '/services/cloud-finops-optimizer', name: 'Cloud FinOps Optimizer', matches: (p: string) => p.startsWith('/services/
        { key: 'ai-compliance-assistant', href: '/services/ai-compliance-assistant', name: 'AI Compliance Assistant', matches: (p: string) => p.startsWith('/services/
        { key: 'ai-auto-email-responder', href: '/services/ai-auto-email-responder', name: 'AI Auto Email Responder', matches: (p: string) => p.startsWith('/services/
        { key: 'mobile-feedback-surveys', href: '/services/mobile-feedback-surveys', name: 'Feedback Surveys', matches: (p: string) => p.startsWith('/services/
        { key: 'ai-compliance-copilot', href: '/services/ai-compliance-copilot', name: 'AI Compliance Copilot', matches: (p: string) => p.startsWith('/services/
        { key: 'llm-content-studio', href: '/services/llm-content-studio', name: 'LLM Content Studio', matches: (p: string) => p.startsWith('/services/
        { key: 'finops-advisor', href: '/services/finops-advisor', name: 'FinOps Advisor', matches: (p: string) => p.startsWith('/services/
        { key: 'returns-management', href: '/services/returns-management', name: 'Returns Management', matches: (p: string) => p.startsWith('/services/
        { key: 'email-sequencer', href: '/services/email-sequencer', name: 'Email Sequencer', matches: (p: string) => p.startsWith('/services/
        { key: 'podcast-transcription', href: '/services/podcast-transcription', name: 'Podcast Transcription', matches: (p: string) => p.startsWith('/services/
        { key: 'micro-crm', href: '/services/micro-crm', name: 'Micro CRM', matches: (p: string) => p.startsWith('/services/
        { key: 'website-analytics', href: '/services/website-analytics', name: 'Website Analytics', matches: (p: string) => p.startsWith('/services/
        { key: 'it-helpdesk', href: '/services/it-helpdesk', name: 'IT Helpdesk', matches: (p: string) => p.startsWith('/services/
        { key: 'affiliate-tracking', href: '/services/affiliate-tracking', name: 'Affiliate Tracking', matches: (p: string) => p.startsWith('/services/
        { key: 'mobile-survey', href: '/services/mobile-survey', name: 'Mobile Survey', matches: (p: string) => p.startsWith('/services/
      href: any
      matches: (path: string)  => path.startsWith('/
      href: any
      matches: (path: string)  => path.startsWith('/
      href: any
      matches: (path: string)  => path.startsWith('/
      href: any
      matches: (path: string)  => path.startsWith('/
      href: any
      matches: (path: string)  => path.startsWith('/
      matches: (path: string)  => path.starts_with ('/
      href: any
      matches: (path: string) => path.startsWith('/