/**
 * Advanced Security Manager
    };

    this.metrics = {
      totalEvents: 0,
      blockedEvents: 0,
      eventsByType: {},
      eventsBySeverity: {},
      threatLevel: 'low'
    };

    this.initializeSecurity();
  }

      }
    });
  }

    const suspiciousPatterns = [
      /javascript:/i,
      /on\w+\s*=/i,
