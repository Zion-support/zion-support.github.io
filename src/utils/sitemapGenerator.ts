interface SitemapUrl { url: string; lastmod?: string; changefreq?: "always" | "hourly" | "daily" | "weekly" | "monthly" | "yearly" | "never"; priority?: number;   } interface SitemapConfig { baseUrl: string; urls: SitemapUrl[]; outputPath?: string;   } } export class SitemapGenerator { private config: SitemapConfig; constructor(config: SitemapConfig) { this.config = config; } generateXML(): string { const { baseUrl, urls } = this.config; const _xmlUrls = urls.map(url => { const _lastmod = url.lastmod || new Date().toISOString().split("T")[0]; const _changefreq = url.changefreq || "weekly"; const _priority = url.priority || 0.5; return " <url> <loc>${baseUrl}${url.url}</loc> <lastmod>${lastmod}</lastmod> <changefreq>${changefreq}</changefreq> <priority>${priority}</priority> </url>"; }).join("
"); return `<?xml version = "1.0" encoding="UTF-8"?>; <urlset xmlns="http:
