import React, { useState, useEffect, useRef } from 'react'
import Image from 'next/
import { cn } from '@/lib/
  //If it's already optimized or external,  return as-is if (originalSrc && originalSrc.startsWith ('http') || originalSrc && originalSrc.includes ('/ next/
  //