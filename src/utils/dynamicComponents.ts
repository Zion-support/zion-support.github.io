/**
 * Dynamic component imports - reduces initial bundle size
 * Auto-generated by performance optimizer
 */
import React from "react";

const LoadingSpinner = () =>
  React.createElement(
    "div",
    {
      className: "flex items-center justify-center p-4",
    },
    React.createElement(
      "div",
      {
        className: "animate-spin rounded-full h-8 w-8 border-b-2 border-blue-600",
      }
    )
  );

// Lazy load heavy components
export const LazyServicesShowcase = React.lazy(() => 
  import("../components/ServicesShowcase").catch(() => ({
    default: () => React.createElement("div", { className: "p-4 text-center" }, "Services temporarily unavailable")
  }))
);

export const LazyPricingSection = React.lazy(() => 
  import("../components/PricingSection").catch(() => ({
    default: () => React.createElement("div", { className: "p-4 text-center" }, "Pricing temporarily unavailable")
  }))
);

export const LazyTestimonials = React.lazy(() => 
  import("../components/TestimonialsSection").catch(() => ({
    default: () => React.createElement("div", { className: "p-4 text-center" }, "Testimonials temporarily unavailable")
  }))
);

export const LazyContactForm = React.lazy(() => 
  import("../components/ContactForm").catch(() => ({
    default: () => React.createElement("div", { className: "p-4 text-center" }, "Contact form temporarily unavailable")
  }))
);

// Higher-order component for error boundaries
export const withErrorBoundary = (Component: React.ComponentType<any>) => {
  return React.forwardRef((props: any, ref: any) => {
    return React.createElement(
      React.Suspense,
      { fallback: React.createElement(LoadingSpinner) },
      React.createElement(Component, { ...props, ref })
    );
  });
};

// Export loading component
export { LoadingSpinner };