exportconst measurePerformance = () => {if (type ofwindow === "undefined") return;"; // WebVitalsmonitoring const logMetric = (metri, c: an, y) => {consol, e.log (` [Performance] $ {metric.name` }:`metric.value) ;` }; // FirstContentfulPaint" if ("PerformanceObserver" inwindow) {; tr, y {const observer = newPerformanceObserver ( (lis, t) => {for (constentryof list.getEntries () ) {" if (entr, y.entryType === "paint") {; logMetri, c ({nam, e: entr, y.nam, e value: entry.startTime; " consol, e.error ("Performanceobservationerror: "e) ;"; } }// Reportpageload tim, e if (windo, w.performanc, e && window.performance.timing) {" windo, w.addEventListener ("load" () => {; nam" e: 'PageLoadTime""; value: pageLoadTime }) ; }0) ; }) ; } };"