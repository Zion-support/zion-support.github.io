'use client'
import { logger } from './logger';
/**
 * Advanced Performance Monitoring System;
 * Tracks Core Web Vitals and custom performance metrics;
 */
<keyof WebVitals, { good: number; poor: number }> = {
    <keyof WebVitals, { good: number; poor: number }> = {<= threshold.good) return 'good'
    if (value <= threshold.poor) return 'needs-improvement'
    return 'poor'
    };
    const threshold = thresholds[name];
    if (!threshold) return 'good';
    if (value;
          <= threshold.good) return 'good';
    if (value <= threshold.poor) return 'needs-improvement';
    return 'poor';
  }
   * Record a custom metric;
  recordCustomMetric(name: string, value: number, unit: CustomMetric['unit']): void {
    <void> {
    <void> {<T>(name: string, fn: () => T): T {
    <T>(name: string, fn: () => T): T {<T>(name: string, fn: () => Promise<T>): Promise<T> {
    <T>(name: string, fn: () => Promise<T>): Promise<T> {
}}}}}}}