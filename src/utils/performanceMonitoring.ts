export const measurePerformance = () => { if (typeof window === 'undefined') return;'; // Web Vitals monitoring const logMetric = (metric: any) => { console.log(`[Performance] ${metric.name }:`, metric.value); }; // First Contentful Paint if ('PerformanceObserver' in window) {; try { const observer = new PerformanceObserver((list) => { for (const entry of list.getEntries()) { if (entry.entryType === 'paint') {; logMetric({ name: entry.name, value: entry.startTime 