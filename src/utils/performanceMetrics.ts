/** * Performance Metrics Dashboard Utility * * Comprehensive performance monitoring and metrics collection for * web vitals, resource timing, and custom performance markers. * * Features: * - Core Web Vitals tracking (LCP, FID, CLS, FCP, TTFB), * - Custom performance markers * - Resource timing analysis * - Performance budgets and alerts * - Real-time performance dashboard data */ export interface PerformanceMetric { name: string;, value: number;, rating: 'good' | 'needs-improvement' | 'poor';, timestamp: number; } export interface ResourceTiming { name: string;, duration: number;, size: number;, type: string; } export interface PerformanceBudget { metric: string;, budget: number;, current: number;, status: 'pass' | 'warn' | 'fail'; 