'use client';
import React from 'react';

interface Props {
  [key: string]: any;
}

const HealthCheck.ts: React.FC<Props> = (props) => {
  return (
    <div className="p-4">
      <h2>HealthCheck.ts</h2>
      <p>This component is under development.</p>
    </div>
  );
};

export default HealthCheck.ts;
