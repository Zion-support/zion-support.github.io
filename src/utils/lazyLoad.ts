import { la z y ComponentTy p e } from ' react ' ; / * * * Enhanced lazy loading with retry logic for failed chunk loa d s * Helps recover from network issues during code splittin g * / export function lazyRetry<TextendsComponentType<any>>(componentImport: ( ) = > Promi s e < { defau l t: T } > retri e s = 3 del a y = 1 0 0 0 ): Rea c t.LazyExoticComponent < T > { return laz y ( ( ) = > { return new Promise < { defau l t: T } > ( ( resol v e reje c t ) = > { const attemptLoad = ( attemptsLe f t: num b e r ) = > { componentImpo r t ( ) .th e n ( resol v e ) .catch ( error = > { if ( attemptsLe f t = = = 1 ) { reje c t ( error ); ret u r n; } / / Wait before retrying setTimeo u t ( ( ) = > { console.log( ` Retrying component load... ( $ { attemptsLe f t - 1 } attempts lef t ) ` ); attemptLo a d ( attemptsLe f t - 1 ); } del a y ); } ); }; attemptLo a d ( retri e s ); } ); } ); } / * * * Preload a component for better perceived performan c e * / export function preloadComponent(componentImport: ( ) = > Promi s e < { defau l t: ComponentTy p e < an y > } > ): vo i d { componentImpo r t ( ) .catch ( error = > { console.warn( ' Failed to preload compon e n t: ' error ); } ); } / * * * Create a lazy - loaded component with automatic retr y * / export const createLazyComponent = < T extends ComponentType < a n y > > ( import F n: ( ) = > Promi s e < { defau l t: T } > ) = > { return lazyRetr y ( import F n 3 10 0 0 ); }; export default lazyRetry; $ $ 