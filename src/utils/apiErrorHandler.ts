import { toast } from '@/hooks/use-toast'; interface ApiError { message?: string; status?: number; code?: string; } export const showApiError = (error?: ApiError, fallbackMessage?: string) => { const message = error?.message || fallbackMessage || 'An unexpected error occurred'; toast({ variant: "destructive", title: "Error", description: message, }); }; export const handleApiError = (error: any) => { console.error('API Error:', error); if (error.response) {