import { toast } from '@/hooks/use-toast'; interface ApiError { message?: string; status?: number; } export const showApiError = (error: ApiError | null, fallbackMessage?: string) => { const message = error?.message || fallbackMessage || 'An unexpected error occurred'; toast({ variant: "destructive", title: "Error", description: message, }); };