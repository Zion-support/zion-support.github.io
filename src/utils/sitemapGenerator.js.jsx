export class SitemapGenerator { config; constructor(config) { this.config = { outputPath: './public/sitemap.xml', ...config }; } generateXML() { const { baseUrl, urls } = this.config; const xmlHeader = '<?xml version="1.0" encoding="UTF-8"?>'; const urlElement = `<url> <loc>${baseUrl}${url.url}</loc> ${url.lastmod ? `<lastmod>${url.lastmod}</lastmod>` : ''} ${url.changefreq ? `<changefreq>${url.changefreq}</changefreq>` : ''} ${url.priority ? `<priority>${url.priority}</priority>` : ''} </url>`; return urlElement.replace(/\s+/g, ' ').trim(); }).join(''); return `${xmlHeader}\n${urlsetOpen}\n${urlElements}\n${urlsetClose}`; } generateIndex(sitemaps) { const xmlHeader = '<?xml version="1.0" encoding="UTF-8"?>'; const sitemapElements = sitemaps.map(sitemap => { return `<sitemap> <loc>${sitemap}</loc> <lastmod>${new Date().toISOString()}</lastmod> </sitemap>`; }).join(''); return `${xmlHeader}\n${sitemapindexOpen}\n${sitemapElements}\n${sitemapindexClose}`; } generateRobotsTxt() { const { baseUrl } = this.config; return `User-agent: * Allow: / # Sitemaps Sitemap: ${baseUrl}/sitemap.xml # Disallow admin and private areas Disallow: /admin/ Disallow: /private/ Disallow: /api/ Disallow: /_next/ # Allow important pages Allow: / Allow: /services/ Allow: /solutions/ Allow: /about/ Allow: /contact/ Allow: /blog/ Allow: /careers/ # Crawl delay (optional) Crawl-delay: 1`; } generateJSON() { const { baseUrl, urls } = this.config; const jsonSitemap = { baseUrl, urls: urls.map(url => ({ ...url, fullUrl: `${baseUrl }${url.url}`, lastmod: url.lastmod || new Date().toISOString() })) }; return JSON.stringify(jsonSitemap, null, 2); } generateHTML() { const { baseUrl, urls } = this.config; const html = `<!DOCTYPE html> <html lang="en"> <head> <meta charset="UTF-8"> <meta name="viewport" content="width=device-width, initial-scale=1.0"> <title>Sitemap - Zion Tech Group</title> <meta name="description" content="Complete sitemap of Zion Tech Group website"> <style> body { font-family: Arial, sans-serif; margin: 40px; line-height: 1.6; } .container { max-width: 1200px; margin: 0 auto; } h1 { color: #00e5ff; border-bottom: 2px solid #00e5ff; padding-bottom: 10px; } .sitemap-section { margin: 30px 0; } .sitemap-section h2 { color: #333; margin-bottom: 15px; } .sitemap-links { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 20px; } .sitemap-link { padding: 10px; border: 1px solid #ddd; border-radius: 5px; text-decoration: none; color: #333; } .sitemap-link:hover { background-color: #f5f5f5; border-color: #00e5ff; } .priority-high { border-left: 4px solid #00e5ff; } .priority-medium { border-left: 4px solid #ff9800; } .priority-low { border-left: 4px solid #4caf50; } </style> </head> <body> <div className="container"> <h1>Zion Tech Group - Sitemap</h1> <p>Complete navigation guide for our website. Find all our services, solutions, and resources.</p> <div className="sitemap-section"> <h2>Main Pages</h2> <div className="sitemap-links"> ${urls .filter(url => url.priority && url.priority >= 0.8) .map(url => ` <a href="${baseUrl}${url.url}" className="sitemap-link priority-high"> ${url.url === '/' ? 'Home' : url.url.split('/').pop()?.replace(/-/g, ' ').replace(/\b\w/g, l => l.toUpperCase()) || url.url} </a> `).join('')} </div> </div> <div className="sitemap-section"> <h2>Services</h2> <div className="sitemap-links"> ${urls .filter(url => url.url.startsWith('/services/')) .map(url => ` <a href="${baseUrl}${url.url}" className="sitemap-link priority-medium"> ${url.url.split('/').pop()?.replace(/-/g, ' ').replace(/\b\w/g, l => l.toUpperCase()) || url.url} </a> `).join('')} </div> </div> <div className="sitemap-section"> <h2>Solutions</h2> <div className="sitemap-links"> ${urls .filter(url => url.url.startsWith('/solutions/')) .map(url => ` <a href="${baseUrl}${url.url}" className="sitemap-link priority-medium"> ${url.url.split('/').pop()?.replace(/-/g, ' ').replace(/\b\w/g, l => l.toUpperCase()) || url.url} </a> `).join('')} </div> </div> <div className="sitemap-section"> <h2>Other Pages</h2> <div className="sitemap-links"> ${urls .filter(url => !url.url.startsWith('/services/') && !url.url.startsWith('/solutions/') && url.url !== '/' && url.priority && url.priority < 0.8) .map(url => ` <a href="${baseUrl}${url.url}" className="sitemap-link priority-low"> ${url.url.split('/').pop()?.replace(/-/g, ' ').replace(/\b\w/g, l => l.toUpperCase()) || url.url} </a> `).join('')} </div> </div> <div className="sitemap-section"> <p><strong>Total Pages:</strong> ${urls.length}</p> <p><strong>Last Updated:</strong> ${new Date().toLocaleDateString()}</p> </div> </div> </body> </html>`; return html; } }