// Stub offworld orbitdb utility - placeholder for missing functionality
export const createOrbitDB = async () => {
  // Placeholder implementation
  return { id: 'placeholder-orbitdb-id', success: true };
};

export const connectToOrbitDB = async (id: string) => {
  // Placeholder implementation
  return { connected: true, success: true };
};