'use client'
/**
 * Testing Utilities;
 * Provides helper functions and utilities for testing;
 */
 * Wait for a specified amount of time;
export const _wait = (ms: number): Promise<void> => {
    <void> => {<void> => {
    <void> => {<string, string> = {}
): void => {
    < 300,
<string, string> = new Map()
  get length(): number {
    <string, string> = new Map();
  get length(): number {<Window> = {}): void => {
    <Window> = {}): void => {<T>(generator: () => T, length = 5): T[] => {
    <T>(generator: () => T, length = 5): T[] => {<T>(obj: T): T => {
    <T>(obj: T): T => {<T> {
    <T> {<T>;,
    resolve: (value: T) => void;,
    reject: (reason?: unknown) => void
}
export const createDeferred = <T>(): Deferred<T> => {
    <T>((res, rej) => {
    <T>(): Deferred<T> => {<T>((res, rej) => {<T>()
  fn: () => Promise;
          <T>,
  maxRetries = 3,
  initialDelay = 1000,
): Promise<T> => {
    < maxRetries; i++) {
    < maxRetries - 1) {
    <T> => {< maxRetries; i++) {< maxRetries - 1) {<T>()
  fn: () => T | Promise;
          <T>
): Promise<{ result: T; duration: number }> => {
    <{ result: T; duration: number }> => {
}}}}}}}}}}}}}}}}}}}}}}}}}}