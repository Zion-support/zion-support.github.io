'use client'
<T extends (...args: any[]) => any>(),
    func: T,
  wait: number,
): ((...args: Parameters
          <T>) => void) => {
    <T>) => {
    <T>) => void) => {<T>) => {<T extends (...args: any[]) => any>(),
  limit: number,
    <T>) => void) => {<T>) => {<{
    <{
    <{<{<{
}}}}}}}}}}}}}}