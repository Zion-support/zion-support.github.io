if(process.env.NODE_ENV === 'development') { export function logWarningToProduction(message,context = {}) { if (process.env.NODE_ENV === 'development') { console.warn('[Production Logger]',message,context)} } export function logInfoToProduction(message,context = {}) { if (process.env.NODE_ENV === 'development') { console.info('[Production Logger]',message,context)} }