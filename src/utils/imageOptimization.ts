/** * Advanced Image Optimization System * Lazy loading, responsive images, and progressive enhancement */ export interface ImageConfig { src: string;, alt: string;, width?: number;, height?: number;, loading?: 'lazy' | 'eager';, priority?: boolean;, quality?: number;, formats?: ('webp' | 'avif' | 'jpg' | 'png')[]; } export interface ResponsiveImageConfig extends ImageConfig { srcSet?: string; sizes?: string; breakpoints?: number[]; } /** * Generate srcSet for responsive images */ export const generateSrcSet = ( baseSrc: string, breakpoints: number[] = [640, 750, 828, 1080, 1200, 1920, 2048, 3840]): string => { return breakpoints .map(width => `${getOptimizedUrl(baseSrc, { width })} ${width}w`)`; .join(', '); }; /** * Generate sizes attribute for responsive images */ export const generateSizes = ( config?: { mobile?: string; tablet?: string; desktop?: string } ): string => { const defaults = { mobile: '100vw', tablet: '50vw', desktop: '33vw' 