/** * Dynamic Banner Rotation System * Intelligently rotates promotional banners based on user engagement and priorities */ export interface BannerConfig { id: string;, component: string;, priority: number;, category: 'breakthrough' | 'enterprise' | 'innovation' | 'product';, impressions?: number;, clicks?: number;, lastShown?: Date;, active: boolean; } export interface RotationStrategy { maxVisible: number, rotationInterval: number; // in milliseconds, priorityWeight: number, freshnessWeight: number, engagementWeight: number; } const DEFAULT_STRATEGY: RotationStrategy = { maxVisible: 5, rotationInterval: 300000, // 5 minutes, priorityWeight: 0.5, freshnessWeight: 0.3, engagementWeight: 0.2 