/** * Cache Manager * Implements intelligent caching strategies for better performance */ export interface CacheOptions { ttl?: number; // Time to live in milliseconds strategy?: 'memory' | 'localStorage' | 'sessionStorage'; 