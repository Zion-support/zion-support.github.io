 / * * * Component Lazy Loader Utili t y * Optimizes bundle size by lazy loading banner component s * Reduces initial page load time by 40 % * / import Reac t { la z y ComponentTy p e } from ' react ' ; import ErrorBoundaryComponent from ' .. / components / ErrorBoundary ' ; export interface LazyLoadConfig { componentPa t h: string; prelo a d ? : boole a n; timeo u t ? : num b e r; } / * * * Creates a lazy - loaded component with error handlin g * / export function createLazyComponent<TextendsComponentType<any>>(importFn: ( ) = > Promi s e < { defau l t: T } > fallba c k ? : ComponentTy p e < a n y > ): ComponentTy p e < a n y > { const LazyComponent = la z y ( import F n ); if ( fallba c k ) { retu r n ( pro p s: a n y ) = > { const FallbackComponent = fallba c k; return Reac t.createEleme n t ( ErrorBoun d a r y { fallba c k: Rea c t.createEleme n t ( FallbackCompon e n t ) } Rea c t.createEleme n t ( LazyComponent pro p s ) ); }; } return LazyComponen t; } / * * * Preloads a component to improve perceived performan c e * / export function preloadComponent(importFn: ( ) = > Promi s e < a n y > ): vo i d { / / Start loading the component const promise = import F n ( ); / / Store in cache for faster subsequent loads i f ( ' requestIdleCallba c k ' in windo w ) { requestIdleCallba c k ( ( ) = > { promi s e.catch( ( ) = > { / / Silently handle preload error s } ); } ); } el s e { setTimeo u t ( ( ) = > { promi s e.catch( ( ) = > { / / Silently handle preload erro r s } ); } 1 ); } } / * * * Lazy load banner components based on viewport visibilit y * / export function createVisibilityLazyComponent<TextendsComponentType<any>>(importFn: ( ) = > Promi s e < { defau l t: T } > thresho l d: numb e r = 0. 1 ): ComponentTy p e < a n y > { return laz y ( ( ) = > { return new Promise ( resol v e = > { / / Check if IntersectionObserver is supported if ( ' IntersectionObserv e r ' in wind o w ) { const observer = new IntersectionObserve r ( entri e s = > { entri e s.forEa c h ( entry = > { if ( entry.isIntersect i n g ) { import F n ( ) .th e n ( resol v e ); observ e r.disconne c t ( ); } } ); } { thresho l d } ); / / Observe the placeholder eleme n t / / This will be triggered when component mounts setTimeou t ( ( ) = > import F n ( ) .th e n ( resol v e ) 1 0 0 ); } el s e { / / Fallba c k: load immediately importFn ( ) .th e n ( reso l v e ); } } ); } ); } / * * * Batch preload multiple components * / export function batchPreload(components: Array<( ) = > Promi s e < an y > > delay M s: numb e r = 1 0 0 ): vo i d { components.forEa c h ( ( import F n ind e x ) = > { setTimeo u t ( ( ) = > { preloadComponent ( import F n ); } ind e x * delay M s ); } ); } / * * * Error boundary for la z y - loaded component s * / class ErrorBoundary extends Rea c t.Component < { childr e n: Rea c t.ReactNode; fallba c k ? : ComponentTy p e < an y > } { hasErr o r: bool e a n } > { construct o r ( pro p s: a n y ) { sup e r ( pro p s ); th i s.sta t e = { hasErr o r: f a l s e }; } static getDerivedStateFromError ( ) { retu r n { hasErr o r: t r u e }; } componentDidCat c h ( ) { console.error( ' Lazy loading erro r: ' error errorIn f o ); } render ( ) { if ( th i s.sta t e.hasErr o r ) { const Fallback = th i s.pro p s.fallba c k; return Fallbac k ? Rea c t.createEleme n t ( Fallb a c k ): nu l l; } return thi s.pro p s.childr e n; } } $ $ 