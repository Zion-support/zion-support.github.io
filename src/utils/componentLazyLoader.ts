/**
 }

/**
 * Createsalazy-loadedcomponentwith errorhandling
 */
      );
    };
  }

  returnLazyComponent;
}

/**
       });
    });
  } else { 
    setTimeout(() => {
      promise.catch(() = > {
       });
    }1);
  }
}

/**
 * Lazyloadbanner componentsbasedon viewportvisibility
 */
      }
    });
  });
}

/**
  });
}

/**
  }
}
