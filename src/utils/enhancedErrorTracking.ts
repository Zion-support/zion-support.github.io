'use client'
/**
 * Enhanced Error Tracking Utility;
 * Provides comprehensive error tracking with detailed context;
 */
export interface ErrorContext {
    <string, unknown>
}
export interface TrackedError {
    <string, unknown>;
export interface TrackedError {<string, unknown>) => void
<string, unknown>) => void
      ).gtag('event', 'exception', {
    <string, number>
    recent: TrackedError[]
  } {
    <string, number> = {}
    this.errors.forEach(error => {
      byComponent[component] = (byComponent[component] || 0) + 1
  )
    return {
    <string, unknown>) => void;
      ).gtag('event', 'exception', {<string, number>;
    recent: TrackedError[];
  } {<string, number> = {};
    this.errors.forEach(error => {byComponent[component] = (byComponent[component] || 0) + 1;}
    });