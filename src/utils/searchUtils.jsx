import React from 'react'; "export const calculateRelevanceScore = (result, searchTerm) => {"export const debounceSearch = (func, delay) => {; export const filterSearchResults = (results, filters) => {; export const getSearchSuggestions = (searchHistory = [], export const highlightSearchTerms = (text, searchTerm) => {; export const matchesSearchTerm = (text, searchTerm) => {; export const performSearch = (data, export const sortSearchResults = (results, sortBy, searchTerm) => {; export default { /** * Highlight search terms in text with HTML mark tags*/ */ export const highlightSearchTerms = (text, searchTerm) => {; if (!searchTerm.trim()) return text; const escaped = searchTerm.replace(/[.*+?^${}()|[\]\\]/g, \\$&");" const regex = new RegExp(`(${escaped})`, gi");" return text.replace(regex, <mark class="bg-yellow-200 text-black px-1 rounded">$1</mark>);"};"`/** * Check if a text contains the search term (case-insensitive)*/ */ export const matchesSearchTerm = (text, searchTerm) => {; if (!text || !searchTerm.trim()) 