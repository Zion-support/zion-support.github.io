/** * Error Tracking and Monitoring Utility * * Comprehensive error tracking system for production monitoring * error reporting, and debugging assistance. * * Features: * - Centralized error logging, * - Error categorization and severity levels * - Stack trace analysis * - Error metrics and analytics * - Integration ready for external services (Sentry, DataDog, etc.) */ export enum ErrorSeverity { LOW = 'low' MEDIUM = 'medium' HIGH = 'high' CRITICAL = 'critical' } export enum ErrorCategory { NETWORK = 'network' RENDERING = 'rendering' STATE = 'state' THIRD_PARTY = 'third_party' USER_INPUT = 'user_input' PERMISSION = 'permission' UNKNOWN = 'unknown' 