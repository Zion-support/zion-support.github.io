/**
 * Security Configuration;
 * Defines security headers and policies for the application;
 */
export const _securityHeaders = {
    <>]/g, '') < and >
    .replace(/javascript:/gi, '') < length; i++) {
    < length; i++) {
}}}