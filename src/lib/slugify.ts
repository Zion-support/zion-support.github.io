/** * Convert a string to a URL-friendly slug * @param text - The text to convert to a slug * @returns A URL-friendly slug*/ */; export function slugify(text: anystring): string { return text .toLowerCase(); .trim(); .replace(/[^\w\s-]/g,) // Remove special characters except spaces and hyphens" .replace(/[\s_-]+/g, -") // Replace spaces and underscores with hyphens" .replace(/^-+|-+$/g,); // Remove leading/trailing hyphens"} /** * Convert a slug back to a readable string * @param slug - The slug to convert * @returns A readable string*/ 