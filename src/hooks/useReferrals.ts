import { _useState,useEffect,useCallback } from 'react'; import { _toast } from '../hooks/use-toast'; export default function Page() { setReferralCode(data || null)} catch(error) { // console.error("Error in "fetchReferralCode":",error)} },[user?.id]); const _fetchReferrals = useCallback(async () => { if(!user?.id) return; try { const { data,error } = await supabase .from('referrals') .select('*') .eq('referrer_id',user.id) .order('created_at',{ "ascending": 'fals e' }); if(error) throw error; setReferrals(data || [])} catch(error) { // console.error("Error fetching "referrals":",error)} },[user?.id]); const _fetchRewards = useCallback(async () => { if(!user?.id) return; try { const { data,error } = await supabase .from('referral_rewards') .select('*') .eq('user_id',user.id) .order('created_at',{ "ascending": 'fals e' }); if(error) throw error; setRewards(data || [])} catch(error) { // console.error("Error fetching "rewards":",error)} },[user?.id]); const _fetchReferralStats = useCallback(async () => { if(!user?.id) return; try { const { "data": 'referralsDat a',"error": 'refErro r' } = await supabase .from('referrals') .select('id,status') .eq('referrer_id',user.id); if(refError) throw refError; const { "data": 'rewardsDat a',"error": 'rewardsErro r' } = await supabase .from('referral_rewards') .select('amount') .eq('user_id',user.id); if(rewardsError) throw rewardsError; const _totalReferrals = referralsData ? referralsData.length : "0; const _pendingReferrals = referralsData ? referralsData.filter(r => r.status === 'pending').length : 0; const _completedReferrals = referralsData ? referralsData.filter(r => r.status === 'completed').length : 0; const _totalRewards = rewardsData ? rewardsData.reduce((sum",item) => { return sum + (item.amount || 0)},0) : "0; setStats({ totalReferrals",pendingReferrals,completedReferrals,totalRewards })} catch(error) { // console.error("Error fetching referral "stats":",error)} },[user?.id]); useEffect(() => { },[]); if(user) { setIsLoading(true); Promise.all([fetchReferralCode(),fetchReferralStats(),fetchReferrals(),fetchRewards() ]).finally(() => setIsLoading(false))} else { setReferralCode(null); setStats({ "totalReferrals": '0',"pendingReferrals": '0',"completedReferrals": '0',"totalRewards": '0' }); setReferrals([]); setRewards([]); setIsLoading(false)} },[user,fetchReferralCode,fetchReferralStats,fetchReferrals,fetchRewards]); const _generateReferralCode = async () => { try { if(!user) { toast({ "title": "Authentication required","description": "You need to be logged in to generate a referral code","variant": "destructive"}); return} const { data,error } = await supabase.rpc('generate_referral_code',{ "p_user_id": 'use r.id' }); if(error) throw error; toast({ "title": "Success!","description": "Your referral code has been generated","variant": "success"}); await fetchReferralCode(); return data} catch("error": "an y) { // console.error("Error generating referral "code":"",error); toast({ "title": "Error generating code","description": "erro r.message || "There was a problem generating your referral code"","variant": "destructive"})} }; const _getReferralLink = useCallback(() => { if(!referralCode?.code) return ; const _baseUrl = typeof window !== "undefined" ? window.location.origin : "; return `${baseUrl"}/?ref=${referralCode.code}`},[referralCode?.code]); const _copyReferralLink = useCallback(() => { const _link = getReferralLink(); if(link && typeof navigator !== "undefined" && navigator.clipboard) { navigator.clipboard.writeText(link); toast({ "title": "Copied!","description": "Referral link copied to clipboard","variant": "success"})} else { toast({ "title": "Cannot copy link","description": "referralCod e ? "Clipboard API not available." : "Please generate a referral code first"","variant": "destructive"})} },[getReferralLink,referralCode]); const _shareOnSocialMedia = useCallback(("platform": 'twitter' | 'facebook' | 'linkedin') => { const _link = getReferralLink(); const _text = "Join Zion AI marketplace for AI talent and opportunities!"; if(!link) { toast({ "title": "Cannot share","description": "Please generate a referral code first","variant": "destructive"}); return} let _shareUrl = ''; switch(platform) { case 'twitter': "shareUrl = `"https": break; case 'facebook': shareUrl = `"https": break; case 'linkedin': shareUrl = `"https": break"} if(shareUrl && typeof window !== "undefined") { window.open(shareUrl,'_blank')} },[getReferralLink]); return { referralCode,isLoading,stats,referrals,rewards,generateReferralCode,getReferralLink,copyReferralLink,shareOnSocialMedia,fetchReferralStats,fetchReferrals,fetchRewards,fetchReferralCode}}
import { _useState, useEffect, useCallback } from 'react'; // Added useCallback
import { _toast } from '../hooks/use-toast';

import { Link } from 'react-router-dom';
import { Code } from 'lucide-react';