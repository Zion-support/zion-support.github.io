import { useState, useEffect } from 'react' ; ;export function useDebounce<T></T>(value: T, delay: number): T { const [debouncedValue, setDebouncedValue] = useState<any></an></any>(value)  useEffect(() => { const handler = setTimeout(() => { setDebouncedValue(value)}, delay)  return () => { clearTimeout(handler)}}, [value, delay])  return debouncedValue};