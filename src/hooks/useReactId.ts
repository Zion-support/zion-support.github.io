"// Fallback ID generator using useRef, itself a valid custom hook. const useIdFallback = (): string => {; const idRef = React.useRef<string | null>(null); if (idRef.current === null) { // Generate a simple random string for fallback // This is not cryptographically secure, but fine for unique IDs in React. idRef.current = `fallback-id-${Math.random().toString(36).slice(2)}`;` } 