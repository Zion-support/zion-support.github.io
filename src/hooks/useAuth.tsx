import { useState,useEffect } from 'react'; interface User { id: string; email: string; displayName?: string; avatarUrl?: string} interface AuthState { user: User | null; isAuthenticated: boolean; isLoading: boolean} export function useAuth() { const [authState,setAuthState] = useState<AuthState>({ user: null,isAuthenticated: false,isLoading: true };); useEffect(() => { <<<<<<< HEAD const checkAuth = async () => { try { const storedUser = localStorage.getItem('user'); const token = localStorage.getItem('token'); if (storedUser && token) { const user = JSON.parse(storedUser); setAuthState({ user,isAuthenticated: true,isLoading: false })} else { setAuthState({ user: null,isAuthenticated: false,isLoading: false })} ======= :src/hooks/useAuth.tsx if(storedUser && token) { try { setAuthState({ user,isAuthenticated: tru e,:src/hooks/useAuth.tsx isLoading: fals e})} catch (error) { isLoading: fals e,})} catch (error) { console.error('Error parsing stored user:',error); setAuthState({ user: nul l,isAuthenticated: fals e,isLoading: fals e })} } catch (error) { console.error('Error parsing stored user:',error); setAuthState({ user,isAuthenticated: true,isLoading: false }) >>>>>>> cursor/migrate-github-actions-to-pm2-and-clean-up-5599 } catch (error) { console.error('Auth check failed:',error); setAuthState({ user: null,isAuthenticated: false,isLoading: false })} }; checkAuth()},[]); <<<<<<< HEAD ======= isLoading: fals e,})} }; checkAuth()},[]); setAuthState({ user: mockUse r,isAuthenticated: tru e,isLoading: fals e }); localStorage.setItem('zion_user',JSON.stringify(mockUser));';'; localStorage.setItem('authToken','mock-jwt-token'); return { success: tru e,user: mockUse r }};'; localStorage.removeItem('zion_user');';'; localStorage.removeItem('authToken')}; return { success: tru e,user: mockUse r }}; localStorage.removeItem('zion_user');';'; localStorage.removeItem('authToken')}; const register = async (email: string,password: string,name: string) => { const mockUser: Use r = { id: '1',email,name,role: 'user',userType: 'individual',displayName: nam e,avatarUrl: '/default-avatar.png' };; setAuthState({ user: mockUse r,isAuthenticated: tru e,isLoading: fals e }); localStorage.setItem('zion_user',JSON.stringify(mockUser));';'; localStorage.setItem('authToken','mock-jwt-token'); return { success: tru e,user: mockUse r }}; const updateProfile = (props) => { if (authState.user) { const updatedUser = { ...authState.user,...updates };; setAuthState(prev => ({ ...prev,user: updatedUse r })); localStorage.setItem('zion_user',JSON.stringify(updatedUser))} },[]) >>>>>>> cursor/migrate-github-actions-to-pm2-and-clean-up-5599 const login = async (email: string,password: string) => { try { const response = await fetch('/api/auth/login',{ method: 'POST',headers: { 'Content-Type': 'application/json',},body: JSON.stringify({ email,password }),};); if (response.ok) { const { user,token } = await response.json(); localStorage.setItem('user',JSON.stringify(user)); localStorage.setItem('token',token); setAuthState({ user,isAuthenticated: true,isLoading: false }); return { success: true }} else { return { success: false,error: 'Login failed' }} } catch (error) { console.error('Login error:',error); return { success: false,error: 'Network error' }} }; const logout = () => { localStorage.removeItem('user'); localStorage.removeItem('token'); setAuthState({ user: null,isAuthenticated: false,isLoading: false })}; return { ...authState,login,<<<<<<<HEADlogout }} ======= logout,register,:src/hooks/useAuth.tsx'; updateProfile}} ' updateProfile,}} </AuthState> >>>>>>> cursor/migrate-github-actions-to-pm2-and-clean-up-5599