"// Mock Sentry&apos;s captureException"jest.mock("@/utils/sentry", () => ({" captureException: jest.fn(), }));" const mockFetcher = jest.fn(); // Helper to wrap hook with SWRConfig and a clear cache const renderUseQuoteWizard = (category: string) => {; return renderHook(() => useQuoteWizard(category), {; 