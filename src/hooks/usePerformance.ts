import { } from 'react'; // Simple web vitals reporter const reportWebVitals = (metric: { name: string; value: number; delta: number }) => { if (process.env.NODE_ENV === 'development') {; console.log('Web Vital: ', metric);', 