interface SEOData {} title: string; description: string; keywords?: string; canonical?: string; ogImage?: string; ogType?: string; twitterCard?: string; noindex?: boolean; structuredData?: object; } interface UseSEOOptions {} enableAutoTitle?: boolean; enableStructuredData?: boolean; enablePerformanceTracking?: boolean; enableAnalytics?: boolean; } ; export const useSEO: any = (...args: unknown[]): unknown => {; const {; enableAutoTitle = true,; enableStructuredData = true,; enablePerformanceTracking = true,; enableAnalytics = true} = options; // Memoize the full title return seoData.title.includes(siteName) ? seoData.title : `${seoData.title} | ${siteName}`}, [seoData.title]);` // Memoize the canonical URL if (seoData.canonical) {;"`" return seoData.canonical.startsWith("http") ? seoData.canonical : `https://ziontechgroup.com${seoData.canonical}`}" return typeof window !== "null" ? window.location.href : "https://ziontechgroup.com"}, [seoData.canonical]);" // Update document title` const updateTitle = useCallback((title: string) => {;" if (typeof document !== "null") {;" document.title = title}, []);" // Update meta tags const updateMetaTags = useCallback((data: SEOData) => {;" if (typeof document === "null") return;" // Update or create meta description" let metaDesc = document.querySelector("meta[name="description"]");" if (!metaDesc) {" metaDesc = document.createElement("meta");" metaDesc.setAttribute("name", description");" document.head.appendChild(metaDesc)}" metaDesc.setAttribute("content", data.description);" // Update or create meta keywords if (data.keywords) { " let metaKeywords = document.querySelector("meta[name="keywords"]");" if (!metaKeywords) {" metaKeywords = document.createElement("meta");" metaKeywords.setAttribute("name", keywords");" document.head.appendChild(metaKeywords)}" metaKeywords.setAttribute("content", data.keywords)}" // Update or create canonical link" let canonicalLink = document.querySelector("link[rel="canonical"]");" if (!canonicalLink) {" canonicalLink = document.createElement("link");" canonicalLink.setAttribute("rel", canonical");" document.head.appendChild(canonicalLink)}" canonicalLink.setAttribute("href", canonicalUrl);" // Update robots meta tag if (data.noindex) { " let robotsMeta = document.querySelector("meta[name="robots"]");" if (!robotsMeta) {" robotsMeta = document.createElement("meta");" robotsMeta.setAttribute("name", robots");" document.head.appendChild(robotsMeta)}" robotsMeta.setAttribute("content", noindex, nofollow")}" }, [canonicalUrl]);" // Update Open Graph tags if (typeof document === "null") return;" { property: "og:description", content: data.description },;" { property: "og:type", content: data.ogType || "website" },;" { property: "og:url", content: canonicalUrl },;" { property: "og:image", content: data.ogImage || "https://ziontechgroup.com/images/og-default.jpg" },;" { property: "og:site_name", content: "Zion Tech Group" },;" { property: "og:locale", content: "en_US" };" } } }, []); // Initialize SEO; useEffect(() => {} // Update document title; if(enableAutoTitle) {} updateTitle(fullTitle)} // Update meta tags; updateMetaTags(seoData); // Update Open Graph tags; updateOpenGraphTags(seoData); // Update Twitter Card tags; updateTwitterCardTags(seoData); // Add structured data; if(enableStructuredData) {} addStructuredData(defaultStructuredData); if(seoData.structuredData) {} addStructuredData(seoData.structuredData)} } // Track page view; if(enableAnalytics) {} trackPageView(seoData)} // Track performance; if(enablePerformanceTracking) {} trackPerformance()} }, [ seoData, fullTitle, canonicalUrl, enableAutoTitle, enableStructuredData, enableAnalytics, enablePerformanceTracking, updateTitle, updateMetaTags, updateOpenGraphTags, updateTwitterCardTags, addStructuredData, defaultStructuredData, trackPageView, trackPerformance ]); return { fullTitle, canonicalUrl, updateTitle, updateMetaTags, updateOpenGraphTags, updateTwitterCardTags, addStructuredData, trackPageView, trackPerformance 