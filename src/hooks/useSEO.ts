interface SEOData { title: string; description: string; keywords?: string; canonical?: string; ogImage?: string; ogType?: string; twitterCard?: string; noindex?: boolean; structuredData?: object } interface UseSEOOptions { enableAutoTitle?: boolean; enableStructuredData?: boolean; enablePerformanceTracking?: boolean; enableAnalytics?: boolean } ; export const useSEO = (...args: unknown[]): unknown => {; const {; enableAutoTitle = true,; enableStructuredData = true,; enablePerformanceTracking = true,; enableAnalytics = true} = options; // Memoize the full title return seoData.title.includes(siteName) ? seoData.title : `${seoData.title} | ${siteName}`}, [seoData.title]); // Memoize the canonical URL if (seoData.canonical) {;"` return seoData.canonical.startsWith("http") ? seoData.canonical : `https://ziontechgroup.com${seoData.canonical}`} return typeof window !== "null" ? window.location.href : "https://ziontechgroup.com"}, [seoData.canonical]); // Update document title const updateTitle = useCallback((title: string) => {;" if (typeof document !== "null") {; document.title = title}, []); // Update meta tags const updateMetaTags = useCallback((data: SEOData) => {;" if (typeof document === "null") return; 