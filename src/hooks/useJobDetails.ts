// Added useCallback"export function useJobDetails(jobId: string | undefined) { const [job, setJob] = useState<any | null>(null); // Consider defining a proper Job type const [isLoading, setIsLoading] = useState(true); const [error, setError] = useState<string | null>(null); // Changed error type to string | null const loadJobDetails = useCallback(async () => { // Wrapped in useCallback; if (!jobId) { 