// Re-export the `DateRange` type from `react-day-picker` so that
// components using the shared `DateRange` interface stay compatible
// with the library's expectations.
export type { DateRange } from 'react-day-picker';
