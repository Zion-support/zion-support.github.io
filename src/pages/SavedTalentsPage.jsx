import React from 'react'; "export default function SavedTalentsPage () {" const { user } = useAuth () ; const [savedTalents, setSavedTalents] = useState ([]) ; const [isLoading, setIsLoading] = useState (true) ; const navigate = useNavigate () ; useEffect ( () => { const fetchSavedTalents = async () => {; setIsLoading (true) ; try { if (!user) { // // // // // // // console.warn ("User not authenticated.") ;" return;" console.warn ("User not authenticated.") ;" return;" } const { data, error } = await supabase .from ("saved_talents") .select (`" talent_profile (id, user_id," full_name, professional_title,` profile_picture_url, hourly_rate, bio, years_experience, key_projects, skills, location, availability, is_verified) `) .eq ("user_id", user.id) ;" if (error) {" throw error;` } if (data) { // Extract talent profiles and convert to TalentProfile type const talentProfiles = data.map (item => item.talent_profile) ; setSavedTalents (talentProfiles) ; } } catch (error) { // // // // // // // console.error ("Error fetching saved talents: ", error) ;" toast ({" title: "Error", description: "Failed to load saved talents. Please try again later."," variant: "destructive", }) ;" } finally {" setIsLoading (false) ; 