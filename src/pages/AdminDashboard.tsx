import React, { useState, useEffect } from 'react'; import { BarChart3, Users, Settings, FileText, TrendingUp, Activity, Shield, Database, Globe, Mail, Bell, Search, Filter, Download, Upload, Edit, Trash2, Eye, Plus, RefreshCw, AlertTriangle, CheckCircle, Clock, DollarSign, Star, MessageSquare, Calendar, UserPlus, UserMinus, Lock, Unlock, Archive, ArchiveRestore } from 'lucide-react'; import { useTheme } from '../context/ThemeContext'; import { useNotifications } from '../components/ui/NotificationSystem'; import { ModernLoader } from '../components/ui/ModernLoader'; interface DashboardStats { totalUsers: number; activeUsers: number; totalServices: number; activeServices: number; totalRevenue: number; monthlyRevenue: number; totalOrders: number; pendingOrders: number; systemHealth: number; uptime: number; } interface User { id: string; name: string; email: string; role: 'admin' | 'user' | 'moderator'; status: 'active' | 'inactive' | 'suspended'; lastLogin: string; joinDate: string; orders: number; totalSpent: number; } interface Service { id: string; name: string; category: string; status: 'active' | 'inactive' | 'draft'; price: number; orders: number; revenue: number; rating: number; lastUpdated: string; } interface Order { id: string; userId: string; userName: string; serviceName: string; amount: number; status: 'pending' | 'completed' | 'cancelled' | 'refunded'; date: string; paymentMethod: string; } export const AdminDashboard: React.FC = () => { const [activeTab, setActiveTab] = useState('overview'); const [stats, setStats] = useState<DashboardStats>({ totalUsers: 0, activeUsers: 0, totalServices: 0, activeServices: 0, totalRevenue: 0, monthlyRevenue: 0, totalOrders: 0, pendingOrders: 0, systemHealth: 0, uptime: 0 }); const [users, setUsers] = useState<User[]>([]); const [services, setServices] = useState<Service[]>([]); const [orders, setOrders] = useState<Order[]>([]); const [isLoading, setIsLoading] = useState(true); const [searchTerm, setSearchTerm] = useState(''); const [filterStatus, setFilterStatus] = useState('all'); const { theme, config } = useTheme(); const { addNotification } = useNotifications();