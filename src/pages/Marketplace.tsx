

        {availabilityOptions.map(opt => (
          <option key={opt} value={opt as string}>{opt}</option>
        ))}
      </select>
    </div>


            ))}
          </div>
        </motion.div>
      )}

      {/* End of Results */}

