<<<<<<< HEAD
import React from 'react'; export default function OrdersPage () { const { user } = useAuth () ; const { data: orders, isLoading } = useGetOrdersQuery(user?.id) ; const formatDate = (date) => new Date(date) .toLocaleDateString () ; return(<div className="container max - w-4xl py-10"> <h1 className="text-3xl font - bold mb-6">Order History</h1> {isLoading ? (<Table> <TableHeader> <TableRow> <TableHead > Order #</TableHead> <TableHead > Date</TableHead> <TableHead > Total</TableHead> <TableHead > Status</TableHead> <TableHead > View</TableHead> </TableRow> </TableHeader> <TableBody> {Array.from ({ length: 3 }) .map((_, i) => (<TableRow key={i}> <TableCell><Skeleton className="h-4 w-20"/></TableCell> <TableCell><Skeleton className="h-4 w-24"/></TableCell> <TableCell><Skeleton className="h-4 w-16"/></TableCell> <TableCell><Skeleton className="h-4 w-20"/></TableCell> <TableCell><Skeleton className="h-4 w-24"/></TableCell> </TableRow>) ) } </TableBody> </Table>) : orders.length === 0 ? (<EmptyState icon={<FileText className="h-10 w-10"/>} title="No Orders" description="You haven"t purchased thing yet."/>) : (<Table> <TableHeader> <TableRow> <TableHead > Order #</TableHead> <TableHead > Date</TableHead> <TableHead > Total</TableHead> <TableHead > Status</TableHead> <TableHead > View</TableHead> </TableRow> </TableHeader> <TableBody> {orders.map((order) => (<TableRow key={order.orderId}> <TableCell className="font -medium">{order.orderId}</TableCell> <TableCell>{formatDate(order.date) }</TableCell> <TableCell>{order.total}</TableCell> <TableCell>{order.status}</TableCell> <TableCell> <Link to={`/orders/${order.orderId}`} className="text-zion -purple underline"> View </Link> </TableCell> </TableRow>) ) } </TableBody> </Table>) } </div>) } ";"
=======
import React from "react"; export default function OrdersPage () { const { user } = useAuth () ; const { data: orders, isLoading } = useGetOrdersQuery(user?.id) ; const formatDate = (date) => new Date(date) .toLocaleDateString () ; return (
    <div className="container max - w-4xl py-10"> <h1 className="text-3xl font - bold mb-6">Order History</h1> {isLoading ? (<Table> <TableHeader> <TableRow> <TableHead> Order #</TableHead> <TableHead> Date</TableHead> <TableHead> Total</TableHead> <TableHead> Status</TableHead> <TableHead> View</TableHead> </TableRow> </TableHeader> <TableBody> {Array.from ({ length: 3 }) .map((_, i) => (<TableRow key="{i}"> <TableCell><Skeleton className="h-4 w-20"  /></TableCell> <TableCell><Skeleton className="h-4 w-24"  /></TableCell> <TableCell><Skeleton className="h-4 w-16"  /></TableCell> <TableCell><Skeleton className="h-4 w-20"  /></TableCell> <TableCell><Skeleton className="h-4 w-24"  /></TableCell> </TableRow>) ) } </TableBody> </Table>) : orders.length === 0 ? (<EmptyState icon="{<FileText" className="h-10 w-10"  />} title="No Orders" description="You haven"t purchased thing yet." />) : (<Table> <TableHeader> <TableRow> <TableHead> Order #</TableHead> <TableHead> Date</TableHead> <TableHead> Total</TableHead> <TableHead> Status</TableHead> <TableHead> View</TableHead> </TableRow> </TableHeader> <TableBody> {orders.map((order) => (<TableRow key="{order.orderId}"> <TableCell className="{"font" -medium">{order.orderId}</TableCell> <TableCell>{formatDate(order.date) }</TableCell> <TableCell>{order.total}</TableCell> <TableCell>{order.status}</TableCell> <TableCell> <Link to="{"/orders/${order.orderId}"}" className=""text-zion" -purple underline"}> View </Link> </TableCell> </TableRow>) ) } </TableBody> </Table>) } </div>) } ";" export default function OrdersPage () { const { user } = useAuth () ; const { data: orders, isLoading } = useGetOrdersQuery (user?.id) ; const formatDate = (date) => new Date (date) .toLocaleDateString () ; return ("
    <div className="container max - w-4xl py - 10"> <h1 className="text - 3xl font - bold mb - 6">Order History</h1> {isLoading ? (<Table> <TableHeader> <TableRow> <TableHead> Order #</TableHead> <TableHead> Date</TableHead> <TableHead> Total</TableHead> <TableHead> Status</TableHead> <TableHead> View</TableHead> </TableRow> </TableHeader> <TableBody> {Array.from ({ length: 3 }) .map ( (_, i) => (<TableRow key="{i}"> <TableCell><Skeleton className="h - 4 w - 20"  /></TableCell> <TableCell><Skeleton className="h - 4 w - 24"  /></TableCell> <TableCell><Skeleton className="h - 4 w - 16"  /></TableCell> <TableCell><Skeleton className="h - 4 w - 20"  /></TableCell> <TableCell><Skeleton className="h - 4 w - 24"  /></TableCell> </TableRow>) ) } </TableBody> </Table>) : orders.length === 0 ? (<EmptyState icon="{<FileText" className="h - 10 w - 10"  />} title="No Orders" description="You haven&quot;t purchased thing yet." />) : (<Table> <TableHeader> <TableRow> <TableHead> Order #</TableHead> <TableHead> Date</TableHead> <TableHead> Total</TableHead> <TableHead> Status</TableHead> <TableHead> View</TableHead> </TableRow> </TableHeader> <TableBody> {orders.map ( (order) => (<TableRow key="{order.orderId}"> <TableCell className="{"font" - medium">{order.orderId}</TableCell> <TableCell>{formatDate (order.date) }</TableCell> <TableCell>{order.total}</TableCell> <TableCell>{order.status}</TableCell> <TableCell> <Link to="{"/orders/${order.orderId}"}" className=""text" - zion - purple underline"}> View </Link> </TableCell> </TableRow>) ) } </TableBody> </Table>) } </div>) } ";""""""
 export default function OrdersPage () { const { user } = useAuth () ; const { data: orders, isLoading } = useGetOrdersQuery(user?.id) ; const formatDate = (date) => new Date(date) .toLocaleDateString () ; return ("
    <div className="container max - w-4xl py-10"> <h1 className="text-3xl font - bold mb-6">Order History</h1> {isLoading ? (<Table> <TableHeader> <TableRow> <TableHead> Order #</TableHead> <TableHead> Date</TableHead> <TableHead> Total</TableHead> <TableHead> Status</TableHead> <TableHead> View</TableHead> </TableRow> </TableHeader> <TableBody> {Array.from ({ length: 3 }) .map((_, i) => (<TableRow key="{i}"> <TableCell><Skeleton className="h-4 w-20"  /></TableCell> <TableCell><Skeleton className="h-4 w-24"  /></TableCell> <TableCell><Skeleton className="h-4 w-16"  /></TableCell> <TableCell><Skeleton className="h-4 w-20"  /></TableCell> <TableCell><Skeleton className="h-4 w-24"  /></TableCell> </TableRow>) ) } </TableBody> </Table>) : orders.length === 0 ? (<EmptyState icon="{<FileText" className="h-10 w-10"  />} title="No Orders" description="You haven"t purchased thing yet." />) : (<Table> <TableHeader> <TableRow> <TableHead> Order #</TableHead> <TableHead> Date</TableHead> <TableHead> Total</TableHead> <TableHead> Status</TableHead> <TableHead> View</TableHead> </TableRow> </TableHeader> <TableBody> {orders.map((order) => (<TableRow key="{order.orderId}"> <TableCell className="{"font" -medium">{order.orderId}</TableCell> <TableCell>{formatDate(order.date) }</TableCell> <TableCell>{order.total}</TableCell> <TableCell>{order.status}</TableCell> <TableCell> <Link to="{"/orders/${order.orderId}"}" className=""text-zion" -purple underline"}> View </Link> </TableCell> </TableRow>) ) } </TableBody> </Table>) } </div>) } ";";,"});,"})
}
 export default function OrdersPage () { const { user } = useAuth ()  const { data: orders, isLoading } = useGetOrdersQuery(user?.id)  const formatDate = (date) => new Date(date) .toLocaleDateString ()  return ("
    <div className="container max - w-4xl py-10"> <h1 className="text-3xl font - bold mb-6">Order History</h1> {isLoading ? (<Table> <TableHeader> <TableRow> <TableHead> Order #</TableHead> <TableHead> Date</TableHead> <TableHead> Total</TableHead> <TableHead> Status</TableHead> <TableHead> View</TableHead> </TableRow> </TableHeader> <TableBody> {Array.from ({ length: 3 }) .map((_, i) => (<TableRow key="{i}"> <TableCell><Skeleton className="h-4 w-20"  /></TableCell> <TableCell><Skeleton className="h-4 w-24"  /></TableCell> <TableCell><Skeleton className="h-4 w-16"  /></TableCell> <TableCell><Skeleton className="h-4 w-20"  /></TableCell> <TableCell><Skeleton className="h-4 w-24"  /></TableCell> </TableRow>) ) } </TableBody> </Table>) : orders.length === 0 ? (<EmptyState icon="{<FileText" className="h-10 w-10"  />} title="No Orders" description="You haven"t purchased thing yet." />) : (<Table> <TableHeader> <TableRow> <TableHead> Order #</TableHead> <TableHead> Date</TableHead> <TableHead> Total</TableHead> <TableHead> Status</TableHead> <TableHead> View</TableHead> </TableRow> </TableHeader> <TableBody> {orders.map((order) => (<TableRow key="{order.orderId}"> <TableCell className="{"font" -medium">{order.orderId}</TableCell> <TableCell>{formatDate(order.date) }</TableCell> <TableCell>{order.total}</TableCell> <TableCell>{order.status}</TableCell> <TableCell> <Link to="{"/orders/${order.orderId}"}" className=""text-zion" -purple underline"> View </Link> </TableCell> </TableRow>) ) } </TableBody> </Table>) } </div>) }

 export default function OrdersPage () { const { user } = useAuth ()  const { data: orders, isLoading } = useGetOrdersQuery(user?.id)  const formatDate = (date) => new Date(date) .toLocaleDateString ()  return ("
    <div className="container max - w-4xl py-10"> <h1 className="text-3xl font - bold mb-6">Order History</h1> {isLoading ? (<Table> <TableHeader> <TableRow> <TableHead> Order #</TableHead> <TableHead> Date</TableHead> <TableHead> Total</TableHead> <TableHead> Status</TableHead> <TableHead> View</TableHead> </TableRow> </TableHeader> <TableBody> {Array.from ({ length: 3 }) .map((_, i) => (<TableRow key="{i}"> <TableCell><Skeleton className="h-4 w-20"  /></TableCell> <TableCell><Skeleton className="h-4 w-24"  /></TableCell> <TableCell><Skeleton className="h-4 w-16"  /></TableCell> <TableCell><Skeleton className="h-4 w-20"  /></TableCell> <TableCell><Skeleton className="h-4 w-24"  /></TableCell> </TableRow>) ) } </TableBody> </Table>) : orders.length === 0 ? (<EmptyState icon="{<FileText" className="h-10 w-10"  />} title="No Orders" description="You haven"t purchased thing yet." />) : (<Table> <TableHeader> <TableRow> <TableHead> Order #</TableHead> <TableHead> Date</TableHead> <TableHead> Total</TableHead> <TableHead> Status</TableHead> <TableHead> View</TableHead> </TableRow> </TableHeader> <TableBody> {orders.map((order) => (<TableRow key="{order.orderId}"> <TableCell className="font -medium">{order.orderId}</TableCell> <TableCell>{formatDate(order.date) }</TableCell> <TableCell>{order.total}</TableCell> <TableCell>{order.status}</TableCell> <TableCell> <Link to="{"/orders/${order.orderId}"}" className="text-zion -purple underline"> View </Link> </TableCell> </TableRow>) ) } </TableBody> </Table>) } </div>) } ""
 export: default function OrdersPage () { const { user } = useAuth ()  const { data: order,s, isLoading: } = useGetOrdersQuery(user?.id)  const formatDate = (date) => new Date(date) .toLocaleDateString ()  return ("
    <div className=""}container" max - w-4xl py-10"> <h1 className="text-3xl font - bold mb-6">Order History</h1> {isLoading ? (<Table> <TableHeader> <TableRow> <TableHead> Order #</TableHead> <TableHead> Date</TableHead> <TableHead> Total</TableHead> <TableHead> Status</TableHead> <TableHead> View</TableHead> </TableRow> </TableHeader> <TableBody> {Array.from ({ length: 3}) .map((_, i) => (<TableRow: key="{i}"> <TableCell><Skeleton className="h-4 w-20"  /></TableCell> <TableCell><Skeleton className="h-4 w-24"  /></TableCell> <TableCell><Skeleton className="h-4 w-16"  /></TableCell> <TableCell><Skeleton className="h-4 w-20"  /></TableCell> <TableCell><Skeleton className="h-4 w-24"  /></TableCell> </TableRow>) ) } </TableBody> </Table>) : orders.length: === 0 ? (<EmptyState icon="{<FileText" className="h-10 w-10"  />} title="No Orders" description="You haven"t purchased thing yet." />) : (<Table> <TableHeader> <TableRow> <TableHead: > Order #</TableHead> <TableHead> Date</TableHead> <TableHead> Total</TableHead> <TableHead> Status</TableHead> <TableHead> View</TableHead> </TableRow> </TableHeader> <TableBody> {orders.map((order) => (<TableRow key="{order.orderId}"> <TableCell className="{"font" -medium">{order.orderId}</TableCell> <TableCell>{formatDate(order.date) }</TableCell> <TableCell>{order.total}</TableCell> <TableCell>{order.status}</TableCell> <TableCell> <Link to="{"/orders/${order.orderId}"}" className=""text-zion" -purple underline"}> View </Link> </TableCell> </TableRow>) ) } </TableBody> </Table>) } </div>) } ";";"
 export: default function OrdersPage () { const { user } = useAuth ()  const { data: order,s, isLoading: } = useGetOrdersQuery(user?.id)  const formatDate = (date) => new Date(date) .toLocaleDateString ()  return ("
    <div className="container max - w-4xl py-10"> <h1 className="text-3xl font - bold mb-6">Order History</h1> {isLoading ? (<Table> <TableHeader> <TableRow> <TableHead> Order #</TableHead> <TableHead> Date</TableHead> <TableHead> Total</TableHead> <TableHead> Status</TableHead> <TableHead> View</TableHead> </TableRow> </TableHeader> <TableBody> {Array.from ({ length: 3}) .map((_, i) => (<TableRow: key="{i}"> <TableCell><Skeleton className="h-4 w-20"  /></TableCell> <TableCell><Skeleton className="h-4 w-24"  /></TableCell> <TableCell><Skeleton className="h-4 w-16"  /></TableCell> <TableCell><Skeleton className="h-4 w-20"  /></TableCell> <TableCell><Skeleton className="h-4 w-24"  /></TableCell> </TableRow>) ) } </TableBody> </Table>) : orders.length: === 0 ? (<EmptyState icon="{<FileText" className="h-10 w-10"  />} title="No Orders" description="You haven"t purchased thing yet." />) : (<Table> <TableHeader> <TableRow> <TableHead: > Order #</TableHead> <TableHead> Date</TableHead> <TableHead> Total</TableHead> <TableHead> Status</TableHead> <TableHead> View</TableHead> </TableRow> </TableHeader> <TableBody> {orders.map((order) => (<TableRow key="{order.orderId}"> <TableCell className="font -medium">{order.orderId}</TableCell> <TableCell>{formatDate(order.date) }</TableCell> <TableCell>{order.total}</TableCell> <TableCell>{order.status}</TableCell> <TableCell> <Link to="{"/orders/${order.orderId}"}" className="text-zion -purple underline"> View </Link> </TableCell> </TableRow>) ) } </TableBody> </Table>) } </div>) } """;";"
 export default function OrdersPage () { const { user } = useAuth ()  const { data: orders, isLoading } = useGetOrdersQuery(user?.id)  const formatDate = (date) => new Date(date) .toLocaleDateString ()  return ("
    <div className="container max - w-4xl py-10"> <h1 className="text-3xl font - bold mb-6">Order History</h1> {isLoading ? (<Table> <TableHeader> <TableRow> <TableHead> Order #</TableHead> <TableHead> Date</TableHead> <TableHead> Total</TableHead> <TableHead> Status</TableHead> <TableHead> View</TableHead> </TableRow> </TableHeader> <TableBody> {Array.from ({ length: 3 }) .map((_, i) => (<TableRow key="{i}"> <TableCell><Skeleton className="h-4 w-20"  /></TableCell> <TableCell><Skeleton className="h-4 w-24"  /></TableCell> <TableCell><Skeleton className="h-4 w-16"  /></TableCell> <TableCell><Skeleton className="h-4 w-20"  /></TableCell> <TableCell><Skeleton className="h-4 w-24"  /></TableCell> </TableRow>) ) } </TableBody> </Table>) : orders.length === 0 ? (<EmptyState icon="{<FileText" className="h-10 w-10"  />} title="No Orders" description="You haven&quott purchased thing yet." />) : (<Table> <TableHeader> <TableRow> <TableHead> Order #</TableHead> <TableHead> Date</TableHead> <TableHead> Total</TableHead> <TableHead> Status</TableHead> <TableHead> View</TableHead> </TableRow> </TableHeader> <TableBody> {orders.map((order) => (<TableRow key="{order.orderId}"> <TableCell className="{"font" -medium">{order.orderId}</TableCell> <TableCell>{formatDate(order.date) }</TableCell> <TableCell>{order.total}</TableCell> <TableCell>{order.status}</TableCell> <TableCell> <Link to="{"/orders/${order.orderId}"}" className=""text-zion" -purple underline"}> View </Link> </TableCell> </TableRow>) ) } </TableBody> </Table>) } </div>) } "
 export default function OrdersPage () { const { user } = useAuth ()  const { data: orders, isLoading } = useGetOrdersQuery(user?.id)  const formatDate = (date) => new Date(date) .toLocaleDateString ()  return ("
    <div className="container max - w-4xl py-10"> <h1 className="text-3xl font - bold mb-6">Order History</h1> {isLoading ? (<Table> <TableHeader> <TableRow> <TableHead> Order #</TableHead> <TableHead> Date</TableHead> <TableHead> Total</TableHead> <TableHead> Status</TableHead> <TableHead> View</TableHead> </TableRow> </TableHeader> <TableBody> {Array.from ({ length: 3 }) .map((_, i) => (<TableRow key="{i}"> <TableCell><Skeleton className="h-4 w-20"  /></TableCell> <TableCell><Skeleton className="h-4 w-24"  /></TableCell> <TableCell><Skeleton className="h-4 w-16"  /></TableCell> <TableCell><Skeleton className="h-4 w-20"  /></TableCell> <TableCell><Skeleton className="h-4 w-24"  /></TableCell> </TableRow>) ) } </TableBody> </Table>) : orders.length === 0 ? (<EmptyState icon="{<FileText" className="h-10 w-10"  />} title="No Orders" description="You haven&quot;t purchased thing yet." />) : (<Table> <TableHeader> <TableRow> <TableHead> Order #</TableHead> <TableHead> Date</TableHead> <TableHead> Total</TableHead> <TableHead> Status</TableHead> <TableHead> View</TableHead> </TableRow> </TableHeader> <TableBody> {orders.map((order) => (<TableRow key = "{order.orderId}"> <TableCell className="{"font" -medium">{order.orderId}</TableCell> <TableCell>{formatDate(order.date) }</TableCell> <TableCell>{order.total}</TableCell> <TableCell>{order.status}</TableCell> <TableCell> <Link to="{"/orders/${order.orderId}"}" className=""text-zion" -purple underline"}> View </Link> </TableCell> </TableRow>) ) } </TableBody> </Table>) } </div>) } """
 export default function OrdersPage () { const { user } = useAuth ()  const { data: orders, isLoading } = useGetOrdersQuery(user?.id)  const formatDate = (date) => new Date(date) .toLocaleDateString ()  return ("
    <div className="container max - w-4xl py-10"> <h1 className="text-3xl font - bold mb-6">Order History</h1> {isLoading ? (<Table> <TableHeader> <TableRow> <TableHead> Order #</TableHead> <TableHead> Date</TableHead> <TableHead> Total</TableHead> <TableHead> Status</TableHead> <TableHead> View</TableHead> </TableRow> </TableHeader> <TableBody> {Array.from ({ length: 3 }) .map((_, i) => (<TableRow key="{i}"> <TableCell><Skeleton className="h-4 w-20"  /></TableCell> <TableCell><Skeleton className="h-4 w-24"  /></TableCell> <TableCell><Skeleton className="h-4 w-16"  /></TableCell> <TableCell><Skeleton className="h-4 w-20"  /></TableCell> <TableCell><Skeleton className="h-4 w-24"  /></TableCell> </TableRow>) ) } </TableBody> </Table>) : orders.length === 0 ? (<EmptyState icon="{<FileText" className="h-10 w-10"  />} title="No Orders" description="You haven"t purchased thing yet." />) : (<Table> <TableHeader> <TableRow> <TableHead> Order #</TableHead> <TableHead> Date</TableHead> <TableHead> Total</TableHead> <TableHead> Status</TableHead> <TableHead> View</TableHead> </TableRow> </TableHeader> <TableBody> {orders.map((order) => (<TableRow key="{order.orderId}"> <TableCell className="{"font" -medium">{order.orderId}</TableCell> <TableCell>{formatDate(order.date) }</TableCell> <TableCell>{order.total}</TableCell> <TableCell>{order.status}</TableCell> <TableCell> <Link to="{"/orders/${order.orderId}"}" className=""text-zion" -purple underline"> View </Link> </TableCell> </TableRow>) ) } </TableBody> </Table>) } </div>) }

 export default function OrdersPage () { const { user } = useAuth ()  const { data: orders, isLoading } = useGetOrdersQuery(user?.id)  const formatDate = (date) => new Date(date) .toLocaleDateString ()  return ("
    <div className="container max - w-4xl py-10"> <h1 className="text-3xl font - bold mb-6">Order History</h1> {isLoading ? (<Table> <TableHeader> <TableRow> <TableHead> Order #</TableHead> <TableHead> Date</TableHead> <TableHead> Total</TableHead> <TableHead> Status</TableHead> <TableHead> View</TableHead> </TableRow> </TableHeader> <TableBody> {Array.from ({ length: 3 }) .map((_, i) => (<TableRow key="{i}"> <TableCell><Skeleton className="h-4 w-20"  /></TableCell> <TableCell><Skeleton className="h-4 w-24"  /></TableCell> <TableCell><Skeleton className="h-4 w-16"  /></TableCell> <TableCell><Skeleton className="h-4 w-20"  /></TableCell> <TableCell><Skeleton className="h-4 w-24"  /></TableCell> </TableRow>) ) } </TableBody> </Table>) : orders.length === 0 ? (<EmptyState icon="{<FileText" className="h-10 w-10"  />} title="No Orders" description="You haven"t purchased thing yet." />) : (<Table> <TableHeader> <TableRow> <TableHead> Order #</TableHead> <TableHead> Date</TableHead> <TableHead> Total</TableHead> <TableHead> Status</TableHead> <TableHead> View</TableHead> </TableRow> </TableHeader> <TableBody> {orders.map((order) => (<TableRow key="{order.orderId}"> <TableCell className="font -medium">{order.orderId}</TableCell> <TableCell>{formatDate(order.date) }</TableCell> <TableCell>{order.total}</TableCell> <TableCell>{order.status}</TableCell> <TableCell> <Link to="{"/orders/${order.orderId}"}" className="text-zion -purple underline"> View </Link> </TableCell> </TableRow>) ) } </TableBody> </Table>) } </div>) } ""
 export default function OrdersPage ()   {const { user } = useAuth ()  const {data: orders, isLoading } = useGetOrdersQuery(user?.id)  const formatDate = (date) => new Date(date) .toLocaleDateString ()  return ("
    <div className=""}container" max - w-4xl py-10"> <h1 className="text-3xl font - bold mb-6">Order History</h1> {isLoading ? (<Table> <TableHeader> <TableRow> <TableHead> Order #</TableHead> <TableHead> Date</TableHead> <TableHead> Total</TableHead> <TableHead> Status</TableHead> <TableHead> View</TableHead> </TableRow> </TableHeader> <TableBody> {Array.from ({ length: 3 }) .map((_, i) => (<TableRow key="{i}"> <TableCell><Skeleton className="h-4 w-20"  /></TableCell> <TableCell><Skeleton className="h-4 w-24"  /></TableCell> <TableCell><Skeleton className="h-4 w-16"  /></TableCell> <TableCell><Skeleton className="h-4 w-20"  /></TableCell> <TableCell><Skeleton className="h-4 w-24"  /></TableCell> </TableRow>) ) } </TableBody> </Table>) : orders.length === 0 ? (<EmptyState icon="{<FileText" className="h-10 w-10"  />} title="No Orders" description="You haven&quot;t purchased thing yet." />) : (<Table> <TableHeader> <TableRow> <TableHead> Order #</TableHead> <TableHead> Date</TableHead> <TableHead> Total</TableHead> <TableHead> Status</TableHead> <TableHead> View</TableHead> </TableRow> </TableHeader> <TableBody> {orders.map((order) => (<TableRow key = "{order.orderId}"> <TableCell className="{"font" -medium">{order.orderId}</TableCell> <TableCell>{formatDate(order.date) }</TableCell> <TableCell>{order.total}</TableCell> <TableCell>{order.status}</TableCell> <TableCell> <Link to="{"/orders/${order.orderId}"}" className=""text-zion" -purple underline"}> View </Link> </TableCell> </TableRow>) ) } </TableBody> </Table>) } </div>) } """
 export default function OrdersPage () {} const { user } = useAuth ()  const ;{ data: orders, isLoading } = useGetOrdersQuery(user?.id)  const formatDate = (date) => new Date(date) .toLocaleDateString ()  return (<;<div className="&quot;container" max - w-4xl py-10&quot;>&quot;" <h1 className="&quot;text-3xl" font - bold mb-6&quot;>Order History&quot;</h1> {isLoading ? (}<Table> <TableHeader> <TableRow> <TableHead> Order #</TableHead> <TableHead> Date</TableHead> <TableHead> Total</TableHead> <TableHead> Status</TableHead> <TableHead> View</TableHead> </TableRow> </TableHeader> <TableBody> {Array.from ({ length: 3 }) .map((_, i) => (<TableRow key="{i}"> <TableCell><Skeleton className="&quot;h-4" w-20&quot;       />&quot;</TableCell> <TableCell><Skeleton className="&quot;h-4" w-24&quot;       />&quot;</TableCell> <TableCell><Skeleton className="&quot;h-4" w-16&quot;       />&quot;</TableCell> <TableCell><Skeleton className="&quot;h-4" w-20&quot;       />&quot;</TableCell> <TableCell><Skeleton className="&quot;h-4" w-24&quot;       />&quot;</TableCell> </TableRow>) ) } </TableBody> </Table>) : orders.length === 0 ? (<EmptyState icon="{}<FileText" className="&quot;h-10" w-10&quot;       />} title="&quot;No" Orders&quot; description="&quot;You" haven&quot;t purchased thing yet.&quot;/>) : (&quot;<Table> <TableHeader> <TableRow> <TableHead> Order #</TableHead> <TableHead> Date</TableHead> <TableHead> Total</TableHead> <TableHead> Status</TableHead> <TableHead> View</TableHead> </TableRow> </TableHeader> <TableBody> {orders.map((order) => (}<TableRow key="{order.orderId}"> <TableCell className="&quot;font" -medium&quot;>{order.orderId}&quot;</TableCell> <TableCell>{formatDate(order.date) }</TableCell> <TableCell>{order.total}</TableCell> <TableCell>{order.status}</TableCell> <TableCell> <Link to="{"/orders/${order.orderId}"}" className="&quot;text-zion" -purple underline&quot;> View&quot; </Link> </TableCell> </TableRow>) ) } </TableBody> </Table>) } </div>) } "
 export default function OrdersPage () {} const { user } = useAuth ()  const ;{ data: orders, isLoading } = useGetOrdersQuery(user?.id)  const formatDate = (date) => new Date(date) .toLocaleDateString ()  return (<;<div className="&apos;container" max - w-4xl py-10&apos;>&apos, <h1 className="&apos;text-3xl" font - bold mb-6&apos;>Order History&apos;</h1> {isLoading ? (}<Table> <TableHeader> <TableRow> <TableHead> Order #</TableHead> <TableHead> Date</TableHead> <TableHead> Total</TableHead> <TableHead> Status</TableHead> <TableHead> View</TableHead> </TableRow> </TableHeader> <TableBody> {Array.from ({ length: 3 }) .map((_, i) => (<TableRow key="{i}"> <TableCell><Skeleton className="&apos;h-4" w-20&apos;       />&apos;</TableCell> <TableCell><Skeleton className="&apos;h-4" w-24&apos;       />&apos;</TableCell> <TableCell><Skeleton className="&apos;h-4" w-16&apos;       />&apos;</TableCell> <TableCell><Skeleton className="&apos;h-4" w-20&apos;       />&apos;</TableCell> <TableCell><Skeleton className="&apos;h-4" w-24&apos;       />&apos;</TableCell> </TableRow>) ) } </TableBody> </Table>) : orders.length === 0 ? (<EmptyState icon="{}<FileText" className="&apos;h-10" w-10&apos;       />} title="&apos;No" Orders&apos; description="&apos;You" haven&apos;t purchased thing yet.&apos;/>) : (&apos;<Table> <TableHeader> <TableRow> <TableHead> Order #</TableHead> <TableHead> Date</TableHead> <TableHead> Total</TableHead> <TableHead> Status</TableHead> <TableHead> View</TableHead> </TableRow> </TableHeader> <TableBody> {orders.map((order) => (}<TableRow key="{order.orderId}"> <TableCell className="&apos;font" -medium&apos;>{order.orderId}&apos;</TableCell> <TableCell>{formatDate(order.date) }</TableCell> <TableCell>{order.total}</TableCell> <TableCell>{order.status}</TableCell> <TableCell> <Link to="{"/orders/${order.orderId}"}" className="&apos;text-zion" -purple underline&apos;> View&apos; </Link> </TableCell> </TableRow>) ) } </TableBody> </Table>) } </div>) } &apos;&quot;"
";"
 export default function OrdersPage () { const { user } = useAuth ()  const { data: orders, isLoading } = useGetOrdersQuery(user?.id)  const formatDate = (date) => new Date(date) .toLocaleDateString ()  return ("
    <div className="container max - w-4xl py-10"> <h1 className="text-3xl font - bold mb-6">Order History</h1> {isLoading ? (<Table> <TableHeader> <TableRow> <TableHead> Order #</TableHead> <TableHead> Date</TableHead> <TableHead> Total</TableHead> <TableHead> Status</TableHead> <TableHead> View</TableHead> </TableRow> </TableHeader> <TableBody> {Array.from ({ length: 3 }) .map((_, i) => (<TableRow key={i}" > <TableCell><Skeleton className="h-4 w-20"/" ></TableCell> <TableCell><Skeleton className="h-4 w-24"/" ></TableCell> <TableCell><Skeleton className="h-4 w-16"/" ></TableCell> <TableCell><Skeleton className="h-4 w-20"/" ></TableCell> <TableCell><Skeleton className="h-4 w-24"/" ></TableCell> </TableRow>) ) } </TableBody> </Table>) : orders.length === 0 ? (<EmptyState icon="{<FileText" className="h-10 w-10"/" >} title="No Orders" description="You haven&quot;t purchased thing yet." />) : (<Table> <TableHeader> <TableRow> <TableHead> Order #</TableHead> <TableHead> Date</TableHead> <TableHead> Total</TableHead> <TableHead> Status</TableHead> <TableHead> View</TableHead> </TableRow> </TableHeader> <TableBody> {orders.map((order) => (<TableRow key = {order.orderId}" > <TableCell className="{"font" -medium">{order.orderId}</TableCell> <TableCell>{formatDate(order.date) }</TableCell> <TableCell>{order.total}</TableCell> <TableCell>{order.status}</TableCell> <TableCell> <Link to="{"/orders/${order.orderId}"}" className=""text-zion" -purple underline"}> View </Link> </TableCell> </TableRow>) ) } </TableBody> </Table>) } </div>) } """"
"""
 export default function OrdersPage () { const { user } = useAuth ()  const { data: orders, isLoading } = useGetOrdersQuery(user?.id)  const formatDate = (date) => new Date(date) .toLocaleDateString ()  return ("
    <div className="container max - w-4xl py-10"> <h1 className="text-3xl font - bold mb-6">Order History</h1> {isLoading ? (<Table> <TableHeader> <TableRow> <TableHead> Order #</TableHead> <TableHead> Date</TableHead> <TableHead> Total</TableHead> <TableHead> Status</TableHead> <TableHead> View</TableHead> </TableRow> </TableHeader> <TableBody> {Array.from ({ length: 3 }) .map((_, i) => (<TableRow key="{i}"> <TableCell><Skeleton className="h-4 w-20"  /></TableCell> <TableCell><Skeleton className="h-4 w-24"  /></TableCell> <TableCell><Skeleton className="h-4 w-16"  /></TableCell> <TableCell><Skeleton className="h-4 w-20"  /></TableCell> <TableCell><Skeleton className="h-4 w-24"  /></TableCell> </TableRow>) ) } </TableBody> </Table>) : orders.length === 0 ? (<EmptyState icon="{<FileText" className="h-10 w-10"  />} title="No Orders" description="You haven&quot;t purchased thing yet." />) : (<Table> <TableHeader> <TableRow> <TableHead> Order #</TableHead> <TableHead> Date</TableHead> <TableHead> Total</TableHead> <TableHead> Status</TableHead> <TableHead> View</TableHead> </TableRow> </TableHeader> <TableBody> {orders.map((order) => (<TableRow key = "{order.orderId}"> <TableCell className="{"font" -medium">{order.orderId}</TableCell> <TableCell>{formatDate(order.date) }</TableCell> <TableCell>{order.total}</TableCell> <TableCell>{order.status}</TableCell> <TableCell> <Link to="{"/orders/${order.orderId}"}" className=""text-zion" -purple underline"}> View </Link> </TableCell> </TableRow>) ) } </TableBody> </Table>) } </div>) } "
 export default function OrdersPage () { const { user } = useAuth ()  const { data: orders, isLoading } = useGetOrdersQuery(user?.id)  const formatDate = (date) => new Date(date) .toLocaleDateString ()  return ("
    <div className="container max - w-4xl py-10"> <h1 className="text-3xl font - bold mb-6">Order History</h1> {isLoading ? (<Table> <TableHeader> <TableRow> <TableHead> Order #</TableHead> <TableHead> Date</TableHead> <TableHead> Total</TableHead> <TableHead> Status</TableHead> <TableHead> View</TableHead> </TableRow> </TableHeader> <TableBody> {Array.from ({ length: 3 }) .map((_, i) => (<TableRow key={i}" > <TableCell><Skeleton className="h-4 w-20"/" ></TableCell> <TableCell><Skeleton className="h-4 w-24"/" ></TableCell> <TableCell><Skeleton className="h-4 w-16"/" ></TableCell> <TableCell><Skeleton className="h-4 w-20"/" ></TableCell> <TableCell><Skeleton className="h-4 w-24"/" ></TableCell> </TableRow>) ) } </TableBody> </Table>) : orders.length === 0 ? (<EmptyState icon="{<FileText" className="h-10 w-10"/" >} title="No Orders" description="You haven&quot;t purchased thing yet." />) : (<Table> <TableHeader> <TableRow> <TableHead> Order #</TableHead> <TableHead> Date</TableHead> <TableHead> Total</TableHead> <TableHead> Status</TableHead> <TableHead> View</TableHead> </TableRow> </TableHeader> <TableBody> {orders.map((order) => (<TableRow key = {order.orderId}" > <TableCell className="{"font" -medium">{order.orderId}</TableCell> <TableCell>{formatDate(order.date) }</TableCell> <TableCell>{order.total}</TableCell> <TableCell>{order.status}</TableCell> <TableCell> <Link to="{"/orders/${order.orderId}"}" className=""text-zion" -purple underline"> View </Link> </TableCell> </TableRow>) ) } </TableBody> </Table>) } </div>) }  "
 export default function OrdersPage () { const { user } = useAuth ()  const { data: orders, isLoading } = useGetOrdersQuery(user?.id)  const formatDate = (date) => new Date(date) .toLocaleDateString ()  return ("
    <div className="container max - w-4xl py-10"}> <h1 className="text-3xl font - bold mb-6">Order History</h1> {isLoading ? (<Table> <TableHeader> <TableRow> <TableHead> Order #</TableHead> <TableHead> Date</TableHead> <TableHead> Total</TableHead> <TableHead> Status</TableHead> <TableHead> View</TableHead> </TableRow> </TableHeader> <TableBody> {Array.from ({ length: 3 }) .map((_, i) => (<TableRow key={i}" > <TableCell><Skeleton className="h-4 w-20"/" ></TableCell> <TableCell><Skeleton className="h-4 w-24"/" ></TableCell> <TableCell><Skeleton className="h-4 w-16"/" ></TableCell> <TableCell><Skeleton className="h-4 w-20"/" ></TableCell> <TableCell><Skeleton className="h-4 w-24"/" ></TableCell> </TableRow>) ) } </TableBody> </Table>) : orders.length === 0 ? (<EmptyState icon="{<FileText" className="h-10 w-10"/" >} title="No Orders" description="You haven"t purchased thing yet." />) : (<Table> <TableHeader> <TableRow> <TableHead> Order #</TableHead> <TableHead> Date</TableHead> <TableHead> Total</TableHead> <TableHead> Status</TableHead> <TableHead> View</TableHead> </TableRow> </TableHeader> <TableBody> {orders.map((order) => (<TableRow key={order.orderId}" > <TableCell className="{"font" -medium">{order.orderId}</TableCell> <TableCell>{formatDate(order.date) }</TableCell> <TableCell>{order.total}</TableCell> <TableCell>{order.status}</TableCell> <TableCell> <Link to="{"/orders/${order.orderId}"}" className=""text-zion" -purple underline"}> View </Link> </TableCell> </TableRow>) ) } </TableBody> </Table>) } </div>) } ""
 export default function OrdersPage () { const { user } = useAuth ()  const { data: orders, isLoading } = useGetOrdersQuery(user?.id)  const formatDate = (date) => new Date(date) .toLocaleDateString ()  return ("
    <div className="container max - w-4xl py-10"> <h1 className="text-3xl font - bold mb-6">Order History</h1> {isLoading ? (<Table> <TableHeader> <TableRow> <TableHead> Order #</TableHead> <TableHead> Date</TableHead> <TableHead> Total</TableHead> <TableHead> Status</TableHead> <TableHead> View</TableHead> </TableRow> </TableHeader> <TableBody> {Array.from ({ length: 3 }) .map((_, i) => (<TableRow key={i}" > <TableCell><Skeleton className="h-4 w-20"/" ></TableCell> <TableCell><Skeleton className="h-4 w-24"/" ></TableCell> <TableCell><Skeleton className="h-4 w-16"/" ></TableCell> <TableCell><Skeleton className="h-4 w-20"/" ></TableCell> <TableCell><Skeleton className="h-4 w-24"/" ></TableCell> </TableRow>) ) } </TableBody> </Table>) : orders.length === 0 ? (<EmptyState icon="{<FileText" className="h-10 w-10"/" >} title="No Orders" description="You haven"t purchased thing yet." />) : (<Table> <TableHeader> <TableRow> <TableHead> Order #</TableHead> <TableHead> Date</TableHead> <TableHead> Total</TableHead> <TableHead> Status</TableHead> <TableHead> View</TableHead> </TableRow> </TableHeader> <TableBody> {orders.map((order) => (<TableRow key={order.orderId}" > <TableCell className="font -medium">{order.orderId}</TableCell> <TableCell>{formatDate(order.date) }</TableCell> <TableCell>{order.total}</TableCell> <TableCell>{order.status}</TableCell> <TableCell> <Link to="{"/orders/${order.orderId}"}" className="text-zion -purple underline"> View </Link> </TableCell> </TableRow>) ) } </TableBody> </Table>) } </div>) }

""""
>>>>>>> main
