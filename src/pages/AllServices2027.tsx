import React from 'react'; "export default AllServices2027;"import {; " Brain, Shield, Zap, Code, Signal, Users, Truck, ArrowRight, Phone, Mail, MapPin, CheckCircle, TrendingUp, Globe, Cpu, Database, Network, Lock, Cloud, Rocket } from "lucide - react";"const AllServices2027: React.FC = () => {; const [selectedCategory, setSelectedCategory] = useState < string> ("All") ;" const categories = [; { id: "All", name: "All Services", icon: Globe, color: "from - cyan - 500 to - blue - 600" },;" { id: "AI Services", name: "AI Services", icon: Brain, color: "from - purple - 500 to - pink - 600" },;" { id: "Cybersecurity", name: "Cybersecurity", icon: Shield, color: "from - red - 500 to - orange - 600" },;" { id: "Emerging Technology", name: "Emerging Technology", icon: Zap, color: "from - yellow - 500 to - orange - 600" },;" { id: "IT Services", name: "IT Services", icon: Code, color: "from - green - 500 to - teal - 600" },;" { id: "Telecommunications", name: "Telecommunications", icon: Signal, color: "from - blue - 500 to - indigo - 600" };" ];" const filteredServices = INNOVATIVE_SERVICES_2027.filter (service =>; selectedCategory === "All" || service.category === selectedCategory) ;" const getCategoryStats = (categoryId: string) => {; if (categoryId === "All") {" return {" count: innovativeServices2027.length, avgPrice: Math.round (innovativeServices2027.reduce ( (sum, s) => sum + parseInt (s.price.replace (/[$,]/g, ") .split ("/") [0]) , 0) / innovativeServices2027.length) , avgRating: Math.round ( (innovativeServices2027.reduce ( (sum, s) => sum + s.rating, 0) / innovativeServices2027.length) * 10) / 10;" }};" const services = innovativeServices2027.filter (s => s.category === categoryId) ; return { count: services.length, avgPrice: Math.round (services.reduce ( (sum, s) => sum + parseInt (s.price.replace (/[$,]/g, ") .split ("/") [0]) , 0) / services.length) , avgRating: Math.round ( (services.reduce ( (sum, s) => sum + s.rating, 0) / services.length) * 10) / 10" }};" const filteredServices = INNOVATIVE_SERVICES_2027.filter (service => ; selectedCategory === "All" || service.category === selectedCategory;) ;" const getCategoryStats = (categoryId: string) => {; if (categoryId === "All") {;" return {;" count: INNOVATIVE_SERVICES_2027.length,; avgPrice: Math.round (INNOVATIVE_SERVICES_2027.reduce ( (sum, s) => sum + s.price, 0) / INNOVATIVE_SERVICES_2027.length) ,; avgRating: Math.round ( (INNOVATIVE_SERVICES_2027.reduce ( (sum, s) => sum + s.rating, 0) / INNOVATIVE_SERVICES_2027.length) * 10) / 10; }; const services = INNOVATIVE_SERVICES_2027.filter (s => s.category === categoryId) ; return { count: services.length, avgPrice: Math.round (services.reduce ( (sum, s) => sum + s.price, 0) / services.length) , avgRating: Math.round ( (services.reduce ( (sum, s) => sum + s.rating, 0) / services.length) * 10) / 10 }; }; const containerVariants = {; hidden: { opacity: 0 },; visible: {; 