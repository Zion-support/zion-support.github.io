" // Changed import" " // Adjust path as needed; // Define interfaces for the data structures based on your API and Prisma schema; interface TrustScoreComponent {} id: number; componentType: string; value: number; weight: number; details?: unknown; // JSON field; createdAt: string; } interface TrustScoreData {} id: number; score: number; userId: number; operatorGptAnalysis?: string | null; components: TrustScoreComponent[]; createdAt: string; updatedAt: string; } interface TrustScoreHistoryEntry {} id: number; userId: number; trustScoreId?: number | null; score: number; operatorGptAnalysis?: string | null; changedAt: string; reasonForChange?: string | null; componentValues?: unknown; // JSON field; } const UserProfileTrustPage: React.FC = () => { const { id: userId } = useParams<{ id: string }>(); // Changed to useParams, assuming route is /profile/:id/trust const [trustScoreData, setTrustScoreData] = useState<TrustScoreData | null>( null ); 