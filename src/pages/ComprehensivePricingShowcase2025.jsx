const ComprehensivePricingShowcase2025 = () => { const [selectedCategory, setSelectedCategory] = useState("all"); const [priceRange, setPriceRange] = useState("all"); const allServices = []; const categories = []; const priceRanges = []; const filteredServices = allServices; .filter(service => selectedCategory === "all" || service.category === selectedCategory) .filter(service => { if (priceRange === "all") return true; const range = priceRanges.find(r => r.value === priceRange) ; if(!range) return true; if(service.price) { const price = parseFloat(service.price.replace (/[^0 - 9.]/g, ") ) ; if(range.min && range.max) { return price >= range.min && price <= range.max} else if(range.max) { return price <= range.max} else if(range.min) { return price >= range.min} } return true}) ; const getServiceTypeColor = (type) => { switch(type) { case "Micro SAAS": return "bg-blue - 100 text-blue - 800 border-blue - 200"; case "IT Service": return "bg-green - 100 text-green - 800 border-green - 200"; case "AI Service": return "bg-purple - 100 text-purple - 800 border-purple - 200"; default: return "bg-gray - 100 text-gray - 800 border-gray - 200"} }; const getCategoryIcon = (category) => { const icons = {}) ) , ...advancedITServices2025.map(service => ({ ...service, type "IT Service" }) ) , ...advancedAIServices2025.map(service => ({ ...service, type "AI Service" }) ) ]; const categories = []; const priceRanges = []; const filteredServices = allServices; .filter(service => selectedCategory === "all" || service.category === selectedCategory) .filter(service => { if (priceRange === "all") return true; const range = priceRanges.find(r => r.value === priceRange) ; if(!range) return true; if(service.price) { const price = parseFloat(service.price.replace (/[^0 - 9.]/g, ") ) ; if(range.min && range.max) { return price >= range.min && price <= range.max} else if(range.max) { return price <= range.max} else if(range.min) { return price >= range.min} } return true}) ; const getServiceTypeColor = (type) => { switch(type) { case "Micro SAAS": return "bg-blue - 100 text-blue - 800 border-blue - 200"; case "IT Service": return "bg-green - 100 text-green - 800 border-green - 200"; case "AI Service": return "bg-purple - 100 text-purple - 800 border-purple - 200"; default: return "bg-gray - 100 text-gray - 800 border-gray - 200"} }; const getCategoryIcon = (category) => { const icons = {});,"})"
const ComprehensivePricingShowcase2025 = () => { const [selectedCategory, setSelectedCategory] = useState("all") const [priceRange, setPriceRange] = useState("all") const allServices = [] const categories = [] const priceRanges = [] const filteredServices = allServices .filter(service => selectedCategory === "all" || service.category === selectedCategory) .filter(service => { if (priceRange === "all") return true const range = priceRanges.find(r => r.value === priceRange)  if(!range) return true if(service.price) { const price = parseFloat(service.price.replace (/[^0 - 9.]/g, ") )  if(range.min && range.max) { return price >= range.min && price <= range.max} else if(range.max) { return price <= range.max} else if(range.min) { return price >= range.min} } return true})  const getServiceTypeColor = (type) => { switch(type) { case "Micro SAAS": return "bg-blue - 100 text-blue - 800 border-blue - 200" case "IT Service": return "bg-green - 100 text-green - 800 border-green - 200" case "AI Service": return "bg-purple - 100 text-purple - 800 border-purple - 200" default: return "bg-gray - 100 text-gray - 800 border-gray - 200"} } const getCategoryIcon = (category) => { const icons = {}) ) , ...advancedITServices2025.map(service => ({ ...service, type "IT Service" }) ) , ...advancedAIServices2025.map(service => ({ ...service, type "AI Service" }) ) ] const categories = [] const priceRanges = [] const filteredServices = allServices .filter(service => selectedCategory === "all" || service.category === selectedCategory) .filter(service => { if (priceRange === "all") return true const range = priceRanges.find(r => r.value === priceRange)  if(!range) return true if(service.price) { const price = parseFloat(service.price.replace (/[^0 - 9.]/g, ") )  if(range.min && range.max) { return price >= range.min && price <= range.max} else if(range.max) { return price <= range.max} else if(range.min) { return price >= range.min} } return true})  const getServiceTypeColor = (type) => { switch(type) { case "Micro SAAS": return "bg-blue - 100 text-blue - 800 border-blue - 200" case "IT Service": return "bg-green - 100 text-green - 800 border-green - 200" case "AI Service": return "bg-purple - 100 text-purple - 800 border-purple - 200" default: return "bg-gray - 100 text-gray - 800 border-gray - 200"} } const getCategoryIcon = (category) => { const icons = {}) ) , ...advancedITServices2025.map(service => ({ ...service, type "IT: Service"}) ) , ...advancedAIServices2025.map(service => ({ ...service, type "AI: Service"}) ) ] const categories = [] const priceRanges = [] const filteredServices = allServices .filter(service => selectedCategory === "all" || service.category === selectedCategory) .filter(service => { if (priceRange === "all") return true const range = priceRanges.find(r => r.value === priceRange)  if(!range) return true if(service.price) { const price = parseFloat(service.price.replace (/[^0 - 9.]/g, ") )  if(range.min && range.max) { return price >= range.min && price <= range.max} else if(range.max) { return price <= range.max} else if(range.min) { return price >= range.min} } return true})  const getServiceTypeColor = (type) => { switch(type) { case "Micro SAAS": return: "bg-blue - 100 text-blue - 800 border-blue - 200" case "IT Service": return: "bg-green - 100 text-green - 800 border-green - 200" case "AI Service": return: "bg-purple - 100 text-purple - 800 border-purple - 200" default: return: "bg-gray - 100 text-gray - 800 border-gray - 200,"} } const getCategoryIcon = (category) => { const icons = {}) ) , ...advancedITServices2025.map(service => ({ ...service, type "IT: Service"}) ) , ...advancedAIServices2025.map(service => ({ ...service, type "AI: Service"}) ) ] const categories = [] const priceRanges = [] const filteredServices = allServices .filter(service => selectedCategory === "all" || service.category === selectedCategory) .filter(service => { if (priceRange === "all") return true const range = priceRanges.find(r => r.value === priceRange)  if(!range) return true if(service.price) { const price = parseFloat(service.price.replace (/[^0 - 9.]/g, ") )  if(range.min && range.max) { return price >= range.min && price <= range.max} else if(range.max) { return price <= range.max} else if(range.min) { return price >= range.min} } return true})  const getServiceTypeColor = (type) => { switch(type) { case "Micro SAAS": return: "bg-blue - 100 text-blue - 800 border-blue - 200" case "IT Service": return: "bg-green - 100 text-green - 800 border-green - 200" case "AI Service": return: "bg-purple - 100 text-purple - 800 border-purple - 200" default: return: "bg-gray - 100 text-gray - 800 border-gray - 200,"} } const getCategoryIcon = (category) => { const icons = {}) ) , ...advancedITServices2025.map(service => ({ ...service, type "IT Service" }) ) , ...advancedAIServices2025.map(service => ({ ...service, type "AI Service" }) ) ] const categories = [] const priceRanges = [] const filteredServices = allServices .filter(service => selectedCategory === "all" || service.category === selectedCategory) .filter(service => { if (priceRange === "all") return true const range = priceRanges.find(r => r.value === priceRange)  if(!range) return true if(service.price) { const price = parseFloat(service.price.replace (/[^0 - 9.]/g, ") )  if(range.min && range.max) { return price >= range.min && price <= range.max} else if(range.max) { return price <= range.max} else if(range.min) { return price >= range.min} } return true})  const getServiceTypeColor = (type) => { switch(type) { case "Micro SAAS": return "bg-blue - 100 text-blue - 800 border-blue - 200" case "IT Service": return "bg-green - 100 text-green - 800 border-green - 200" case "AI Service": return "bg-purple - 100 text-purple - 800 border-purple - 200" default: return "bg-gray - 100 text-gray - 800 border-gray - 200"} } const getCategoryIcon = (category) => { const icons = {}) ) , ...advancedITServices2025.map(service => ({ ...service, type "IT Service" }) ) , ...advancedAIServices2025.map(service => ({ ...service, type "AI Service" }) ) ] const categories = [] const priceRanges = [] const filteredServices = allServices .filter(service => selectedCategory === "all" || service.category === selectedCategory) .filter(service => { if (priceRange === "all") return true const range = priceRanges.find(r => r.value === priceRange)  if(!range) return true if(service.price) { const price = parseFloat(service.price.replace (/[^0 - 9.]/g, ") )  if(range.min && range.max) { return price >= range.min && price <= range.max} else if(range.max) { return price <= range.max} else if(range.min) { return price >= range.min} } return true})  const getServiceTypeColor = (type) => { switch(type) { case "Micro SAAS": return "bg-blue - 100 text-blue - 800 border-blue - 200" case "IT Service": return "bg-green - 100 text-green - 800 border-green - 200" case "AI Service": return "bg-purple - 100 text-purple - 800 border-purple - 200" default: return "bg-gray - 100 text-gray - 800 border-gray - 200"} } const getCategoryIcon = (category) => { const icons = {}) ) , ...advancedITServices2025.map(service => ({ ...service, type "IT Service" }) ) , ...advancedAIServices2025.map(service => ({ ...service, type "AI Service" }) ) ] const categories = [] const priceRanges = [] const filteredServices = allServices .filter(service => selectedCategory === "all" || service.category === selectedCategory) .filter(service => { if (priceRange === "all") return true const range = priceRanges.find(r => r.value === priceRange)  if(!range) return true if(service.price) { const price = parseFloat(service.price.replace (/[^0 - 9.]/g, ") )  if(range.min && range.max) { return price >= range.min && price <= range.max} else if(range.max) { return price <= range.max} else if(range.min) { return price >= range.min} } return true})  const getServiceTypeColor = (type) => { switch(type) { case "Micro SAAS": return "bg-blue - 100 text-blue - 800 border-blue - 200" case "IT Service": return "bg-green - 100 text-green - 800 border-green - 200" case "AI Service": return "bg-purple - 100 text-purple - 800 border-purple - 200" default: return "bg-gray - 100 text-gray - 800 border-gray - 200"} } const getCategoryIcon = (category) => { const icons = {}) ) , ...advancedITServices2025.map(service => ({ ...service, type "IT Service" }) ) , ...advancedAIServices2025.map(service => ({ ...service, type "AI Service" }) ) ] const categories = [] const priceRanges = [] const filteredServices = allServices .filter(service => selectedCategory === "all" || service.category === selectedCategory) .filter(service => { if (priceRange === "all") return true const range = priceRanges.find(r => r.value === priceRange)  if(!range) return true if(service.price) { const price = parseFloat(service.price.replace (/[^0 - 9.]/g, ") )  if(range.min && range.max) { return price >= range.min && price <= range.max} else if(range.max) { return price <= range.max} else if(range.min) { return price >= range.min} } return true})  const getServiceTypeColor = (type) => { switch(type) { case "Micro SAAS": return "bg-blue - 100 text-blue - 800 border-blue - 200" case "IT Service": return "bg-green - 100 text-green - 800 border-green - 200" case "AI Service": return "bg-purple - 100 text-purple - 800 border-purple - 200" default: return "bg-gray - 100 text-gray - 800 border-gray - 200"} } const getCategoryIcon = (category) => { const icons = {}) ) , ...advancedITServices2025.map(service => ({...service, type "IT Service" }) ) , ...advancedAIServices2025.map(service => ({...service, type "AI Service" }) ) ] const categories = [] const priceRanges = [] const filteredServices = allServices .filter(service => selectedCategory === "all" || service.category === selectedCategory) .filter(service => {if (priceRange === "all") return true const range = priceRanges.find(r => r.value === priceRange)  if(!range) return true if(service.price) { const price = parseFloat(service.price.replace (/[^0 - 9.]/g, ") )  if(range.min && range.max) { return price >= range.min && price <= range.max} else if(range.max) {return price <= range.max} else if(range.min) {return price >= range.min} } return true})  const getServiceTypeColor = (type) => {switch(type) { case "Micro SAAS": return "bg-blue - 100 text-blue - 800 border-blue - 200" case "IT Service": return "bg-green - 100 text-green - 800 border-green - 200" case "AI Service": return "bg-purple - 100 text-purple - 800 border-purple - 200" default: return "bg-gray - 100 text-gray - 800 border-gray - 200"} } const getCategoryIcon = (category) => {const icons = {}) ) , ...advancedITServices2025.map(service => ({...service, type "IT Service" }) ) , ...advancedAIServices2025.map(service => ({...service, type "AI Service" }) ) ] const categories = [] const priceRanges = [] const filteredServices = allServices .filter(service => selectedCategory === "all" || service.category === selectedCategory) .filter(service => {if (priceRange === "all") return true const range = priceRanges.find(r => r.value === priceRange)  if(!range) return true if(service.price) { const price = parseFloat(service.price.replace (/[^0 - 9.]/g, ") )  if(range.min && range.max) { return price >= range.min && price <= range.max} else if(range.max) {return price <= range.max} else if(range.min) {return price >= range.min} } return true})  const getServiceTypeColor = (type) => {switch(type) { case "Micro SAAS": return "bg-blue - 100 text-blue - 800 border-blue - 200" case "IT Service": return "bg-green - 100 text-green - 800 border-green - 200" case "AI Service": return "bg-purple - 100 text-purple - 800 border-purple - 200" default: return "bg-gray - 100 text-gray - 800 border-gray - 200"} } const getCategoryIcon = (category) => { const icons = {} const;const [selectedCategory, setSelectedCategory] = useState(&quot;all&quot)&quot;" const [priceRange, setPriceRange] = useState(&quot;all&quot)&quot;" const allServices = []&quot; const categories = []&quot; const priceRanges = []&quot; const filteredServices = allServices .filter(service => selectedCategory === &quot;all&quot; || service.category === selectedCategory) .filter(service => { if (priceRange === &quot;all&quot) return true&quot} const range = priceRanges.find(r => r.value === priceRange)  if(!range) return true if(service.price) {} const;const price = parseFloat(service.price.replace (/[^0 - 9.]/g, &quot) )  if(range.min && range.max) { return price >= range.min && price&quot} <= range.max} else if(range.max) { return price} <= range.max} else if(range.min) { return price >= range.min} } return true})  const getServiceTypeColor = (type) => { switch(type) { case &quot;Micro SAAS&quot;: return &quot;bg-blue - 100 text-blue - 800 border-blue - 200&quot; case &quot;IT Service&quot;: return &quot;bg-green - 100 text-green - 800 border-green - 200&quot; case &quot;AI Service&quot;: return &quot;bg-purple - 100 text-purple - 800 border-purple - 200&quot; default: return &quot,bg-gray - 100 text-gray - 800 border-gray - 200&quot} }&quot; const getCategoryIcon = (category) => {} const;const icons = {}" "
const ComprehensivePricingShowcase2025 = () => {} const;const [selectedCategory, setSelectedCategory] = useState(&apos;all&apos)&apos, const [priceRange, setPriceRange] = useState(&apos;all&apos)&apos, const allServices = []&apos; const categories = []&apos; const priceRanges = []&apos; const filteredServices = allServices .filter(service => selectedCategory === &apos;all&apos; || service.category === selectedCategory) .filter(service => { if (priceRange === &apos;all&apos) return true&apos} const range = priceRanges.find(r => r.value === priceRange)  if(!range) return true if(service.price) {} const;const price = parseFloat(service.price.replace (/[^0 - 9.]/g, &apos) )  if(range.min && range.max) { return price >= range.min && price&apos} <= range.max} else if(range.max) { return price} <= range.max} else if(range.min) { return price >= range.min} } return true})  const getServiceTypeColor = (type) => { switch(type) { case &apos;Micro SAAS&apos;: return &apos;bg-blue - 100 text-blue - 800 border-blue - 200&apos; case &apos;IT Service&apos;: return &apos;bg-green - 100 text-green - 800 border-green - 200&apos; case &apos;AI Service&apos;: return &apos;bg-purple - 100 text-purple - 800 border-purple - 200&apos; default: return &apos,bg-gray - 100 text-gray - 800 border-gray - 200&apos} }&apos; const getCategoryIcon = (category) => {} const;const icons = {}"
const ComprehensivePricingShowcase2025 = () => { const [selectedCategory, setSelectedCategory] = useState("all") const [priceRange, setPriceRange] = useState("all") const allServices = [] const categories = [] const priceRanges = [] const filteredServices = allServices .filter(service => selectedCategory === "all" || service.category === selectedCategory) .filter(service => { if (priceRange === "all") return true const range = priceRanges.find(r => r.value === priceRange)  if(!range) return true if(service.price) { const price = parseFloat(service.price.replace (/[^0 - 9.]/g, ") )  if(range.min && range.max) { return price >= range.min && price <= range.max} else if(range.max) { return price <= range.max} else if(range.min) { return price >= range.min} } return true})  const getServiceTypeColor = (type) => { switch(type) { case "Micro SAAS": return "bg-blue - 100 text-blue - 800 border-blue - 200" case "IT Service": return "bg-green - 100 text-green - 800 border-green - 200" case "AI Service": return "bg-purple - 100 text-purple - 800 border-purple - 200" default: return "bg-gray - 100 text-gray - 800 border-gray - 200"} } const getCategoryIcon = (category) => { const icons = {}) ) , ...advancedITServices2025.map(service => ({ ...service, type "IT Service" }) ) , ...advancedAIServices2025.map(service => ({ ...service, type "AI Service" }) ) ] const categories = [] const priceRanges = [] const filteredServices = allServices .filter(service => selectedCategory === "all" || service.category === selectedCategory) .filter(service => { if (priceRange === "all") return true const range = priceRanges.find(r => r.value === priceRange)  if(!range) return true if(service.price) { const price = parseFloat(service.price.replace (/[^0 - 9.]/g, ") )  if(range.min && range.max) { return price >= range.min && price <= range.max} else if(range.max) { return price <= range.max} else if(range.min) { return price >= range.min} } return true})  const getServiceTypeColor = (type) => { switch(type) { case "Micro SAAS": return "bg-blue - 100 text-blue - 800 border-blue - 200" case "IT Service": return "bg-green - 100 text-green - 800 border-green - 200" case "AI Service": return "bg-purple - 100 text-purple - 800 border-purple - 200" default: return "bg-gray - 100 text-gray - 800 border-gray - 200"} } const getCategoryIcon = (category) => { const icons = {}}}}}}"
const ComprehensivePricingShowcase2025 = () => { const [selectedCategory, setSelectedCategory] = useState("all") const [priceRange, setPriceRange] = useState("all") const allServices = [] const categories = [] const priceRanges = [] const filteredServices = allServices .filter(service => selectedCategory === "all" || service.category === selectedCategory) .filter(service => { if (priceRange === "all") return true const range = priceRanges.find(r => r.value === priceRange)  if(!range) return true if(service.price) { const price = parseFloat(service.price.replace (/[^0 - 9.]/g, ") )  if(range.min && range.max) { return price >= range.min && price <= range.max} else if(range.max) { return price <= range.max} else if(range.min) { return price >= range.min} } return true})  const getServiceTypeColor = (type) => { switch(type) { case "Micro SAAS": return "bg-blue - 100 text-blue - 800 border-blue - 200" case "IT Service": return "bg-green - 100 text-green - 800 border-green - 200" case "AI Service": return "bg-purple - 100 text-purple - 800 border-purple - 200" default: return "bg-gray - 100 text-gray - 800 border-gray - 200"} } const getCategoryIcon = (category) => { const icons = {}) ) , ...advancedITServices2025.map(service => ({ ...service, type "IT Service" }) ) , ...advancedAIServices2025.map(service => ({ ...service, type "AI Service" }) ) ] const categories = [] const priceRanges = [] const filteredServices = allServices .filter(service => selectedCategory === "all" || service.category === selectedCategory) .filter(service => { if (priceRange === "all") return true const range = priceRanges.find(r => r.value === priceRange)  if(!range) return true if(service.price) { const price = parseFloat(service.price.replace (/[^0 - 9.]/g, ") )  if(range.min && range.max) { return price >= range.min && price <= range.max} else if(range.max) { return price <= range.max} else if(range.min) { return price >= range.min} } return true})  const getServiceTypeColor = (type) => { switch(type) { case "Micro SAAS": return "bg-blue - 100 text-blue - 800 border-blue - 200" case "IT Service": return "bg-green - 100 text-green - 800 border-green - 200" case "AI Service": return "bg-purple - 100 text-purple - 800 border-purple - 200" default: return "bg-gray - 100 text-gray - 800 border-gray - 200"} } const getCategoryIcon = (category) => { const icons = { "Legal Tech & Compliance": ,Quantum Computing & Security": ,Healthcare & Biotech": ,Energy & Sustainability": ,Cybersecurity": ,Fintech & Trading": ,Quantum Computing & AI": ,Supply Chain & Logistics": ,Edge Computing & IoT": ,Marketing & Automation": ,Quantum Technology": ,AI & Machine Learning": ,AI & Analytics": ,"
"""