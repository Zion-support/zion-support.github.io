import React, { useState } from 'react'; import { motion, AnimatePresence } from 'framer-motion'; import { CheckCircle, Star, TrendingUp, DollarSign, Clock, Users, Shield, Zap, Brain, Rocket, Atom, Leaf, Cpu, CreditCard, Phone, Mail, MessageSquare, ArrowRight, X, Info, Target, Award, Globe, Building, Briefcase } from 'lucide-react'; import { enhancedServices2025, EnhancedService } from '../data/enhancedServices2025'; const EnhancedPricingPage: React.FC = () => { const [selectedPlan, setSelectedPlan] = useState<string | null>(null); const [billingCycle, setBillingCycle] = useState<'month' | 'year'>('month'); const [showComparison, setShowComparison] = useState(false); const allServices = enhancedServices2025.flatMap(category => category.items); const featuredServices = allServices.filter(service => service.featured); const getDiscountedPrice = (price: string, cycle: 'month' | 'year') => { if (cycle === 'year') { const monthlyPrice = parseInt(price.replace(/[^0-9]/g, '')); const yearlyPrice = monthlyPrice * 12 * 0.8;