import React from 'react'; "export default AdvancedInnovativeServicesShowcase2026;}}}}}}}}"import {; " Brain, Zap, Globe, Shield, Cpu, Database, Network, Lock, TrendingUp, Users, Search, Filter, Star, Clock, DollarSign, Target, ArrowRight, Phone, Mail, MapPin, ExternalLink } from "lucide - react";"const AdvancedInnovativeServicesShowcase2026: React.FC = () => {; const [searchTerm, setSearchTerm] = useState (") ;" const [selectedCategory, setSelectedCategory] = useState ("all") ;" const [sortBy, setSortBy] = useState ("innovation") ;" const cats = new Set (ADVANCED_INNOVATIVE_SERVICES_2026.map (service => service.category) ) ; return ["all", ...Array.from (cats) ];" }, []) ;" const filtered = ADVANCED_INNOVATIVE_SERVICES_2026; // Filter by category if (selectedCategory !== "all") {" filtered = filtered.filter (service => service.category === selectedCategory) }" // Filter by search term if (searchTerm) { filtered = filtered.filter (service =>; service.title.toLowerCase () .includes (searchTerm.toLowerCase () ) ||; service.description.toLowerCase () .includes (searchTerm.toLowerCase () ) ||; service.tags.some (tag => tag.toLowerCase () .includes (searchTerm.toLowerCase () ) ) ;) ; } // Sort services filtered.sort ( (a, b) => { switch (sortBy) { case "innovation": return b.innovationLevel.localeCompare (a.innovationLevel) ;" case "roi": return parseFloat (b.roi.replace ("%", ") ) - parseFloat (a.roi.replace ("%", ") ) ;" case "price": return a.price - b.price;" case "delivery": return a.estimatedDelivery.localeCompare (b.estimatedDelivery) ;" default: return 0}" }) ; return filtered}, [searchTerm, selectedCategory, sortBy]) ; const containerVariants = {; hidden: { opacity: 0 },; visible: {; 