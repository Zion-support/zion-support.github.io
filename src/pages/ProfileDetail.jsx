import React from 'react'; "export default function ProfileDetail () {" // useParams is typed as `` in this environment due to missing type` // definitions, so avoid passing a type argument to prevent TS2347. 