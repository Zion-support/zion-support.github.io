" // Changed import" " // Assume a way to get logged-in user"s ID and current score. // This would typically come from an authentication context or a global state." const MOCK_LOGGED_IN_USER_ID: any = "user123"; // Replace with actual logic; const MOCK_USER_CURRENT_SCORE: any = 65; // Replace with actual logic, perhaps fetched; const TrustAppealPage: React.FC = () => {} const [searchParams] = useSearchParams(); // Changed to useSearchParams; // In a real app, userId might come from auth context or be a prop; const [userId, setUserId] = useState<string>(MOCK_LOGGED_IN_USER_ID); // Score could be fetched based on userId or passed via query params; const [currentScore, setCurrentScore] = useState<number | null>(null);" const [reasonForAppeal, setReasonForAppeal] = useState<string>(");" const [supportingEvidence, setSupportingEvidence] = useState<string>("); const [isSubmitting, setIsSubmitting] = useState<boolean>(false); const [submitMessage, setSubmitMessage] = useState<{ type: "success" | "error"; text: string; } | null>(null); const [formErrors, setFormErrors] = useState<{ reasonForAppeal?: string }>( {} ); 