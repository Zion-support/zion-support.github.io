import React from 'react';
import { motion } from 'framer-motion';

const Careers: React.FC = () => {
  const benefits = [
    {
    }
  ];

  const values = [
    {
    }
  ];

  return (
        </div>
      </div>

              </motion.div>
            ))}
          </div>
        </div>
      </div>

              </motion.div>
            ))}
          </div>
        </div>
      </div>

      {/* Open Positions */}
              </motion.div>
            ))}
          </div>
        </div>
      </div>

      {/* CTA Section */}
            </div>
          </motion.div>
        </div>
      </div>
    </div>
  );
};

