import { Zap } from "lucide-react"; import React, { useMemo, useState } from "react"; import BlogPromotionBanner from "../components/BlogPromotionBanner"; // const Footer: React.FC = () => null; // import Header from "../components/Header"; interface BlogPost { id: number;, title: string;, excerpt: string;, content: string;, author: string;, date: string;, readTime: string;, category: string;, tags: string[];, image: string;, featured: boolean;, views: number; } export default function Blog(): React.JSX.Element { const [selectedCategory, setSelectedCategory] = useState("All");"; const [searchTerm, setSearchTerm] = useState("");"; const categories = [ "All", "AI Strategy", "Security", "Edge Computing", "GenAI", "Data Engineering", "Technology", "AI Solutions", "Case Studies", "Industry Insights", "Tutorials" 