import { useEffect, useState } from "react"
import { EnhancedSearchInput } from "@/components/search/
import { generateSearchSuggestions } from "@/data/
import { SearchSuggestion } from "@/types/
  TabsTrigger} from "@/components/ui/
  const escaped = term.replace(/[.*+?^${}()|[\]\\]/
  !loading && marketplaceResults.length = = 0 && blogResults.length > 0 && (<div> <p className="text-zion-slate-light mb-2" >No marketplace results found. Related blog posts: any