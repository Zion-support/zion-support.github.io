import React from 'react'; "export default OpenAppRedirect;" // This component handles deep linking to the mobile app const OpenAppRedirect = () => {; const navigate = useNavigate () ; const [status, setStatus] = useState ("redirecting") ;" useEffect ( () => {" const attemptAppOpen = async () => {; const isAndroid = /Android/.test (navigator.userAgent) ; // App scheme URLs (these would be your actual app&apos;s URL schemes) const androidAppUrl = "market: //details?id = app.zion.marketplace";" const iosAppUrl = "https: //apps.apple.com / app / zion - ai - marketplace / id0000000000";" const fallbackUrl = "/mobile - launch"; // Fallback to mobile launch page" let timeout;" // Try to open the app if (isAndroid || isiOS) { // Set a timeout to redirect to app store if the app doesn&apos;t open" timeout = window.setTimeout ( () => {" setStatus ("timeout") ;" if (isAndroid) {" window.location.href = androidAppUrl} else if (isiOS) { 