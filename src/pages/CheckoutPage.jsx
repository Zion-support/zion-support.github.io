import React from 'react'; "// Changed from useParams"export default function CheckoutPage () {" const router = useNavigate () ; const { id: rawId } = router.query; const id = typeof rawId === "string" ? rawId : null;" const [product, setProduct] = useState (null) ;" const [intent, setIntent] = useState (null) ; const [isLoading, setIsLoading] = useState (true) ; const [error, setError] = useState (null) ; useEffect ( () => { const fetchProduct = async () => {; if (!id) return; setIsLoading (true) ; setError (null) ; try { const res = await fetch (`/api / products/${id}`) ;` if (!res.ok) throw new Error ("Failed to fetch product") ;" const data = await res.json () ;" setProduct (data) ; } catch (err) { // // // // // // // console.error ("Failed to load product", err) ;" setError (err.message) ;" const fallback = NEW_PRODUCTS.find (p => p.id === id) || null; setProduct (fallback) ; } finally { setIsLoading (false) ; 