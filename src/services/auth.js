import React from 'react'; export default authService; // Mock authentication service; // In a real application, this would connect to your backend API; ; const API_BASE_URL = process.env.REACT_APP_API_URL || "https: //api.zion.com";";"class AuthService { constructor() { this.token = localStorage.getItem("token");" this.user = JSON.parse(localStorage.getItem("user") || "null");" }" // Login user async login(credentials) { try { // Simulate API call delay await new Promise (resolve => setTimeout (resolve, 1000) ) ; // Mock validation if (!credentials.email || !credentials.password) { throw new Error("Email and password are required");" }" // Mock successful login const mockUser = {; id: 1, email: credentials.email,; name: "John Doe", role: "user"," avatar: null, createdAt: new Date().toISOString()};" const mockToken = "mock-jwt-token-" + Date.now();" // Store in localStorage this.token = mockToken; this.user = mockUser; localStorage.setItem("token", mockToken);" localStorage.setItem("user", JSON.stringify(mockUser));" return { user: mockUser, token: mockToken}; } catch (error) { throw new Error(error.message || "Login failed");" }" } 