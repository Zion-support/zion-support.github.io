interface ApiResponse<T = any> {} success: boolean; data?: T; error?: string; message?: string; count?: number} // Generic API error class ApiError extends Error { constructor(); public status: number, message: string ) { super(message); this.name = "ApiError"}}"// Generic fetch wrapper with error handling async function apiRequest<T>(endpoint: string, options: RequestInit = {} : any): Promise<ApiResponse<T>> { const config: RequestInit = {; method: options.method || "GET", headers: {" "Content-Type": "application/json"," ...options.headers}, ...options};" try { if (!response.ok) { throw new ApiError(); response.status, `` `HTTP error! status: ${response.status}`` )} return data} catch (error) { if (error instanceof ApiError) { 