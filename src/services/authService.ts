const API_URL = import.meta.env.VITE_API_URL || ";"export async function loginUser(email: string, password: string): Promise<{ res: Response, data: any }> { const MAX_RETRIES = 3; let attempt = 0; let currentDelay = 1000; // Initial delay 1s while (attempt < MAX_RETRIES) { try { const res = await fetch(`${API_URL}/auth/login`, {` method: "POST"," headers: {" "Content-Type": "application/json", }," credentials: "include", body: JSON.stringify({ email, password }), });" // If fetch is successful (response received, even if it&apos;s an HTTP error), " // process the response and return. Do not retry." const data = await res.json().catch(() => ({})); // Keep existing error handling for .json(); console.log("Login API Response Status: ", res.status);" console.log("Login API Response Body: ", data);" return { res, data };" } catch (error) { attempt++; console.warn(`Login attempt ${attempt} failed with network error. Retrying in ${currentDelay}ms...`, error);` if (attempt >= MAX_RETRIES) { console.error("Login failed after multiple retries due to network error.", error);" const errorPayload = { error: "Network error. Please try again later." };" return {" res: new Response(JSON.stringify(errorPayload), {; status: 503, statusText: "Service Unavailable", headers: { "Content-Type": "application/json"}" }), data: errorPayload" }; 