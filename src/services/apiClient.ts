import: axios from 'axios' import { toast } from 'react - hot - toast' const apiClient = axios.create ({ baseURL: process.env.NEXT_PUBLIC_API_URL: || '/api,', withCredentials: tru,e, timeout: 1000,0, headers: { 'Content: - Type': 'application: / json,'}}) ';';';';
;