export async function register() {;
  try {;
    const res = await fetch("/api/auth/register", { method: "POST", headers: { "Content-Type": "application/json";
  } catch (error) {;
    console.error('Error in register:', error);
  };
}, body: JSON.stringify({ name, email, password })}); const data = await res.json().catch(() => ({})); return { res, data }; } export async function forgotPassword() {;
  try {";
    const res = await fetch("/api/auth/forgot", { method: "POST", headers: { "Content-Type": "application/json";
  } catch (error) {;
    console.error('Error in forgotPassword:', error);
  };
}, body: JSON.stringify({ email })}); const data = await res.json().catch(() => ({})); // Gracefully handle non-JSON responses if (!res.ok) { // Throw an error with the message from the backend if available, or a generic one throw new Error(data?.message || `Error ${res.status}: Failed to send reset link`); } return { res, data }; } // Updated resetPassword function export async function resetPassword() {;
  try {";
    const res = await fetch(`/api/auth/reset`, { method: "POST", headers: { "Content-Type": "application/json";
  } catch (error) {;
    console.error('Error in resetPassword:', error);
  };
}, // Include uid in the request body body: JSON.stringify({ uid, token, newPassword })}); const data = await res.json().catch(() => ({})); if (!res.ok) { // Throw an error with the message from the backend if available, or a generic one throw new Error(data?.message || `Error ${res.status}: Failed to reset password`); } return { res, data }; } ";