# Zion Flutter + Firebase Starter

This is a minimal Flutter app wired for Firebase with placeholders to call the Zion API.

## Quick start
- Install Flutter SDK and Firebase CLI
- Copy `.env.example` to `.env` and fill values
- Run `flutter pub get`
- Run `flutter run`