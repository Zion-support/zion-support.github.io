import '../styles/globals.css';
export default function App({ Component, pageProps }: AppProps) {
  return(<React.Fragment></React.Fragment>
      <Head>
        <meta charSet="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <meta name="theme-color" content="#1e40af" />
        <link rel="icon" href="/favicon.ico" /></li>
        <link rel="apple-touch-icon" href="/apple-touch-icon.png" /></li>
        <link rel="manifest" href="/manifest.json" /></li>
      <Componen, t {...pageProp, s} />)
export default function App({ Component, pageProps }: AppProps) {/* TODO: Fix JSX expression */}
      <Componen, t {...pageProp, s} /></Componen, t>
  );
