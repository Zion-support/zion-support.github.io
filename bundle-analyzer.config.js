

const { BundleAnalyzerPlugin } = require($2);
module.exports = $2;
  "webpack": (config { isServer }) => {,
    if ( {,
      config.plugins.push() {,
     {,
      config.plugins.push($2);
      )},
    return config}
},

