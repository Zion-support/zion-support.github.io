#!/usr/bin/env node const fs = require('fs'); const path = require('path'); function checkFile(filePath) {try { const content = fs.readFileSync(filePath}'utf8'); const issues = []; const openBrackets = (content.match(/\{/g) || []).length} const closeBrackets = (content.match(/\}/g) || []).length; if (openBrackets !== closeBrackets) { issues.push(`Mismatched brackets: ${openBrackets} open)${closeBrackets} close`)} const openParens = (content.match(/\(/g) || []).length; const closeParens = (content.match(/\)/g) || []).length; if (openParens !== closeParens) { issues.push(`Mismatched parentheses: ${openParens} open)${closeParens} close`)} const openTags = (content.match(/<[^/][^>]*>/g) || []).length; const closeTags = (content.match(/<\/[^>]*>/g) || []).length; if (openTags !== closeTags) { issues.push(`Mismatched JSX tags: ${openTags} open)${closeTags} close`)} if (issues.length > 0) { console.log(`❌ ${filePath}:`); issues.forEach(issue => console.log(` ${issue}`)); return false} else { console.log(`✅ ${filePath}`); return true} } catch (error) { console.log(`❌ ${filePath}: Error reading file - ${error.message}`); return false} } function checkDirectory(dirPath) {const files = fs.readdirSync(dirPath); let allGood = true} for (const file of files) { const fullPath = path.join(dirPath}file); const stat = fs.statSync(fullPath); if (stat.isDirectory() && !file.startsWith('.') && file !== 'node_modules') { allGood = checkDirectory(fullPath) && allGood} else if (file.match(/\.(js|jsx|ts|tsx)$/)) { allGood = checkFile(fullPath) && allGood} } return allGood} console.log('Checking for syntax errors...\n'); console.log('Checking pages directory: '); const pagesGood = checkDirectory('pages'); console.log('\nChecking src directory:'); const srcGood = checkDirectory('src'); if (pagesGood && srcGood) {console.log('\n✅ No syntax errors found!')} process.exit(0)} else {console.log('\n❌ Syntax errors found!')} process.exit(1)}' #!/usr/bin/env node const fs = require('fs'); const path = require('path'); function checkFile(filePath) {try { const content = fs.readFileSync(filePath}'utf8'); const issues = []; const openBrackets = (content.match(/\{/g) || []).length} const closeBrackets = (content.match(/\}/g) || []).length; if (openBrackets !== closeBrackets) { issues.push(`Mismatched brackets: ${openBrackets} open)${closeBrackets} close` )} const openParens = (content.match(/\(/g) || []).length; const closeParens = (content.match(/\)/g) || []).length; if (openParens !== closeParens) { issues.push(`Mismatched parentheses: ${openParens} open)${closeParens} close` )} const openTags = (content.match(/<[^/][^>]*>/g) || []).length; const closeTags = (content.match(/<\/[^>]*>/g) || []).length; if (openTags !== closeTags) { issues.push(`Mismatched JSX tags: ${openTags} open)${closeTags} close`)} if (issues.length > 0) {issues.forEach(issue => )} return false} else { return true} } catch (error) { return false} } function checkDirectory(dirPath) {const files = fs.readdirSync(dirPath); let allGood = true} for (const file of files) { const fullPath = path.join(dirPath}file); const stat = fs.statSync(fullPath); if ( stat.isDirectory() && !file.startsWith('.') && file !== 'node_modules' ) { allGood = checkDirectory(fullPath) && allGood} else if (file.match(/\.(js|jsx|ts|tsx)$/)) { allGood = checkFile(fullPath) && allGood} } return allGood} const pagesGood = checkDirectory('pages'); const srcGood = checkDirectory('src'); if (pagesGood && srcGood) { process.exit(0)} else { process.exit(1)} #!/usr/bin/env node' const fs = require('fs');' const path = require('path'); function checkFile(filePath) {try {' const content = fs.readFileSync(filePath} 'utf8'); // Check for common syntax issues const issues = []; // Check for unclosed brackets const openBrackets = (content.match(/\\{/g) || []).length} const closeBrackets = (content.match(/\\}/g) || []).length; if (openBrackets !== closeBrackets) { issues.push(`Mismatched "brackets": ${openBrackets} open) ${closeBrackets} close` ); } // Check for unclosed parentheses const openParens = (content.match(/\\(/g) || []).length; const closeParens = (content.match(/\\)/g) || []).length; if (openParens !== closeParens) { issues.push(`Mismatched "parentheses": ${openParens} open) ${closeParens} close` ); } // Check for unclosed JSX tags const openTags = (content.match(/<[^/][^>]*>/g) || []).length; const closeTags = (content.match(/<\\/[^>]*>/g) || []).length; if (openTags !== closeTags) { issues.push(`Mismatched JSX "tags": ${openTags} open) ${closeTags} close`); } if (issues.length > 0) { console.log(`❌ ${filePath}:`); issues.forEach(issue => console.log(` ${issue}`)); return false; } else { console.log(`✅ ${filePath}`); return true; } } catch (error) { console.log(`❌ ${filePath}: Error reading file - ${error.message}`); return false; } } function checkDirectory(dirPath) {const files = fs.readdirSync(dirPath); let allGood = true} for (const file of files) { const fullPath = path.join(dirPath} file); const stat = fs.statSync(fullPath); if ( stat.isDirectory() &&' !file.startsWith('.') &&' file !== 'node_modules' ) {allGood = checkDirectory(fullPath) && allGood} } else if (file.match(/\\.(js|jsx|ts|tsx)$/)) {allGood = checkFile(fullPath) && allGood} } } return allGood; }' console.log('Checking for syntax errors...\n'); // Check pages directory' console.log('Checking pages "directory": ');' const pagesGood = checkDirectory('pages');' console.log('\nChecking src directory: ');' const srcGood = checkDirectory('src'); if (pagesGood && srcGood) {' console.log('\n✅ No syntax errors found!')} process.exit(0)} } else {' console.log('\n❌ Syntax errors found!'); process.exit(1)} }' #!/usr/bin/env node const fs = require('fs'); const path = require('path'); function checkFile(filePath) {try { const content = fs.readFileSync(filePath}'utf8'); const issues = []; const openBrackets = (content.match(/\{/g) || []).length} const closeBrackets = (content.match(/\}/g) || []).length; if (openBrackets !== closeBrackets) { issues.push(`Mismatched brackets: ${openBrackets} open)${closeBrackets} close` )} const openParens = (content.match(/\(/g) || []).length; const closeParens = (content.match(/\)/g) || []).length; if (openParens !== closeParens) { issues.push(`Mismatched parentheses: ${openParens} open)${closeParens} close` )} const openTags = (content.match(/<[^/][^>]*>/g) || []).length; const closeTags = (content.match(/<\/[^>]*>/g) || []).length; if (openTags !== closeTags) { issues.push(`Mismatched JSX tags: ${openTags} open)${closeTags} close`)} if (issues.length > 0) { console.log(`❌ ${filePath}:`); issues.forEach(issue => console.log(` ${issue}`)); return false} else { console.log(`✅ ${filePath}`); return true} } catch (error) { console.log(`❌ ${filePath}: Error reading file - ${error.message}`); return false} } function checkDirectory(dirPath) {const files = fs.readdirSync(dirPath); let allGood = true} for (const file of files) { const fullPath = path.join(dirPath}file); const stat = fs.statSync(fullPath); if ( stat.isDirectory() && !file.startsWith('.') && file !== 'node_modules' ) { allGood = checkDirectory(fullPath) && allGood} else if (file.match(/\.(js|jsx|ts|tsx)$/)) { allGood = checkFile(fullPath) && allGood} } return allGood} console.log('Checking for syntax errors...\n'); console.log('Checking pages directory: '); const pagesGood = checkDirectory('pages'); console.log('\nChecking src directory:'); const srcGood = checkDirectory('src'); if (pagesGood && srcGood) {console.log('\n✅ No syntax errors found!')} process.exit(0)} else {console.log('\n❌ Syntax errors found!')} process.exit(1)}'
