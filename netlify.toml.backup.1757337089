[build]
<<<<<<< HEAD
  command = "mise install --yes && python -m pip install -r requirements.txt && npm ci --no-audit --no-fund && npm run build"
=======
  # Install Python dependencies first, then Node.js dependencies and build
  # Explicitly use system Python to avoid mise shim issues
  command = "/usr/bin/python3 -m pip install -r requirements.txt && npm install --no-audit --no-fund && npm run build"
>>>>>>> origin/cursor/fix-netlify-build-and-merge-to-main-0376
  publish = "dist"
=======
  # Use prebuilt static site in 'out' to avoid broken build step
  command = "echo Using prebuilt out directory"
  publish = "out"
  command_timeout = "30m"

[build.environment]
  NODE_VERSION = "20"
  PYTHON_VERSION = "3.11"
  NETLIFY_SKIP_INSTALL = "true"
<<<<<<< HEAD
  MISE_PYTHON_COMPILE = "false"


=======
  # Disable mise shims entirely during Netlify builds
  MISE_DISABLE = "1"
  MISE_SHIMS = "0"
  # Force mise to use system Python even during Netlify's dependency stage (defensive)
  MISE_TOOL_VERSIONS__python = "system"
  # Skip running install scripts (avoids Electron postinstall and similar)
  NPM_CONFIG_IGNORE_SCRIPTS = "true"
  ELECTRON_SKIP_BINARY_DOWNLOAD = "1"
>>>>>>> origin/cursor/fix-netlify-build-and-merge-to-main-0376


[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
