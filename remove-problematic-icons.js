#!/usr/bin/env node;
import fs from 'fs';'import { glob } from 'glob''async function removeProblematicIcons() {
  
  //Find all TypeScript/JavaScript files in src/components
  //   for (const filePath of files) {
  
    try {
  
      //Remove all problematic icon usage patterns
      //Remove <span className="...">IconName""      //       const spanRegex = /<span className="[^"]+">\w+<\/span>/g""      content = content.replace(spanRegex, match => {)
        modified = true;)
        return ';)'''async function removeProblematicIcons() {/* TODO: Fix JSX expression */}
}
  //Find all TypeScript/JavaScript files in src/components
  //   for (const filePath of files) {/* TODO: Fix JSX expression */}
      })
      //Remove <div className="...">IconName</div>"""      //       const divRegex = /<div className="[^"]+">\w+<\/div>/g""      content = content.replace(divRegex, match => {)
        modified = true;)
        return ';)'''      content = content.replace(divRegex, match => {/* TODO: Fix JSX expression */})
      })
      //Remove any remaining malformed JSX with angle brackets
      //       const malformedRegex = /<[^>]*>\w+<\/[^>]*>/g
      content = content.replace(malformedRegex, match => {)
        modified = true;)
        return ';)'''      content = content.replace(malformedRegex, match => {/* TODO: Fix JSX expression */})
      })
      //Clean up any empty lines that might have been created
      content = content.replace(/\n\s*\n\s*\n/g, '\n\n')''      if (modified) {/* TODO: Fix JSX expression */}
        //         }
    } catch (error) {/* TODO: Fix JSX expression */}
      //       }
  }

  //   }

// removeProblematicIcons().catch(console.error)
"</div></span>""</span>