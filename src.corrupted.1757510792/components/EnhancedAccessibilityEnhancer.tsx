import React,{ useEffect,useState } from 'react'; interface AccessibilitySettings { highContrast: boolean; largeText: boolean; focusIndicator: boolean; keyboardNavigation: boolean; reducedMotion: boolean; screenReader: boolean; zoomLevel: number} export default function EnhancedAccessibilityEnhancer({ enabled = true }: { enabled?: boolean }) { const [settings,setSettings] = useState<AccessibilitySettings>({ highContrast: false,largeText: false,focusIndicator: true,keyboardNavigation: true,reducedMotion: false,screenReader: false,zoomLevel: 100 }); const applySettings = (newSettings: AccessibilitySettings) => { const root = document.documentElement; if (newSettings.highContrast) { root.classList.add('high-contrast')} else { root.classList.remove('high-contrast')} if (newSettings.largeText) { root.classList.add('large-text')} else { root.classList.remove('large-text')} if (newSettings.reducedMotion) { root.classList.add('reduced-motion')} else { root.classList.remove('reduced-motion')} root.style.fontSize = `${newSettings.zoomLevel}%`}; useEffect(() => { if (!enabled) return; applySettings(settings); localStorage.setItem('accessibility-settings',JSON.stringify(settings))},[settings,enabled]); useEffect(() => { if (!enabled) return; const savedSettings = localStorage.getItem('accessibility-settings'); if (savedSettings) { const parsedSettings = JSON.parse(savedSettings); setSettings(parsedSettings); applySettings(parsedSettings)} },[enabled]); if (!enabled) return null; return null}