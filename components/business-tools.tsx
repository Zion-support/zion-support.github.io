import React from 'react'
import Head from 'next/
import Card from '../components/ui/
import Button from '../components/ui/
          website: any
                    className={`card-hover border-gradient-blue group ${tool.popular ? 'ring-2 ring-cursor-blue/