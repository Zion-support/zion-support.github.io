import React, { useState } from 'react'
import Head from 'next/
import Link from 'next/
import Layout from '../components/layout/
import { ultimateFuturisticServices2025 } from '../data/
import Layout from '../components/layout/
import { ultimateFuturisticServices2025 } from '../data/
import Layout from '../components / layout /
import { ultimateFuturisticServices2025 } from '../data /
import Layout from '../components/layout/
import { ultimateFuturisticServices2025 } from '../data/
  website: any
  { number: any
  { number: any
          parseFloat(a && a.price.replace(/[^0-9.]/
          parseFloat(b && b.price.replace(/[^0-9.]/
          parseFloat(b && b.price.replace(/[^0-9.]/
          parseFloat(a && a.price.replace(/[^0-9.]/
        return b && b.popular ? 1 : -1;    }        return parseFloat(a && a.price.replace(/[^0-9.]/g, '') - parseFloat(b && b.price.replace(/[^0-9.]/
        return parseFloat(b && b.price.replace(/[^0-9.]/g, '') - parseFloat(a && a.price.replace(/[^0-9.]/
          content='https: any
          href='https: any