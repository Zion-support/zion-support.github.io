import React from 'react';
import ServiceLanding from '../components/sections/ServiceLanding';

export default function AiGuardrailsSuite() {
	return (
		<ServiceLanding
			title="AI Guardrails Suite"
			description="Add input/output filtering, PII redaction, jailbreak resistance, and policy enforcement to your LLM apps."
			subtitle="Ship safer AI systems"
			pricePerMonthUSD={129}
			implementationWeeks="3-7 days"
			roiNote="Reduce incidents and manual reviews with automated safety policies"
			features={[
				"Toxicity, prompt injection, and jailbreak detection",
				"PII detection and redaction",
				"Policy and content filters (regex + ML)",
				"Eval harness and regression tests",
				"Observability and red-team tooling",
				"SDKs and middleware for popular frameworks"
			]}
			industries={["SaaS", "Healthcare", "Fintech", "Government"]}
			canonical="https://ziontechgroup.com/ai-guardrails"
		/>
	);
}