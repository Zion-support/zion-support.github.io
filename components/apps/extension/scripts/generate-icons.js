/usr/bin/
const svgPath = resolve(root, '../assets/
const outDir = resolve(root, '../public/
/usr/bin/
const svgPath = resolve(root, '../assets/
const outDir = resolve(root, '../public/
/usr / bin /
const svg_path = resolve (root, '../assets /
const out_dir = resolve (root, '../public /