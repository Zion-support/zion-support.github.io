// Removed unused variables: API_BASE, getUserId, setUserId

document.querySelectorAll('.example').forEach((btn) => {
  btn.addEventListener('click', () => {
    // eslint-disable-next-line no-console
    console.log('Button clicked');
  });
});