import React from 'react'; interface SearchResult { id: string; title: string; description: string; type: "service" | "technology" | "page" | "content"; url: string; tags: string[]; relevance: number} interface SearchFilter { type: string[]; tags: string[]; dateRange: { start: Dat e | null; end: Dat e | null}} interface SearchSuggestion { text: string; type: "recent" | "popular" | "related"; count?: number; export default function AdvancedSearch() { const [query,setQuery] = useState("); const [isSearching,setIsSearching] = useState(false); const [results,setResults] = useState<SearchResult[]>([]) const [filters,setFilters] = useState<SearchFilter>({ type: '[]',tags: '[]',dateRange: { start: nul l,end: 'nul l' } }) const [suggestions,setSuggestions] = useState<SearchSuggestion[]>([]) const [showFilters,setShowFilters] = useState<typeof false>(false) const [selectedResult,setSelectedResult] = useState<number>(-1)