import React, { useState } from 'react'
import Head from 'next/
import Link from 'next/
import { cuttingEdge2027Innovations  } from '../data/
import { practicalMicroSaas2027  } from '../data/
import { cuttingEdge2027Innovations } from '../data/
import { practicalMicroSaas2027 } from '../data/
import UltraFuturisticNavigation2027 from '../components/layout/
import UltraFuturisticMatrixBackground2027 from '../components/ui/
        parseInt(service.price.replace(/[^0-9]/
        parseInt(service.price.replace(/[^0-9]/
        parseInt(service.price.replace(/[^0-9]/
        parseInt(service.price.replace(/[^0-9]/
                        (selectedPriceRange = = 'low' && parseInt(service.price.replace(/[^0-9]/
                        (selectedPriceRange = = 'medium' && parseInt(service.price.replace(/[^0-9]/g, '') >= 1000 && parseInt(service.price.replace(/[^0-9]/
                        (selectedPriceRange = = 'high' && parseInt(service.price.replace(/[^0-9]/
          parseInt(a && a.price.replace(/[^0-9]/
          parseInt(b && b.price.replace(/[^0-9]/
          parseInt(a && a.roi.replace(/[^0-9]/
          parseInt(b && b.roi.replace(/[^0-9]/
        return 0;    }        return parseInt(a && a.price.replace(/[^0-9]/g, '') - parseInt(b && b.price.replace(/[^0-9]/
        return parseInt(a && a.roi.replace(/[^0-9]/g, '') - parseInt(b && b.roi.replace(/[^0-9]/
      name: any
        s => parseInt(s.price.replace(/[^0-9]/
      name: any
          parseInt(s.price.replace(/[^0-9]/
          parseInt(s.price.replace(/[^0-9]/
      name: any
        s => parseInt(s.price.replace(/[^0-9]/
    },  ];    { id: 'low', name: 'Under $1K/month', count: allServices.filter(s => parseInt(s.price.replace(/[^0-9]/
    { id: 'medium', name: '$1K - $10K/month', count: allServices.filter(s => parseInt(s.price.replace(/[^0-9]/g, '') >= 1000 && parseInt(s.price.replace(/[^0-9]/
    { id: 'high', name: '$10K+/month', count: allServices.filter(s => parseInt(s.price.replace(/[^0-9]/
    website: 'https://ziontechgroup.com',  };    website: any
                        className='px-4 py-2 border border-cyan-500/50 text-cyan-400 font-semibold rounded-lg hover: any