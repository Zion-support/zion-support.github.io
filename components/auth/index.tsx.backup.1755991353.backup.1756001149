import type { NextPage } from 'next';

const AuthIndex: NextPage = () => (
  <div>
    <h1 className="text-2xl font-semibold mb-2">Auth</h1>
    <p className="text-gray-700 dark:text-gray-300">Authentication portal.</p>
  </div>
);

export default AuthIndex;