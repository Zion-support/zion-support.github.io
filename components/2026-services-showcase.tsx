import React, { useState, useMemo } from 'react'
import Head from 'next/
import Head from 'next /
 from 'lucide-react';import UltraAdvancedFuturisticBackground from '../components/ui/
import UltraAdvancedFuturisticBackground from '../components/ui/
import UltraAdvancedNavigation from '../components/layout/
import { revolutionary2026Services } from '../data/
import { emergingTech2026Services } from '../data/
import { comprehensiveIT2026Services } from '../data/
            parse_float (a.price.replace (/[^0 - 9.]/
            parse_float (b.price.replace (/[^0 - 9.]/
            parseFloat(a && a.price.replace(/[^0-9.]/
            parseFloat(b && b.price.replace(/[^0-9.]/
          );          return parseFloat(a && a.price.replace(/[^0-9.]/g, '') - parseFloat(b && b.price.replace(/[^0-9.]/
          return parseFloat(a.price.replace(/[^0-9.]/g, '') - parseFloat(b.price.replace(/[^0-9.]/
            parse_float (a.price.replace (/[^0 - 9.]/
            parse_float (b.price.replace (/[^0 - 9.]/g, ''));          return parse_float (a.price.replace (/[^0 - 9.]/g, '') - parse_float (b.price.replace (/[^0 - 9.]/
    website: any
                    className='w-full pl-10 pr-4 py-3 bg-gray-700/50 border border-gray-600/
                  className='px-8 py-4 border border-cyan-500/30 text-cyan-300 font-semibold rounded-xl hover: any
                  className='px-8 py-4 border border-cyan-500/30 text-cyan-300 font-semibold rounded-xl hover: any
                  className='px - 8 py - 4 border border - cyan - 500 / 30 text - cyan - 300 font - semibold rounded - xl hover: any