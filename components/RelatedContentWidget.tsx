"use client";
import React from 'react';

type Post = { title: string; slug: string; excerpt?: string; image?: string };

export default function RelatedContentWidget({ title, posts }: { title: string; posts: Post[] }) {
  return null;
}

