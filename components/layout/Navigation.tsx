import React from 'react';

const Navigation: React.FC = () => {
  // Temporarily simplified to avoid build issues
  return <div className="sr-only">Navigation temporarily disabled</div>;
};

export default Navigation;
