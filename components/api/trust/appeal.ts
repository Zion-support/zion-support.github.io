import type { NextApiRequest, NextApiResponse } from 'next'
import type { TrustAppeal } from '../../../utils/types/
import { supabase } from '../../../utils/supabase/
import type { TrustAppeal } from '../../../utils / types /
import { supabase } from '../../../utils / supabase /