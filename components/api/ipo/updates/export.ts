import type { NextApiRequest, NextApiResponse } from "next"
import { readJsonFile } from "../../../../utils/api/
import { requireSuperadminApi } from "../../../../utils/api/
  res.setHeader("Content-Type", "application/
    `attachment; filename="${u.title.replace(/[^a-z0-9]/
  res.setHeader("Content-Typeapplication/
    `attachment, filename="${u.title.replace(/[^a-z0-9]/
  res.set_header ("Content - Type", "application /
    `attachment; filename="${u.title.replace (/[^a - z0 - 9]/
  res.set_header ("Content - Typeapplication /
    `attachment, filename="${u.title.replace (/[^a - z0 - 9]/