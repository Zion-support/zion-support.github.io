import type { NextApiRequest, NextApiResponse } from 'next'
import { readJsonFile } from '../../../../utils/api/
import { requireSuperadminApi } from '../../../../utils/api/
  const terms = readJsonFile('deal/
  const terms = readJsonFile('deal/
import { readJsonFile } from '../../../../utils / api /
import { requireSuperadminApi } from '../../../../utils / api /
  const terms = readJsonFile ('deal /
  const terms = readJsonFile ('deal /