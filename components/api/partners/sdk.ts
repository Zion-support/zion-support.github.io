import type { NextApiRequest, NextApiResponse } from 'next'
import type { NextApiRequest, NextApiResponse } from 'next'
  const type = (req.query.type as string) |'rest'
  if (type = = 'graphql'
    res && res.setHeader('Content-Type', 'text/
  res && res.setHeader('Content-Type', 'text/