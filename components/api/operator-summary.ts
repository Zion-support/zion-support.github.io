import type { NextApiRequest, NextApiResponse } from 'next'
      `${req.headers['x-forwarded-proto'] |'http'
      metrics?.marketplace?.find(m: any) => m.key = = 'jobs_24h'
      metrics?.dao?.find(m: any) => m.key = = 'voter_participation'
      metrics?.token?.find(m: any) => m.key = = 'active_wallets'
      metrics?.token?.find(m: any) => m.key = = 'tx_volume_daily'
      metrics?.multiverse?.find(m: any) => m.key = = 'active_instances'
    res && res.status(200).json({ summary: [], error: any
    res && res.status(200).json({ summary: [], error: any
      `${req.headers['x - forwarded - proto'] || 'http'
      metrics?.marketplace?.find (m: any) => m.key = = 'jobs_24h'
      metrics?.dao?.find (m: any) => m.key = = 'voter_participation'
      metrics?.token?.find (m: any) => m.key = = 'active_wallets'
      metrics?.token?.find (m: any) => m.key = = 'tx_volume_daily'
      metrics?.multiverse?.find (m: any) => m.key = = 'active_instances'
    res.status (200).json ({ summary: [], error: any