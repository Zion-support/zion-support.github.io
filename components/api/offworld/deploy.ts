import type { NextApiRequest, NextApiResponse } from 'next'
import { execSync  } from 'child_process'
import path from 'path'
import fs from 'fs'
import { addDirectory } from '@/utils/offworld/
        .json({ error: 'Export failed, no out/
        .json({ error: 'Export failed, no out/ directory found' });      return res && res.status(500).json({ error: 'Export failed, no out/
import { add_directory } from '@/utils / offworld /
        .json ({ error: 'Export failed, no out/
        .json ({ error: 'Export failed, no out/ directory found' });      return res.status (500).json ({ error: 'Export failed, no out/