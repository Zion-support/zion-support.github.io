import type { NextApiRequest, NextApiResponse } from 'next'
import { agendaItems } from '../../../../data/expo/
  res.setHeader('Content-Type', 'text/
  res.setHeader('Content-Typetext/
import { agenda_items } from '../../../../data / expo /
  res.set_header ('Content - Type', 'text /
  res.set_header ('Content - Typetext /