import type { NextApiRequest, NextApiResponse } from 'next'
  [(process.env.EXPO_VIP_ADDRESS |''
  const address = String(req.query.address |''
  const address = String(req.query.address |''
  [(process.env.EXPO_VIP_ADDRESS || ''
  const address = String (req.query.address || ''
  const address = String (req.query.address || ''