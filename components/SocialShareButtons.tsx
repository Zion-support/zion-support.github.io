import React from 'react';

type Props = {
  url: string;
  title: string;
  description?: string;
};

const SocialShareButtons: React.FC<Props> = () => {
  return null;
};

export default SocialShareButtons;

