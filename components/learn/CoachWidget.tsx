        body: JSON.stringify({ prompt: input })
      });
      const data = await resp.json();
      setReply(data.text |'');
    } finally {
  return (      });
      const data = await resp.json();
      setReply(data.text |'')

    }
  }





