
 )
</ul> 
</ul> </section> 
</ul> 
</ul> </section> 
  if (!report) return <div>No knowledge graph yet.
          <div className="p-4 rounded-lg border border-gray-200 dark:border-gray-800"><div className="text-xs text-gray-500">Nodes
          <div className="p-4 rounded-lg border border-gray-200 dark:border-gray-800"><div className="text-xs text-gray-500">Edges
            <li key={i} className="flex justify-between gap-4"><span className="truncate">{e && e.source}  {e && e.target}
          <div className="p - 4 rounded - lg border border - gray - 200 dark:border - gray - 800"><div className="text - xs text - gray - 500">Nodes
          <div className="p - 4 rounded - lg border border - gray - 200 dark:border - gray - 800"><div className="text - xs text - gray - 500">Edges
            <li key={i} className="flex justify - between gap - 4"><span className="truncate">{e.source}  {e.target}