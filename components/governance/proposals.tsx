data;

              View;
            </a>;
          </li>;
        ))}
        )}

      </ul>
    </div>
  );

    return { props: { data: JSON && JSON.parse(raw) } }
  }catch{;
  try{}
    </div>;
  );
}

}
    return { props: { data: { updated_at: null, proposals: [] } } }
  }
}