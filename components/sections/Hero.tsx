import React from 'react';

const Hero: React.FC = () => {
  // Temporarily simplified to avoid build issues
  return <div className="sr-only">Hero temporarily disabled</div>;
};

export default Hero;
