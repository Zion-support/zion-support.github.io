import React, { useState } from 'react'
import Head from 'next/
import Link from 'next/
import UltraFuturisticNavigation2029 from '../components/layout/
import UltraFuturisticFooter2029 from '../components/layout/
import { cuttingEdge2028MicroSaas  } from '../data/
import { practicalBusinessSolutions2028  } from '../data/
import { advancedAIAutonomousServices2028  } from '../data/
import { quantumSpaceInnovations2028 } from '../data/
import UltraFuturisticNavigation2029 from '../components/layout/
import UltraFuturisticFooter2029 from '../components/layout/
import { cuttingEdge2028MicroSaas } from '../data/
import { practicalBusinessSolutions2028 } from '../data/
import { advancedAIAutonomousServices2028 } from '../data/
import { quantumSpaceInnovations2028 } from '../data/
        parseFloat(service.price.replace(/[$]/
        parseFloat(service.price.replace(/[$]/
        parseFloat(service.price.replace(/[$]/
        parseFloat(service.price.replace(/[$]/
        parseFloat(service.price.replace(/[$]/
        parseFloat(service.price.replace(/[$]/
      (selectedPriceRange = = 'low' && parseFloat(service.price.replace(/[$]/
      (selectedPriceRange = = 'medium' && parseFloat(service.price.replace(/[$]/g, '') >= 5000 && parseFloat(service.price.replace(/[$]/
      (selectedPriceRange = = 'high' && parseFloat(service.price.replace(/[$]/g, '') >= 15000 && parseFloat(service.price.replace(/[$]/
      (selectedPriceRange = = 'premium' && parseFloat(service.price.replace(/[$]/
import UltraFuturisticNavigation2029 from '../components / layout /
import UltraFuturisticFooter2029 from '../components / layout /
import { cuttingEdge2028MicroSaas } from '../data /
import { practicalBusinessSolutions2028 } from '../data /
import { advancedAIAutonomousServices2028 } from '../data /
import { quantumSpaceInnovations2028 } from '../data /
        parse_float (service.price.replace (/[$]/
        parse_float (service.price.replace (/[$]/
        parse_float (service.price.replace (/[$]/
        parse_float (service.price.replace (/[$]/
        parse_float (service.price.replace (/[$]/
        parse_float (service.price.replace (/[$]/
      (selectedPriceRange = = 'low' && parse_float (service.price.replace (/[$]/
      (selectedPriceRange = = 'medium' && parse_float (service.price.replace (/[$]/g, '') >= 5000 && parse_float (service.price.replace (/[$]/
      (selectedPriceRange = = 'high' && parse_float (service.price.replace (/[$]/g, '') >= 15000 && parse_float (service.price.replace (/[$]/
      (selectedPriceRange = = 'premium' && parse_float (service.price.replace (/[$]/
    website: 'https://ziontechgroup.com',  };    website: any
        parseFloat(service && service.price.replace(/[$]/
        parseFloat(service && service.price.replace(/[$]/
        parseFloat(service && service.price.replace(/[$]/
        parseFloat(service && service.price.replace(/[$]/
        parseFloat(service && service.price.replace(/[$]/
        parseFloat(service && service.price.replace(/[$]/
      (selectedPriceRange = = 'low' && parseFloat(service && service.price.replace(/[$]/
      (selectedPriceRange = = 'medium' && parseFloat(service && service.price.replace(/[$]/g, '') >= 5000 && parseFloat(service && service.price.replace(/[$]/
      (selectedPriceRange = = 'high' && parseFloat(service && service.price.replace(/[$]/g, '') >= 15000 && parseFloat(service && service.price.replace(/[$]/
      (selectedPriceRange = = 'premium' && parseFloat(service && service.price.replace(/[$]/
    website: any
    website: 'https://ziontechgroup && ziontechgroup.com',  };    website: any
          href='https: any
          content='https: any
                className='px-8 py-4 border-2 border-cyan-500/50 text-cyan-400 font-bold rounded-xl hover: any
                className='px-8 py-4 border-2 border-cyan-500/50 text-cyan-400 font-bold rounded-xl hover: any
                          className='px-2 py-1 bg-gray-700/50 text-cyan-400 text-xs rounded-lg border border-cyan-500/
                        