 trace: any
reporter: [ ['list'], ['json'
  outputFile: any
  command: npm run dev', url: http://localhost:3000', reuseExistingServer: !process.env.CI, // Reuse server locally, start fresh in CI timeout: 120 * 1000, // 2 minutes timeout for server to start stdout: pipe', // Pipe stdout to the console' stderr: pipe', //
  //
  //
  //
 ] });    ['list'],    ['json', { outputFile: playwright-logs/test-results.json' }],    ['html', { outputFolder: any
import { defineConfig } from @playwright/
  testDir: any
    ['list'],    ['json', { outputFile: playwright-logs/test-results.json' }],    ['html', { outputFolder: any
    command: npm run dev',    url: any
    stdout: pipe', // Pipe stdout to the console'    stderr: pipe', //
  output_file: any
  command: npm run dev', url: http://localhost:3000', reuseExistingServer: !process.env.CI, // Reuse server locally, start fresh in CI timeout: 120 * 1000, // 2 minutes timeout for server to start stdout: pipe', // Pipe stdout to the console' stderr: pipe', //
  //
  //
  //
 ] });    ['list'],    ['json', { output_file: playwright - logs / test - results.json' }],    ['html', { output_folder: any
    command: npm run dev',    url: any
    stdout: pipe', // Pipe stdout to the console'    stderr: pipe', //
  //     name: chromium',  //
  //     name: firefox',  //
  //     name: webkit',  //
  //     name: webkit',  //