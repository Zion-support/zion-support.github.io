const OptimizedImage: React.FC<OptimizedImageProps> = ({
  src;
  alt;

  width = 800;
  height = 600;
  quality = 75,
}) => {
  return (
    <Image;
      src={src}
      alt={alt}
      width={width}
      height={height}
export default OptimizedImage;
