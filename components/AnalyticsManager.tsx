"use client";
import React from 'react';

// Minimal, safe stub to avoid build errors. Extend later if needed.
const AnalyticsManager: React.FC = () => {
  return null;
};

export default AnalyticsManager;