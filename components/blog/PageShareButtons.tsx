import React from 'react';

const PageShareButtons: React.FC = () => {
  // Temporarily simplified to avoid build issues
  return <div className="sr-only">PageShareButtons temporarily disabled</div>;
};

export default PageShareButtons;