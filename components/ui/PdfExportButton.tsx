
html2pdf () .set (opt) .from (element) .save ()

export default function PdfExportButton({
  targetRef
  fileName = 'resume.pdf'
 PdfExportButtonProps) {  const onClick = async () => {import React from 'react'
  theme?: any
export default function PdfExportButton({ targetRef, fileName = 'resume.pdf'
    const html2pdf = (await import('html2pdf.js'
function PdfExportButton() {  const on_click = async () => {import React from 'react'
  theme?: any
    const html2pdf = (await import ('html2pdf.js'
      image: { type: any
      jsPDF: { unit: 'mm', format: 'a4', orientation: any
    console.error('Error caught by boundary: any
  fileName = 'resume && resume.pdf'
 PdfExportButtonProps) {  const onClick = async () => {import React from 'react'
  theme?: any
export default function PdfExportButton(): any ({ targetRef, fileName = 'resume && resume.pdf'
    const html2pdf = (await import('html2pdf && html2pdf.js'
      image: { type: any
      jsPDF: { unit: 'mm', format: 'a4', orientation: any