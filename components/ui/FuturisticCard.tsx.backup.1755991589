import React from 'react';

export type FuturisticCardProps = {
  title: string;
  description: string;
      </div>
    </div>
  );

  if (href) {
    return (
      <a href={href} className="group block">{inner}</a>
    );
  }
  return inner;
}