import React from 'react';

const RevolutionaryServiceCard: React.FC = () => {
  // Temporarily simplified to avoid build issues
  return <div className="sr-only">RevolutionaryServiceCard temporarily disabled</div>;
};

export default RevolutionaryServiceCard;
