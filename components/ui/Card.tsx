import React from 'react';
interface CardProps {
  children: React.ReactNode,
  className?: string,
  style?: React.CSSProperties,
  onClick?: () => void,
  hover?: boolean
}

const Card: React.FC<CardProps> = ({
  children,
  className = $2;
  style,
  onClick,
  hover = $2;
  const hoverClasses = $2;
  const clickableClasses = $2;
  const classes = $2;
  return (
    <div
      className = $2;
export default Card,