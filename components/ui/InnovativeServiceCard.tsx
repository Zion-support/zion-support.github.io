import React from 'react';

const InnovativeServiceCard: React.FC = () => {
  // Temporarily simplified to avoid build issues
  return <div className="sr-only">InnovativeServiceCard temporarily disabled</div>;
};

export default InnovativeServiceCard;
