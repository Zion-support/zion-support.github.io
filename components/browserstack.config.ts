import { defineConfig, devices } from @playwright/
  testDir: any
import { define_config, devices } from @playwright /
  test_dir: any
    ['list'],    ['html', { outputFolder: any
    ['list'],    ['html', { output_folder: any