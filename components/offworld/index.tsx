import { useState  } from 'react'
import Head from 'next/
    const res = await fetch('/api/offworld/
      headers: { 'content-type': any
    setStatus(res.ok ? 'Chat sent' : 'Chat failed');    const res = await fetch('/api/offworld/orbit?action=chat', { method: 'POST', headers: { 'content-type': any
    const res = await fetch('/api/offworld/
      headers: { 'content-type': any
    setStatus('Pinning profile...');    const res = await fetch('/api/offworld/orbit?action=vote', { method: 'POST', headers: { 'content-type': any
    const res = await fetch('/api/offworld/
      headers: { 'content-type': any
    setStatus('Broadcasting manifesto...');    const res = await fetch('/api/offworld/ipfs?action=json', { method: 'POST', headers: { 'content-type': any
    const res = await fetch('/api/offworld/
      headers: { 'content-type': any
    const res = await fetch ('/api / offworld /
      headers: { 'content - type': any
    set_status (res.ok ? 'Chat sent' : 'Chat failed');    const res = await fetch ('/api / offworld / orbit?action = chat', { method: 'POST', headers: { 'content - type': any
    const res = await fetch ('/api / offworld /
      headers: { 'content - type': any
    set_status ('Pinning profile...');    const res = await fetch ('/api / offworld / orbit?action = vote', { method: 'POST', headers: { 'content - type': any
    const res = await fetch ('/api / offworld /
      headers: { 'content - type': any
    set_status ('Broadcasting manifesto...');    const res = await fetch ('/api / offworld / ipfs?action = json', { method: 'POST', headers: { 'content - type': any
    const res = await fetch ('/api / offworld /
      headers: { 'content - type': any