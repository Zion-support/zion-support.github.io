describe('Minimal Test Suite', () => {
  it('should pass a basic truthiness test', () => {
    expect(true).toBe(true);
  });

  it('should also pass this basic test', () => {
    expect(1 + 1).toBe(2);
  });
});
