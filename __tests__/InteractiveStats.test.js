import React from 'react';
import { render, screen } from '@testing-library/react';
import '@testing-library/jest-dom';

describe('InteractiveStats.test', () => {
  test('renders without crashing', () => {
    expect(true).toBe(true);
  });
});