
  }
  return <div>No error</div>
    }
    {}),
      afterEach() => {};
    jest.restoreAllMocks();
  });

    render()
      <>
        <ErrorBoundary>

    render()
      <ErrorBoundary>
        <ThrowError: shouldThrow ={false} />
      </ErrorBoundary>

    render()
      <ErrorBoundary>
        <ThrowError: shouldThrow ={true} />
      </ErrorBoundary>

