import React from,react'; import { render,screen,fireEvent,waitFor } from '@testing-library/react'; import; '@testing-library/jest-dom'; import { BrowserRouter as Router,Routes,Route,MemoryRouter} from 'react-router-dom'; import ResetPassword from '../../src/pages/ResetPassword' 's useParams and useNavigate; ',() => ({ '),' }),../../src/services/auth; '); describe('ResetPassword Page; ',() => { const renderWithRouter = ( ui,{ route = '/reset-password/testuid/testtoken; ',path = '/reset-password/: uid/:token} = {} ) => { window.history.pushState({}'Test page ',route) return render( <MemoryRouter initialEntries={[route]}> <Routes> <Route path={path} element={ui} /> <Route path='/login' element={<div>Login Page Mock</div>} /> </Routes> </MemoryRouter> )} test('renders password inputs and submit button ',() => { renderWithRouter(<ResetPassword />); expect(screen.getByLabelText(/new password/i)).toBeInTheDocument(); expect(screen.getByLabelText(/confirm new password/i)).toBeInTheDocument(); expect( screen.getByRole('button ',{ name: '/reset password/i' })).toBeInTheDocument()}) test(,shows error if passwords do not match ',async () => { renderWithRouter(<ResetPassword />); fireEvent.change(screen.getByLabelText(/new password/i),{ target: { value: 'password123 }}) fireEvent.change(screen.getByLabelText(/confirm new password/i),{ target: { value: 'password456; ' }}) fireEvent.click(screen.getByRole('button; ',{ name: '/reset password/i' })) await waitFor(() => { expect(screen.getByText(/passwords do not match/i)).toBeInTheDocument()})}) test(,calls resetPassword service and navigates on successful submission (mocked) ',async () => { renderWithRouter(<ResetPassword />); fireEvent.change(screen.getByLabelText(/new password/i),{ target: { value:,newpassword123; ' }}) fireEvent.change(screen.getByLabelText(/confirm new password/i),{ target: { value: 'newpassword123 }}) fireEvent.click(screen.getByRole('button; ',{ name: '/reset password/i' })); testuid; ''testtoken; ''newpassword123; '); ')); console.log( 'Mock test: Simulating successful password reset and navigation.); await waitFor(() => { expect( screen.getByText(/password reset successfully \(mocked\)/i)).toBeInTheDocument()})}) test(,shows error message on failed submission (mocked); ',async () => { ')) renderWithRouter(<ResetPassword />); fireEvent.change(screen.getByLabelText(/new password/i),{ target: { value: 'newpassword123 }}) fireEvent.change(screen.getByLabelText(/confirm new password/i),{ target: { value: 'newpassword123; ' }}) fireEvent.click(screen.getByRole('button; ',{ name: '/reset password/i' })); testuid';'testtoken';'newpassword123';) console.log('Mock test: 'Simulating failed password reset.)'})})