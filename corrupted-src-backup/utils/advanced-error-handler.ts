const errorHandler = {
  setReportingEnabled: (enabled: boolean) => {
    //     }
  handleError: (error: unknown) => {
const errorHandler = {}
    //     }
  handleErro
  r: (erro)
  r: unknown) => {}
    //     }}
export default errorHandler