import fs from '
import path from '
import { execSync } from '
if (!['node_modules', '.git', 'dist', 'build'
    } else if (file.endsWith('.tsx') || file.endsWith('.ts'
    execSync(`npx eslint "${filePath}" --fix --quiet`, { stdio: 'pipe'
console.log('Done fixing unused imports!'