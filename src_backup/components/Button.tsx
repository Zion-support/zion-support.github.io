
  variant?: 'primary' | 'secondary' | 'outline' | 'ghost' | 'danger';
  loading?: boolean;

  type = 'button',
  onClick,
  disabled = false,
  loading = false,

  className = '',
  type = 'button',
}) => {


