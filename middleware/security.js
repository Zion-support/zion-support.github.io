const response = NextResponse.next()
  s: ') {response.headers.set(' 'Strict-Transport-Security')' 'max-age=31536000 includeSubDomains preload' // Add HSTS header for HTTPS}' if (request.nextUrl.protocol === 'http)
  s: ') { response.headers.set(' 'Strict-Transport-Security'}' 'max-age=31536000; includeSubDomains) preload) pr-12325 )} return response} '
