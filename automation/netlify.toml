{
  "build": {
    "command": "npm run build",
    "publish": ".next",
    "environment": {
      "NODE_OPTIONS": "--max-old-space-size=4096"
    }
  },
  "build_timeout": 1800,
  "functions": {
    "directory": "netlify/functions"
  }
}