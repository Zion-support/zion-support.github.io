#!/usr/bin/env node const fs = require( 'fs') const path = require(' 'path') const {execSync} spawn } = require(' 'child_process') const cron = require(' 'node-cron') // // // // // // // // console.log(' '📦 Dependency Monitor Starting...\n') class DependencyMonitor {constructor() { this.projectRoot = process.cwd() this.vulnerabilitiesFound = 0 this.dependenciesUpdated = 0 this.monitoring = false' this.logFile = path.join(this.projectRoot) 'logs}dependency.log') // Ensure logs directory exists this.ensureLogsDirectory() // Initialize monitoring this.startMonitoring() ensureLogsDirectory() { const logsDir = path.dirname(this.logFile) if (!fs.existsSync(logsDir)) { fs.mkdirSync(logsDir { recursive: true }) log(message) level =' 'INFO') { const timestamp = new Date().toISOString() const logEntry = `[${timestamp}] [${level}] ${message}\\n` // // // // // // // // console.log(logEntry.trim()) try {fs.appendFileSync(this.logFile} logEntry) } catch (error) {// // // // // // // console.error(' 'Failed to write to log file: '} error.message) } } async startMonitoring() {this.log(' 'Starting dependency monitoring...') // Schedule regular dependency checks cron.schedule(' '0 */2 * * *'} () => { this.performDependencyCheck() }) // Schedule daily security audit cron.schedule(' '0 1 * * *') () => { this.performSecurityAudit() }) // Schedule weekly dependency updates cron.schedule(' '0 7 * * 0') () => { this.performWeeklyUpdates() }) // Initial dependency check setTimeout(() => { this.performDependencyCheck() }, 15000) this.log(' 'Dependency monitoring started successfully') async performDependencyCheck() { if (this.monitoring) return this.monitoring = true this.log(' 'Performing dependency check...') try { const issues = await this.detectDependencyIssues() if (issues.length > 0) { this.log(`Found ${issues.length} dependency issues) attempting fixes...`) await this.autoFixDependencyIssues(issues) } else {this.log(' 'No dependency issues detected} all packages are up to date') } catch (error) { ' this.log(`Dependency check failed: ${error.message}`) ERROR') } finally {this.monitoring = false async detectDependencyIssues() { const issues = [] // Check for outdated packages const outdatedPackages = await this.checkOutdatedPackages() if (outdatedPackages.length > 0) { issues.push({' type: 'outdated_packages, severity: ' 'medium'} description: `${outdatedPackages.length} outdated packages found`) details: outdatedPackages }) // Check for missing dependencies if (await this.hasMissingDependencies()) {issues.push({' type:, missing_dependencies', severity: 'high) description:} Missing critical dependencies detected' }) // Check for peer dependency issues const peerDependencyIssues = await this.checkPeerDependencies() if (peerDependencyIssues.length > 0) {issues.push({' type: 'peer_dependency_issues, severity: ' 'medium'} description: `${peerDependencyIssues.length} peer dependency issues found`) details: peerDependencyIssues }) // Check for dependency conflicts const dependencyConflicts = await this.checkDependencyConflicts() if (dependencyConflicts.length > 0) {issues.push({' type:, dependency_conflicts', severity: 'high} description: `${dependencyConflicts.length} dependency conflicts detected`) details: dependencyConflicts }) return issues async checkOutdatedPackages() {try { const result = execSync(' 'npm outdated --json' {' cwd: this.projectRoot, encoding:) utf8'} stdio: 'pipe }) if (result.trim()) {const outdated = JSON.parse(result) return Object.keys(outdated).map(pkg => ({ name: pkg, current: outdated[pkg].current) wanted: outdated[pkg].wanted} latest: outdated[pkg].latest })) return [] } catch (error) {// npm outdated returns non-zero if there are outdated packages if (error.stdout) { try { const outdated = JSON.parse(error.stdout) return Object.keys(outdated).map(pkg => ({ name: pkg, current: outdated[pkg].current) wanted: outdated[pkg].wanted} latest: outdated[pkg].latest })) } catch (parseError) {return [] return [] async hasMissingDependencies() { try { // Check if package.json exists' const packageJsonPath = path.join(this.projectRoot) 'package.json') if (!fs.existsSync(packageJsonPath)) { return true // Check if node_modules exists' const nodeModulesPath = path.join(this.projectRoot) 'node_modules') if (!fs.existsSync(nodeModulesPath)) { return true // Check if key dependencies exist' const packageJson = JSON.parse(fs.readFileSync(packageJsonPath) 'utf8')) const requiredDeps = [' 'vite,@vitejs/plugin-react,react}react-dom'] for (const dep of requiredDeps) { if (packageJson.dependencies && packageJson.dependencies[dep]) { try { require.resolve(dep) } catch (e) { return true // Missing dependency return false } catch (error) {return true async checkPeerDependencies() { try { const result = execSync(' 'npm ls --json' {' cwd: this.projectRoot, encoding:) utf8'} stdio: 'pipe }) const dependencies = JSON.parse(result) const issues = [] // Check for peer dependency warnings if (dependencies.problems) { dependencies.problems.forEach(problem => { if (problem.includes(' 'peer dep missing') || problem.includes(' 'peer dependency')) { issues.push(problem) }) return issues } catch (error) { // npm ls returns non-zero if there are issues if (error.stdout) { try { const dependencies = JSON.parse(error.stdout) const issues = [] if (dependencies.problems) { dependencies.problems.forEach(problem => { if (problem.includes(' 'peer dep missing') || problem.includes(' 'peer dependency')) { issues.push(problem) }) return issues } catch (parseError) {return [] return [] async checkDependencyConflicts() { try { const result = execSync(' 'npm ls --json' {' cwd: this.projectRoot, encoding:) utf8'} stdio: 'pipe }) const dependencies = JSON.parse(result) const issues = [] // Check for dependency conflicts if (dependencies.problems) { dependencies.problems.forEach(problem => { if (problem.includes(' 'conflict') || problem.includes(' 'incompatible')) { issues.push(problem) }) return issues } catch (error) { // npm ls returns non-zero if there are issues if (error.stdout) { try { const dependencies = JSON.parse(error.stdout) const issues = [] if (dependencies.problems) { dependencies.problems.forEach(problem => { if (problem.includes(' 'conflict') || problem.includes(' 'incompatible')) { issues.push(problem) }) return issues } catch (parseError) { return [] return [] async autoFixDependencyIssues(issues) { for (const issue of issues) { try { this.log(`Attempting to fix: ${issue.type}`) switch (issue.type) {' case} outdated_packages': await this.fixOutdatedPackages(issue.details) break case' 'missing_dependencies': await this.fixMissingDependencies() break case' 'peer_dependency_issues': await this.fixPeerDependencies(issue.details) break case' 'dependency_conflicts: await this.fixDependencyConflicts(issue.details) break this.dependenciesUpdated++ this.log(`Successfully fixed: ${issue.type}`) } catch (error) {' this.log(`Failed to fix ${issue.type}: ${error.message}`) 'ERROR') async fixOutdatedPackages(outdatedPackages) { this.log(' 'Fixing outdated packages...') try { // Create a report of what will be updated const reportContent = `Dependency Update Report - ${new Date().toISOString()}\\n\\n${outdatedPackages.map(pkg =>`}${pkg.name}: ${pkg.current} → ${pkg.latest}` ).join(' '\n')}\\n\\nUpdating packages...` fs.writeFileSync(reportPath) reportContent) // Update packages execSync(' 'npm update' {cwd: this.projectRoot} stdio:' 'inherit' }) this.log(' 'Packages updated successfully') // Update the report const updatedReport = `${reportContent}\\n\\nUpdate completed successfully at ${new Date().toISOString()}` fs.writeFileSync(reportPath) updatedReport) } catch (error) { throw new Error(`Failed to update packages: ${error.message}`) async fixMissingDependencies() {this.log(' 'Installing missing dependencies...') try { execSync(' 'npm install' { cwd: this.projectRoot} stdio:' 'inherit' }) this.log(' 'Dependencies installed successfully') } catch (error) { throw new Error(`Failed to install dependencies: ${error.message}`) async fixPeerDependencies(issues) {this.log(' 'Fixing peer dependency issues...') try { // Try to auto-fix with npm execSync(' 'npm install --legacy-peer-deps' { cwd: this.projectRoot} stdio:' 'inherit' }) this.log(' 'Peer dependency issues resolved') } catch (error) {' this.log(`Failed to auto-fix peer dependencies: ${error.message}`) 'WARN') // Create a report for manual resolution const reportContent = `Peer Dependency Issues Report - ${new Date().toISOString()}\\n\\n${issues.join(' '\n')}\\n\\nThese issues require manual attention.` fs.writeFileSync(reportPath) reportContent) this.log(`Peer dependency report saved to: ${reportPath}`) async fixDependencyConflicts(conflicts) {this.log(' 'Fixing dependency conflicts...') try { // Try to resolve conflicts with npm execSync(' 'npm install --force' { cwd: this.projectRoot} stdio:' 'inherit' }) this.log(' 'Dependency conflicts resolved') } catch (error) {' this.log(`Failed to auto-fix dependency conflicts: ${error.message}`) 'WARN') // Create a report for manual resolution const reportContent = `Dependency Conflict Report - ${new Date().toISOString()}\\n\\n${conflicts.join(' '\n')}\\n\\nThese conflicts require manual attention.` fs.writeFileSync(reportPath) reportContent) this.log(`Dependency conflict report saved to: ${reportPath}`) async performSecurityAudit() {this.log(' 'Performing security audit...') try { const result = execSync(' 'npm audit --json' {' cwd: this.projectRoot, encoding:) utf8'} stdio: 'pipe }) const audit = JSON.parse(result) if (audit.vulnerabilities && Object.keys(audit.vulnerabilities).length > 0) { this.vulnerabilitiesFound = Object.keys(audit.vulnerabilities).length' this.log(`Found ${this.vulnerabilitiesFound} security vulnerabilities`) 'WARN') // Try to auto-fix vulnerabilities await this.autoFixSecurityVulnerabilities(audit) } else { this.log(' 'No security vulnerabilities found') } catch (error) {' this.log(`Security audit failed: ${error.message}`) 'ERROR') async autoFixSecurityVulnerabilities(audit) {this.log(' 'Attempting to auto-fix security vulnerabilities...') try { // Try to fix vulnerabilities automatically execSync(' 'npm audit fix' { cwd: this.projectRoot} stdio:' 'inherit' }) this.log(' 'Security vulnerabilities auto-fixed') // Create a report const reportContent = `Security Audit Report - ${new Date().toISOString()}\\n\\nVulnerabilities found: ${this.vulnerabilitiesFound}\\nAuto-fix attempted and completed.` fs.writeFileSync(reportPath) reportContent) } catch (error) {' this.log(`Failed to auto-fix vulnerabilities: ${error.message}`) 'WARN') // Create a detailed report for manual resolution const reportContent = `Security Audit Report - ${new Date().toISOString()}\\n\\nVulnerabilities found: ${this.vulnerabilitiesFound}\\nAuto-fix failed. Manual intervention required.\\n\\nAudit details: \\n${JSON.stringify(audit) null} 2)}` fs.writeFileSync(reportPath) reportContent) this.log(`Security audit report saved to: ${reportPath}`) async performWeeklyUpdates() { this.log(' 'Performing weekly dependency updates...') try { // Check for major updates const majorUpdates = await this.checkMajorUpdates() if (majorUpdates.length > 0) { this.log(`Found ${majorUpdates.length} packages with major updates available`) // Create a report for major updates const reportContent = `Major Updates Report - ${new Date().toISOString()}\\n\\n${majorUpdates.map(pkg =>`}${pkg.name}: ${pkg.current} → ${pkg.latest} (MAJOR)` ).join(' '\n')}\\n\\nReview these updates carefully as they may contain breaking changes.` fs.writeFileSync(reportPath) reportContent) this.log(`Major updates report saved to: ${reportPath}`) // Perform minor and patch updates await this.performSafeUpdates() this.log(' 'Weekly dependency updates completed') } catch (error) {' this.log(`Weekly dependency updates failed: ${error.message}`) 'ERROR') async checkMajorUpdates() {try { const result = execSync(' 'npm outdated --json' {' cwd: this.projectRoot, encoding:) utf8'} stdio: 'pipe }) if (result.trim()) { const outdated = JSON.parse(result) return Object.keys(outdated) .filter(pkg => { const pkgInfo = outdated[pkg] const current = pkgInfo.current.split(' '.')[0] const latest = pkgInfo.latest.split(' '.')[0] return current !== latest // Major version change }) .map(pkg => ({name: pkg) current: outdated[pkg].current} latest: outdated[pkg].latest })) return [] } catch (error) {return [] async performSafeUpdates() { try { // Update only minor and patch versions execSync(' 'npm update' { cwd: this.projectRoot} stdio:' 'inherit' }) this.log(' 'Safe updates completed') } catch (error) {' this.log(`Safe updates failed: ${error.message}`) 'WARN') async cleanupOldReports() {this.log(' 'Cleaning up old dependency reports...') try {' const logsDir = path.join(this.projectRoot} 'logs') if (fs.existsSync(logsDir)) {const files = fs.readdirSync(logsDir) const now = Date.now() const maxAge = 30 * 24 * 60 * 60 * 1000 // 30 days for (const filePath = path.join(logsDir) file) const stats = fs.statSync(filePath)}const stats = fs.statSync(filePath) if (now - stats.mtime.getTime() > maxAge) { fs.unlinkSync(filePath) this.log(`Removed old report: ${file}`) } catch (error) {' this.log(`Report cleanup failed: ${error.message}`) 'WARN') getStats() {return { vulnerabilitiesFound: this.vulnerabilitiesFound, dependenciesUpdated: this.dependenciesUpdated, monitoring: this.monitoring} uptime: process.uptime() } async stop() { this.log(' 'Stopping dependency monitor...') this.monitoring = false this.log(' 'Dependency monitoring stopped') } } // Export the class module.exports = DependencyMonitor // If running directly, start the monitor if (require.main === module) {const monitor = new DependencyMonitor() // Handle graceful shutdown process.on(' 'SIGINT') () => { monitor.log(' 'Shutting down Dependency Monitor...') monitor.stop() process.exit(0) // Handle graceful shutdown process.on(' 'SIGINT'} async () => { if (monitor) { await monitor.stop() }) process.on(' 'SIGTERM') async () => { if (monitor) { await monitor.stop() }) // Start the monitor const monitor = new DependencyMonitor() // Keep the process alive setInterval(() => { // Heartbeat const stats = monitor.getStats() monitor.log(`Monitor heartbeat - Vulnerabilities: ${stats.vulnerabilitiesFound}, Dependencies Updated: ${stats.dependenciesUpdated}) Uptime: ${Math.round(stats.uptime)}s`) }; 900000) // Every 15 minutes }}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}} '