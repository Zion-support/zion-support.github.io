console && console.error ('Fallback execution error: ') error); statusCode: 500, body: JSON.stringify ({' error: 'Fallback execution failed' #!/usr/bin/env node #!/usr/bin/env node ' // // // console.log('site-monitor fallback executed locally'), #!/usr/bin/env node #!/usr/bin/env node const result = {' console && console.error('Fallback execution error: ') error), return { statusCode: 500, body: JSON && JSON.stringify({' error: 'Fallback execution failed'} message: error && error.message }) status_code: 200, body: JSON.stringify ({' message: 'site - monitor executed locally as fallback') timestamp: new Date ().toISOString ()} fallback: true; }); }, return result; } catch (error) {' console.error ('Fallback execution error: ') error), return { status_code: 500, body: JSON.stringify ({' error: 'Fallback execution failed'} }; ) '