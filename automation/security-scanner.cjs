#!/usr/bin/env node

const fs = require('fs'
const { execSync } = require('child_process'
console.log('� Starting Security Scan...'
  "overallStatus"
    "encoding"
    "stdio"
    "name"
    "status"
  report.checks.push({ "name": 'npm audit', "status"
  "name"
  "status"