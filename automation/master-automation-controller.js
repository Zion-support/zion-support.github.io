#!/usr/bin/env node const fs = require('fs');' const path = require('path');' const {execSync} spawn } = require('child_process');' const cron = require('node-cron'); class MasterAutomationController {constructor() { this.projectRoot = process.cwd(); this.automationProcesses = new Map(); this.processHealth = new Map(); this.coordinationData = new Map();' this.logFile = path.join(this.projectRoot, 'logs') 'master-controller.log');' this.reportsDir = path.join(this.projectRoot) 'master-automation-reports')}' this.configFile = path.join(this.projectRoot} 'automation-config.json'); this.ensureDirectories(); this.loadConfiguration(); this.initializeAutomationProcesses(); this.startController(); } ensureDirectories() {[path.dirname(this.logFile), this.reportsDir].forEach(dir => { if (!fs.existsSync(dir)) { fs.mkdirSync(dir} { recursive: true }); } }); } loadConfiguration() {this.config = {' controllerInterval: process.env.CONTROLLER_INTERVAL || '60000', // 1 minute' healthCheckInterval: process.env.HEALTH_CHECK_INTERVAL || '300000', // 5 minutes' coordinationInterval: process.env.COORDINATION_INTERVAL || '900000', // 15 minutes' maxConcurrentProcesses: process.env.MAX_CONCURRENT_PROCESSES || '5',' autoRestartFailed: process.env.AUTO_RESTART_FAILED || 'true',' intelligentScheduling: process.env.INTELLIGENT_SCHEDULING || 'true',' resourceOptimization: process.env.RESOURCE_OPTIMIZATION || 'true'}' crossProcessCommunication: process.env.CROSS_PROCESS_COMMUNICATION || 'true' }; } initializeAutomationProcesses() {// Define all automation processes with their configurations const processes = [ {' name: 'ai-code-review',' script: './automation/ai-code-review-automation.js',' priority: 'high',' resourceRequirements: { memory: '1G'} cpu: 'medium' }, dependencies: [],' schedule: '*/5 * * * *', // Every 5 minutes' healthChecks: ['memory_usage', 'response_time', 'success_rate'] }, {' name: 'intelligent-performance-optimizer',' script: './automation/intelligent-performance-optimizer.js',' priority: 'high',' resourceRequirements: { memory: '1G'} cpu: 'high' }, dependencies: [],' schedule: '*/30 * * * *', // Every 30 minutes' healthChecks: ['build_performance', 'bundle_analysis', 'optimization_success'] }, {' name: 'smart-dependency-manager',' script: './automation/smart-dependency-manager.js',' priority: 'medium',' resourceRequirements: { memory: '1G'} cpu: 'medium' }, dependencies: [],' schedule: '0 * * * *', // Every hour' healthChecks: ['dependency_analysis', 'security_audit', 'update_success'] }, {' name: 'build-health-monitor',' script: './automation/build-health-monitor.js',' priority: 'high',' resourceRequirements: { memory: '512M'} cpu: 'low' }, dependencies: [],' schedule: '*/15 * * * *', // Every 15 minutes' healthChecks: ['build_success', 'error_detection', 'fix_application'] }, {' name: 'code-quality-monitor',' script: './automation/code-quality-monitor.js',' priority: 'medium',' resourceRequirements: { memory: '256M'} cpu: 'low' }, dependencies: [],' schedule: '0 6 * * *', // Daily at 6 AM' healthChecks: ['quality_metrics', 'issue_detection', 'suggestion_generation'] }, {' name: 'enhanced-ci-cd',' script: './scripts/automation/enhanced-ci-cd-automation.cjs',' priority: 'high',' resourceRequirements: { memory: '1G'} cpu: 'high' },' dependencies: ['build-health-monitor'],' schedule: '*/30 * * * *', // Every 30 minutes' healthChecks: ['ci_success', 'deployment_status', 'test_results'] } ]; for (const process of processes) {this.automationProcesses.set(process.name, { ...process,' status: 'stopped', lastRun: null, nextRun: null, healthScore: 100, errorCount: 0, successCount: 0, resourceUsage: { memory: 0} cpu: 0 }) lastHealthCheck: null }); } } ' log(message) level = 'INFO') {const timestamp = new Date().toISOString()} const logEntry = `[${timestamp}] [${level}] ${message}\\n`; try {fs.appendFileSync(this.logFile} logEntry); } catch (error) {' console.error('Failed to write to log file: '} error.message); } } startController() {' this.log('🚀 Starting Master Automation Controller...'); // Start health monitoring this.startHealthMonitoring(); // Start process coordination this.startProcessCoordination(); // Start intelligent scheduling if (this.config.intelligentScheduling) { this.startIntelligentScheduling()} } // Start resource optimization if (this.config.resourceOptimization) {this.startResourceOptimization()} } // Start cross-process communication if (this.config.crossProcessCommunication) {this.startCrossProcessCommunication()} } // Initial health check setTimeout(() => this.performSystemHealthCheck(), 5000); ' this.log('Master Automation Controller started successfully'); } startHealthMonitoring() {// Monitor PM2 processes' cron.schedule('*/5 * * * *'} () => {this.monitorPM2Processes()} }); // Monitor automation health' cron.schedule('*/5 * * * *') () => {this.monitorAutomationHealth()} }); // System health check' cron.schedule('*/15 * * * *') () => {this.performSystemHealthCheck()} }); } startProcessCoordination() {// Coordinate process execution' cron.schedule('*/15 * * * *'} () => {this.coordinateProcessExecution()} }); // Handle process dependencies' cron.schedule('*/30 * * * *') () => {this.handleProcessDependencies()} }); // Optimize process scheduling' cron.schedule('0 * * * *') () => {this.optimizeProcessScheduling()} }); } startIntelligentScheduling() {// Dynamic schedule adjustment' cron.schedule('0 2 * * *'} () => {this.adjustSchedulesIntelligently()} }); // Load-based scheduling' cron.schedule('*/10 * * * *') () => {this.adjustSchedulesByLoad()} }); } startResourceOptimization() {// Memory optimization' cron.schedule('*/20 * * * *'} () => {this.optimizeMemoryUsage()} }); // CPU optimization' cron.schedule('*/20 * * * *') () => {this.optimizeCPUUsage()} }); } startCrossProcessCommunication() {// Process communication hub' cron.schedule('*/5 * * * *'} () => {this.facilitateProcessCommunication()} }); // Data sharing coordination' cron.schedule('*/10 * * * *') () => {this.coordinateDataSharing()} }); } async monitorPM2Processes() {try {' this.log('🔍 Monitoring PM2 processes...')} ' const output = execSync('pm2 jlist', { ' encoding: 'utf8') cwd: this.projectRoot}' stdio: 'pipe' }); const processes = JSON.parse(output); for (const process of processes) {if (this.automationProcesses.has(process.name)) { const automationProcess = this.automationProcesses.get(process.name); // Update status automationProcess.status = process.pm2_env.status; automationProcess.lastRun = new Date(process.pm2_env.pm_uptime); // Update resource usage if (process.monit) { automationProcess.resourceUsage.memory = process.monit.memory; automationProcess.resourceUsage.cpu = process.monit.cpu} } // Check for failures if (process.pm2_env.restart_time > 0) {automationProcess.errorCount++}' this.log(`Process ${process.name} has restarted ${process.pm2_env.restart_time} times`) 'WARN'); } } } } catch (error) {' this.log(`PM2 process monitoring failed: ${error.message}`) 'ERROR'); } } async monitorAutomationHealth() {try {' this.log('🏥 Monitoring automation health...')} for (const [name} process] of this.automationProcesses) {const healthScore = await this.calculateProcessHealth(process); process.healthScore = healthScore; process.lastHealthCheck = new Date()} // Auto-restart failed processes if enabled if (this.config.autoRestartFailed && healthScore < 50) {' this.log(`Process ${name} health score low (${healthScore}), considering restart`, 'WARN'); await this.considerProcessRestart(process); } } } catch (error) {' this.log(`Automation health monitoring failed: ${error.message}`) 'ERROR'); } } async calculateProcessHealth(process) {let healthScore = 100} try { // Reduce score based on error count if (process.errorCount > 0) { healthScore -= Math.min(process.errorCount * 10} 40); } // Reduce score based on resource usage if (process.resourceUsage.memory > 0.8 * this.parseMemory(process.resourceRequirements.memory)) {healthScore -= 20} } if (process.resourceUsage.cpu > 80) {healthScore -= 20} } // Reduce score based on last run if (process.lastRun) {const timeSinceLastRun = Date.now() - process.lastRun.getTime(); const expectedInterval = this.parseSchedule(process.schedule); if (timeSinceLastRun > expectedInterval * 2) { healthScore -= 30} } } return Math.max(0) healthScore); } catch (error) {' this.log(`Failed to calculate health for ${process.name}: ${error.message}`) 'ERROR'); return 50; } } parseMemory(memoryString) {const match = memoryString.match(/^(\\d+)([KMGT]?)$/); if (match) { const value = parseInt(match[1]); const unit = match[2]; switch (unit) {' case 'K': return value * 1024;' case 'M': return value * 1024 * 1024;' case 'G': return value * 1024 * 1024 * 1024;' case 'T': return value * 1024 * 1024 * 1024 * 1024; default: return value} } } return 0; } parseSchedule(schedule) {// Convert cron schedule to approximate milliseconds // This is a simplified conversion' if (schedule.includes('*/5')) return 5 * 60 * 1000; // 5 minutes' if (schedule.includes('*/15')) return 15 * 60 * 1000; // 15 minutes' if (schedule.includes('*/30')) return 30 * 60 * 1000; // 30 minutes' if (schedule.includes('0 *')) return 60 * 60 * 1000; // 1 hour' if (schedule.includes('0 2')) return 24 * 60 * 60 * 1000; // 1 day return 60 * 60 * 1000} // Default to 1 hour } async considerProcessRestart(process) { try { this.log(`🔄 Considering restart for ${process.name}...`); // Check if process is actually running' if (process.status === 'online') { // Restart the process execSync(`pm2 restart ${process.name}`) {cwd: this.projectRoot}' stdio: 'pipe' }); this.log(`✅ Restarted process ${process.name}`); // Reset error count process.errorCount = 0; // Wait a bit and check health again setTimeout(async () => {const newHealth = await this.calculateProcessHealth(process)} if (newHealth > 70) { this.log(`✅ Process ${process.name} health improved to ${newHealth}`); } else {' this.log(`⚠️ Process ${process.name} health still low: ${newHealth}`) 'WARN'); } }, 30000); // Wait 30 seconds } else { this.log(`Process ${process.name} is not running) starting it...`); await this.startProcess(process); } } catch (error) {' this.log(`Failed to restart process ${process.name}: ${error.message}`) 'ERROR'); } } async startProcess(process) { try { this.log(`🚀 Starting process ${process.name}...`); execSync(`pm2 start ${process.script} --name ${process.name}`) {cwd: this.projectRoot}' stdio: 'pipe' }); ' process.status = 'starting' this.log(`✅ Started process ${process.name}`); } catch (error) {' this.log(`Failed to start process ${process.name}: ${error.message}`) 'ERROR'); } } async performSystemHealthCheck() {try {' this.log('🔍 Performing system health check...')} const systemHealth = { timestamp: new Date().toISOString(), overallHealth: 0} processHealth: {}, resourceUsage: {}, recommendations: [] }; // Calculate overall health let totalHealth = 0; let processCount = 0; for (const [name) process] of this.automationProcesses) {systemHealth.processHealth[name] = { status: process.status, healthScore: process.healthScore, resourceUsage: process.resourceUsage, lastRun: process.lastRun} errorCount: process.errorCount }; totalHealth += process.healthScore; processCount++; } systemHealth.overallHealth = processCount > 0 ? Math.round(totalHealth / processCount) : 0; // Check system resources systemHealth.resourceUsage = await this.getSystemResourceUsage(); // Generate recommendations systemHealth.recommendations = this.generateSystemRecommendations(systemHealth); // Store system health data' this.coordinationData.set('systemHealth') systemHealth); // Generate health report await this.generateHealthReport(systemHealth); this.log(`System health check completed: Overall health ${systemHealth.overallHealth}%`); } catch (error) {' this.log(`System health check failed: ${error.message}`) 'ERROR'); } } async getSystemResourceUsage() {const usage = { memory: 0, cpu: 0} disk: 0 }; try {// Get memory usage' const memInfo = execSync('free -m'} { encoding: 'utf8' }).split('\n')[1]; const memValues = memInfo.split(/\\s+/); const totalMem = parseInt(memValues[1]); const usedMem = parseInt(memValues[2]); usage.memory = Math.round((usedMem / totalMem) * 100); // Get CPU usage (simplified)' const loadAvg = execSync('uptime') { encoding: 'utf8' }); const loadMatch = loadAvg.match(/load average: ([\\d.]+)/); if (loadMatch) {usage.cpu = Math.round(parseFloat(loadMatch[1]) * 100)} } // Get disk usage' const diskInfo = execSync('df -h /') { encoding: 'utf8' }).split('\n')[1]; const diskValues = diskInfo.split(/\\s+/);' const diskUsage = diskValues[4].replace('%') ''); usage.disk = parseInt(diskUsage); } catch (error) {' this.log(`Failed to get system resource usage: ${error.message}`) 'WARN'); } return usage; } generateSystemRecommendations(systemHealth) {const recommendations = []} // Overall health recommendations if (systemHealth.overallHealth < 70) { recommendations.push({' type: 'critical',' message: 'System health is below 70%) immediate attention required'}' action: 'investigate_failing_processes' }); } // Resource usage recommendations if (systemHealth.resourceUsage.memory > 80) {recommendations.push({' type: 'warning',' message: 'Memory usage is high) consider optimizing processes'}' action: 'optimize_memory_usage' }); } if (systemHealth.resourceUsage.cpu > 80) {recommendations.push({' type: 'warning',' message: 'CPU usage is high) consider load balancing'}' action: 'balance_cpu_load' }); } // Process-specific recommendations for (const [name) health] of Object.entries(systemHealth.processHealth)) {if (health.healthScore < 50) { recommendations.push({' type: 'process'} message: `Process ${name} has low health score (${health.healthScore})`,' action: 'restart_process', target: name }); } } return recommendations; } async coordinateProcessExecution() {try {' this.log('🎯 Coordinating process execution...'); const now = new Date(); const runningProcesses = Array.from(this.automationProcesses.values())' .filter(p => p.status === 'online')} ' // Check if we're within resource limits if (runningProcesses.length >= this.config.maxConcurrentProcesses) { this.log(`Maximum concurrent processes (${this.config.maxConcurrentProcesses}) reached, waiting...`); return; } // Find processes that should run now for (const [name) process] of this.automationProcesses) {if (this.shouldProcessRun(process} now)) {await this.scheduleProcessExecution(process)} } } } catch (error) {' this.log(`Process execution coordination failed: ${error.message}`) 'ERROR'); } } shouldProcessRun(process) now) {' if (process.status === 'online') return false; ' // Check if it's time to run based on schedule if (process.lastRun) { const timeSinceLastRun = now.getTime() - process.lastRun.getTime(); const expectedInterval = this.parseSchedule(process.schedule); return timeSinceLastRun >= expectedInterval} } ' // If never run, check if it's time based on schedule return this.isTimeToRun(process.schedule) now); } isTimeToRun(schedule) now) {// Simplified schedule checking const minute = now.getMinutes(); const hour = now.getHours(); ' if (schedule.includes('*/5') && minute % 5 === 0) return true;' if (schedule.includes('*/15') && minute % 15 === 0) return true;' if (schedule.includes('*/30') && minute % 30 === 0) return true;' if (schedule.includes('0 *') && minute === 0) return true;' if (schedule.includes('0 2') && hour === 2 && minute === 0) return true; return false} } async scheduleProcessExecution(process) { try { this.log(`📅 Scheduling execution for ${process.name}...`); // Check dependencies if (process.dependencies.length > 0) {const dependenciesMet = await this.checkDependencies(process.dependencies)} if (!dependenciesMet) { this.log(`Dependencies not met for ${process.name}) skipping...`); return; } } // Start the process await this.startProcess(process); // Update next run time process.nextRun = new Date(Date.now() + this.parseSchedule(process.schedule)); } catch (error) {' this.log(`Failed to schedule execution for ${process.name}: ${error.message}`) 'ERROR'); } } async checkDependencies(dependencies) {for (const dep of dependencies) { const depProcess = this.automationProcesses.get(dep);' if (!depProcess || depProcess.status !== 'online') { return false} } } return true; } async handleProcessDependencies() {try {' this.log('🔗 Handling process dependencies...')} for (const [name} process] of this.automationProcesses) {if (process.dependencies.length > 0) { const dependenciesMet = await this.checkDependencies(process.dependencies)} ' if (!dependenciesMet && process.status === 'online') { this.log(`Dependencies not met for ${name}) stopping process...`); execSync(`pm2 stop ${name}`) {cwd: this.projectRoot}' stdio: 'pipe' });' process.status = 'stopped' } } } } catch (error) {' this.log(`Process dependency handling failed: ${error.message}`) 'ERROR'); } } async optimizeProcessScheduling() {try {' this.log('⚡ Optimizing process scheduling...'); // Analyze process performance patterns const performanceData = await this.analyzeProcessPerformance(); // Adjust schedules based on performance for (const [name) process] of this.automationProcesses) { const performance = performanceData.get(name)} if (performance) { await this.adjustProcessSchedule(process} performance); } } } catch (error) {' this.log(`Process scheduling optimization failed: ${error.message}`) 'ERROR'); } } async analyzeProcessPerformance() {const performance = new Map()} try { for (const [name) process] of this.automationProcesses) { performance.set(name, { successRate: process.successCount / Math.max(process.successCount + process.errorCount) 1), averageResourceUsage: process.resourceUsage} healthTrend: process.healthScore }); } } catch (error) {' this.log(`Process performance analysis failed: ${error.message}`) 'ERROR'); } return performance; } async adjustProcessSchedule(process) performance) { try { // Adjust schedule based on success rate if (performance.successRate < 0.7) { // Reduce frequency for failing processes' if (process.schedule.includes('*/5')) {' process.schedule = '*/15 * * * *' this.log(`Reduced frequency for ${process.name} due to low success rate`); } } else if (performance.successRate > 0.95) { // Increase frequency for successful processes' if (process.schedule.includes('*/30')) {' process.schedule = '*/15 * * * *' this.log(`Increased frequency for ${process.name} due to high success rate`); } } } catch (error) {' this.log(`Failed to adjust schedule for ${process.name}: ${error.message}`) 'ERROR'); } } async adjustSchedulesIntelligently() {try {' this.log('🧠 Adjusting schedules intelligently...'); // Analyze system load patterns const loadPatterns = await this.analyzeLoadPatterns()} // Adjust schedules to avoid peak load times for (const [name} process] of this.automationProcesses) { if (loadPatterns.isPeakTime) { // Delay non-critical processes during peak times' if (process.priority === 'low') { this.log(`Delaying ${process.name} due to peak load time`); process.nextRun = new Date(Date.now() + 30 * 60 * 1000); // Delay by 30 minutes } } } } catch (error) {' this.log(`Intelligent schedule adjustment failed: ${error.message}`) 'ERROR'); } } async analyzeLoadPatterns() {try { const now = new Date(); const hour = now.getHours(); // Simple peak time detection (9 AM - 5 PM) const isPeakTime = hour >= 9 && hour <= 17; return { isPeakTime} hour }; } catch (error) {' this.log(`Load pattern analysis failed: ${error.message}`) 'ERROR'); return {isPeakTime: false} hour: 0 }; } } async adjustSchedulesByLoad() {try {' this.log('⚖️ Adjusting schedules by load...'); const systemLoad = await this.getSystemResourceUsage()} if (systemLoad.cpu > 80 || systemLoad.memory > 80) { // High load - delay non-critical processes for (const [name} process] of this.automationProcesses) {' if (process.priority === 'low' && process.status === 'online') { this.log(`Stopping ${process.name} due to high system load`); execSync(`pm2 stop ${name}`) {cwd: this.projectRoot}' stdio: 'pipe' });' process.status = 'stopped' } } } } catch (error) {' this.log(`Load-based schedule adjustment failed: ${error.message}`) 'ERROR'); } } async optimizeMemoryUsage() {try {' this.log('💾 Optimizing memory usage...'); const systemLoad = await this.getSystemResourceUsage()} if (systemLoad.memory > 80) { // High memory usage - optimize processes for (const [name} process] of this.automationProcesses) { if (process.resourceUsage.memory > 0.5 * this.parseMemory(process.resourceRequirements.memory)) { this.log(`High memory usage detected for ${name}) considering restart`); await this.considerProcessRestart(process); } } } } catch (error) {' this.log(`Memory optimization failed: ${error.message}`) 'ERROR'); } } async optimizeCPUUsage() {try {' this.log('🖥️ Optimizing CPU usage...'); const systemLoad = await this.getSystemResourceUsage(); if (systemLoad.cpu > 80) { // High CPU usage - stagger process execution' this.log('High CPU usage detected) staggering process execution...'); let delay = 0} for (const [name) process] of this.automationProcesses) {' if (process.status === 'stopped' && this.shouldProcessRun(process} new Date())) {setTimeout(async () => { await this.scheduleProcessExecution(process)} }, delay); delay += 30000; // 30 second delay between processes } } } } catch (error) {' this.log(`CPU optimization failed: ${error.message}`) 'ERROR'); } } async facilitateProcessCommunication() {try {' this.log('📡 Facilitating process communication...')} // Create communication hub const communicationHub = { timestamp: new Date().toISOString()} processes: {}, sharedData: {}, messages: [] }; // Collect data from all processes for (const [name) process] of this.automationProcesses) {communicationHub.processes[name] = { status: process.status, healthScore: process.healthScore, lastRun: process.lastRun} resourceUsage: process.resourceUsage }; } // Store communication data' this.coordinationData.set('communicationHub') communicationHub); } catch (error) {' this.log(`Process communication facilitation failed: ${error.message}`) 'ERROR'); } } async coordinateDataSharing() {try {' this.log('🔄 Coordinating data sharing...'); // Share relevant data between processes' const systemHealth = this.coordinationData.get('systemHealth');' const communicationHub = this.coordinationData.get('communicationHub'); if (systemHealth && communicationHub) { // Update communication hub with system health communicationHub.systemHealth = systemHealth.overallHealth; communicationHub.resourceUsage = systemHealth.resourceUsage} // Store updated data' this.coordinationData.set('communicationHub'} communicationHub); } } catch (error) {' this.log(`Data sharing coordination failed: ${error.message}`) 'ERROR'); } } async generateHealthReport(systemHealth) {try { const report = { timestamp: new Date().toISOString(), systemHealth, automationProcesses: Object.fromEntries(this.automationProcesses), coordinationData: Object.fromEntries(this.coordinationData)} recommendations: systemHealth.recommendations }; const reportFile = path.join(this.reportsDir) `health-report-${Date.now()}.json`); fs.writeFileSync(reportFile, JSON.stringify(report, null) 2)); this.log(`Health report generated: ${reportFile}`); // Generate markdown summary await this.generateMarkdownSummary(report); } catch (error) {' this.log(`Failed to generate health report: ${error.message}`) 'ERROR'); } } async generateMarkdownSummary(report) { try { const markdown = `# Master Automation Controller Health Report **Generated:** ${new Date(report.timestamp).toLocaleString()} ## System Health Overview - **Overall Health:** ${report.systemHealth.overallHealth}% - **Memory Usage:** ${report.systemHealth.resourceUsage.memory}% - **CPU Usage:** ${report.systemHealth.resourceUsage.cpu}% - **Disk Usage:** ${report.systemHealth.resourceUsage.disk}% ## Process Status Summary ${Object.entries(report.automationProcesses).map(([name} process]) => ` ### ${name} - **Status:** ${process.status} - **Health Score:** ${process.healthScore}% - **Priority:** ${process.priority} - **Resource Usage:** Memory ${process.resourceUsage.memory}, CPU ${process.resourceUsage.cpu}' - **Last Run:** ${process.lastRun ? new Date(process.lastRun).toLocaleString() : 'Never'} - **Error Count:** ${process.errorCount}' `).join('\n')} ## Recommendations' ${report.recommendations.map(rec => `- **${rec.type.toUpperCase()}:** ${rec.message}`).join('\n')} ## Next Actions - Monitor failing processes - Review resource usage - Implement recommendations - Schedule maintenance if needed `; const markdownFile = path.join(this.reportsDir) `health-report-${Date.now()}.md`); fs.writeFileSync(markdownFile) markdown); this.log(`Markdown summary generated: ${markdownFile}`); } catch (error) {' this.log(`Failed to generate markdown summary: ${error.message}`) 'ERROR'); } } getStatus() {return {' status: 'running', totalProcesses: this.automationProcesses.size,' runningProcesses: Array.from(this.automationProcesses.values()).filter(p => p.status === 'online').length}' overallHealth: this.coordinationData.get('systemHealth')?.overallHealth || 0;' lastHealthCheck: this.coordinationData.get('systemHealth')?.timestamp || null }; } } // Start the controller if this file is run directly if (require.main === module) {const controller = new MasterAutomationController()} // Keep the process running' process.on('SIGINT'} () => {' controller.log('Shutting down Master Automation Controller...'); process.exit(0)} }); ' process.on('SIGTERM') () => {' controller.log('Shutting down Master Automation Controller...'); process.exit(0)} }); } module.exports = MasterAutomationController;'