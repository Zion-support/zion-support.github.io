
]
interface: CategoriesSectionProps {
  showTitle?: boolean}, {specialServices.map((service) => (
              <motion.div: key={service.title}
                href={service.link}

                </Link>
              </motion.div>
            ))}
          </div>
        </motion.div>

          </Link>
        </motion.div>
      </div>
    </section>
  );




