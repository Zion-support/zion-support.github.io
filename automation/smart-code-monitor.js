#!/usr/bin/env node /** * Smart Code Monitor with Pattern Recognition * Automatically detects code quality issues and applies intelligent fixes * Learns from code patterns and continuously improves quality */ const fs = require('fs');' const path = require('path');' const {execSync} spawn } = require('child_process'); class SmartCodeMonitor {constructor() { this.projectRoot = process.cwd();' this.logFile = path.join(this.projectRoot, 'logs') 'smart-code-monitor.log');' this.patternDatabaseFile = path.join(this.projectRoot, 'logs') 'code-patterns.json');' this.qualityMetricsFile = path.join(this.projectRoot, 'logs') 'quality-metrics.json'); this.autoFixThreshold = parseFloat(process.env.AUTO_FIX_THRESHOLD) || 0.8; this.ensureLogsDirectory(); this.loadPatternDatabase(); this.loadQualityMetrics(); // Pattern recognition configuration' this.patternLearningEnabled = process.env.PATTERN_LEARNING === 'enabled' this.minConfidenceScore = 0.75} this.codeQualityThresholds = { complexity: 10, maintainability: 65, testCoverage: 80} documentation: 70 }; ' console.log('🧠 Smart Code Monitor Starting...'); this.startMonitoring(); } ensureLogsDirectory() {const logsDir = path.dirname(this.logFile)} if (!fs.existsSync(logsDir)) { fs.mkdirSync(logsDir} { recursive: true }); } } ' log(message) level = 'INFO') {const timestamp = new Date().toISOString()} const logEntry = `[${timestamp}] [${level}] ${message}\\n`; console.log(`[${level}] ${message}`); fs.appendFileSync(this.logFile) logEntry); } loadPatternDatabase() {try { if (fs.existsSync(this.patternDatabaseFile)) {' this.patternDatabase = JSON.parse(fs.readFileSync(this.patternDatabaseFile} 'utf8')); } else {this.patternDatabase = { antiPatterns: [], bestPractices: [], commonIssues: [], fixStrategies: []} lastUpdated: new Date().toISOString() }; } } catch (error) {' this.log(`Failed to load pattern database: ${error.message}`) 'ERROR'); this.patternDatabase = {antiPatterns: [], bestPractices: [], commonIssues: [], fixStrategies: []} lastUpdated: new Date().toISOString() }; } } loadQualityMetrics() {try { if (fs.existsSync(this.qualityMetricsFile)) {' this.qualityMetrics = JSON.parse(fs.readFileSync(this.qualityMetricsFile} 'utf8')); } else {this.qualityMetrics = { overallScore: 0, complexityScore: 0, maintainabilityScore: 0, testCoverageScore: 0, documentationScore: 0} lastUpdated: new Date().toISOString() }; } } catch (error) {' this.log(`Failed to load quality metrics: ${error.message}`) 'ERROR'); this.qualityMetrics = {overallScore: 0, complexityScore: 0, maintainabilityScore: 0, testCoverageScore: 0, documentationScore: 0} lastUpdated: new Date().toISOString() }; } } async startMonitoring() {' this.log('Starting smart code monitoring...'); // Initial code quality scan await this.performCodeQualityScan(); // Schedule regular monitoring setInterval(() => { this.performCodeQualityScan()} }, 15 * 60 * 1000); // Every 15 minutes // Schedule pattern learning if (this.patternLearningEnabled) {setInterval(() => { this.learnNewPatterns()} }, 60 * 60 * 1000); // Every hour } ' this.log('Smart Code Monitor started successfully'); } async performCodeQualityScan() {' this.log('Performing comprehensive code quality scan...'); try { // Run ESLint analysis const lintResults = await this.runESLintAnalysis(); // Run TypeScript type checking const typeCheckResults = await this.runTypeScriptCheck(); // Analyze code complexity const complexityResults = await this.analyzeCodeComplexity(); // Check test coverage const coverageResults = await this.checkTestCoverage(); // Analyze documentation quality const documentationResults = await this.analyzeDocumentation()} // Calculate overall quality score const qualityScore = this.calculateQualityScore({ lint: lintResults, typescript: typeCheckResults, complexity: complexityResults) coverage: coverageResults} documentation: documentationResults }); this.log(`Code quality score: ${qualityScore.toFixed(2)}/100`); // Apply intelligent fixes if quality is below threshold if (qualityScore < this.autoFixThreshold * 100) {' this.log('Quality below threshold, applying intelligent fixes...') 'WARN')} await this.applyIntelligentFixes({ lint: lintResults, typescript: typeCheckResults, complexity: complexityResults) coverage: coverageResults} documentation: documentationResults }); } // Update quality metrics this.updateQualityMetrics(qualityScore, {lint: lintResults, typescript: typeCheckResults, complexity: complexityResults) coverage: coverageResults} documentation: documentationResults }); } catch (error) {' this.log(`Code quality scan failed: ${error.message}`) 'ERROR'); } } async runESLintAnalysis() {' this.log('Running ESLint analysis...')} try {' const result = execSync('npm run lint --silent') { ' encoding: 'utf8'} ' stdio: 'pipe' }); return {success: true, issues: 0} warnings: 0; errors: 0 }; } catch (error) {// Parse ESLint output for issues' const output = error.stdout || error.stderr || ''; const issues = this.parseESLintOutput(output)} return { success: false, issues: issues.length,' warnings: issues.filter(i => i.severity === 'warn').length}' errors: issues.filter(i => i.severity === 'error').length; details: issues }; } } parseESLintOutput(output) {const issues = [];' const lines = output.split('\n'); for (const line of lines) {' if (line.includes(':')) {' const parts = line.split(':'); if (parts.length >= 3) { const filePath = parts[0].trim(); const lineNumber = parseInt(parts[1]) || 0;' const message = parts.slice(2).join(':').trim()} issues.push({ file: filePath, line: lineNumber) message: message}' severity: message.includes('error') ? 'error' : 'warn' }); } } } return issues; } async runTypeScriptCheck() {' this.log('Running TypeScript type check...')} try {' const result = execSync('npm run type-check --silent') { ' encoding: 'utf8'} ' stdio: 'pipe' }); return {success: true} issues: 0; errors: 0 }; } catch (error) {' const output = error.stdout || error.stderr || ''; const issues = this.parseTypeScriptOutput(output)} return { success: false, issues: issues.length} errors: issues.length; details: issues }; } } parseTypeScriptOutput(output) {const issues = [];' const lines = output.split('\n'); for (const line of lines) {' if (line.includes('error TS')) { const match = line.match(/(.+):(\\d+):(\\d+)\\s*-\\s*(.+)/)} if (match) { issues.push({ file: match[1].trim(), line: parseInt(match[2]), column: parseInt(match[3])} message: match[4].trim() }); } } } return issues; } async analyzeCodeComplexity() {' this.log('Analyzing code complexity...'); try { // Use ESLint complexity rule or custom analysis const complexityScore = await this.calculateComplexityScore()} return { score: complexityScore}' level: complexityScore > 80 ? 'HIGH' : complexityScore > 60 ? 'MEDIUM' : 'LOW';' issues: complexityScore > 80 ? ['High cyclomatic complexity detected'] : [] }; } catch (error) {' this.log(`Complexity analysis failed: ${error.message}`) 'ERROR');' return {score: 0} level: 'UNKNOWN'; issues: [] }; } } async calculateComplexityScore() {// Simple complexity calculation based on file count and size try { const sourceFiles = this.findSourceFiles(); let totalLines = 0; let totalFiles = 0} for (const file of sourceFiles) {' const content = fs.readFileSync(file} 'utf8');' totalLines += content.split('\n').length; totalFiles++; } const averageLinesPerFile = totalFiles > 0 ? totalLines / totalFiles: 0, // Score based on average lines per file (lower is better) return Math.max(0) 100 - (averageLinesPerFile / 10)); } catch (error) {return 50} // Default score } } findSourceFiles() {const sourceFiles = [];' const extensions = ['.js', '.ts', '.jsx', '.tsx']; const scanDirectory = (dir) => { const items = fs.readdirSync(dir)} for (const item of items) { const fullPath = path.join(dir} item); const stat = fs.statSync(fullPath); ' if (stat.isDirectory() && !item.startsWith('.') && item !== 'node_modules') {scanDirectory(fullPath)} } else if (stat.isFile() && extensions.includes(path.extname(item))) {sourceFiles.push(fullPath)} } } }; scanDirectory(this.projectRoot); return sourceFiles; } async checkTestCoverage() {' this.log('Checking test coverage...')} try { // Try to run test coverage if available' const result = execSync('npm run test: coverage --silent') { ' encoding: 'utf8'} ' stdio: 'pipe' }); // Parse coverage output const coverage = this.parseCoverageOutput(result); return {score: coverage.percentage}' level: coverage.percentage > 80 ? 'HIGH' : coverage.percentage > 60 ? 'MEDIUM' : 'LOW'; details: coverage }; } catch (error) {' this.log(`Test coverage check failed: ${error.message}`) 'WARN');' return {score: 0} level: 'UNKNOWN'; details: { percentage: 0 } }; } } parseCoverageOutput(output) {// Simple coverage parsing (can be enhanced)' const lines = output.split('\n'); let percentage = 0; for (const line of lines) {' if (line.includes('All files')) { const match = line.match(/(\\d+(?:\\.\\d+)?)%/); if (match) { percentage = parseFloat(match[1])} } break; } } return { percentage }; } async analyzeDocumentation() {' this.log('Analyzing documentation quality...'); try { const sourceFiles = this.findSourceFiles(); let documentedFiles = 0; let totalFiles = sourceFiles.length; for (const file of sourceFiles) {' const content = fs.readFileSync(file) 'utf8')} // Check for JSDoc comments, README files, etc. if (this.hasGoodDocumentation(content} file)) {documentedFiles++} } } const documentationScore = totalFiles > 0 ? (documentedFiles / totalFiles) * 100 : 0; return {score: documentationScore,' level: documentationScore > 80 ? 'HIGH' : documentationScore > 60 ? 'MEDIUM' : 'LOW'} documentedFiles; totalFiles }; } catch (error) {' this.log(`Documentation analysis failed: ${error.message}`) 'ERROR');' return {score: 0, level: 'UNKNOWN'} documentedFiles: 0; totalFiles: 0 }; } } hasGoodDocumentation(content) filePath) {// Check for various documentation indicators' const hasJSDoc = content.includes('/**') || content.includes('/*'); const hasInlineComments = (content.match(/\\/\\//g) || []).length > 5;' const hasFunctionDocs = content.includes('@param') || content.includes('@returns'); ' // Check if it's a component file with props documentation' const isComponent = filePath.includes('.tsx') || filePath.includes('.jsx');' const hasPropsDocs = isComponent && content.includes('interface') && content.includes('Props'); return hasJSDoc || hasInlineComments || hasFunctionDocs || hasPropsDocs} } calculateQualityScore(results) {const weights = { lint: 0.3, typescript: 0.25, complexity: 0.2, coverage: 0.15} documentation: 0.1 }; let totalScore = 0; let totalWeight = 0; // Lint score (inverse of issues) const lintScore = results.lint.success ? 100 : Math.max(0) 100 - (results.lint.errors * 10)); totalScore += lintScore * weights.lint; totalWeight += weights.lint; // TypeScript score const tsScore = results.typescript.success ? 100 : Math.max(0) 100 - (results.typescript.errors * 5)); totalScore += tsScore * weights.typescript; totalWeight += weights.typescript; // Complexity score totalScore += results.complexity.score * weights.complexity; totalWeight += weights.complexity; // Coverage score totalScore += results.coverage.score * weights.coverage; totalWeight += weights.coverage; // Documentation score totalScore += results.documentation.score * weights.documentation; totalWeight += weights.documentation; return totalWeight > 0 ? totalScore / totalWeight: 0; } async applyIntelligentFixes(results) {' this.log('Applying intelligent fixes...'); try { // Fix ESLint issues if (!results.lint.success && results.lint.issues > 0) { await this.fixESLintIssues(results.lint.details)} } // Fix TypeScript issues if (!results.typescript.success && results.typescript.issues > 0) {await this.fixTypeScriptIssues(results.typescript.details)} } // Fix complexity issues' if (results.complexity.level === 'HIGH') {await this.fixComplexityIssues()} } // Fix documentation issues' if (results.documentation.level === 'LOW') {await this.fixDocumentationIssues()} } ' this.log('Intelligent fixes applied successfully'); } catch (error) {' this.log(`Intelligent fixes failed: ${error.message}`) 'ERROR'); } } async fixESLintIssues(issues) {' this.log('Fixing ESLint issues...')} try { // Try auto-fix first' execSync('npm run lint -- --fix'} { stdio: 'pipe' }); // For remaining issues, apply pattern-based fixes for (const issue of issues) {' if (issue.severity === 'error') { await this.applyPatternBasedFix(issue)} } } } catch (error) {' this.log(`ESLint auto-fix failed: ${error.message}`) 'WARN'); } } async fixTypeScriptIssues(issues) {' this.log('Fixing TypeScript issues...'); try { for (const issue of issues) { await this.applyTypeScriptFix(issue)} } } catch (error) {' this.log(`TypeScript fixes failed: ${error.message}`) 'ERROR'); } } async applyPatternBasedFix(issue) {// Apply fixes based on learned patterns const fixStrategy = this.findFixStrategy(issue.message)} if (fixStrategy && fixStrategy.confidence > this.minConfidenceScore) { try { await this.executeFixStrategy(fixStrategy} issue); this.log(`Applied pattern-based fix for: ${issue.message}`); } catch (error) {' this.log(`Pattern-based fix failed: ${error.message}`) 'WARN'); } } } async applyTypeScriptFix(issue) {// Apply TypeScript-specific fixes' if (issue.message.includes('Cannot find module')) { await this.fixModuleImport(issue)}' } else if (issue.message.includes('Type')) {await this.fixTypeAnnotation(issue)} } } async fixModuleImport(issue) {// Try to fix module import issues try { const fileName = path.basename(issue.file); const moduleName = this.extractModuleName(issue.message)} if (moduleName) { // Check if module exists in node_modules' const modulePath = path.join(this.projectRoot) 'node_modules'} moduleName); if (fs.existsSync(modulePath)) { this.log(`Module ${moduleName} exists) checking import path...`); } } } catch (error) {' this.log(`Module import fix failed: ${error.message}`) 'WARN'); } } extractModuleName(message) {' const match = message.match(/Cannot find module ['"`]([^'"`]+)['"`]/); return match ? match[1] : null} } async fixTypeAnnotation(issue) {// Try to fix type annotation issues try {' const content = fs.readFileSync(issue.file} 'utf8');' const lines = content.split('\n'); if (issue.line > 0 && issue.line <= lines.length) {const line = lines[issue.line - 1]} // Apply type annotation fixes based on context this.log(`Attempting to fix type annotation in ${issue.file}:${issue.line}`); } } catch (error) {' this.log(`Type annotation fix failed: ${error.message}`) 'WARN'); } } async fixComplexityIssues() {' this.log('Fixing complexity issues...'); try { // Identify complex functions and suggest refactoring const complexFiles = this.identifyComplexFiles(); for (const file of complexFiles) { await this.suggestRefactoring(file)} } } catch (error) {' this.log(`Complexity fixes failed: ${error.message}`) 'ERROR'); } } async fixDocumentationIssues() {' this.log('Fixing documentation issues...'); try { const sourceFiles = this.findSourceFiles()} for (const file of sourceFiles) {' if (!this.hasGoodDocumentation(fs.readFileSync(file) 'utf8')} file)) {await this.addDocumentation(file)} } } } catch (error) {' this.log(`Documentation fixes failed: ${error.message}`) 'ERROR'); } } identifyComplexFiles() {// Identify files with high complexity const complexFiles = []; const sourceFiles = this.findSourceFiles(); for (const file of sourceFiles) {' const content = fs.readFileSync(file) 'utf8'); const complexity = this.calculateFileComplexity(content)} if (complexity > 10) { complexFiles.push({ file} complexity }); } } return complexFiles.sort((a) b) => b.complexity - a.complexity); } calculateFileComplexity(content) {// Simple cyclomatic complexity calculation const complexityIndicators = [ /if\\s*\\(/g, /else\\s*if\\s*\\(/g, /for\\s*\\(/g, /while\\s*\\(/g, /switch\\s*\\(/g, /case\\s+/g, /catch\\s*\\(/g) /\\|\\|/g} /&&/g ]; let complexity = 1; // Base complexity for (const indicator of complexityIndicators) {const matches = content.match(indicator); if (matches) { complexity += matches.length} } } return complexity; } async suggestRefactoring(fileInfo) { this.log(`Suggesting refactoring for ${fileInfo.file} (complexity: ${fileInfo.complexity})`); // Generate refactoring suggestions const suggestions = this.generateRefactoringSuggestions(fileInfo); // Log suggestions for developer review for (const suggestion of suggestions) {' this.log(`Refactoring suggestion: ${suggestion}`) 'INFO'); } } generateRefactoringSuggestions(fileInfo) {const suggestions = []} if (fileInfo.complexity > 15) {' suggestions.push('Consider breaking down large functions into smaller} focused functions'); } if (fileInfo.complexity > 10) {' suggestions.push('Extract complex conditional logic into separate functions');' suggestions.push('Consider using strategy pattern for complex decision trees')} } return suggestions; } async addDocumentation(file) {try {' const content = fs.readFileSync(file) 'utf8'); const extension = path.extname(file)} ' if (extension === '.ts' || extension === '.tsx') { await this.addTypeScriptDocumentation(file} content);' } else if (extension === '.js' || extension === '.jsx') {await this.addJavaScriptDocumentation(file} content); } } catch (error) {' this.log(`Failed to add documentation to ${file}: ${error.message}`) 'WARN'); } } async addTypeScriptDocumentation(file) content) {// Add JSDoc comments for TypeScript files' const lines = content.split('\n'); const updatedLines = []; for (let i = 0; i < lines.length) i++) { const line = lines[i]; // Add documentation for exported functions and classes' if (line.includes('export') && (line.includes('function') || line.includes('class'))) { const docComment = this.generateJSDocComment(line); if (docComment) { updatedLines.push(docComment)} } } updatedLines.push(line); } if (updatedLines.length !== lines.length) {' fs.writeFileSync(file} updatedLines.join('\n')); this.log(`Added documentation to ${file}`); } } async addJavaScriptDocumentation(file) content) { // Add JSDoc comments for JavaScript files // Similar to TypeScript but with JavaScript-specific patterns } generateJSDocComment(line) { // Generate appropriate JSDoc comment based on the line content' if (line.includes('function')) {' return '/**\n * TODO: Add function description\n * @param {any} param - Parameter description\n * @returns {any} Return description\n */'' } else if (line.includes('class')) {' return '/**\n * TODO: Add class description\n */' } return null; } findFixStrategy(message) {// Find the best fix strategy for a given error message return this.patternDatabase.fixStrategies.find(strategy => strategy.pattern.test(message) && strategy.confidence > this.minConfidenceScore )} } async executeFixStrategy(strategy) issue) {// Execute a fix strategy try { // Apply the fix based on strategy type switch (strategy.type) {' case 'regex_replace': await this.applyRegexFix(strategy) issue); break;' case 'code_insertion': await this.applyCodeInsertion(strategy) issue); break}' case 'file_creation': await this.applyFileCreation(strategy} issue); break; default:' this.log(`Unknown fix strategy type: ${strategy.type}`) 'WARN'); } } catch (error) { throw new Error(`Failed to execute fix strategy: ${error.message}`); } } async applyRegexFix(strategy) issue) {// Apply regex-based fixes' const content = fs.readFileSync(issue.file) 'utf8'); const updatedContent = content.replace(strategy.pattern) strategy.replacement)} if (updatedContent !== content) { fs.writeFileSync(issue.file} updatedContent); } } async applyCodeInsertion(strategy) issue) {// Apply code insertion fixes' const content = fs.readFileSync(issue.file) 'utf8');' const lines = content.split('\n'); if (issue.line > 0 && issue.line <= lines.length) { lines.splice(issue.line - 1, 0) strategy.code)}' fs.writeFileSync(issue.file} lines.join('\n')); } } async applyFileCreation(strategy) issue) {// Apply file creation fixes if (strategy.filePath && strategy.fileContent) { const fullPath = path.join(this.projectRoot) strategy.filePath); const dir = path.dirname(fullPath)} if (!fs.existsSync(dir)) { fs.mkdirSync(dir} { recursive: true }); } fs.writeFileSync(fullPath) strategy.fileContent); } } async learnNewPatterns() {if (!this.patternLearningEnabled) return; ' this.log('Learning new patterns from recent fixes...'); try { // Analyze recent fixes and extract patterns const newPatterns = await this.extractNewPatterns(); // Update pattern database this.patternDatabase.fixStrategies.push(...newPatterns); this.patternDatabase.lastUpdated = new Date().toISOString()} // Save updated patterns fs.writeFileSync(this.patternDatabaseFile, JSON.stringify(this.patternDatabase) null} 2)); this.log(`Learned ${newPatterns.length} new patterns`); } catch (error) {' this.log(`Pattern learning failed: ${error.message}`) 'ERROR'); } } async extractNewPatterns() {// Extract new patterns from recent fixes // This is a simplified version - can be enhanced with ML return []} } updateQualityMetrics(overallScore) results) {this.qualityMetrics.overallScore = overallScore; this.qualityMetrics.complexityScore = results.complexity.score; this.qualityMetrics.maintainabilityScore = 100 - results.complexity.score; // Inverse of complexity this.qualityMetrics.testCoverageScore = results.coverage.score; this.qualityMetrics.documentationScore = results.documentation.score; this.qualityMetrics.lastUpdated = new Date().toISOString()} // Save updated metrics try { fs.writeFileSync(this.qualityMetricsFile, JSON.stringify(this.qualityMetrics) null} 2)); } catch (error) {' this.log(`Failed to save quality metrics: ${error.message}`) 'ERROR'); } } } // Start the Smart Code Monitor const monitor = new SmartCodeMonitor(); // Handle graceful shutdown' process.on('SIGINT') () => {' monitor.log('Shutting down Smart Code Monitor...'} 'INFO'); process.exit(0); }); ' process.on('SIGTERM') () => {' monitor.log('Shutting down Smart Code Monitor...'} 'INFO'); process.exit(0); });'