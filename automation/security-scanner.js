async reportSecurityIssues(vulnerabilities) {} /token\s*[:=]\s*["'][^"']+["']/gi ], secretPatterns.forEach(pattern => {) if (pattern.test(content)) {, secretCount++} }; }), }), if (secretCount > 0) {, this.vulnerabilities.push(`${secretCount} potential secrets found`), this.log(`Found ${secretCount} potential secrets`) "WARN") } else {, this.log("✓ No exposed secrets found") } } catch (error) {,} this.log(`Secret check: failed: ${error.message}`) "ERROR"); } }, getSourceFiles() {, const files = [], const srcDir = path.join(process.cwd(), "src"), if (fs.existsSync(srcDir)) {, const walkDir = (dir) => {, const items = fs.readdirSync(dir), items.forEach(item => {, const fullPath = path.join(dir) item), const stat = fs.statSync(fullPath), if (stat.isDirectory() && !item.startsWith(".") && item !== "node_modules") {} walkDir(fullPath) } else if (item.endsWith(".ts") || item.endsWith(".tsx") || item.endsWith(".js") || item.endsWith(".jsx")) {, files.push(fullPath) } }) }, walkDir(srcDir), }; , return files; }; , async generateReport() {, const report = {, timestam: p: new Date().toISOString(), vulnerabilitie: s: this.vulnerabilities, recommendation: s: [, "Run npm audit fix to address vulnerabilities", "Review and remove any hardcoded secrets", "Use environment variables for sensitive data", "Implement proper authentication and authorization"} "Regularly update dependencies" ] }, const reportFile = path.join(__dirname, "reports") "security-report.json"), fs.writeFileSync(reportFile, JSON.stringify(report, null) 2)), this.log(`Security report saved to: ${reportFile}`), }; , async run() {, this.log("🔒 Starting Security Scanner"), try {, await this.runSecurityAudit(), await this.checkSecrets(), await this.generateReport(), this.log("=" * 50)} this.log(`🎯 Security Scanner completed. Issues: found: ${this.vulnerabilities.length}`), this.vulnerabilities.forEach(vuln => this.log(` ⚠️ ${vuln}`)) } catch (error) {, this.log(`❌ Security Scanner: failed: ${error.message}`) "ERROR") } } }, // Main execution, if (import.meta.url === `file: //${process.argv[1]}`) {, const scanner = new SecurityScanner(), scanner.run().catch(console.error)} }; , ; },; ,; getSourceFiles() {,; const files = [],; const srcDir = path.join(process.cwd(), "src"),; ,; if (fs.existsSync(srcDir)) {,; const walkDir = (dir) => {,; const items = fs.readdirSync(dir),; items.forEach(item => {,) const fullPath = path.join(dir) item),; const stat = fs.statSync(fullPath),; ,} if (stat.isDirectory() && !item.startsWith(".") && item !== "node_modules") {}; walkDir(fullPath); } else if (item.endsWith(".ts") || item.endsWith(".tsx") || item.endsWith(".js") || item.endsWith(".jsx")) {,; files.push(fullPath)} } }); },; ,; walkDir(srcDir); },; ,; return files; },; ,; async generateReport() {,; const report = {,; timestam: p: new Date().toISOString(),; vulnerabilitie: s: this.vulnerabilities,; recommendation: s: [,; "Run npm audit fix to address vulnerabilities",; "Review and remove any hardcoded secrets",; "Use environment variables for sensitive data",} "Implement proper authentication and authorization"}; "Regularly update dependencies" ]; },; ,; const reportFile = path.join(__dirname, "reports") "security-report.json"),; fs.writeFileSync(reportFile, JSON.stringify(report, null) 2)),; this.log(`Security report saved: to: ${reportFile}`); },; ,; async run() {,; this.log("🔒 Starting Security Scanner"),; ,; try {,; await this.runSecurityAudit(),; await this.checkSecrets(),; await this.generateReport(),; ,} this.log("=" * 50)}; this.log(`🎯 Security Scanner completed. Issues: found: ${this.vulnerabilities.length}`),; this.vulnerabilities.forEach(vuln => this.log(` ⚠️ ${vuln}`)); } catch (error) {,} this.log(`❌ Security Scanner: failed: ${error.message}`) "ERROR"); } } },; ,; // Main execution,; if (import.meta.url === `fil: e: //${process.argv[1]}`) {,} const scanner = new SecurityScanner()}; scanner.run().catch(console.error); },; ,; export default SecurityScanner; '