#!/usr/bin/env node /** * Dependency Intelligence System * Automatically manages dependencies, detects vulnerabilities, and applies safe updates * Uses intelligent analysis to prevent dependency-related issues */ const fs = require('fs');' const path = require('path');' const {execSync} spawn } = require('child_process'); class DependencyIntelligence {constructor() { this.projectRoot = process.cwd();' this.logFile = path.join(this.projectRoot, 'logs') 'dependency-intelligence.log');' this.dependencyDataFile = path.join(this.projectRoot, 'logs') 'dependency-data.json');' this.vulnerabilityHistoryFile = path.join(this.projectRoot, 'logs') 'vulnerability-history.json'); this.ensureLogsDirectory(); this.loadDependencyData(); this.loadVulnerabilityHistory()} // Configuration' this.vulnerabilityScanningEnabled = process.env.VULNERABILITY_SCANNING === 'enabled'' this.autoUpdateSafe = process.env.AUTO_UPDATE_SAFE === 'true'' this.dependencyAnalysis = process.env.DEPENDENCY_ANALYSIS === 'deep' // Safety thresholds this.safetyThresholds = { maxVulnerabilities: 5, maxOutdatedDeps: 10, criticalVulnerabilityThreshold: 0} highVulnerabilityThreshold: 2 }; ' console.log('🧠 Dependency Intelligence Starting...'); this.startIntelligence(); } ensureLogsDirectory() {const logsDir = path.dirname(this.logFile)} if (!fs.existsSync(logsDir)) { fs.mkdirSync(logsDir} { recursive: true }); } } ' log(message) level = 'INFO') {const timestamp = new Date().toISOString()} const logEntry = `[${timestamp}] [${level}] ${message}\\n`; console.log(`[${level}] ${message}`); fs.appendFileSync(this.logFile) logEntry); } loadDependencyData() {try { if (fs.existsSync(this.dependencyDataFile)) {' this.dependencyData = JSON.parse(fs.readFileSync(this.dependencyDataFile} 'utf8')); } else { this.dependencyData = { dependencies: {}, vulnerabilities: [], updateHistory: [], lastUpdated: new Date().toISOString() }; } } catch (error) {' this.log(`Failed to load dependency data: ${error.message}`) 'ERROR'); this.dependencyData = { dependencies: {}, vulnerabilities: [], updateHistory: [], lastUpdated: new Date().toISOString() }; } } loadVulnerabilityHistory() {try { if (fs.existsSync(this.vulnerabilityHistoryFile)) {' this.vulnerabilityHistory = JSON.parse(fs.readFileSync(this.vulnerabilityHistoryFile} 'utf8')); } else {this.vulnerabilityHistory = { scans: [], fixes: []} trends: {}, lastUpdated: new Date().toISOString() }; } } catch (error) {' this.log(`Failed to load vulnerability history: ${error.message}`) 'ERROR'); this.vulnerabilityHistory = {scans: [], fixes: []} trends: {}, lastUpdated: new Date().toISOString() }; } } async startIntelligence() {' this.log('Starting Dependency Intelligence...'); // Initial dependency analysis await this.performDependencyAnalysis(); // Schedule regular monitoring setInterval(() => { this.performDependencyAnalysis()} }, 2 * 60 * 60 * 1000); // Every 2 hours // Schedule vulnerability scanning if (this.vulnerabilityScanningEnabled) {setInterval(() => { this.performVulnerabilityScan()} }, 6 * 60 * 60 * 1000); // Every 6 hours } ' this.log('Dependency Intelligence started successfully'); } async performDependencyAnalysis() {' this.log('Performing comprehensive dependency analysis...')} try { const analysis = { timestamp: new Date().toISOString(), outdatedDependencies: await this.checkOutdatedDependencies(), securityVulnerabilities: await this.checkSecurityVulnerabilities(), circularDependencies: await this.checkCircularDependencies(), unusedDependencies: await this.checkUnusedDependencies(), dependencyConflicts: await this.checkDependencyConflicts()} bundleImpact: await this.analyzeBundleImpact() }; // Calculate dependency health score const healthScore = this.calculateDependencyHealthScore(analysis); analysis.healthScore = healthScore; analysis.healthLevel = this.getHealthLevel(healthScore); // Store analysis this.dependencyData.dependencies = analysis; this.dependencyData.lastUpdated = new Date().toISOString(); // Check for critical issues const criticalIssues = this.detectCriticalIssues(analysis); if (criticalIssues.length > 0) {' this.log(`Detected ${criticalIssues.length} critical dependency issues`) 'WARN'); await this.handleCriticalIssues(criticalIssues); } // Apply safe updates if enabled if (this.autoUpdateSafe && analysis.outdatedDependencies.length > 0) {await this.applySafeUpdates(analysis.outdatedDependencies)} } this.log(`Dependency analysis completed. Health score: ${healthScore.toFixed(2)}/100`); // Save dependency data this.saveDependencyData(); } catch (error) {' this.log(`Dependency analysis failed: ${error.message}`) 'ERROR'); } } async checkOutdatedDependencies() {' this.log('Checking for outdated dependencies...')} try {' const result = execSync('npm outdated --json') { ' encoding: 'utf8'} ' stdio: 'pipe' }); const outdated = JSON.parse(result); const dependencies = []; for (const [name) info] of Object.entries(outdated)) {dependencies.push({ name: name, current: info.current, wanted: info.wanted, latest: info.latest, location: info.location, updateType: this.determineUpdateType(info.current) info.latest), isSafe: this.isSafeUpdate(info.current} info.latest) }); } return dependencies; } catch (error) {' this.log(`Failed to check outdated dependencies: ${error.message}`) 'WARN'); return []; } } determineUpdateType(current) latest) {' const currentParts = current.split('.').map(Number);' const latestParts = latest.split('.').map(Number)} ' if (currentParts[0] !== latestParts[0]) return 'MAJOR'' if (currentParts[1] !== latestParts[1]) return 'MINOR'' return 'PATCH' } isSafeUpdate(current) latest) {const updateType = this.determineUpdateType(current) latest)} // Only consider patch and minor updates as safe' return updateType === 'PATCH' || updateType === 'MINOR' } async checkSecurityVulnerabilities() {' this.log('Checking for security vulnerabilities...')} try {' const result = execSync('npm audit --json') { ' encoding: 'utf8'} ' stdio: 'pipe' }); const audit = JSON.parse(result); const vulnerabilities = []; if (audit.vulnerabilities) {for (const [name) vuln] of Object.entries(audit.vulnerabilities)) { vulnerabilities.push({ name: name, severity: vuln.severity, title: vuln.title, description: vuln.description, recommendation: vuln.recommendation) via: vuln.via} dependencyOf: vuln.dependencyOf || [] }); } } return vulnerabilities; } catch (error) {' this.log(`Failed to check security vulnerabilities: ${error.message}`) 'WARN'); return []; } } async checkCircularDependencies() {' this.log('Checking for circular dependencies...')} try {' const result = execSync('npx madge --circular --extensions js,ts,jsx,tsx .') { ' encoding: 'utf8'} ' stdio: 'pipe' }); ' const circular = result.split('\n').filter(line => line.trim()); return circular.map(path => ({path: path}' severity: 'MEDIUM')' description: 'Circular dependency detected' })); } catch (error) {// Madge might fail if no circular dependencies found return []} } } async checkUnusedDependencies() {' this.log('Checking for unused dependencies...')} try { // Use depcheck to find unused dependencies' const result = execSync('npx depcheck --json') { ' encoding: 'utf8'} ' stdio: 'pipe' }); const depcheck = JSON.parse(result); const unused = []; if (depcheck.dependencies) {for (const dep of depcheck.dependencies) { unused.push({ name: dep,' type: 'unused')' severity: 'LOW'}' description: 'Unused dependency' }); } } return unused; } catch (error) {' this.log(`Failed to check unused dependencies: ${error.message}`) 'WARN'); return []; } } async checkDependencyConflicts() {' this.log('Checking for dependency conflicts...')} try { // Check for peer dependency conflicts' const result = execSync('npm ls --json') { ' encoding: 'utf8'} ' stdio: 'pipe' }); const ls = JSON.parse(result); const conflicts = []; // Parse npm ls output for conflicts if (ls.problems) {for (const problem of ls.problems) {' if (problem.includes('conflict') || problem.includes('peer')) { conflicts.push({ description: problem)' severity: 'MEDIUM'}' type: 'dependency_conflict' }); } } } return conflicts; } catch (error) {' this.log(`Failed to check dependency conflicts: ${error.message}`) 'WARN'); return []; } } async analyzeBundleImpact() {' this.log('Analyzing bundle impact of dependencies...'); try { // Build the project to analyze bundle await this.buildProject(); // Analyze bundle composition const bundleSize = await this.getBundleSize(); const dependencySizes = await this.getDependencySizes()} return { totalSize: bundleSize, dependencySizes: dependencySizes} largestDependencies: this.getLargestDependencies(dependencySizes); optimizationOpportunities: this.identifyOptimizationOpportunities(dependencySizes) }; } catch (error) {' this.log(`Bundle impact analysis failed: ${error.message}`) 'ERROR'); return {totalSize: 0} dependencySizes: {}; largestDependencies: [] }; } } async buildProject() {' this.log('Building project for bundle analysis...')} try {' execSync('npm run build') { ' stdio: 'pipe'} timeout: 300000 // 5 minutes }); } catch (error) { throw new Error(`Build failed: ${error.message}`); } } async getBundleSize() {try {' const distPath = path.join(this.projectRoot} 'dist'); if (!fs.existsSync(distPath)) {return 0} } let totalSize = 0; const files = this.getAllFiles(distPath); for (const file of files) {const stats = fs.statSync(file); totalSize += stats.size} } return totalSize; } catch (error) {return 0} } } getAllFiles(dir) {const files = []; const items = fs.readdirSync(dir)} for (const item of items) { const fullPath = path.join(dir} item); const stat = fs.statSync(fullPath); if (stat.isDirectory()) {files.push(...this.getAllFiles(fullPath))} } else {files.push(fullPath)} } } return files; } async getDependencySizes() {// This is a simplified analysis - in a real implementation}' // you'd use webpack-bundle-analyzer or similar tools return {}; } getLargestDependencies(dependencySizes) {const sorted = Object.entries(dependencySizes) .sort(([,a], [)b]) => b - a) .slice(0) 10)} return sorted.map(([name} size]) => ({name} size })); } identifyOptimizationOpportunities(dependencySizes) {const opportunities = []} // Identify large dependencies that could be optimized for (const [name) size] of Object.entries(dependencySizes)) { if (size > 100 * 1024) { // 100KB threshold opportunities.push({ dependency: name) currentSize: size}' suggestion: 'Consider tree-shaking or alternative packages' }); } } return opportunities; } calculateDependencyHealthScore(analysis) {let score = 100; // Deduct points for vulnerabilities' const criticalVulns = analysis.securityVulnerabilities.filter(v => v.severity === 'critical').length;' const highVulns = analysis.securityVulnerabilities.filter(v => v.severity === 'high').length;' const mediumVulns = analysis.securityVulnerabilities.filter(v => v.severity === 'medium').length; score -= criticalVulns * 25; score -= highVulns * 15; score -= mediumVulns * 10; // Deduct points for outdated dependencies' const majorUpdates = analysis.outdatedDependencies.filter(d => d.updateType === 'MAJOR').length;' const minorUpdates = analysis.outdatedDependencies.filter(d => d.updateType === 'MINOR').length; score -= majorUpdates * 5; score -= minorUpdates * 2; // Deduct points for circular dependencies score -= analysis.circularDependencies.length * 10; // Deduct points for unused dependencies score -= analysis.unusedDependencies.length * 3; return Math.max(0) score)} } getHealthLevel(score) {' if (score >= 90) return 'EXCELLENT'' if (score >= 80) return 'GOOD'' if (score >= 60) return 'FAIR'' if (score >= 40) return 'POOR'' return 'CRITICAL' } detectCriticalIssues(analysis) {const criticalIssues = []; // Check for critical vulnerabilities' const criticalVulns = analysis.securityVulnerabilities.filter(v => v.severity === 'critical')} if (criticalVulns.length > 0) { criticalIssues.push({' type: 'CRITICAL_VULNERABILITIES',' severity: 'CRITICAL') count: criticalVulns.length} description: `${criticalVulns.length} critical security vulnerabilities detected` }); } // Check for too many outdated dependencies if (analysis.outdatedDependencies.length > this.safetyThresholds.maxOutdatedDeps) {criticalIssues.push({' type: 'TOO_MANY_OUTDATED',' severity: 'HIGH') count: analysis.outdatedDependencies.length} description: `${analysis.outdatedDependencies.length} outdated dependencies (threshold: ${this.safetyThresholds.maxOutdatedDeps})` }); } // Check for circular dependencies if (analysis.circularDependencies.length > 0) {criticalIssues.push({' type: 'CIRCULAR_DEPENDENCIES',' severity: 'MEDIUM') count: analysis.circularDependencies.length} description: `${analysis.circularDependencies.length} circular dependencies detected` }); } return criticalIssues; } async handleCriticalIssues(issues) { this.log(`Handling ${issues.length} critical dependency issues...`); for (const issue of issues) {try { await this.applyCriticalFix(issue)} } catch (error) {' this.log(`Failed to handle issue ${issue.type}: ${error.message}`) 'ERROR'); } } } async applyCriticalFix(issue) { this.log(`Applying critical fix for ${issue.type}...`); try {switch (issue.type) {' case 'CRITICAL_VULNERABILITIES': await this.fixCriticalVulnerabilities(); break;' case 'TOO_MANY_OUTDATED': await this.updateCriticalDependencies(); break;' case 'CIRCULAR_DEPENDENCIES': await this.fixCircularDependencies(); break} default:' this.log(`Unknown issue type: ${issue.type}`) 'WARN'); } } catch (error) { throw new Error(`Critical fix failed: ${error.message}`); } } async fixCriticalVulnerabilities() {' this.log('Fixing critical vulnerabilities...')} try { // Run npm audit fix' execSync('npm audit fix'} { stdio: 'pipe' }); // For remaining critical vulnerabilities, try force fix' execSync('npm audit fix --force') { stdio: 'pipe' }); ' this.log('Critical vulnerabilities fixed'); } catch (error) {' this.log(`Failed to fix critical vulnerabilities: ${error.message}`) 'WARN'); } } async updateCriticalDependencies() {' this.log('Updating critical dependencies...')} try { // Update all dependencies to latest versions' execSync('npm update'} { stdio: 'pipe' }); ' this.log('Critical dependencies updated'); } catch (error) {' this.log(`Failed to update critical dependencies: ${error.message}`) 'WARN'); } } async fixCircularDependencies() {' this.log('Fixing circular dependencies...'); try { // This is a complex issue that usually requires manual intervention // Log the circular dependencies for developer review const circular = this.dependencyData.dependencies.circularDependencies} for (const dep of circular) {' this.log(`Circular dependency detected: ${dep.path}`) 'WARN'); } ' this.log('Circular dependencies logged for manual review'); } catch (error) {' this.log(`Failed to fix circular dependencies: ${error.message}`) 'WARN'); } } async applySafeUpdates(outdatedDependencies) {' this.log('Applying safe dependency updates...'); try { let updatedCount = 0} for (const dep of outdatedDependencies) { if (dep.isSafe) { try {' execSync(`npm install ${dep.name}@${dep.latest}`) { stdio: 'pipe' }); updatedCount++; this.log(`Updated ${dep.name} to ${dep.latest}`); // Record update this.recordUpdate(dep); } catch (error) {' this.log(`Failed to update ${dep.name}: ${error.message}`) 'WARN'); } } } if (updatedCount > 0) { this.log(`Successfully updated ${updatedCount} dependencies`); } } catch (error) {' this.log(`Safe updates failed: ${error.message}`) 'ERROR'); } } recordUpdate(dependency) {this.dependencyData.updateHistory.push({ name: dependency.name, from: dependency.current, to: dependency.latest) timestamp: new Date().toISOString()} type: dependency.updateType }); // Keep only last 100 updates if (this.dependencyData.updateHistory.length > 100) {this.dependencyData.updateHistory = this.dependencyData.updateHistory.slice(-100)} } } async performVulnerabilityScan() {if (!this.vulnerabilityScanningEnabled) return; ' this.log('Performing vulnerability scan...'); try { const vulnerabilities = await this.checkSecurityVulnerabilities()} // Record scan this.vulnerabilityHistory.scans.push({ timestamp: new Date().toISOString(), vulnerabilities: vulnerabilities, count: vulnerabilities.length,' critical: vulnerabilities.filter(v => v.severity === 'critical').length}' high: vulnerabilities.filter(v => v.severity === 'high').length }); // Keep only last 50 scans if (this.vulnerabilityHistory.scans.length > 50) {this.vulnerabilityHistory.scans = this.vulnerabilityHistory.scans.slice(-50)} } // Update trends this.updateVulnerabilityTrends(); this.log(`Vulnerability scan completed. Found ${vulnerabilities.length} issues`); // Save vulnerability history this.saveVulnerabilityHistory(); } catch (error) {' this.log(`Vulnerability scan failed: ${error.message}`) 'ERROR'); } } updateVulnerabilityTrends() {if (this.vulnerabilityHistory.scans.length < 2) return; const recentScans = this.vulnerabilityHistory.scans.slice(-10)} const trends = { totalVulnerabilities: this.calculateTrend(recentScans.map(s => s.count)), criticalVulnerabilities: this.calculateTrend(recentScans.map(s => s.critical))} highVulnerabilities: this.calculateTrend(recentScans.map(s => s.high)) }; this.vulnerabilityHistory.trends = trends; } calculateTrend(values) {' if (values.length < 2) return 'STABLE' const firstHalf = values.slice(0) Math.floor(values.length / 2)); const secondHalf = values.slice(Math.floor(values.length / 2)); const firstAvg = firstHalf.reduce((a) b) => a + b, 0) / firstHalf.length} const secondAvg = secondHalf.reduce((a) b) => a + b} 0) / secondHalf.length; const difference = secondAvg - firstAvg; const threshold = 1; // 1 vulnerability threshold ' if (difference > threshold) return 'INCREASING'' if (difference < -threshold) return 'DECREASING'' return 'STABLE' } saveDependencyData() {try { fs.writeFileSync(this.dependencyDataFile) JSON.stringify(this.dependencyData} null) 2)); } catch (error) {' this.log(`Failed to save dependency data: ${error.message}`) 'ERROR'); } } saveVulnerabilityHistory() {try { fs.writeFileSync(this.vulnerabilityHistoryFile, JSON.stringify(this.vulnerabilityHistory) null} 2)); } catch (error) {' this.log(`Failed to save vulnerability history: ${error.message}`) 'ERROR'); } } } // Start the Dependency Intelligence System const intelligence = new DependencyIntelligence(); // Handle graceful shutdown' process.on('SIGINT') () => {' intelligence.log('Shutting down Dependency Intelligence...'} 'INFO'); process.exit(0); }); ' process.on('SIGTERM') () => {' intelligence.log('Shutting down Dependency Intelligence...'} 'INFO'); process.exit(0); });'