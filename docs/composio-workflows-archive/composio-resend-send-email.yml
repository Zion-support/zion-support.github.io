name: composio-resend-send-email

on:
  workflow_dispatch:
    inputs:
      to:
        description: 'Recipient email'
        required: true
        default: 'kleber@ziontechgroup.com'
      subject:
        description: 'Email subject'
        required: true
        default: 'Zion Composio test'
      body:
        description: 'Email body'
        required: true
        default: 'Test email sent via Composio Resend workflow'

jobs:
  send:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Composio SDK
        run: npm install @composio/core

      - name: Send email via Resend
        id: resend
        env:
          COMPOSIO_API_KEY: ${{ secrets.COMPOSIO_API_KEY }}
          COMPOSIO_RESEND_CONNECTION_ID: ${{ secrets.COMPOSIO_RESEND_CONNECTION_ID }}
        run: |
          cat > send.mjs <<'EOF'
          import { Composio } from '@composio/core';
          const composio = new Composio({ apiKey: process.env.COMPOSIO_API_KEY });
          const client = composio.getClient();
          const connectionId = process.env.COMPOSIO_RESEND_CONNECTION_ID;
          const result = await client.tools.execute('RESEND_SEND_EMAIL', {
            connection_id: connectionId,
            entity_id: 'pg-test-b311dcc6-03f6-4077-8774-c90cfd6fcf29',
            arguments: {
              to: '${{ github.event.inputs.to }}',
              subject: '${{ github.event.inputs.subject }}',
              body: '${{ github.event.inputs.body }}',
              from: 'Zion <noreply@ziontechgroup.com>'
            }
          });
          console.log(JSON.stringify(result, null, 2));
          EOF
          node send.mjs
