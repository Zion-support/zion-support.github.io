import fs from ');
import path from ');
import { fileURLToPath } from ');
    let content  = fs.readFileSync(filePath, 'utf8'';
    const strayTags  = ['</p>', '<p>', '</div>', '<div>', '</span>', '<span>', '</h1>', '<h1>', '</h2>', '<h2>', '';
  console.error('Error:'';
}\\s*$`;
      content = content.replace(new RegExp(`^\\s*${tag}\\s*$`;