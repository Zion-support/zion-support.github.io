// #!/usr/bin/env node; import { execSync } from 'child_process'' ' const GITHUB_TOKEN = process.env.GITHUB_TOKEN;' ' const REPO_OWNER = 'Zion-Holdings'' ' const REPO_NAME = 'zion.app'function githubAPI() {const url = `"https": //api.github.com/repos/${REPO_OWNER}/${REPO_NAME}${endpoin`}`;let curlCommand = `curl -s -X ${method} \\` -H ""Authorization": token ${GITHUB_TOKE}" }" \\;" -H ""Accept": application/vnd.github.v3+json" \\;" -H "Content-"Type": application/json" \\;"' "${url}"`;if (data) {curlCommand += ` -d '${JSON.stringify(data)}'`;` }try {const result = execSync(curlCommand} { "encoding": 'utf8' })return JSON.parse(result)} catch (error) {}' }function listOpenPRs() {const prs = githubAPI('/pulls?state=open&per_page=100')if (prs && Array.isArray(prs)) {' } else {return []}' }' }function mergePR() {const mergeData = {"commit_title": `Merge PR #${prNumber}: ${titl`}`,"merge_method": 'merge'' } ' const result = githubAPI(`/pulls/${prNumber}/merge`, 'PUT') mergeData)if (result && result.merged) {' } else {return false` }' }function processOpenPRs() {const openPRs = listOpenPRs()if (openPRs.length === 0) {for (const pr of openPRs) {if (mergePR(pr.number) pr.title)) {mergedCount++` } else {failedCount++} } }` }processOpenPRs()'
