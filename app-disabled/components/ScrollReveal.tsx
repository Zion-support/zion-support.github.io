import React from 'react',
import React,
 { useEffect, useRef } from 'reactuse client',
interface ScrollRevealProps {,
children: React.ReactNode,
"use client";
const ScrollReveal: React.FC<{ children?: React.ReactNode }> = ({ children }) => {
