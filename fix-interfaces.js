import fs from 'fs';' import path from 'path';' import { fileURLToPath } from 'url'; const __filename = fileURLToPath(import.meta.url); const __dirname = path.dirname(__filename); // Function to fix interface definitions with extra commas function fixInterfaceErrors(filePath) { try {' let content = fs.readFileSync(filePath, 'utf8'); let modified = false; // Fix interface properties with extra commas after semicolons const interfaceRegex = /interface\\s+\\w+\\s*{([^}]+)}/g; content = content.replace(interfaceRegex, (match, body) => {' const lines = body.split('\n'); const fixedLines = lines.map(line => { // Remove extra commas after semicolons in interface properties' return line.replace(/;,\s*$/, ';'); }); ' const fixedBody = fixedLines.join('\n'); if (fixedBody !== body) { modified = true; return `interface ${match.match(/interface\\s+(\\w+)/)[1]} {${fixedBody}}`; } return match; }); // Fix type definitions with extra commas const typeRegex = /type\\s+\\w+\\s*=\\s*{([^}]+)}/g; content = content.replace(typeRegex, (match, body) => {' const lines = body.split('\n'); const fixedLines = lines.map(line => {' return line.replace(/;,\s*$/, ';'); }); ' const fixedBody = fixedLines.join('\n'); if (fixedBody !== body) { modified = true; return `type ${match.match(/type\\s+(\\w+)/)[1]} = {${fixedBody}}`; } return match; }); if (modified) {' fs.writeFileSync(filePath, content, 'utf8'); console.log(`Fixed interface errors in: ${filePath}`); } } catch (error) { console.error(`Error processing ${filePath}:`, error.message); } } // Find all TypeScript files function findTsFiles(dir) { const files = []; const items = fs.readdirSync(dir); for (const item of items) { const fullPath = path.join(dir, item); const stat = fs.statSync(fullPath); ' if (stat.isDirectory() && !item.startsWith('.') && item !== 'node_modules') { files.push(...findTsFiles(fullPath));' } else if (stat.isFile() && (item.endsWith('.ts') || item.endsWith('.tsx'))) { files.push(fullPath); } } return files; } // Process all files' const srcDir = path.join(__dirname, 'src'); const files = findTsFiles(srcDir); console.log(`Found ${files.length} TypeScript files to process...`); files.forEach(file => { fixInterfaceErrors(file); }); ' console.log('Interface fixing complete!');'
