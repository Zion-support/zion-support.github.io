<<<<<<< HEAD
name: autonomous-cloud
on:
  workflow_dispatch: {}
  schedule:
  - cron: '0 2 * * *'
permissions:
  contents: read
  actions: read
  
concurrency:
  group: autonomous-cloud-\${{ github.ref }}
  cancel-in-progress: false
jobs:
  main:
    name: Autonomous Cloud Job
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
      - name: Install dependencies
        run: npm ci --no-audit --no-fund
      - name: Run workflow
        run: echo "Workflow completed successfully"
=======
name: Autonomous Cloud Automations

on:
  schedule:
    - cron: '*/30 * * * *' # every 30 minutes
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  run-automations:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install deps
        run: npm ci --prefer-offline --no-audit

      - name: Run automations
        run: |
          node automation/auto-health-monitor/index.mjs || true
          node automation/sitemap-maintenance-agents/generate-sitemap.cjs || true
          node automation/autonomous-cloud/ai-talent-intake.cjs || true
          node automation/autonomous-cloud/partner-kpis.cjs || true
          node automation/autonomous-cloud/impact-metrics.cjs || true
          node automation/git-health.cjs --fast || true

      - name: Build site
        run: npm run build

      - name: Commit changes
        run: |
          git config user.name "autonomous-bot"
          git config user.email "bot@users.noreply.github.com"
          git add -A
          if ! git diff --cached --quiet; then
            git commit -m "chore(automations): autonomous cloud updates [skip ci]"
            git push
          else
            echo "No changes to commit"
          fi
>>>>>>> origin/auto/autonomy-17186719616
