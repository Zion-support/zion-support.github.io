name: Zion Revenue Pipeline v2.0

# Disabled: .github/scripts/composio/revenue-pipeline-v2.mjs is missing.
# Revenue pulse lives in Zion Daily Growth Loop + Stripe/Calendly automations.
# Do not remint Composio Connect Links from CI.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  revenue-pipeline:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Guard
        run: |
          echo "Revenue Pipeline v2 is disabled. Script revenue-pipeline-v2.mjs is not in the repo."
          exit 1
