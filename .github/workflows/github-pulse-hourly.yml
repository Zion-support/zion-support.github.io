name: GitHub Pulse (Hourly)

on:
  schedule:
    - cron: '11 * * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  pulse:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20.x
          cache: 'npm'
      - name: Install deps
        run: npm ci
      - name: Run GitHub Pulse
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: node automation/github-pulse.cjs
      - name: Commit and push if changed
        run: |
          if [ -n "$(git status --porcelain data/github-pulse.json)" ]; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add data/github-pulse.json
            git commit -m "chore(automation): update GitHub Pulse data"
            git push origin HEAD:main
          else
            echo "No changes to commit."
          fi