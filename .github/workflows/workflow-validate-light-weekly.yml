name: Workflow validate light (weekly)

permissions:
  contents: read

on:
  schedule:
    - cron: '35 11 * * 3' # Wednesdays 11:35 UTC — validate:light (Ruby YAML contract + bash grep guards + bash -n on push helpers)
  workflow_dispatch:

concurrency:
  group: workflow-validate-light-weekly
  cancel-in-progress: true

jobs:
  validate-light:
    uses: ./.github/workflows/reusable-workflow-ci.yml
    timeout-minutes: 12
