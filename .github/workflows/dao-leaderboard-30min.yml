name: DAO Leaderboard (30-min)

on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  dao-leaderboard:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20.x
          cache: 'npm'
      - name: Install deps
        run: npm ci
      - name: Run DAO Leaderboard
        run: node automation/dao-leaderboard.cjs
      - name: Commit and push if changed
        run: |
          if [ -n "$(git status --porcelain data/dao-leaderboard.json)" ]; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add data/dao-leaderboard.json
            git commit -m "chore(automation): update DAO leaderboard"
            git push origin HEAD:main
          else
            echo "No changes to commit."
          fi