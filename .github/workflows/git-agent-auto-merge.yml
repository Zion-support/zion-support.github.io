# Git Integration Agent
on:
  schedule:
    - cron: "0 */6 * * *"
jobs:
  run-agent:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: python automation/git_integration_bot.py

