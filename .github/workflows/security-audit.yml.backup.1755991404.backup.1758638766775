<<<<<<< HEAD
name: Weekly Security Audit

on:
  schedule:
    - cron: '41 4 * * 1'
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

=======
name: Security Audit

on:
  schedule:
    - cron: '30 2 * * *'
  workflow_dispatch:

>>>>>>> origin/cursor/design-zion-ai-marketplace-mobile-app-ui-348a
jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
<<<<<<< HEAD
      - name: Checkout
        uses: actions/checkout@v4
      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20.x
          cache: npm
      - name: Install deps
        run: npm ci
      - name: Run audit and save report
        run: node automation/security-audit.cjs
      - name: Create Pull Request
        id: cpr
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: 'chore(security): update npm audit report'
          title: 'chore(security): npm audit report'
          body: 'Weekly npm audit report in data/security/npm-audit.json'
          branch: automation/security-audit
          delete-branch: true
          labels: automation, security
=======
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: npm ci
      - run: npm run security:audit
      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add public/automation/security-audit.json automation_logs/security-audit.log || true
          if ! git diff --cached --quiet; then
            git commit -m "chore(automation): update security audit [skip ci]"
            git push
          else
            echo "No changes to commit"
          fi
>>>>>>> origin/cursor/design-zion-ai-marketplace-mobile-app-ui-348a
