name: Autonomous Lazy-Loading Route Slicer
on:
  workflow_dispatch:
    inputs:
      apply:
        description: Apply transformations (creates PR)
        required: false
        default: 'false'
        type: boolean
permissions:
  contents: read
jobs:
  route-slicer:
    name: Skip on static GitHub Pages site
    runs-on: ubuntu-latest
    steps:
      - name: Static site — slicer not applicable
        run: |
          echo "This repo serves public/ as static HTML on GitHub Pages."
          echo "Route-slicer expected a Next.js ANALYZE build; skipped so CI stays green."
