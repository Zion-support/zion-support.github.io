name: Automation Guardian

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

permissions:
  contents: write
  actions: read

concurrency:
  group: guardian-scheduler-${{ github.ref }}
  cancel-in-progress: true

jobs:
  guardian:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          persist-credentials: true
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.18.1'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run Automation Guardian (single cycle)
        env:
          TARGET_BRANCH: main
          PUSH_TO_MAIN: 'true'
          NODE_ENV: production
        run: node automation/automation-guardian-10min.cjs
