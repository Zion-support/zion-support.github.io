name: StackExchange Insights (15-min)

on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  se:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20.x
          cache: 'npm'
      - name: Install deps
        run: npm ci
      - name: Run StackExchange Insights
        run: node automation/stackexchange-insights.cjs
      - name: Commit and push if changed
        run: |
          if [ -n "$(git status --porcelain data/stackexchange-insights.json)" ]; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add data/stackexchange-insights.json
            git commit -m "chore(automation): update StackExchange Insights data"
            git push origin HEAD:main
          else
            echo "No changes to commit."
          fi