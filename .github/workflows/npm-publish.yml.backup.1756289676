name: Publish npm package

on:
  push:
    branches:
      - main
    paths-ignore:
      - '**/*.md'
      - '.github/**'
      - 'docs/**'

jobs:
  publish:
    name: Publish to npm
    runs-on: ubuntu-latest
    if: ${{ !contains(github.event.head_commit.message, '[skip ci]') }}
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

<<<<<<< HEAD
      - name: Setup Node.js
=======
      - name: Use Node.js 20
>>>>>>> origin/cursor/website-audit-and-enhancement-4b68
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org'

      - name: Install dependencies
        run: npm ci

<<<<<<< HEAD
      - name: Check if package is private
        id: check-private
        run: |
          if npm run --silent echo:private 2>/dev/null | grep -q "true"; then
            echo "private=true" >> $GITHUB_OUTPUT
          else
            echo "private=false" >> $GITHUB_OUTPUT
          fi

      - name: Skip publish for private packages
        if: steps.check-private.outputs.private == 'true'
        run: |
          echo "Package is private, skipping publish"
          exit 0

      - name: Run tests
        if: steps.check-private.outputs.private == 'false'
        run: npm test --if-present
=======
      - name: Build project
        run: npm run build
>>>>>>> origin/cursor/website-audit-and-enhancement-4b68

      - name: Build project
        if: steps.check-private.outputs.private == 'false'
        run: npm run build

      - name: Publish to npm
        if: steps.check-private.outputs.private == 'false'
        run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish success
        if: steps.check-private.outputs.private == 'false'
        run: echo "Package published successfully to npm"
