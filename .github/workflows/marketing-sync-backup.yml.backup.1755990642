# Original workflow: Marketing Sync
# Backup workflow - Auto-generated by redundancy manager
name: marketing-sync-backup

on:
  schedule:
    - cron: '0 */24 * * *'  # Daily backup
    - cron: '0 */12 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  run-marketing-sync:
    runs-on: ubuntu-latest
    steps:
name: marketing-sync-backup
        uses: actions/checkout@v4

name: marketing-sync-backup
        uses: actions/setup-node@v4
        with:
          node-version: '20'

name: marketing-sync-backup
        env:
          LINKEDIN_ACCESS_TOKEN: ${{ secrets.LINKEDIN_ACCESS_TOKEN }}
          LINKEDIN_URN: ${{ secrets.LINKEDIN_URN }}
          IG_USER_ID: ${{ secrets.IG_USER_ID }}
          IG_ACCESS_TOKEN: ${{ secrets.IG_ACCESS_TOKEN }}
        run: node automation/marketing-sync.js

name: marketing-sync-backup
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          if [ -n "$(git status --porcelain)" ]; then
            git add -A
            git commit -m "chore(marketing): update marketing-sync report"
            git push origin HEAD:main
          else
            echo "No changes to commit."
          fi


