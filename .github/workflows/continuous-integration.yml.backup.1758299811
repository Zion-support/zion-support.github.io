name: Continuous Integration

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
<<<<<<< HEAD
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Generate Netlify functions manifest
        run: npm run netlify:manifest

      - name: Lint, type-check and build (self-healing)
        run: npm run ci:heal

      - name: Security scan (non-blocking)
        run: npm run security:scan
        continue-on-error: true

  lint:
    name: Lint
    runs-on: ubuntu-latest
    timeout-minutes: 15
=======
  build-test:
    name: Build, Lint, Type-check, Test
    runs-on: ubuntu-latest
    timeout-minutes: 30
>>>>>>> origin/pm2-automation-fixes
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

<<<<<<< HEAD
      - name: ESLint (non-blocking)
        run: npm run lint
=======
      - name: Install Playwright (Chromium only)
        run: npx playwright install --with-deps chromium

      - name: Type check
        run: npm run type-check

      - name: Lint
        run: npm run lint -- --max-warnings 100 || echo "Linting completed with warnings/errors - continuing build"

      - name: Build
        env:
      NODE_OPTIONS: "--max-old-space-size=4096"
          CI: true
        run: npm run build

      - name: Smoke tests
        env:
          CI: true
        run: npm run test:smoke
>>>>>>> origin/pm2-automation-fixes
        continue-on-error: true


