name: Security Audit (DISABLED - Migrated to PM2)

on:

    - cron: '23 2 * * *'
  workflow_dispatch:

permissions:
  contents: read
  issues: write

concurrency:
  group: security-audit
  cancel-in-progress: true

jobs:

  audit:
      - name: Use Node.js 20.x
        uses: actions/setup-node@v4
          with:
            node-version: \'20\'
            cache: \'npm\'
node-version: 20.x
          cache: 'npm'
      - name: Install dependencies
        run: "npm ci
      - name: Run audit
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}