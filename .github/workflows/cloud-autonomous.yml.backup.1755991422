<<<<<<< HEAD
name: Cloud Autonomous Automations

on:
  push:
    branches:
      - '**'
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch: {}

concurrency:
  group: cloud-autonomous-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write
  pull-requests: write

jobs:
  run-automations:
    runs-on: ubuntu-latest
    timeout-minutes: 25
=======
name: Cloud Autonomous Automation

on:
  schedule:
    - cron: '*/15 * * * *' # every 15 minutes
  workflow_dispatch:

permissions:
  contents: write
  actions: read

jobs:
  autonomous:
    runs-on: ubuntu-latest
>>>>>>> origin/cursor/continuous-website-link-checker-and-fixer-d4cc
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

<<<<<<< HEAD
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
=======
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
>>>>>>> origin/cursor/continuous-website-link-checker-and-fixer-d4cc

      - name: Install dependencies
        run: npm ci

<<<<<<< HEAD
      - name: Run cloud autonomous automations
        run: node automation/cloud-autonomous-runner.cjs

      - name: Configure git user
        run: |
          git config user.name "zion-cloud-bot"
          git config user.email "cloud-bot@users.noreply.github.com"

      - name: Commit changes if any
        run: |
          if [ -n "$(git status --porcelain)" ]; then
            git add -A
            git commit -m "chore(cloud): autonomous automation run $(date -u +"%Y-%m-%dT%H:%M:%SZ")"
          else
            echo "No changes to commit"
          fi

      - name: Push changes
        if: success()
        run: |
          if [ -n "$(git log origin/${{ github.ref_name }}..HEAD)" ]; then
            git push origin HEAD:${{ github.ref_name }}
          else
            echo "No new commits to push"
          fi
=======
      - name: Run site link crawl+fix
        env:
          APP_MARKETING_URL: https://ziontechgroup.com
        run: |
          node automation/site-link-crawler.cjs | cat
          node automation/site-link-fixer.cjs | cat

      - name: Keep sitemap fresh
        run: node automation/sitemap-keeper.cjs | cat

      - name: SEO meta audit
        env:
          APP_MARKETING_URL: https://ziontechgroup.com
        run: node automation/seo-meta-audit.cjs | cat

      - name: Commit and push changes
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: node automation/cloud-auto-commit.cjs | cat

      - name: Upload automation artifacts
        uses: actions/upload-artifact@v4
        with:
          name: cloud-automation-artifacts
          path: |
            data/reports/**/*
            automation/logs/**/*
          if-no-files-found: ignore
>>>>>>> origin/cursor/continuous-website-link-checker-and-fixer-d4cc
