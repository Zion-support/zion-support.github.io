name: Release Drafter

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update_release_draft:
    runs-on: ubuntu-latest
    steps:
    branches: [ main ]  workflow_dispatch:

permissions:
  contents: write
  pull-requests: read

    branches: [ main ]  workflow_dispatch:

permissions:
  contents: write
  pull-requests: read
