name: Continuous Improvement

on:
  schedule:
<<<<<<< HEAD
    - cron: '0 2 * * 1'  # Weekly on Monday at 2 AM
=======
    - cron: '0 */4 * * *'
>>>>>>> origin/auto/autonomy-17186719616
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
<<<<<<< HEAD
  maintenance:
    name: Repository Maintenance
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
=======
  improve:
    runs-on: ubuntu-latest
>>>>>>> origin/auto/autonomy-17186719616
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
<<<<<<< HEAD
          
=======
>>>>>>> origin/auto/autonomy-17186719616
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20.x
          cache: 'npm'
<<<<<<< HEAD
          
      - name: Install dependencies
        run: npm ci
        
      - name: Run security audit
        run: npm audit --audit-level=moderate
        continue-on-error: true
        
      - name: Check for outdated packages
        run: npm outdated || true
        
      - name: Run linting
        run: npm run lint
        continue-on-error: true
        
      - name: Run type checking
        run: npm run type-check
        continue-on-error: true
        
      - name: Build project
        run: npm run build
        continue-on-error: true

      - name: Create maintenance report
        run: |
          echo "# Repository Maintenance Report" > MAINTENANCE_REPORT.md
          echo "" >> MAINTENANCE_REPORT.md
          echo "Generated on: $(date)" >> MAINTENANCE_REPORT.md
          echo "" >> MAINTENANCE_REPORT.md
          echo "## Security Audit" >> MAINTENANCE_REPORT.md
          npm audit --audit-level=moderate --json | jq -r '.metadata.vulnerabilities | to_entries[] | "- \(.key): \(.value)"' >> MAINTENANCE_REPORT.md 2>/dev/null || echo "- No vulnerabilities found" >> MAINTENANCE_REPORT.md
          echo "" >> MAINTENANCE_REPORT.md
          echo "## Outdated Packages" >> MAINTENANCE_REPORT.md
          npm outdated --json | jq -r 'to_entries[] | "- \(.key): \(.value.current) → \(.value.latest)"' >> MAINTENANCE_REPORT.md 2>/dev/null || echo "- All packages are up to date" >> MAINTENANCE_REPORT.md

=======
      - name: Install dependencies
        run: npm ci
      - name: Run continuous improvement
        run: |
          npm run automation:improvement || true
          npm run diversify || true
>>>>>>> origin/auto/autonomy-17186719616
      - name: Create Pull Request
        id: cpr
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
<<<<<<< HEAD
          commit-message: "chore(maintenance): weekly repository maintenance report"
          title: "chore(maintenance): weekly repository maintenance report"
          body: |
            Automated weekly maintenance report.
            
            This PR contains:
            - Security audit results
            - Outdated package information
            - Linting and type checking results
            - Build verification
            
            Review and merge if everything looks good.
          branch: maintenance/weekly-report
          delete-branch: true
          signoff: false
          labels: |
            maintenance
            automated
            weekly
=======
          commit-message: "chore(improvement): continuous improvement + diversity results"
          title: "chore(improvement): apply continuous improvement and diversity results"
          body: |
            Automated improvement and diversity loop.

            - Runs automation:improvement orchestration
            - Runs diversification orchestrator
          branch: automation/continuous-improvement
          delete-branch: true
          signoff: false
          labels: |
            automation
            improvement
            auto-merge
      - name: Enable Auto-merge
        if: steps.cpr.outputs.pull-request-number
        uses: peter-evans/enable-pull-request-automerge@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          pull-request-number: ${{ steps.cpr.outputs.pull-request-number }}
          merge-method: squash
      - name: Attempt immediate merge
        if: steps.cpr.outputs.pull-request-number
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const pr = Number(core.getInput('pr'))
            try {
              await github.pulls.merge({
                owner: context.repo.owner,
                repo: context.repo.repo,
                pull_number: pr,
                merge_method: 'squash'
              })
            } catch (e) {
              core.info(`Merge not completed yet: ${e.message}`)
            }
          result-encoding: string
          inputs: |
            pr: ${{ steps.cpr.outputs.pull-request-number }}
>>>>>>> origin/auto/autonomy-17186719616
