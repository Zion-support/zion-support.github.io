name: Autonomous Automations
on:
  schedule:
    - cron: '*/30 * * * *'
  push:
    branches: [ main ]
  workflow_dispatch: {}
jobs:
  run-automations:
    runs-on: ubuntu-latest
    permissions:
      contents: write