<<<<<<< HEAD
name: Knowledge Graph Builder

on:
  schedule:
    - cron: '*/50 * * * *'
  push:
    paths:
      - 'pages/**'
      - 'public/talent/index.json'
  workflow_dispatch: {}

permissions:
  contents: write
=======
name: Knowledge Graph

on:
  schedule:
    - cron: '0 3 * * *'
  workflow_dispatch:
>>>>>>> origin/cursor/design-zion-ai-marketplace-mobile-app-ui-348a

jobs:
  graph:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
<<<<<<< HEAD
      - name: Install deps
        run: npm ci --no-audit --no-fund
      - name: Build knowledge graph
        run: node automation/knowledge-graph-builder.cjs
      - name: Commit graph
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          if [[ -n "$(git status --porcelain)" ]]; then
            git add -A
=======
          cache: 'npm'
      - run: npm ci
      - run: npm run knowledge:graph
      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add public/automation/knowledge-graph.json automation_logs/knowledge-graph.log || true
          if ! git diff --cached --quiet; then
>>>>>>> origin/cursor/design-zion-ai-marketplace-mobile-app-ui-348a
            git commit -m "chore(automation): update knowledge graph [skip ci]"
            git push
          else
            echo "No changes to commit"
          fi