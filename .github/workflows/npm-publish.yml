# This workflow is disabled since this is a Next.js application, not an npm package
# To enable npm publishing, this repository would need to be converted to a package library

name: NPM Package Publishing (DISABLED)

on:
  # Disabled - no npm package to publish
  # push:
  #   branches:
  #     - main
  #   paths-ignore:
  #     - '**/*.md'

jobs:
  publish:
    runs-on: ubuntu-latest
    if: false  # Always skip this job
    steps:
      - name: Skip publishing
        run: |
          echo "This workflow is disabled"
          echo "This is a Next.js application, not an npm package"
          echo "To enable npm publishing, convert this to a package library"
