# This workflow is disabled since this is a private package
# and doesn't need to be published to npm
name: NPM Publish (Disabled)

on:
  # Disabled - no triggers
  # push:
  #   branches:
  #     - main
  #   paths-ignore:
  #     - '**/*.md'

jobs:
  # No jobs - workflow disabled
  # publish:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - name: Use Node.js 18
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: '18'
  #         registry-url: 'https://registry.npmjs.org'
  #     - name: Install dependencies
  #       run: npm ci
  #     - name: Run tests
  #       run: npm test --if-present
  #     - name: Publish to npm
  #       run: npm publish --access public
  #       env:
  #         NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
