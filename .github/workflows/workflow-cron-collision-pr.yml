name: Workflow cron collision report (PR / main)

permissions:
  contents: read

"on":
  pull_request:
    paths:
      - '.github/workflows/**'
      - 'automation/ai-cron-collision-tuner.cjs'
  push:
    branches: [main]
    paths:
      - '.github/workflows/**'
      - 'automation/ai-cron-collision-tuner.cjs'
  workflow_dispatch:

concurrency:
  group: workflow-cron-collision-pr-${{ github.ref }}
  cancel-in-progress: true

jobs:
  collision-report:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    env:
      NEXT_TELEMETRY_DISABLED: "1"
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup Node.js
        if: hashFiles('automation/ai-cron-collision-tuner.cjs') != ''
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version-file: '.nvmrc'
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        if: hashFiles('automation/ai-cron-collision-tuner.cjs') != ''
        run: npm ci --prefer-offline --no-audit --fund=false

      - name: Generate cron collision report
        run: |
          if [ -f automation/ai-cron-collision-tuner.cjs ]; then
            node automation/ai-cron-collision-tuner.cjs
          else
            echo "automation/ai-cron-collision-tuner.cjs is not on this branch; skipping"
          fi

      - name: Upload cron collision report
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: cron-collision-pr-${{ github.run_id }}
          path: |
            automation/reports/cron-collision-latest.json
            automation/reports/cron-collision-latest.md
          if-no-files-found: ignore
          retention-days: 14
