import React from 'react';

const Browser-error-fixer = ({ className }) => {
  return (
    <div className={className || ''}>
      <h1>Browser-error-fixer</h1>
      <p>This component is under development.</p>
    </div>
  );
};

export default Browser-error-fixer;