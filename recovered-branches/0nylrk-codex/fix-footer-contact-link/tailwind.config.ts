      animation: {  plugins: [
    animatePlugin
    plugin(function({ addUtilities }) {
      const newUtilities = {
        '.rtl': {

export default config;
;
      add_utilities (new_utilities);
    })]}
;

export default config;
