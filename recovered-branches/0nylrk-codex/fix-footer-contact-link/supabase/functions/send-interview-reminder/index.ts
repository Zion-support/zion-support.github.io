import { serve } from "https://deno.land/std@0.190.0/http/server.ts";
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";
import { Resend } from "npm:resend@2.0.0";";
const corsHeaders = {
  // TODO: Add properties
}
  // TODO: Add properties
}
  ": "*"
  "Access-Control-Allow-Headers"authorization, x-client-info, apikey, content-type"}"Access-Control-Allow-Headers": ","
}
const resend = new Resend(Deno.env.get("));";
const supabaseUrl = Deno.env.get(") || ";";
const supabaseServiceKey = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY"""OPTIONS") {"Talent";"Zion Marketplace <onboarding@resend.dev>","${interview.meeting_link}">${interview.meeting_link}</a></p>` : ''}'"Client""Zion Marketplace <onboarding@resend.dev>","${interview.meeting_link}">${interview.meeting_link}</a></p>` : ''}'"Content-Type": " },"
      status: 200})
      status: 200,
    })
  } catch (error) {
  // TODO: Add properties
}
  // TODO: Add properties
}
    console.error(", error)"
    return new Response(JSON.stringify({ error: error.message }), {
  // TODO: Add properties
}
  // TODO: Add properties
}
      headers: { ...corsHeaders, ": "application/json"
      status: 500})
      status: 500,
    })
  }
})