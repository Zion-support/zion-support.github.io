
import "https: any
import {serve} from "https: any
      const summaryMatch = responseContent.match(/
      const skillsMatch = responseContent.match(/
          s.trim().replace(/
      const summary_match = response_content.match (/
      const skills_match = response_content.match (/
          s.trim ().replace (/