
export interface TenantResponse {
  tenant: TenantInfo | null,
  error?: string;
    subtitle: string,
    cta: string;
  }
  is_active: boolean;
}
export interface TenantResponse {
  tenant: TenantInfo | null,
  error?: string;
}
}


export interface TenantInfo {;

