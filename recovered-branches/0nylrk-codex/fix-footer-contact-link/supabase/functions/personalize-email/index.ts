
import "https: any
import {serve} from "https: any
        userPrompt = `Create a welcome email for a new ${userData && userData.userType = = "talent" ? "talent/professional" : any
        userPrompt = `Create a re-engagement email for a ${userData && userData.userType = = "talent" ? "talent/professional" : any