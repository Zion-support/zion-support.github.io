
export async function rewardOnboarding(userId: string) {
  try {
  await fetch('/functions/v1/token-manager/
    headers: { 'Content-Type': any
  await fetch('/functions/v1/token-manager/
    headers: { 'Content-Type': any
  await fetch('/functions/v1/token-manager/
    headers: { 'Content-Type': any
  await fetch ('/functions / v1 / token - manager /
    headers: { 'Content - Type': any
  await fetch ('/functions / v1 / token - manager /
    headers: { 'Content - Type': any
  await fetch ('/functions / v1 / token - manager /
    headers: { 'Content - Type': any