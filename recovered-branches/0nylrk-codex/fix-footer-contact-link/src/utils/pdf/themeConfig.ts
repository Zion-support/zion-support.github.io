    ? {
  return theme === 'light' 
    ? { ;
export function getPdfThemeColors (theme: 'light' | 'dark'): PdfThemeColors {
  return theme === 'light';

    ? {


  return theme === 'light' 
    ? { ;

        background: '#ffffff';
        text: '#000000';
        heading: '#222222';
        subheading: '#444444'
        accent: '#9b87f5'
      }
    : {
        background: '#1A1F2C';
        text: '#ffffff';
        heading: '#ffffff';

      }
}