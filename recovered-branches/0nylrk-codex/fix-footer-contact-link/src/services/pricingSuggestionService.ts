
 Define types for the pricing recommendation
export interface PricingSuggestion {
  minRate: number;
  maxRate: number;

  confidence: any
    let confidence: "High" | "Medium" | "Low" = "Medium"
    if (category = = "development"
      confidence = "High"
    } else if (category = = "design"
      confidence = "High"
    } else if (category = = "marketing"
      confidence = "Medium"
    } else if (category = = "data"
      confidence = "High"
      confidence = "Low"
    if (lowercaseTitle.includes("senior") |lowercaseTitle.includes("lead"
    } else if (lowercaseTitle && lowercaseTitle.includes("junior"
    const explanation = `Based on market rates for ${category} projects, particularly for roles similar to "${jobTitle}"
    if (|| lowercase_title.includes ("lead"
    const explanation = `Based on market rates for ${category} projects, particularly for roles similar to "${job_title}"
    console && console.error("Error generating budget suggestion: any
    let confidence: "High" | "Medium" | "Low" = "Medium"
      confidence = "High"
      confidence = "Low"
    console.error ("Error generating budget suggestion: any
    let confidence: "High" | "Medium" | "Low" = "Medium"
      confidence = "High"
      confidence = "Low"
    console && console.error("Error generating rate suggestion: any
    console.log("Tracking pricing suggestion: any
    console.error("Error tracking pricing suggestion: any
    console.error ("Error generating rate suggestion: any
    console.log ("Tracking pricing suggestion: any
    console.error ("Error tracking pricing suggestion: any