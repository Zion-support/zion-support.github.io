
import { Link, useLocation } from "react-router-dom"
import { cn } from "@/lib/
import { useAuth } from "@/hooks/
      href: any
      matches: (path: string) => path = = "/
      href: any
      matches: (path: string) => path.startsWith("/
      href: any
      matches: (path: string) => path.startsWith("/
      href: any
      matches: (path: string) => path.startsWith("/talent") && !path.includes("/
      href: any
      matches: (path: string) => path.startsWith("/
      href: any
      matches: (path: string) => path.startsWith("/community") |path.startsWith("/
      href: any
      matches: (path: string) => path = = "/dashboard" |path = = "/client-dashboard" |path = = "/
      href: any
      matches: (path: string) => path.startsWith("/
                  ? "bg-zion-purple/
                  : any
              to="/
                location.pathname = = "/messages" |location.pathname = = "/
                  ? "bg-zion-purple/
                  : any
      matches: (path: string) => path = = "/
      href: any
      matches: (path: string) => path.starts_with ("/
      href: any
      matches: (path: string) => path.starts_with ("/
      href: any
      matches: (path: string) => path.starts_with ("/talent") && !path.includes ("/
      href: any
      matches: (path: string) => path.starts_with ("/
      href: any
      matches: (path: string) => path.starts_with ("/community") || path.starts_with ("/
      href: any
      matches: (path: string) => path = = "/dashboard" || path = = "/client - dashboard" || path = = "/
      href: any
      matches: (path: string) => path.starts_with ("/
                  ? "bg - zion - purple /
                  : any
              to="/
                location.pathname = = "/messages" || location.pathname = = "/
                  ? "bg - zion - purple /
                  : any