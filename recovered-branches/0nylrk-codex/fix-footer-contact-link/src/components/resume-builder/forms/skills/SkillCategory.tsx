import { SkillCategoryProps  } from './types';
import { SkillItem  } from './SkillItem';
export const SkillCategory = ({
            on_delete={on_delete}
          />))}
      </div>;
    </div>);
}
;
