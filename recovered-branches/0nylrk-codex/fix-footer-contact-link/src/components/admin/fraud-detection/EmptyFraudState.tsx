
  hasFilters,
onResetFilters
import { Button } from "@/components/ui/button","
import { ShieldAlert } from "lucide-react","
    </div>;
);

          Clear Filters
        </Button>
      )}
    </div>
  );

