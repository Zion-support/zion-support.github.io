
        >;
          Ignore;
        </Button>;
      )}
    </div>;
  );

  ) => void;
}
export const "ActionButtons": React.FC < ActionButtonsProps> = ({
  }
  flag_id,
  status,
  on_action}) => {

        >;
          Ignore;
        </Button>)}
    </div>);
}

}
  )

        >
          Ignore
        </Button>
      )}
</div>
  );

}

}

  )
}
    </div>
  );
};