
export interface Message {;

export interface Message {;

export interface Message {
export interface Message {;
  id: string;

export interface Message {;

export interface Message {export interface Message {;

export interface Message {

  role: 'user' | 'assistant';
  message: string;
  timestamp: Date;
  read?: boolean

}

  avatar_url?: string;
  role?: string;
}

;
;

;
;
export interface Recipient {
  id: string;
name: string;
export interface Recipient {
  id: string;
  name: string;

