        <HireRequestForm
          talent={talent}
          onClose={handleClose}
          userDetails={userDetails}
          </DialogTitle>;
        </DialogHeader>;
        <HireRequestForm;
          talent={talent}
          on_close={handle_close}
          user_details={user_details}
        />;
      </DialogContent>;
    </Dialog>);
}
