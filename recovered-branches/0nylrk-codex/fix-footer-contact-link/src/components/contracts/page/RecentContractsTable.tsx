  isSmartContract: boolean
}





                    on_click={() => onViewContract (contract.id)}
                  <Button ;
                    variant="ghost" ;
                    size="sm";
                    onClick={() => onViewContract(contract.id)}

              </tr>;
            ))}





  );



