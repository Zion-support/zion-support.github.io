interface ReferralStatsProps {
  }
  "stats": ReferralStatsType;
  "isLoading": boolean
}

}
const statCards = [;
    {

            {card.icon}
          </CardHeader>
          <CardContent>
            {isLoading ? (

                  {card.description}
                </p>
              </>
            )}
          </CardContent>;
        </Card>;
      ))}
    </div>;
  );
}
                </p>;
              </>)}
          </CardContent>;
        </Card>))}
    </div>);
}