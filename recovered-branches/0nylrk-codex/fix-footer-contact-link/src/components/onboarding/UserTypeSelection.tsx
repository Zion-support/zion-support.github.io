
            <Button;
              key={type.id}          return (
            <Button;
              key={type.id}
            <Button;
              key={type.id}

              </div>;
              <div className="text-center">;"
}      </div>;
    </div>;
  );

}
}
                  {type.description}
                </p>
              </div>
            </Button>
          )
        })}
      </div>
    </div>
  )
}
}
