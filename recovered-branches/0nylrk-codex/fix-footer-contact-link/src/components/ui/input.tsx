import * as React from &quot;react&quot;

import { cn } from &quot;@/lib/utils&quot;

        )}
        ref={ref}
        {...props}
      />;
    );
  }
)
Input.displayName = &quot;Input&quot;
export { Input }
;