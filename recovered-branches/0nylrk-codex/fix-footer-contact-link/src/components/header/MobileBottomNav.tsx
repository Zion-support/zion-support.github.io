
import React from "react"
import { Link, useLocation } from "react-router-dom"
import { Home, Search, BriefcaseIcon, MessageSquare, User, MessageCircle } from "lucide-react"
import { cn } from "@/lib/
import { useAuth } from "@/hooks/
      href: any
      matches: (path: string) => path = = "/
      href: any
      matches: (path: string) => path.startsWith("/talent") |path.startsWith("/categories") |path.startsWith("/
      href: any
      matches: (path: string) => path.startsWith("/community") |path.startsWith("/
      href: any
      matches: (path: string) => path.startsWith("/messages") |path.startsWith("/
      href: any
      matches: (path: string) => path.startsWith("/
    
      matches: (path: string) => path = = "/
      href: any
      matches: (path: string) => path.starts_with ("/talent") || path.starts_with ("/categories") || path.starts_with ("/
      href: any
      matches: (path: string) => path.starts_with ("/community") || path.starts_with ("/
      href: any
      matches: (path: string) => path.starts_with ("/messages") || path.starts_with ("/
      href: any
      matches: (path: string) => path.starts_with ("/
    