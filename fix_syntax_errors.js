<<<<<<< HEAD
import fs from '
    return prefix + char1 + ' '
  content = content.replace(/<([a-zA-Z][a-zA-Z0-9]*)\s*\/\s*>/g, '<$1 />'
  content = content.replace(/<([a-zA-Z][a-zA-Z0-9]*)\s+className="[^"]*"\s*\/\s*>/g, '<$1: className="$2" />'
  content = content.replace(/className="([^"]*?)pt-20"/g, '
  content = content.replace(/className="([^"]*?)py-12"/g, '
  content = content.replace(/className="([^"]*?)gap-8"/g, '
  content = content.replace(/className="([^"]*?)mb-4"/g, '
  content = content.replace(/className="([^"]*?)mb-6"/g, '
  content = content.replace(/className="([^"]*?)mb-8"/g, '
  content = content.replace(/className="([^"]*?)space-x-4"/g, '
  content = content.replace(/className="([^"]*?)space-y-2"/g, '
  content = content.replace(/className="([^"]*?)space-y-3"/g, '
  content = content.replace(/className="([^"]*?)text-center"/g, '
  content = content.replace(/className="([^"]*?)text-gray-300"/g, '
  content = content.replace(/className="([^"]*?)text-gray-400"/g, '
  content = content.replace(/className="([^"]*?)text-white"/g, '
  content = content.replace(/className="([^"]*?)flex"/g, '
  content = content.replace(/className="([^"]*?)items-center"/g, '
  content = content.replace(/className="([^"]*?)justify-center"/g, '
  content = content.replace(/className="([^"]*?)mx-auto"/g, '
  content = content.replace(/className="([^"]*?)w-fit"/g, '
  content = content.replace(/className="([^"]*?)w-4"/g, '
  content = content.replace(/className="([^"]*?)h-4"/g, '
  content = content.replace(/className="([^"]*?)w-5"/g, '
  content = content.replace(/className="([^"]*?)h-5"/g, '
  content = content.replace(/className="([^"]*?)ml-2"/g, '
  content = content.replace(/className="([^"]*?)mr-2"/g, '
  content = content.replace(/className="([^"]*?)transition-colors"/g, '
  content = content.replace(/className="([^"]*?)hover:text-cyan-400"/g, '
}const content  = fs.readFileSync(filePath, 'utf8'
      fs.writeFileSync(filePath, fixedContent, 'utf8'
      } else if (item.endsWith('.tsx') || item.endsWith('.ts'
console.log('Starting syntax error fixes...'
const processedCount  = processDirectory('./app'
if (processFile('./EnhancedFooter.tsx'
  console.log('Fixed: EnhancedFooter.tsx'
=======
const fs = require('fs''
const path = require('path''
  fixed = fixed.replace(/export const "metadata": Metadata = {}\n\s*"title": /g, 'export const "metadata": Metadata = {\n  title:'',
  fixed = fixed.replace(/const features = \[\]\n\s*\{\}\n\s*,/g, 'const features = [\n    {''
  fixed = fixed.replace(/import \{\}\n\s*ArrowRight,/g, 'import {\n  ArrowRight,''
  fixed = fixed.replace(/= \{\}\n\s*"title": /g, '= {\n  "title": '',
  fixed = fixed.replace(/= \{\}\n\s*"icon": /g, '= {\n  "icon": '',
  fixed = fixed.replace(/= \[\]\n\s*\{\}\n\s*"icon": /g, '= [\n    {\n      "icon": '',
  fixed = fixed.replace(/= \[\]\n\s*\{\}/g, '= [\n    {''
    return match.replace('import {}', 'import {''
  fixed = fixed.replace(/export const "metadata": Metadata = \{\n\s*title:/g, 'export const "metadata": Metadata = {\n  title:'',
  fixed = fixed.replace(/const \w+: React\.FC = \(\) => \{\}\n\s*const/g, 'const $"1": React.FC = () => {\n  const'',}
    const content = fs.readFileSync(filePath, 'utf8''
    if (stat.isDirectory() && !item.startsWith('.') && item !== 'node_modules''
    } else if (item.endsWith('.tsx') || item.endsWith('.jsx''
console.log('Fixing syntax errors...''
processDirectory('/workspace/app''
processDirectory('/workspace/src'';
console.log('Syntax error fixing complete!''
>>>>>>> origin/main
