import type { AppProps } from 'next/app';
import React, { useRef } from 'react';
import '../styles/globals.css';
			<Layout>
				<Component {...pageProps} />
			</Layout>
		</SEOContext.Provider>
	);
}
