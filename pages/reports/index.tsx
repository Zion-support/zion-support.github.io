import React from 'react';
import Head from 'next/head';
import Link from 'next/link';

// Basic index that lists the latest update pages available in /reports/updates
export default function ReportsIndexPage() {
  const updates = [
    'update-2025-08-15-0406',
    'update-2025-08-15-0405',
    'update-2025-08-15-0404',
    'update-2025-08-15-0403',
    'update-2025-08-15-0402',
    'update-2025-08-15-0111'
  ];

  return (
    <>
      <Head>
        <title>Reports & Analytics — Zion Tech Group</title>
        <meta name="description" content="Autonomous updates, analytics, and performance reports." />
      </Head>
      <div className="min-h-screen bg-gradient-to-br from-indigo-950 via-purple-950 to-slate-950 text-white">
        <main className="container mx-auto px-6 py-12">
          <section className="text-center mb-10">
            <h1 className="text-4xl font-extrabold bg-gradient-to-r from-indigo-400 via-blue-400 to-cyan-400 bg-clip-text text-transparent">Reports & Analytics</h1>
            <p className="mt-3 text-white/85">Latest autonomous updates generated by our platform.</p>
          </section>

          <section className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 max-w-6xl mx-auto">
            {updates.map((slug) => (
              <Link key={slug} href={`/reports/updates/${slug}`} className="group relative overflow-hidden rounded-2xl border border-white/10 bg-gradient-to-br from-white/10 to-white/5 p-6 backdrop-blur-xl hover:border-indigo-400/30">
                <div className="pointer-events-none absolute -inset-px -z-10 bg-gradient-to-r from-fuchsia-500/0 via-cyan-400/10 to-fuchsia-500/0 opacity-0 blur-2xl transition-opacity group-hover:opacity-100" />
                <h3 className="text-lg font-semibold">{slug.replace('update-', 'Autonomous Update — ').replaceAll('-', ': ')}</h3>
                <p className="mt-1 text-sm text-white/75">Freshly published by autonomous agents.</p>
                <div className="mt-3 inline-flex items-center gap-1 text-xs text-indigo-300/90">Open <span aria-hidden>→</span></div>
              </Link>
            ))}
          </section>
        </main>
      </div>
    </>
  );
}

