import React from 'react';
import Head from 'next/head';
import Link from 'next/link';
import type { GetStaticProps } from 'next';
import fs from 'fs';
import path from 'path';

type UpdateEntry = {
  slug: string;
  title: string;
};

type ReportsIndexProps = {
  updates: UpdateEntry[];
};

export const getStaticProps: GetStaticProps<ReportsIndexProps> = async () => {
  const updatesDir = path.join(process.cwd(), 'pages', 'reports', 'updates');
  let updates: UpdateEntry[] = [];

  try {
    const files = fs.readdirSync(updatesDir)
      .filter((file) => file.endsWith('.tsx'))
      .sort()
      .reverse();

    updates = files.map((file) => {
      const slug = file.replace(/\.tsx$/, '');
      const prettyTitle = slug
        .replace(/^update-/, 'Autonomous Update — ')
        .replace(/-/g, ': ')
        .replace(/: (\d{4}): (\d{2}): (\d{2})/, ': $1-$2-$3');

      return {
        slug,
        title: prettyTitle
      };
    });
  } catch {
    updates = [];
  }

  return {
    props: {
      updates
    }
  };
};

export default function ReportsIndexPage({ updates }: ReportsIndexProps) {
  return (
    <>
      <Head>
        <title>Reports & Updates | Zion Tech Group</title>
        <meta name="description" content="Browse autonomous platform reports and the latest updates generated by our automation systems." />
        <meta property="og:title" content="Reports & Updates | Zion Tech Group" />
        <meta property="og:description" content="Autonomous reports and latest updates." />
      </Head>

      <div className="min-h-screen bg-gradient-to-br from-indigo-950 via-purple-950 to-slate-950 text-white">
        <main className="container mx-auto px-6 py-12">
          <nav className="mb-8">
            <Link href="/" className="text-cyan-400 hover:text-cyan-300 transition-colors">← Back to Home</Link>
          </nav>

          <header className="text-center mb-12">
            <h1 className="text-5xl font-extrabold mb-6 bg-gradient-to-r from-cyan-400 to-fuchsia-400 bg-clip-text text-transparent">
              Reports & Updates
            </h1>
            <p className="text-xl text-white/80 max-w-3xl mx-auto">
              Explore the latest autonomous updates and performance reports generated by our platform.
            </p>
          </header>

          <section className="mx-auto max-w-7xl">
            <h2 className="text-2xl font-bold mb-6 text-white">Latest Updates</h2>
            {updates.length === 0 ? (
              <div className="text-white/70">No updates available yet.</div>
            ) : (
              <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                {updates.map((u) => (
                  <Link
                    key={u.slug}
                    href={`/reports/updates/${u.slug}`}
                    className="group relative overflow-hidden rounded-2xl border border-white/10 bg-gradient-to-br from-white/10 to-white/5 p-6 backdrop-blur-xl hover:border-cyan-400/30"
                  >
                    <div className="pointer-events-none absolute -inset-px -z-10 bg-gradient-to-r from-fuchsia-500/0 via-cyan-400/10 to-fuchsia-500/0 opacity-0 blur-2xl transition-opacity group-hover:opacity-100" />
                    <h3 className="text-lg font-semibold">{u.title}</h3>
                    <p className="mt-1 text-sm text-white/75">Generated by autonomous agents.</p>
                    <div className="mt-3 inline-flex items-center gap-1 text-xs text-cyan-300/90">Open <span aria-hidden>→</span></div>
                  </Link>
                ))}
              </div>
            )}
          </section>
        </main>
      </div>
    </>
  );
}

