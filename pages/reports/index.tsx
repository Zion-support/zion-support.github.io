import fs from 'fs';
import path from 'path';
import Link from 'next/link';
import Head from 'next/head';

type Report = { slug: string; title: string };

export default function ReportsIndex({ updates }: { updates: Report[] }) {
  return (
    <div className="min-h-screen bg-gradient-to-br from-indigo-950 via-purple-950 to-slate-950 text-white">
      <Head>
        <title>Reports & Analytics — Zion Tech Group</title>
      </Head>
      <main className="container mx-auto px-6 py-12">
        <div className="mb-8">
          <Link href="/" className="text-cyan-300 hover:text-cyan-200">← Back to Home</Link>
        </div>
        <h1 className="text-4xl font-extrabold mb-8 bg-gradient-to-r from-cyan-400 via-fuchsia-400 to-blue-400 bg-clip-text text-transparent">Reports & Analytics</h1>
        <p className="text-white/80 mb-8 max-w-3xl">Explore the latest autonomous updates and performance insights generated by our platform.</p>
        <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
          {updates.map((u) => (
            <Link key={u.slug} href={`/reports/updates/${u.slug}`} className="group relative overflow-hidden rounded-2xl border border-white/10 bg-gradient-to-br from-white/10 to-white/5 p-6 backdrop-blur-xl hover:border-cyan-400/30">
              <div className="pointer-events-none absolute -inset-px -z-10 bg-gradient-to-r from-fuchsia-500/0 via-cyan-400/10 to-fuchsia-500/0 opacity-0 blur-2xl transition-opacity group-hover:opacity-100" />
              <h3 className="text-lg font-semibold">{u.title}</h3>
              <div className="mt-3 inline-flex items-center gap-1 text-xs text-cyan-300/90">Open <span aria-hidden>→</span></div>
            </Link>
          ))}
        </div>
      </main>
    </div>
  );
}

export async function getStaticProps() {
  const updatesDir = path.join(process.cwd(), 'pages', 'reports', 'updates');
  const files = fs.readdirSync(updatesDir).filter((f) => f.endsWith('.tsx'));
  const updates = files
    .map((file) => {
      const slug = file.replace(/\.tsx$/, '');
      const title = `Autonomous Update — ${slug.replace('update-', '').replace(/-/g, ': ').trim()}`;
      return { slug, title };
    })
    .sort((a, b) => (a.slug < b.slug ? 1 : -1));

  return {
    props: { updates }
  };
}

