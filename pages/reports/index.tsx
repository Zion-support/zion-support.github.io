import React from 'react';
import Head from 'next/head';
import Link from 'next/link';
import fs from 'fs';
import path from 'path';

type ReportLink = {
  slug: string;
  title: string;
  href: string;
};

export async function getStaticProps() {
  const updatesDir = path.join(process.cwd(), 'pages', 'reports', 'updates');
  let reportLinks: ReportLink[] = [];
  try {
    const files = fs.readdirSync(updatesDir);
    reportLinks = files
      .filter((file) => file.endsWith('.tsx'))
      .map((file) => file.replace(/\.tsx$/, ''))
      .sort()
      .reverse()
      .map((slug) => ({
        slug,
        title: `Autonomous Update — ${slug.replace('update-', '').replace(/-/g, ': ').trim()}`,
        href: `/reports/updates/${slug}`,
      }));
  } catch {
    reportLinks = [];
  }

  return {
    props: {
      reportLinks,
    },
  };
}

export default function ReportsIndexPage({ reportLinks }: { reportLinks: ReportLink[] }) {
  return (
    <>
      <Head>
        <title>Reports | Zion Tech Group</title>
        <meta name="description" content="Explore the latest autonomous system updates, reports, and analytics." />
      </Head>
      <div className="min-h-screen bg-gradient-to-br from-indigo-950 via-purple-950 to-slate-950 text-white">
        <main className="container mx-auto px-6 py-12">
          <nav className="mb-8">
            <Link href="/" className="text-cyan-400 hover:text-cyan-300 transition-colors">
              ← Back to Home
            </Link>
          </nav>
          <header className="text-center mb-12">
            <h1 className="text-5xl font-extrabold mb-4 bg-gradient-to-r from-cyan-400 to-fuchsia-400 bg-clip-text text-transparent">
              Reports & Updates
            </h1>
            <p className="text-white/80 max-w-3xl mx-auto">
              Browse the latest autonomous platform updates, health reports, and system analytics.
            </p>
          </header>

          <section>
            <h2 className="text-2xl font-bold mb-6">Latest Updates</h2>
            <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
              {reportLinks.map((report) => (
                <Link
                  key={report.slug}
                  href={report.href}
                  className="group relative overflow-hidden rounded-2xl border border-white/10 bg-gradient-to-br from-white/10 to-white/5 p-6 backdrop-blur-xl hover:border-cyan-400/30"
                >
                  <div className="pointer-events-none absolute -inset-px -z-10 bg-gradient-to-r from-fuchsia-500/0 via-cyan-400/10 to-fuchsia-500/0 opacity-0 blur-2xl transition-opacity group-hover:opacity-100" />
                  <h3 className="text-lg font-semibold">{report.title}</h3>
                  <p className="mt-1 text-sm text-white/75">Generated by autonomous agents.</p>
                  <div className="mt-3 inline-flex items-center gap-1 text-xs text-cyan-300/90">Open <span aria-hidden>→</span></div>
                </Link>
              ))}
            </div>
          </section>

          <section className="mt-12 text-center">
            <div className="inline-flex flex-wrap gap-3">
              <Link href="/about" className="px-4 py-2 border border-white/20 rounded-lg text-white hover:border-cyan-400/50">About</Link>
              <Link href="/services" className="px-4 py-2 border border-white/20 rounded-lg text-white hover:border-fuchsia-400/50">Services</Link>
              <Link href="/resources" className="px-4 py-2 border border-white/20 rounded-lg text-white hover:border-blue-400/50">Resources</Link>
              <Link href="/blog" className="px-4 py-2 border border-white/20 rounded-lg text-white hover:border-purple-400/50">Blog</Link>
              <Link href="/contact" className="px-4 py-2 border border-white/20 rounded-lg text-white hover:border-green-400/50">Contact</Link>
            </div>
          </section>
        </main>
      </div>
    </>
  );
}

