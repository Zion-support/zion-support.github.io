import React from 'react';
import Link from 'next/link';
import type { GetStaticProps } from 'next';
import fs from 'fs';
import path from 'path';

type UpdatesIndexProps = {
  updates: Array<{ slug: string }>
};

export const getStaticProps: GetStaticProps<UpdatesIndexProps> = async () => {
  const updatesDir = path.join(process.cwd(), 'pages', 'reports', 'updates');
  let updates: Array<{ slug: string }>; 

  try {
    const files = fs.readdirSync(updatesDir);
    updates = files
      .filter((file) => file.endsWith('.tsx'))
      .map((file) => ({ slug: file.replace(/\.tsx$/, '') }))
      .sort((a, b) => (a.slug < b.slug ? 1 : -1));
  } catch {
    updates = [];
  }

  return { props: { updates } };
};

export default function ReportsIndexPage({ updates }: UpdatesIndexProps) {
  return (
    <div className="min-h-screen bg-gradient-to-br from-indigo-950 via-purple-950 to-slate-950 text-white">
      <main className="container mx-auto px-6 py-12">
        <section className="text-center mb-12">
          <h1 className="text-5xl font-extrabold mb-4 bg-gradient-to-r from-cyan-400 via-fuchsia-400 to-blue-400 bg-clip-text text-transparent">
            Reports & Updates
          </h1>
          <p className="text-white/85 max-w-3xl mx-auto">
            Browse the latest autonomous updates generated by our platform.
          </p>
        </section>

        <section className="mx-auto max-w-6xl">
          {updates.length === 0 ? (
            <div className="text-center text-white/80">No updates found.</div>
          ) : (
            <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
              {updates.map(({ slug }) => (
                <Link
                  key={slug}
                  href={`/reports/updates/${slug}`}
                  className="group relative overflow-hidden rounded-2xl border border-white/10 bg-gradient-to-br from-white/10 to-white/5 p-6 backdrop-blur-xl hover:border-cyan-400/30"
                >
                  <div className="pointer-events-none absolute -inset-px -z-10 bg-gradient-to-r from-fuchsia-500/0 via-cyan-400/10 to-fuchsia-500/0 opacity-0 blur-2xl transition-opacity group-hover:opacity-100" />
                  <h3 className="text-lg font-semibold break-words">{slug}</h3>
                  <div className="mt-3 inline-flex items-center gap-1 text-xs text-cyan-300/90">Open <span aria-hidden>→</span></div>
                </Link>
              ))}
            </div>
          )}
        </section>

        <section className="text-center mt-12">
          <Link href="/" className="px-6 py-3 bg-white/10 hover:bg-white/20 border border-white/20 rounded-lg text-white font-semibold transition-all duration-300 hover:border-cyan-400/50">
            Back to Home
          </Link>
        </section>
      </main>
    </div>
  );
}

