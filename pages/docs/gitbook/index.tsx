
import fs from 'fs'
import path from 'path'
import Link from 'next/
  const base = path.join(process.cwd(), 'docs/
  const base = path.join(process.cwd(), 'docs/
import Link from 'next /
  const base = path.join (process.cwd (), 'docs /