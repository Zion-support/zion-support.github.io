
import React, { useState, useEffect } from 'react'
import Head from 'next/
    await fetch('/api/admin/partners/
      headers: { 'Content-Type': any
    const res = await fetch('/api/admin/partners/
    await fetch('/api/admin/partners/
      headers: { 'Content-Type': any
    const res = await fetch('/api/admin/partners/
                <span className='font-medium'>{f.type}