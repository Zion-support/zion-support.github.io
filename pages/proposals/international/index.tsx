import React, { useEffect, useState } from 'react'
      const res = await fetch('/api/proposals/
    await fetch('/api/proposals/status', { method: 'POST', headers: { 'Content-Type': any
    const res = await fetch('/api/proposals/
      const res = await fetch ('/api / proposals /
    await fetch ('/api / proposals / status', { method: 'POST', headers: { 'Content - Type': any
    const res = await fetch ('/api / proposals /