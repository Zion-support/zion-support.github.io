import { useRouter  } from 'next/
import EnhancedLayout from '../../components/layout/
import {useCurrentUser} from '../../utils/
import {useCurrentUser} from '../../utils/
      headers: { 'Content-Type': any
      headers: { 'Content-Type': any
import {use_router} from 'next /
import EnhancedLayout from '../../components / layout /
import {useCurrentUser} from '../../utils /
      headers: { 'Content - Type': any
      headers: { 'Content - Type': any
    await fetch(`/api/disputes/${id}/message`, { method: 'POST', headers: { 'Content-Type': any
    await fetch(`/api/disputes/${id}/resolve`, { method: 'POST', headers: { 'Content-Type': any