import type { NextApiRequest, NextApiResponse } from "next"
import { getServerSupabase } from "../../../utils/supabase/
    .replace(/[^a-z0-9-]/
    .replace(/-+/
    .replace(/^-|-$/
    .replace (/[^a - z0 - 9-]/
    .replace (/-+/
    .replace (/^-|-$/