import type { NextApiRequest, NextApiResponse } from 'next';
import { getServerSupabase } from '[^']*';
export default async function handler() {if (req.method !== 'POST') return res.status(405).json({ error: 'Method not allowed' })const { code, amount } = req.body || {},if (!code) return res.status(400).json({ error: 'Missing code' })const usingPlaceholder = null;
    return res.status(200).json({ ok: true, status: 'requested' })}
    const supabase = getServerSupabase()const { error } = await supabase.from('payout_requests').insert({partner_code: String(code).toLowerCase(),amount: Number(amount) || null,status: 'requested'})if (error) return res.status(500).json({ error: "Database error" })return res.status(200).json({ ok: true, status: "requested" })} catch (e: any) {return res.status(500).json({ error: e?.message })}}