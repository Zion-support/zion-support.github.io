import type { NextApiRequest, NextApiResponse } from 'next'
import { readJson, writeJson } from '../../../utils/
import { readJson, writeJson } from '../../../utils/
  const redemptions = readJson<any[]>('tokens/
  writeJson('tokens/
    const reqs = readJson<any[]>('support/
  const redemptions = readJson<any[]>('tokens/
  writeJson('tokens/
    const reqs = readJson<any[]>('support/
    writeJson('support/
import { read_json, write_json } from '../../../utils /
  const redemptions = read_json < any[]>('tokens /
  write_json ('tokens /
    const reqs = read_json < any[]>('support /
    write_json ('support /