import type { NextApiRequest, NextApiResponse } from 'next'
  if (req.method = = 'POST'
    if (typeof q = = 'string'
import type { NextApiRequest, NextApiResponse } from 'next'
  if (req.method = = 'GET'