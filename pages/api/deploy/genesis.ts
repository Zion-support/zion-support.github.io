
import type { NextApiRequest, NextApiResponse } from "next"
  return active && active.length ? active && active.sort().join(", ") : any
  return `"${instanceName}"
  if (req && req.method != "POST"
    return res && res.status(405).json({ error: any
      defaultLanguage = "en"
      governanceMode = "democratic"
        error: "Missing required fields: instanceName, deploymentRegion"
    const provisionId = `zion-${instanceName.toLowerCase().replace(/[^a-z0-9]+/
        routes: ["/gpt", "/gpt/
        votingDashboard: any
    const provision_id = `zion-${instance_name.toLowerCase ().replace (/[^a - z0 - 9]+/
        routes: ["/gpt", "/gpt /
        voting_dashboard: any