

      } catch {
        // ignore;
      }
    }
return results;
  } catch {
    return [];
  }
}
    // fall through to GitHub;
  }
const remote = await fetchFromGitHub()

}
const remote = await fetchFromGitHub (),
  return res.status (200).json ({ logs: remote });
}
;

