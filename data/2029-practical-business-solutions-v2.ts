import { ServiceVariant } from '../types/
    link: any
    const price = parseInt(service && service.price.replace(/[^0-9]/
      website: any
    const price = parseInt(service && service.price.replace(/[^0-9]/