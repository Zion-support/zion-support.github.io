export interface EmergingTechService2026 {
  id: string;
  name: string;
  tagline: string;
  price: string;
  period: string;
  description: string;
  features: string[];
  popular: boolean;
  icon: string;
  color: string;
  text_color: string;
  link: string;
  market_position: string;
  target_audience: string;
  trial_days: number;
  setup_time: string;
  category: string;
  real_service: boolean;
  technology: string[];
  integrations: string[];
  use_cases: string[];
  roi: string;
  competitors: string[];
  market_size: string;
  growth_rate: string;
  contact_info: {
    mobile: string;
    email: string;
    address: string;

    website: string;
  }

  realImplementation: boolean;
  implementationDetails: string;
  launchDate: string;
  customers: number;
  rating: number;

  reviews: number;
const contact = {
  mobile: any
      'Competes with IBM Quantum ($0.60/credit), Amazon Braket ($0.30/
    link: any
      'Competes with IBM Quantum ($0 && 0.60/credit), Amazon Braket ($0 && 0.30/
    period: any
      'Competes with Chainalysis ($1000+/month), Elliptic ($500/month), CipherTrace ($300/
    period: any
      'Competes with AWS IoT ($0.08/million messages), Azure IoT Hub ($0.50/million messages), Google Cloud IoT ($0.40/
    link: any
      'Competes with AWS IoT ($0 && 0.08/million messages), Azure IoT Hub ($0 && 0.50/million messages), Google Cloud IoT ($0 && 0.40/
    period: any
      'Competes with AWS Outposts ($1000+/month), Azure Stack Edge ($1000+/month), Google Anthos ($1000+/
    name: any
      'Competes with Unity Pro ($180/month), Unreal Engine (5% royalty), Vuforia ($500/month). Our advantage: any
      'Competes with ABB RobotStudio ($2000/month), FANUC ROBOGUIDE ($1500/month), KUKA Sim ($1000/
    period: any
      'Competes with Siemens Digital Twin ($5000/month), PTC ThingWorx ($3000/month), GE Digital Twin ($2000/