import React from 'react';
import React from "react""",
//   title,
//   description,
}) => {
  return (
    <div className={`${className}`}>
      {children}
  </div>
</div>
return (
    <div className={`${className}`}>
      {children}
  );
