# Memory — 2026-09-16

## Service Pipeline Deployer Cron Run (batches 19620-19720, 100 pages total)

- **Pre-run output**: 50 landing pages generated (services[19620:19670]), progress 19620→19670, sitemap 7763 URLs (32 static + 7731 service pages), 0 skipped
- **Explicit execution**: Ran `python3 generate_pages_for_new.py --once` → 50 NEW page.tsx (services[19670:19720]), progress 19670→19720, sitemap regenerated to 7813 URLs (32 static + 7781 service pages), 0 skipped. `--once` flag silently ignored (no argparse).
- **Docs artifact gap fix (progress-file swap)**: Pre-run batch (19620-19670) had no docs/services/ HTML artifacts. Set progress=19670 → ran scripts/generate_docs_artifacts.py (50 artifacts for batch 1) → restored 19720 → ran again (50 artifacts for batch 2). 100 docs/services/*/index.html artifacts total, 0 skipped.
- **Staging**: `git add app/services/*/page.tsx docs/services/*/index.html public/sitemap.xml outreach_monitor/processed/last_service_count.txt` — 202 files staged, zero cron_output/memory transients
- **Commit**: 61105043d2 on `hero-carousel` — 202 files, 18501 insertions, 1 deletion. Local/remote SHA match confirmed.
- **Push**: Clean to origin/hero-carousel (61105043d2ebf72dde1c0ef9048c33678801a8d3)
- **Verification**: 
  - Site root: HTTP 200 ✓
  - Sitemap: HTTP 200 ✓ (7813 URLs)
  - New page (e.g. /services/next-generation-smart-hospitality-automation-95ca0500): HTTP 404 — expected, deploy-on-push triggers on main only
- **Catalog total**: 29,020 services, 7,813 sitemap URLs (32 static + 7,781 service pages)

## Pattern (systematic)
1. Trust pre-run output as authoritative
2. Run explicit-execution script (`generate_pages_for_new.py --once`) to generate next batch
3. Run docs artifacts via progress-file swap (set batch1 boundary → generate → restore → generate) to fill docs/ gap for both batches
4. Stage explicitly: app/services/*/page.tsx + docs/services/*/index.html + public/sitemap.xml + last_service_count.txt
5. Commit with batch range in message, push to hero-carousel
6. Verify: root 200, sitemap 200, new page 404 expected (deploy on main only)
