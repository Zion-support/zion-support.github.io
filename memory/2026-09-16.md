# Memory — 2026-09-16

## Service Pipeline Cron Run (batch 19020-19070)

- **Pre-run**: 50 landing pages generated in `app/services/{slug}/page.tsx`, sitemap regenerated (7163 URLs), progress file at 19070
- **Docs artifacts**: Ran `scripts/generate_docs_artifacts.py` → 50 `docs/services/{slug}/index.html` artifacts generated (0 skipped)
- **Commit**: 762c77b8ec on `hero-carousel` — 102 files (50 page.tsx + 50 docs HTML + sitemap.xml + last_service_count.txt)
- **Push**: Local/remote SHA match confirmed
- **Site root HTTP 200, sitemap HTTP 200**, new slug in local sitemap (1 match), new page HTTP 404 (expected — deploy triggers on main only)
- **Post-commit**: Concurrent process briefly overwrote sitemap (7163→639 locs); restored from HEAD via `git checkout HEAD -- public/sitemap.xml out/sitemap.xml`; no conflicting process running
- **Catalog total**: 29,020 services, 7,117 service page directories, 7,163 sitemap URLs

## Pattern (systematic)
1. Trust pre-run output as authoritative
2. Run `scripts/generate_docs_artifacts.py` to fill docs/services/ gap
3. Stage explicitly: app/services/*/page.tsx + docs/services/*/index.html + public/sitemap.xml + outreach_monitor/processed/last_service_count.txt
4. Commit with batch range in message
5. Push to hero-carousel
6. Verify: root 200, sitemap 200, loc count matches pre-run, new slug in sitemap

## Service Pipeline Cron Run (batches 19070-19170, 100 pages)

- **Pre-run**: 50 landing pages generated (services[19070:19120]), progress 19070→19120, sitemap 7213 URLs
- **Explicit execution**: Task instructed `generate_pages_for_new.py --once` → 50 MORE pages (services[19120:19170]), progress 19120→19170, sitemap 7263 URLs (7231 service pages)
- **Docs artifacts**: Ran generate_docs_artifacts.py twice (progress-swap technique) → 100 docs/services/ HTML artifacts (50+50), 0 skipped. Pre-run batch had NO docs artifacts — gap filled.
- **Staged**: 202 files (100 page.tsx + 100 docs HTML + sitemap.xml + last_service_count.txt), zero cron_output/memory transients
- **Commit**: b67d4accac on hero-carousel, 202 files, 18501 insertions. Local/remote SHA match.
- **Push**: clean to origin/hero-carousel
- **Site**: root HTTP 200, sitemap HTTP 200, new page HTTP 404 (expected — deploy triggers on main only)
- **Catalog**: 29,020 services, 7,263 sitemap URLs (32 static + 7,231 service pages)
- **Gap**: generate_docs_artifacts.py only processes last batch (last_processed-50:last_processed). When two batches exist in one cron run, must run docs generator for each batch via progress-file swap.

## Service Pipeline Cron Run (batch 19170-19220, 50 pages)

- **Pre-run**: 50 landing pages generated in `app/services/{slug}/page.tsx`, sitemap regenerated (7313 URLs: 32 static + 7281 service pages), progress file updated 19170→19220, 0 pages skipped
- **Verification**: Sample page (ai-powered-quantum-ready-sustainability-optimization-engine-3345cfce) confirmed — proper metadata, BreadcrumbSchema, canonical URL
- **Commit**: 915f641f8c on `hero-carousel` — 55 files (50 page.tsx + sitemap.xml + sitemap.xml), 4291 insertions, 826 deletions
- **Push**: Local/remote SHA match confirmed (915f641f8c4df3ebb17fdc56943aa9a49bfcd428)
- **Site root HTTP 200, sitemap HTTP 200**, new page HTTP 404 (expected — deploy triggers on main only)
- **Note**: `out/feed.xml`, `out/sitemap.xml`, `memory/`, `outreach_monitor/` changes are gitignored — only app/services/, public/sitemap.xml, sitemap.xml staged
- **Catalog total**: 29,220 services, 72,20 service page directories, 7,313 sitemap URLs

## Service Pipeline Cron Run (batch 19270-19320, 50 pages)

- **Pre-run data**: 50 landing pages generated (from previous pre-run), progress 19220→19270, sitemap 7363 URLs — confirmed already committed in earlier commit
- **Explicit execution**: Ran `generate_pages_for_new.py --once` → 50 MORE pages (services[19270:19320]), progress 19270→19320, sitemap 7413 URLs (32 static + 7381 service pages), 0 pages skipped
- **Staged**: 121 files (100 page.tsx from both batches + sitemap.xml + last_service_count.txt + cron_output/ + memory/)
- **Commit**: 5f6d760b23 on `hero-carousel` — 121 files, 8173 insertions, 1 deletion
- **Push**: clean to origin/hero-carousel
- **Verification**: sitemap.xml HTTP 200 (last-modified: Wed, 16 Sep 2026 01:54:55 GMT), 7413 URLs locally, 3 sample pages confirmed OK
- **Note**: `generate_docs_artifacts.py` does NOT exist in ~/.hermes/scripts/; docs/services/ uses different naming convention (ai-4801) — not part of this pipeline
- **Catalog total**: 29,020 services, 7413 sitemap URLs

## Service Pipeline Cron Run (batch 19320-19370, 50 pages)
- **Execution**: Ran `generate_pages_for_new.py` (no `--once` flag support — silently ignored). 50 pages generated (services[19320:19370]), progress 19320→19370, 0 skipped.
- **Sitemap**: Regenerated via `fix_sitemap.py` → 7463 URLs (32 static + 7431 service pages).
- **Staged**: 53 files (50 page.tsx + sitemap.xml + last_service_count.txt + 2 cron_output transients).
- **Commit**: f7dd274257 on `hero-carousel` — 53 files, pushed cleanly. Local/remote SHA match.
- **Verify**: Sample page confirmed well-formed (metadata + canonical + BreadcrumbSchema + 3-tier pricing + Calendly CTA). New-page 404 expected (deploy triggers on main only).
- **Script error note**: Data-collection script reported exit code -15 (SIGTERM) — see final report; not this pipeline.

## Service Pipeline Cron Run (batches 19370-19470, 100 pages total)
|- Pre-run output trusted as context: 50 page.tsx generated (batch 19370-19420), sitemap 7513 URLs, progress 19420. Task explicitly instructed to execute `generate_pages_for_new.py --once`.
|- Ran `python3 generate_pages_for_new.py --once` from ~/.hermes/scripts/ → 50 NEW page.tsx (services[19420:19470], 0 skipped), progress 19420→19470, sitemap regenerated to 7563 URLs (32 static + 7531 service pages). `--once` was a no-op (no argparse).
|- **Docs artifact gap fix (progress-file swap)**: Pre-run batch (19370-19420) had NO docs/services/ HTML artifacts. Used progress-file swap: set progress=19420 → ran generate_docs_artifacts.py (50 artifacts for 19370-19420) → restored 19470 → ran again (50 artifacts for 19420-19470). 100 docs artifacts total, 0 skipped.
|- Verification: 100 page.tsx files on disk (50 pre-run + 50 script), 100 docs HTML artifacts, sitemap loc count = 7563, progress file at 19470.
|- Staged exactly 202 files (100 page.tsx + 100 docs HTML + public/sitemap.xml + last_service_count.txt). Zero cron_output/memory transients.
|- Commit 6af98022e3 on hero-carousel, pushed; local/remote SHA match (6af98022e390eb982f2c3ab4f2420c1c382a1295).
|- Site verification: root HTTP 200, sitemap HTTP 200 (sitemapindex → sitemap-0.xml, deploy triggers on main only), new page HTTP 404 (expected).
|- Post-mortem: Dual-batch docs artifact generation via progress-file swap successfully closed the docs gap for both pre-run and explicit-execution batches. Explicit-path git add staging held perfectly — no transients across 202 files.
|- Rules affirmed: docs/ artifact gap, commit hygiene (explicit paths, never git add -A), post-commit site verification (200/200/404), dual-batch docs artifact generation (progress-file swap technique).
