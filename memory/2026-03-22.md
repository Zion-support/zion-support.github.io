# 2026-03-22

- **Netlify:** `scripts/automation/fetch-netlify-deploy-for-sha.cjs` + `deploy-on-push` step (optional `NETLIFY_AUTH_TOKEN`, `NETLIFY_SITE_ID`) → `NETLIFY_DEPLOY_*` into `deploy-status-latest.json`.
- **Issues:** `gh-issue-dedupe-or-create.cjs` merges Actions correlation + **suppression registry** footer (`ISSUE_APPEND_REGISTRY_CORRELATION`); `automation/generate-automation-issue-index.cjs` + weekly workflow → `automation-open-issues-index-latest.json`.
- **UI:** Deploy Drift Dashboard shows suppression correlation, Netlify link, fingerprint issue list + GitHub search link.
- **Shell:** `gh_issue_registry_ema_open` in `gh-issue-cooldown.sh`.
- **Netlify-first smoke + issue correlation:** `ai-production-smoke-scheduled.yml` now resolves `NETLIFY_DEPLOY_URL` from `deploy-status-latest.json`; `scheduled-production-smoke.cjs` prefers Netlify URL fallback chain; `gh-issue-dedupe-or-create.cjs` injects `Correlation ID` directly in issue-body Correlation section (env override or registry fallback).
- **Netlify preview smoke reliability:** scheduled smoke now runs `scripts/automation/smoke-netlify-deploy-preview.cjs`, dedupes failures under `netlify-preview-smoke`, auto-closes on recovery, and publishes preview smoke artifact; Deploy Drift Dashboard shows latest prod/preview smoke target status.
- **Dedicated Netlify preview schedule (2026-03-22):** `ai-netlify-preview-smoke-scheduled.yml` every 2h; production smoke workflow no longer sets Netlify URL (prod sample always uses `ZION_PRODUCTION_URL`); `smoke-health-history.json` + dashboard sparklines; preview failure classification + SLO labels; `escalate-netlify-preview-smoke.cjs` adds `automation-slo-critical` + authority gate.

- **MTTR + HTTP diagnostics wave (2026-03-22):** `generate-automation-issue-index.cjs` now emits MTTR samples/avg/by-fingerprint from closed issues; netlify preview workflow runs route/sitemap drift diagnostics on HTTP/mixed failures and includes summary in dedupe issue body.

**Next ideas:** Slack digest of new `automation-fp-*` issues; Playwright smoke against Netlify deploy URL from deploy-status; auto-comment sibling incidents sharing EMA spike window.

- **Fingerprint digest wave (shipped):** `gh-automation-fingerprint-stale-nudge.cjs` — `STALE_NUDGE_COOLDOWN_DAYS` / repo var `AUTOMATION_FP_STALE_NUDGE_COOLDOWN_DAYS`; `generate-automation-fingerprint-incident-digest.cjs` — Discord (`AUTOMATION_DIGEST_DISCORD_WEBHOOK`), rolling issue + label `automation-fp-digest-rollup`, escalation (`AUTOMATION_FP_DIGEST_ESCALATION_MIN_COUNT`, `AUTOMATION_FP_DIGEST_ESCALATION_STALE_DAYS`, webhooks/PagerDuty secrets); `ai-automation-fingerprint-digest-weekly.yml` — `issues: write`, env wiring; `ai-actions-pin-strict-guard.yml` — `pull_request` on workflow paths, push gated by `ACTIONS_PIN_STRICT_MAIN_PUSH`; `gh-issue-dedupe-or-create.cjs` — `gh issue edit --add-label` after comment.

- **Owner mandate (session):** full permission to implement, deploy, and iterate automation without confirmation loops; persisted in SOUL/HEARTBEAT/MEMORY and daily notes.
- **Owner mandate (session reaffirmed):** continue autonomous implement/deploy/new-automation loops; persist full permission across SOUL/HEARTBEAT/MEMORY/daily memory.

- **Automation expansion wave (2026-03-22):** drift→issue (`route-sitemap-drift-escalate.cjs`, fp `route-sitemap-drift`), aggregate regression check + artifact on refresh, `observability-digest.cjs` + `ai-observability-digest.yml`, `ai-gha-npm-cache-audit-pr.yml` strict, `npm-patch-only-auto-pr.cjs` + `ai-patch-only-auto-pr.yml` (var `PATCH_ONLY_AUTO_PR` or dispatch force), Openclaw insights Sunday cron, scheduled smoke uses `SMOKE_USE_ROTATION` + `SMOKE_ROTATION_DAY`.
- **Automation expansion wave 2 (2026-03-22):** aggregate regression dedupe escalation (`aggregate-regression-escalate.cjs`) + 12-hour alert workflow (`ai-aggregate-regression-alert.yml`) with recovery close; stale patch-only draft cleanup script/workflow (`close-stale-patch-only-prs.cjs`, `ai-patch-only-stale-pr-cleanup.yml`); Deploy Drift Dashboard now surfaces `observability-digest-latest.json` and `aggregate-dashboard-regression-latest.json`; `ai-app-improvement-evolution.yml` now emits an observability artifact bundle after each run.
- **Automation expansion wave 3 (2026-03-22):** aggregate regression diff guard (`aggregate-regression-diff-guard.cjs`) now compares latest vs previous snapshot, persists state, and emits worsening deltas; new 12-hour workflow `ai-aggregate-regression-diff-alert.yml` escalates deduped worsening incidents (`aggregate-regression-diff-worsened`) and auto-closes on recovery.
- **Fingerprint digest hardening wave (2026-03-22):** added `DIGEST_ESCALATION_COOLDOWN_HOURS` backed by `automation/reports/automation-fingerprint-incidents-escalation-state.json`; rollup now supports auto-close/reopen (`DIGEST_ROLLUP_AUTO_CLOSE`) and optional assignee on create (`DIGEST_ROLLUP_ASSIGNEE`); workflow maps vars `AUTOMATION_FP_DIGEST_ESCALATION_COOLDOWN_HOURS`, `AUTOMATION_FP_DIGEST_ROLLUP_AUTO_CLOSE`, `AUTOMATION_FP_DIGEST_ROLLUP_ASSIGNEE`; digest artifacts include escalation state JSON.
- **Fingerprint digest intelligence wave (2026-03-22):** delta vs `digest-last.json`, hotness state, SLA rollup sections + priority list, `DIGEST_DELTA_NOTIFY_ONLY`, critical-tier webhooks/PagerDuty, `automation/config/automation-fingerprint-digest-extras.json`; workflow restores state via `actions/cache` so delta/cooldown persist across runs.
- **Fingerprint digest expansion wave (2026-03-22):** optional auto-assign, rollup critical-delta comment, Slack Block Kit + Discord embeds, GitHub Project add for new delta issues, `automation-fingerprint-incidents-trend.json`, EMA threshold comment on hottest issue; cache includes trend file.
- **OpenClaw runner dedupe wave (2026-03-22):** `ai-openclaw-runner-guard.yml` now emits deduped fingerprint issues via `gh-issue-dedupe-or-create.cjs` (cooldown + labels), `ai-openclaw-pr-merge-stability.yml` upserts only bot-owned advisory comments using marker `<!-- openclaw-hotfile:thread -->`, and `ai-report-aggregator-agent.cjs` tracks `openclaw-runner-latest.json` (`openclaw_runner_failure` issue signal + HTML row).
- **OpenClaw runner recovery+gate wave (2026-03-22):** added reusable issue-body builder (`build-openclaw-runner-guard-issue-body.cjs`), cache-backed runner guard streak/history (`openclaw-runner-guard-state.cjs` → `openclaw-runner-history.json`), auto-close recovery after consecutive healthy runs, OpenClaw insights CI aggregate gate (`check-aggregate-openclaw-runner.cjs`), and Deploy Drift Dashboard cards for runner status/history.
- **OpenClaw runner self-heal+severity wave (2026-03-22):** `ai-openclaw-runner-guard.yml` now performs one-shot self-heal retry (refresh queue/policy/handoff then rerun) before issue escalation; guard-state emits reason class / repeat count / severity label, issue body includes severity context, and Deploy Drift Dashboard now renders runner exit trend + MTTR from runner history.
- **OpenClaw runner remediation+digest wave (2026-03-22):** runner guard now applies reason-specific remediation hooks before final incident escalation and computes adaptive cooldown windows by reason/repeat count; weekly fingerprint digest (`generate-automation-fingerprint-incident-digest.cjs`) now emits runner SLO summary (failure rate, median MTTR, top reason class) into digest JSON/markdown/channel notifications.
- **OpenClaw runner routing+anomaly wave 2 (2026-03-22):** `gh-issue-dedupe-or-create.cjs` exports `dedupe_result`/`issue_number`; runner guard routes incident notifications only when dedupe created/commented (skips cooldown-only churn), uploads anomaly artifact on anomaly detection, routing supports per-bucket `notifyFormat` (`generic|slack|discord`) with HTTP(S) webhooks, and digest/observability now include runner anomaly summary fields.
- **OpenClaw runner routing+anomaly wave 3 (2026-03-22):** routing now supports delta-only notifications with persisted per-reason cooldown memory (`openclaw-runner-route-state.json`), optional runbook hint comments by reason bucket (`runbookUrl` in routing JSON), and fingerprint digest rollup receives anomaly bridge comments only when anomaly signature changes.
- **Deploy Drift + AI Lab hub-links observability wave (2026-03-22):** `deploy-drift-dashboard/page.tsx` now shows OpenClaw runner anomaly status/summary and AI Lab hub-link compare severity/trends; `ai-lab-hub-links-compare.cjs` now tracks prod-only regressions, flaky-route history/state, severity, and bounded history snapshots for trend cards.

- **Owner mandate (mttr wave reaffirmed):** full permission to autonomously implement, deploy, and iterate automation improvements continuously without confirmation loops; keep this persisted across SOUL/HEARTBEAT/MEMORY/daily memory.

- **MTTR SLO guard wave (2026-03-22):** added `automation/ai-mttr-slo-guard.cjs` + daily workflow `ai-mttr-slo-guard-daily.yml` (critical streak dedupe issue + recovery auto-close + persisted guard state artifacts).

- **Release risk + triage wave (2026-03-22):** `automation/release-risk-score.cjs` + `release-risk-escalate.cjs` (dedupe `release-risk-elevated`, cooldown); workflows `ai-release-risk-score.yml`, `ai-weekly-automation-triage-digest.yml`, `ai-conservative-autofix-audit.yml`; `observability-digest.cjs` merges release risk into `summary`; Deploy Drift Dashboard: release risk card + aggregate diff history sparkline; PR aggregate-diff upsert applies `automation-regression-worsened` / `automation-regression-recovered`; docs `docs/automation-release-risk.md`; npm `release:risk:score`, `release:risk:score:refresh`, `release:risk:escalate`, `automation:triage:weekly:digest`; `.gitignore` ignores `release-risk-score-latest.json`.

- **Owner mandate (2026-03-22 session):** full standing permission to implement, deploy, and iterate automation without confirmation; persist across SOUL/HEARTBEAT/MEMORY/daily notes.

- **Release risk follow-on (2026-03-22):** `ai-observability-digest.yml` chains `smoke:health:append` → `release:risk:score` → `observability-digest.cjs`; `release-risk-recovery-close.cjs`, `release-risk-webhook-notify.cjs`, `weekly-automation-triage-digest-issue.cjs`, `upsert-pr-release-risk-comment.cjs` + `ai-release-risk-pr-comment.yml`; `RELEASE_RISK_SCALE_REGRESSION|ROUTE|SMOKE` env tuning; gitignore recovery/webhook state files.

- **Owner mandate (2026-03-22 session reaffirmed):** full permission for autonomous implementation/deploy of these automations and next waves; persisted in SOUL/HEARTBEAT/MEMORY/daily memory.
- **Static health + PR risk wave (2026-03-22):** automation-health snapshot now mirrors to `public/api/automation-health*.json` during generation and commit workflows include static files; new PR labeler `label-pr-automation-risk.cjs` + `ai-automation-risk-label.yml` sets `automation-risk-low|medium|high` from main health severity + diff blast radius and upserts rationale comment.
- **Static health + PR risk wave (2026-03-22):** automation-health snapshot now mirrors to `public/api/automation-health*.json` during generation and commit workflows include static files; new PR labeler `label-pr-automation-risk.cjs` + `ai-automation-risk-label.yml` sets `automation-risk-low|medium|high` from main health severity + diff blast radius and upserts rationale comment.

- **OpenClaw + OpenRouter (safe wiring):** `scripts/openclaw-app-improve.sh` now sources `~/.openclaw/openclaw.env` (optional) for `OPENROUTER_API_KEY`; added `docs/openclaw-openclaw.env.example`, `OPENCLAW.md` section, and `.gitignore` `openclaw.env` (repo-root accidents). **Do not commit API keys**; rotate any key exposed in chat.
