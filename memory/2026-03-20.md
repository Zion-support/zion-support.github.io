# 2026-03-20

- Added a new AI Lab product: `autonomous-ai-experience-studio` (in-browser launch intelligence simulator).
- Promoted new/live AI experiences on the homepage with direct entry points from `app/page.tsx`.
- Added automation guard `ai-ai-lab-integrity-guard.cjs` to detect duplicate IDs/hrefs and missing internal routes in AI Lab + featured items.
- Wired integrity guard into CI (`ci-cd.yml`) and npm scripts (`ai-lab:integrity-check`).
- Updated heartbeat and soul docs to reflect trusted autonomous operation mandate while preserving safety constraints.
- Reinforced standing permission: continue autonomous implementation and deployment cycles by default, with quality gates and secret-safety always enforced.
- Added new AI Lab experience `autonomous-deploy-optimizer` and promoted it on the homepage as a live in-browser deployment intelligence tool.
- Tightened deploy workflow behavior to fail fast when `NETLIFY_BUILD_HOOK` is missing, preventing silent non-deploys.
- Reaffirmed owner standing permission for fully autonomous implementation/deployment cycles; persisted mandate in `SOUL.md` and `HEARTBEAT.md`.
- Hardened key GitHub Actions with safer shell execution (`set -euo pipefail`), retry/timeouts for deploy hooks, and deterministic push behavior (`origin HEAD:main`).
- Added new AI Lab experience `autonomous-funnel-orchestrator` and promoted it from the homepage for faster discovery of conversion-intelligence workflows.
- Extended `ai-ai-lab-integrity-guard.cjs` to validate `app/config/aiCatalog.ts` and homepage internal route references to reduce duplicate/broken-link regressions.
- Implemented automated smoke-route source of truth (`scripts/automation/generate-smoke-routes.cjs`) with CI drift check and shared route file (`config/smoke-routes.txt`) consumed by deploy smoke workflows.
- Reaffirmed owner full autonomous permission again and continued autonomous improvement/deployment cycle without confirmation loops.
- Added `autonomous-conversion-copilot` as a new in-browser AI Lab experience to close a promoted-route gap and strengthen conversion-focused autonomous planning.
- Implemented autonomous reliability layer: `deploy-watchdog.cjs` (post-deploy health polling) and `validate-promoted-routes.cjs` (homepage AI route existence checks), wired into deploy/smoke workflows.
- Verified production `ziontechgroup.com` live routes for:
  - `/ai-lab/autonomous-rag-knowledge-workspace`
  - `/ai-lab/autonomous-media-prompt-studio`
  - and confirmed the AI Lab hub lists both tools.
- Verified production `ziontechgroup.com` live routes for:
  - `/ai-lab/autonomous-retention-playbook`
  - `/ai-lab/autonomous-conversion-copilot`
  - `/ai-services/autonomous-growth-intelligence`
- Updated `deploy-on-push.yml` to proceed with smoke/route watchdog even when `NETLIFY_BUILD_HOOK` is missing (so the pipeline self-diagnoses instead of aborting early).
- Updated `commands/smoke-check.cjs` to use `config/smoke-routes.txt` as source-of-truth for route checks.
- Verified live production after deploy workflow fix:
  - Homepage includes the new promoted AI Lab tokens (in-browser experiences).
  - `/ai-lab` and promoted AI Lab routes return `200` after redirects.
