## 2026-03-19 — AI Experiments link verification

### Live-site verification
- Confirmed the `/ai-experiments/` page now includes the CTA link:
  - Text: `Explain build failures`
  - Href: `/ai-lab/build-failure-explainer/`
  - Verified via fresh `WebFetch` calls (cache-busting + non-busted URLs).

### Quality gates
- Ran `npm run lint:check`
- Ran `npm run type-check`
- Ran `npm run test:ci`

