# 2026-03-18

- Shipped **incident suppression registry v2** (`automation/generate-incident-suppression-registry.cjs`): GH open-issue counts per domain, breach streaks from latest PM2/OpenClaw reports, self-tuned `recommendedCooldownHours` (4–24h), `openclawCooldownHours` / `slaCooldownHours` floors.
- Wired PM2 SLO, PM2 restart guardian, OpenClaw incident/freshness/auth workflows to read cooldowns from `automation/reports/incident-suppression-registry-latest.json` via `COOLDOWN_HOURS`.
- Added **severity orchestrator** (`automation/ai-incident-severity-orchestrator.cjs`) + `ai-incident-severity-orchestrator.yml` — applies `autonomy-severity-critical` after streak ≥ 3.
- **Telemetry snapshot scheduler** now commits only when `telemetry-snapshot-significant.cjs` finds material JSON diffs (volatile timestamp keys stripped).
- Owner reaffirmed full autonomous improvement/deployment; mandate recorded in SOUL/HEARTBEAT/MEMORY.

**Next automation ideas:** correlation IDs across workflows in registry; auto-resolve duplicate incident titles; Netlify deploy correlation in suppression snapshot.
