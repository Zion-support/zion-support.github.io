# 2026-03-18

- Shipped observability + Netlify + incident UX wave:
  - `automation/observability-ema-webhook-digest.cjs` + `ai-observability-ema-webhook-daily.yml` (optional Slack/Discord/generic webhooks; cooldown file committed when present).
  - `scripts/automation/fetch-netlify-deploy-for-sha.cjs` — polling with `NETLIFY_DEPLOY_POLL_ATTEMPTS` / `NETLIFY_DEPLOY_POLL_DELAY_MS`.
  - `scripts/automation/smoke-netlify-deploy-preview.cjs` + deploy-on-push step; commit includes `netlify-preview-smoke-latest.json` when changed.
  - `automation/ai-incident-sibling-crosslink.cjs` + `ai-incident-sibling-crosslink-weekly.yml`.
  - Homepage dark “Automation health” strip: EMA from suppression registry, deploy status, Netlify preview link, optional preview smoke summary.
- Owner reaffirmed full autonomous permission; persisted in SOUL, HEARTBEAT, MEMORY.

## Next automation ideas (backlog)

Completed this wave:
1. Artifact upload for `netlify-preview-smoke-latest.json` in deploy-on-push.
2. Deploy-drift dashboard preview smoke tile + EMA/FP trend sparkline (from history snapshots).
3. PagerDuty/Opsgenie route for dual EMA+FP observability breach.
4. Auto-open deduped issue when preview smoke fails N times in a row.
5. Netlify API lookup cache sidecar to reduce polling pressure.

## Next automation ideas (new backlog)

1. Auto-close `netlify-preview-smoke-repeated` issue once streak returns to 0 for 2 consecutive runs.
2. Add a compact `/api/automation-health.json` static snapshot consumed by homepage + AI Lab pages.
3. Add severity tiers to observability history and colorized badges in deploy-drift dashboard.
4. Add Slack thread follow-up links for related sibling incidents to reduce triage time.
5. Add daily guard that fails if observability history file stops updating for >48h.
