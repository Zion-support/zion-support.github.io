# 2026-09-21

## Pages deploy gap (Family A + MIT FGV)

### P0
- Live `ziontechgroup.com/` still 526KB Next.js shell (`/_next/`, last-modified 2026-09-21 21:26:05 GMT).
- main tip already sealed: `public/index.html` Family A (~5.5KB, Discovery $99, site.css); MIT has `#proof-fgv` / SDCOMPRASTIC-6963.
- Continuous Pages Deploy #71405 (run 35657344060) cancelled. In-flight run 35654644483 is pre-#71405 emergency-recovery SHA `1b57b93b3110` (60m Next.js build).
- Root cause: `cancel-in-progress: false` + long Next.js build. New commercial/dispatch runs queue, then GitHub cancels the *previously pending* run when the next one arrives. Hermes STATUS.md is already paths-ignored; CI/dispatch floods still share the slot.
- `gh run cancel` / `workflow_dispatch` are 403 from this agent.

### Fix in flight
- Branch `cursor/pages-deploy-concurrency-8c31`: Continuous Pages Deploy uses `prepare-pages-out.sh` (public/ first), commercial path allowlist, STATUS/ops ignore, same concurrency group with `cancel-in-progress: true` so merge preempts the stale Next.js job.
- prepare-pages-out guards: Family A homepage, MIT FGV, no AAA `$6,500`.

## Internal link hotfix

### Task
Kleber asked Chefe de gabinete to fix all broken internal links on ziontechgroup.com (252 404s from the 2026-09-21 audit).

### What shipped
- Rewrote `public/services/` and `public/about/` to official hubs only (no invented premium SKUs).
- Stripped premium links from industry leaves + industries hub + use-cases hub.
- Thin HTML redirect stubs + `_redirects` 301s for 9 `/services/premium/*` paths.
- Field-services LOCK: hub grid now lists only the 6 live country paths (brazil, grenada, haiti, honduras, hong-kong, hungary). Did not rewrite those 6 leaves.
- 225 dead country URLs: HTML stubs under `public/field-services/<slug>/` + explicit `_redirects` 301 → `/field-services/`.
- Misc commercial 404s (pricing-calculator, affiliate, explore, roteiros, EN ai-automation, 3 service stubs, 6 use-cases, 4 blogs) → nearest live hub.
- Removed bad `/case-studies/ → /contact/` sink so FGV case-studies stay reachable.

### Notes
- Root `_redirects` is the CPD source of truth (`gh-pages.yml` copies it last). GitHub Pages does not honor Netlify splats; HTML stubs are what actually stop 404s on Pages.
- The 5 non-Brazil “live” country pages were already thin redirects to `/zion-field/countries/*.html` in the repo; left untouched.

### Branch
`cursor/fix-broken-internal-links-4d89`

## Field-services country page recovery (URGENT)

### Cause
`d7ce56465f80` / closed #71408 replaced 225 real `/field-services/<country>/` rate cards with meta-refresh stubs to the hub and trimmed the hub to 6 countries. `public/zion-field/countries/` never existed in this repo.

Real pages last lived at `a743c5c06b8d` / `d7d7cd61663b` / `47d479b305d9` (231 countries). Last full tree before the Sep-13 Family A hub-only rewrite: `530ed6d2d418`. They disappeared from the `dad00de3` / `4d0a7ff25d32` lineage, then #71408 stamped stubs onto main.

### What this session did
- Kept Brazil Family A byte-identical to `d7ce564`.
- Restored remaining stub leaves from `a743c5c06b8d` (same source as the in-flight 12-page batches already on main).
- Hub already lists 231 countries (restored earlier on main by `c3c3600945be`); country→hub 301s already stripped.
- Branch: `cursor/restore-field-services-countries-cd9c`
