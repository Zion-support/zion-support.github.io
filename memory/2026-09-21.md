# 2026-09-21 — Unified page layout

- Added shared `PageShell` plus design tokens (`container-page`, `page-card`, `page-hero`, `page-cta`) so company, legal, portal, tools, and blog routes share one chrome.
- Migrated primary nav/footer pages (about, contact, pricing, FAQ, partners, careers, academy, agents, solutions, tools, portal, legal) onto `PageShell`.
- Normalized generated service landings with CSS so nested `<main>` wrappers, cards, and CTAs match the dark slate/purple system.
- Footer audience routes (`/providers`, `/integrators`, `/it-vendors`, `/business-customers`, `/governments`) now exist as first-class pages.
- Standing owner mandate remains: implement, validate, and ship without confirmation loops.
