# 2026-03-21

- **Suppression registry v3:** EMA-smoothed open-incident noise → `recommendedCooldownHours`; `correlation` ties registry generation to Actions run URL, commit SHA, and `deploy-status-latest.json` (optional Netlify id/url when env/secrets set).
- **Deploy status on main:** `deploy-on-push` now commits `automation/reports/deploy-status-latest.json` (best-effort push) so scheduled registry runs read fresh deploy context.
- **Severity symmetry:** `ai-incident-severity-orchestrator.cjs` removes `autonomy-severity-critical` when domain breach streak &lt; 3; PM2 SLO escalation titles aligned (`PM2 SLO critical breach detected` + legacy title for downgrade).
- **generate-deploy-status.cjs:** accepts `NETLIFY_DEPLOY_ID` / `NETLIFY_DEPLOY_URL` (and aliases) for future wiring from Netlify API or manual secret.
- **PM2 static-checks:** PR drift comment step uses `continue-on-error: true` so forked PRs / `gh` token limits do not fail lint + type-check.
- **PM2 CI wave:** stable drift comment upsert (`gh api`), `ecosystem:validate-scripts` in static-checks, ops digest `correlation` + optional `TELEGRAM_*` in `ai-pm2-ops-digest.yml`, PagerDuty/GENERIC contention webhooks, PR labels `pm2`/`automation` when ecosystem file changes (`issues: write` for `gh label create`).

**Next automation ideas:** parse Netlify build-hook response or API poll for real deploy id; add `correlationId` into issue bodies via dedupe template; cross-link registry JSON from deploy-drift dashboard UI.

- **Shipped (follow-up):** `ai-incident-suppression-registry.yml` now passes `GITHUB_RUN_ID`, `GITHUB_REPOSITORY`, `GITHUB_SHA`, `GITHUB_WORKFLOW`, `GITHUB_SERVER_URL` into `generate-incident-suppression-registry.cjs` so every registry JSON includes live correlation fields.

- **Automation intelligence wave:** `gh-issue-close-on-recovery.cjs` + `validate-workflow-issue-dedupe-contract.cjs` (wired into `automation:preflight`); `generate-automation-fingerprint-incident-digest.cjs` + weekly workflow `ai-automation-fingerprint-digest-weekly.yml`; preflight runs `pin-actions-weekly.cjs` and warns on unpinned `uses:`; many guards now auto-close fingerprint issues on healthy paths; deploy/route/Openclaw bodies include **Dedupe key** lines; `ai-launch-digest-freshness-sla.yml` adds checkout for Node helpers; npm scripts `issues:close-on-recovery`, `workflows:issue-dedupe-contract`, `automation:fingerprint-digest`; docs `docs/automation-fingerprint-incident-digest.md` + dedupe helper updates; owner full autonomous permission reaffirmed in SOUL/HEARTBEAT/MEMORY.
- **Owner reaffirmed autonomy (newest request):** proceed fully autonomously with implementation/deployment and iterative automation ideas without confirmation loops; persist this mandate across SOUL/HEARTBEAT/MEMORY/daily memory.
