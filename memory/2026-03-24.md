# 2026-03-24

- Workflow grep guards: enforce **top-level `permissions:`** on every `.github/workflows/*.{yml,yaml}` (with `find`, portable).
- `ai-observability-ema-webhook-daily.yml`: **concurrency** group `ai-observability-ema-webhook-daily`, `cancel-in-progress: false` so state commits to `main` do not overlap.
- **Push + concurrency policy:** eight workflows that `git push origin HEAD:main` lacked `concurrency:` (issue index, opportunity radar, sibling crosslink, suppression registry, nav audit fix/pages, telemetry compactor/snapshot) — added queued groups with `cancel-in-progress: false`. **`run-workflow-grep-guards.sh --push`** now fails if any push-to-main workflow omits top-level `concurrency:`.
- **`pin-actions-weekly`:** `cancel-in-progress: false` (was `true`) so pin commits to `main` are not aborted mid-run. Grep guard extended: push-to-main workflows must not use `cancel-in-progress: true`. **`ai-github-actions-audit`:** replace `git push … || true` with explicit warning on failure.
- **Job `timeout-minutes`:** `parse-github-workflows-yaml.rb` now fails if any job omits `timeout-minutes`. Fixed the only two gaps: `ai-app-evolution-trigger-on-regression` (45m), `ai-launch-digest-freshness-sla` (10m).
- **Job `runs-on`:** parse contract requires `runs-on` for any job that defines `steps` (skips reusable-workflow callers with `uses:`). **`workflow-yaml-sanity`** now runs **`run-workflow-grep-guards.sh`** after the Ruby parse (same as `workflows:validate:light`, no npm); triggers extended when the grep script changes.
- **Workflow `name` + `on`:** `parse-github-workflows-yaml.rb` requires a non-empty top-level **`name`** and **`on:` as a mapping** (Hash), not a scalar (`data['on'] || data[true]` for Psych/YAML 1.1 `on` key).
- **Non-empty `jobs`:** parse contract requires at least one job per workflow.
- **No `pull_request_target`:** `run-workflow-grep-guards.sh` always fails if that trigger appears (fork PR token risk); runs after `--pin` / `--permissions` / `--push` subsets too.
- **PAT leak scan:** grep guards reject `github_pat_*` / `ghp_*` / `gho_*` / etc. in workflow YAML. **`pull_request_target` check** uses YAML key pattern `pull_request_target:` only (avoids false positives in step names).
- **Bearer leak scan:** grep guards reject `Authorization: Bearer` followed by obvious PAT prefixes in workflow YAML.
- **No `self-hosted`:** grep guards reject the substring `self-hosted` in workflow YAML (GitHub-hosted runners only unless policy changes).
- **Pin guard:** also rejects floating **`@v*`** refs on **non-`actions/*`** steps and **`docker://...:latest`**.
- **Push helpers (repo-wide):** migrated workflows off inline `git push origin HEAD:main` to **`scripts/automation/commit-and-push-main.sh`** (commit + rebase-retry push) or **`scripts/automation/push-main-with-retry.sh`** (post-rebase push only). **`commit-and-push-main.sh`** supports optional **`COMMIT_AND_PUSH_RECORD_OUTPUT=true`** for `committed=` in **`GITHUB_OUTPUT`**. Grep guards treat both scripts like push-to-main for **concurrency** / **`cancel-in-progress`** rules; empty **`hits[@]`** loop fixed for **`set -u`**. Adoption audit counts **`push-main-with-retry.sh`** as helper usage.
