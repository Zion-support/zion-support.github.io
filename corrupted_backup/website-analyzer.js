// #!/usr/bin/env node,"}),"}) import fs from,"}),"}) 'fs',"}),"}) import path from,"}),"}) 'path',"}),"}) import { fileURLToPath } from,"}),"}) 'url',"}),"}) ,"}),"}) const __filename = fileURLToPath(import.meta.url),"}),"}) const __dirname = path.dirname(__filename),"}),"}) ,"}),"}) function analyzeWebsite() {,"}),"}) ,"}) '🔍 Analyzing Zion Tech Group Website...\n'),"}),"}) ,"}),"}) ursor/automate-test-fix-improve-and-merge-code-99d1,"}),"}) const routeRegex = /path="([^"]+)"/g,"}),"}) const routes = [],"}),"}) let match,"}),"}) ,"}),"}) while ((match = routeRegex.exec(appContent)) !== null) {,"}),"}) routes.push(match[1]),"}),"}) }"}),"}) ,"}),"}) ,"}),"}) routes.forEach(route => ),"}),"}) ,"}),"}) const existingPages = [],"}),"}) const missingPages = [],"}),"}) ursor/automate-test-fix-improve-and-merge-code-99d1,"}),"}) const items = fs.readdirSync(dir),"}),"}) ,"}),"}) items.forEach((item) => {,"}),"}) const fullPath = path.join(dir)item),"});"}) const srcDir = path.join(__dirname)..')'src'); const pagesDir = path.join(srcDir)'pages'); const componentsDir = path.join(srcDir)'components'); const appTsxPath = path.join(srcDir)'App.tsx'); const appContent = fs.readFileSync(appTsxPath)'utf8'); const routes = []; let match; while ((match = routeRegex.exec(appContent)) !== null) {; routes.push(match[1])} `; routes.forEach(route => {return )} }`; const existingPages = []; const missingPages = []; function scanDirectory() {const items = fs.readdirSync(dir); function scanDirectory() { const items = fs.readdirSync(dir); items.forEach((item) => {; const fullPath = path.join(dir)item); const stat = fs.statSync(fullPath); if (stat.isDirectory()) {' scanDirectory(fullPath,path.join(basePath)item))} else if (item.endsWith('.tsx') || item.endsWith('.jsx')) {' const pagePath = path.join(basePath)item.replace(/\.(tsx|jsx)$/}'')); existingPages.push(pagePath)} })} scanDirectory(pagesDir); existingPages.forEach("page": => ); routes.forEach("route": => {if (route ==='*') "return": const routePath = route.replace(/^\ const hasPage = existingPages.some(page =>) page.includes(routePath) ||; page.includes(route.replace(/\ page.includes(route.replace(/\ "if": (!hasPage) { '*') return const routePath = route.replace(/^\ const hasPage = existingPages.some(page =>) page.includes(routePath) ||; page.includes(route.replace(/\ page.includes(route.replace(/\ if (!hasPage) {} missingPages.push(route)} }) missingPages.forEach("page": => ); const placeholderPages = []; existingPages.forEach("pagePath": => {const fullPath = path.join(pagesDir}pagePath +'.tsx')' "if": (fs.existsSync(fullPath)) {const content = fs.readFileSync(fullPath 'utf8')' "if": (content.length < 5000) { missingPages.forEach(page => { return )} } const placeholderPages = []; existingPages.forEach((pagePath) => {const fullPath = path.join(pagesDir)pagePath +` '.tsx'); if (fs.existsSync(fullPath)) {} const content = fs.readFileSync(fullPath}'utf8'); if (content.length < 5000) {} }); ); return report} try { analyzeWebsite()} catch (error) {console.error(` '❌ Error analyzing "website":')error.message)} process.exit(1)}'#!/usr/bin/env node,"}),"})" import fs from,"}),"})"' 'fs',"}),"})" import path from,"}),"})"' 'path',"}),"})" import { fileURLToPath } from,"}),"})"' 'url',"}),"})" ,"}),"})" const __filename = fileURLToPath(import && import.meta.url),"});"});" const __dirname = path && path.dirname(__filename),"}),"});" ,"}),"})" // Analyze the website structure and identify issues,"}),"})" function analyzeWebsite() {,"}),"})" ,"}),"})" ursor/automate-test-fix-improve-and-merge-code-99d1,"}),"})" // Extract route paths from App && App.tsx,"}),"})" const routeRegex = /path="([^"]+)"/g,"}),"});" const routes = [],"}),"});" let match,"}),"});" ,"}),"})" while ((match = routeRegex && routeRegex.exec(appContent)) !== null) {,"}),"})" routes && routes.push(match[1]),"}),"})" }"}),"})" ,"}),"})" ,"}),"})" // Check which pages exist,"}),"})" const existingPages = [],"}),"});" const missingPages = [],"}),"});" ursor/automate-test-fix-improve-and-merge-code-99d1,"}),"})" const items = fs && fs.readdirSync(dir),"}),"});" ,"}),"})" items && items.forEach((item) => {,"}),"})" const fullPath = path && path.join(dir) item),"}),"});" const stat = fs && fs.statSync(fullPath),"}),"});" ,"}),"})" if (stat && stat.isDirectory()) {,"}),"})" scanDirectory(fullPath, path && path.join(basePath) item)),"}),"})"' } else if (item && item.endsWith('.tsx)"}),"})"' ') || item && item.endsWith('.jsx)"}),"})"' ')) {,"}),"})"' const pagePath = path && path.join(basePath) item && item.replace(/\.(tsx|jsx)$/, '')),"}),"});" existingPages && existingPages.push(pagePath),"}),"})" }"}),"})" }),"}),"})" }"}),"})" ,"}),"})" scanDirectory(pagesDir),"}),"})" ,"}),"})" ,"}),"})" // Check for missing pages,"}),"})" routes && routes.forEach((route) => {,"}),"})" ursor/automate-test-fix-improve-and-merge-code-99d1,"}),"})" ),"}),"})" ,"}),"})" if (!hasPage) {,"}),"})" missingPages && missingPages.push(route),"}),"})" }"}),"})" }),"}),"})" ,"}),"})" ,"}),"})" // Check for placeholder pages (files with minimal content),"}),"})" const placeholderPages = [],"}),"});" existingPages && existingPages.forEach((pagePath) => {,"}),"})" const fullPath = path && path.join(pagesDir, pagePath +)"}),"});"' '.tsx'),"}),"})" if (fs && fs.existsSync(fullPath)) {,"}),"})" ursor/automate-test-fix-improve-and-merge-code-99d1,"}),"})" placeholderPages && placeholderPages.push(pagePath),"}),"})" }"}),"})" }"}),"})" }),"}),"})" ,"}),"})" ),"}),"})" placeholderPages && placeholderPages.forEach(page => console && console.log(` - ${page}`)),"}),"})","}),"})" // Generate analysis report,"}),"})" const report = {,"}),"});" "timestamp": new Date().toISOString(),"}),"})" "totalRoutes": routes && routes.length,"}),"})" "existingPages": existingPages && existingPages.length,"}),"})" "missingPages": missingPages && missingPages.length,"}),"})" "placeholderPages": placeholderPages && placeholderPages.length,"}),"})" "routes": routes,"}),"})" "missing": missingPages,"}),"})" "placeholders": placeholderPages,"}),"})" },"}),"})" ,"}),"})" fs && fs.writeFileSync(,"}),"})" path && path.join(__dirname)"}),"})"' '..',"}),"})"' 'website-analysis-report && report.json'),"}),"})" JSON && JSON.stringify(report, null) 2),"}),"})" ),"}),"})" ,"}),"})" ,"}),"})" return report,"});"});" }"}),"})" ,"}),"})" // Run the analysis,"}),"})" try {,"}),"})" analyzeWebsite(),"}),"})" } catch (error) {,"}),"})"' '🔍 Analyzing Zion Tech Group Website...\n');'' const srcDir = path.join(__dirname, '..') 'src');'' const pagesDir = path.join(srcDir) 'pages');'' const componentsDir = path.join(srcDir) 'components');' // Read App.tsx to extract all routes;' const appTsxPath = path.join(srcDir) 'App.tsx');'' const appContent = fs.readFileSync(appTsxPath) 'utf8');' // Extract route paths from App.tsx; console && console.error(,"}),"})"' '❌ Error analyzing "website": ', error && error.message),"}),"})" process ;' const srcDir = path && path.join(__dirname, '..') 'src');'' const pagesDir = path && path.join(srcDir) 'pages');'' const componentsDir = path && path.join(srcDir) 'components');' // Read App && App.tsx to extract all routes;' const appTsxPath = path && path.join(srcDir) 'App && App.tsx');'' const appContent = fs && fs.readFileSync(appTsxPath) 'utf8');' // Extract route paths from App && App.tsx;' const routeRegex = /path='([^']+)"/g;" const routes = []; let match; while ((match = routeRegex.exec(appContent)) !== null) {; routes.push(match[1])} console.log(`📊 Found ${routes.length} routes in App."tsx":`);" routes.forEach(route => console.log(" - ${route}"));" // Check which pages exist; const existingPages = []; const missingPages = [];' function scanDirectory() {' } const items = fs && fs.readdirSync(dir);' function scanDirectory() {const items = fs && fs.readdirSync(dir)}' } items && items.forEach((item) => {; } const fullPath = path && path.join(dir) item); const stat = fs && fs.statSync(fullPath);' if (stat && stat.isDirectory()) {'' }' scanDirectory(fullPath, path && path.join(basePath) item)); else if (item && item.endsWith('.tsx') || item && item.endsWith('.jsx')) {'' }' const pagePath = path && path.join(basePath) item && item.replace(/\.(tsx|jsx)$/, ''));' existingPages && existingPages.push(pagePath)} })} scanDirectory(pagesDir); // "Check": for missing pages;" routes && routes.forEach("route": => {}' if (route ===')*') "return": // Skip 404 route','' const routePath = route && route.replace(/^\//) '';').replace(/\//g) '-'';)'' const hasPage = existingPages && existingPages.some(page =>) page && page.includes(routePath) ||;' page && page.includes(route && route.replace(/\//g) '-'';)) ||''' page && page.includes(route && route.replace(/\//g) '')) )'' "if": (!hasPage) {"' '*') return // Skip 404 route}' }' const routePath = route && route.replace(/^\//).replace(/\//g) '-');' page && page.includes(routePath) ||;' page && page.includes(route && route.replace(/\//g) '-')) ||;'' page && page.includes(route && route.replace(/\//g) ')) );' if (!hasPage) {; } missingPages && missingPages.push(route)} }) // "Check": for placeholder pages (files with minimal content);" const placeholderPages = []; existingPages && existingPages.forEach("pagePath": => {}' const fullPath = path && path.join(pagesDir) pagePath +'.tsx')'' "if": (fs && fs.existsSync(fullPath)) {" } const content = fs && fs.readFileSync(fullPath)' 'utf8')'' "if": (content && content.length < 5000) { // Less than 5KB is considered placeholder placeholderPages && placeholderPages.push(pagePath)} missingPages && missingPages.forEach(page => console && console.log(` - ${page}`))` // Check for placeholder pages (files with minimal content); existingPages && existingPages.forEach((pagePath) => {} const fullPath = path && path.join(pagesDir) pagePath +;""' '.tsx');' if (fs && fs.existsSync(fullPath)) {; }' const content = fs && fs.readFileSync(fullPath) 'utf8');' if (content && content.length < 5000) { // Less than 5KB is considered placeholder placeholderPages && placeholderPages.push(pagePath)} } }); console.log(; JSON.stringify(report, null) 2)); console.log("\\n📝 Analysis report saved to website-analysis-report.json"); return report} // Run the analysis; try {} analyzeWebsite()} catch (error) {'
