import { defineConfig, devices } from "@playwright/test";

export default defineConfig({
  testDir: "tests/e2e",
  use: {
  },
  projects: [
    {
      name: "Desktop Chrome",
      use: {
      },
    },
    {
      name: "Desktop Firefox",
      use: {
      },
    },
    {
      name: "Desktop Safari",
      use: {
      },
    },
    {
      name: "Mobile Chrome",
      use: {
      },
    },
    {
      name: "Mobile Safari",
      use: {
      },
    },
  ],
  reporter: [
  ],
});
