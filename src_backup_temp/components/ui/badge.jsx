import React from 'react';
export { Badge };
;
export { Badge };

return()
    <div className={`${baseClasses} ${variantClasses[variant]} ${className}`}>
      {children}
    </div>
    );
};

export {Badge};

export {Badge};

export {Badge};

export {Badge};

export {Badge};

export {Badge};
