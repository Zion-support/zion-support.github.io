import {cn} from "@/lib/utils";
import React from "react";
;
export function GradientHeading("props": "any) {;
    const Tag = level;
    ;
    return (;
        <Tag ;
            id={id"} ;
            className={cn(;
                "text-4xl font-bold tracking-tight bg-gradient-to-r from-zion-cyan via-zion-purple-light to-zion-purple bg-clip-text text-transparent",;
                className;
            )}
        >;
            {children}
        </Tag>;
    );
}
;
export default GradientHeading;
import {cn} from &quot;@/lib/utils&quot;; import React from &quot;react&quot;; export function GradientHeading($1) { const Tag = level; return ( <Tag id={id} className={cn( &quot;text-4xl font-bold tracking-tight bg-gradient-to-r from-zion-cyan via-zion-purple-light to-zion-purple bg-clip-text text-transparent&quot;,className )} > {children} </Tag> )} export default GradientHeading;
const {cn} from "@/lib/utils"; import React from "react"; export function GradientHeading($1) { const Tag = level; return ( <Tag id={id} className={cn( "text-4xl font-bold tracking-tight bg-gradient-to-r from-zion-cyan via-zion-purple-light to-zion-purple bg-clip-text text-transparent",className )} > {children} </Tag> )} export default GradientHeading;""
import {cn} from "@/lib/utils"; import _React from "react"; export function GradientHeading(_) { const Tag = level; return ( <Tag id={id} className={cn( "text-4xl font-bold tracking-tight bg-gradient-to-r from-zion-cyan via-zion-purple-light to-zion-purple bg-clip-text text-transparent",className )} > {children} </Tag> )} export default GradientHeading;
