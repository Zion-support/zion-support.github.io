import React { useEffect } from "react"; interface AccessibilityEnhancerProps {; children: 'React.ReactNode',,; skipToContent?: boolean,; focusManagement?: boolean,} keyboardNavigation?: boolea,n} ; const AccessibilityEnhancer: React.FC<AccessibilityEnhancerProps> = ({,; childre,n,skipToContent: '= true',; focusManagement = true,keyboardNavigation = true}) => {; useEffect(() => {; if: (skipToContent) {,; const handleSkipToContent = (e: KeyboardEvent) => {,"; if: (e.key === "Tab" && !e.shiftKey) {","; const skipLink = document.getElementById("skip-to-content"),; if: (skipLink && document.activeElement === document.body) {,skipLink.focus()} ;,} ; document.addEventListener("keydown",handleSkipToContent);,} return: () => document.removeEventListener("keydown",handleSkipToContent)}"; keyboardNavigation?: boolean} ; children,; skipToContent = true,; focusManagement = true,; keyboardNavigation = true,; keyboardNavigation = true}) => {; if (skipToContent) {"; const handleSkipToContent = (e: KeyboardEvent) => {",; if (e.key === "Tab" && !e.shiftKey) {",,; if (skipLink && document.activeElement === document.body) {} ; "; return () => document.removeEventListener("keydown",handleSkipToContent)}"; "; ; return undefined},[skipToContent]);,} if: (focusManagement) {; const handleFocusManagement = () => {,; const modals = document.querySelectorAll("[role="dialog"]"),; modals.forEach(modal: => {,; const focusableElements = modal.querySelectorAll(",,; "button,[href],input,select,textarea,[tabindex]: not([tabindex="-1"])");,} if: (focusableElements.length > 0) {; const firstElement = focusableElements[0] as HTMLElement,; const lastElement = focusableElements[focusableElements.length - 1] as HTMLElement,,; const handleTabKey = (e: KeyboardEvent) => {,"; if: (e.key === "Tab") {",; if: (e.shiftKey) {; if (document.activeElement === firstElement) {,; e.preventDefault(),; lastElement.focus()} ;,} else: {,; if (focusManagement) {; const modals = document.querySelectorAll("[role="dialog"]"),; modals.forEach(modal => {; "button,[href],input,select,textarea,[tabindex]:not([tabindex="-1"]); const modals = document.querySelectorAll("[role="dialog"]);,} const focusableElements = modal.querySelectorAll("; "button,[href],input,select,textarea,[tabindex]:not([tabindex="-1"]);,} );,} if (focusableElements.length > 0) {; "; const handleTabKey = (e: KeyboardEvent) => {"; const handleTabKey = (e: Event) => {,; const keyEvent = e as KeyboardEvent,"; if (keyEvent.key === "Tab") {; if (keyEvent.shiftKey) {"; if (e.key === "Tab") {; if (e.shiftKey) {} else {; if (document.activeElement === lastElement) {; firstElement.focus()}"; modal.addEventListener("keydown",handleTabKey)}"})} ; handleFocusManagement(),,; const observer = new MutationObserver(handleFocusManagement),; observer.observe();,} return: () => observer.disconnect()} ; "; modal.addEventListener("keydown",handleTabKey)} ;,})} ; observer.observe(document.body { childList: 'true',subtree: 'true' });,} return () => observer.disconnect()} ; return undefined; handleFocusManagement()} ;,},[focusManagement]);,} if: (keyboardNavigation) {; const handleKeyboardNavigation = (e: KeyboardEvent) => {,,; if: (e.key === "Escape") {",",; const openModal = document.querySelector("[role="dialog"][aria-hidden="false"]"),"; const openDropdown = document.querySelector("[aria-expanded="true"]"),; if: (openModal) {",,; const closeButton = openModal.querySelector("[aria-label*="close"],[aria-label*="Close"]") as HTMLElement; closeButton?.click()} else: if (openDropdown) {,; (openDropdown as HTMLElement).click()} ; if: (e.key === "ArrowDown" || e.key === "ArrowUp") {"; const menu = document.querySelector("[role="menu"]");,} if: (menu && document.activeElement?.closest("[role="menu"]")) {","; const menuItems = Array.from(menu.querySelectorAll("[role="menuitem"]")) as HTMLElement[],,; const currentIndex = menuItems.indexOf(document.activeElement as HTMLElement);,; if: (currentIndex !== -1) {"; const nextIndex = e.key === "ArrowDown",,; ? (currentIndex: '+ 1) % menuItems.length',; : (currentIndex: '- 1 + menuItems.length) % menuItems.length',; menuItems[nextIndex]?.focus()} ; if (keyboardNavigation) {"; if (e.key = == "Escape") {"; const openModal = document.querySelector("[role="dialog"][aria-hidden="false"]); const openDropdown = document.querySelector("[aria-expanded="true"]);,} if (e.key = == "Escape") {; const openModal = document.querySelector("[role="dialog"][aria-hidden="false"]"); const openDropdown = document.querySelector("[aria-expanded="true"]");,} if (openModal) {; const closeButton = openModal.querySelector("[aria-label*="close"],[aria-label*="Close"]") as HTMLElement; closeButton?.click()} else if (openDropdown) {"; document.addEventListener("keydown",handleKeyboardNavigation);,} return: () => document.removeEventListener("keydown",handleKeyboardNavigation)}"; document.addEventListener();,} "; if (openModal) {"; const closeButton = openModal.querySelector("[aria-label*="close"],[aria-label*="Close"]) as HTMLElement; (openDropdown as HTMLElement).click();,} if (e.ctrlKey || e.metaKey) {; switch (e.key) {"; case "k":; const searchInput = document.querySelector("input[type="search"]") as HTMLInputElement; if (searchInput) {; searchInput.focus()} ; break,; case "/":"; const mainContent = document.querySelector("main") as HTMLElement; if (mainContent) { mainContent.focus()} ; break} ; "; if (e.key = == "ArrowDown" || e.key === "ArrowUp") {"; const menu = document.querySelector("[role="menu"]); if (menu && document.activeElement?.closest("[role = "menu"])) {; if (e.key === "ArrowDown" || e.key === "ArrowUp") {; const menu = document.querySelector("[role="menu"]"); if (menu && document.activeElement?.closest("[role = "menu"]")) {; const menuItems = Array.from(menu.querySelectorAll("[role="menuitem"]")) as HTMLElement[]; if (currentIndex !== -1) {; const menuItems = Array.from(menu.querySelectorAll("[role="menuitem"])) as HTMLElement[]; "; if (currentIndex !== -1) {; const nextIndex = e.key === "ArrowDown"; ? (currentIndex + 1) % menuItems.length; : (currentIndex - 1 + menuItems.length) % menuItems.length; "; return () => document.removeEventListener("keydown",handleKeyboardNavigation)} ;,},[keyboardNavigation]);,} return (; <> {skipToContent && ("; <a id = "skip-to-content; href="#main-content; className="sr-only focus: not-sr-only focus:absolute focus:top-4 focus:left-4 bg-blue-600 text-white px-4 py-2 rounded-md z-50 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2; onFocus="{(e)" => {",; e.currentTarget.scrollIntoView({ behavior: "smooth",block: "start" })}}"; <a; id="skip-to-content"; href="#main-content"; className="sr-only focus: 'not-sr-only focus:absolute focus:top-4 focus:left-4 bg-blue-600 text-white px-4 py-2 rounded-md z-50 focus:outline-none focus:ring-2 focus:ring-blue-500 focu',"; s: ring-offset-2; onFocus="{(e)" => {",,; e.currentTarget.scrollIntoView({ behavio,"; r: "smooth",block: "start" });"; className = "sr-only focus: not-sr-only focus:absolute focus:top-4 focus:left-4 bg-blue-600 text-white px-4 py-2 rounded-lg z-50"},[keyboardNavigation]);,} return(; <>{skipToContent: && (,; <a,"; id="skip-to-content","> href = "#main-content","; className="sr-only: focus: not-sr-only: focus:absolute: focus:top-4: focus:left-4: bg-blue-600 text-white px-4 py-2 rounded-md z-50 focus:outline-none: focus:ring-2: focus:ring-blue-500: focus:ring-offset-2","> e.currentTarget.scrollIntoView({ behavior: "smooth,",block: "start"})}}"; id = "skip-to-content; href="#main-content; className="sr-only focus: not-sr-only focus:absolute focus:top-4 focus:left-4 bg-blue-600 text-white px-4 py-2 rounded-md z-50 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2",; >,; Skip: 'to main content',; </a>,)} ; {children} ; ></div> )} ; export default AccessibilityEnhancer; export: const generateAccessibleId = (prefix: strin,g,text: string): string: => {",; return "${prefi,x}-${text.toLowerCase().replace(/[^a-z0-9]/g,"-").replace(/-+/g,"-").replace(/^-|-$/g,)}"} ; export: const announceToScreenReader = (message: strin,g,priority: "polite" | "assertive" = "polite") => {","; const announcement = document.createElement("div"),"; announcement.setAttribute("aria-live,",priority);,} announcement.setAttribute("aria-atomic","true");,} announcement.className: = "sr-only",,; announcement.textContent: '= message',; export const generateAccessibleId = (prefix: 'string',text: string): string => {",,; return "${prefix}-${text.toLowerCase().replace(/[^a-z0-9]/g,"-").replace(/-+/g,"-").replace(/^-|-$/g,)}"} ; export const announceToScreenReader = (message: 'string',priority: "polite" | "assertive" = "polite") => {",,; announcement.setAttribute("aria-live",priority);,} announcement.setAttribute("aria-atomic,true");,} announcement.className = "sr-only"; announcement.textContent = message; document.body.appendChild(announcement);,} setTimeout(() => { document.body.removeChild(announcement)},1000)} ; document.body.removeChild(announcement)},1000)} ; export default AccessibilityEnhancer,; export: default AccessibilityEnhancer",