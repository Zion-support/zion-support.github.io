import React,{ useEffect,useState } from "react"; type FontSize = "small" | "normal" | "large" | "xlarge"; interface AccessibilitySettings { highContrast: 'boolean; fontSize: FontSize; reducedMotion: boolean; focusVisible: boolean; screenReader: boolean;' } } const AccessibilityManager = () => { const [settings,setSettings] = useState<AccessibilitySettings>({ highContrast: 'false',fontSize: "normal",reducedMotion: 'false',focusVisible: 'false',screenReader: 'false',}); useEffect(() => { const saved = localStorage.getItem('accessibilitySettings'); if (saved) { try { const parsed = JSON.parse(saved) as AccessibilitySettings; setSettings(parsed); applyAccessibilityStyles(parsed)} catch (error) { console.error('Error parsing accessibility settings:',error)} } },[]); const updateSetting = (key: 'keyof AccessibilitySettings',value: boolean | FontSize) => { const newSettings = { ...settings,[key]: value } as AccessibilitySettings; setSettings(newSettings); localStorage.setItem('accessibilitySettings',JSON.stringify(newSettings)); applyAccessibilityStyles(newSettings)}; const applyAccessibilityStyles = (s: AccessibilitySettings) => { const root = document.documentElement; root.classList.toggle('high-contrast',s.highContrast); root.classList.remove('font-small','font-normal','font-large','font-xlarge'); root.classList.add(`font-${s.fontSize}`); root.classList.toggle('reduced-motion',s.reducedMotion); root.classList.toggle('focus-visible',s.focusVisible)}; return ( <div className="accessibility-manager"> <h2>Accessibility Settings</h2> <div className="setting-group"> <label> <input type="checkbox" checked={settings.highContrast} onChange={(e) => updateSetting('highContrast',e.target.checked)} /> High Contrast Mode </label> </div> <div className="setting-group"> <label> Font Size: <select value={settings.fontSize} onChange={(e) => updateSetting('fontSize',e.target.value as FontSize)} > <option value="small">Small</option> <option value="normal">Normal</option> <option value="large">Large</option> <option value="xlarge">Extra Large</option> </select> </label> </div> <div className="setting-group"> <label> <input type="checkbox" checked={settings.reducedMotion} onChange={(e) => updateSetting('reducedMotion',e.target.checked)} /> Reduce Motion </label> </div> <div className="setting-group"> <label> <input type="checkbox" checked={settings.focusVisible} onChange={(e) => updateSetting('focusVisible',e.target.checked)} /> Enhanced Focus Indicators </label> </div> <div className="setting-group"> <label> <input type="checkbox" checked={settings.screenReader} onChange={(e) => updateSetting('screenReader',e.target.checked)} /> Screen Reader Optimized </label> </div> </div> )}; export default AccessibilityManager;