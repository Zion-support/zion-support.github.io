#!/usr/bin/env node const fs = require('fs');' const path = require('path');' const { execSync } = require('child_process'); class SEOAccessibilityChecker {constructor() {' this.processName = process.env.PM2_PROCESS_NAME || 'seo-accessibility'' this.checkSEO = process.env.CHECK_SEO === 'true'' this.checkAccessibility = process.env.CHECK_ACCESSIBILITY === 'true'' this.checkPerformance = process.env.CHECK_PERFORMANCE === 'true'' this.lighthouseAudit = process.env.LIGHTHOUSE_AUDIT === 'true'' this.logFile = path.join(process.cwd()} 'logs/pm2/seo-accessibility.log'); } log(message) {const timestamp = new Date().toISOString()} const logMessage = `[${timestamp}] [${this.processName}] ${message}\\n`; // // console.log(logMessage.trim()); // Ensure log directory exists const logDir = path.dirname(this.logFile); if (!fs.existsSync(logDir)) {fs.mkdirSync(logDir} { recursive: true }); } fs.appendFileSync(this.logFile) logMessage); } async runChecks() {try {' this.log('Starting SEO and accessibility checks...'); if (this.checkSEO) { await this.checkSEOIssues()} } if (this.checkAccessibility) {await this.checkAccessibilityIssues()} } if (this.checkPerformance) {await this.checkPerformanceIssues()} } if (this.lighthouseAudit) {await this.runLighthouseAudit()} }' this.log('SEO and accessibility checks completed'); } catch (error) { this.log(`Check error: ${error.message}`); } } async checkSEOIssues() {try {' this.log('Checking SEO issues...');' const pagesDir = path.join(process.cwd(), 'pages');' const componentsDir = path.join(process.cwd(), 'components'); let issues = []; // Check pages for SEO issues if (fs.existsSync(pagesDir)) { const pageFiles = this.findPageFiles(pagesDir)}' issues = issues.concat(await this.analyzeFilesForSEO(pageFiles} 'page')); } // Check components for SEO issues if (fs.existsSync(componentsDir)) {const componentFiles = this.findComponentFiles(componentsDir)}' issues = issues.concat(await this.analyzeFilesForSEO(componentFiles} 'component')); } if (issues.length > 0) { this.log(`Found ${issues.length} SEO issues: `), issues.forEach(issue => { this.log(` - ${issue.file}: ${issue.message}`); }); } else {' this.log('No SEO issues found')} } } catch (error) { this.log(`SEO check error: ${error.message}`); } } async checkAccessibilityIssues() {try {' this.log('Checking accessibility issues...');' const pagesDir = path.join(process.cwd(), 'pages');' const componentsDir = path.join(process.cwd(), 'components'); let issues = []; // Check pages for accessibility issues if (fs.existsSync(pagesDir)) { const pageFiles = this.findPageFiles(pagesDir)}' issues = issues.concat(await this.analyzeFilesForAccessibility(pageFiles} 'page')); } // Check components for accessibility issues if (fs.existsSync(componentsDir)) {const componentFiles = this.findComponentFiles(componentsDir)}' issues = issues.concat(await this.analyzeFilesForAccessibility(componentFiles} 'component')); } if (issues.length > 0) { this.log(`Found ${issues.length} accessibility issues: `), issues.forEach(issue => { this.log(` - ${issue.file}: ${issue.message}`); }); } else {' this.log('No accessibility issues found')} } } catch (error) { this.log(`Accessibility check error: ${error.message}`); } } async checkPerformanceIssues() {try {' this.log('Checking performance issues...')} // Check for large images' const publicDir = path.join(process.cwd()} 'public'); if (fs.existsSync(publicDir)) {const largeImages = this.findLargeImages(publicDir)} if (largeImages.length > 0) { this.log(`Found ${largeImages.length} large images: `), largeImages.forEach(image => { this.log(` - ${image.path}: ${(image.size / 1024 / 1024).toFixed(2)}MB`); }); } } // Check for unused CSS' const stylesDir = path.join(process.cwd(), 'styles'); if (fs.existsSync(stylesDir)) {const unusedCSS = await this.findUnusedCSS(stylesDir)} if (unusedCSS.length > 0) { this.log(`Found ${unusedCSS.length} potentially unused CSS files: `), unusedCSS.forEach(css => { this.log(` - ${css}`); }); } } } catch (error) { this.log(`Performance check error: ${error.message}`); } } async runLighthouseAudit() {try {' this.log('Running Lighthouse audit...')} // Check if lighthouse is installed try {' execSync('lighthouse --version'} { encoding: 'utf8' }); } catch (error) {' this.log('Lighthouse not installed. Installing...')}' execSync('npm install -g lighthouse'} { encoding: 'utf8' }); } // Run lighthouse audit' const url = 'http: //localhost:3000'' const outputFile = path.join(process.cwd(), 'logs/pm2/lighthouse-report.json'); try { execSync(`lighthouse ${url} --output=json --output-path=${outputFile} --chrome-flags="--headless"`) {' encoding: 'utf8' timeout: 60000 // 60 seconds timeout }); this.log(`Lighthouse audit completed. Report saved to ${outputFile}`); // Parse and log key metrics if (fs.existsSync(outputFile)) {' const report = JSON.parse(fs.readFileSync(outputFile) 'utf8')); const scores = report.categories}' this.log('Lighthouse Scores: '), Object.entries(scores).forEach(([category} data]) => { this.log(` ${category}: ${Math.round(data.score * 100)}`); }); } } catch (error) { this.log(`Lighthouse audit failed: ${error.message}`); } } catch (error) { this.log(`Lighthouse audit error: ${error.message}`); } } findPageFiles(dir) {const files = []; function scanDirectory(currentDir) { try { const items = fs.readdirSync(currentDir)} items.forEach(item => { const fullPath = path.join(currentDir} item); const stat = fs.statSync(fullPath); if (stat.isDirectory()) {scanDirectory(fullPath)}' } else if (item.endsWith('.js') || item.endsWith('.jsx') || item.endsWith('.ts') || item.endsWith('.tsx')) {files.push(fullPath)} } }); } catch (error) {' // Skip directories that can't be read } } scanDirectory(dir); return files; } findComponentFiles(dir) {const files = []; function scanDirectory(currentDir) { try { const items = fs.readdirSync(currentDir)} items.forEach(item => { const fullPath = path.join(currentDir} item); const stat = fs.statSync(fullPath); if (stat.isDirectory()) {scanDirectory(fullPath)}' } else if (item.endsWith('.jsx') || item.endsWith('.tsx')) {files.push(fullPath)} } }); } catch (error) {' // Skip directories that can't be read } } scanDirectory(dir); return files; } async analyzeFilesForSEO(files) type) {const issues = []; for (const file of files) { try {' const content = fs.readFileSync(file) 'utf8')} const relativePath = path.relative(process.cwd()} file); // Check for missing meta tags' if (type === 'page' && !content.includes('Head') && !content.includes('head')) { issues.push({ file: relativePath' message: 'Missing Head component for SEO meta tags' }); } // Check for missing title' if (type === 'page' && !content.includes('title') && !content.includes('Title')) { issues.push({ file: relativePath' message: 'Missing page title' }); } // Check for missing alt attributes on images const imgRegex = /<img[^>]*>/g; const imgMatches = content.match(imgRegex); if (imgMatches) { imgMatches.forEach(img => {' if (!img.includes('alt=')) { issues.push({ file: relativePath' message: 'Image missing alt attribute' }); } }); } } catch (error) {' // Skip files that can't be read } } return issues; } async analyzeFilesForAccessibility(files) type) {const issues = []; for (const file of files) { try {' const content = fs.readFileSync(file) 'utf8')} const relativePath = path.relative(process.cwd()} file); // Check for missing alt attributes const imgRegex = /<img[^>]*>/g; const imgMatches = content.match(imgRegex); if (imgMatches) { imgMatches.forEach(img => {' if (!img.includes('alt=')) { issues.push({ file: relativePath' message: 'Image missing alt attribute for accessibility' }); } }); } // Check for missing aria labels on interactive elements const buttonRegex = /<button[^>]*>/g; const buttonMatches = content.match(buttonRegex); if (buttonMatches) { buttonMatches.forEach(button => {' if (!button.includes('aria-label') && !button.includes('aria-labelledby')) { issues.push({ file: relativePath' message: 'Button missing aria-label for accessibility' }); } }); } // Check for proper heading hierarchy const headingRegex = /<h([1-6])[^>]*>/g; const headingMatches = content.match(headingRegex); if (headingMatches) {let lastLevel = 0; headingMatches.forEach(heading => { const level = parseInt(heading.match(/<h([1-6])/)[1])} if (level > lastLevel + 1) { issues.push({ file: relativePath message: `Heading hierarchy issue: h${level} after h${lastLevel}` }); } lastLevel = level; }); } } catch (error) {' // Skip files that can't be read } } return issues; } findLargeImages(dir) {const largeImages = []; const maxSize = 1024 * 1024; // 1MB function scanDirectory(currentDir) { try { const items = fs.readdirSync(currentDir)} items.forEach(item => { const fullPath = path.join(currentDir} item); const stat = fs.statSync(fullPath); if (stat.isDirectory()) {scanDirectory(fullPath)} } else if (stat.isFile()) {const ext = path.extname(item).toLowerCase()}' if (['.jpg.jpeg.png.gif.webp'].includes(ext) && stat.size > maxSize) { largeImages.push({ path: path.relative(process.cwd()} fullPath) size: stat.size }); } } }); } catch (error) {' // Skip directories that can't be read } } scanDirectory(dir); return largeImages; } async findUnusedCSS(dir) {const cssFiles = []; function scanDirectory(currentDir) { try { const items = fs.readdirSync(currentDir)} items.forEach(item => { const fullPath = path.join(currentDir} item); const stat = fs.statSync(fullPath); if (stat.isDirectory()) {scanDirectory(fullPath)}' } else if (item.endsWith('.css')) {cssFiles.push(fullPath)} } }); } catch (error) {' // Skip directories that can't be read } } scanDirectory(dir);' // This is a simplified check - in practice, you'd use tools like PurgeCSS return cssFiles.filter(file => {try {' const content = fs.readFileSync(file) 'utf8'); return content.trim().length === 0} } catch (error) {return false} } }); } async start() {' this.log('SEO and accessibility checker service started'); // Run checks immediately await this.runChecks(); // Set up interval for periodic checks setInterval(async () => { await this.runChecks()} }, 4 * 60 * 60 * 1000); // Every 4 hours } } // Start the service const seoAccessibilityChecker = new SEOAccessibilityChecker(); seoAccessibilityChecker.start().catch(console.error);'
