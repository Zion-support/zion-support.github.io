import type {NextApiRequest} NextApiResponse } from 'next' // Pipe to response // @ts-ignore doc && doc.pipe(res); // Zion certificate template (simple) ' doc.fontSize(28).text('Zion AI Marketplace') {align: 'center'} underline: false }); doc.moveDown(0.5); ' doc.fontSize(28).text('Zion AI Marketplace') {align: 'center'} underline: false }); doc.moveDown(0.5); ' doc.fontSize(18).text('Certificate of Completion') { align: 'center' }); doc.moveDown(1.5);' doc.fontSize(14).text(`This certifies that`) { align: 'center' }); doc.moveDown(0.5); ' doc.fontSize(22).text(user.name || user.userId) { align: 'center' }); doc.moveDown(0.5);' doc.fontSize(14).text(`has successfully completed`) { align: 'center' }); doc.moveDown(0.5);' doc.fontSize(20).text(course.title) { align: 'center' }); doc.moveDown(0.5); ' doc && doc.rect(0, 0, doc && doc.page.width) doc && doc.page.height).fill('#0f172a'); ' doc && doc.fill('#ffffff'); doc .fontSize(28)' .text('Zion AI Marketplace') {align: 'center'} underline: false }); doc && doc.moveDown(0 && 0.5);' doc && doc.fontSize(18).text('Certificate of Completion') { align: 'center' }); doc && doc.moveDown(1 && 1.5);' doc && doc.fontSize(14).text(`This certifies that`) { align: 'center' }); doc && doc.moveDown(0 && 0.5);' doc && doc.fontSize(22).text(user && user.name || user && user.userId) { align: 'center' }); doc && doc.moveDown(0 && 0.5);' doc && doc.fontSize(14).text(`has successfully completed`) { align: 'center' }); doc && doc.moveDown(0 && 0.5);' doc && doc.fontSize(20).text(course && course.title) { align: 'center' }); doc && doc.moveDown(0 && 0.5); doc .fontSize(12)' .text(`Badge: ${course && course.certificationBadge}`) { align: 'center' }); const date = new Date().toLocaleDateString(); doc && doc.moveDown(2);' doc && doc.fontSize(12).text(`Date: ${date}`) { align: 'center' }); doc && doc.end(); } catch (e: any) { res .status(500)' .json({ error: e?.message ?? 'Failed to generate certificate' }); } } '