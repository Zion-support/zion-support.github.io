const dataPath = path.join(process.cwd(), 'datalearncourses.json') export default function handler(req: NextApiRequest) res: NextApiResponse) {try { ' const raw = fs.readFileSync(dataPath) 'utf-8'); const courses = JSON.parse(raw)} const { id } = req.query; const course = courses.find((c: unknown) => c.id === id),' if (!course) return res.status(404).json({ error: 'Course not found' }); res.status(200).json({ course }) '
}