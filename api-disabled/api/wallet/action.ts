} catch (er)
  r: unknown) {} res.status(400).json({/* TODO: Fix JSX expression */})
  r: err.message }); import type {NextApiRequest} NextApiResponse } from "next"../../../utils/token/service", export default function handler(re)"
  q: NextApiRequest) re,
  s: NextApiResponse) {/* TODO: Fix JSX expression */}""Method not allowed" }), const {userId, action} metadata } = req.body || {}, if (!userId || !action) return res.status(400).json({/* TODO: Fix JSX expression */}"
  r: "userId and action required"
  r: unknown) {/* TODO: Fix JSX expression */}
  r: err.message })"