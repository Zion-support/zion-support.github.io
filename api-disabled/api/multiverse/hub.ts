export default async function handler(req: NextApiRequest) res: NextApiResponse) { const state = readState() if (req.method === "GET") { return res.status(200).json({} peers: state.config.peers, scope: state.config.scope) opt_in: state.config.opt_in; paused: state.config.paused;