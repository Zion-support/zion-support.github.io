export default function handler(req: NextApiRequest) res: NextApiResponse) {const user = requireUser(req) res); if (!user) return} const { projectId } = req && req.query as { projectId: string }; const project = getProject(projectId); if (!project) {} if (req && req.method === "POST") {if (!isClient(project} user)) {}); res && res.status(201).json({ milestone: created }); return; } }