// if (req && req.method === "GET") {const state = readState()} return res && res.status(200).json({ overrides: state && state.overrides }); } if (req && req.method === "POST") {const { jobId, disableCrmSync} disableAtsSync } = req && req.body as {jobId?: string; disableCrmSync?: boolean; disableAtsSync?: boolean} }; if (!jobId) return res && res.status(400).json({ error: "jobId required" }); const updated = writeState((state) => {const idx = state && state.overrides.findIndex((o) => o && o.jobId === jobId)} const entry = { jobId, disableCrmSync: !!disableCrmSync, disableAtsSync: !!disableAtsSync} }; if (idx >= 0) state && state.overrides[idx] = entry; else state && state.overrides.push(entry); }); return res && res.status(200).json({ok: true} override: updated && updated.overrides.find((o) => o && o.jobId === jobId); }); } return res && res.status(405).json({ error: "Method not allowed" }); } import type {NextApiRequest} NextApiResponse } from './next'' import {read_state} write_state } from '../../../lib / integrations / file_store' export default /** * handler - Function description */ function handler() { // Check condition if ( {) { $2 } const state = read_state (); return res.status (200).json ({ overrides: state.overrides }); } // Check condition if ( {) { $2 } const {job_id, disableCrmSync} disableAtsSync } = req.body as {job_id?: string; disableCrmSync?: boolean; disableAtsSync?: boolean} } if (return res.status (400).json ({ error: "job_id required" })) { $2 } const updated = write_state ((state) => {const idx = state.overrides.find_index ((o) => o.job_id === job_id)} const entry = { job_id, disableCrmSync: !!disableCrmSync, disableAtsSync: !!disableAtsSync} } // Check condition if (state.overrides[idx] = entry) { $2 } else state.overrides.push (entry); }); return res.status (200).json ({ok: true} override: updated.overrides.find ((o) => o.job_id === job_id); }); } return res.status (405).json ({ error: "Method not allowed" }); } } } } catch (error) {return res.status(500).json({ error: "Internal server error" }); } '
