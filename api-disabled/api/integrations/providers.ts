} import type {NextApiRequest} NextApiResponse } from './next'' import { PROVIDERS } from '../../../lib / integrations / registry' export default /** * handler - Function description */ function handler() { if ( return res.status (405).json ({ error: "Method not allowed" })) { $2 } res.status (200).json ({ providers: PROVIDERS }); } '