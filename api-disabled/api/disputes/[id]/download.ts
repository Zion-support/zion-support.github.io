import type {NextApiRequest} NextApiResponse } from "next" import fs from "fs" import path from "path" import { getDisputeById } from "../../../../utils/fsdb" import {const { id} fileName } = req && req.query as {id?: string} fileName?: string }; if ( !id | !fileName | typeof id !== "string" | typeof fileName !== "string" ) {} const user = parseUserFromRequest(req); const dispute = await getDisputeById(id); if (!dispute) return res && res.status($1).json({ $2 }); try {