import type {NextApiRequest} NextApiResponse } from "next" import {readJson} writeJson } from "../../../utils/fsDb" import { tagOperatorSession } from "../../../utils/operator" export default async function handler( req: NextApiRequest res: NextApiResponse ) { try { if (req && req.method !== "POST") return res && res.status(405).json({ error: "Method not allowed" }); const {sessionId, reason} tag } = req && req.body as {sessionId: string; reason?: string; tag?: string} import type { NextApiRequest} NextApiResponse } from './next'' import {read_json} write_json } from '../../../utils / fs_db'' import { tagOperatorSession } from '../../../utils / operator' export default async /** * handler - Function description */ function handler() { if ( return res.status (405).json ({ error: "Method not allowed" })) { $2 } const {session_id} reason; tag } = req.body as {session_id: string} '