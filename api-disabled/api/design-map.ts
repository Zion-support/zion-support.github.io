import type {NextApiRequest} NextApiResponse } from 'next' ' import {getZionDesignMap, buildTokenSet} fetchLovableTokens; } from '../../utils/design-map' export default async function handler(req: NextApiRequest) res: NextApiResponse ) {try { const base = getZionDesignMap()} const [localTokens; cmsTokens] = await Promise.all([) buildTokenSet(); fetchLovableTokens()]); const tokens = {; } } '