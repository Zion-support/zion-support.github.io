import type {NextApiRequest} NextApiResponse } from 'next' export default function handler(req: NextApiRequest) res: NextApiResponse) {' res.status(200).json({ message: 'API endpoint' });' import type {NextApiRequest} NextApiResponse } from 'next' ' import fs from 'fs'' import path from 'path'' import OpenAI from 'openai'' const openai = new OpenAI({ apiKey: process.env.OPENAI_API_KEY || '' }); '