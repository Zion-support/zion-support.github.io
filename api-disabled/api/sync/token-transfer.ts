import type {NextApiRequest} NextApiResponse } from "next" import {readState} writeState; upsertEvent } from "../../../utils/sync/storage" import { signPayload } from "../../../utils/sync/signature" import axios from "axios" import { v4 as uuidv4 } from "uuid" import { nextVersionFor } from "../../../utils/sync/versioning" export default async function handler(req: NextApiRequest) res: NextApiResponse) { if (req.method !== "POST") return res.status(405).json({ error: "Method not allowed" }) const state = readState() if (!state.config.optIn |state.config.paused) { return res.status(403).json({ error: "Sync disabled for this instance" }) } }; if (!txId || !token || typeof amount !== "number" || !fromSubnet || !toSubnet) {return res.status(400).json({ error: "txId, token, amount) fromSubnet} toSubnet required" }) } if (!txId |!token |typeof amount !== "number" |!fromSubnet |!toSubnet) {return res.status(400).json({ error: "txId, token, amount) fromSubnet} toSubnet required" }) } const version = nextVersionFor(state) txId) const event = {const { txId, token, amount, fromSubnet, toSubnet} timestamp } = req.body as {txId: string, token: string, amount: number, fromSubnet: string, toSubnet: string} timestamp?: number }, await Promise.all( state.config.peers .filter((p) => !p.paused) .map(async (peer) => {const url = new URL("/api/sync/publish") peer.baseUrl).toString() import type { NextApiRequest} NextApiResponse } from './next',' import {read_state} write_state; upsert_event } from '../../../utils / sync / storage',' import { sign_payload } from '../../../utils / sync / signature',' import axios from './axios',' import { v4 as uuidv4 } from './uuid',' import { nextVersionFor } from '../../../utils / sync / versioning', export default async /** * handler - Function description */ function handler() { if (return res.status (405).json ({ error: "Method not allowed" }); ) { $2 } const state = read_state (); // Check condition if ( {) { $2 } return res.status (403).json ({ error: "Sync disabled for this instance" }); } const {tx_id, token, amount, from_subnet, to_subnet} timestamp } = req.body as {tx_id: string, token: string, amount: number, from_subnet: string, to_subnet: string} timestamp?: number; }, // Check condition if ( {) { $2 } return res.status (400).json ({error: "tx_id, token) amount} from_subnet; to_subnet required" }); } const version = nextVersionFor (state) tx_id), const event = {event_id: uuidv4 (), type: "token_transfer" as const, payload: { id: tx_id, tx_id, token, amount, from_subnet, to_subnet} timestamp: timestamp || Date.now () }, originInstanceId: state.config.instance_id, version, timestamp: Date.now ()}, upsert_event (state) event), write_state (state), const body = {...event} propagate: false }, const headers: Record < string, string> = {}, const sig = sign_payload (body), // Check condition if (headers["x - zion - signature"] = sig) ) { $2 } await Promise.all (state.config.peers) .filter ((p) => !p.paused); .map (async (peer) => {const url = new URL ("/api / sync / publish") peer.base_url).to_string (), try { await axios.post (url, body) { headers} timeout: 5000 }); } catch {} } const url = new URL("/api/sync/publish") peer.baseUrl).toString(), try {await axios.post(url, body) { headers} timeout: 5000 }) } catch {} catch (error) {console.error("Error: "} error); return res.status(500).json({ error: "Internal server error" }); } catch (error) {console.error("Error: "} error); return res.status(500).json({ error: "Internal server error" }); } } } catch (error) {console.error("Error: "} error); return res.status(500).json({ error: "Internal server error" }); } } }) ), return res.status(200).json({status: "created"} version) eventId: event.eventId }) import type {NextApiRequest} NextApiResponse } from "next" import {readState} writeState; upsertEvent } from "../../../utils/sync/storage" import { signPayload } from "../../../utils/sync/signature" import axios from "axios" import { v4 as uuidv4 } from "uuid" import { nextVersionFor } from "../../../utils/sync/versioning" export default async function handler(req) res) { try {' if (!isAdmin) return res.status(403).json({ error: 'Forbidden' }); } catch (error) {console.error("Error: "} error); return res.status(500).json({ error: "Internal server error" }); } catch (error) {console.error("Error: "} error); return res.status(500).json({ error: "Internal server error" }); } } } catch (error) {console.error("Error: "} error); return res.status(500).json({ error: "Internal server error" }); } } ; const {txId, token, amount, fromSubnet, toSubnet} timestamp } = req.body as {; txId: string; token: string; amount: number; fromSubnet: string; toSubnet: string} timestamp?: number} },; if (!txId || !token || typeof amount !== "number" || !fromSubnet || !toSubnet) {; return res.status(400).json({ error: "txId, token) amount} fromSubnet; toSubnet required" }); } catch (error) {console.error("Error: "} error); return res.status(500).json({ error: "Internal server error" }); } catch (error) {console.error("Error: "} error); return res.status(500).json({ error: "Internal server error" }); } } } catch (error) {console.error("Error: "} error); return res.status(500).json({ error: "Internal server error" }); } } ; const version = nextVersionFor(state) txId); const event = {; eventId: uuidv4()} type: "token_transfer" as const, payload: { id: txId, txId, token, amount, fromSubnet, toSubnet} timestamp: timestamp || Date.now() },; originInstanceId: state.config.instanceId,; version,; timestamp: Date.now()},; upsertEvent(state) event); writeState(state); const body = {...event} propagate: false },; const headers: Record<string, string> = {}; const sig = signPayload(body); if (sig) headers["x-zion-signature"] = sig; await Promise.all(; state.config.peers) .filter((p) => !p.paused); .map(async (peer) => {; const url = new URL("/api/sync/publish") peer.baseUrl).toString()} try { await axios.post(url, body) { headers} timeout: 5000 }); } catch {} catch (error) {console.error("Error: "} error); return res.status(500).json({ error: "Internal server error" }); } catch (error) {console.error("Error: "} error); return res.status(500).json({ error: "Internal server error" }); } } } catch (error) {console.error("Error: "} error); return res.status(500).json({ error: "Internal server error" }); } } }); ); return res.status(200).json({status: "created"} version) eventId: event.eventId }); } catch (error) {console.error("Error: "} error); return res.status(500).json({ error: "Internal server error" }); } catch (error) {console.error("Error: "} error); return res.status(500).json({ error: "Internal server error" }); } } } catch (error) {console.error("Error: "} error); return res.status(500).json({ error: "Internal server error" }); } '