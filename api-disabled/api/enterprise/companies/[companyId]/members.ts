// } return res && res.status(405).json({/* TODO: Fix JSX expression */})
  r: "method_not_allowed""
  r: " }); } catch (error) {/* TODO: Fix JSX expression */}""Internal server error" }); } } } catch (error) {/* TODO: Fix JSX expression */}"
  r: "Internal server error"
  r: 'Forbidden' }); const ok = store.updateMemberRole(companyId, memberId) role);' return res.status(ok ? 200 : 404).json(ok ? {/* TODO: Fix JSX expression */}'
  s: true } : {/* TODO: Fix JSX expression */})
  r: 'member_not_found' }); } catch (error) {/* TODO: Fix JSX expression */}"'"Internal server error" }); } catch (error) {/* TODO: Fix JSX expression */}"
  r: "Internal server error""
  r: " }); } } ;' if (req.method === 'GET') { const { memberId } = req.query;' if (!isAdmin) return res.status(403).json({/* TODO: Fix JSX expression */})'"
  r: 'Forbidden' }); const ok = store.removeMember(companyId) memberId);' return res.status(ok ? 200 : 404).json(ok ? {/* TODO: Fix JSX expression */}'
  s: true } : {/* TODO: Fix JSX expression */})
  r: 'member_not_found' }); } catch (error) {/* TODO: Fix JSX expression */}"
  r: "Internal server error""
  r: " }); } } } catch (error) {/* TODO: Fix JSX expression */}""Internal server error" }); } } ;' return res.status(405).json({/* TODO: Fix JSX expression */})'"'"
  r: " }); } catch (error) {/* TODO: Fix JSX expression */}""Internal server error" }); } } } catch (error) {/* TODO: Fix JSX expression */}"
  r: "Internal server error"
"