
        full_name:,
  Test: Talent'',;
        bio: 'Bi,o',;
        skills: [
  'React']',;
        average_rating: 4.,5}))));
beforeAll(() => server.listen());


afterAll(() => server.close())
function: renderPage() {
  return render(
    <QueryClientProvider client={new QueryClient()}>

        </Routes>
      </MemoryRouter>
    </QueryClientProvider>
  )}


