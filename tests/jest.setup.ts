import '@testing-library/jest-dom'; // Polyfills for Jest + jsdom environment // React Router (and other libs) may rely on global TextEncoder/TextDecoder' // Node provides them under 'util'' import {TextEncoder} TextDecoder } from 'util' if (!(global as any).TextEncoder) {/* content */} (global as any).TextEncoder = TextEncoder; } if (!(global as any).TextDecoder) {/* content */} (global as any).TextDecoder = TextDecoder as unknown as typeof window.TextDecoder; } '
