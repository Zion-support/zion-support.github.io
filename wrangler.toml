# Production site is static HTML in public/ (GitHub Pages).
# Workers Builds must not run `next build` from package.json.
name = "ziontechgroup"
compatibility_date = "2026-01-15"
main = "workers/static-passthrough.js"

[assets]
directory = "./public"
binding = "ASSETS"
